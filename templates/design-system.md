---
Generated: "{date}"
Skill: /pde:system (SYS)
Version: v{N}
Status: draft
Preset: "{preset_name|custom}"
Enhanced By: "{MCP list or none}"
---

# Design System

---

## Token Overview

| Category | File | Token Count | Notes |
|----------|------|-------------|-------|
| Color | SYS-colors.css | {N} | {light + dark} |
| Typography | SYS-typography.css | {N} | {scale ratio} |
| Spacing | SYS-spacing.css | {N} | {base unit} |
| Shadows | SYS-shadows.css | {N} | |
| Borders | SYS-borders.css | {N} | |
| Motion | SYS-motion.css | {N} | |
| Components | SYS-components.css | {N} | {component count} |

## Brand Configuration

| Property | Value |
|----------|-------|
| Primary Color | {oklch value} |
| Secondary Color | {oklch value} |
| Display Font | {font name} |
| Body Font | {font name} |
| Type Scale Ratio | {ratio name} ({value}) |
| Spacing Base | {value} |

## Usage Guide

### Consuming Tokens

{Framework-specific import instructions}

```html
<!-- HTML: link unified token file -->
<link rel="stylesheet" href="../../assets/tokens.css">

<!-- Or import individual category files -->
<link rel="stylesheet" href="../visual/SYS-colors.css">
<link rel="stylesheet" href="../visual/SYS-typography.css">
```

```css
/* CSS: use tokens via custom properties */
.my-component {
  background: var(--color-bg-surface);
  color: var(--color-text-primary);
  padding: var(--space-4);
  font-size: var(--font-size-base);
  border-radius: var(--radius-md);
}
```

### Color Usage

{Semantic color usage examples with code}

```css
/* Semantic colors — use these, not primitives */
.action    { color: var(--color-action); }
.success   { color: var(--color-success); }
.warning   { color: var(--color-warning); }
.error     { color: var(--color-error); }
.muted     { color: var(--color-text-secondary); }

/* Backgrounds */
.surface   { background: var(--color-bg-surface); }
.elevated  { background: var(--color-bg-elevated); }
```

### Typography Usage

{Type scale usage with heading/body examples}

```css
/* Heading scale */
h1 { font-size: var(--font-size-4xl); line-height: var(--line-height-tight); }
h2 { font-size: var(--font-size-3xl); line-height: var(--line-height-tight); }
h3 { font-size: var(--font-size-2xl); line-height: var(--line-height-snug); }
h4 { font-size: var(--font-size-xl);  line-height: var(--line-height-snug); }

/* Body text */
p  { font-size: var(--font-size-base); line-height: var(--line-height-normal); }
small { font-size: var(--font-size-sm); }
```

### Component Usage

{Key component examples with HTML + class usage}

```html
<!-- Button variants -->
<button class="pde-btn pde-btn--primary">Primary Action</button>
<button class="pde-btn pde-btn--secondary">Secondary</button>
<button class="pde-btn pde-btn--primary pde-btn--sm">Small</button>
<button class="pde-btn pde-btn--primary pde-btn--lg">Large</button>

<!-- Card -->
<div class="pde-card">
  <div class="pde-card__header">{title}</div>
  <div class="pde-card__body">{content}</div>
  <div class="pde-card__footer">{actions}</div>
</div>

<!-- Input field -->
<div class="pde-field">
  <label class="pde-label" for="email">Email</label>
  <input class="pde-input" type="email" id="email" placeholder="you@example.com">
  <span class="pde-error">Please enter a valid email</span>
</div>
```

## Files Generated

| File | Path | Description |
|------|------|-------------|
| Unified Tokens (CSS) | assets/tokens.css | All tokens as CSS custom properties |
| Unified Tokens (JSON) | visual/SYS-tokens.json | DTCG canonical source |
| Color Tokens | visual/SYS-colors.css | Color primitives + semantics |
| Typography Tokens | visual/SYS-typography.css | Type scale + font stacks |
| Spacing Tokens | visual/SYS-spacing.css | Spacing scale |
| Shadow Tokens | visual/SYS-shadows.css | Elevation shadows |
| Border Tokens | visual/SYS-borders.css | Radii + border styles |
| Motion Tokens | visual/SYS-motion.css | Duration + easing |
| Component Styles | visual/SYS-components.css | .pde-* component classes |
| Utility Classes | visual/SYS-utilities.css | Layout + spacing utilities |
| Preview Page | visual/SYS-preview.html | Visual preview with light/dark toggle |
| Usage Guide | visual/SYS-usage-guide.md | This document |
| Component Catalog | visual/components/ | Individual component reference pages |

---

*Generated by PDE-OS /pde:system | {date}*

{If Figma MCP was used: "[Enhanced by Figma MCP -- tokens imported from Figma file]"}
{If Figma MCP was unavailable: "[Generated from PDE algorithms -- install Figma MCP to import from your Figma file]"}
