---
phase: 174-cli-wrap-skill
plan: 02
type: execute
wave: 2
depends_on: [174-01]
files_modified:
  - commands/cli-wrap.md
  - tests/phase-174/cli-wrap-integration.test.mjs
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03]

must_haves:
  truths:
    - "/pde:cli-wrap slash command is available in Claude Code"
    - "/pde:cli-wrap <slug> invokes pde-tools app cli-wrap <slug> and produces all artifacts"
    - "The integration test verifies the full pipeline: slug in, artifacts out"
  artifacts:
    - path: "commands/cli-wrap.md"
      provides: "/pde:cli-wrap slash command definition"
      contains: "pde:cli-wrap"
    - path: "tests/phase-174/cli-wrap-integration.test.mjs"
      provides: "End-to-end integration test for the cli-wrap pipeline"
  key_links:
    - from: "commands/cli-wrap.md"
      to: "bin/pde-tools.cjs"
      via: "node bin/pde-tools.cjs app cli-wrap"
      pattern: "pde-tools.cjs app cli-wrap"
    - from: "tests/phase-174/cli-wrap-integration.test.mjs"
      to: "bin/lib/app-cli-wrap.cjs"
      via: "import { cmdCliWrap } from app-cli-wrap"
      pattern: "cmdCliWrap"
---

<objective>
Create the `/pde:cli-wrap` slash command and an integration test that verifies the full pipeline end-to-end (mocked filesystem/subprocesses).

Purpose: The slash command is the user-facing entry point — the "one command" from CLI-01. The integration test validates that the complete pipeline (approval gate -> harness detection -> strategy routing -> artifact generation -> bridge registration) works as a unit.
Output: `commands/cli-wrap.md`, `tests/phase-174/cli-wrap-integration.test.mjs`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/174-cli-wrap-skill/174-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From commands/wrap.md (pattern reference):
```markdown
---
name: pde:wrap
description: Auto-wrap any CLI binary as an agent-native MCP server with recursive subcommand discovery
argument-hint: "<binary-path> (binary name on PATH or absolute path)"
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Glob
  - Grep
---
```

From bin/lib/app-cli-wrap.cjs (created in Plan 01):
```javascript
module.exports = {
  detectHarness,      // (slug, pipxBinDir) => { found: boolean, binaryPath: string|null }
  wrapViaHarness,     // (cwd, slug, harnessBinaryPath, registryEntry) => { strategy, outDir, serverDir, capabilities }
  wrapViaNativeHelp,  // (cwd, slug, registryEntry) => { strategy, outDir, serverDir, capabilities, parseQuality }
  cmdCliWrap,         // (cwd, args) => void (main orchestrator)
  resolvePipxBinDir,  // (_execFn?) => string|null
  storePipxBinDir,    // (cwd, pipxBinDir) => void
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /pde:cli-wrap slash command</name>
  <files>commands/cli-wrap.md</files>
  <read_first>
    - commands/wrap.md (exact format reference — frontmatter fields, section structure, process block)
  </read_first>
  <action>
    Create `commands/cli-wrap.md` following the exact format of `commands/wrap.md`. The file must contain:

    Frontmatter:
    ```yaml
    ---
    name: pde:cli-wrap
    description: Wrap any approved installed app as an agent-native CLI with dual-strategy routing
    argument-hint: "<app-slug> (must be in app-registry with approved status)"
    allowed-tools:
      - Read
      - Write
      - Edit
      - Bash
      - Glob
      - Grep
    ---
    ```

    Body sections (follow wrap.md structure exactly):

    1. `<objective>` block: "Execute the /pde:cli-wrap command. Wrap an approved app as an agent-native CLI with automatic strategy selection."

    2. `# /pde:cli-wrap` heading

    3. Description: "One-command app wrapping: approval check -> strategy routing -> capability model -> MCP server -> SKILL.md -> bridge registration."

    4. `## Usage` section: `/pde:cli-wrap <slug>`

    5. `## What It Does` section with numbered steps:
       1. Reads `app-registry.json` to confirm the app has `approved` status
       2. Checks if a CLI-Anything pre-built harness (`cli-anything-<slug>`) is available via pipx
       3. If harness found: uses it as the capability source (FAST PATH — skips native --help parsing)
       4. If no harness: falls back to native `--help` -> capability model -> codegen (FALLBACK PATH)
       5. Writes all artifacts to `.planning/app-wrappers/{slug}/`
       6. Registers the server with `mcp-bridge.cjs` for immediate tool use

    6. `## Output` section listing:
       - `.planning/app-wrappers/{slug}/capability-model.json` -- Unified capability model
       - `.planning/app-wrappers/{slug}/server/server.cjs` -- MCP server (stdio transport)
       - `.planning/app-wrappers/{slug}/server/SKILL.md` -- Agent skill documentation

    7. `## Prerequisites` section:
       - App must be discovered and approved: `pde-tools app discover <name>` then `pde-tools app approve <name>`
       - For fast path: pipx configured via `pde-tools app pipx-setup` (run once)

    8. `## Examples` section:
       ```
       /pde:cli-wrap blender
       /pde:cli-wrap gimp
       /pde:cli-wrap inkscape
       ```

    9. `## Implementation` section with code block: `node bin/pde-tools.cjs app cli-wrap "$1"`

    10. `<process>` block:
        ```
        Run:
        ```
        node bin/pde-tools.cjs app cli-wrap $ARGUMENTS
        ```
        On success: report the strategy used (harness or fallback), the number of capabilities discovered, and the output file paths.
        On failure: diagnose the error. Common issues:
        - "not approved" — run `pde-tools app approve <slug>` first
        - "not in registry" — run `pde-tools app discover <slug>` first
        - pipx not found — run `pde-tools app pipx-setup` or install pipx
        ```
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep -c "pde:cli-wrap" commands/cli-wrap.md</automated>
  </verify>
  <acceptance_criteria>
    - File commands/cli-wrap.md exists
    - grep -q "name: pde:cli-wrap" commands/cli-wrap.md returns 0
    - grep -q "argument-hint" commands/cli-wrap.md returns 0
    - grep -q "allowed-tools" commands/cli-wrap.md returns 0
    - grep -q "app-wrappers" commands/cli-wrap.md returns 0 (correct output directory)
    - grep -q "pde-tools.cjs app cli-wrap" commands/cli-wrap.md returns 0
    - grep -q "pipx-setup" commands/cli-wrap.md returns 0 (prerequisite documented)
    - grep -q "FAST PATH" commands/cli-wrap.md returns 0 (dual strategy documented)
    - grep -q "FALLBACK" commands/cli-wrap.md returns 0
    - grep -q "<process>" commands/cli-wrap.md returns 0 (process block present)
  </acceptance_criteria>
  <done>
    `/pde:cli-wrap` slash command file exists with correct frontmatter, usage docs, prerequisite notes, dual-strategy explanation, and process block that invokes `pde-tools app cli-wrap`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create integration test for full cli-wrap pipeline</name>
  <files>tests/phase-174/cli-wrap-integration.test.mjs</files>
  <read_first>
    - bin/lib/app-cli-wrap.cjs (the module created in Plan 01 — read full file to understand the orchestration flow)
    - tests/phase-173/pde-tools-app-register.test.mjs (integration test pattern reference — how mocking is done for pde-tools app subcommands)
    - tests/phase-174/app-cli-wrap.test.mjs (Plan 01 unit tests — avoid duplicating, focus on integration)
  </read_first>
  <behavior>
    - Integration test: given a mocked approved registry entry and mocked harness binary, cmdCliWrap produces capability-model.json + server.cjs + SKILL.md in the correct output directory
    - Integration test: given a mocked approved registry entry and NO harness, cmdCliWrap falls back to native path and still produces all artifacts
    - Integration test: cmdCliWrap throws/exits when slug is not approved in registry
    - Integration test: fast path result includes strategy: 'harness', fallback includes strategy: 'fallback' and parseQuality
    - Integration test: registerDynamicServer is called with correct slug, server path, and capabilities array
  </behavior>
  <action>
    Create `tests/phase-174/cli-wrap-integration.test.mjs` using vitest. This tests the full cmdCliWrap orchestration pipeline with dependency injection mocks (not function-level unit tests — those are in Plan 01).

    Structure:
    - Import `vi, describe, it, expect, beforeEach, afterEach` from vitest
    - Use `vi.mock()` to mock:
      - `child_process` (spawnSync for `which` calls)
      - `fs` (existsSync, mkdirSync, writeFileSync, readFileSync for config + artifacts)
      - The lazy-required modules inside app-cli-wrap.cjs:
        - `../../bin/lib/cli-anything/help-parser.cjs` — mock `discoverCapabilities` to return `[{ name: 'render', flags: ['--output'] }, { name: 'export', flags: ['--format'] }, { name: 'convert', flags: [] }]`
        - `../../bin/lib/cli-anything/model.cjs` — mock `validateCapabilityModel` to return the input (pass-through)
        - `../../bin/lib/cli-anything/server-gen.cjs` — mock `writeServer` as no-op
        - `../../bin/lib/cli-anything/skill-gen.cjs` — mock `writeSkillMd` as no-op
        - `../../bin/lib/app-registry.cjs` — mock `checkApproved` to return `{ slug: 'testapp', status: 'approved', binaryPath: '/usr/bin/testapp', version: '3.0' }`
        - `../../bin/lib/mcp-bridge.cjs` — mock `registerDynamicServer` as spy
        - `../../bin/lib/core.cjs` — mock `safeReadFile` to return JSON config string

    Test cases:
    1. "fast path: harness available via pipx_bin_dir" — set up config with pipx_bin_dir, mock fs.existsSync for harness binary, verify wrapViaHarness path executed, verify registerDynamicServer called with slug and capabilities
    2. "fallback path: no harness available" — no harness anywhere, verify native --help path executed, verify parseQuality in written model file
    3. "security: rejects unapproved apps" — mock checkApproved to throw, verify cmdCliWrap propagates the error (no harness detection attempted)
    4. "output directory: uses app-wrappers not cli-anything" — verify writeFileSync calls use `.planning/app-wrappers/` path, not `.planning/cli-anything/`
    5. "bridge registration: passes correct capabilities" — verify registerDynamicServer receives the capabilities array from the model
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && npx vitest run tests/phase-174/cli-wrap-integration.test.mjs --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File tests/phase-174/cli-wrap-integration.test.mjs exists
    - grep -q "cmdCliWrap" tests/phase-174/cli-wrap-integration.test.mjs returns 0
    - grep -q "FAST PATH\|fast path\|harness" tests/phase-174/cli-wrap-integration.test.mjs returns 0
    - grep -q "FALLBACK\|fallback\|native" tests/phase-174/cli-wrap-integration.test.mjs returns 0
    - grep -q "registerDynamicServer" tests/phase-174/cli-wrap-integration.test.mjs returns 0
    - grep -q "checkApproved" tests/phase-174/cli-wrap-integration.test.mjs returns 0
    - grep -q "app-wrappers" tests/phase-174/cli-wrap-integration.test.mjs returns 0
    - All vitest tests pass (0 failures)
  </acceptance_criteria>
  <done>
    Integration test verifies the complete cli-wrap pipeline for both fast and fallback paths, confirms security gate ordering, correct output directory, and bridge registration. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. All phase-174 tests pass: `npx vitest run tests/phase-174/ --reporter=verbose`
2. `/pde:cli-wrap` command file exists: `test -f commands/cli-wrap.md`
3. Command file has correct frontmatter: `grep "name: pde:cli-wrap" commands/cli-wrap.md`
4. Integration tests cover both strategy paths: `grep -c "fast path\|fallback path\|harness\|native" tests/phase-174/cli-wrap-integration.test.mjs`
</verification>

<success_criteria>
- commands/cli-wrap.md exists with correct frontmatter, dual-strategy documentation, prerequisites, and process block
- Integration test validates full pipeline for fast path, fallback path, security gate, output directory, and bridge registration
- All phase-174 tests pass (unit + integration)
- Phase 174 is complete: any approved app can be wrapped in one command via /pde:cli-wrap
</success_criteria>

<output>
After completion, create `.planning/phases/174-cli-wrap-skill/174-02-SUMMARY.md`
</output>
