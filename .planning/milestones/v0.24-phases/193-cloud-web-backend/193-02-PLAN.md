---
phase: 193-cloud-web-backend
plan: 02
type: execute
wave: 2
depends_on: ["193-01"]
files_modified:
  - packages/dispatcher/lib/remote-router.cjs
  - packages/dispatcher/lib/coordinator.cjs
  - tests/dispatcher/coordinator-cloud.test.cjs
autonomous: true
requirements: [CLD-01, CLD-02, CLD-07, CLD-08]

must_haves:
  truths:
    - "dispatch() with preferred_backend cloud routes to _runCloudSession"
    - "dispatch() with backend cloud skips relay spawn"
    - "dispatch() emits routing_fallback when cloud probe fails and falls to ssh or local"
    - "routeSession returns cloud when preferred_backend is cloud and probe available"
    - "routeSession falls through to ssh when cloud probe unavailable"
    - "Cloud sessions use RemoteAggregator via aggregator.watch(relayId, 'cloud')"
    - "All 11 test cases (CC-01 through CC-11) pass"
  artifacts:
    - path: "packages/dispatcher/lib/remote-router.cjs"
      provides: "Cloud routing rule with fallback"
      contains: "cloud"
    - path: "packages/dispatcher/lib/coordinator.cjs"
      provides: "_runCloudSession method, cloud relay guard, routing_fallback emission"
      contains: "_runCloudSession"
    - path: "tests/dispatcher/coordinator-cloud.test.cjs"
      provides: "Full test coverage for CLD-01, CLD-02, CLD-07, CLD-08"
      min_lines: 150
  key_links:
    - from: "packages/dispatcher/lib/coordinator.cjs"
      to: "packages/dispatcher/lib/remote-cloud.cjs"
      via: "require + _deps.spawnCloudSession"
      pattern: "spawnCloudSession"
    - from: "packages/dispatcher/lib/remote-router.cjs"
      to: "packages/dispatcher/lib/remote-managed.cjs"
      via: "detectManagedBackend probe"
      pattern: "detectManaged"
    - from: "packages/dispatcher/lib/coordinator.cjs"
      to: "aggregator.watch(relayId, 'cloud')"
      via: "sessionType parameter"
      pattern: "watch.*cloud"
---

<objective>
Integrate cloud dispatch into the router and coordinator: add cloud routing rules with fallback chain, add _runCloudSession to coordinator, extend relay spawn guard, emit routing_fallback events, and create the full coordinator-cloud.test.cjs test suite covering all four requirements.

Purpose: This wires Plan 01's standalone modules into the dispatch pipeline and validates all CLD requirements via comprehensive CLI-stubbed tests.
Output: Two modified production files (remote-router.cjs, coordinator.cjs) + one new test file (coordinator-cloud.test.cjs)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/193-cloud-web-backend/193-CONTEXT.md
@.planning/phases/193-cloud-web-backend/193-RESEARCH.md
@.planning/phases/193-cloud-web-backend/193-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From packages/dispatcher/lib/remote-cloud.cjs (created in Plan 01):
```javascript
class CloudPoller {
  constructor(taskId, onLine, opts) { /* opts._execCommand, opts.pollInterval */ }
  start() {}
  stop() {}
}
function spawnCloudSession(opts) {
  // opts: { sessionId, phase, plan, cloudConfig, onLine, onExit, _deps }
  // _deps: { execCommand }
  // Returns: { kill: Function }
}
module.exports = { CloudPoller, spawnCloudSession };
```

From packages/dispatcher/lib/remote-managed.cjs (modified in Plan 01):
```javascript
async function detectManagedBackend(_deps) {
  // _deps: { execCommand }
  // Returns: { available: boolean, reason?: string }
}
module.exports = { detectManagedBackend };
```

From packages/dispatcher/lib/coordinator.cjs (current state):
```javascript
// Line 287: relay spawn guard
if (backend !== 'ssh' && backend !== 'docker') {
  const relayHandle = this._spawnRelay(relayId);
  ...
}

// Line 293-299: backend dispatch branching
if (backend === 'ssh') {
  this._queue.add(() => this._runRemoteSession(...));
} else if (backend === 'docker') {
  this._queue.add(() => this._runDockerSession(...));
} else {
  this._queue.add(() => this._runSession(...));
}
```

From packages/dispatcher/lib/remote-router.cjs (current state):
```javascript
async function routeSession({ isAutonomous, remoteConfig, dockerConfig, _detectManaged }) {
  // Rules: !isAutonomous -> local, docker preferred -> docker, docker enabled -> docker,
  //        no host -> local, managed preferred -> probe managed, host -> ssh, default -> local
}
```

From tests/dispatcher/coordinator-docker.test.cjs (test pattern to mirror):
```javascript
// Uses: createDockerTestCoordinator with full _deps injection
// Pattern: vi.fn() mocks, await new Promise(r => setImmediate(r)) for microtask flushing
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend router with cloud rules and coordinator with _runCloudSession</name>
  <files>
    packages/dispatcher/lib/remote-router.cjs
    packages/dispatcher/lib/coordinator.cjs
  </files>
  <read_first>
    - packages/dispatcher/lib/remote-router.cjs (add cloud routing rule)
    - packages/dispatcher/lib/coordinator.cjs (add _runCloudSession, relay guard, routing_fallback)
    - packages/dispatcher/lib/remote-cloud.cjs (import target)
    - tests/dispatcher/coordinator-docker.test.cjs (understand _runDockerSession pattern to mirror)
  </read_first>
  <action>
**remote-router.cjs** -- Add cloud routing with fallback:
- Update JSDoc header to reference Phase 193 and CLD-07
- Update the routing targets comment to include 'cloud'
- Add cloudConfig parameter to routeSession: ({ isAutonomous, remoteConfig, dockerConfig, cloudConfig, _detectManaged })
- Add cloud rules BEFORE the existing managed rule (Rule 2.7 and 2.8):
  - Rule 2.7: if (remoteConfig and remoteConfig.preferred_backend === 'cloud') -- probe managed backend. If available return 'cloud'. If unavailable, fall through to SSH rules.
  - Rule 2.8: if (cloudConfig and cloudConfig.enabled) -- probe managed backend. If available return 'cloud'. If unavailable, fall through.
- The probe call: detectManaged(_deps) where _deps comes from a new parameter (or reuse the _detectManaged injection). The signature for detectManaged now accepts _deps but the router passes nothing (uses its own default). So the call is just: const probe = await detectManaged(); if (probe.available) return 'cloud';
- Keep all existing rules unchanged after cloud rules

**coordinator.cjs** -- Four changes:

1. Add import: const { spawnCloudSession } = require('./remote-cloud.cjs');

2. In constructor, add cloud DI and config:
   - this._spawnCloudSession = deps.spawnCloudSession || spawnCloudSession;
   - this._cloudConfig = (options.config && options.config.dispatch && options.config.dispatch.cloud) || null;

3. Pass cloudConfig to routeSession call in dispatch():
   - Change the routeSession call to include cloudConfig: this._cloudConfig

4. Add relay spawn guard for cloud: Change line 287's condition from
   if (backend !== 'ssh' && backend !== 'docker')
   to
   if (backend !== 'ssh' && backend !== 'docker' && backend !== 'cloud')
   (CRITICAL per Pitfall 4 -- cloud sessions must NOT spawn relay)

5. Change aggregator.watch call for cloud sessions: When backend === 'cloud', call this._aggregator.watch(relayId, 'cloud') instead of this._aggregator.watch(relayId). Simplest approach: pass sessionType to watch:
   const sessionType = (backend === 'cloud') ? 'cloud' : undefined;
   this._aggregator.watch(relayId, sessionType);

6. Add backend dispatch branch for cloud in the queue section:
   } else if (backend === 'cloud') {
     this._queue.add(() => this._runCloudSession(sessionId, phaseNum, plan, worktreePath, branch, relayId));
   }

7. Add _runCloudSession method (mirror _runDockerSession exactly):
   _runCloudSession(sessionId, phase, plan, worktreePath, branch, relayId) {
     return new Promise((resolve) => {
       const handle = this._spawnCloudSession({
         sessionId,
         relayId,
         phase,
         plan,
         worktreePath,
         cloudConfig: this._cloudConfig || {},
         onLine: (sid, event) => {
           this._aggregator.emit('event', sid, event);
         },
         onExit: (sid, exitCode) => {
           this._handleExit(sid, exitCode, worktreePath, branch).then(resolve);
         },
         _deps: { execCommand: this._spawnCloudSession._execCommand },
       });
       this._sessions.set(sessionId, handle);
     });
   }
   Note: _deps in production will be undefined (uses default execCommand). Tests inject _deps via the spawnCloudSession mock.

8. Add routing_fallback emission in dispatch() -- AFTER routeSession returns and BEFORE lock acquisition:
   - Store original requested backend: const requestedBackend = (this._cloudConfig && this._cloudConfig.enabled) ? 'cloud' : (this._remoteConfig && this._remoteConfig.preferred_backend) || 'local';
   - After routeSession returns, compare: if (requestedBackend === 'cloud' && backend !== 'cloud')
   - Emit: this._aggregator.emit('event', 'system', { type: 'system', subtype: 'routing_fallback', from: 'cloud', to: backend });
   - This keeps the router pure (no EventEmitter) per research recommendation
  </action>
  <verify>
    <automated>node -e "const r = require('./packages/dispatcher/lib/remote-router.cjs'); const c = require('./packages/dispatcher/lib/coordinator.cjs'); console.assert(typeof r.routeSession === 'function'); console.assert(typeof c.DispatchCoordinator === 'function'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "cloud" packages/dispatcher/lib/remote-router.cjs (cloud routing rule exists)
    - grep -q "cloudConfig" packages/dispatcher/lib/remote-router.cjs
    - grep -q "_runCloudSession" packages/dispatcher/lib/coordinator.cjs
    - grep -q "spawnCloudSession" packages/dispatcher/lib/coordinator.cjs
    - grep -q "backend !== 'cloud'" packages/dispatcher/lib/coordinator.cjs (relay guard)
    - grep -q "routing_fallback" packages/dispatcher/lib/coordinator.cjs
    - grep -q "watch.*cloud" packages/dispatcher/lib/coordinator.cjs
    - grep -q "_cloudConfig" packages/dispatcher/lib/coordinator.cjs
  </acceptance_criteria>
  <done>Router has cloud routing rules with fallback to SSH/local. Coordinator has _runCloudSession, skips relay for cloud, passes 'cloud' sessionType to aggregator.watch, and emits routing_fallback when cloud probe fails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create coordinator-cloud.test.cjs with full CC-01 through CC-11 coverage</name>
  <files>tests/dispatcher/coordinator-cloud.test.cjs</files>
  <read_first>
    - tests/dispatcher/coordinator-docker.test.cjs (EXACT template to mirror -- createDockerTestCoordinator pattern)
    - packages/dispatcher/lib/coordinator.cjs (understand dispatch flow after Task 1 changes)
    - packages/dispatcher/lib/remote-cloud.cjs (CloudPoller and spawnCloudSession exports)
    - packages/dispatcher/lib/remote-managed.cjs (detectManagedBackend signature)
    - packages/dispatcher/lib/tmux-fanout.cjs (sourceLabel export)
  </read_first>
  <behavior>
    - CC-01: dispatch() with backend=cloud queues _runCloudSession (spawnCloudSession mock called, not spawnSession/spawnRemoteSession/spawnDockerSession)
    - CC-02: dispatch() with backend=cloud does NOT spawn relay (spawnChildProcess not called)
    - CC-03: _runCloudSession passes correct opts to spawnCloudSession (sessionId, phase, plan, cloudConfig present)
    - CC-04: _runCloudSession onLine forwards events to aggregator.emit
    - CC-05: _runCloudSession onExit calls _handleExit (mergeSession called on exit 0)
    - CC-06: detectManagedBackend returns { available: false } when authMethod !== 'claude.ai' (stub returns { loggedIn: true, authMethod: 'api_key' })
    - CC-07: detectManagedBackend returns { available: true } when authMethod === 'claude.ai' and loggedIn === true
    - CC-08: CloudPoller emits cloud_heartbeat when status=running, then session_end when status=completed, and stops
    - CC-09: Fallback chain: routeSession with cloud preferred but probe unavailable returns ssh (when host configured) or local (when no host)
    - CC-10: sourceLabel('cloud') returns 'C'
    - CC-11: aggregator.watch(id, 'cloud') uses RemoteAggregator (not TailCursor)
  </behavior>
  <action>
Create NEW file tests/dispatcher/coordinator-cloud.test.cjs mirroring coordinator-docker.test.cjs:

1. Test setup: createCloudTestCoordinator factory with full _deps injection per research Pattern 7. Config block includes dispatch.remote.preferred_backend = 'cloud' and dispatch.cloud = { enabled: true, poll_interval: 50, idle_timeout: 300 }. Mock routeSession resolves to 'cloud'.

2. Test fixtures (at top of file):
   - authAvailable = JSON.stringify({ loggedIn: true, authMethod: 'claude.ai', apiProvider: 'firstParty', email: 'test@example.com' })
   - authUnavailable = JSON.stringify({ loggedIn: false, authMethod: 'none' })
   - runningStatus = JSON.stringify({ status: 'running' })
   - completedStatus = JSON.stringify({ status: 'completed' })

3. Implement all 11 tests (CC-01 through CC-11) per the behavior section above. Key patterns:
   - Use vi.fn() for all mocks
   - Use await new Promise(r => setImmediate(r)) for microtask flushing after dispatch
   - For CC-08 (CloudPoller): import CloudPoller directly from remote-cloud.cjs, create with mock execCommand that returns runningStatus then completedStatus, use vi.useFakeTimers() to advance interval
   - For CC-06/CC-07: import detectManagedBackend from remote-managed.cjs, pass { execCommand: vi.fn().mockResolvedValue(fixture) }
   - For CC-09: import routeSession from remote-router.cjs, call with _detectManaged returning { available: false }
   - For CC-10: import sourceLabel from tmux-fanout.cjs
   - For CC-11: import Aggregator from aggregator.cjs, create with mock RemoteAggregatorClass, call watch(id, 'cloud'), verify RemoteAggregator was instantiated

4. CRITICAL test constraints:
   - NEVER call real claude CLI -- all execCommand is vi.fn()
   - spawnCloudSession mock must trigger onLine/onExit callbacks for CC-04/CC-05 tests
   - Use describe blocks: 'DispatchCoordinator — Cloud Backend', 'detectManagedBackend', 'CloudPoller', 'routeSession — cloud fallback', 'sourceLabel', 'Aggregator — cloud sessions'
  </action>
  <verify>
    <automated>npx vitest run tests/dispatcher/coordinator-cloud.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "CC-01" tests/dispatcher/coordinator-cloud.test.cjs
    - grep -q "CC-11" tests/dispatcher/coordinator-cloud.test.cjs
    - grep -q "detectManagedBackend" tests/dispatcher/coordinator-cloud.test.cjs
    - grep -q "CloudPoller" tests/dispatcher/coordinator-cloud.test.cjs
    - grep -q "routing_fallback" tests/dispatcher/coordinator-cloud.test.cjs
    - grep -q "sourceLabel.*cloud.*C" tests/dispatcher/coordinator-cloud.test.cjs
    - grep -q "cloud_heartbeat" tests/dispatcher/coordinator-cloud.test.cjs
    - grep -q "session_end" tests/dispatcher/coordinator-cloud.test.cjs
    - npx vitest run tests/dispatcher/coordinator-cloud.test.cjs exits 0
  </acceptance_criteria>
  <done>All 11 test cases pass. CC-01 through CC-05 verify coordinator cloud dispatch. CC-06/CC-07 verify auth probe. CC-08 verifies CloudPoller lifecycle. CC-09 verifies fallback chain. CC-10 verifies source label. CC-11 verifies aggregator uses RemoteAggregator for cloud.</done>
</task>

</tasks>

<verification>
Full dispatcher test suite still passes after changes:
```bash
npx vitest run tests/dispatcher/
```

Specific cloud test passes:
```bash
npx vitest run tests/dispatcher/coordinator-cloud.test.cjs
```
</verification>

<success_criteria>
- routeSession handles cloud preferred_backend with probe fallback
- coordinator._runCloudSession mirrors _runDockerSession pattern
- Relay spawn guard excludes cloud backend
- aggregator.watch called with 'cloud' sessionType for cloud sessions
- routing_fallback event emitted when cloud probe fails
- All 11 CC-* tests pass
- Existing dispatcher tests unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/193-cloud-web-backend/193-02-SUMMARY.md`
</output>
