---
phase: 138-pwa-and-push-notifications
plan: 02
type: execute
wave: 2
depends_on: ["138-01"]
files_modified:
  - dashboard/lib/push.ts
  - dashboard/app/actions.ts
  - dashboard/hooks/use-push-subscription.ts
  - dashboard/components/pwa/push-manager.tsx
  - dashboard/components/pwa/push-status-banner.tsx
  - dashboard/components/pwa/install-prompt.tsx
  - dashboard/app/api/ingest/route.ts
  - dashboard/app/settings/page.tsx
  - dashboard/lib/__tests__/actions.test.ts
  - dashboard/lib/__tests__/use-push-capability.test.ts
autonomous: false
requirements:
  - PWA-02
  - PWA-03

user_setup:
  - service: vapid
    why: "VAPID keys for Web Push encryption"
    env_vars:
      - name: NEXT_PUBLIC_VAPID_PUBLIC_KEY
        source: "Run npx web-push generate-vapid-keys from dashboard/ and copy Public Key"
      - name: VAPID_PRIVATE_KEY
        source: "Run npx web-push generate-vapid-keys from dashboard/ and copy Private Key"

must_haves:
  truths:
    - "When an approval_request event is ingested, a push notification fires to the subscribed user"
    - "When a critical error event is ingested, a push notification fires to the subscribed user"
    - "User can subscribe to push notifications from the Settings page via a Subscribe button click"
    - "On iOS not installed as PWA, user sees install instructions instead of subscribe button"
    - "On browsers without PushManager, user sees push not available banner"
    - "When push subscription expires (410), stale entry is removed from Redis"
    - "Notification click opens the dashboard sessions page"
  artifacts:
    - path: "dashboard/lib/push.ts"
      provides: "Server-side VAPID web-push configuration"
      contains: "setVapidDetails"
    - path: "dashboard/app/actions.ts"
      provides: "Server Actions for subscribe/unsubscribe/send push"
      exports: ["subscribeUser", "unsubscribeUser", "sendPushToOwner"]
    - path: "dashboard/hooks/use-push-subscription.ts"
      provides: "Client hook for push capability detection and subscription state"
      exports: ["usePushCapability", "PushCapability"]
    - path: "dashboard/components/pwa/push-manager.tsx"
      provides: "Subscribe/unsubscribe UI component"
      contains: "subscribeToPush"
    - path: "dashboard/components/pwa/push-status-banner.tsx"
      provides: "Fallback banner for unsupported platforms"
      contains: "not-supported"
    - path: "dashboard/components/pwa/install-prompt.tsx"
      provides: "iOS install instructions when not standalone"
      contains: "not-installed"
    - path: "dashboard/app/settings/page.tsx"
      provides: "Settings page hosting push subscription UI"
      contains: "PushManager"
  key_links:
    - from: "dashboard/app/api/ingest/route.ts"
      to: "dashboard/app/actions.ts"
      via: "sendPushToOwner call on approval_request or error events"
      pattern: "sendPushToOwner"
    - from: "dashboard/components/pwa/push-manager.tsx"
      to: "dashboard/app/actions.ts"
      via: "subscribeUser Server Action call"
      pattern: "subscribeUser"
    - from: "dashboard/app/actions.ts"
      to: "dashboard/lib/push.ts"
      via: "webpush import for sendNotification"
      pattern: "webpush"
    - from: "dashboard/app/actions.ts"
      to: "dashboard/lib/redis.ts"
      via: "Redis get/set/del for subscription storage"
      pattern: "redis"
---

<objective>
Implement Web Push notifications triggered by approval gates and critical errors, with platform capability detection that shows clear fallback messages on unsupported platforms, and a Settings page for managing push subscriptions.

Purpose: Users get notified on their phone when PDE needs attention (approval required, critical error) even when the dashboard tab is not open, completing the mobile monitoring experience.
Output: Working push notification pipeline from ingest to device, with graceful degradation on unsupported platforms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-pwa-and-push-notifications/138-RESEARCH.md
@.planning/phases/138-pwa-and-push-notifications/138-01-SUMMARY.md

@dashboard/app/api/ingest/route.ts
@dashboard/lib/redis.ts
@dashboard/app/layout.tsx
@dashboard/components/session-detail.tsx
@dashboard/hooks/use-push-subscription.ts
@dashboard/app/sw.ts

<interfaces>
From dashboard/lib/redis.ts:
```typescript
import { Redis } from '@upstash/redis';
export const redis = new Redis({ url: process.env.UPSTASH_REDIS_REST_URL!, token: process.env.UPSTASH_REDIS_REST_TOKEN! });
```

From dashboard/app/api/ingest/route.ts (existing, will be extended):
```typescript
export async function POST(request: Request): Promise<NextResponse>
// Processes validatedBatch of WireEnvelope[]
// Has access to event.event_type and event.approval_id
```

From dashboard/app/sw.ts (created in Plan 01):
```typescript
// Already has push event listener and notificationclick handler
self.addEventListener("push", (event) => { ... showNotification ... });
self.addEventListener("notificationclick", (event) => { ... openWindow ... });
```

From dashboard/components/layout/bottom-nav.tsx (created in Plan 01):
```typescript
const tabs = [
  { href: "/", label: "Sessions", icon: Activity },
  { href: "/settings", label: "Settings", icon: Settings },
] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create push server lib, Server Actions, capability hook, and ingest push trigger</name>
  <files>
    dashboard/lib/push.ts,
    dashboard/app/actions.ts,
    dashboard/hooks/use-push-subscription.ts,
    dashboard/app/api/ingest/route.ts,
    dashboard/lib/__tests__/actions.test.ts,
    dashboard/lib/__tests__/use-push-capability.test.ts
  </files>
  <read_first>
    dashboard/lib/redis.ts,
    dashboard/app/api/ingest/route.ts,
    dashboard/hooks/use-event-stream.ts,
    .planning/phases/138-pwa-and-push-notifications/138-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: subscribeUser stores subscription JSON in Redis at key push:sub:owner
    - Test 2: unsubscribeUser deletes key push:sub:owner from Redis
    - Test 3: sendPushToOwner calls webpush.sendNotification with correct payload shape (title, body, tag, url)
    - Test 4: sendPushToOwner returns success false with reason no-subscription when no sub in Redis
    - Test 5: sendPushToOwner deletes stale subscription from Redis when webpush throws error with statusCode 410
    - Test 6: sendPushToOwner deletes stale subscription from Redis when webpush throws error with statusCode 404
    - Test 7: detectCapability returns not-supported when navigator has no serviceWorker
    - Test 8: detectCapability returns not-supported when window has no PushManager
    - Test 9: detectCapability returns not-installed on iOS (UA contains iPhone) when not in standalone mode
    - Test 10: detectCapability returns permission-denied when Notification.permission is denied
    - Test 11: detectCapability returns supported when serviceWorker and PushManager exist and permission is not denied
  </behavior>
  <action>
    1. Create dashboard/lib/push.ts with server-side VAPID configuration:

       Import webpush from "web-push". Guard the setVapidDetails call behind an env var check so the app does not crash during dev/test without keys:

       ```typescript
       import webpush from "web-push";

       if (process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY && process.env.VAPID_PRIVATE_KEY) {
         webpush.setVapidDetails(
           "mailto:pde-dashboard@localhost",
           process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY,
           process.env.VAPID_PRIVATE_KEY
         );
       }

       export { webpush };
       ```

    2. Create dashboard/app/actions.ts with three Server Actions:

       - subscribeUser(sub: PushSubscriptionJSON): stores JSON.stringify(sub) in Redis at key "push:sub:owner"
       - unsubscribeUser(): deletes key "push:sub:owner" from Redis
       - sendPushToOwner(payload: { title: string; body: string; url: string; tag: string }):
         - Reads subscription from Redis key "push:sub:owner"
         - If null, returns { success: false, reason: "no-subscription" }
         - Calls webpush.sendNotification(sub, JSON.stringify(payload))
         - On success returns { success: true }
         - On error: if err.statusCode is 410 or 404, deletes the stale subscription from Redis
         - Returns { success: false, reason: err.message } on any error

       File must start with "use server" directive. Import webpush from "@/lib/push" and redis from "@/lib/redis".

    3. Create dashboard/hooks/use-push-subscription.ts as a client component hook:

       Export type PushCapability = "supported" | "not-supported" | "not-installed" | "permission-denied"

       Export function usePushCapability() that uses useState + useEffect to detect:
       - If navigator lacks serviceWorker OR window lacks PushManager: "not-supported"
       - If iOS UA detected (iPad|iPhone|iPod regex) AND NOT standalone mode (matchMedia display-mode): "not-installed"
       - If Notification.permission === "denied": "permission-denied"
       - Otherwise: "supported"

       Default state is "not-supported" (safe default before useEffect runs).

    4. Update dashboard/app/api/ingest/route.ts to trigger push notifications:

       After the existing `await p.exec()` line (Step 5) and before the return statement (Step 6), add a loop through validatedBatch. For each event:
       - If event_type is "approval_request" AND event.approval_id is truthy:
         Dynamic import sendPushToOwner from "@/app/actions"
         Call sendPushToOwner with title "Approval Required", body from event context field (cast event as Record to read it, fallback "PDE needs your approval"), url "/", tag "approval-{approval_id}"
         Wrap in .catch(() => {}) so push failure never breaks ingest
         Break after first match (one push per batch)
       - If event_type is "error" OR "critical_error":
         Dynamic import sendPushToOwner from "@/app/actions"
         Call sendPushToOwner with title "PDE Error", body from event message field (fallback "A critical error occurred"), url "/", tag "error-notification"
         Wrap in .catch(() => {}) so push failure never breaks ingest
         Break after first match

       Use dynamic import (await import("@/app/actions")) NOT top-level import. This avoids loading web-push module on every ingest request when push is not configured.

    5. Create dashboard/lib/__tests__/actions.test.ts with mocked Redis and webpush:

       Mock @/lib/redis with { redis: { get: vi.fn(), set: vi.fn(), del: vi.fn() } }
       Mock @/lib/push with { webpush: { sendNotification: vi.fn() } }
       Import the three Server Actions after mocks are set up (use await import)

       Tests:
       - subscribeUser stores sub at key "push:sub:owner"
       - unsubscribeUser deletes key "push:sub:owner"
       - sendPushToOwner returns no-subscription when Redis returns null
       - sendPushToOwner calls sendNotification with correct payload
       - sendPushToOwner deletes sub on 410 error
       - sendPushToOwner deletes sub on 404 error

    6. Create dashboard/lib/__tests__/use-push-capability.test.ts:

       Test the detection logic as a pure function (avoids jsdom serviceWorker limitations):

       ```typescript
       function detectCapability(env: {
         hasServiceWorker: boolean;
         hasPushManager: boolean;
         userAgent: string;
         isStandalone: boolean;
         notificationPermission: string;
       }): string {
         if (!env.hasServiceWorker || !env.hasPushManager) return "not-supported";
         const isIOS = /iPad|iPhone|iPod/.test(env.userAgent);
         if (isIOS && !env.isStandalone) return "not-installed";
         if (env.notificationPermission === "denied") return "permission-denied";
         return "supported";
       }
       ```

       Tests cover: no serviceWorker, no PushManager, iOS non-standalone, iOS standalone, permission denied, all conditions met.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && npm test --prefix dashboard -- --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/lib/push.ts contains "webpush.setVapidDetails"
    - dashboard/lib/push.ts contains "NEXT_PUBLIC_VAPID_PUBLIC_KEY"
    - dashboard/lib/push.ts contains "VAPID_PRIVATE_KEY"
    - dashboard/app/actions.ts contains "use server" as first line content
    - dashboard/app/actions.ts contains "export async function subscribeUser"
    - dashboard/app/actions.ts contains "export async function unsubscribeUser"
    - dashboard/app/actions.ts contains "export async function sendPushToOwner"
    - dashboard/app/actions.ts contains "push:sub:owner"
    - dashboard/app/actions.ts contains "statusCode === 410"
    - dashboard/hooks/use-push-subscription.ts contains "export type PushCapability"
    - dashboard/hooks/use-push-subscription.ts contains "export function usePushCapability"
    - dashboard/hooks/use-push-subscription.ts contains "not-installed"
    - dashboard/hooks/use-push-subscription.ts contains "not-supported"
    - dashboard/hooks/use-push-subscription.ts contains "permission-denied"
    - dashboard/app/api/ingest/route.ts contains "sendPushToOwner"
    - dashboard/app/api/ingest/route.ts contains "approval_request"
    - dashboard/app/api/ingest/route.ts contains "critical_error"
    - npm test --prefix dashboard exits 0
  </acceptance_criteria>
  <done>Server Actions store/delete push subscriptions in Redis, sendPushToOwner handles 410/404 stale cleanup, ingest route fires push for approval_request and error events, capability hook detects all platform states, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create push UI components, Settings page, and wire into layout</name>
  <files>
    dashboard/components/pwa/push-manager.tsx,
    dashboard/components/pwa/push-status-banner.tsx,
    dashboard/components/pwa/install-prompt.tsx,
    dashboard/app/settings/page.tsx
  </files>
  <read_first>
    dashboard/hooks/use-push-subscription.ts,
    dashboard/app/actions.ts,
    dashboard/components/session-detail.tsx,
    dashboard/app/layout.tsx,
    dashboard/components/layout/bottom-nav.tsx,
    .planning/phases/138-pwa-and-push-notifications/138-RESEARCH.md
  </read_first>
  <action>
    1. Create dashboard/components/pwa/install-prompt.tsx as a client component:

       Renders a Card (from @/components/ui/card) with amber-tinted border (border-amber-500/30 bg-amber-500/5).
       Title: "Install Required for Notifications" (text-amber-500, text-sm font-semibold).
       Body text explaining iOS install requirement.
       Ordered list with three steps:
       - Tap the Share button in Safari
       - Scroll down and tap "Add to Home Screen"
       - Tap "Add" to confirm

    2. Create dashboard/components/pwa/push-status-banner.tsx as a client component:

       Props: { capability: PushCapability } (imported from @/hooks/use-push-subscription).
       If capability is "supported", return null (no banner needed).

       Define messages record mapping each non-supported capability to title+body:
       - "not-supported": title "Push Notifications Not Available", body "Your browser does not support push notifications. Try Chrome, Edge, or Safari 16.4+."
       - "not-installed": title "Install App for Notifications", body "Push notifications require installing this app to your Home Screen."
       - "permission-denied": title "Notifications Blocked", body "You have blocked notifications for this site. Enable them in your browser settings to receive alerts."

       Renders a Card with amber-tinted styling showing the appropriate message.

    3. Create dashboard/components/pwa/push-manager.tsx as a client component:

       Include the urlBase64ToUint8Array helper function that converts VAPID public key from base64url to Uint8Array:
       ```
       function urlBase64ToUint8Array(base64String: string): Uint8Array {
         const padding = "=".repeat((4 - (base64String.length % 4)) % 4);
         const base64 = (base64String + padding).replace(/-/g, "+").replace(/_/g, "/");
         const rawData = window.atob(base64);
         const outputArray = new Uint8Array(rawData.length);
         for (let i = 0; i < rawData.length; ++i) {
           outputArray[i] = rawData.charCodeAt(i);
         }
         return outputArray;
       }
       ```

       State: isSubscribed (boolean), isLoading (boolean).

       On mount (useEffect): check navigator.serviceWorker.ready then reg.pushManager.getSubscription() to set isSubscribed.

       handleSubscribe (useCallback):
       - CRITICAL: Call Notification.requestPermission() DIRECTLY inside the click handler. NOT in useEffect, NOT after setTimeout. iOS silently ignores it outside user gesture.
       - If permission !== "granted", return early
       - Get registration via navigator.serviceWorker.ready
       - Call registration.pushManager.subscribe({ userVisibleOnly: true, applicationServerKey: urlBase64ToUint8Array(process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY!) })
       - Call subscribeUser Server Action with JSON.parse(JSON.stringify(sub))
       - Set isSubscribed to true

       handleUnsubscribe (useCallback):
       - Get registration, get subscription, call sub.unsubscribe()
       - Call unsubscribeUser Server Action
       - Set isSubscribed to false

       Renders Card with:
       - Left side: title "Push Notifications" + description text
       - Right side: Button with min-h-[44px] min-w-[120px] (touch target compliance)
       - When subscribed: secondary variant, BellOff icon, "Unsubscribe" text
       - When not subscribed: default variant, Bell icon, "Subscribe" text
       - Button disabled when isLoading

       Import Bell and BellOff from lucide-react. Import Button from @/components/ui/button.

    4. Create dashboard/app/settings/page.tsx as a client component ("use client"):

       Import usePushCapability from @/hooks/use-push-subscription.
       Import PushManager from @/components/pwa/push-manager.
       Import PushStatusBanner from @/components/pwa/push-status-banner.
       Import InstallPrompt from @/components/pwa/install-prompt.

       Layout: main with className "px-4 sm:px-8 max-w-screen-sm mx-auto py-12 pb-24 md:pb-12 space-y-4"
       (pb-24 md:pb-12 matches the bottom nav clearance pattern from Plan 01)

       H1: "Settings" with same styling as home page (text-xl sm:text-[28px] font-semibold leading-[1.1])

       Notifications section (h2 "Notifications"):
       - When capability is "supported": render PushManager component
       - When capability is "not-installed": render InstallPrompt component
       - When capability is NOT "supported": render PushStatusBanner with capability prop
       This means "not-installed" shows BOTH InstallPrompt (iOS-specific steps) AND PushStatusBanner (general message).
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && npm test --prefix dashboard -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/components/pwa/push-manager.tsx contains "Notification.requestPermission()"
    - dashboard/components/pwa/push-manager.tsx contains "subscribeUser"
    - dashboard/components/pwa/push-manager.tsx contains "unsubscribeUser"
    - dashboard/components/pwa/push-manager.tsx contains "min-h-[44px]"
    - dashboard/components/pwa/push-manager.tsx contains "urlBase64ToUint8Array"
    - dashboard/components/pwa/push-manager.tsx contains "NEXT_PUBLIC_VAPID_PUBLIC_KEY"
    - dashboard/components/pwa/push-status-banner.tsx contains "not-supported"
    - dashboard/components/pwa/push-status-banner.tsx contains "not-installed"
    - dashboard/components/pwa/push-status-banner.tsx contains "permission-denied"
    - dashboard/components/pwa/push-status-banner.tsx contains "Push Notifications Not Available"
    - dashboard/components/pwa/install-prompt.tsx contains "Add to Home Screen"
    - dashboard/app/settings/page.tsx contains "usePushCapability"
    - dashboard/app/settings/page.tsx contains "PushManager"
    - dashboard/app/settings/page.tsx contains "PushStatusBanner"
    - dashboard/app/settings/page.tsx contains "InstallPrompt"
    - dashboard/app/settings/page.tsx contains "pb-24 md:pb-12"
    - npm test --prefix dashboard exits 0
  </acceptance_criteria>
  <done>Settings page shows push subscription controls when supported, iOS install instructions when not standalone, push not available banner on unsupported platforms, all buttons have 44px min touch targets, all tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of PWA and push UI</name>
  <files>none</files>
  <action>
    Human visual verification of the complete PWA setup:
    1. Installable web manifest and Serwist service worker (Plan 01)
    2. Bottom tab navigation on mobile with safe area handling (Plan 01)
    3. Settings page with push notification subscribe/unsubscribe
    4. Platform capability detection with fallback banners
    5. Push notifications triggered from ingest for approval gates and errors

    Steps to verify:
    1. Run npm run build from dashboard/ to verify production build succeeds with Serwist
    2. Run npm run start from dashboard/ and open http://localhost:3000
    3. Verify bottom tab navigation appears on mobile viewport (resize browser below 768px)
    4. Verify bottom nav disappears on desktop viewport (768px and above)
    5. Navigate to Settings tab via bottom nav and verify push notification section renders
    6. In Chrome DevTools Application tab Manifest section: verify manifest loads with correct name icons display standalone
    7. In Chrome DevTools Application tab Service Workers section: verify service worker registered at /serwist/sw.js
    8. (Optional) If VAPID keys are configured in .env click Subscribe and verify notification permission prompt appears
  </action>
  <verify>Human visual inspection confirms all PWA features render correctly</verify>
  <done>User confirms PWA installability, bottom nav, settings page, and push UI all render and function correctly</done>
</task>

</tasks>

<verification>
1. npm test --prefix dashboard passes with all tests (existing + actions + capability + manifest + bottom-nav)
2. grep sendPushToOwner dashboard/app/api/ingest/route.ts confirms push trigger wired into ingest
3. grep subscribeUser dashboard/app/actions.ts confirms Server Action exists
4. grep not-supported dashboard/components/pwa/push-status-banner.tsx confirms fallback UI
5. grep not-installed dashboard/components/pwa/install-prompt.tsx confirms iOS install instructions
6. grep usePushCapability dashboard/app/settings/page.tsx confirms Settings page uses capability hook
7. npx next build from dashboard/ succeeds (production build with Serwist)
</verification>

<success_criteria>
- Push notifications fire server-side when approval_request or error events arrive at ingest
- Server Actions correctly store/delete push subscriptions in Redis at key push:sub:owner
- Stale subscriptions (410/404) are automatically cleaned from Redis
- Settings page shows Subscribe button when push is supported
- Settings page shows iOS install instructions when on iOS but not installed as PWA
- Settings page shows push not available banner on browsers without PushManager
- Settings page shows notifications blocked message when permission is denied
- requestPermission() is called inside click handler (not useEffect) for iOS compatibility
- All existing tests continue to pass plus new tests for actions and capability detection pass
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/138-pwa-and-push-notifications/138-02-SUMMARY.md`
</output>
