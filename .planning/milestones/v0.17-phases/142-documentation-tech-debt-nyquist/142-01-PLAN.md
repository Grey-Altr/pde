---
phase: 142-documentation-tech-debt-nyquist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/136.3-final-documentation-filter-cleanup/136.3-01-SUMMARY.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "ROADMAP.md plan checkboxes for 137-02, 137-03, 138-02, 139-01, 139-02, and 141-01 are all [x]"
    - "REQUIREMENTS.md APR-01 through APR-05 rows have Plan and Verified=Yes filled"
    - "REQUIREMENTS.md PWA-01 through PWA-04 rows have Plan and Verified=Yes filled"
    - "REQUIREMENTS.md HRD-01 through HRD-05 rows have Plan and Verified=Yes filled"
    - "REQUIREMENTS.md HRD-04 text references bash_called/file_changed/tool_called (not tool_start/tool_complete)"
    - "136.3-01-SUMMARY.md requirements-completed field is present (even if empty [])"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Plan checkboxes for all completed plans"
      contains: "[x] 137-02-PLAN.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Complete traceability table with Plan and Verified for APR/PWA/HRD"
      contains: "bash_called/file_changed/tool_called"
    - path: ".planning/phases/136.3-final-documentation-filter-cleanup/136.3-01-SUMMARY.md"
      provides: "requirements-completed field"
      contains: "requirements-completed"
  key_links:
    - from: ".planning/v0.17-MILESTONE-AUDIT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "traceability table fill"
      pattern: "APR-01.*137-02.*Yes"
    - from: "137-02-SUMMARY.md requirements-completed"
      to: "REQUIREMENTS.md APR-01 Plan column"
      via: "evidence cross-reference"
      pattern: "137-02"
---

<objective>
Fix ROADMAP plan checkboxes, fill REQUIREMENTS.md traceability, update HRD-04 requirement text, and verify 136.3-01-SUMMARY.md has requirements-completed field.

Purpose: Close the 14 documentation gaps in the v0.17 milestone audit covering ROADMAP, REQUIREMENTS, and SUMMARY artifacts.
Output: ROADMAP.md with all plan checkboxes checked, REQUIREMENTS.md with complete APR/PWA/HRD traceability, corrected HRD-04 text, and 136.3-01-SUMMARY.md with requirements-completed present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v0.17-MILESTONE-AUDIT.md
@.planning/phases/136.3-final-documentation-filter-cleanup/136.3-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check ROADMAP.md plan checkboxes for all completed plans</name>
  <read_first>
    .planning/ROADMAP.md
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    Update ROADMAP.md to check the following plan checkboxes from `[ ]` to `[x]`.

    The lines to change are in the Plans sections for phases 137, 138, 139, and 141. Find each line by its plan filename and change `[ ]` to `[x]`. Do not change any other content on those lines.

    Lines to change (exact current text, change only `[ ]` → `[x]`):
    - `- [ ] 137-02-PLAN.md -- ApprovalCard with AlertDialog confirmation, SessionDetail wiring, SessionCard badge, visual verification (APR-01, APR-02, APR-05)`
    - `- [ ] 137-03-PLAN.md -- Relay daemon approval polling: getApprovalResponse HTTP, polling loop, timeout handling (APR-03, APR-04)`
    - `- [ ] 138-02-PLAN.md -- Web Push Server Actions, VAPID push delivery, capability detection, Settings page, ingest push trigger (PWA-02, PWA-03)`
    - `- [ ] 139-01-PLAN.md -- Dashboard hardening: Redis TTL, rate limiting, cron GC endpoint (HRD-01, HRD-02, HRD-05)`
    - `- [ ] 139-02-PLAN.md -- Relay hardening: event downsampling, buffer cap verification (HRD-03, HRD-04)`
    - `- [ ] 141-01-PLAN.md -- Fix start-relay.cjs stdio + add poll-approval to pde-tools.cjs (APR-04)`

    Also update the progress table at the bottom of ROADMAP.md. Find the rows for phases 137, 138, 139, and 141 and update their Plans Complete counts:
    - Phase 137: currently shows `1/3` → change to `3/3`
    - Phase 138: currently shows `1/2` → change to `2/2`
    - Phase 139: currently shows `0/2` → change to `2/2`
    - Phase 141: currently shows `0/1` → change to `1/1`

    Per D-01: these plans were executed and have SUMMARYs but their checkboxes were never ticked.
  </action>
  <verify>
    <automated>grep -c '\[ \] 137-02\|\[ \] 137-03\|\[ \] 138-02\|\[ \] 139-01\|\[ \] 139-02\|\[ \] 141-01' "/Users/greyaltaer/code/projects/Platform Development Engine/.planning/ROADMAP.md" || echo "0 unchecked (correct)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '[ ] 137-02-PLAN.md' .planning/ROADMAP.md` returns no lines (checkbox checked)
    - `grep '[ ] 137-03-PLAN.md' .planning/ROADMAP.md` returns no lines
    - `grep '[ ] 138-02-PLAN.md' .planning/ROADMAP.md` returns no lines
    - `grep '[ ] 139-01-PLAN.md' .planning/ROADMAP.md` returns no lines
    - `grep '[ ] 139-02-PLAN.md' .planning/ROADMAP.md` returns no lines
    - `grep '[ ] 141-01-PLAN.md' .planning/ROADMAP.md` returns no lines
    - `grep '\[x\] 137-02-PLAN.md' .planning/ROADMAP.md` returns 1 line
    - `grep '\[x\] 141-01-PLAN.md' .planning/ROADMAP.md` returns 1 line
    - Progress table rows for phases 137 (3/3), 138 (2/2), 139 (2/2), 141 (1/1) all show correct counts
  </acceptance_criteria>
  <done>All 6 plan checkboxes changed from [ ] to [x] and progress table counts updated.</done>
</task>

<task type="auto">
  <name>Task 2: Fill REQUIREMENTS.md traceability and fix HRD-04 text</name>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/v0.17-MILESTONE-AUDIT.md
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Make two targeted edits to REQUIREMENTS.md.

    **Edit 1: Fill APR/PWA/HRD traceability table rows**

    In the Traceability table (starting around line 73), find and replace these rows that currently have `—` in Plan and Verified columns.

    Replace these rows exactly:

    Current → Replacement:
    ```
    | APR-01 | 137 | — | — |
    ```
    → `| APR-01 | 137 | 137-02 | Yes |`

    ```
    | APR-02 | 137 | — | — |
    ```
    → `| APR-02 | 137 | 137-02 | Yes |`

    ```
    | APR-03 | 137 | — | — |
    ```
    → `| APR-03 | 137 | 137-03 | Yes |`

    ```
    | APR-04 | 141 | Complete | — |
    ```
    → `| APR-04 | 141 | 141-01 | Yes |`

    ```
    | APR-05 | 137 | — | — |
    ```
    → `| APR-05 | 137 | 137-02 | Yes |`

    ```
    | PWA-01 | 138 | — | — |
    ```
    → `| PWA-01 | 138 | 138-01 | Yes |`

    ```
    | PWA-02 | 138 | — | — |
    ```
    → `| PWA-02 | 138 | 138-02 | Yes |`

    ```
    | PWA-03 | 138 | — | — |
    ```
    → `| PWA-03 | 138 | 138-02 | Yes |`

    ```
    | PWA-04 | 138 | — | — |
    ```
    → `| PWA-04 | 138 | 138-01 | Yes |`

    ```
    | HRD-01 | 139 | — | — |
    ```
    → `| HRD-01 | 139 | 139-01 | Yes |`

    ```
    | HRD-02 | 139 | — | — |
    ```
    → `| HRD-02 | 139 | 139-01 | Yes |`

    ```
    | HRD-03 | 139 | — | — |
    ```
    → `| HRD-03 | 139 | 139-02 | Yes |`

    ```
    | HRD-04 | 139 | — | — |
    ```
    → `| HRD-04 | 139 | 139-02 | Yes |`

    ```
    | HRD-05 | 139 | — | — |
    ```
    → `| HRD-05 | 139 | 139-01 | Yes |`

    Evidence for these values (per D-02, from SUMMARY frontmatter + VERIFICATION cross-reference):
    - APR-01, APR-02, APR-05 → 137-02-SUMMARY.md has `requirements-completed: [APR-01, APR-02, APR-05]`
    - APR-03 → 137-03-SUMMARY.md has `requirements-completed: [APR-03, APR-04]`
    - APR-04 → 141-01 was the final fix that closed the integration gap; 141-VERIFICATION confirms satisfied
    - PWA-01, PWA-04 → 138-01-SUMMARY.md has `requirements-completed: [PWA-01, PWA-04]`
    - PWA-02, PWA-03 → 138-02-SUMMARY.md has `requirements-completed: [PWA-02, PWA-03]`
    - HRD-01, HRD-02, HRD-05 → 139-01-SUMMARY.md has `requirements-completed: [HRD-01, HRD-02, HRD-05]`
    - HRD-03, HRD-04 → 139-02-SUMMARY.md has `requirements-completed: [HRD-03, HRD-04]`

    **Edit 2: Fix HRD-04 requirement text** (per D-03)

    In the Production Hardening requirements section, find and replace:
    ```
    - [x] **HRD-04**: Event downsampling reduces volume during autonomous mode (tool_start/tool_complete events sampled at 1-in-N)
    ```
    →
    ```
    - [x] **HRD-04**: Event downsampling reduces volume during autonomous mode (bash_called/file_changed/tool_called events sampled at 1-in-N)
    ```

    The actual implementation in bin/lib/relay.cjs uses DOWNSAMPLE_TYPES with bash_called, file_changed, tool_called — not tool_start/tool_complete (which do not exist as PDE event types).
  </action>
  <verify>
    <automated>grep 'bash_called' "/Users/greyaltaer/code/projects/Platform Development Engine/.planning/REQUIREMENTS.md" && grep 'APR-01.*137-02.*Yes' "/Users/greyaltaer/code/projects/Platform Development Engine/.planning/REQUIREMENTS.md" && grep 'PWA-01.*138-01.*Yes' "/Users/greyaltaer/code/projects/Platform Development Engine/.planning/REQUIREMENTS.md" && grep 'HRD-01.*139-01.*Yes' "/Users/greyaltaer/code/projects/Platform Development Engine/.planning/REQUIREMENTS.md" && echo "All verifications passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'bash_called' .planning/REQUIREMENTS.md` returns the HRD-04 line (not tool_start/tool_complete)
    - `grep 'tool_start' .planning/REQUIREMENTS.md` returns no lines
    - `grep 'APR-01.*137-02.*Yes' .planning/REQUIREMENTS.md` returns 1 line
    - `grep 'APR-04.*141-01.*Yes' .planning/REQUIREMENTS.md` returns 1 line
    - `grep 'PWA-02.*138-02.*Yes' .planning/REQUIREMENTS.md` returns 1 line
    - `grep 'HRD-03.*139-02.*Yes' .planning/REQUIREMENTS.md` returns 1 line
    - `grep -E 'APR-0[1-5]|PWA-0[1-4]|HRD-0[1-5]' .planning/REQUIREMENTS.md | grep ' — ' ` returns no lines (all `—` cells replaced)
  </acceptance_criteria>
  <done>All 14 traceability cells filled and HRD-04 requirement text corrected to bash_called/file_changed/tool_called.</done>
</task>

<task type="auto">
  <name>Task 3: Verify 136.3-01-SUMMARY.md requirements-completed field</name>
  <read_first>
    .planning/phases/136.3-final-documentation-filter-cleanup/136.3-01-SUMMARY.md
  </read_first>
  <files>.planning/phases/136.3-final-documentation-filter-cleanup/136.3-01-SUMMARY.md</files>
  <action>
    Read 136.3-01-SUMMARY.md and confirm the `requirements-completed` field is present in the YAML frontmatter block (between the `---` delimiters).

    Per D-04 and the research finding: the file already has `requirements-completed: []` in its frontmatter. If the field IS present with `[]`, no change is needed — the audit item was "missing requirements-completed" but direct inspection showed it is present. Leave it as-is.

    If for any reason the field is absent, add `requirements-completed: []` to the YAML frontmatter block, after the existing `key-decisions` line and before `patterns-established`.

    Phase 136.3 was a gap-closure phase with no formal requirement IDs (only SC items), so the empty list `[]` is the correct value.
  </action>
  <verify>
    <automated>grep 'requirements-completed' "/Users/greyaltaer/code/projects/Platform Development Engine/.planning/phases/136.3-final-documentation-filter-cleanup/136.3-01-SUMMARY.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'requirements-completed' .planning/phases/136.3-final-documentation-filter-cleanup/136.3-01-SUMMARY.md` returns at least 1 line
    - The field appears within the YAML frontmatter block (before the second `---` delimiter)
    - Value is `[]` (empty list, which is correct since 136.3 had no formal requirement IDs)
  </acceptance_criteria>
  <done>136.3-01-SUMMARY.md has requirements-completed field present in YAML frontmatter. No change needed if already present.</done>
</task>

</tasks>

<verification>
Run these commands to verify all tasks complete:

```bash
# SC-1: ROADMAP checkboxes
grep -c '\[ \] 137-02\|\[ \] 137-03\|\[ \] 138-02\|\[ \] 139-01\|\[ \] 139-02\|\[ \] 141-01' .planning/ROADMAP.md || echo "0 unchecked"

# SC-2: REQUIREMENTS traceability filled
grep -E 'APR-0[1-5]|PWA-0[1-4]|HRD-0[1-5]' .planning/REQUIREMENTS.md | grep ' — '
# (should return nothing)

# SC-3: HRD-04 text
grep 'bash_called' .planning/REQUIREMENTS.md

# SC-4: SUMMARY field
grep 'requirements-completed' .planning/phases/136.3-final-documentation-filter-cleanup/136.3-01-SUMMARY.md
```
</verification>

<success_criteria>
- 6 ROADMAP plan checkboxes changed from [ ] to [x] (137-02, 137-03, 138-02, 139-01, 139-02, 141-01)
- ROADMAP progress table shows correct plan counts for phases 137, 138, 139, 141
- 14 REQUIREMENTS.md traceability cells filled (APR-01–05, PWA-01–04, HRD-01–05 Plan and Verified columns)
- HRD-04 text corrected from tool_start/tool_complete to bash_called/file_changed/tool_called
- 136.3-01-SUMMARY.md requirements-completed field confirmed present
</success_criteria>

<output>
After completion, create `.planning/phases/142-documentation-tech-debt-nyquist/142-01-SUMMARY.md`
</output>
