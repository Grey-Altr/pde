---
phase: 31-skill-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/skill-quality-rubric.md
  - agents/pde-skill-builder.md
  - agents/pde-design-quality-evaluator.md
  - bin/lib/model-profiles.cjs
  - references/model-profiles.md
  - .planning/phases/31-skill-builder/test_skill01_create_mode.sh
  - .planning/phases/31-skill-builder/test_skill02_improve_mode.sh
  - .planning/phases/31-skill-builder/test_skill03_eval_mode.sh
  - .planning/phases/31-skill-builder/test_skill04_validation_gate.sh
  - .planning/phases/31-skill-builder/test_skill05_reference_loading.sh
  - .planning/phases/31-skill-builder/test_skill06_path_sandboxing.sh
autonomous: true
requirements:
  - SKILL-03
  - SKILL-04
  - SKILL-05
  - SKILL-06

must_haves:
  truths:
    - "pde-skill-builder agent exists with protected-files constraint and 7-step anatomy generation instructions"
    - "pde-design-quality-evaluator agent exists with skill-quality-rubric loading and structured JSON return format"
    - "Skill quality rubric adapts Awwwards 4 dimensions for skill files, not visual design"
    - "Model profiles include pde-skill-builder entry"
    - "Nyquist test scripts exist for all 6 SKILL requirements and can run without error"
  artifacts:
    - path: "agents/pde-skill-builder.md"
      provides: "Skill generation agent for create/improve modes"
      contains: "protected-files.json"
    - path: "agents/pde-design-quality-evaluator.md"
      provides: "Skill evaluation agent for eval mode"
      contains: "skill-quality-rubric"
    - path: "references/skill-quality-rubric.md"
      provides: "Eval mode rubric adapted for skill quality"
      contains: "Design.*40%"
    - path: "bin/lib/model-profiles.cjs"
      provides: "Model resolution for pde-skill-builder"
      contains: "pde-skill-builder"
  key_links:
    - from: "agents/pde-skill-builder.md"
      to: "references/skill-style-guide.md"
      via: "required_reading @ reference"
      pattern: "skill-style-guide\\.md"
    - from: "agents/pde-skill-builder.md"
      to: "references/tooling-patterns.md"
      via: "required_reading @ reference"
      pattern: "tooling-patterns\\.md"
    - from: "agents/pde-design-quality-evaluator.md"
      to: "references/skill-quality-rubric.md"
      via: "required_reading @ reference"
      pattern: "skill-quality-rubric\\.md"
---

<objective>
Create the infrastructure for Phase 31 Skill Builder: the two new agent files (pde-skill-builder for create/improve, pde-design-quality-evaluator for eval), the skill-quality-rubric reference, model-profiles entry, and all 6 Nyquist test scripts.

Purpose: Agents and rubric must exist before the workflow can reference them. Nyquist tests must exist (Wave 0) so Plan 02 tasks have automated verification.
Output: 2 agent files, 1 reference file, model-profiles updated, 6 test scripts.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-skill-builder/31-RESEARCH.md
@.planning/phases/31-skill-builder/31-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill-quality-rubric reference and both agent files</name>
  <files>references/skill-quality-rubric.md, agents/pde-skill-builder.md, agents/pde-design-quality-evaluator.md</files>
  <read_first>
    - agents/pde-skill-improver.md (pattern for agent constraint format, protected-files block, return JSON shape)
    - agents/pde-quality-auditor.md (pattern for rubric-based evaluation agent, structured JSON return)
    - agents/pde-skill-validator.md (pattern for required_reading block and validation checks)
    - references/quality-standards.md (Awwwards 4-dimension rubric to adapt for skill quality)
    - references/skill-style-guide.md (conventions the skill-builder must enforce)
    - references/tooling-patterns.md (LINT rules the skill-builder must reference)
    - protected-files.json (constraint block to embed in pde-skill-builder)
    - bin/lib/model-profiles.cjs (confirm pde-design-quality-evaluator exists, pde-skill-builder does not)
  </read_first>
  <action>
Create three files:

**1. `references/skill-quality-rubric.md`** — Skill quality evaluation rubric adapted from Awwwards 4 dimensions:

```markdown
# Skill Quality Rubric

> Evaluation rubric for PDE skill files. Adapts the Awwwards 4-dimension framework for skill quality assessment (not visual design quality).

## Dimensions

### Design (40% weight)
Structural quality of the skill file itself.

| Score | Criteria |
|-------|----------|
| 1-3 | Missing required XML sections; no step numbering; frontmatter invalid |
| 4-6 | All 7 anatomy sections present but sparse; step numbering inconsistent; some flags missing |
| 7-8 | Complete 7-step anatomy; all 4 universal flags (--dry-run, --quick, --verbose, --no-mcp); proper XML nesting; step labels match "Step N/7:" format |
| 9-10 | Exemplary structure matching elevated skills; context_routing handles all input modes; process steps have clear preconditions and postconditions |

### Usability (30% weight)
Developer experience when using the skill.

| Score | Criteria |
|-------|----------|
| 1-3 | No flag documentation; error messages absent; no prerequisite checks |
| 4-6 | Flags table present but incomplete; some prerequisite checks; generic error messages |
| 7-8 | All flags documented with Type and Behavior; Step 2 checks prerequisites with soft/hard dependency distinction; error messages name the missing item |
| 9-10 | Flag descriptions include examples; prerequisite errors suggest fix commands; edge cases handled (empty input, missing files, partial state) |

### Creativity (20% weight)
Domain sophistication beyond a naive implementation.

| Score | Criteria |
|-------|----------|
| 1-3 | Skill is a thin wrapper with no domain logic; no MCP integration |
| 4-6 | Basic domain logic in Step 4; MCP probe exists but unused in enrichment |
| 7-8 | Step 3 probes relevant MCPs; Step 5 uses MCP results for meaningful enrichment; domain-specific heuristics in core generation |
| 9-10 | Novel domain patterns; multiple enrichment strategies per MCP availability; context_routing adapts generation to input state |

### Content (10% weight)
Reference quality and purpose precision.

| Score | Criteria |
|-------|----------|
| 1-3 | No required_reading; purpose is vague or missing |
| 4-6 | required_reading lists skill-style-guide.md; purpose exists but generic |
| 7-8 | required_reading includes skill-style-guide.md, mcp-integration.md, and domain-specific references; purpose is one dense paragraph naming output format and downstream consumers |
| 9-10 | All references current and relevant; purpose names exact file paths and formats; skill_domain matches required_reading references |

## Scoring

Overall score = (Design * 0.40) + (Usability * 0.30) + (Creativity * 0.20) + (Content * 0.10)

## Return Format

Evaluator agents MUST return JSON matching this schema:

{
  "skill_path": "path/to/skill.md",
  "overall_score": 7.2,
  "dimensions": {
    "design": { "score": 7.5, "weight": 0.40, "findings": ["specific finding text"] },
    "usability": { "score": 8.0, "weight": 0.30, "findings": [] },
    "creativity": { "score": 6.0, "weight": 0.20, "findings": ["specific finding text"] },
    "content": { "score": 7.0, "weight": 0.10, "findings": ["specific finding text"] }
  },
  "lint_errors": [],
  "lint_warnings": [],
  "recommendations": [
    { "priority": "HIGH|MEDIUM|LOW", "action": "specific remediation instruction" }
  ]
}
```

**2. `agents/pde-skill-builder.md`** — Agent for create and improve modes. Must include:

- Title: `# pde-skill-builder`
- Role paragraph: "You are PDE's skill builder agent. You create new skills from descriptions and improve existing skills with targeted enhancements."
- **Protected Files Constraint** section (copy pattern from pde-skill-improver.md): "Before every Write or Edit tool call, read ${CLAUDE_PLUGIN_ROOT}/protected-files.json. You MUST NOT write to any path listed in protected[] or any subdirectory in protected_directories[]. Allowed write destinations: commands/ (for PDE-internal skills via --for-pde), {cwd}/.claude/skills/ (for user-project skills, the default). Note: Claude Code Write and Edit tools bypass the bwrap filesystem sandbox. This constraint is enforced by your instructions only, not OS-level protection. You must honor it explicitly."
- **Required Reading** section with:
  - @${CLAUDE_PLUGIN_ROOT}/references/skill-style-guide.md
  - @${CLAUDE_PLUGIN_ROOT}/references/tooling-patterns.md
  - @${CLAUDE_PLUGIN_ROOT}/references/mcp-integration.md
  - @${CLAUDE_PLUGIN_ROOT}/protected-files.json
- **Mode: CREATE** section: "Produce a complete, valid skill workflow file at the output_path provided. The file MUST include all 7 anatomy sections: <purpose>, <required_reading>, <flags>, <process> (7 numbered steps titled 'Step N/7:'), <skill_code>, <skill_domain>, <context_routing>. Required flags in every skill: --dry-run, --quick, --verbose, --no-mcp. Step 7 MUST include the standard output summary table from skill-style-guide.md. YAML frontmatter MUST include: name, description, argument-hint, allowed-tools (minimum: Read, Write, Bash, Glob, Grep)."
- **Mode: IMPROVE (additive, no --rewrite)** section: "Read the existing skill file. Identify specific deficiencies against the style guide and tooling patterns. Produce ONLY additions and corrections — never delete working content. Return a JSON object: {\"additions\": [{\"after_section\": \"section_name\", \"content\": \"new content\"}], \"replacements\": [{\"original\": \"exact text to find\", \"replacement\": \"replacement text\"}]}. Do NOT output the full file."
- **Mode: IMPROVE (--rewrite)** section: "Read the existing skill file for context. Generate a complete replacement following all CREATE mode requirements. The workflow will back up the original before overwriting."
- **Skill Code Assignment** section: "Propose a 2-4 uppercase letter skill code based on the skill description. Check against known codes in the prompt context to avoid LINT-011 collisions. Output the proposed code for user confirmation."
- **Return Format** section documenting the expected output for each mode.

**3. `agents/pde-design-quality-evaluator.md`** — Agent for eval mode. Must include:

- Title: `# pde-design-quality-evaluator`
- Role paragraph: "You are PDE's design quality evaluator agent. You assess skill files against the skill quality rubric, producing structured scores with specific findings per dimension."
- **Your Constraints** section: "READ-ONLY. You MUST NOT write to any file. You MUST NOT use the Write or Edit tools. Your sole output is structured JSON returned to the orchestrating workflow."
- **Required Reading** section with:
  - @${CLAUDE_PLUGIN_ROOT}/references/skill-quality-rubric.md
  - @${CLAUDE_PLUGIN_ROOT}/references/skill-style-guide.md
  - @${CLAUDE_PLUGIN_ROOT}/references/tooling-patterns.md
- **Your Task** section: "1. Read the target skill file at the path provided. 2. Load all required reading references. 3. Evaluate the skill against each of the 4 rubric dimensions (Design 40%, Usability 30%, Creativity 20%, Content 10%). 4. For each dimension, assign a score 1-10 and list specific findings (not generic observations). 5. Run mental LINT checks from tooling-patterns.md and note any errors/warnings. 6. Produce recommendations sorted by priority (HIGH, MEDIUM, LOW). 7. Calculate overall_score as weighted sum. 8. Return the JSON object matching the schema in skill-quality-rubric.md."
- **Return Format** section with the exact JSON schema from the rubric.
  </action>
  <verify>
    <automated>grep -l "skill-quality-rubric" references/skill-quality-rubric.md && grep -l "pde-skill-builder" agents/pde-skill-builder.md && grep -l "pde-design-quality-evaluator" agents/pde-design-quality-evaluator.md && echo "All 3 files exist"</automated>
  </verify>
  <acceptance_criteria>
    - references/skill-quality-rubric.md contains "Design.*40%" and "Usability.*30%" and "Creativity.*20%" and "Content.*10%"
    - references/skill-quality-rubric.md contains "overall_score" JSON schema example
    - agents/pde-skill-builder.md contains "protected-files.json"
    - agents/pde-skill-builder.md contains "skill-style-guide.md" in required_reading
    - agents/pde-skill-builder.md contains "tooling-patterns.md" in required_reading
    - agents/pde-skill-builder.md contains "Mode: CREATE" and "Mode: IMPROVE"
    - agents/pde-skill-builder.md contains "additions" and "replacements" (improve mode JSON format)
    - agents/pde-skill-builder.md contains "LINT-011" (collision check)
    - agents/pde-design-quality-evaluator.md contains "READ-ONLY"
    - agents/pde-design-quality-evaluator.md contains "skill-quality-rubric.md" in required_reading
    - agents/pde-design-quality-evaluator.md contains "overall_score" in return format
  </acceptance_criteria>
  <done>All three files exist with correct content: rubric has 4 weighted dimensions, skill-builder agent has protected-files constraint and both mode instructions, evaluator agent is read-only with rubric reference</done>
</task>

<task type="auto">
  <name>Task 2: Register pde-skill-builder in model-profiles</name>
  <files>bin/lib/model-profiles.cjs, references/model-profiles.md</files>
  <read_first>
    - bin/lib/model-profiles.cjs (current MODEL_PROFILES object — need to add pde-skill-builder entry)
    - references/model-profiles.md (markdown table — need to add matching row)
  </read_first>
  <action>
Add `pde-skill-builder` entry to both files. Per Phase 31 research decision (aligned with pde-skill-improver tier since both do skill content generation):

In `bin/lib/model-profiles.cjs`, add this line after the `'pde-skill-validator'` entry:
```javascript
  'pde-skill-builder':            { quality: 'sonnet', balanced: 'sonnet', budget: 'haiku' },
```

In `references/model-profiles.md`, add this row after the `pde-skill-validator` row in the Profile Definitions table:
```
| pde-skill-builder | sonnet | sonnet | haiku | inherit |
```

NOTE: `bin/lib/model-profiles.cjs` is in `protected_directories[]` (bin/) AND explicitly in `protected[]`. This is a planning-phase edit that the executor applies directly — it is not an agent self-modification. The protected-files constraint applies to fleet agents at runtime, not to the GSD executor.
  </action>
  <verify>
    <automated>node -e "const m = require('./bin/lib/model-profiles.cjs'); if (!m.MODEL_PROFILES['pde-skill-builder']) { process.exit(1); } console.log('pde-skill-builder registered:', JSON.stringify(m.MODEL_PROFILES['pde-skill-builder']));"</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/model-profiles.cjs contains 'pde-skill-builder' key in MODEL_PROFILES
    - bin/lib/model-profiles.cjs pde-skill-builder entry has quality:'sonnet', balanced:'sonnet', budget:'haiku'
    - references/model-profiles.md contains "pde-skill-builder" row
    - `node -e "require('./bin/lib/model-profiles.cjs')"` exits 0 (no syntax errors)
  </acceptance_criteria>
  <done>pde-skill-builder is registered in model-profiles.cjs with sonnet/sonnet/haiku tiers and matching row exists in model-profiles.md</done>
</task>

<task type="auto">
  <name>Task 3: Create all 6 Nyquist test scripts (Wave 0)</name>
  <files>
    .planning/phases/31-skill-builder/test_skill01_create_mode.sh,
    .planning/phases/31-skill-builder/test_skill02_improve_mode.sh,
    .planning/phases/31-skill-builder/test_skill03_eval_mode.sh,
    .planning/phases/31-skill-builder/test_skill04_validation_gate.sh,
    .planning/phases/31-skill-builder/test_skill05_reference_loading.sh,
    .planning/phases/31-skill-builder/test_skill06_path_sandboxing.sh
  </files>
  <read_first>
    - .planning/phases/30-self-improvement-fleet-audit-command/test_audit01_audit_command.sh (reference for Bash test script pattern — PASS/FAIL format, exit codes)
    - .planning/phases/30-self-improvement-fleet-audit-command/test_audit08_validate_skill.sh (reference for validate-skill testing pattern)
    - .planning/phases/31-skill-builder/31-VALIDATION.md (Wave 0 requirements list)
    - skill-registry.md (verify IMP entry exists for test_skill01)
    - protected-files.json (verify allowed_write_directories for test_skill06)
  </read_first>
  <action>
Create 6 Bash test scripts following the established pattern from Phase 30 tests. Each script:
- Starts with `#!/usr/bin/env bash` and `set -euo pipefail`
- Sets `PASS=0` and `FAIL=0` counters
- Defines `check()` function that increments PASS or FAIL and prints result
- Uses `PLUGIN_ROOT` resolved via `cd "$(dirname "$0")/../../.." && pwd`
- Ends with summary and `exit $FAIL`

**test_skill01_create_mode.sh** (SKILL-01):
- Assert `commands/improve.md` exists
- Assert `workflows/improve.md` exists
- Assert `agents/pde-skill-builder.md` exists
- Assert `agents/pde-design-quality-evaluator.md` exists
- Assert skill-registry.md contains "IMP" code
- Assert skill-registry.md IMP row points to `workflows/improve.md`
- Assert commands/improve.md contains `name: pde:improve`
- Assert commands/improve.md contains `Task` in allowed-tools
- Assert workflows/improve.md contains `create` mode handling

**test_skill02_improve_mode.sh** (SKILL-02):
- Assert workflows/improve.md contains `--rewrite` flag handling
- Assert workflows/improve.md contains `improve` mode handling
- Assert workflows/improve.md contains additive mode logic (grep for "additions" or "additive")
- Assert workflows/improve.md contains backup logic (grep for `.bak` or `backup`)
- Assert agents/pde-skill-builder.md contains "Mode: IMPROVE" or "IMPROVE" section

**test_skill03_eval_mode.sh** (SKILL-03):
- Assert agents/pde-design-quality-evaluator.md exists
- Assert agents/pde-design-quality-evaluator.md contains "skill-quality-rubric"
- Assert references/skill-quality-rubric.md exists
- Assert references/skill-quality-rubric.md contains "overall_score"
- Assert references/skill-quality-rubric.md contains "dimensions"
- Assert workflows/improve.md contains `eval` mode handling
- Assert `node "${PLUGIN_ROOT}/bin/pde-tools.cjs" resolve-model pde-design-quality-evaluator --raw` succeeds

**test_skill04_validation_gate.sh** (SKILL-04):
- Assert workflows/improve.md contains `validate-skill` (the gate call)
- Assert workflows/improve.md contains retry logic (grep for "CYCLE" or "cycle" or "retry" or "MAX_CYCLES")
- Assert workflows/improve.md contains "reject" or "FAIL" or "failed validation" (rejection path)
- Assert `node "${PLUGIN_ROOT}/bin/pde-tools.cjs" validate-skill --help` or similar does not error

**test_skill05_reference_loading.sh** (SKILL-05):
- Assert agents/pde-skill-builder.md contains "skill-style-guide.md"
- Assert agents/pde-skill-builder.md contains "tooling-patterns.md"
- Assert agents/pde-skill-builder.md contains "required_reading" or "Required Reading"
- Assert references/skill-style-guide.md exists (dependency)
- Assert references/tooling-patterns.md exists (dependency)

**test_skill06_path_sandboxing.sh** (SKILL-06):
- Assert workflows/improve.md contains "protected-files" (path guard check)
- Assert workflows/improve.md contains "commands/" (PDE-internal write path)
- Assert workflows/improve.md contains ".claude/skills/" (user-project write path)
- Assert workflows/improve.md contains "--for-pde" (flag for PDE-internal destination)
- Assert protected-files.json contains "commands/" in allowed_write_directories
- Assert protected-files.json does NOT contain ".claude/skills/" in protected (user-project path is safe)
- Assert agents/pde-skill-builder.md contains "protected-files.json"
  </action>
  <verify>
    <automated>PASS=0; for f in .planning/phases/31-skill-builder/test_skill0*.sh; do [ -f "$f" ] && PASS=$((PASS+1)); done; echo "Test scripts found: $PASS/6"; [ "$PASS" -eq 6 ]</automated>
  </verify>
  <acceptance_criteria>
    - All 6 test scripts exist at .planning/phases/31-skill-builder/test_skill0{1-6}_*.sh
    - Each script contains "#!/usr/bin/env bash"
    - Each script contains "set -euo pipefail"
    - Each script ends with "exit" based on FAIL count
    - test_skill01 checks for commands/improve.md, workflows/improve.md, agents/pde-skill-builder.md
    - test_skill04 checks for validate-skill gate and retry logic in workflow
    - test_skill06 checks for protected-files and --for-pde flag
  </acceptance_criteria>
  <done>All 6 Nyquist test scripts exist. Scripts for tests 01-04 will fail (expected — workflow and command files don't exist yet). Scripts 05 and 06 partially pass (agent files created in Task 1). Full green requires Plan 02 completion.</done>
</task>

</tasks>

<verification>
- `node -e "require('./bin/lib/model-profiles.cjs')"` exits 0 (model-profiles syntax valid)
- `node "${PLUGIN_ROOT}/bin/pde-tools.cjs" resolve-model pde-skill-builder --raw` returns a model name
- `ls agents/pde-skill-builder.md agents/pde-design-quality-evaluator.md references/skill-quality-rubric.md` all exist
- All 6 test scripts exist at expected paths
- `bash .planning/phases/31-skill-builder/test_skill05_reference_loading.sh` passes (agent files + reference files exist)
</verification>

<success_criteria>
- 2 new agent files created matching PDE agent conventions (protected-files constraint, required_reading, structured return format)
- 1 new reference file with skill-adapted Awwwards rubric (4 dimensions, scoring criteria per level, JSON return schema)
- pde-skill-builder registered in model-profiles.cjs and model-profiles.md
- 6 Nyquist test scripts ready for Plan 02 verification
</success_criteria>

<output>
After completion, create `.planning/phases/31-skill-builder/31-01-SUMMARY.md`
</output>
