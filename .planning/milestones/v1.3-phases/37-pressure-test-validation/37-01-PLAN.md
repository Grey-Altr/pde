---
phase: 37-pressure-test-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/pressure-test.md
  - agents/pde-pressure-test-evaluator.md
  - bin/lib/model-profiles.cjs
  - references/model-profiles.md
  - .planning/pressure-test/fixture-greenfield/design/design-manifest.json
  - .planning/pressure-test/fixture-partial/design/design-manifest.json
  - .planning/pressure-test/fixture-rerun/design/design-manifest.json
  - .planning/phases/37-pressure-test-validation/tests/test-pres01-command.sh
  - .planning/phases/37-pressure-test-validation/tests/test-pres02-compliance.sh
  - .planning/phases/37-pressure-test-validation/tests/test-pres03-rubric.sh
  - .planning/phases/37-pressure-test-validation/tests/test-pres04-fixtures.sh
  - .planning/phases/37-pressure-test-validation/tests/test-pres05-report.sh
  - .planning/phases/37-pressure-test-validation/tests/test-pres06-ai-aesthetic.sh
  - .planning/phases/37-pressure-test-validation/tests/run-quick.sh
  - .planning/phases/37-pressure-test-validation/tests/run-all.sh
autonomous: true
requirements: [PRES-01, PRES-02, PRES-03, PRES-04, PRES-05, PRES-06]

must_haves:
  truths:
    - "commands/pressure-test.md exists with correct frontmatter (name: pde:pressure-test)"
    - "agents/pde-pressure-test-evaluator.md exists as a design-artifact quality evaluator (NOT skill evaluator)"
    - "pde-pressure-test-evaluator registered in model-profiles.cjs with quality: opus"
    - "Three fixture directories exist with seeded design-manifest.json files"
    - "All 6 Nyquist test scripts exist and validate structural requirements"
  artifacts:
    - path: "commands/pressure-test.md"
      provides: "Command entry point for /pde:pressure-test"
      contains: "pde:pressure-test"
    - path: "agents/pde-pressure-test-evaluator.md"
      provides: "Design artifact quality evaluator agent"
      contains: "quality-standards.md"
    - path: "bin/lib/model-profiles.cjs"
      provides: "Model profile for pde-pressure-test-evaluator"
      contains: "pde-pressure-test-evaluator"
    - path: ".planning/pressure-test/fixture-greenfield/design/design-manifest.json"
      provides: "Greenfield fixture with all coverage flags false"
      contains: "hasDesignSystem"
    - path: ".planning/pressure-test/fixture-partial/design/design-manifest.json"
      provides: "Partial fixture with stages 1-7 coverage flags true"
      contains: "hasDesignSystem"
    - path: ".planning/pressure-test/fixture-rerun/design/design-manifest.json"
      provides: "Complete fixture with all 13 coverage flags true"
      contains: "hasHandoff"
  key_links:
    - from: "commands/pressure-test.md"
      to: "workflows/pressure-test.md"
      via: "@workflows/pressure-test.md reference"
      pattern: "@workflows/pressure-test.md"
    - from: "agents/pde-pressure-test-evaluator.md"
      to: "references/quality-standards.md"
      via: "@ include required reading"
      pattern: "quality-standards.md"
---

<objective>
Create the /pde:pressure-test command, design-artifact evaluator agent, model profile registration, fixture directories for all three entry states, and Wave 0 Nyquist test scripts.

Purpose: Establish all structural contracts and test scaffolds before the workflow implementation (Plan 02). This is the interface-first approach: command shape, agent prompt, fixture data, and test expectations are all defined first.

Output: Command file, agent file, model-profile entry, 3 fixture directories with seeded manifests, 6 Nyquist test scripts, 2 test runners.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-pressure-test-validation/37-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From commands/audit.md (command pattern):
```yaml
---
name: pde:audit
description: Audit PDE's own artifacts for quality gaps...
argument-hint: '[--fix] [--deep]...'
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - Task
---
<objective>Execute the /pde:audit command.</objective>
<process>Follow @workflows/audit.md exactly. Pass all of $ARGUMENTS to the workflow.</process>
```

From bin/lib/model-profiles.cjs (profile pattern):
```javascript
const MODEL_PROFILES = {
  'pde-design-quality-evaluator': { quality: 'opus', balanced: 'sonnet', budget: 'sonnet' },
  // ... other profiles
};
```

From templates/design-manifest.json (coverage flags — all 13):
```json
{
  "hasDesignSystem": false,
  "hasWireframes": false,
  "hasFlows": false,
  "hasHardwareSpec": false,
  "hasCritique": false,
  "hasIterate": false,
  "hasHandoff": false,
  "hasIdeation": false,
  "hasCompetitive": false,
  "hasOpportunity": false,
  "hasMockup": false,
  "hasHigAudit": false,
  "hasRecommendations": false
}
```

From agents/pde-design-quality-evaluator.md (agent prompt pattern — but this agent evaluates SKILL files, NOT design artifacts):
```markdown
# pde-design-quality-evaluator
You are PDE's design quality evaluator agent. You assess skill files against the skill quality rubric...
## Your Constraints
**READ-ONLY.** You MUST NOT write to any file...
## Required Reading
- @${CLAUDE_PLUGIN_ROOT}/references/skill-quality-rubric.md
- @${CLAUDE_PLUGIN_ROOT}/references/skill-style-guide.md
```

From .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand01_motion_spec.sh (Nyquist test pattern):
```bash
#!/usr/bin/env bash
set -euo pipefail
PASS=0; FAIL=0
check() {
  if grep -qE "$1" "$2"; then
    PASS=$((PASS+1))
  else
    echo "FAIL: $3"
    FAIL=$((FAIL+1))
  fi
}
# checks...
echo "HAND-01: $PASS passed, $FAIL failed"
[ "$FAIL" -eq 0 ]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 Nyquist test scripts and test runners</name>
  <files>
    .planning/phases/37-pressure-test-validation/tests/test-pres01-command.sh
    .planning/phases/37-pressure-test-validation/tests/test-pres02-compliance.sh
    .planning/phases/37-pressure-test-validation/tests/test-pres03-rubric.sh
    .planning/phases/37-pressure-test-validation/tests/test-pres04-fixtures.sh
    .planning/phases/37-pressure-test-validation/tests/test-pres05-report.sh
    .planning/phases/37-pressure-test-validation/tests/test-pres06-ai-aesthetic.sh
    .planning/phases/37-pressure-test-validation/tests/run-quick.sh
    .planning/phases/37-pressure-test-validation/tests/run-all.sh
  </files>
  <read_first>
    .planning/phases/37-pressure-test-validation/37-RESEARCH.md
    .planning/phases/37-pressure-test-validation/37-VALIDATION.md
    .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand01_motion_spec.sh
  </read_first>
  <action>
Create the `tests/` subdirectory under `.planning/phases/37-pressure-test-validation/`.

Create 6 Nyquist test scripts using the Phase 36 bash test pattern (`set -euo pipefail`, `PASS=0; FAIL=0`, `check()` function, `PASS=$((PASS+1))` not `((PASS++))`).

**test-pres01-command.sh** (PRES-01):
- Check `commands/pressure-test.md` exists
- Check it contains `name: pde:pressure-test` in frontmatter
- Check it contains `@workflows/pressure-test.md` reference
- Check `workflows/pressure-test.md` exists
- Check workflow contains `skill_code: PRT` or `PRT` skill code reference
- Echo "PRES-01: $PASS passed, $FAIL failed"

**test-pres02-compliance.sh** (PRES-02):
- Check `workflows/pressure-test.md` contains `coverage-check` (pde-tools invocation)
- Check workflow contains `hasDesignSystem` (coverage flag reference)
- Check workflow contains `hasHandoff` (last coverage flag)
- Check workflow contains `BRF-brief-v` (brief Glob-only check — no hasBrief flag)
- Check workflow contains `hasIterate.*false` or `?? false` (nullish coalescing for hasIterate)
- Echo "PRES-02: $PASS passed, $FAIL failed"

**test-pres03-rubric.sh** (PRES-03):
- Check `workflows/pressure-test.md` contains `pde-pressure-test-evaluator` (agent reference)
- Check workflow contains `Task(` (Task invocation pattern for agent)
- Check workflow contains `quality-standards.md` (rubric reference)
- Check `agents/pde-pressure-test-evaluator.md` exists
- Check agent file contains `quality-standards.md` (required reading)
- Echo "PRES-03: $PASS passed, $FAIL failed"

**test-pres04-fixtures.sh** (PRES-04):
- Check `.planning/pressure-test/fixture-greenfield/design/design-manifest.json` exists
- Check `.planning/pressure-test/fixture-partial/design/design-manifest.json` exists
- Check `.planning/pressure-test/fixture-rerun/design/design-manifest.json` exists
- Check greenfield manifest contains `"hasDesignSystem": false` (all flags false)
- Check partial manifest contains `"hasDesignSystem": true` (stages 1-7 done)
- Check rerun manifest contains `"hasHandoff": true` (all flags true)
- Echo "PRES-04: $PASS passed, $FAIL failed"

**test-pres05-report.sh** (PRES-05):
- Check `workflows/pressure-test.md` contains `pressure-test-report.md` (output path)
- Check workflow contains `Tier 1` or `Process Compliance` (compliance tier section)
- Check workflow contains `Tier 2` or `Design Quality` (quality tier section)
- Check workflow contains `PASS` and `FAIL` (per-check result)
- Echo "PRES-05: $PASS passed, $FAIL failed"

**test-pres06-ai-aesthetic.sh** (PRES-06):
- Check `workflows/pressure-test.md` contains `VISUAL-HOOK` (concept-specific interaction check)
- Check workflow contains `asymmetr` (intentional asymmetry check — case insensitive)
- Check workflow contains `OKLCH\|color.*generic\|palette` or similar color check
- Check workflow contains `choreograph\|motion.*stagger\|entrance.*order` (motion check)
- Echo "PRES-06: $PASS passed, $FAIL failed"

**run-quick.sh**: Runs test-pres01-command.sh and test-pres04-fixtures.sh (fast structural checks only). Prints overall pass/fail.

**run-all.sh**: Runs all 6 test scripts sequentially. Prints overall pass/fail count. Exits 0 only if all pass.

All scripts must use `#!/usr/bin/env bash`, `set -euo pipefail`, and the `PASS=$((PASS+1))` pattern (not `((PASS++))` which fails under `set -e` when value is 0).

All `check()` grep patterns must reference files relative to the PDE project root. Use `PROJ_ROOT` variable set to the directory containing `commands/` (detect via: `PROJ_ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"` — adjust as needed for the tests/ subdirectory depth, which is `.planning/phases/37-pressure-test-validation/tests/` = 4 levels deep from project root).
  </action>
  <verify>
    <automated>ls .planning/phases/37-pressure-test-validation/tests/test-pres0*.sh | wc -l | grep -q 6 && echo "6 test scripts exist"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/37-pressure-test-validation/tests/test-pres01-command.sh contains "set -euo pipefail"
    - .planning/phases/37-pressure-test-validation/tests/test-pres01-command.sh contains "PASS=\$((PASS+1))"
    - .planning/phases/37-pressure-test-validation/tests/test-pres01-command.sh contains "pressure-test.md"
    - .planning/phases/37-pressure-test-validation/tests/test-pres04-fixtures.sh contains "fixture-greenfield"
    - .planning/phases/37-pressure-test-validation/tests/test-pres04-fixtures.sh contains "fixture-partial"
    - .planning/phases/37-pressure-test-validation/tests/test-pres04-fixtures.sh contains "fixture-rerun"
    - .planning/phases/37-pressure-test-validation/tests/test-pres06-ai-aesthetic.sh contains "VISUAL-HOOK"
    - .planning/phases/37-pressure-test-validation/tests/run-quick.sh contains "test-pres01"
    - .planning/phases/37-pressure-test-validation/tests/run-all.sh contains "test-pres0"
    - All 8 .sh files are executable (chmod +x)
  </acceptance_criteria>
  <done>6 Nyquist test scripts and 2 runners exist in tests/ directory, all executable, using correct bash patterns</done>
</task>

<task type="auto">
  <name>Task 1: Create command, agent, model-profile registration, and fixture directories</name>
  <files>
    commands/pressure-test.md
    agents/pde-pressure-test-evaluator.md
    bin/lib/model-profiles.cjs
    references/model-profiles.md
    .planning/pressure-test/fixture-greenfield/design/design-manifest.json
    .planning/pressure-test/fixture-partial/design/design-manifest.json
    .planning/pressure-test/fixture-rerun/design/design-manifest.json
  </files>
  <read_first>
    commands/audit.md
    commands/build.md
    agents/pde-design-quality-evaluator.md
    bin/lib/model-profiles.cjs
    references/model-profiles.md
    references/quality-standards.md
    templates/design-manifest.json
    .planning/phases/37-pressure-test-validation/37-RESEARCH.md
  </read_first>
  <action>
**1. Create `commands/pressure-test.md`:**

```yaml
---
name: pde:pressure-test
description: Run the full 13-stage design pipeline on a product concept and evaluate output for process compliance and Awwwards-rubric design quality
argument-hint: '[--fixture greenfield|partial|rerun] [--skip-build] [--verbose]'
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - Task
  - Skill
---
```

Body follows the exact pattern from `commands/audit.md`:
```xml
<objective>Execute the /pde:pressure-test command.</objective>
<process>Follow @workflows/pressure-test.md exactly. Pass all of $ARGUMENTS to the workflow.</process>
```

**2. Create `agents/pde-pressure-test-evaluator.md`:**

This is a NEW agent distinct from `pde-design-quality-evaluator`. It evaluates **design output artifacts** (HTML mockups, CSS token files, MD wireframes, MD critique, MD handoff specs), NOT skill files.

Structure:
```markdown
# pde-pressure-test-evaluator

You are PDE's design output quality evaluator. You assess design pipeline artifacts — the output produced by running the 13-stage design pipeline — against the Awwwards quality rubric.

**CRITICAL:** You evaluate DESIGN OUTPUT (`.planning/design/**` artifacts like mockup HTML, system CSS, wireframe MD, critique MD, handoff specs). You do NOT evaluate skill files (commands/*.md, workflows/*.md). Those are evaluated by pde-design-quality-evaluator.

## Your Constraints

**READ-ONLY.** You MUST NOT write to any file. You MUST NOT use the Write or Edit tools under any circumstances. Your sole output is structured JSON returned to the orchestrating workflow.

Before any tool call, verify you are only using Read, Glob, Grep, and Bash (read-only commands only).

## Required Reading

Load these before evaluating any artifact:
- @${CLAUDE_PLUGIN_ROOT}/references/quality-standards.md
- @${CLAUDE_PLUGIN_ROOT}/references/composition-typography.md
- @${CLAUDE_PLUGIN_ROOT}/references/motion-design.md

## Your Task

For each design artifact provided in the prompt:

1. Read the artifact file
2. Evaluate against the 4 Awwwards dimensions (Design 40%, Usability 30%, Creativity 20%, Content 10%) using criteria from quality-standards.md
3. For each dimension, assign a score 1-10 with specific findings that NAME elements (not generic observations)
4. Run the 4 AI Aesthetic Avoidance checks:
   a. **Concept-specific interaction (VISUAL-HOOK):** Search mockup HTML for `<!-- VISUAL-HOOK:` comment. PASS if found with a named concept-specific element. FAIL if absent or generic.
   b. **Non-generic color choice:** Check system CSS tokens for OKLCH notation and custom palette. FAIL if colors match common defaults (Tailwind indigo `#6366f1`, generic blue-purple gradient).
   c. **Intentional asymmetry:** Search wireframe/mockup for "asymmetry" or "asymmetric" annotations. PASS if at least one axis break is documented with purpose.
   d. **Custom motion choreography:** Check mockup for narrative entrance order. FAIL if stagger pattern is uniform `.forEach delay` or all-at-once. PASS if elements enter in content-meaning order.
5. Calculate overall_score as weighted sum: (design * 0.40) + (usability * 0.30) + (creativity * 0.20) + (content * 0.10)

## Return Schema

Return a JSON object:
```json
{
  "artifacts_evaluated": [
    {
      "artifact": "path/to/artifact",
      "stage": "system|wireframe|mockup|critique|handoff|...",
      "dimensions": {
        "design": { "score": 7, "findings": ["specific finding 1", "..."] },
        "usability": { "score": 8, "findings": ["..."] },
        "creativity": { "score": 6, "findings": ["..."] },
        "content": { "score": 7, "findings": ["..."] }
      },
      "overall_score": 7.1
    }
  ],
  "ai_aesthetic_checks": {
    "concept_specific_interaction": { "result": "PASS|FAIL", "evidence": "named element or absence" },
    "non_generic_color": { "result": "PASS|FAIL", "evidence": "specific color or default detected" },
    "intentional_asymmetry": { "result": "PASS|FAIL", "evidence": "axis break cited or symmetric grid" },
    "custom_motion_choreography": { "result": "PASS|FAIL", "evidence": "named entrance order or generic stagger" }
  },
  "quality_result": "PASS|FAIL",
  "quality_threshold": 6.5
}
```

## Anti-Patterns

- Do NOT evaluate skill files (commands/*.md, workflows/*.md) — those are for pde-design-quality-evaluator
- Do NOT produce generic findings like "good use of color" — name the SPECIFIC color, element, or pattern
- Do NOT assign scores without findings — every score must have at least one supporting observation
- Do NOT check for skill anatomy sections (<purpose>, <process>, etc.) — those belong to skill files, not design output
```

**3. Register model profile in `bin/lib/model-profiles.cjs`:**

Add this entry AFTER the `pde-skill-builder` line (last current entry):
```javascript
  'pde-pressure-test-evaluator':  { quality: 'opus',   balanced: 'sonnet', budget: 'sonnet' },
```

Use `quality: 'opus'` because design quality judgment requires maximum reasoning capability (matches `pde-design-quality-evaluator` precedent).

**4. Register in `references/model-profiles.md`:**

Add a row to the model profiles table:
```
| pde-pressure-test-evaluator | opus | sonnet | sonnet | Design output artifact evaluation requires maximum reasoning |
```

**5. Create 3 fixture directories with seeded manifests:**

All three fixtures use the SAME product concept: **"Tide — a marine biology field research platform for coastal ecologists"**. This concept forces domain-specific color (ocean blues, tide pool palettes in OKLCH), distinctive typography (scientific notation + field labels), and concept-specific interactions (tide cycle visualization, specimen tagging).

**fixture-greenfield/design/design-manifest.json:**
```json
{
  "projectName": "Tide",
  "productType": "SaaS Platform",
  "outputRoot": ".planning/design",
  "mode": "full",
  "version": 1,
  "designCoverage": {
    "hasDesignSystem": false,
    "hasWireframes": false,
    "hasFlows": false,
    "hasHardwareSpec": false,
    "hasCritique": false,
    "hasIterate": false,
    "hasHandoff": false,
    "hasIdeation": false,
    "hasCompetitive": false,
    "hasOpportunity": false,
    "hasMockup": false,
    "hasHigAudit": false,
    "hasRecommendations": false
  }
}
```

Also create `fixture-greenfield/design/DESIGN-STATE.md` with:
```markdown
# Design State — Tide
Status: Not started
Product: Tide — marine biology field research platform for coastal ecologists
```

Also create empty subdirectories: `fixture-greenfield/design/strategy/`, `fixture-greenfield/design/visual/`, `fixture-greenfield/design/ux/`, `fixture-greenfield/design/handoff/`, `fixture-greenfield/design/review/` (use `.gitkeep` files).

**fixture-partial/design/design-manifest.json:**
Same as greenfield but with stages 1-7 set to true:
```json
{
  "projectName": "Tide",
  "productType": "SaaS Platform",
  "outputRoot": ".planning/design",
  "mode": "full",
  "version": 1,
  "designCoverage": {
    "hasDesignSystem": true,
    "hasWireframes": true,
    "hasFlows": true,
    "hasHardwareSpec": false,
    "hasCritique": false,
    "hasIterate": false,
    "hasHandoff": false,
    "hasIdeation": true,
    "hasCompetitive": true,
    "hasOpportunity": true,
    "hasMockup": false,
    "hasHigAudit": false,
    "hasRecommendations": true
  }
}
```

Also create stub artifacts in partial fixture:
- `fixture-partial/design/strategy/REC-recommendations-v1.md` — stub with "# Recommendations — Tide" header + placeholder content
- `fixture-partial/design/strategy/CPT-competitive-v1.md` — stub
- `fixture-partial/design/strategy/OPP-opportunity-v1.md` — stub
- `fixture-partial/design/strategy/IDT-ideation-v1.md` — stub
- `fixture-partial/design/strategy/BRF-brief-v1.md` — Real product concept brief for Tide (3-5 paragraphs describing the marine biology field research platform, its users, key workflows, aesthetic direction)
- `fixture-partial/design/visual/SYS-tokens-v1.css` — stub CSS with `:root { /* Tide design tokens */ }` header
- `fixture-partial/design/ux/FLW-flows-v1.md` — stub flows doc
- `fixture-partial/design/ux/WFR-dashboard-v1.md` — stub wireframe
- `fixture-partial/design/DESIGN-STATE.md` — "Status: Stages 1-7 complete"
- Create empty subdirs with `.gitkeep`: `fixture-partial/design/handoff/`, `fixture-partial/design/review/`, `fixture-partial/design/ux/mockups/`

**fixture-rerun/design/design-manifest.json:**
All 13 coverage flags true:
```json
{
  "projectName": "Tide",
  "productType": "SaaS Platform",
  "outputRoot": ".planning/design",
  "mode": "full",
  "version": 1,
  "designCoverage": {
    "hasDesignSystem": true,
    "hasWireframes": true,
    "hasFlows": true,
    "hasHardwareSpec": true,
    "hasCritique": true,
    "hasIterate": true,
    "hasHandoff": true,
    "hasIdeation": true,
    "hasCompetitive": true,
    "hasOpportunity": true,
    "hasMockup": true,
    "hasHigAudit": true,
    "hasRecommendations": true
  }
}
```

Also create stub artifacts for ALL 13 stages in fixture-rerun (same pattern as partial but with all stages populated):
- All strategy stubs from partial, PLUS:
- `fixture-rerun/design/review/CRT-critique-v1.md` — stub
- `fixture-rerun/design/review/ITR-iterate-v1.md` — stub
- `fixture-rerun/design/review/HIG-audit-v1.md` — stub
- `fixture-rerun/design/ux/mockups/mockup-dashboard.html` — stub HTML
- `fixture-rerun/design/handoff/HND-spec-v1.md` — stub
- `fixture-rerun/design/DESIGN-STATE.md` — "Status: All 13 stages complete"
  </action>
  <verify>
    <automated>grep -q "pde:pressure-test" commands/pressure-test.md && grep -q "pde-pressure-test-evaluator" agents/pde-pressure-test-evaluator.md && grep -q "pde-pressure-test-evaluator" bin/lib/model-profiles.cjs && test -f .planning/pressure-test/fixture-greenfield/design/design-manifest.json && test -f .planning/pressure-test/fixture-partial/design/design-manifest.json && test -f .planning/pressure-test/fixture-rerun/design/design-manifest.json && echo "ALL STRUCTURAL FILES EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - commands/pressure-test.md contains "name: pde:pressure-test"
    - commands/pressure-test.md contains "@workflows/pressure-test.md"
    - commands/pressure-test.md contains "Skill" in allowed-tools
    - agents/pde-pressure-test-evaluator.md contains "quality-standards.md"
    - agents/pde-pressure-test-evaluator.md contains "composition-typography.md"
    - agents/pde-pressure-test-evaluator.md contains "motion-design.md"
    - agents/pde-pressure-test-evaluator.md contains "VISUAL-HOOK"
    - agents/pde-pressure-test-evaluator.md contains "ai_aesthetic_checks"
    - agents/pde-pressure-test-evaluator.md does NOT contain "skill-quality-rubric" or "skill-style-guide"
    - bin/lib/model-profiles.cjs contains "'pde-pressure-test-evaluator'"
    - bin/lib/model-profiles.cjs line for pde-pressure-test-evaluator contains "quality: 'opus'"
    - references/model-profiles.md contains "pde-pressure-test-evaluator"
    - .planning/pressure-test/fixture-greenfield/design/design-manifest.json contains '"hasDesignSystem": false'
    - .planning/pressure-test/fixture-partial/design/design-manifest.json contains '"hasDesignSystem": true'
    - .planning/pressure-test/fixture-partial/design/design-manifest.json contains '"hasCritique": false'
    - .planning/pressure-test/fixture-rerun/design/design-manifest.json contains '"hasHandoff": true'
    - .planning/pressure-test/fixture-partial/design/strategy/BRF-brief-v1.md exists and contains "Tide"
    - .planning/pressure-test/fixture-rerun/design/ux/mockups/mockup-dashboard.html exists
    - .planning/pressure-test/fixture-rerun/design/handoff/HND-spec-v1.md exists
  </acceptance_criteria>
  <done>Command file, agent file, model-profile registration, and 3 fixture directories with seeded manifests and stub artifacts all exist. Nyquist tests for PRES-01 and PRES-04 pass.</done>
</task>

</tasks>

<verification>
Run all Nyquist tests (those that can pass at this point — PRES-01 partially, PRES-04 fully):

```bash
bash .planning/phases/37-pressure-test-validation/tests/test-pres04-fixtures.sh
```

Note: PRES-01 test-pres01-command.sh will partially pass (command exists) but fail on workflow checks until Plan 02 creates the workflow.
</verification>

<success_criteria>
- All 8 test scripts exist and are executable
- commands/pressure-test.md exists with correct frontmatter
- agents/pde-pressure-test-evaluator.md exists targeting design artifacts (not skills)
- pde-pressure-test-evaluator registered in model-profiles.cjs
- 3 fixture directories exist with correctly seeded manifests
- test-pres04-fixtures.sh passes (all fixture checks green)
</success_criteria>

<output>
After completion, create `.planning/phases/37-pressure-test-validation/37-01-SUMMARY.md`
</output>
