---
phase: 32-design-elevation-system-skill
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - workflows/system.md
autonomous: true
requirements:
  - SYS-03
  - SYS-04

must_haves:
  truths:
    - "System skill generates all 4 harmony palette types (analogous, complementary, split-complementary, triadic) with OKLCH hue rotation formulas"
    - "Each harmony color has a $description documenting the hue rotation from primary"
    - "Text color tokens include |Lc| APCA contrast values in their $description fields"
    - "CSS output includes an APCA contrast guidance comment block mapping type scale steps to Lc thresholds"
  artifacts:
    - path: "workflows/system.md"
      provides: "Harmony palette generation and APCA contrast guidance in Step 4"
      contains: "analogous-warm"
    - path: "workflows/system.md"
      provides: "APCA Lc annotations on text color tokens"
      contains: "|Lc|"
  key_links:
    - from: "workflows/system.md"
      to: "references/color-systems.md"
      via: "Harmony hue-rotation formulas"
      pattern: "H \\+ 180\\|H \\+ 120\\|H \\+ 150"
    - from: "workflows/system.md"
      to: "references/composition-typography.md"
      via: "APCA threshold table"
      pattern: "APCA.*Lc"
---

<objective>
Add OKLCH harmony palettes and APCA contrast guidance to the system skill's color and typography token generation.

Purpose: Harmony palettes elevate color selection from arbitrary to perceptually grounded. APCA guidance ensures text readability is documented at every type scale step with modern contrast science, not just legacy WCAG ratios.
Output: Updated workflows/system.md with harmony palette generation (7 harmony colors across 4 types) and APCA Lc annotations on all text color tokens.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-design-elevation-system-skill/32-RESEARCH.md
@.planning/phases/32-design-elevation-system-skill/32-01-SUMMARY.md

<interfaces>
<!-- Key existing color token sections in workflows/system.md -->

Current secondary palette generation (around line 230-232):
```
**Generate secondary palette:**
- Secondary hue: SECONDARY_H = (SEED_H + 180) % 360 (complementary)
- Same scale table with secondary hue, same SEED_C.
```

Current semantic color aliases (around lines 247-261):
```
--color-text-primary: var(--color-neutral-900)
--color-text-secondary: var(--color-neutral-600)
--color-text-muted: var(--color-neutral-400)
```

Current DTCG JSON color section (around lines 693-722):
```json
"color": {
  "primitive": {
    "primary": { ... },
    "secondary": { "500": { "$value": "oklch(0.55 0.180 70)", "$type": "color" } },
    "neutral": { ... },
    "success": { ... }
  },
  "semantic": { ... }
}
```

Harmony hue-rotation formulas from references/color-systems.md:
```
Analogous:           H +/- 30
Complementary:       H + 180
Split-complementary: H +/- 150
Triadic:             H +/- 120
```

APCA Lc thresholds from references/composition-typography.md:
| Font Size | Weight | Minimum |Lc| | Preferred |Lc| |
|-----------|--------|----------|------------|
| 48px+     | Any    | 30       | 45         |
| 36px      | 300+   | 45       | 60         |
| 24px      | 700    | 45       | 60         |
| 18px      | 400    | 75       | 90         |
| 16px      | 500    | 75       | 90         |
| 14px      | 700    | 75       | 90         |
| 12px      | 900    | 90       | --         |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OKLCH harmony palette generation (SYS-03)</name>
  <files>workflows/system.md</files>
  <read_first>
    workflows/system.md
    references/color-systems.md
    .planning/phases/32-design-elevation-system-skill/32-RESEARCH.md
  </read_first>
  <action>
Modify `workflows/system.md` in 4 locations. Targeted edits only.

**Edit 1: Expand "Generate secondary palette" to "Generate harmony palettes"** (around line 230)

Keep the existing secondary palette as-is (it remains the primary semantic alias). Add a NEW subsection after it:

```
**Generate harmony palettes:**

Using the seed color (SEED_H, SEED_C, SEED_L), generate 7 harmony colors across 4 harmony types. Apply the hue-rotation formulas from references/color-systems.md:

| Harmony Token | Hue Formula | Example (SEED_H=280) |
|---------------|-------------|----------------------|
| analogous-warm | SEED_H + 30 | 310 |
| analogous-cool | SEED_H - 30 | 250 |
| complementary  | SEED_H + 180 | 100 |
| split-warm     | SEED_H + 150 | 70  |
| split-cool     | SEED_H - 150 | 130 |
| triadic-a      | SEED_H + 120 | 40  |
| triadic-b      | SEED_H - 120 | 160 |

**All hue values wrap at 360** (e.g., 280 + 150 = 430 → 70).

**Perceptual balance rules:**
- Keep L and C identical to SEED_L and SEED_C for all harmonies. OKLCH perceptual uniformity means equal L/C across hues produces visually balanced palettes by construction.
- Exception: If two harmony hues land within 15 degrees of each other, increase chroma separation by +/-0.04 on the closer pair.
- Apply C_safe_max clamps from the scale generation table (same as primary palette). High-chroma out-of-gamut colors render inconsistently on sRGB monitors.

For each harmony color, generate a full 11-step scale (50-950) using the same scale table as the primary palette but with the harmony hue. Store as:
```
--color-harmony-analogous-warm-{step}: oklch({L} {C} {H});
--color-harmony-analogous-cool-{step}: oklch({L} {C} {H});
--color-harmony-complementary-{step}: oklch({L} {C} {H});
--color-harmony-split-warm-{step}: oklch({L} {C} {H});
--color-harmony-split-cool-{step}: oklch({L} {C} {H});
--color-harmony-triadic-a-{step}: oklch({L} {C} {H});
--color-harmony-triadic-b-{step}: oklch({L} {C} {H});
```
```

**Edit 2: Add harmony block to DTCG JSON example** (after the `"color": { "primitive": { ... }` block)

Add a `"harmony"` key inside `"color"` at the same level as `"primitive"` and `"semantic"`:

```json
"harmony": {
  "analogous-warm": {
    "$description": "Analogous +30 degrees from primary hue",
    "500": { "$value": "oklch(0.55 0.18 {SEED_H+30})", "$type": "color" }
  },
  "analogous-cool": {
    "$description": "Analogous -30 degrees from primary hue",
    "500": { "$value": "oklch(0.55 0.18 {SEED_H-30})", "$type": "color" }
  },
  "complementary": {
    "$description": "Complementary +180 degrees from primary hue",
    "500": { "$value": "oklch(0.55 0.18 {SEED_H+180})", "$type": "color" }
  },
  "split-warm": {
    "$description": "Split-complementary +150 degrees from primary hue",
    "500": { "$value": "oklch(0.55 0.18 {SEED_H+150})", "$type": "color" }
  },
  "split-cool": {
    "$description": "Split-complementary -150 degrees from primary hue",
    "500": { "$value": "oklch(0.55 0.18 {SEED_H-150})", "$type": "color" }
  },
  "triadic-a": {
    "$description": "Triadic +120 degrees from primary hue",
    "500": { "$value": "oklch(0.55 0.18 {SEED_H+120})", "$type": "color" }
  },
  "triadic-b": {
    "$description": "Triadic -120 degrees from primary hue",
    "500": { "$value": "oklch(0.55 0.18 {SEED_H-120})", "$type": "color" }
  }
}
```

Show the full 11-step example for `analogous-warm` (50 through 950) with placeholder formulas for computed L and C values. Other harmonies show only the 500 step for brevity with a note: "Generate full 11-step scale (50-950) for each harmony, identical to primary palette scale generation."

**Edit 3: Add harmony colors to SYS-colors.css template** (after the semantic palettes section, before semantic aliases)

Add:
```css
/* === Harmony Colors === */
:root {
  /* Analogous warm (H + 30) */
  --color-harmony-analogous-warm-50: oklch({L} {C} {H});
  /* ... all 11 steps ... */
  /* Analogous cool (H - 30) */
  --color-harmony-analogous-cool-50: oklch({L} {C} {H});
  /* ... all 11 steps ... */
  /* Complementary (H + 180) */
  --color-harmony-complementary-50: oklch({L} {C} {H});
  /* ... etc for split-warm, split-cool, triadic-a, triadic-b ... */
}
```

**Edit 4: Update dry-run color estimate** (around line 118)

Change from:
```
Color: ~88 primitives + 12 semantic + 12 dark overrides
```
To:
```
Color: ~88 primitives + 77 harmony (7 harmonies x 11 steps) + 12 semantic + 12 dark overrides
```
  </action>
  <verify>
    <automated>bash .planning/phases/32-design-elevation-system-skill/test_sys03_harmony_palettes.sh</automated>
  </verify>
  <acceptance_criteria>
    - workflows/system.md contains "analogous-warm" harmony color token
    - workflows/system.md contains "analogous-cool" harmony color token
    - workflows/system.md contains "split-warm" and "split-cool" harmony tokens
    - workflows/system.md contains "triadic-a" and "triadic-b" harmony tokens
    - workflows/system.md contains "SEED_H + 30" or "H + 30" for analogous-warm hue formula
    - workflows/system.md contains "SEED_H + 180" or "H + 180" for complementary hue formula
    - workflows/system.md contains "SEED_H + 120" or "H + 120" for triadic hue formula
    - workflows/system.md contains "oklch" in harmony color values
    - workflows/system.md contains "$description" with harmony rationale (hue rotation documentation)
    - workflows/system.md contains "C_safe_max" reference in harmony perceptual balance rules
    - workflows/system.md contains "color-harmony-" CSS custom property prefix
    - test_sys03_harmony_palettes.sh exits 0
  </acceptance_criteria>
  <done>Harmony palettes generate 7 colors across 4 types (analogous, complementary, split-complementary, triadic) with full 11-step scales, perceptual balance rules, and gamut-safe chroma clamping. DTCG JSON and CSS output both include harmony block.</done>
</task>

<task type="auto">
  <name>Task 2: Add APCA contrast guidance (SYS-04)</name>
  <files>workflows/system.md</files>
  <read_first>
    workflows/system.md
    references/composition-typography.md
    .planning/phases/32-design-elevation-system-skill/32-RESEARCH.md
  </read_first>
  <action>
Modify `workflows/system.md` in 3 locations. Targeted edits only.

**Edit 1: Add APCA Lc annotations to text color semantic tokens** (around lines 247-255)

Update the semantic color aliases section to include APCA guidance in comments. Change:

```
--color-text-primary: var(--color-neutral-900)
--color-text-secondary: var(--color-neutral-600)
--color-text-muted: var(--color-neutral-400)
```

To:

```
--color-text-primary: var(--color-neutral-900)   /* |Lc| ~95 on white bg — meets APCA preferred for all sizes */
--color-text-secondary: var(--color-neutral-600) /* |Lc| ~68 on white bg — meets APCA min 60; use 16px+/400 or 14px+/700 */
--color-text-muted: var(--color-neutral-400)     /* |Lc| ~45 on white bg — decorative/large text only (24px+). Below body text threshold. */
```

**Edit 2: Add APCA Lc annotations to DTCG JSON text color tokens** (in the semantic section of the JSON example)

Add `$description` fields with |Lc| values to the text color tokens in the DTCG JSON:

```json
"text": {
  "primary": {
    "$value": "{color.primitive.neutral.900}",
    "$type": "color",
    "$description": "Primary text on default bg: |Lc| ~95 (exceeds preferred 90). Meets APCA for all font sizes."
  },
  "secondary": {
    "$value": "{color.primitive.neutral.600}",
    "$type": "color",
    "$description": "Secondary text on default bg: |Lc| ~68 (meets minimum 60). Use 16px+/400 or 14px+/700."
  },
  "muted": {
    "$value": "{color.primitive.neutral.400}",
    "$type": "color",
    "$description": "Muted/decorative text on default bg: |Lc| ~45. Large text only (24px+)."
  }
}
```

Also add equivalent annotations to the dark mode semantic text tokens:
```json
"dark": {
  "text": {
    "primary": {
      "$value": "{color.primitive.neutral.50}",
      "$type": "color",
      "$description": "Primary text on dark bg: |Lc| ~95 (light on dark — APCA polarity reversed). Meets all sizes."
    },
    "secondary": {
      "$value": "{color.primitive.neutral.300}",
      "$type": "color",
      "$description": "Secondary text on dark bg: |Lc| ~65. Use 16px+/400."
    }
  }
}
```

**Edit 3: Add APCA contrast guidance CSS comment block to SYS-typography.css template**

After the letter-spacing section (around line 937), add an APCA guidance comment block:

```css
/*
 * APCA Contrast Guidance — Type Scale
 * Background: --color-bg-default (oklch(0.97 0.005 {H}) ~ white)
 * Text: --color-text-primary (oklch(0.23 ... {H}) ~ near-black)
 *
 * Scale Step | Computed Size | Min Weight | |Lc| (primary text) | Threshold
 * text-4xl   | {computed}rem  | 300        | ~95                  | Preferred (min 45)
 * text-3xl   | {computed}rem  | 300        | ~95                  | Preferred (min 45)
 * text-2xl   | {computed}rem  | 400        | ~95                  | Preferred (min 75)
 * text-xl    | {computed}rem  | 400        | ~90                  | Preferred (min 75)
 * text-lg    | {computed}rem  | 400        | ~90                  | Preferred (min 75)
 * text-base  | 1rem (16px)    | 400        | ~90                  | Preferred (min 75)
 * text-sm    | {computed}rem  | 500        | ~85                  | Check weight (min 75)
 * text-xs    | {computed}rem  | 700        | ~85                  | Needs bold weight (min 75)
 *
 * For secondary text (--color-text-secondary, |Lc| ~68):
 * - Minimum size: 16px at weight 500, or 14px at weight 700
 * - NOT suitable for body text at weight 400 below 18px
 *
 * For muted text (--color-text-muted, |Lc| ~45):
 * - Minimum size: 24px at any weight (decorative/large text only)
 * - NOT suitable for informational text at any size
 *
 * Calculator: myndex.com/APCA
 * Algorithm: APCA 0.98G-4g (|Lc| absolute value notation per project convention)
 */
```

Note: The `{computed}` placeholders are filled at generation time from the typography scale — the skill already computes these values in the typography tokens section.
  </action>
  <verify>
    <automated>bash .planning/phases/32-design-elevation-system-skill/test_sys04_apca_guidance.sh</automated>
  </verify>
  <acceptance_criteria>
    - workflows/system.md contains "|Lc| ~95" annotation on text-primary token
    - workflows/system.md contains "|Lc| ~68" annotation on text-secondary token
    - workflows/system.md contains "|Lc| ~45" annotation on text-muted token
    - workflows/system.md contains "APCA Contrast Guidance" comment block header
    - workflows/system.md contains "myndex.com/APCA" calculator reference
    - workflows/system.md contains "APCA 0.98G" algorithm version reference
    - workflows/system.md contains "Preferred (min 75)" threshold text
    - workflows/system.md DTCG JSON contains "$description" on text.primary with "|Lc|"
    - workflows/system.md DTCG JSON contains "$description" on text.secondary with "|Lc|"
    - workflows/system.md contains dark mode text token "$description" with "|Lc|"
    - test_sys04_apca_guidance.sh exits 0
  </acceptance_criteria>
  <done>APCA |Lc| contrast values annotated on all text color tokens (light and dark). Type scale APCA guidance comment block added to CSS output with threshold mapping per scale step. Calculator reference included.</done>
</task>

</tasks>

<verification>
bash .planning/phases/32-design-elevation-system-skill/test_sys03_harmony_palettes.sh && bash .planning/phases/32-design-elevation-system-skill/test_sys04_apca_guidance.sh
</verification>

<success_criteria>
- SYS-03: System skill generates all 4 harmony types with OKLCH hue rotation, perceptual balance rules, and gamut-safe chroma
- SYS-04: System skill annotates all text tokens with |Lc| APCA values and includes a type scale contrast guidance table
- test_sys03 and test_sys04 pass green
</success_criteria>

<output>
After completion, create `.planning/phases/32-design-elevation-system-skill/32-02-SUMMARY.md`
</output>
