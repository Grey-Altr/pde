---
phase: 32-design-elevation-system-skill
plan: 03
type: execute
wave: 3
depends_on:
  - 32-02
files_modified:
  - workflows/system.md
autonomous: true
requirements:
  - SYS-05
  - SYS-06

must_haves:
  truths:
    - "System skill generates semantic spacing tokens with 3 density contexts (compact, default, cozy)"
    - "Density contexts use the same primitive spacing scale with different semantic token assignments"
    - "System skill produces type pairing recommendations with classification contrast rationale"
    - "At least 3 type pairings are documented with 4-field format (classification, roles, APCA note, avoid)"
  artifacts:
    - path: "workflows/system.md"
      provides: "Density spacing system and type pairing recommendations in Step 4"
      contains: 'data-density="compact"'
    - path: "workflows/system.md"
      provides: "Type pairing recommendations section"
      contains: "Type Pairing"
  key_links:
    - from: "workflows/system.md"
      to: "references/composition-typography.md"
      via: "Type pairing classification taxonomy"
      pattern: "humanist serif\\|humanist sans\\|geometric sans"
---

<objective>
Add density-context spacing system and type pairing recommendations to the system skill.

Purpose: Density spacing enables the same token set to serve hero sections and data-dense views without separate scales. Type pairing recommendations with classification contrast rationale prevent arbitrary font selection.
Output: Updated workflows/system.md with density spacing (compact/default/cozy) and type pairing recommendation section with at least 5 documented pairings.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-design-elevation-system-skill/32-RESEARCH.md
@.planning/phases/32-design-elevation-system-skill/32-02-SUMMARY.md

<interfaces>
<!-- Key existing spacing section in workflows/system.md -->

Current spacing tokens section (around lines 360-382):
```
#### Spacing tokens

Standard scale with 4px base unit:
| Token | Value |
| --space-0 | 0px |
| --space-px | 1px |
| --space-0_5 | 2px |
| --space-1 | 4px |
| --space-2 | 8px |
... (15 steps total through --space-24 = 96px)
```

Current SYS-spacing.css template (around lines 942-965):
```css
:root {
  --space-0:    0px;
  --space-px:   1px;
  --space-0_5:  2px;
  --space-1:    4px;
  ... (15 steps)
  --space-24:   96px;
}
```

Type pairing classification from references/composition-typography.md:
| Category | Examples |
| Humanist serif | EB Garamond, Playfair Display |
| Geometric sans | Futura, DM Sans |
| Humanist sans | Inter, Fira Sans |
| Grotesque sans | Helvetica, Aktiv |
| Slab serif | Roboto Slab |
| Display | Clash Display, Fraunces |
| Monospace | JetBrains Mono |

IBM Carbon density pattern:
| Context | Multiplier | Use |
| compact | 0.75x | Data tables, admin |
| default | 1x | Standard |
| cozy | 1.5x | Hero, editorial |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add density spacing system (SYS-05)</name>
  <files>workflows/system.md</files>
  <read_first>
    workflows/system.md
    references/composition-typography.md
    .planning/phases/32-design-elevation-system-skill/32-RESEARCH.md
  </read_first>
  <action>
Modify `workflows/system.md` in 3 locations. Targeted edits only.

**Edit 1: Add semantic spacing tokens and density contexts to the Spacing tokens section** (after the existing 15-step primitive scale, around line 382)

Keep the existing primitive spacing scale UNCHANGED. Append after it:

```
**Semantic spacing tokens** (reference the primitive scale above):

These tokens map spacing intent to primitive values. Downstream CSS uses semantic names, not raw scale numbers.

```
/* Default density semantic tokens */
:root {
  --spacing-component-gap: var(--space-4);    /* 16px — gap between components in a group */
  --spacing-section-gap:   var(--space-16);   /* 64px — gap between page sections */
  --spacing-content-gap:   var(--space-8);    /* 32px — gap between content blocks */
  --spacing-inline:        var(--space-2);    /* 8px  — inline element spacing (icon + label) */
  --spacing-card-padding:  var(--space-6);    /* 24px — card/panel internal padding */
  --spacing-page-margin:   var(--space-8);    /* 32px — page-level side margins */
}
```

**Density context overrides** (3 contexts — compact, default, cozy — following the IBM Carbon density pattern from references/composition-typography.md):

```
/* Compact density — data tables, admin panels, code editors */
/* Multiplier: ~0.75x — maps semantic tokens to smaller primitive values */
[data-density="compact"] {
  --spacing-component-gap: var(--space-2);    /* 8px  (was 16px) */
  --spacing-section-gap:   var(--space-8);    /* 32px (was 64px) */
  --spacing-content-gap:   var(--space-4);    /* 16px (was 32px) */
  --spacing-inline:        var(--space-1);    /* 4px  (was 8px)  */
  --spacing-card-padding:  var(--space-3);    /* 12px (was 24px) */
  --spacing-page-margin:   var(--space-4);    /* 16px (was 32px) */
}

/* Cozy density — hero sections, marketing pages, editorial layouts */
/* Multiplier: ~1.5x — maps semantic tokens to larger primitive values */
[data-density="cozy"] {
  --spacing-component-gap: var(--space-8);    /* 32px (was 16px) */
  --spacing-section-gap:   var(--space-24);   /* 96px (was 64px) */
  --spacing-content-gap:   var(--space-12);   /* 48px (was 32px) */
  --spacing-inline:        var(--space-4);    /* 16px (was 8px)  */
  --spacing-card-padding:  var(--space-10);   /* 40px (was 24px) */
  --spacing-page-margin:   var(--space-16);   /* 64px (was 32px) */
}
```

**Optical adjustment notes** (document after density contexts):

```
**Optical spacing adjustments** (apply after mathematical spacing):
- First child of section: reduce top padding by 1 scale step (visually flush to header)
- Icon + label pairs: reduce gap by half (icons read as visually larger than their pixel box)
- Numeric data columns: use compact density regardless of page density (data readability)
- Touch targets: maintain 44px minimum regardless of density context (accessibility)
```
```

**Edit 2: Add density spacing to SYS-spacing.css template** (after the current `:root` block, around line 965)

Append after the primitive `:root` block:

```css
/* === Semantic Spacing (Default Density) === */
:root {
  --spacing-component-gap: var(--space-4);
  --spacing-section-gap:   var(--space-16);
  --spacing-content-gap:   var(--space-8);
  --spacing-inline:        var(--space-2);
  --spacing-card-padding:  var(--space-6);
  --spacing-page-margin:   var(--space-8);
}

/* === Compact Density === */
[data-density="compact"] {
  --spacing-component-gap: var(--space-2);
  --spacing-section-gap:   var(--space-8);
  --spacing-content-gap:   var(--space-4);
  --spacing-inline:        var(--space-1);
  --spacing-card-padding:  var(--space-3);
  --spacing-page-margin:   var(--space-4);
}

/* === Cozy Density === */
[data-density="cozy"] {
  --spacing-component-gap: var(--space-8);
  --spacing-section-gap:   var(--space-24);
  --spacing-content-gap:   var(--space-12);
  --spacing-inline:        var(--space-4);
  --spacing-card-padding:  var(--space-10);
  --spacing-page-margin:   var(--space-16);
}
```

**Edit 3: Add spacing density to DTCG JSON** (in the `"spacing"` section of the DTCG JSON example)

Add semantic spacing tokens with density context using `$extensions`:

```json
"spacing": {
  "0":  { "$value": "0px",  "$type": "dimension" },
  "1":  { "$value": "4px",  "$type": "dimension" },
  "4":  { "$value": "16px", "$type": "dimension" },
  "semantic": {
    "component-gap": { "$value": "{spacing.4}", "$type": "dimension", "$description": "Default density — 16px between components" },
    "section-gap":   { "$value": "{spacing.16}", "$type": "dimension", "$description": "Default density — 64px between sections" },
    "content-gap":   { "$value": "{spacing.8}", "$type": "dimension", "$description": "Default density — 32px between content blocks" },
    "inline":        { "$value": "{spacing.2}", "$type": "dimension", "$description": "Default density — 8px inline spacing" },
    "compact": {
      "$extensions": { "com.pde.condition": "density-compact" },
      "component-gap": { "$value": "{spacing.2}", "$type": "dimension" },
      "section-gap":   { "$value": "{spacing.8}", "$type": "dimension" },
      "content-gap":   { "$value": "{spacing.4}", "$type": "dimension" },
      "inline":        { "$value": "{spacing.1}", "$type": "dimension" }
    },
    "cozy": {
      "$extensions": { "com.pde.condition": "density-cozy" },
      "component-gap": { "$value": "{spacing.8}", "$type": "dimension" },
      "section-gap":   { "$value": "{spacing.24}", "$type": "dimension" },
      "content-gap":   { "$value": "{spacing.12}", "$type": "dimension" },
      "inline":        { "$value": "{spacing.4}", "$type": "dimension" }
    }
  }
}
```

Also update the dry-run spacing estimate from `Spacing: 15 values` to `Spacing: 15 primitives + 6 semantic + 12 density overrides (compact + cozy)`.
  </action>
  <verify>
    <automated>bash .planning/phases/32-design-elevation-system-skill/test_sys05_density_spacing.sh</automated>
  </verify>
  <acceptance_criteria>
    - workflows/system.md contains 'data-density="compact"' CSS selector
    - workflows/system.md contains 'data-density="cozy"' CSS selector
    - workflows/system.md contains "--spacing-component-gap" semantic token
    - workflows/system.md contains "--spacing-section-gap" semantic token
    - workflows/system.md contains "--spacing-content-gap" semantic token
    - workflows/system.md contains "--spacing-inline" semantic token
    - workflows/system.md contains "--spacing-card-padding" semantic token
    - workflows/system.md contains "Optical spacing adjustment" or "Optical adjustment" text
    - workflows/system.md contains "density-compact" in DTCG JSON $extensions
    - workflows/system.md contains "density-cozy" in DTCG JSON $extensions
    - The primitive spacing scale (--space-0 through --space-24) is preserved unchanged
    - test_sys05_density_spacing.sh exits 0
  </acceptance_criteria>
  <done>Density spacing system added with 3 contexts (compact/default/cozy) using semantic token overrides on the same primitive scale. Optical adjustment notes documented. DTCG JSON and CSS output both include density blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Add type pairing recommendations (SYS-06)</name>
  <files>workflows/system.md</files>
  <read_first>
    workflows/system.md
    references/composition-typography.md
    .planning/phases/32-design-elevation-system-skill/32-RESEARCH.md
  </read_first>
  <action>
Modify `workflows/system.md` in 2 locations. Targeted edits only.

**Edit 1: Add "Type pairing recommendations" subsection after Typography tokens** (insert between typography tokens and spacing tokens sections)

```
#### Type pairing recommendations

Generate type pairing guidance in the usage guide output (SYS-guide.md). This is design recommendation content, not raw token data — pairings are design decisions that require rationale, not measurable values.

**Classification taxonomy** (Vox-ATypI simplified for digital, from references/composition-typography.md):

| Category | Description | Examples | Token value |
|----------|-------------|---------|-------------|
| Humanist serif | Variable stroke, bracketed serifs, calligraphic origin | EB Garamond, Libre Baskerville, Playfair Display | `humanist-serif` |
| Transitional serif | Geometric regularization, vertical stress, high contrast | Georgia, Charter | `transitional-serif` |
| Geometric sans | Minimal contrast, circular forms, single-storey a/g | Futura, DM Sans, Circular | `geometric-sans` |
| Humanist sans | Calligraphic influence, variable stroke, two-storey a | Inter, Gill Sans, Fira Sans | `humanist-sans` |
| Grotesque sans | Realist, minimal contrast, derived from early 19th C. | Helvetica, Arial, Aktiv | `grotesque-sans` |
| Slab serif | Square brackets, low contrast, sturdy | Roboto Slab, Zilla Slab | `slab-serif` |
| Display | Expressive, concept-specific, legibility not primary | Clash Display, Fraunces | `display` |
| Monospace | Fixed-width, code/data contexts | JetBrains Mono, Fira Code | `monospace` |

**Generate at least 5 pairings in the usage guide with this 4-field format:**

```
### Recommendation {N}: {Display Font} ({classification}) + {Body Font} ({classification})
**Classification contrast:** {serif vs sans | geometric vs humanist | etc.} — {category contrast | personality contrast | purpose contrast | rhythm contrast}
**{Display Font}** serves {role: display headings, hero text, etc.} at {size range} where {quality: personality, authority, impact, etc.} matters.
**{Body Font}** serves {role: body text, UI labels, etc.} at {size range} where {quality: legibility, neutrality, readability} is critical.
**APCA note:** Pair requires |Lc| >= {threshold} for {Body Font} at body sizes ({size}/{weight}); {Display Font} at {display size}+ needs |Lc| >= {threshold}.
**Avoid:** {Anti-pattern — e.g., using display font below certain size, mixing two fonts of same classification}
```

**The 5 recommended pairings to generate:**

1. **Playfair Display (humanist serif) + Inter (humanist sans)**
   - Classification contrast: serif vs sans — category contrast in stroke style
   - Playfair serves display headings (32px+) where personality and authority matter
   - Inter serves body text and UI labels where legibility at small sizes is critical
   - APCA note: |Lc| >= 75 for Inter at body sizes (16px/400); Playfair at 36px+ needs |Lc| >= 45
   - Avoid: Using Playfair below 24px — serifs at small sizes reduce legibility

2. **EB Garamond (humanist serif) + DM Sans (geometric sans)**
   - Classification contrast: humanist serif + geometric sans — personality contrast (calligraphic vs constructed)
   - EB Garamond serves editorial headings (24px+) with classical, bookish character
   - DM Sans serves body and UI with clean geometric neutrality
   - APCA note: |Lc| >= 75 for DM Sans body (16px/400); EB Garamond at 24px+ needs |Lc| >= 45
   - Avoid: Mixing EB Garamond with another humanist serif — insufficient contrast

3. **Fraunces (display serif) + Source Sans 3 (humanist sans)**
   - Classification contrast: display + humanist sans — expressive vs functional role contrast
   - Fraunces serves high-impact hero/brand moments with optical size axis animation
   - Source Sans 3 serves body text with neutral readability and variable font wght axis
   - APCA note: |Lc| >= 75 for Source Sans 3 body; Fraunces display-only at 36px+
   - Avoid: Using Fraunces for body text — display fonts sacrifice legibility for personality

4. **JetBrains Mono (monospace) + Inter (humanist sans)**
   - Classification contrast: monospace + humanist sans — character rhythm contrast
   - JetBrains Mono serves code blocks, data displays, terminal-like contexts
   - Inter serves prose, UI, and navigation with proportional readability
   - APCA note: |Lc| >= 75 for both at body sizes; monospace at 14px+ needs |Lc| >= 75
   - Avoid: Using monospace for body prose — fixed-width reduces reading speed 10-15%

5. **Clash Display (display) + Satoshi (grotesque sans)**
   - Classification contrast: display + grotesque — purpose contrast (expressive vs neutral)
   - Clash Display serves startup/creative hero headings with geometric personality
   - Satoshi serves body text with clean, modern neutrality
   - APCA note: |Lc| >= 75 for Satoshi body; Clash Display at 36px+ needs |Lc| >= 45
   - Avoid: Using Clash Display below 32px or for body text — display fonts need optical size
```

**Edit 2: Add type pairing metadata to DTCG JSON** (inside the `"typography"` block)

Add a `classification` field to the font family tokens:

```json
"typography": {
  "fontFamily": {
    "display": {
      "$value": ["system-ui", "-apple-system", "sans-serif"],
      "$type": "fontFamily",
      "$description": "Display/heading typeface. Classification: determined at generation time from pairing selection."
    },
    "body": {
      "$value": ["system-ui", "-apple-system", "sans-serif"],
      "$type": "fontFamily",
      "$description": "Body/UI typeface. Classification: determined at generation time from pairing selection."
    }
  },
  "pairing": {
    "$type": "string",
    "$value": "system-default",
    "$description": "Type pairing rationale — see SYS-guide.md Type Pairings section for full classification contrast documentation."
  }
}
```
  </action>
  <verify>
    <automated>bash .planning/phases/32-design-elevation-system-skill/test_sys06_type_pairings.sh</automated>
  </verify>
  <acceptance_criteria>
    - workflows/system.md contains "Type Pairing" or "Type pairing" section heading
    - workflows/system.md contains "Playfair Display" and "Inter" in a pairing recommendation
    - workflows/system.md contains "EB Garamond" and "DM Sans" in a pairing recommendation
    - workflows/system.md contains "Fraunces" and "Source Sans 3" in a pairing recommendation
    - workflows/system.md contains "JetBrains Mono" in a pairing recommendation
    - workflows/system.md contains "Clash Display" and "Satoshi" in a pairing recommendation
    - workflows/system.md contains "classification contrast" or "category contrast" rationale text
    - workflows/system.md contains "humanist serif" classification label
    - workflows/system.md contains "humanist sans" classification label
    - workflows/system.md contains "geometric sans" classification label
    - workflows/system.md contains "|Lc|" in at least one pairing APCA note
    - workflows/system.md contains "Avoid:" anti-pattern guidance in at least one pairing
    - workflows/system.md contains "pairing" key in DTCG JSON typography block
    - test_sys06_type_pairings.sh exits 0
  </acceptance_criteria>
  <done>Type pairing recommendations added with 5 pairings using 4-field rationale format (classification contrast, roles, APCA note, avoid). Vox-ATypI classification taxonomy embedded. DTCG JSON includes pairing metadata reference.</done>
</task>

</tasks>

<verification>
bash .planning/phases/32-design-elevation-system-skill/test_sys05_density_spacing.sh && bash .planning/phases/32-design-elevation-system-skill/test_sys06_type_pairings.sh
</verification>

<success_criteria>
- SYS-05: System skill generates density spacing with compact/default/cozy contexts using semantic token overrides on the same primitive scale
- SYS-06: System skill includes 5+ type pairing recommendations with classification contrast rationale in 4-field format
- All 6 Nyquist tests pass green (full phase validation)
- Full suite: `for f in .planning/phases/32-design-elevation-system-skill/test_sys*.sh; do bash "$f"; done` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/32-design-elevation-system-skill/32-03-SUMMARY.md`
</output>
