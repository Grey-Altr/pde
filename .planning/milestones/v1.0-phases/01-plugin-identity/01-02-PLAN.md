---
phase: 01-plugin-identity
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified: [".planning/ROADMAP.md"]
autonomous: false
gap_closure: true
requirements: ["PLUG-01", "PLUG-02"]

must_haves:
  truths:
    - "ROADMAP success criterion 2 matches the implemented reality (0.1.0, kebab-case name)"
    - "GitHub remote https://github.com/Grey-Altr/pde.git is configured and pushed"
    - "claude plugin install https://github.com/Grey-Altr/pde.git succeeds without errors"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated success criterion 2 reflecting user decision"
      contains: "plugin.json contains name \"platform-development-engine\", description, and version 0.1.0"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".claude-plugin/plugin.json"
      via: "success criterion text matches actual manifest values"
      pattern: "0\\.1\\.0"
---

<objective>
Close two verification gaps from Phase 1: (1) update stale ROADMAP text to match the user decision on version/naming, and (2) push the repository to GitHub so PLUG-01 install can be verified end-to-end.

Purpose: The ROADMAP spec conflict creates confusion in future verification runs. The missing GitHub remote blocks PLUG-01 completion.
Output: Updated ROADMAP.md, GitHub remote configured and pushed, PLUG-01 install verified.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-identity/01-01-SUMMARY.md
@.planning/phases/01-plugin-identity/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ROADMAP spec conflict and push to GitHub</name>
  <files>.planning/ROADMAP.md</files>
  <action>
1. Update ROADMAP.md Phase 1 success criterion 2. Change:
   "plugin.json displays name "Platform Development Engine", description, and version 1.0.0"
   To:
   "plugin.json contains name \"platform-development-engine\" (kebab-case machine ID), description, and version 0.1.0 (per user decision — bumps to 1.0.0 after Phase 7 or 8)"

   This reflects the documented user decision from 01-CONTEXT.md: version starts at 0.1.0, name field is kebab-case machine identifier.

2. Configure the GitHub remote:
   git remote add origin https://github.com/Grey-Altr/pde.git

3. Push all commits to the remote:
   git push -u origin main

   If push fails due to authentication, this will be caught and the human-verify checkpoint below will handle it.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep -q "0.1.0" .planning/ROADMAP.md && grep -q "platform-development-engine" .planning/ROADMAP.md && git remote get-url origin | grep -q "Grey-Altr/pde" && echo "PASS"</automated>
  </verify>
  <done>ROADMAP success criterion 2 says "0.1.0" and "platform-development-engine". GitHub remote is configured. Push attempted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify GitHub push and plugin install</name>
  <action>
User verifies that the repository is live on GitHub and the plugin installs successfully via the git URL mechanism.
  </action>
  <what-built>Updated ROADMAP to match user decisions and pushed repository to GitHub. If the push succeeded, the plugin should now be installable via git URL.</what-built>
  <how-to-verify>
    1. Confirm the GitHub repository exists: visit https://github.com/Grey-Altr/pde
    2. If the repo does not exist yet, create it on GitHub first (empty repo, no README), then re-run: git push -u origin main
    3. Once pushed, test plugin install:
       claude plugin install https://github.com/Grey-Altr/pde.git
    4. Verify it appears in installed plugins:
       claude plugin list
    5. Confirm no errors or warnings in a new Claude Code session
  </how-to-verify>
  <verify>User confirms plugin install succeeded</verify>
  <done>PLUG-01 verified: plugin installs from GitHub URL without errors</done>
  <resume-signal>Type "approved" if install succeeded, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
- ROADMAP.md success criterion 2 no longer references "1.0.0" or display name "Platform Development Engine" in the name field context
- GitHub remote is configured and commits are pushed
- PLUG-01 verified: claude plugin install from GitHub URL succeeds
</verification>

<success_criteria>
- ROADMAP spec conflict resolved — criterion 2 matches implementation
- Repository pushed to https://github.com/Grey-Altr/pde.git
- Plugin installable via claude plugin install https://github.com/Grey-Altr/pde.git (verified by user)
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-identity/01-02-SUMMARY.md`
</output>
