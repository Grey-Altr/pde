---
phase: 08-onboarding-distribution
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
  - scripts/validate-install.sh
autonomous: true
requirements: [BRAND-06]

must_haves:
  truths:
    - "marketplace.json exists and enables plugin distribution via /plugin marketplace add Grey-Altr/pde"
    - "Validation script detects hardcoded usernames, GSD references, version mismatches, and missing marketplace.json"
    - "Validation script passes on current codebase (zero errors)"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Plugin marketplace catalog for distribution"
      contains: "platform-development-engine"
    - path: "scripts/validate-install.sh"
      provides: "Automated install validation checks"
      contains: "validate-install"
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: ".claude-plugin/plugin.json"
      via: "Plugin name must match exactly"
      pattern: "platform-development-engine"
    - from: "scripts/validate-install.sh"
      to: "VERSION"
      via: "Version consistency check"
      pattern: "VERSION.*plugin.json"
---

<objective>
Create the distribution infrastructure: marketplace.json for Claude Code plugin discovery and a validation script that verifies install readiness.

Purpose: marketplace.json is required for users to install PDE via `/plugin marketplace add Grey-Altr/pde`. The validation script provides automated checks for BRAND-06 compliance and portability. Both must exist before documentation can reference them accurately.

Output: `.claude-plugin/marketplace.json`, `scripts/validate-install.sh`
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-onboarding-distribution/08-CONTEXT.md
@.planning/phases/08-onboarding-distribution/08-RESEARCH.md

<interfaces>
<!-- Key files the executor needs to reference -->

From .claude-plugin/plugin.json:
```json
{
  "name": "platform-development-engine",
  "version": "0.1.0",
  "description": "Full lifecycle platform: from raw idea to shipped product with research, planning, execution, and verification",
  "author": {
    "name": "GreyA",
    "email": "grey.altaer@gmail.com"
  },
  "homepage": "https://github.com/Grey-Altr/pde.git",
  "repository": "https://github.com/Grey-Altr/pde.git",
  "license": "MIT"
}
```

From VERSION:
```
0.1.0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketplace.json for plugin distribution</name>
  <files>.claude-plugin/marketplace.json</files>
  <action>
Create `.claude-plugin/marketplace.json` with the following exact structure:

```json
{
  "name": "pde",
  "owner": {
    "name": "GreyA",
    "email": "grey.altaer@gmail.com"
  },
  "metadata": {
    "description": "Platform Development Engine — full lifecycle platform for solo agentic development"
  },
  "plugins": [
    {
      "name": "platform-development-engine",
      "source": "./",
      "description": "Full lifecycle platform: from raw idea to shipped product with research, planning, execution, and verification",
      "version": "0.1.0",
      "homepage": "https://github.com/Grey-Altr/pde"
    }
  ]
}
```

Critical details:
- The `plugins[0].name` MUST be `"platform-development-engine"` to match `plugin.json`'s `name` field exactly
- The `version` stays `"0.1.0"` for now (Plan 03 bumps it to 1.0.0)
- The `"source": "./"` points to the repo root relative to the marketplace root
- The marketplace `name` is `"pde"` (short form used in install command: `@pde`)
- No trailing commas in JSON
  </action>
  <verify>
    <automated>cat .claude-plugin/marketplace.json | python3 -m json.tool > /dev/null && echo "Valid JSON" && grep -q '"platform-development-engine"' .claude-plugin/marketplace.json && echo "Plugin name matches"</automated>
  </verify>
  <done>marketplace.json exists at .claude-plugin/marketplace.json, is valid JSON, and plugin name matches plugin.json</done>
</task>

<task type="auto">
  <name>Task 2: Create install validation script</name>
  <files>scripts/validate-install.sh</files>
  <action>
Create `scripts/` directory and `scripts/validate-install.sh`. Make it executable (`chmod +x`).

The script must check four things:

1. **No hardcoded usernames in source files** — `grep -r "/Users/[a-zA-Z]" bin/ lib/ commands/ workflows/ templates/ references/ .claude-plugin/` across .md, .json, .cjs, .sh files. Error if any found.

2. **No GSD references in documentation** — `grep -ril "gsd\|get-shit-done" README.md GETTING-STARTED.md` (suppress errors for missing files). Error if any found. Also check source dirs: `grep -ril "gsd\|get-shit-done" bin/ lib/ commands/ workflows/ templates/ references/ .claude-plugin/` — error if any found.

3. **VERSION and plugin.json version in sync** — Compare `cat VERSION | tr -d '[:space:]'` with version extracted from plugin.json via grep/sed. Error if mismatch.

4. **marketplace.json exists** — Check file existence at `.claude-plugin/marketplace.json`. Error if missing.

Script format:
- Shebang: `#!/usr/bin/env bash`
- `set -e` at top
- Print header "PDE Install Validation" with separator
- Each check prints what it's checking, then OK or ERROR
- Track error count, exit 1 if any errors, exit 0 if all pass
- Print summary line at end: "PASSED: All checks clean" or "FAILED: N error(s) found"

Use the exact script structure from 08-RESEARCH.md as the reference implementation, but add the source directory GSD check (check 2b) that extends the documentation-only check.

After creating the file, run `chmod +x scripts/validate-install.sh`.
  </action>
  <verify>
    <automated>bash scripts/validate-install.sh</automated>
  </verify>
  <done>Validation script exists, is executable, and passes all 4 checks on current codebase (README.md and GETTING-STARTED.md may not exist yet — script handles missing files gracefully)</done>
</task>

</tasks>

<verification>
- `cat .claude-plugin/marketplace.json | python3 -m json.tool` succeeds (valid JSON)
- `grep '"platform-development-engine"' .claude-plugin/marketplace.json` matches (name consistent with plugin.json)
- `bash scripts/validate-install.sh` exits 0 with all checks passing
- `grep -i "gsd\|get-shit-done" .claude-plugin/marketplace.json scripts/validate-install.sh` returns no matches (BRAND-06)
</verification>

<success_criteria>
- marketplace.json enables the two-step install: `/plugin marketplace add Grey-Altr/pde` then `/plugin install platform-development-engine@pde`
- Validation script catches hardcoded usernames, GSD references, version mismatches, and missing marketplace.json
- Both files contain zero GSD references
</success_criteria>

<output>
After completion, create `.planning/phases/08-onboarding-distribution/08-01-SUMMARY.md`
</output>
