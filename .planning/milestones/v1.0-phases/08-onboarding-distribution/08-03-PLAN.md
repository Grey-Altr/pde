---
phase: 08-onboarding-distribution
plan: "03"
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - README.md
  - VERSION
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
autonomous: false
requirements: [BRAND-06]

must_haves:
  truths:
    - "README.md sells PDE's value and links to GETTING-STARTED.md"
    - "Mermaid workflow diagram renders the discuss -> plan -> execute -> verify loop"
    - "VERSION, plugin.json, and marketplace.json all show 1.0.0"
    - "Git tag v1.0.0 exists locally"
    - "Validation script passes after version bump"
    - "Zero GSD references in README.md"
  artifacts:
    - path: "README.md"
      provides: "GitHub landing page with value pitch and install instructions"
      min_lines: 50
      contains: "GETTING-STARTED.md"
    - path: "VERSION"
      provides: "Version source of truth"
      contains: "1.0.0"
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest with 1.0.0 version"
      contains: "1.0.0"
  key_links:
    - from: "README.md"
      to: "GETTING-STARTED.md"
      via: "Markdown link in How it works section"
      pattern: "GETTING-STARTED.md"
    - from: "VERSION"
      to: ".claude-plugin/plugin.json"
      via: "Version string must match"
      pattern: "1.0.0"
    - from: ".claude-plugin/marketplace.json"
      to: ".claude-plugin/plugin.json"
      via: "Version string must match"
      pattern: "1.0.0"
---

<objective>
Write README.md as the GitHub landing page, bump version to 1.0.0 across all version-bearing files, and create the v1.0.0 git tag.

Purpose: README.md is the first thing users see on GitHub -- it must sell the value and direct them to GETTING-STARTED.md. The version bump to 1.0.0 signals PDE is feature-complete and ready for public use. The user reviews everything before pushing.

Output: `README.md`, updated `VERSION`, updated `.claude-plugin/plugin.json`, updated `.claude-plugin/marketplace.json`, git tag `v1.0.0`
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-onboarding-distribution/08-CONTEXT.md
@.planning/phases/08-onboarding-distribution/08-RESEARCH.md
@.planning/phases/08-onboarding-distribution/08-01-SUMMARY.md
@.planning/phases/08-onboarding-distribution/08-02-SUMMARY.md

<interfaces>
<!-- Files this plan modifies -- executor needs current state -->

From VERSION (current):
```
0.1.0
```

From .claude-plugin/plugin.json (current):
```json
{
  "name": "platform-development-engine",
  "version": "0.1.0",
  "description": "Full lifecycle platform: from raw idea to shipped product with research, planning, execution, and verification",
  "author": { "name": "GreyA", "email": "grey.altaer@gmail.com" },
  "homepage": "https://github.com/Grey-Altr/pde.git",
  "repository": "https://github.com/Grey-Altr/pde.git",
  "license": "MIT",
  "keywords": ["platform", "development-lifecycle", "planning", "research", "execution", "verification", "workflow", "claude-code"]
}
```

From GETTING-STARTED.md (created by Plan 02):
- Exists at repo root
- Contains full walk-through tutorial
- README must link to it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md and bump version to 1.0.0</name>
  <files>README.md, VERSION, .claude-plugin/plugin.json, .claude-plugin/marketplace.json</files>
  <action>
**Part A: Create README.md at repo root**

Follow ALL locked decisions from CONTEXT.md:

Structure:
1. **Header** -- "Platform Development Engine (PDE)" with version badge:
   `![Version](https://img.shields.io/badge/version-1.0.0-blue)`
   And license badge: `![License](https://img.shields.io/badge/license-MIT-green)`

2. **One-line description** -- What PDE is in a single sentence

3. **Mermaid workflow diagram** -- Use Mermaid (per Claude's discretion, Mermaid preferred for GitHub rendering):
   ```mermaid
   flowchart LR
       NP["/pde:new-project"] --> D["/pde:discuss-phase"]
       D --> P["/pde:plan-phase"]
       P --> E["/pde:execute-phase"]
       E --> V["/pde:verify-work"]
       V -->|next phase| D
       V -->|milestone done| M["/pde:complete-milestone"]
   ```

4. **Key capabilities** -- 6-8 bullet points, one-liner each (scannable):
   - Structured phases: discuss, plan, execute, verify
   - Adaptive questioning to capture project vision
   - Parallel task execution with wave-based orchestration
   - Automatic state persistence across context resets
   - Requirements traceability from idea to shipped feature
   - Atomic git commits with verification gates
   - 34 slash commands covering the full development lifecycle
   - Works with any project type (web apps, CLIs, APIs, libraries)

5. **How it works** -- 2-3 sentences explaining the core loop, then link: "See the [Getting Started guide](GETTING-STARTED.md) for a complete walk-through."

6. **Quick install** -- The two-step install commands:
   ```
   /plugin marketplace add Grey-Altr/pde
   /plugin install platform-development-engine@pde
   ```
   With prerequisite note: "Requires Claude Code and an active Anthropic subscription."

7. **License** -- "MIT" with link to LICENSE file if it exists, otherwise just state MIT

Style rules: No GSD references. No marketing superlatives. Professional but approachable.

**Part B: Bump version to 1.0.0**

Update these files atomically:
1. `VERSION` -- Change content from `0.1.0` to `1.0.0`
2. `.claude-plugin/plugin.json` -- Change `"version": "0.1.0"` to `"version": "1.0.0"`
3. `.claude-plugin/marketplace.json` -- Change `"version": "0.1.0"` to `"version": "1.0.0"` in the plugins array

All three files must show `1.0.0` after this task.
  </action>
  <verify>
    <automated>bash scripts/validate-install.sh && grep -q "1.0.0" VERSION && grep -q '"1.0.0"' .claude-plugin/plugin.json && grep -q '"1.0.0"' .claude-plugin/marketplace.json && grep -q "GETTING-STARTED.md" README.md && grep -ci "gsd\|get-shit-done" README.md || echo "OK: Zero GSD refs"</automated>
  </verify>
  <done>README.md exists with workflow diagram, capabilities list, install instructions, and link to GETTING-STARTED.md. VERSION, plugin.json, and marketplace.json all show 1.0.0. Zero GSD references in README.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review README, docs, and version before tagging v1.0.0</name>
  <files>README.md, GETTING-STARTED.md, VERSION, .claude-plugin/plugin.json, .claude-plugin/marketplace.json</files>
  <action>
Present all Phase 8 deliverables for user review. After user approval, create git tag:
`git tag -a v1.0.0 -m "PDE v1.0.0 -- initial public release"`
The tag will NOT be pushed automatically -- user pushes when ready via `git push origin v1.0.0`.
  </action>
  <verify>
    <automated>bash scripts/validate-install.sh && git tag --list v1.0.0 | grep -q "v1.0.0" && echo "Tag exists"</automated>
  </verify>
  <done>User has reviewed and approved all deliverables. Git tag v1.0.0 created locally. User can push at their discretion.</done>
</task>

</tasks>

<verification>
- `bash scripts/validate-install.sh` passes (all checks green)
- `cat VERSION` shows `1.0.0`
- `grep '"version"' .claude-plugin/plugin.json` shows `1.0.0`
- `grep '"version"' .claude-plugin/marketplace.json` shows `1.0.0`
- `grep -q "GETTING-STARTED.md" README.md` succeeds (link exists)
- `grep -ci "gsd\|get-shit-done" README.md` returns 0 (BRAND-06)
- `git tag --list v1.0.0` shows the tag exists (after user approval)
</verification>

<success_criteria>
- README.md is a compelling GitHub landing page with diagram, capabilities, install instructions, and link to guide
- All version-bearing files show 1.0.0
- Validation script passes
- Git tag v1.0.0 created (user pushes when ready)
- Zero GSD references in any new file (BRAND-06)
</success_criteria>

<output>
After completion, create `.planning/phases/08-onboarding-distribution/08-03-SUMMARY.md`
</output>
