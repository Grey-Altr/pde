---
phase: 11-command-reference-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/recommend.md
  - commands/wireframe.md
  - commands/system.md
  - commands/critique.md
  - commands/hig.md
  - commands/mockup.md
  - commands/handoff.md
  - commands/flows.md
  - commands/competitive.md
  - commands/opportunity.md
  - commands/hardware.md
  - commands/iterate.md
  - commands/brief.md
  - commands/setup.md
  - commands/test.md
  - commands/resume-work.md
  - commands/debug.md
  - commands/discuss-milestone.md
  - commands/join-discord.md
  - commands/reapply-patches.md
autonomous: true
requirements:
  - CMD-01

must_haves:
  truths:
    - "Every /pde: command referenced in workflows/ and references/ resolves to a commands/*.md entry"
    - "/pde:recommend exists as a command stub and is invokable"
    - "Design-pipeline commands (wireframe, system, critique, etc.) show v2 planned status when invoked"
    - "No user-facing workflow output suggests a command that does not exist"
  artifacts:
    - path: "commands/recommend.md"
      provides: "v2 skill stub for MCP recommendation"
      contains: "name: pde:recommend"
    - path: "commands/wireframe.md"
      provides: "v2 skill stub for wireframing"
      contains: "name: pde:wireframe"
    - path: "commands/resume-work.md"
      provides: "Alias stub routing to resume-project workflow"
      contains: "name: pde:resume-work"
    - path: "commands/debug.md"
      provides: "Stub for debug command"
      contains: "name: pde:debug"
  key_links:
    - from: "commands/resume-work.md"
      to: "workflows/resume-project.md"
      via: "execution_context reference"
      pattern: "workflows/resume-project\\.md"
    - from: "commands/debug.md"
      to: "workflows/diagnose-issues.md"
      via: "execution_context reference (debug aliases diagnose-issues)"
      pattern: "workflows/diagnose-issues\\.md"
---

<objective>
Create 20 command stubs for all /pde: commands referenced in workflows/ and references/ that lack entries in commands/, then verify zero dangling references remain.

Purpose: Close the gap between commands referenced in user-facing docs and commands registered in the palette. Users should never encounter a "command not found" when following workflow guidance.
Output: 20 new command stub files in commands/, audit confirming zero dangling references.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-command-reference-cleanup/11-CONTEXT.md
@.planning/phases/11-command-reference-cleanup/11-RESEARCH.md

<interfaces>
<!-- Existing command stub pattern from commands/progress.md -->
```markdown
---
name: pde:{command-name}
description: {one-liner description}
argument-hint: ""
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Glob
  - Grep
  - Task
---
<objective>
Execute the /pde:{command-name} workflow.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/workflows/{command-name}.md
</execution_context>

<process>
Execute the workflow from @${CLAUDE_PLUGIN_ROOT}/workflows/{command-name}.md.
Pass any $ARGUMENTS to the workflow process.
</process>
```

<!-- Workflows that exist (relevant subset): -->
- workflows/resume-project.md (backs resume-work command)
- workflows/diagnose-issues.md (backs debug command)
- No workflow exists for: discuss-milestone, join-discord, reapply-patches
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 20 command stubs in commands/</name>
  <files>commands/recommend.md, commands/wireframe.md, commands/system.md, commands/critique.md, commands/hig.md, commands/mockup.md, commands/handoff.md, commands/flows.md, commands/competitive.md, commands/opportunity.md, commands/hardware.md, commands/iterate.md, commands/brief.md, commands/setup.md, commands/test.md, commands/resume-work.md, commands/debug.md, commands/discuss-milestone.md, commands/join-discord.md, commands/reapply-patches.md</files>
  <action>
Create 20 new command stub files in commands/ following two patterns:

**Pattern A: Workflow-backed stubs (2 stubs)**

These commands have existing backing workflows. Use the standard stub format with execution_context pointing to the real workflow file:

1. `commands/resume-work.md` — Routes to `workflows/resume-project.md` (same behavior, different name).
   - name: pde:resume-work
   - description: Resume work from a previous session with full context restoration
   - execution_context: @${CLAUDE_PLUGIN_ROOT}/workflows/resume-project.md
   - process: Execute the workflow from @${CLAUDE_PLUGIN_ROOT}/workflows/resume-project.md

2. `commands/debug.md` — Routes to `workflows/diagnose-issues.md` (debug is the user-facing name for diagnose-issues).
   - name: pde:debug
   - description: Diagnose and fix issues in your project
   - execution_context: @${CLAUDE_PLUGIN_ROOT}/workflows/diagnose-issues.md
   - process: Execute the workflow from @${CLAUDE_PLUGIN_ROOT}/workflows/diagnose-issues.md

**Pattern B: v2/future stubs (18 stubs)**

These commands have no backing workflow. Use the v2 stub pattern: no execution_context block, and a process block that states "Planned -- available in PDE v2" with a brief description of what the command does. Do NOT reference nonexistent workflow files.

Core commands (no backing workflow):
3. `commands/discuss-milestone.md` — name: pde:discuss-milestone, description: Gather context and scope for a new milestone through adaptive questioning. Process: Planned v2 feature; for now use /pde:new-milestone which includes context gathering.
4. `commands/join-discord.md` — name: pde:join-discord, description: Get the PDE community Discord invite link. Process: Planned v2 feature; community channels coming soon.
5. `commands/reapply-patches.md` — name: pde:reapply-patches, description: Re-apply patches after a PDE update. Process: Planned v2 feature; for now re-run /pde:update to get the latest version.

Design-pipeline skills (all v2 stubs):
6. `commands/recommend.md` — name: pde:recommend, description: Discover and install MCPs and tools relevant to your project
7. `commands/wireframe.md` — name: pde:wireframe, description: Generate browser-viewable HTML/CSS wireframes for specified screens, argument-hint: '"screen1, screen2, ..."'
8. `commands/system.md` — name: pde:system, description: Generate a design system with tokens, typography, and color palette
9. `commands/critique.md` — name: pde:critique, description: Run multi-perspective design critique on specified artifacts
10. `commands/hig.md` — name: pde:hig, description: Audit designs against HIG and WCAG accessibility guidelines
11. `commands/mockup.md` — name: pde:mockup, description: Generate high-fidelity interactive HTML/CSS mockups
12. `commands/handoff.md` — name: pde:handoff, description: Produce developer handoff package with component APIs
13. `commands/flows.md` — name: pde:flows, description: Map user flows as Mermaid flowchart diagrams
14. `commands/competitive.md` — name: pde:competitive, description: Run competitive analysis for your product space
15. `commands/opportunity.md` — name: pde:opportunity, description: Score feature opportunities using RICE and market signal analysis
16. `commands/hardware.md` — name: pde:hardware, description: Generate hardware specification with DFM analysis
17. `commands/iterate.md` — name: pde:iterate, description: Revise design artifacts based on critique feedback
18. `commands/brief.md` — name: pde:brief, description: Generate a structured product design brief
19. `commands/setup.md` — name: pde:setup, description: Install and configure PDE dependencies and MCP servers
20. `commands/test.md` — name: pde:test, description: Validate skill files against style rules and run smoke tests

For all v2 design-pipeline stubs (items 6-20), include in the process block:
- "**Status:** Planned -- available in PDE v2 (design pipeline)."
- A 1-2 sentence description of what the command does
- A mention of related design-pipeline commands (brief, flows, system, wireframe, mockup, critique, hig, iterate, handoff)
- A pointer to references/mcp-integration.md or references/skill-style-guide.md for documentation

For all stubs:
- argument-hint defaults to "" unless noted
- allowed-tools MUST include: Read, Write, Edit, Bash, Glob, Grep, Task (all 7)
- Frontmatter name field MUST exactly match the command name as referenced in docs (e.g., pde:resume-work not pde:resume_work)
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && echo "--- Stub count ---" && ls commands/*.md | wc -l && echo "--- All have name: pde: ---" && grep -L "^name: pde:" commands/*.md | head -5 && echo "--- All have Task in allowed-tools ---" && grep -L "Task" commands/*.md | head -5 && echo "--- New stubs exist ---" && for f in recommend wireframe system critique hig mockup handoff flows competitive opportunity hardware iterate brief setup test resume-work debug discuss-milestone join-discord reapply-patches; do test -f "commands/$f.md" && echo "OK: $f" || echo "MISSING: $f"; done</automated>
  </verify>
  <done>All 20 command stub files exist in commands/ with correct frontmatter (name: pde:*, all 7 allowed-tools including Task). Workflow-backed stubs point to existing workflow files. v2 stubs have inline process blocks with planned status.</done>
</task>

<task type="auto">
  <name>Task 2: Run cross-reference audit and fix any remaining gaps</name>
  <files>commands/*.md</files>
  <action>
Run the automated grep audit to verify zero dangling /pde: command references remain:

1. Extract all unique /pde: command names from workflows/ and references/ (exclude .planning/):
   ```
   rg -oh "/pde:[a-z-]+" workflows/ references/ | sort -u > /tmp/pde-referenced.txt
   ```

2. Extract all registered command names from commands/:
   ```
   ls commands/*.md | sed 's|commands/||; s|\.md$||' | sed 's/^/\/pde:/' | sort > /tmp/pde-registered.txt
   ```

3. Diff to find unresolved references:
   ```
   comm -23 /tmp/pde-referenced.txt /tmp/pde-registered.txt
   ```

4. If any unresolved references remain, create additional stubs using the v2 pattern from Task 1. If they reference commands that are clearly typos or variants of existing commands (e.g., /pde:source vs /pde:migrate), create stubs using the appropriate pattern.

5. Also audit templates/ for completeness (secondary scope per CONTEXT.md):
   ```
   rg -oh "/pde:[a-z-]+" templates/ | sort -u | comm -23 - /tmp/pde-registered.txt
   ```

6. Verify all stubs have valid frontmatter:
   ```
   grep "^name:" commands/*.md | grep -v "pde:"
   ```
   This must produce zero output.

7. Verify no stub references a nonexistent workflow file. For each stub with an execution_context block, confirm the referenced workflow file exists.

If any gaps are found in steps 3-5, create the missing stubs immediately before marking this task done. The audit MUST produce zero output (clean) to pass.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && echo "=== AUDIT: Referenced vs Registered ===" && comm -23 <(rg -oh "/pde:[a-z-]+" workflows/ references/ 2>/dev/null | sort -u) <(ls commands/*.md | sed 's|commands/||; s|\.md$||' | sed 's/^/\/pde:/' | sort) && echo "=== AUDIT: Templates ===" && comm -23 <(rg -oh "/pde:[a-z-]+" templates/ 2>/dev/null | sort -u) <(ls commands/*.md | sed 's|commands/||; s|\.md$||' | sed 's/^/\/pde:/' | sort) && echo "=== AUDIT: Bad frontmatter ===" && grep "^name:" commands/*.md | grep -v "pde:" && echo "=== AUDIT COMPLETE ==="</automated>
  </verify>
  <done>Cross-reference audit produces zero dangling references. Every /pde: command mentioned in workflows/, references/, and templates/ has a corresponding commands/*.md entry. All frontmatter name fields are valid pde:* format.</done>
</task>

</tasks>

<verification>
1. `ls commands/*.md | wc -l` shows 54 (34 existing + 20 new) or more
2. Audit grep `comm -23` produces zero output for workflows/ and references/
3. All stubs include Task in allowed-tools: `grep -L "Task" commands/*.md` returns nothing
4. No stub references a nonexistent workflow: workflow-backed stubs only point to resume-project.md and diagnose-issues.md (both verified to exist)
</verification>

<success_criteria>
- 20+ new command stubs created in commands/
- Zero dangling /pde: references in workflows/ and references/
- All stubs have valid frontmatter with name: pde:* and Task in allowed-tools
- Workflow-backed stubs (resume-work, debug) point to existing workflow files
- v2 stubs clearly indicate planned status
</success_criteria>

<output>
After completion, create `.planning/phases/11-command-reference-cleanup/11-01-SUMMARY.md`
</output>
