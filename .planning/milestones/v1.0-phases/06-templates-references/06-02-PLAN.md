---
phase: 06-templates-references
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [TOOL-04]

must_haves:
  truths:
    - "Every reference and guide file contains zero GSD/get-shit-done strings"
    - "Reference documents that mention commands use /pde: prefix, not /gsd:"
    - "All file types in references/ (md and subdirectories) are covered by audit"
  artifacts:
    - path: "references/"
      provides: "30 reference and guide files with PDE naming"
      contains: "zero matches for gsd|get-shit-done"
  key_links:
    - from: "references/"
      to: "workflow .md files"
      via: "@-reference in execution_context blocks"
      pattern: "/pde: command references consistent between workflows and guides"
---

<objective>
Audit all reference and guide files to verify PDE naming and zero GSD references.

Purpose: Confirm TOOL-04 — that references and guides use PDE naming throughout, with /pde: command prefix where commands are mentioned. Research pre-confirmed zero matches; this plan produces the verified evidence.
Output: Verified clean audit of references/ directory with confirmation that /pde: command references are present and correct.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-templates-references/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive GSD string audit of references/ directory</name>
  <files></files>
  <action>
Run the following grep commands from the project root and record the output as evidence:

1. `grep -rni "gsd\|get-shit-done" references/` — must return zero matches
2. `grep -rni "/gsd:" references/` — must return zero matches (no old command prefix)
3. `grep -rni "~/.gsd\|gsd-tools" references/` — must return zero matches (no old config paths)
4. `grep -rni "get.shit.done" references/` — must return zero matches (dot-separated variant)

Also verify file-type and subdirectory coverage:
5. `find references/ -type f | wc -l` — confirm all ~30 files were in grep scope
6. `find references/ -type d` — confirm subdirectories (e.g., references/techniques/) are included

If ANY grep returns matches: read the file, evaluate context, and fix with targeted replacement. Write the corrected file back. Re-run grep to confirm fix.

If ALL greps return zero matches (expected per research): record the evidence as the deliverable.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep -rnic "gsd\|get-shit-done\|/gsd:\|~/.gsd\|gsd-tools" references/ | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <done>grep audit returns zero GSD matches across all 30+ reference files including subdirectories. Evidence recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Verify /pde: command references are correct in guides</name>
  <files></files>
  <action>
Confirm that reference documents mentioning commands use the correct /pde: prefix:

1. `grep -rni "/pde:" references/` — should find /pde: references in relevant guides (continuation-format.md, tooling-patterns.md, verification-patterns.md, git-integration.md, etc.)

2. Review the output to confirm:
   - Command references use `/pde:` prefix (not `/gsd:`)
   - Command names match actual registered commands (e.g., `/pde:progress`, `/pde:plan-phase`, `/pde:execute-phase`)
   - No broken or misspelled command references

3. Check key reference files that are most likely to contain command references:
   - `references/continuation-format.md`
   - `references/tooling-patterns.md`
   - `references/verification-patterns.md`
   - `references/git-integration.md`
   - `references/planning-config.md`

4. For each file with /pde: references, confirm the commands are valid by cross-referencing against the known command list (34 /pde: commands registered in Phase 3).

Note: Not all reference files will mention commands — design references (typography.md, color-systems.md, etc.) are domain guides with no command references. That is expected and correct.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && echo "GSD_COMMANDS:" && grep -rnic "/gsd:" references/ | grep -v ":0$" | wc -l | tr -d ' ' && echo "PDE_COMMANDS:" && grep -rnic "/pde:" references/ | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <done>Zero /gsd: command references in references/. Where commands are mentioned, /pde: prefix is used correctly. Evidence documented in SUMMARY.</done>
</task>

</tasks>

<verification>
Combined verification: `grep -rnic "gsd\|get-shit-done\|/gsd:\|~/.gsd\|gsd-tools" references/ | grep -v ":0$"` returns empty (zero files with any GSD match).
</verification>

<success_criteria>
- Zero GSD string matches across all reference files (all types, all subdirectories)
- /pde: command references present and correct where commands are mentioned
- Zero /gsd: command references remain
- Evidence documented in SUMMARY for Phase 7 cross-reference
</success_criteria>

<output>
After completion, create `.planning/phases/06-templates-references/06-02-SUMMARY.md`
</output>
