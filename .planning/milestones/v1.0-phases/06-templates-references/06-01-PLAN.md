---
phase: 06-templates-references
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [TOOL-03]

must_haves:
  truths:
    - "Every template file contains zero GSD/get-shit-done strings"
    - "Template-generated .planning/ artifacts would contain zero GSD strings"
    - "JSON template files (config.json, design-manifest.json, etc.) are also GSD-free"
  artifacts:
    - path: "templates/"
      provides: "55 template files with PDE branding"
      contains: "zero matches for gsd|get-shit-done"
  key_links:
    - from: "templates/"
      to: ".planning/ generated files"
      via: "template expansion in workflows"
      pattern: "zero GSD strings in template source guarantees clean output"
---

<objective>
Audit all template files to verify PDE branding and zero GSD references.

Purpose: Confirm TOOL-03 — that templates carry PDE branding with no GSD banners, stage names, or path references. Research pre-confirmed zero matches; this plan produces the verified evidence.
Output: Verified clean audit of templates/ directory across all file types (md, json).
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-templates-references/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive GSD string audit of templates/ directory</name>
  <files></files>
  <action>
Run the following grep commands from the project root and record the output as evidence:

1. `grep -rni "gsd\|get-shit-done" templates/` — must return zero matches
2. `grep -rni "/gsd:" templates/` — must return zero matches (no old command prefix)
3. `grep -rni "~/.gsd\|gsd-tools" templates/` — must return zero matches (no old config paths)
4. `grep -rni "get.shit.done" templates/` — must return zero matches (dot-separated variant)

Also verify file-type coverage:
5. `find templates/ -type f | wc -l` — confirm all ~55 files were in grep scope
6. `find templates/ -name "*.json" -exec grep -li "gsd" {} \;` — explicitly confirm JSON files are covered

If ANY grep returns matches: read the file, evaluate context, and fix with targeted replacement (not blind sed). Write the corrected file back. Re-run grep to confirm fix.

If ALL greps return zero matches (expected per research): record the evidence as the deliverable. This audit confirmation IS the work product.

Additionally, verify PDE branding presence:
7. `grep -rni "pde\|platform.development" templates/` — confirm PDE references exist where branding is expected (note: many templates are generic project scaffolds and may not mention PDE directly — that is fine; the requirement is zero GSD, not mandatory PDE in every file)
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep -rnic "gsd\|get-shit-done\|/gsd:\|~/.gsd\|gsd-tools" templates/ | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <done>grep audit returns zero GSD matches across all 55+ template files including JSON files. Evidence recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Verify template-generated artifacts would be GSD-free</name>
  <files></files>
  <action>
Confirm that the template-to-output chain produces clean artifacts:

1. Read the key template files that generate .planning/ artifacts:
   - `templates/context.md`
   - `templates/milestone.md`
   - `templates/config.json`
   - `templates/VALIDATION.md`
   - `templates/continue-here.md`

2. For each, verify the content contains no GSD strings and uses appropriate PDE naming where branding appears.

3. Check the UI layer that renders banners during template expansion:
   - Confirm `lib/ui/splash.cjs` displays "Platform Development Engine" (not GSD)
   - Confirm `lib/ui/components.cjs` banner function is generic (takes stage name as argument, no hardcoded GSD brand)

4. Since running an actual `/pde:new-project` requires an interactive session, the verification is structural: clean templates + clean UI layer = clean generated output. Document this chain of evidence.

Note per research: banner stage names like "QUESTIONING", "PLANNING PHASE {X}" are passed by workflow .md files (Phase 3 scope, already verified). Templates do not contain banner calls.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep -c "Platform Development Engine" lib/ui/splash.cjs && grep -c "gsd\|GSD\|get-shit-done" lib/ui/splash.cjs lib/ui/components.cjs | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <done>Template-to-output chain verified: key templates contain zero GSD, splash.cjs shows "Platform Development Engine", components.cjs banner is generic. Evidence supports TOOL-03 claim that fresh project output would be GSD-free.</done>
</task>

</tasks>

<verification>
Combined verification: `grep -rnic "gsd\|get-shit-done\|/gsd:\|~/.gsd\|gsd-tools" templates/ | grep -v ":0$"` returns empty (zero files with any GSD match).
</verification>

<success_criteria>
- Zero GSD string matches across all template files (md, json, all types)
- Template-to-output chain confirmed clean (templates + UI layer)
- Evidence documented in SUMMARY for Phase 7 cross-reference
</success_criteria>

<output>
After completion, create `.planning/phases/06-templates-references/06-01-SUMMARY.md`
</output>
