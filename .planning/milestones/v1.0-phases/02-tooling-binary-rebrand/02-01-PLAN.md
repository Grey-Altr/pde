---
phase: 02-tooling-binary-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/pde-tools.cjs
  - bin/lib/commands.cjs
  - bin/lib/config.cjs
  - bin/lib/core.cjs
  - bin/lib/frontmatter.cjs
  - bin/lib/init.cjs
  - bin/lib/milestone.cjs
  - bin/lib/phase.cjs
  - bin/lib/roadmap.cjs
  - bin/lib/state.cjs
  - bin/lib/template.cjs
  - bin/lib/verify.cjs
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-05
  - TOOL-06

must_haves:
  truths:
    - "bin/pde-tools.cjs exists in the project repo and runs without crashing"
    - "No GSD or get-shit-done strings exist anywhere in bin/"
    - "Config system references ~/.pde/ for global defaults and Brave API key"
    - "Git branch templates use pde/ prefix in both core.cjs and config.cjs"
    - "Plugin still validates after adding bin/ directory"
  artifacts:
    - path: "bin/pde-tools.cjs"
      provides: "Main CLI entry point for PDE tooling"
      contains: "pde-tools"
    - path: "bin/lib/core.cjs"
      provides: "Shared utilities, MODEL_PROFILES with pde-* keys, branch templates with pde/ prefix"
      contains: "pde/phase-"
    - path: "bin/lib/config.cjs"
      provides: "Config system reading from ~/.pde/defaults.json and ~/.pde/brave_api_key"
      contains: ".pde/defaults.json"
    - path: "bin/lib/init.cjs"
      provides: "Workflow bootstrapping with pde-* agent names"
      contains: "pde-"
  key_links:
    - from: "bin/pde-tools.cjs"
      to: "bin/lib/*.cjs"
      via: "require('./lib/core.cjs') etc."
      pattern: "require.*\\./lib/"
    - from: "bin/lib/config.cjs"
      to: "~/.pde/defaults.json"
      via: "fs.readFileSync path construction"
      pattern: "\\.pde/defaults\\.json"
    - from: "bin/lib/core.cjs"
      to: "branch template defaults"
      via: "hardcoded loadConfig defaults"
      pattern: "pde/phase-\\{phase\\}"
---

<objective>
Copy the already-rebranded PDE binary tooling from the reference installation at ~/.claude/pde/bin/ into the project repository at bin/, set correct permissions, and verify all four TOOL requirements are satisfied.

Purpose: Phase 2 establishes the bin/ directory in the PDE plugin repo. The reference copy at ~/.claude/pde/bin/ has already been fully rebranded (confirmed by research grep audit). This plan copies those clean files into the project repo and runs a comprehensive verification suite.

Output: bin/pde-tools.cjs (executable) + bin/lib/ (11 module files), all verified clean of GSD references.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tooling-binary-rebrand/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy PDE bin files into project repo</name>
  <files>bin/pde-tools.cjs, bin/lib/commands.cjs, bin/lib/config.cjs, bin/lib/core.cjs, bin/lib/frontmatter.cjs, bin/lib/init.cjs, bin/lib/milestone.cjs, bin/lib/phase.cjs, bin/lib/roadmap.cjs, bin/lib/state.cjs, bin/lib/template.cjs, bin/lib/verify.cjs</files>
  <action>
    1. Create the bin/lib/ directory in the project repo: `mkdir -p bin/lib`
    2. Copy pde-tools.cjs from the reference installation: `cp ~/.claude/pde/bin/pde-tools.cjs bin/pde-tools.cjs`
    3. Copy all 11 lib files: `cp ~/.claude/pde/bin/lib/*.cjs bin/lib/`
    4. Set executable permission on the entry point: `chmod +x bin/pde-tools.cjs`
    5. Do NOT copy `~/.claude/pde/bin/pde` — that is a launcher script for a separate pde-os project and is not part of the GSD-equivalent plugin tooling.
    6. Verify the copy is complete: `ls -la bin/pde-tools.cjs bin/lib/` should show pde-tools.cjs with executable bit and 11 .cjs files in lib/.
  </action>
  <verify>
    <automated>test -f bin/pde-tools.cjs && test -x bin/pde-tools.cjs && test $(ls bin/lib/*.cjs | wc -l) -eq 11 && echo PASS || echo FAIL</automated>
  </verify>
  <done>bin/pde-tools.cjs exists with executable permission, bin/lib/ contains exactly 11 .cjs module files, no pde launcher script is present</done>
</task>

<task type="auto">
  <name>Task 2: Verify all TOOL requirements and run full audit</name>
  <files>bin/pde-tools.cjs, bin/lib/core.cjs, bin/lib/config.cjs</files>
  <action>
    Run the complete Phase 2 verification suite. Each check maps to a specific requirement. If ANY check fails, investigate the specific file and fix the issue before proceeding.

    TOOL-01 checks:
    1. `test -f bin/pde-tools.cjs && echo PASS || echo FAIL` — file exists
    2. `node bin/pde-tools.cjs 2>&1 | grep -q "pde-tools" && echo PASS || echo FAIL` — runs and identifies itself correctly

    TOOL-02 check:
    3. `grep -rn "\.gsd" bin/ && echo FAIL || echo PASS` — no .gsd path references anywhere in bin/

    TOOL-05 checks:
    4. `grep -q "\.pde/defaults.json" bin/lib/config.cjs && echo PASS || echo FAIL` — config reads ~/.pde/defaults.json
    5. `grep -q "\.pde/brave_api_key" bin/lib/config.cjs && echo PASS || echo FAIL` — config reads ~/.pde/brave_api_key

    TOOL-06 checks:
    6. `grep -q "pde/phase-" bin/lib/core.cjs && echo PASS || echo FAIL` — branch template in core.cjs
    7. `grep -q "pde/phase-" bin/lib/config.cjs && echo PASS || echo FAIL` — branch template in config.cjs

    Regression check:
    8. `grep -rni "gsd\|get-shit-done" bin/ && echo FAIL || echo PASS` — zero GSD strings anywhere

    Integration check:
    9. `claude plugin validate .` — plugin still validates after adding bin/

    Also investigate the open question from research: run `grep "gsd_state_version" bin/lib/state.cjs` to determine if the gsd_state_version frontmatter key is exposed in output. If it is rendered to users, note it as a finding for Phase 7. If it is purely internal parsing, no action needed.

    All 9 checks must show PASS. If any shows FAIL, fix the offending file and re-run the failing check.
  </action>
  <verify>
    <automated>test -f bin/pde-tools.cjs && node bin/pde-tools.cjs 2>&1 | grep -q "pde-tools" && ! grep -rqn "\.gsd" bin/ && grep -q "\.pde/defaults.json" bin/lib/config.cjs && grep -q "\.pde/brave_api_key" bin/lib/config.cjs && grep -q "pde/phase-" bin/lib/core.cjs && grep -q "pde/phase-" bin/lib/config.cjs && ! grep -rqni "gsd\|get-shit-done" bin/ && echo "ALL PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 9 verification checks pass: pde-tools.cjs exists and runs, no .gsd references in bin/, config uses ~/.pde/ paths, branch templates use pde/ prefix, zero GSD strings in bin/, plugin validates cleanly</done>
</task>

</tasks>

<verification>
Run the full verification suite as a single compound command from the project root:

```bash
echo "=== TOOL-01: File exists ===" && test -f bin/pde-tools.cjs && echo PASS || echo FAIL
echo "=== TOOL-01: Runs correctly ===" && node bin/pde-tools.cjs 2>&1 | grep -q "pde-tools" && echo PASS || echo FAIL
echo "=== TOOL-02: No .gsd refs ===" && grep -rn "\.gsd" bin/ && echo FAIL || echo PASS
echo "=== TOOL-05: Config defaults.json ===" && grep -q "\.pde/defaults.json" bin/lib/config.cjs && echo PASS || echo FAIL
echo "=== TOOL-05: Config brave_api_key ===" && grep -q "\.pde/brave_api_key" bin/lib/config.cjs && echo PASS || echo FAIL
echo "=== TOOL-06: Branch template core ===" && grep -q "pde/phase-" bin/lib/core.cjs && echo PASS || echo FAIL
echo "=== TOOL-06: Branch template config ===" && grep -q "pde/phase-" bin/lib/config.cjs && echo PASS || echo FAIL
echo "=== REGRESSION: Zero GSD strings ===" && grep -rni "gsd\|get-shit-done" bin/ && echo FAIL || echo PASS
echo "=== INTEGRATION: Plugin validates ===" && claude plugin validate .
```

All lines must show PASS. Plugin validate must exit 0.
</verification>

<success_criteria>
- bin/pde-tools.cjs is executable and runs (prints usage info mentioning "pde-tools")
- bin/lib/ contains exactly 11 .cjs module files
- Zero occurrences of "gsd", "GSD", or "get-shit-done" anywhere in bin/
- bin/lib/config.cjs references ~/.pde/defaults.json and ~/.pde/brave_api_key
- bin/lib/core.cjs and bin/lib/config.cjs both contain pde/ branch template prefix
- claude plugin validate . continues to exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-tooling-binary-rebrand/02-01-SUMMARY.md`
</output>
