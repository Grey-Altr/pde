---
phase: 189-technical-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/execute-phase.md
  - workflows/complete-milestone.md
  - knip.json
  - .jscpd.json
  - .planning/phases/189-technical-debt-cleanup/189-knip-report.md
  - .planning/phases/189-technical-debt-cleanup/jscpd-report/jscpd-report.json
  - .planning/phases/189-technical-debt-cleanup/189-jscpd-triage.md
autonomous: true
requirements: [DEB-01, DEB-02, DEB-03]

must_haves:
  truths:
    - "execute-phase.md and complete-milestone.md no longer contain stale pde-os path for pde-tools.cjs"
    - "knip dead-code report exists with every finding triaged as keep/remove/defer"
    - "jscpd duplication report exists with clone blocks triaged"
  artifacts:
    - path: "workflows/execute-phase.md"
      provides: "Corrected pde-tools.cjs path references"
      contains: "CLAUDE_PLUGIN_ROOT.*pde-tools"
    - path: "workflows/complete-milestone.md"
      provides: "Corrected pde-tools.cjs path references"
      contains: "CLAUDE_PLUGIN_ROOT.*pde-tools"
    - path: "knip.json"
      provides: "knip configuration scoping entry points and ignoring non-production dirs"
      contains: "ignoreExportsUsedInFile"
    - path: ".jscpd.json"
      provides: "jscpd configuration for source-only duplication scan"
      contains: "minLines"
    - path: ".planning/phases/189-technical-debt-cleanup/189-knip-report.md"
      provides: "Dead-code triage table"
      contains: "keep\\|remove\\|defer"
    - path: ".planning/phases/189-technical-debt-cleanup/jscpd-report/jscpd-report.json"
      provides: "Raw jscpd JSON output"
    - path: ".planning/phases/189-technical-debt-cleanup/189-jscpd-triage.md"
      provides: "Duplication triage commentary"
      contains: "accept\\|refactor-candidate\\|defer"
  key_links:
    - from: "knip.json"
      to: "npx knip"
      via: "knip reads config from project root"
      pattern: "entry.*bin/pde-tools"
    - from: ".jscpd.json"
      to: "npx jscpd"
      via: "jscpd reads config from project root"
      pattern: "path.*bin.*lib.*packages"
---

<objective>
Fix stale workflow paths (DEB-01) and produce dead-code + duplication triage reports (DEB-02, DEB-03).

Purpose: Correct 4 stale `pde-tools.cjs` path references in workflow files, run knip and jscpd with proper configs, and commit triaged reports as tracked artifacts for future milestones.
Output: Fixed workflow files, `knip.json`, `.jscpd.json`, knip triage report, jscpd report + triage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-technical-debt-cleanup/189-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale pde-tools.cjs paths in workflow files (DEB-01)</name>
  <files>workflows/execute-phase.md, workflows/complete-milestone.md</files>
  <read_first>workflows/execute-phase.md (lines 755-775), workflows/complete-milestone.md (lines 689-710)</read_first>
  <action>
Replace exactly 4 stale path references across two files. Use sed to perform the replacement:

```bash
sed -i '' 's|\$HOME/\.claude/pde-os/engines/gsd/bin/pde-tools\.cjs|\${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs|g' workflows/execute-phase.md workflows/complete-milestone.md
```

The stale lines are:
- execute-phase.md line 760: `$HOME/.claude/pde-os/engines/gsd/bin/pde-tools.cjs` in AUTO_GENERATE assignment
- execute-phase.md line 769: `$HOME/.claude/pde-os/engines/gsd/bin/pde-tools.cjs` in PERSONAS_JSON assignment
- complete-milestone.md line 694: same pattern in AUTO_GENERATE assignment
- complete-milestone.md line 703: same pattern in PERSONAS_JSON assignment

CRITICAL: Do NOT touch any `gsd-tools.cjs` references. Both files contain many correct `pde-os/.../gsd-tools.cjs` paths. The sed pattern specifically matches `pde-tools.cjs` only.

After replacement, verify:
1. `grep "pde-os.*pde-tools" workflows/execute-phase.md` returns empty (0 matches)
2. `grep "pde-os.*pde-tools" workflows/complete-milestone.md` returns empty (0 matches)
3. `grep -c "CLAUDE_PLUGIN_ROOT.*pde-tools" workflows/execute-phase.md` returns 3 (2 fixed + 1 existing)
4. `grep -c "CLAUDE_PLUGIN_ROOT.*pde-tools" workflows/complete-milestone.md` returns 3 (2 fixed + 1 existing)
5. `grep -c "pde-os.*gsd-tools" workflows/execute-phase.md` returns 26 (unchanged)
  </action>
  <verify>
    <automated>grep "pde-os.*pde-tools" workflows/execute-phase.md workflows/complete-milestone.md | wc -l | tr -d ' ' | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
- `grep "pde-os.*pde-tools" workflows/execute-phase.md` returns 0 matches
- `grep "pde-os.*pde-tools" workflows/complete-milestone.md` returns 0 matches
- `grep -c "CLAUDE_PLUGIN_ROOT.*pde-tools" workflows/execute-phase.md` returns exactly 3
- `grep -c "CLAUDE_PLUGIN_ROOT.*pde-tools" workflows/complete-milestone.md` returns exactly 3
- All `gsd-tools.cjs` references remain unchanged
  </acceptance_criteria>
  <done>All 4 stale pde-tools.cjs paths replaced with ${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs. No gsd-tools.cjs references modified.</done>
</task>

<task type="auto">
  <name>Task 2: Run knip dead-code analysis and produce triage report (DEB-02)</name>
  <files>knip.json, .planning/phases/189-technical-debt-cleanup/189-knip-report.md</files>
  <read_first>package.json (to see current dependencies), .planning/phases/189-technical-debt-cleanup/189-RESEARCH.md (DEB-02 section for config and pre-classified triage)</read_first>
  <action>
Step 1: Create `knip.json` at project root with this exact content:

```json
{
  "entry": ["bin/pde-tools.cjs", "bin/*.cjs", "packages/*/index.cjs", "packages/*/discover.cjs", "packages/*/handlers.cjs"],
  "project": ["bin/**/*.cjs", "lib/**/*.cjs", "packages/**/*.cjs", "packages/**/*.ts"],
  "ignore": [
    ".planning/**",
    "dashboard/**",
    "coverage/**",
    ".claude/**",
    ".sessions/**",
    ".tmp-git-commit.mjs"
  ],
  "ignoreDependencies": ["@fontsource/inter", "@gltf-transform/cli", "@gltf-transform/core", "htm", "inter-ui"],
  "ignoreExportsUsedInFile": true
}
```

Step 2: Run `npx knip --reporter compact 2>&1` and capture output.

Step 3: Create `.planning/phases/189-technical-debt-cleanup/189-knip-report.md` with:
- Header: "# Knip Dead-Code Report — Phase 189"
- Date and knip version
- Summary counts (unused files, unused exports, unlisted deps, unused deps)
- Triage table with columns: Finding | Category | Classification | Reason

Pre-classified triage from research (use as baseline, update with actual run output):
- `bin/lib/video-pipeline/remotion/BrandedVideo.tsx` → defer (Remotion not yet integrated)
- `bin/lib/idle-suggestions.cjs` → defer (may be used in future milestones)
- `bin/lib/event-bus.cjs` → keep (runtime dynamic require)
- `bin/lib/commands.cjs` → keep (dynamic dispatch)
- `.tmp-git-commit.mjs` → remove (orphaned temp file)
- Unused exports in `bin/lib/*.cjs` → defer (CLI boundary, external callers)
- `@fontsource/inter`, `htm`, etc. → defer (dashboard sub-app deps)
- `ai` devDependency → keep (vitest tests)
- `node-fetch`, `node-ssh` → defer (unlisted runtime deps, future hygiene)

Each row must have one of: `keep`, `remove`, `defer`.
  </action>
  <verify>
    <automated>test -f knip.json && node -e "const k=require('./knip.json'); if(!k.entry || !k.ignore || !k.ignoreExportsUsedInFile) process.exit(1)" && test -f .planning/phases/189-technical-debt-cleanup/189-knip-report.md && grep -c "keep\|remove\|defer" .planning/phases/189-technical-debt-cleanup/189-knip-report.md | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
- `knip.json` exists at project root with `entry`, `project`, `ignore`, `ignoreDependencies`, `ignoreExportsUsedInFile` fields
- `189-knip-report.md` exists in phase directory
- Report contains triage table with every finding classified as `keep`, `remove`, or `defer`
- Running `npx knip` with the config does not error out
  </acceptance_criteria>
  <done>knip.json config committed. Knip report with full triage table committed as tracked artifact. Every finding classified.</done>
</task>

<task type="auto">
  <name>Task 3: Run jscpd duplication scan and produce triage report (DEB-03)</name>
  <files>.jscpd.json, .planning/phases/189-technical-debt-cleanup/jscpd-report/jscpd-report.json, .planning/phases/189-technical-debt-cleanup/189-jscpd-triage.md</files>
  <read_first>.planning/phases/189-technical-debt-cleanup/189-RESEARCH.md (DEB-03 section for config)</read_first>
  <action>
Step 1: Create `.jscpd.json` at project root with this exact content:

```json
{
  "threshold": 0,
  "minLines": 10,
  "minTokens": 100,
  "reporters": ["json", "console"],
  "output": ".planning/phases/189-technical-debt-cleanup/jscpd-report",
  "ignore": [
    "**/.planning/**",
    "**/node_modules/**",
    "**/coverage/**",
    "**/dashboard/.next/**",
    "**/.claude/**",
    "**/*.md",
    "**/*.json",
    "**/*.sh"
  ],
  "path": ["bin", "lib", "packages"]
}
```

Step 2: Create the output directory: `mkdir -p .planning/phases/189-technical-debt-cleanup/jscpd-report`

Step 3: Run `npx jscpd 2>&1` and capture output. jscpd reads `.jscpd.json` automatically and writes `jscpd-report.json` to the configured output directory.

Step 4: Create `.planning/phases/189-technical-debt-cleanup/189-jscpd-triage.md` with:
- Header: "# jscpd Duplication Report Triage — Phase 189"
- Date and jscpd version
- Summary: total clones found, total duplicated lines, percentage
- Triage table with columns: Clone | File A | File B | Lines | Classification | Reason
- Classifications: `accept` (expected shared pattern), `refactor-candidate` (worth extracting), `defer` (low priority)

If clone count exceeds 100, raise minLines to 15 in `.jscpd.json` and re-run.
  </action>
  <verify>
    <automated>test -f .jscpd.json && test -f .planning/phases/189-technical-debt-cleanup/jscpd-report/jscpd-report.json && test -f .planning/phases/189-technical-debt-cleanup/189-jscpd-triage.md && grep -c "accept\|refactor-candidate\|defer" .planning/phases/189-technical-debt-cleanup/189-jscpd-triage.md | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
- `.jscpd.json` exists at project root with `threshold`, `minLines`, `minTokens`, `path`, `ignore` fields
- `jscpd-report.json` exists in the phase jscpd-report directory
- `189-jscpd-triage.md` exists in phase directory
- Triage file contains every clone block classified as `accept`, `refactor-candidate`, or `defer`
  </acceptance_criteria>
  <done>jscpd config committed. JSON report and triage markdown committed as tracked artifacts. Every clone block classified.</done>
</task>

</tasks>

<verification>
1. `grep "pde-os.*pde-tools" workflows/execute-phase.md workflows/complete-milestone.md` returns empty
2. `npx knip --reporter compact` runs without error using knip.json config
3. `npx jscpd` runs without error using .jscpd.json config
4. All three report/triage files exist in phase directory
5. `npx vitest run` — existing test suite remains green
</verification>

<success_criteria>
- DEB-01: Zero stale pde-tools.cjs paths remain; CLAUDE_PLUGIN_ROOT references correct
- DEB-02: knip.json config + 189-knip-report.md triage committed
- DEB-03: .jscpd.json config + jscpd-report.json + 189-jscpd-triage.md committed
- Existing test suite unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/189-technical-debt-cleanup/189-01-SUMMARY.md`
</output>
