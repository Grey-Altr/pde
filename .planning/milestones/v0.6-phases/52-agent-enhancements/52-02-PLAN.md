---
phase: 52-agent-enhancements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/assumptions.md
  - workflows/list-phase-assumptions.md
  - workflows/plan-phase.md
autonomous: true
requirements:
  - AGNT-01

must_haves:
  truths:
    - "User can invoke /pde:assumptions {phase} and see structured assumptions across 5 areas"
    - "plan-phase.md runs assumptions gate before spawning planner (Step 7.6)"
    - "Assumptions gate is skippable with --skip-assumptions or --auto flags"
    - "User corrections from assumptions gate are passed as additional planner context"
  artifacts:
    - path: "commands/assumptions.md"
      provides: "/pde:assumptions command routing"
      contains: "list-phase-assumptions"
    - path: "workflows/list-phase-assumptions.md"
      provides: "Structured assumptions output format"
      contains: "## Assumptions for Phase"
    - path: "workflows/plan-phase.md"
      provides: "Assumptions gate step before planner spawn"
      contains: "7.6. Assumptions Gate"
  key_links:
    - from: "commands/assumptions.md"
      to: "workflows/list-phase-assumptions.md"
      via: "command dispatches to workflow"
      pattern: "list-phase-assumptions"
    - from: "workflows/plan-phase.md"
      to: "workflows/list-phase-assumptions.md"
      via: "inline assumptions analysis before planner spawn"
      pattern: "assumptions"
---

<objective>
Create /pde:assumptions as a standalone command (alias to restructured list-phase-assumptions) and integrate as a gate step in plan-phase.md before the planner spawns.

Purpose: Users surface and correct planner assumptions before plans are written, preventing wasted planning cycles from wrong assumptions.
Output: commands/assumptions.md, updated list-phase-assumptions.md, updated plan-phase.md with Step 7.6
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-agent-enhancements/52-RESEARCH.md
</context>

<interfaces>
<!-- Existing command file pattern (from commands/list-phase-assumptions.md): -->
From commands/list-phase-assumptions.md:
```markdown
# /pde:list-phase-assumptions
Workflow: workflows/list-phase-assumptions.md
```

From workflows/plan-phase.md Step 7.5 (current — Nyquist check):
```markdown
## 7.5. Verify Nyquist Artifacts
Skip if `nyquist_validation_enabled` is false OR `research_enabled` is false.
...
## 8. Spawn pde-planner Agent
```

From workflows/plan-phase.md Step 8 planner prompt (lines 360-380):
```markdown
<planning_context>
**Phase:** {phase_number}
**Mode:** {standard | gap_closure}
<files_to_read>
- .planning/project-context.md
- {state_path}
- {roadmap_path}
...
</files_to_read>
**Phase requirement IDs:** {phase_req_ids}
</planning_context>
```
</interfaces>

<acceptance_criteria>
- AC-1: Given the user runs /pde:assumptions 52, When the command resolves, Then it dispatches to workflows/list-phase-assumptions.md
- AC-2: Given list-phase-assumptions.md runs, When it completes analysis, Then the output includes structured headings "### Technical Approach", "### Implementation Order", "### Scope Boundaries", "### Risk Areas", "### Dependencies" with confidence markers
- AC-3: Given plan-phase.md runs without --skip-assumptions and without --auto, When Step 7.6 executes, Then assumptions are presented via AskUserQuestion and user must confirm or correct before Step 8
- AC-4: Given plan-phase.md runs with --skip-assumptions OR --auto, When Step 7.6 is reached, Then it is skipped entirely and proceeds to Step 8
- AC-5: Given the user corrects assumptions in Step 7.6, When the planner spawns in Step 8, Then the corrections appear in the planner prompt inside an <assumptions_context> block
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create /pde:assumptions command and update list-phase-assumptions structured output</name>
  <files>commands/assumptions.md, workflows/list-phase-assumptions.md</files>
  <read_first>
    - commands/list-phase-assumptions.md (existing command file — understand routing pattern)
    - workflows/list-phase-assumptions.md (full workflow — current implementation to enhance)
    - commands/discuss-phase.md (command file pattern reference)
  </read_first>
  <action>
    1. Create commands/assumptions.md as a new command file:
    ```markdown
    # /pde:assumptions

    Surface planner assumptions about a phase before planning begins.

    **Usage:** `/pde:assumptions [phase-number]`

    **Workflow:** workflows/list-phase-assumptions.md

    **Arguments:**
    - `phase-number` — Phase to analyze (required)

    **Examples:**
    - `/pde:assumptions 52` — Surface assumptions for Phase 52

    **Related:**
    - `/pde:list-phase-assumptions` — Same workflow (alias)
    - `/pde:plan-phase` — Includes assumptions gate (Step 7.6)
    ```

    2. Update workflows/list-phase-assumptions.md — modify the present_assumptions step to output in a structured, parseable format. The current format is already close but needs:
    - Each assumption within a section prefixed with a confidence marker: `[confident]`, `[assuming]`, or `[unclear]`
    - After the "What do you think?" prompt, capture user corrections into a structured format:
      ```
      ## Corrections
      - [area]: [correction text]
      ```
    - Add a new final section to the gather_feedback step that produces a machine-readable summary block when called with `--structured` flag (for plan-phase integration):
      ```
      <assumptions_result>
      status: confirmed|corrected
      corrections:
        - area: [Technical Approach|Implementation Order|Scope Boundaries|Risk Areas|Dependencies]
          original: [assumption text]
          correction: [user correction text]
      </assumptions_result>
      ```
    - Keep ALL existing behavior intact — the --structured flag is additive only
    - Update the offer_next step to include `/pde:assumptions` alongside `/pde:list-phase-assumptions`
  </action>
  <ac_refs>AC-1, AC-2</ac_refs>
  <verify>
    <automated>test -f commands/assumptions.md && grep -q "list-phase-assumptions" commands/assumptions.md && grep -q "confident\|assuming\|unclear" workflows/list-phase-assumptions.md && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - commands/assumptions.md exists and contains "workflows/list-phase-assumptions.md"
    - commands/assumptions.md contains "Usage:" and "phase-number"
    - workflows/list-phase-assumptions.md contains confidence markers: "[confident]", "[assuming]", "[unclear]"
    - workflows/list-phase-assumptions.md contains "<assumptions_result>" block in gather_feedback step
    - workflows/list-phase-assumptions.md contains "status: confirmed|corrected"
  </acceptance_criteria>
  <done>
    /pde:assumptions command file exists routing to list-phase-assumptions workflow; workflow outputs structured assumptions with confidence markers and machine-readable result block
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate assumptions gate as Step 7.6 in plan-phase.md</name>
  <files>workflows/plan-phase.md</files>
  <read_first>
    - workflows/plan-phase.md (full file — understand step numbering, planner spawn prompt at Step 8)
    - workflows/list-phase-assumptions.md (updated workflow from Task 1 — understand structured output format)
  </read_first>
  <action>
    Insert a new Step 7.6 between the existing Step 7.5 (Verify Nyquist Artifacts) and Step 8 (Spawn pde-planner Agent) in workflows/plan-phase.md.

    Add `--skip-assumptions` to the flag list in Step 2 (Parse and Normalize Arguments). The existing flags line reads:
    ```
    Extract from $ARGUMENTS: phase number (integer or decimal like `2.1`), flags (`--research`, `--skip-research`, `--gaps`, `--skip-verify`, `--prd <filepath>`).
    ```
    Update to:
    ```
    Extract from $ARGUMENTS: phase number (integer or decimal like `2.1`), flags (`--research`, `--skip-research`, `--gaps`, `--skip-verify`, `--skip-assumptions`, `--prd <filepath>`).
    ```

    Insert the following Step 7.6:

    ```markdown
    ## 7.6. Assumptions Gate

    Skip if: `--skip-assumptions` flag OR `--auto` mode OR `--gaps` mode OR `--prd` mode.

    Display banner:
    ```bash
    node "${CLAUDE_PLUGIN_ROOT}/lib/ui/render.cjs" banner "SURFACING ASSUMPTIONS"
    ```

    Run the assumptions analysis inline (same session, not as a subagent):

    1. Read ROADMAP.md phase section for phase goal, requirements, success criteria
    2. Read REQUIREMENTS.md for the phase requirement IDs ({phase_req_ids})
    3. Read CONTEXT.md if it exists ({context_path})
    4. Read RESEARCH.md if it exists ({research_path})

    Generate structured assumptions across 5 areas following the same analysis as list-phase-assumptions.md:
    - **Technical Approach** — libraries, patterns, tools
    - **Implementation Order** — what gets built first/second/third
    - **Scope Boundaries** — in scope, out of scope, ambiguous
    - **Risk Areas** — complexity, challenges, gotchas
    - **Dependencies** — what must exist, external needs

    Each assumption prefixed with confidence marker: [confident], [assuming], or [unclear].

    Present via AskUserQuestion:
    - header: "Phase {phase_number} Assumptions"
    - question: "Review these assumptions. Confirm if accurate, or describe corrections."
    - options:
      - "Confirmed — proceed to planning" — Assumptions accepted as-is
      - "Corrections below" — User will type corrections in next message

    **If confirmed:** Set ASSUMPTIONS_CONTEXT = null. Proceed to Step 8.

    **If corrections:** Capture the user's corrections. Build an assumptions context block:
    ```
    <assumptions_context>
    The user reviewed planner assumptions and provided these corrections:
    {user corrections text}

    Honor these corrections when creating the plan. They override any assumptions
    the planner would otherwise make.
    </assumptions_context>
    ```
    Set ASSUMPTIONS_CONTEXT = the block above. Proceed to Step 8.
    ```

    Then modify Step 8 (Spawn pde-planner Agent) to inject ASSUMPTIONS_CONTEXT. In the planner prompt, after the closing `</planning_context>` tag and before the `<downstream_consumer>` tag, add:

    ```markdown
    {If ASSUMPTIONS_CONTEXT is not null:}
    {ASSUMPTIONS_CONTEXT}
    ```

    This ensures corrections flow directly into the planner's context without requiring a separate file.
  </action>
  <ac_refs>AC-3, AC-4, AC-5</ac_refs>
  <verify>
    <automated>grep -q "7.6. Assumptions Gate" workflows/plan-phase.md && grep -q "skip-assumptions" workflows/plan-phase.md && grep -q "assumptions_context" workflows/plan-phase.md && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - workflows/plan-phase.md contains "## 7.6. Assumptions Gate"
    - workflows/plan-phase.md contains "--skip-assumptions" in Step 2 flags list
    - workflows/plan-phase.md Step 7.6 contains "Skip if:" with "--skip-assumptions", "--auto", "--gaps", "--prd"
    - workflows/plan-phase.md Step 7.6 contains "AskUserQuestion" for presenting assumptions
    - workflows/plan-phase.md Step 7.6 contains "<assumptions_context>" block construction
    - workflows/plan-phase.md Step 8 contains "ASSUMPTIONS_CONTEXT" injection point after </planning_context>
    - Step 7.6 appears AFTER Step 7.5 and BEFORE Step 8
  </acceptance_criteria>
  <done>
    plan-phase.md has Step 7.6 that surfaces assumptions before planner spawn, skippable with flags, corrections injected into planner prompt
  </done>
</task>

</tasks>

<verification>
- `test -f commands/assumptions.md` — command file exists
- `grep -q "7.6. Assumptions Gate" workflows/plan-phase.md` — gate step exists
- `grep -q "skip-assumptions" workflows/plan-phase.md` — skip flag recognized
- `grep -q "assumptions_context" workflows/plan-phase.md` — corrections injected into planner
- `grep -c "confident\|assuming\|unclear" workflows/list-phase-assumptions.md` returns >= 3
</verification>

<success_criteria>
- /pde:assumptions command exists and routes to list-phase-assumptions workflow
- list-phase-assumptions produces structured output with confidence markers
- plan-phase.md Step 7.6 surfaces assumptions before planner spawn
- Assumptions gate skippable with --skip-assumptions, --auto, --gaps, --prd
- User corrections flow into planner prompt via <assumptions_context> block
</success_criteria>

<output>
After completion, create `.planning/phases/52-agent-enhancements/52-02-SUMMARY.md`
</output>
