---
phase: 62-workflow-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/62-workflow-instrumentation/validate-instrumentation.sh
  - workflows/execute-phase.md
  - workflows/execute-plan.md
autonomous: true
requirements: [EVNT-04]

must_haves:
  truths:
    - "Starting a phase execution produces a phase_started event in the NDJSON stream"
    - "Completing a phase execution produces a phase_complete event in the NDJSON stream"
    - "Each wave produces wave_started and wave_complete events with wave number"
    - "Each plan produces plan_started and plan_complete events with plan_id"
    - "All event-emit calls are fire-and-forget — workflow execution never fails due to event emission"
  artifacts:
    - path: "workflows/execute-phase.md"
      provides: "phase_started, phase_complete, wave_started, wave_complete event emissions"
      contains: "event-emit phase_started"
    - path: "workflows/execute-plan.md"
      provides: "plan_started, plan_complete event emissions"
      contains: "event-emit plan_started"
    - path: ".planning/phases/62-workflow-instrumentation/validate-instrumentation.sh"
      provides: "8-check Nyquist validation suite"
      contains: "check()"
  key_links:
    - from: "workflows/execute-phase.md"
      to: "bin/pde-tools.cjs event-emit"
      via: "node invocation with 2>/dev/null || true"
      pattern: "event-emit phase_started.*|| true"
    - from: "workflows/execute-plan.md"
      to: "bin/pde-tools.cjs event-emit"
      via: "node invocation with 2>/dev/null || true"
      pattern: "event-emit plan_started.*|| true"
    - from: "bin/pane-pipeline-progress.sh"
      to: "NDJSON event stream"
      via: "tail -F with jq parsing"
      pattern: "phase_started|wave_started|plan_started"
---

<objective>
Add surgical event-emit calls to execute-phase.md (4 calls: phase_started, phase_complete, wave_started, wave_complete) and execute-plan.md (2 calls: plan_started, plan_complete), plus create the validation script.

Purpose: Enable the dashboard pipeline progress pane and session summaries to display real-time workflow progress. The pane already handles all six event types — it just needs the events to be emitted.
Output: Instrumented workflow files + validate-instrumentation.sh (8 checks)
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-workflow-instrumentation/62-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create validate-instrumentation.sh (Wave 0 Nyquist script)</name>
  <files>.planning/phases/62-workflow-instrumentation/validate-instrumentation.sh</files>
  <read_first>
    - .planning/phases/61-token-context-metering/validate-metering.sh (pattern reference: check() helper, PASS_COUNT/FAIL_COUNT, set -uo pipefail, --quick flag, exit logic)
    - .planning/phases/62-workflow-instrumentation/62-RESEARCH.md (validation architecture section: EVNT-04-A through EVNT-04-H check definitions)
  </read_first>
  <action>
Create `.planning/phases/62-workflow-instrumentation/validate-instrumentation.sh` following the exact pattern from validate-metering.sh (Phase 61).

Structure:
- Shebang: `#!/usr/bin/env bash`
- Comment header: "Phase 62 — Workflow Instrumentation Validation" referencing EVNT-04
- `set -uo pipefail`
- PASS_COUNT=0, FAIL_COUNT=0
- SCRIPT_DIR + PROJECT_ROOT resolution: `PROJECT_ROOT="$(cd "$SCRIPT_DIR/../../.." && pwd)"`
- --quick flag parsing
- check() helper function (identical to Phase 61 pattern)

Eight checks:

EVNT04-A: `grep -c 'event-emit phase_started' "$PROJECT_ROOT/workflows/execute-phase.md"` — expect >= 1
EVNT04-B: `grep -c 'event-emit phase_complete' "$PROJECT_ROOT/workflows/execute-phase.md"` — expect >= 1
EVNT04-C: `grep -c 'event-emit wave_started' "$PROJECT_ROOT/workflows/execute-phase.md"` — expect >= 1
EVNT04-D: `grep -c 'event-emit wave_complete' "$PROJECT_ROOT/workflows/execute-phase.md"` — expect >= 1
EVNT04-E: `grep -c 'event-emit plan_started' "$PROJECT_ROOT/workflows/execute-plan.md"` — expect >= 1 AND `grep -c 'event-emit plan_complete' "$PROJECT_ROOT/workflows/execute-plan.md"` — expect >= 1
EVNT04-F: archive-session.cjs contains `phaseEvents` (aggregation of phase/wave events) — `grep -q 'phaseEvents' "$PROJECT_ROOT/hooks/archive-session.cjs"` (this will fail initially — expected, fixed by Plan 02)
EVNT04-G: archive-session.cjs renders phase progress dynamically — `grep -q 'renderPhaseProgress\|phase_started.*phase_complete' "$PROJECT_ROOT/hooks/archive-session.cjs"` (fails initially — fixed by Plan 02)
EVNT04-H: All event-emit calls in execute-phase.md use `|| true` — `grep 'event-emit' "$PROJECT_ROOT/workflows/execute-phase.md" | grep -v '|| true' | wc -l` — expect 0

For --quick mode: run only EVNT04-A through EVNT04-E (the static analysis checks on workflow files).

Final summary: echo total PASS/FAIL counts, exit 0 if FAIL_COUNT=0, exit 1 otherwise.
  </action>
  <verify>
    <automated>bash ".planning/phases/62-workflow-instrumentation/validate-instrumentation.sh" --quick 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - validate-instrumentation.sh exists and is executable (has shebang)
    - validate-instrumentation.sh contains `set -uo pipefail`
    - validate-instrumentation.sh contains `check()` function
    - validate-instrumentation.sh contains all 8 check IDs: EVNT04-A, EVNT04-B, EVNT04-C, EVNT04-D, EVNT04-E, EVNT04-F, EVNT04-G, EVNT04-H
    - validate-instrumentation.sh --quick runs without error (exit code 0 or 1 — no bash syntax errors)
    - EVNT04-A through EVNT04-E will FAIL at this point (workflow files not yet instrumented) — this is expected
  </acceptance_criteria>
  <done>validate-instrumentation.sh exists with 8 checks covering all EVNT-04 sub-requirements; --quick mode runs EVNT04-A through EVNT04-E only</done>
</task>

<task type="auto">
  <name>Task 2: Instrument execute-phase.md with 4 event-emit calls</name>
  <files>workflows/execute-phase.md</files>
  <read_first>
    - workflows/execute-phase.md (full file — identify exact insertion points: after validate_phase report, start of wave loop before "Describe what's being built", after spot-checks pass in wave complete, and after phase complete CLI call in update_roadmap)
    - .planning/phases/62-workflow-instrumentation/62-RESEARCH.md (exact instrumentation map section: 4 call sites in execute-phase.md)
    - bin/pane-pipeline-progress.sh (consumer — confirm field names: .phase_name, .phase_number, .wave_number)
  </read_first>
  <action>
Insert exactly 4 event-emit calls into workflows/execute-phase.md. Every call uses the pattern:
```
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" event-emit <type> '<json>' 2>/dev/null || true
```

**Call 1 — phase_started:** Insert after the `validate_phase` step's report line ("Found {plan_count} plans..."), before the `discover_and_group_plans` step. Add a new bash block:
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" event-emit phase_started \
  '{"phase_number":"'"${PHASE_NUMBER}"'","phase_name":"'"${PHASE_NAME}"'"}' \
  2>/dev/null || true
```

**Call 2 — wave_started:** Insert at the start of the "For each wave:" loop in the `execute_waves` step, BEFORE step 1 "Describe what's being built". Add:
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" event-emit wave_started \
  '{"phase_number":"'"${PHASE_NUMBER}"'","wave_number":"'"${WAVE_NUM}"'"}' \
  2>/dev/null || true
```
Where WAVE_NUM is the current wave number from the wave loop counter.

**Call 3 — wave_complete:** Insert after the spot-check pass block in step 4 of execute_waves (after the "Wave {N} Complete" report markdown), before step 5 "Handle failures". Add:
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" event-emit wave_complete \
  '{"phase_number":"'"${PHASE_NUMBER}"'","wave_number":"'"${WAVE_NUM}"'"}' \
  2>/dev/null || true
```

**Call 4 — phase_complete:** Insert in the `update_roadmap` step, AFTER the `pde-tools.cjs phase complete "${PHASE_NUMBER}"` call succeeds and BEFORE the git commit. Add:
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" event-emit phase_complete \
  '{"phase_number":"'"${PHASE_NUMBER}"'","phase_name":"'"${PHASE_NAME}"'"}' \
  2>/dev/null || true
```

CRITICAL RULES:
- Every call MUST end with `2>/dev/null || true` — fire-and-forget, never blocks workflow
- Insert as additive lines only — do NOT modify or remove any existing bash logic
- Do NOT emit phase events from execute-plan.md (that file handles plan events only)
- wave_complete goes AFTER spot-checks pass, NOT immediately when agents return
- phase_complete goes AFTER `phase complete` CLI call, NOT before
  </action>
  <verify>
    <automated>bash ".planning/phases/62-workflow-instrumentation/validate-instrumentation.sh" --quick 2>&1 | grep -E 'EVNT04-[A-D]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'event-emit phase_started' workflows/execute-phase.md` returns >= 1
    - `grep -c 'event-emit phase_complete' workflows/execute-phase.md` returns >= 1
    - `grep -c 'event-emit wave_started' workflows/execute-phase.md` returns >= 1
    - `grep -c 'event-emit wave_complete' workflows/execute-phase.md` returns >= 1
    - `grep 'event-emit' workflows/execute-phase.md | grep -v '|| true' | wc -l` returns 0 (all calls have || true)
    - `grep 'event-emit' workflows/execute-phase.md | grep -v '2>/dev/null' | wc -l` returns 0 (all calls have 2>/dev/null)
    - execute-phase.md `event-emit phase_started` appears AFTER the `validate_phase` step name
    - execute-phase.md `event-emit phase_complete` appears AFTER `phase complete "${PHASE_NUMBER}"`
    - execute-phase.md `event-emit wave_started` appears BEFORE "Describe what's being built"
    - execute-phase.md `event-emit wave_complete` appears AFTER "spot-check" references
  </acceptance_criteria>
  <done>execute-phase.md contains 4 fire-and-forget event-emit calls at correct workflow boundaries; EVNT04-A through EVNT04-D and EVNT04-H all PASS</done>
</task>

<task type="auto">
  <name>Task 3: Instrument execute-plan.md with 2 event-emit calls</name>
  <files>workflows/execute-plan.md</files>
  <read_first>
    - workflows/execute-plan.md (full file — identify record_start_time step at line ~50 and create_summary step at line ~386)
    - .planning/phases/62-workflow-instrumentation/62-RESEARCH.md (plan event emit pattern with plan_id composite field)
  </read_first>
  <action>
Insert exactly 2 event-emit calls into workflows/execute-plan.md.

**Call 5 — plan_started:** Insert in the `record_start_time` step, AFTER `PLAN_START_EPOCH=$(date +%s)`. Add:
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" event-emit plan_started \
  '{"plan_id":"'"${PHASE}-${PLAN}"'"}' \
  2>/dev/null || true
```
Where ${PHASE} and ${PLAN} are the phase number and plan number variables already available in execute-plan.md context (extracted from PLAN.md path in the identify_plan step).

Note: The variable names in execute-plan.md use PHASE (from init JSON phase_number) and PLAN (from the plan file path parsing). Verify the exact variable names by reading the init_context and identify_plan steps before inserting.

**Call 6 — plan_complete:** Insert in the `create_summary` step, AFTER the SUMMARY.md file is written (after the writeSummary/create file operation), BEFORE the `update_current_position` step. Add:
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" event-emit plan_complete \
  '{"plan_id":"'"${PHASE}-${PLAN}"'"}' \
  2>/dev/null || true
```

CRITICAL RULES:
- Every call MUST end with `2>/dev/null || true`
- plan_started fires AFTER start time is recorded (not before)
- plan_complete fires AFTER SUMMARY.md is written (not before)
- Do NOT emit phase or wave events from execute-plan.md
- The plan_id field is a composite: "${PHASE}-${PLAN}" (e.g., "62-01")
  </action>
  <verify>
    <automated>bash ".planning/phases/62-workflow-instrumentation/validate-instrumentation.sh" --quick 2>&1 | grep 'EVNT04-E'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'event-emit plan_started' workflows/execute-plan.md` returns >= 1
    - `grep -c 'event-emit plan_complete' workflows/execute-plan.md` returns >= 1
    - `grep 'event-emit' workflows/execute-plan.md | grep -v '|| true' | wc -l` returns 0
    - `grep 'event-emit' workflows/execute-plan.md | grep -v '2>/dev/null' | wc -l` returns 0
    - execute-plan.md `event-emit plan_started` appears AFTER `PLAN_START_EPOCH`
    - execute-plan.md `event-emit plan_complete` appears AFTER `create_summary` step name
  </acceptance_criteria>
  <done>execute-plan.md contains 2 fire-and-forget event-emit calls; EVNT04-E PASS; --quick validation shows EVNT04-A through EVNT04-E all PASS</done>
</task>

</tasks>

<verification>
Quick validation (after each task commit):
```bash
bash .planning/phases/62-workflow-instrumentation/validate-instrumentation.sh --quick
```
Expected after Plan 01: EVNT04-A through EVNT04-E all PASS, EVNT04-H PASS. EVNT04-F and EVNT04-G will FAIL (archive-session.cjs not yet updated — that is Plan 02).
</verification>

<success_criteria>
- workflows/execute-phase.md contains 4 event-emit calls (phase_started, phase_complete, wave_started, wave_complete)
- workflows/execute-plan.md contains 2 event-emit calls (plan_started, plan_complete)
- All event-emit calls use `2>/dev/null || true` pattern
- validate-instrumentation.sh exists with 8 checks
- EVNT04-A through EVNT04-E and EVNT04-H all PASS
</success_criteria>

<output>
After completion, create `.planning/phases/62-workflow-instrumentation/62-01-SUMMARY.md`
</output>
