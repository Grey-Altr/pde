---
phase: 67-ideation-visual-divergence
plan: 02
type: execute
wave: 2
depends_on: [67-01]
files_modified:
  - tests/phase-67/diverge-stitch-flag.test.mjs
  - tests/phase-67/quota-partial-batch.test.mjs
  - tests/phase-67/visual-convergence.test.mjs
  - tests/phase-67/batch-efficiency.test.mjs
  - tests/phase-67/consent-batch.test.mjs
autonomous: true
requirements: [IDT-01, IDT-02, IDT-03, IDT-04, EFF-03]

must_haves:
  truths:
    - "All 5 Nyquist test files pass with node --test tests/phase-67/*.test.mjs"
    - "Every requirement (IDT-01, IDT-02, IDT-03, IDT-04, EFF-03) has at least one test assertion"
    - "Tests use file-parse assertions against workflows/ideate.md (readFileSync + string checks)"
    - "Tests verify structural ordering via indexOf assertions (consent before generation, text diverge before Stitch)"
  artifacts:
    - path: "tests/phase-67/diverge-stitch-flag.test.mjs"
      provides: "IDT-01 (--diverge flag, GEMINI_3_PRO modelId, prompt isolation), IDT-02 (artifact naming), IDT-04 (quota type)"
    - path: "tests/phase-67/quota-partial-batch.test.mjs"
      provides: "IDT-04 (STITCH_BATCH_SIZE from remaining, partial fallback, no abort)"
    - path: "tests/phase-67/visual-convergence.test.mjs"
      provides: "IDT-03 (HAS_VISUAL_VARIANTS detection, Visual column in converge table)"
    - path: "tests/phase-67/batch-efficiency.test.mjs"
      provides: "EFF-03 (no HTML fetch, no annotation, batch prompt construction before loop)"
    - path: "tests/phase-67/consent-batch.test.mjs"
      provides: "CONSENT-04 (batch consent before loop, Experimental model mention, quota display)"
  key_links:
    - from: "tests/phase-67/*.test.mjs"
      to: "workflows/ideate.md"
      via: "readFileSync file-parse assertions"
      pattern: "readFileSync.*ideate\\.md"
---

<objective>
Create Nyquist test suite for Phase 67 covering all 5 requirements (IDT-01, IDT-02, IDT-03, IDT-04, EFF-03) via file-parse assertions against workflows/ideate.md.

Purpose: Automated verification that all requirement-mandated workflow elements are structurally present in ideate.md. Same pattern as Phase 66 Nyquist tests.

Output: 5 test files in tests/phase-67/ that all pass with `node --test tests/phase-67/*.test.mjs`.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-ideation-visual-divergence/67-RESEARCH.md
@.planning/phases/67-ideation-visual-divergence/67-01-SUMMARY.md

<interfaces>
<!-- Reference test pattern from Phase 66 -->

From tests/phase-66/wireframe-stitch-flag.test.mjs:
```javascript
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'fs';
import { fileURLToPath } from 'url';
import { resolve } from 'path';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');
const wireframeMd = readFileSync(resolve(ROOT, 'workflows', 'wireframe.md'), 'utf8');
```

Phase 67 tests follow this exact pattern but read `workflows/ideate.md` instead.

From 67-RESEARCH.md Validation Architecture:
- diverge-stitch-flag.test.mjs: IDT-01, IDT-02, IDT-04 (flag, artifact naming, quota type, modelId)
- quota-partial-batch.test.mjs: IDT-04 (STITCH_BATCH_SIZE from remaining, no abort)
- visual-convergence.test.mjs: IDT-03 (HAS_VISUAL_VARIANTS, Visual column)
- batch-efficiency.test.mjs: EFF-03 (no HTML fetch, no annotation, batch prompt construction)
- consent-batch.test.mjs: CONSENT-04 (batch consent, Experimental mention, quota display)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5 Nyquist test files for Phase 67 requirements</name>
  <files>
    tests/phase-67/diverge-stitch-flag.test.mjs
    tests/phase-67/quota-partial-batch.test.mjs
    tests/phase-67/visual-convergence.test.mjs
    tests/phase-67/batch-efficiency.test.mjs
    tests/phase-67/consent-batch.test.mjs
  </files>
  <read_first>
    - workflows/ideate.md (full file — the file being tested, must see actual strings to assert against)
    - tests/phase-66/wireframe-stitch-flag.test.mjs (reference pattern for file-parse Nyquist tests)
    - tests/phase-66/consent-gates.test.mjs (reference for consent assertion pattern and indexOf ordering)
    - .planning/phases/67-ideation-visual-divergence/67-RESEARCH.md (Validation Architecture section — exact test map)
  </read_first>
  <action>
Create `tests/phase-67/` directory and 5 test files. All files follow the Phase 66 Nyquist pattern:
- Import: `node:test`, `node:assert/strict`, `fs.readFileSync`, `url.fileURLToPath`, `path.resolve`
- Read: `const ideateMd = readFileSync(resolve(ROOT, 'workflows', 'ideate.md'), 'utf8');`
- Assert: String includes checks and indexOf ordering assertions

**File 1: tests/phase-67/diverge-stitch-flag.test.mjs**
Header comment: `Tests: IDT-01 (--diverge flag, GEMINI_3_PRO modelId, prompt isolation), IDT-02 (STH artifact naming), IDT-04 (experimental quota type)`

```javascript
describe('IDT-01: --diverge flag on ideate', () => {
  test('flags table contains --diverge row with Boolean type', () => {
    assert.ok(ideateMd.includes('`--diverge` | Boolean'), '...');
  });
  test('DIVERGE_STITCH variable assignment exists', () => {
    assert.ok(ideateMd.includes('DIVERGE_STITCH'), '...');
  });
  test('stitch:generate-screen call exists in 4-STITCH section', () => {
    assert.ok(ideateMd.includes('stitch:generate-screen'), '...');
  });
  test('GEMINI_3_PRO modelId used for Experimental generation', () => {
    assert.ok(ideateMd.includes('GEMINI_3_PRO'), '...');
  });
  test('No shared project_id — isolation required', () => {
    // The 4-STITCH-D section must mention isolation / no shared project
    assert.ok(ideateMd.includes('isolation required'), '...');
  });
});

describe('IDT-02: STH-ideate-direction artifact naming', () => {
  test('STH-ideate-direction-{i}.png naming convention in persist step', () => {
    assert.ok(ideateMd.includes('STH-ideate-direction-'), '...');
  });
  test('.planning/design/strategy/ is the target directory', () => {
    assert.ok(ideateMd.includes('.planning/design/strategy/STH-ideate-direction-'), '...');
  });
  test('## Visual Variants section template exists', () => {
    assert.ok(ideateMd.includes('## Visual Variants'), '...');
  });
});

describe('IDT-04: Experimental quota type', () => {
  test('checkStitchQuota with experimental type', () => {
    assert.ok(ideateMd.includes("checkStitchQuota('experimental')"), '...');
  });
  test('incrementStitchQuota with experimental type', () => {
    assert.ok(ideateMd.includes("incrementStitchQuota('experimental')"), '...');
  });
});
```

**File 2: tests/phase-67/quota-partial-batch.test.mjs**
Header comment: `Tests: IDT-04 (partial-batch fallback, STITCH_BATCH_SIZE from remaining, no abort)`

```javascript
describe('IDT-04: Partial-batch fallback logic', () => {
  test('STITCH_BATCH_SIZE variable exists', () => {
    assert.ok(ideateMd.includes('STITCH_BATCH_SIZE'), '...');
  });
  test('remaining field used for batch size computation', () => {
    assert.ok(ideateMd.includes('remaining'), '...');
  });
  test('STITCH_FALLBACK_TEXT_ONLY marker exists', () => {
    assert.ok(ideateMd.includes('STITCH_FALLBACK_TEXT_ONLY'), '...');
  });
  test('partial batch warning message present', () => {
    assert.ok(ideateMd.includes('Generating visuals for first'), '...');
  });
  test('run never aborts — text-only fallback is always correct', () => {
    // The 4-STITCH-E section should mention "NEVER aborts"
    assert.ok(ideateMd.includes('NEVER abort'), '...');
  });
  test('quota exhausted falls back gracefully — not a HALT', () => {
    assert.ok(ideateMd.includes('Proceeding with text-only ideation'), '...');
  });
});
```

**File 3: tests/phase-67/visual-convergence.test.mjs**
Header comment: `Tests: IDT-03 (HAS_VISUAL_VARIANTS detection, Visual column in converge scoring table)`

```javascript
describe('IDT-03: Visual variants feed into convergence', () => {
  test('HAS_VISUAL_VARIANTS detection logic in Step 6/7', () => {
    assert.ok(ideateMd.includes('HAS_VISUAL_VARIANTS'), '...');
  });
  test('Visual column in conditional converge scoring table', () => {
    assert.ok(ideateMd.includes('| Visual |'), '...');
  });
  test('Visual Variants section check precedes converge scoring', () => {
    const variantsIdx = ideateMd.indexOf('HAS_VISUAL_VARIANTS');
    const convergeIdx = ideateMd.indexOf('## Converge Phase');
    // HAS_VISUAL_VARIANTS detection should be near or within the converge step
    assert.ok(variantsIdx > 0, '...');
    assert.ok(convergeIdx > 0, '...');
  });
  test('text-only fallback shown in Visual column for non-visual directions', () => {
    assert.ok(ideateMd.includes('text-only'), '...');
  });
  test('backwards-compatible — no Visual column when HAS_VISUAL_VARIANTS is false', () => {
    // The file must show the conditional: IF HAS_VISUAL_VARIANTS and the ELSE branch without Visual
    assert.ok(ideateMd.includes('IF HAS_VISUAL_VARIANTS'), '...');
  });
});
```

**File 4: tests/phase-67/batch-efficiency.test.mjs**
Header comment: `Tests: EFF-03 (batch prompt construction before loop, no HTML fetch, no annotation injection)`

```javascript
describe('EFF-03: Batch MCP efficiency', () => {
  test('prompts built before generation loop (4-STITCH-B before 4-STITCH-D)', () => {
    const promptBuildIdx = ideateMd.indexOf('4-STITCH-B');
    const loopIdx = ideateMd.indexOf('4-STITCH-D');
    assert.ok(promptBuildIdx > 0, '...');
    assert.ok(loopIdx > 0, '...');
    assert.ok(promptBuildIdx < loopIdx, '4-STITCH-B (prompt build) must come before 4-STITCH-D (generation loop)');
  });
  test('no HTML fetch in 4-STITCH section', () => {
    // Extract the 4-STITCH section and verify stitch:fetch-screen-code is absent
    const stitchStart = ideateMd.indexOf('### Step 4-STITCH');
    const stitchEnd = ideateMd.indexOf('### Step 5/7');
    assert.ok(stitchStart > 0, '4-STITCH section must exist');
    assert.ok(stitchEnd > stitchStart, 'Step 5/7 must follow 4-STITCH');
    const stitchSection = ideateMd.slice(stitchStart, stitchEnd);
    assert.ok(!stitchSection.includes('fetch-screen-code'), 'ideation 4-STITCH must NOT contain fetch-screen-code (PNG-only)');
  });
  test('no annotation injection in 4-STITCH section', () => {
    const stitchStart = ideateMd.indexOf('### Step 4-STITCH');
    const stitchEnd = ideateMd.indexOf('### Step 5/7');
    const stitchSection = ideateMd.slice(stitchStart, stitchEnd);
    assert.ok(!stitchSection.includes('injectComponentAnnotations'), 'ideation 4-STITCH must NOT contain annotation injection');
    assert.ok(!stitchSection.includes('@component:'), 'ideation 4-STITCH must NOT contain @component: annotations');
  });
  test('PNG-only: stitch:fetch-screen-image present in loop', () => {
    assert.ok(ideateMd.includes('stitch:fetch-screen-image'), '...');
  });
});
```

**File 5: tests/phase-67/consent-batch.test.mjs**
Header comment: `Tests: CONSENT-04 (single batch consent before loop, Experimental model mention, quota display)`

```javascript
describe('CONSENT-04: Batch outbound consent for ideation', () => {
  test('batch consent prompt exists in 4-STITCH-C', () => {
    assert.ok(ideateMd.includes('4-STITCH-C'), '...');
    assert.ok(ideateMd.includes('Batch outbound consent'), '...');
  });
  test('consent prompt comes before generation loop', () => {
    const consentIdx = ideateMd.indexOf('4-STITCH-C');
    const loopIdx = ideateMd.indexOf('4-STITCH-D');
    assert.ok(consentIdx < loopIdx, 'batch consent (4-STITCH-C) must precede generation loop (4-STITCH-D)');
  });
  test('consent mentions Experimental model type', () => {
    assert.ok(ideateMd.includes('Experimental'), '...');
  });
  test('consent mentions Gemini Pro', () => {
    assert.ok(ideateMd.includes('Gemini Pro'), '...');
  });
  test('consent shows quota remaining after batch', () => {
    assert.ok(ideateMd.includes('remaining after this batch'), '...');
  });
  test('consent mentions 50/month limit', () => {
    assert.ok(ideateMd.includes('50/month'), '...');
  });
  test('user can decline — DIVERGE_STITCH set to false on "no"', () => {
    assert.ok(ideateMd.includes('Visual divergence cancelled by user'), '...');
  });
  test('consent mentions stitch.withgoogle.com', () => {
    assert.ok(ideateMd.includes('stitch.withgoogle.com'), '...');
  });
});
```

Run all tests after creation: `node --test tests/phase-67/*.test.mjs`
All tests must pass. If any fail, read the actual string in ideate.md and adjust the assertion to match the exact text present.
  </action>
  <verify>
    <automated>node --test tests/phase-67/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-67/diverge-stitch-flag.test.mjs exists and contains "IDT-01" and "IDT-02" and "IDT-04" in describe blocks
    - tests/phase-67/quota-partial-batch.test.mjs exists and contains "IDT-04" and "STITCH_BATCH_SIZE" in assertions
    - tests/phase-67/visual-convergence.test.mjs exists and contains "IDT-03" and "HAS_VISUAL_VARIANTS" in assertions
    - tests/phase-67/batch-efficiency.test.mjs exists and contains "EFF-03" and "fetch-screen-code" in absence check
    - tests/phase-67/consent-batch.test.mjs exists and contains "CONSENT-04" and "Experimental" in assertions
    - `node --test tests/phase-67/*.test.mjs` exits with code 0
    - All 5 files use the same pattern: import node:test + assert/strict, readFileSync with ROOT-relative path to workflows/ideate.md
    - At least 30 total test assertions across all 5 files
    - indexOf ordering assertions present in batch-efficiency.test.mjs (4-STITCH-B < 4-STITCH-D) and consent-batch.test.mjs (4-STITCH-C < 4-STITCH-D)
  </acceptance_criteria>
  <done>All 5 Nyquist test files pass with `node --test tests/phase-67/*.test.mjs`, covering IDT-01, IDT-02, IDT-03, IDT-04, and EFF-03 via file-parse assertions against ideate.md</done>
</task>

</tasks>

<verification>
1. `node --test tests/phase-67/*.test.mjs` — all tests pass (exit 0)
2. `node --test tests/phase-65/*.test.mjs tests/phase-66/*.test.mjs tests/phase-67/*.test.mjs` — full suite green (no regressions)
3. Verify each requirement ID appears in at least one test file's describe block
</verification>

<success_criteria>
1. 5 test files exist in tests/phase-67/
2. All tests pass with exit code 0
3. Every Phase 67 requirement (IDT-01, IDT-02, IDT-03, IDT-04, EFF-03) has at least one test
4. Tests use same pattern as Phase 66 (readFileSync + node:test + assert/strict)
5. No regressions in Phase 65 or Phase 66 test suites
</success_criteria>

<output>
After completion, create `.planning/phases/67-ideation-visual-divergence/67-02-SUMMARY.md`
</output>
