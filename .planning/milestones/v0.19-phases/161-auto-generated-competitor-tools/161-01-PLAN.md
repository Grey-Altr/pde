---
phase: 161-auto-generated-competitor-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/competitive.md
  - dashboard/app/api/planning/competitor-tools/route.ts
autonomous: true
requirements:
  - ADV-01
  - ADV-02
  - ADV-04

must_haves:
  truths:
    - "Running /pde:competitive --webmcp generates competitor tool stubs as a post-processing Step 8"
    - "Generated tool descriptions have injection patterns stripped and are capped at 512 characters"
    - "Registry entries are written to .webmcp/competitor-tools-registry.json with status: pending"
    - "Each competitor gets a gate file in .planning/gates/ for human review"
  artifacts:
    - path: "workflows/competitive.md"
      provides: "Step 8/8 competitor tool stub generation block"
      contains: "Step 8/8"
    - path: "dashboard/app/api/planning/competitor-tools/route.ts"
      provides: "API route serving approved competitor tool data from registry"
      exports: ["GET"]
    - path: ".webmcp/competitor-tools-registry.json"
      provides: "Persistent competitor tool registry (written at runtime by workflow)"
  key_links:
    - from: "workflows/competitive.md Step 8"
      to: ".webmcp/competitor-tools-registry.json"
      via: "File write after sanitization pipeline"
      pattern: "competitor-tools-registry\\.json"
    - from: "workflows/competitive.md Step 8"
      to: ".planning/gates/{gate_id}.json"
      via: "Gate file write per competitor"
      pattern: "competitor-tool-.*\\.json"
    - from: "dashboard/app/api/planning/competitor-tools/route.ts"
      to: ".webmcp/competitor-tools-registry.json"
      via: "fs.readFileSync registry read"
      pattern: "competitor-tools-registry\\.json"
---

<objective>
Add Step 8 to the competitive analysis workflow that generates sanitized WebMCP tool stubs from competitor data, writes them to a persistent registry, and creates approval gate files. Also create the API route that serves approved competitor tool data from the registry.

Purpose: Enables browser AI agents to query competitor data via auto-generated tool stubs, with mandatory human approval before activation (ADV-01, ADV-02, ADV-04).
Output: Modified competitive.md with Step 8, new competitor-tools API route, registry JSON format established.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-auto-generated-competitor-tools/161-CONTEXT.md
@.planning/phases/161-auto-generated-competitor-tools/161-RESEARCH.md

<interfaces>
<!-- Existing gate route contract (reused, not modified) -->
From dashboard/app/api/planning/gates/route.ts:
```typescript
// POST body: { gate_id: string, action: 'approve' | 'reject', reason?: string }
// POST response: { ok: true, gate_id, action }
// GET response: { gates: Array<{ gate_id, status, ... }> } (pending only)
export const dynamic = 'force-dynamic';
export async function GET(): Promise<NextResponse>;
export async function POST(request: Request): Promise<NextResponse>;
```

From workflows/competitive.md (current structure):
- Steps 1-7 exist (Step 7/7 is "Update root DESIGN-STATE and manifest")
- --webmcp flag parsed as USE_WEBMCP boolean
- WebMCP Context section appended after summary when USE_WEBMCP is true
- Step numbering currently 7/7 throughout

Registry JSON format (per D-09):
```json
[{
  "name": "query_{sanitized_name}",
  "description": "{sanitized_description}",
  "competitor_name": "{original_name}",
  "status": "pending",
  "gate_id": "competitor-tool-{sanitized_name}-{YYYYMMDD}-{4_HEX}",
  "metadata": {
    "source": "auto-generated",
    "generated_from": "CMP-competitive-v{N}",
    "generated_at": "{ISO 8601 timestamp}"
  },
  "approved_at": null
}]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 8 competitor tool stub generation to competitive.md</name>
  <files>workflows/competitive.md</files>
  <read_first>
    - workflows/competitive.md (full file — understand Step 7 endpoint, summary section, WebMCP Context section, output tags)
    - dashboard/app/api/planning/gates/route.ts (gate file format reference)
    - .planning/phases/161-auto-generated-competitor-tools/161-CONTEXT.md (locked decisions D-01 through D-10)
    - .planning/phases/161-auto-generated-competitor-tools/161-RESEARCH.md (sanitization pipeline details, code examples)
  </read_first>
  <action>
Modify workflows/competitive.md to add Step 8 as a new final step. This requires three changes:

**Change 1: Update all step numbering from X/7 to X/8.**
Every occurrence of "Step N/7" throughout the file must become "Step N/8". This includes:
- Section headers: `### Step 1/7` -> `### Step 1/8`, etc. through Step 7
- Display lines: `Display: \`Step N/7:...` -> `Display: \`Step N/8:...`
- All 7 steps must be renumbered

**Change 2: Insert Step 8 block after Step 7's display line and before the `## Summary` section (currently at line ~734).**

Insert this exact content between the Step 7 display line (`Step 7/8: Root DESIGN-STATE and manifest updated.`) and the `---` that precedes `## Summary`:

```markdown
---

### Step 8/8: Generate competitor tool stubs (--webmcp only)

IF USE_WEBMCP is false OR no competitors were identified in Step 4:
  Skip silently. Display nothing.
  END

For each competitor identified in Step 4a:

1. **Sanitize competitor name** for tool naming:
   - Lowercase, replace spaces and non-alphanumeric chars with `_`
   - Collapse consecutive underscores, strip leading/trailing underscores
   - Result: `{sanitized_name}` (e.g., "Acme Corp" -> "acme_corp")

2. **Build raw description** from:
   - Competitor's key differentiators (Step 4b)
   - Feature matrix row data for this competitor (Step 4c)
   - Pricing tier summary (Step 4f)
   Format: "{competitor_name}: {differentiator_1}, {differentiator_2}. Pricing: {tier_summary}. Features: {feature_summary}."

3. **Sanitize description** (per D-05, D-06):
   - Strip these exact patterns (case-insensitive): `<system>`, `</system>`, `IMPORTANT:`, `You must`, `Ignore previous`, `Ignore all previous`
   - Strip lines that begin with `#` (markdown headers) — match only at line start
   - Truncate at the last `.` before character position 512; if no period found, truncate at last space before 512; append `...` if truncated
   - If description is already <= 512 chars, leave it unchanged

4. **Generate gate ID** (per D-08): `competitor-tool-{sanitized_name}-{YYYYMMDD}-{4_HEX}`
   where `{YYYYMMDD}` is today's date and `{4_HEX}` is the first 4 characters of a random hex string (e.g., `a1b2`).

5. **Build registry entry object** (per D-09):
   ```json
   {
     "name": "query_{sanitized_name}",
     "description": "{sanitized_description}",
     "competitor_name": "{original_competitor_name}",
     "status": "pending",
     "gate_id": "{gate_id}",
     "metadata": {
       "source": "auto-generated",
       "generated_from": "CMP-competitive-v{N}",
       "generated_at": "{ISO 8601 timestamp}"
     },
     "approved_at": null
   }
   ```

6. **Write gate file** to `.planning/gates/{gate_id}.json`:
   ```json
   {
     "gate_id": "{gate_id}",
     "workflow": "competitive",
     "type": "competitor-tool",
     "competitor_name": "{original_competitor_name}",
     "tool_name": "query_{sanitized_name}",
     "status": "pending",
     "created_at": "{ISO 8601 timestamp}"
   }
   ```

After processing all competitors:

7. **Write registry**: Ensure `.webmcp/` directory exists (`mkdir -p .webmcp`).
   Read existing `.webmcp/competitor-tools-registry.json` if present.
   Merge new entries into existing array — skip any entry whose `gate_id` already exists in the array.
   Write the merged array back to `.webmcp/competitor-tools-registry.json`.
   If file was absent, write the new array as the full registry.

Display:
```
Step 8/8: Generated {N} competitor tool stubs.
  -> Registry: .webmcp/competitor-tools-registry.json
  -> Pending gates: {gate_id_1}, {gate_id_2}, ...
  -> Approve via: pde_approval_gate with gate_id and action: "approve"
```
```

**Change 3: Update the `<output>` tag at the bottom of the file** to include the registry file.

Find the `<output>` section (currently lines 812-817) and add after the last `- .planning/design/design-manifest.json` line:
```
- `.webmcp/competitor-tools-registry.json` — competitor tool registry (created/updated when --webmcp flag active)
- `.planning/gates/competitor-tool-*.json` — approval gates for auto-generated tools (when --webmcp flag active)
```

**Change 4: Update the WebMCP Context table** (around line 760) to add a row for the new `query_competitor_data` tool:

In the `**Available tools registered in this browser session:**` table, add this row after the `pde_approval_gate` row:
```
| `query_competitor_data` | To query approved competitor analysis data | `competitor_name` (name of an approved competitor) |
```
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep -c "Step 8/8" workflows/competitive.md && grep -c "Step.*\/7" workflows/competitive.md && grep "competitor-tools-registry.json" workflows/competitive.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - workflows/competitive.md contains the string "### Step 8/8: Generate competitor tool stubs (--webmcp only)"
    - workflows/competitive.md contains zero occurrences of "/7:" (all renumbered to /8)
    - workflows/competitive.md contains "source.*auto-generated" in the registry entry template
    - workflows/competitive.md contains "competitor-tools-registry.json" in both Step 8 and the output section
    - workflows/competitive.md contains "IMPORTANT:" in the sanitization strip list
    - workflows/competitive.md contains "512" for the character truncation limit
    - workflows/competitive.md contains "query_competitor_data" in the WebMCP Context table
    - workflows/competitive.md output section contains ".planning/gates/competitor-tool-*.json"
  </acceptance_criteria>
  <done>competitive.md has Step 8 with full sanitization pipeline instructions, registry write logic, gate file creation, step renumbering from /7 to /8, updated output section, and WebMCP Context table updated</done>
</task>

<task type="auto">
  <name>Task 2: Create competitor-tools API route</name>
  <files>dashboard/app/api/planning/competitor-tools/route.ts</files>
  <read_first>
    - dashboard/app/api/planning/gates/route.ts (reference pattern for file-reading API route with Clerk auth, dynamic export, error handling)
    - dashboard/lib/mcp/browser-tools/use-approval-gate-tool.ts (consumer pattern — handler calls fetch to API route)
    - .planning/phases/161-auto-generated-competitor-tools/161-RESEARCH.md (code example for route)
  </read_first>
  <action>
Create a new file `dashboard/app/api/planning/competitor-tools/route.ts` that serves competitor tool data from the registry.

The route must:

1. Export `const dynamic = 'force-dynamic';` (same as gates route pattern)

2. Export a `GET` handler that:
   - Reads `.webmcp/competitor-tools-registry.json` from `path.join(process.cwd(), '.webmcp', 'competitor-tools-registry.json')`
   - If file doesn't exist: return `NextResponse.json({ error: 'Registry not found' }, { status: 404 })`
   - If file is corrupt (JSON.parse fails): return `NextResponse.json({ error: 'Corrupt registry' }, { status: 500 })`
   - Parses the JSON array
   - Filters to entries where `status === 'approved'`
   - If query param `name` is present (from `request.url`): find the approved entry matching `competitor_name === name`
     - If not found: return `NextResponse.json({ error: 'Not found or not approved' }, { status: 404 })`
     - If found: return `NextResponse.json(tool)` (the single entry object)
   - If no `name` param: return `NextResponse.json({ tools: approved })` (all approved entries)

3. Imports: `fs` from `'node:fs'`, `path` from `'node:path'`, `NextResponse` from `'next/server'`

4. No Clerk auth on GET (read-only, same as gates GET pattern — gates GET also has no auth check)

5. Type the registry entries as `Array<Record<string, unknown>>` (same loose typing as gates route)

Exact file content:

```typescript
import fs from 'node:fs';
import path from 'node:path';
import { NextResponse } from 'next/server';

export const dynamic = 'force-dynamic';

export async function GET(request: Request): Promise<NextResponse> {
  const url = new URL(request.url);
  const name = url.searchParams.get('name');

  const registryPath = path.join(process.cwd(), '.webmcp', 'competitor-tools-registry.json');

  if (!fs.existsSync(registryPath)) {
    return NextResponse.json({ error: 'Registry not found' }, { status: 404 });
  }

  let registry: unknown[];
  try {
    registry = JSON.parse(fs.readFileSync(registryPath, 'utf-8'));
  } catch {
    return NextResponse.json({ error: 'Corrupt registry' }, { status: 500 });
  }

  const approved = (registry as Array<Record<string, unknown>>).filter(
    (t) => t.status === 'approved'
  );

  if (name) {
    const tool = approved.find((t) => t.competitor_name === name);
    if (!tool) {
      return NextResponse.json({ error: 'Not found or not approved' }, { status: 404 });
    }
    return NextResponse.json(tool);
  }

  return NextResponse.json({ tools: approved });
}
```
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && test -f dashboard/app/api/planning/competitor-tools/route.ts && grep "force-dynamic" dashboard/app/api/planning/competitor-tools/route.ts && grep "competitor-tools-registry.json" dashboard/app/api/planning/competitor-tools/route.ts && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/app/api/planning/competitor-tools/route.ts exists
    - File contains `export const dynamic = 'force-dynamic'`
    - File contains `export async function GET(request: Request): Promise<NextResponse>`
    - File contains `competitor-tools-registry.json` as the registry path
    - File contains `t.status === 'approved'` for filtering
    - File contains `url.searchParams.get('name')` for single-competitor lookup
    - File imports from `'node:fs'`, `'node:path'`, `'next/server'`
    - File does NOT contain `auth` import (GET is unauthenticated, matching gates GET pattern)
  </acceptance_criteria>
  <done>API route exists at /api/planning/competitor-tools, serves approved competitor tools from registry, supports ?name= query param for single lookup, returns 404 when registry missing, 500 when corrupt</done>
</task>

</tasks>

<verification>
- `grep -c "Step 8/8" workflows/competitive.md` returns at least 2 (header + display)
- `grep -c "/7:" workflows/competitive.md` returns 0 (all renumbered)
- `test -f dashboard/app/api/planning/competitor-tools/route.ts` succeeds
- `grep "approved" dashboard/app/api/planning/competitor-tools/route.ts` returns matches
</verification>

<success_criteria>
- competitive.md contains Step 8/8 with full sanitization pipeline, registry write, gate file creation
- All existing steps renumbered from /7 to /8
- API route serves approved tools from registry with proper error handling
- No new npm dependencies required
</success_criteria>

<output>
After completion, create `.planning/phases/161-auto-generated-competitor-tools/161-01-SUMMARY.md`
</output>
