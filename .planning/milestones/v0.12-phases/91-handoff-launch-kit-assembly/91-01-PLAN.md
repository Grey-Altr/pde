---
phase: 91-handoff-launch-kit-assembly
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/91-handoff-launch-kit-assembly/tests/test-handoff-launch-kit.cjs
  - workflows/handoff.md
autonomous: true
requirements: [KIT-01, KIT-02, KIT-03, KIT-04, KIT-06]

must_haves:
  truths:
    - "handoff.md detects businessMode and businessTrack at start of Step 4"
    - "handoff.md discovers all 11 upstream business artifacts via Glob in Step 4k"
    - "handoff.md synthesizes LKT manifest, CNT content calendar, and OTR email sequences in Step 4l"
    - "handoff.md extracts domain strategy from brief in Step 4m"
    - "handoff.md writes LKT/CNT/OTR files under existing lock in Step 5e"
    - "OTR email sequences use [YOUR_PRODUCT_NAME] and [YOUR_FROM_ADDRESS] placeholders only"
    - "Investor outreach sequence gated on DPD artifact availability"
  artifacts:
    - path: ".planning/phases/91-handoff-launch-kit-assembly/tests/test-handoff-launch-kit.cjs"
      provides: "Nyquist structural assertions for KIT-01 through KIT-06"
      contains: "KIT-01"
    - path: "workflows/handoff.md"
      provides: "Business mode launch kit assembly steps"
      contains: "LKT-launch-kit"
  key_links:
    - from: "workflows/handoff.md"
      to: ".planning/design/launch/LKT-launch-kit-v*.md"
      via: "Step 5e Write tool"
      pattern: "LKT-launch-kit"
    - from: "workflows/handoff.md"
      to: ".planning/design/launch/CNT-content-calendar-v*.md"
      via: "Step 5e Write tool"
      pattern: "CNT-content-calendar"
    - from: "workflows/handoff.md"
      to: ".planning/design/launch/OTR-outreach-sequences-v*.md"
      via: "Step 5e Write tool"
      pattern: "OTR-outreach-sequences"
---

<objective>
Add business mode launch kit assembly to handoff.md: test scaffold, business artifact discovery (Step 4k), LKT/CNT/OTR content synthesis (Step 4l), domain strategy extraction (Step 4m), and artifact file writes (Step 5e).

Purpose: Enable /pde:handoff to produce a complete launch kit (manifest, content calendar, email sequences) when businessMode is true, creating the artifacts Phase 92 deploy skill consumes.
Output: Nyquist test file + handoff.md extended with Steps 4k-4m and 5e.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-handoff-launch-kit-assembly/91-RESEARCH.md
</context>

<interfaces>
<!-- Key patterns the executor needs from existing codebase -->

From workflows/handoff.md line 1-2 (purpose tag — currently says "14 designCoverage fields"):
```
<purpose>
Synthesize all upstream design pipeline artifacts...preserving all 14 designCoverage fields.
</purpose>
```

From workflows/handoff.md Step 7c (line 1194-1216 — current 16-field write pattern):
```
Parse the JSON output from coverage-check. Extract ALL sixteen current flag values...
```

From workflows/handoff.md Anti-Patterns (line 1271 — says "13 fields"):
```
preserving all 13 fields (hasDesignSystem...hasRecommendations)
```

From workflows/handoff.md output section (line 1308 — says "13 fields"):
```
all 13 fields preserved via read-before-set
```

Business mode detection pattern (used in flows.md, wireframe.md, critique.md):
```bash
BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
if [[ "$BM" == @file:* ]]; then BM=$(cat "${BM#@file:}"); fi
BT=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessTrack 2>/dev/null)
if [[ "$BT" == @file:* ]]; then BT=$(cat "${BT#@file:}"); fi
```

7-call manifest registration pattern (used in all prior phases):
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} code {CODE}
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} name "{Name}"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} type {type}
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} domain launch
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} path ".planning/design/launch/"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} status complete
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} version ${VERSION}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Nyquist test scaffold for KIT-01 through KIT-06 (Wave 0 — TDD RED)</name>
  <files>.planning/phases/91-handoff-launch-kit-assembly/tests/test-handoff-launch-kit.cjs</files>
  <read_first>
    - workflows/handoff.md (current state — tests assert content NOT yet present)
    - .planning/phases/90-critique-hig-extensions/tests/test-critique-hig-business.cjs (pattern reference for Nyquist test structure)
    - .planning/phases/91-handoff-launch-kit-assembly/91-RESEARCH.md (Example 5: Nyquist Test Structure)
  </read_first>
  <action>
Create `.planning/phases/91-handoff-launch-kit-assembly/tests/test-handoff-launch-kit.cjs` with structural assertions reading `workflows/handoff.md` content.

Use `require('node:test')` and `require('node:assert')` (same as Phase 90 test file pattern). Read handoff.md via `fs.readFileSync`.

Test groups and assertions:

**describe('KIT-01: LKT manifest generation'):**
1. `handoff.md contains "manifest-get-top-level businessMode"` — business detection
2. `handoff.md contains "LKT-launch-kit"` — artifact code
3. `handoff.md contains "Deployment Ready"` — LKT manifest column header
4. `handoff.md contains "Artifact Registry"` — LKT manifest section header

**describe('KIT-02: CNT content calendar'):**
5. `handoff.md contains "CNT-content-calendar"` — artifact code
6. `handoff.md contains "30-day" or "30 day"` — calendar duration reference
7. `handoff.md contains "Pre-Launch"` — calendar phase 1 header
8. `handoff.md contains "Post-Launch"` — calendar phase 3 header

**describe('KIT-03: OTR outreach sequences'):**
9. `handoff.md contains "OTR-outreach-sequences"` — artifact code
10. `handoff.md contains "resend-compatible" (case insensitive)` — Resend spec reference
11. `handoff.md contains "DPD_AVAILABLE" or "DPD-pitch-deck"` — investor sequence DPD gate
12. `handoff.md contains "Investor Outreach"` — investor sequence section

**describe('KIT-04: Domain strategy consolidation'):**
13. `handoff.md contains "Domain Strategy"` — domain strategy section in LKT
14. `handoff.md contains "BRIEF_CONTENT" or "BRF"` — brief artifact reference for domain extraction

**describe('KIT-05: hasLaunchKit coverage flag'):**
15. `handoff.md contains "hasLaunchKit"` — coverage flag name
16. `handoff.md contains "hasBusinessThesis"` — 20-field coverage includes business fields
17. `handoff.md contains "hasServiceBlueprint"` — 20-field coverage includes all business fields
18. `handoff.md contains "hasMarketLandscape"` — 20-field coverage includes all business fields

**describe('KIT-06: structural placeholders in OTR'):**
19. `handoff.md contains "[YOUR_PRODUCT_NAME]"` — product name placeholder
20. `handoff.md contains "[YOUR_FROM_ADDRESS]"` — email from address placeholder
21. `handoff.md contains "[YOUR_COMPANY_NAME]"` — company name placeholder

Run tests — expect majority RED (only KIT-04 "BRF" and KIT-05 "hasLaunchKit" assertions may be GREEN if those strings appear in existing handoff.md for other reasons).
  </action>
  <verify>
    <automated>node --test ".planning/phases/91-handoff-launch-kit-assembly/tests/test-handoff-launch-kit.cjs" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/91-handoff-launch-kit-assembly/tests/test-handoff-launch-kit.cjs exists
    - File contains `require('node:test')`
    - File contains `require('node:assert')`
    - File contains `describe('KIT-01`
    - File contains `describe('KIT-02`
    - File contains `describe('KIT-03`
    - File contains `describe('KIT-04`
    - File contains `describe('KIT-05`
    - File contains `describe('KIT-06`
    - File contains `[YOUR_PRODUCT_NAME]`
    - File contains at least 21 `it(` assertions
    - `node --test` runs without syntax errors (test failures expected, runtime errors not)
  </acceptance_criteria>
  <done>21 Nyquist structural assertions created covering KIT-01 through KIT-06; majority RED confirming tests assert against content not yet in handoff.md</done>
</task>

<task type="auto">
  <name>Task 2: handoff.md business mode Steps 4k-4m and 5e — LKT/CNT/OTR generation</name>
  <files>workflows/handoff.md</files>
  <read_first>
    - workflows/handoff.md (full file — understand Step 4i endpoint, Step 5c/5d boundary, Step 7 structure)
    - .planning/phases/91-handoff-launch-kit-assembly/91-RESEARCH.md (Patterns 1-6, Examples 1-5, Anti-Patterns)
    - references/launch-frameworks.md (CNT channel slot derivation, LKT artifact template)
    - references/business-financial-disclaimer.md (placeholder pattern for OTR email content)
    - references/business-track.md (track depth thresholds for email count)
  </read_first>
  <action>
Edit `workflows/handoff.md` to add six new sections. All business steps use INDEPENDENT IF blocks (never ELSE IF from experience/hardware gates).

**1. BM/BT detection block — insert at start of Step 4 (before Step 4a, after "### Step 4/7" heading):**

Add this exact block:
```
**Business mode detection (cached for Steps 4k, 4l, 4m, 5e, 7b-lkt, and 7c):**

\`\`\`bash
BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
if [[ "$BM" == @file:* ]]; then BM=$(cat "${BM#@file:}"); fi
BT=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessTrack 2>/dev/null)
if [[ "$BT" == @file:* ]]; then BT=$(cat "${BT#@file:}"); fi
\`\`\`

Cache `$BM` and `$BT` for use in Steps 4k, 4l, 4m, 5e, 7b-lkt, and 7c.
```

**2. Step 4k: Discover upstream business artifacts — insert AFTER Step 4i (after the hardware/experience conditionals, before Step 5):**

```markdown
#### 4k. Discover upstream business artifacts (business mode only)

IF `$BM != "true"`: skip Step 4k entirely. Set all business artifact discovery flags to false. Set LKT_GENERATED=false.

IF `$BM == "true"`:

> Applies to all productTypes when businessMode is true, including experience+business compositions.

Use Glob to discover each upstream business artifact. For each, find the highest versioned file:

- BTH: Glob `.planning/design/strategy/BTH-business-thesis-v*.md` -> BTH_PATH, BTH_AVAILABLE (true/false)
- LCV: Glob `.planning/design/strategy/LCV-lean-canvas-v*.md` -> LCV_PATH, LCV_AVAILABLE
- CMP: Glob `.planning/design/strategy/CMP-competitive-v*.md` -> CMP_PATH, CMP_AVAILABLE
- MLS: Glob `.planning/design/strategy/MLS-market-landscape-v*.md` -> MLS_PATH, MLS_AVAILABLE
- OPP: Glob `.planning/design/strategy/OPP-opportunity-v*.md` -> OPP_PATH, OPP_AVAILABLE
- SBP: Glob `.planning/design/strategy/SBP-service-blueprint-v*.md` -> SBP_PATH, SBP_AVAILABLE
- GTM: Glob `.planning/design/strategy/GTM-gtm-flow-v*.md` -> GTM_PATH, GTM_AVAILABLE
- MKT: Glob `.planning/design/strategy/MKT-brand-system-v*.md` -> MKT_PATH, MKT_AVAILABLE
- LDP: Glob `.planning/design/launch/LDP-landing-page-v*.md` -> LDP_LAUNCH_PATH, LDP_LAUNCH_AVAILABLE
- STR: Glob `.planning/design/launch/STR-stripe-config-v*.md` -> STR_PATH, STR_AVAILABLE
- DPD: Glob `.planning/design/launch/DPD-pitch-deck-v*.md` -> DPD_PATH, DPD_AVAILABLE

For each: status = "generated" if Glob returns a file, "missing" if Glob returns no results. Handle empty Glob results gracefully — never halt on missing upstream artifacts. The LKT manifest is designed to surface what is missing.

Count available artifacts: BUSINESS_ARTIFACT_COUNT = number of artifacts with status "generated".

Display: `Step 4/7 (4k): Business artifact discovery complete. {BUSINESS_ARTIFACT_COUNT}/11 artifacts found.`
```

**3. Step 4l: Synthesize LKT/CNT/OTR content — insert AFTER Step 4k:**

```markdown
#### 4l. Synthesize launch kit content (business mode only)

IF `$BM != "true"`: skip Step 4l entirely.

IF `$BM == "true"`:

Set LKT_GENERATED=true.

**4l-i. LKT manifest content:**

Build the LKT manifest artifact content with YAML frontmatter (artifact: LKT-launch-kit, version: v{N}, skill: /pde:handoff (LKT), businessTrack: {$BT}, generatedAt: {ISO date}) followed by:

# Launch Kit Manifest: {project_name from manifest}

## Artifact Registry

| Code | Artifact Name | Path | Status | Deployment Ready |
|------|--------------|------|--------|-----------------|
| BTH | Business Thesis | {BTH_PATH or "not generated"} | {BTH_AVAILABLE ? "generated" : "missing"} | {BTH_AVAILABLE ? "yes" : "no"} |
| LCV | Lean Canvas | {LCV_PATH or "not generated"} | {LCV_AVAILABLE ? "generated" : "missing"} | {LCV_AVAILABLE ? "yes" : "no"} |
| CMP | Competitive Analysis | {CMP_PATH or "not generated"} | {CMP_AVAILABLE ? "generated" : "missing"} | {CMP_AVAILABLE ? "yes" : "no"} |
| MLS | Market Landscape | {MLS_PATH or "not generated"} | {MLS_AVAILABLE ? "generated" : "missing"} | {MLS_AVAILABLE ? "yes" : "no"} |
| OPP | Opportunity Scoring | {OPP_PATH or "not generated"} | {OPP_AVAILABLE ? "generated" : "missing"} | {OPP_AVAILABLE ? "yes" : "no"} |
| SBP | Service Blueprint | {SBP_PATH or "not generated"} | {SBP_AVAILABLE ? "generated" : "missing"} | {SBP_AVAILABLE ? "yes" : "no"} |
| GTM | Go-to-Market | {GTM_PATH or "not generated"} | {GTM_AVAILABLE ? "generated" : "missing"} | {GTM_AVAILABLE ? "yes" : "no"} |
| MKT | Brand System | {MKT_PATH or "not generated"} | {MKT_AVAILABLE ? "generated" : "missing"} | {MKT_AVAILABLE ? "yes" : "no"} |
| LDP | Landing Page Spec | {LDP_LAUNCH_PATH or "not generated"} | {LDP_LAUNCH_AVAILABLE ? "generated" : "missing"} | {LDP_LAUNCH_AVAILABLE ? "yes" : "no"} |
| STR | Stripe Config | {STR_PATH or "not generated"} | {STR_AVAILABLE ? "generated" : "missing"} | {STR_AVAILABLE ? "yes" : "no"} |
| DPD | Pitch Deck | {DPD_PATH or "not generated"} | {DPD_AVAILABLE ? "generated" : "missing"} | {DPD_AVAILABLE ? "yes" : "no"} |

## Deployment Readiness Summary

| Check | Status | Notes |
|-------|--------|-------|
| Landing page spec present | {LDP_LAUNCH_AVAILABLE ? "yes" : "no"} | Consumed by /pde:deploy Phase 92 |
| Stripe config present | {STR_AVAILABLE ? "yes" : "no"} | Approval gate required before write |
| Pitch deck present | {DPD_AVAILABLE ? "yes" : "no"} | Required for investor outreach sequence |
| Domain strategy captured | {see Step 4m} | From BRF domain section |

## Domain Strategy

{Populated in Step 4m}

**4l-ii. CNT content calendar content:**

Build the CNT artifact with three phases using relative day offsets. If GTM_AVAILABLE is true, read the GTM artifact and extract channel priority annotations for channel slot names. If GTM is not available, use generic `[YOUR_PRIMARY_CHANNEL]` and `[YOUR_SECONDARY_CHANNEL]` placeholders and emit: "GTM artifact not found — content calendar uses placeholder channel slots. Run /pde:flows for channel-specific calendar."

Structure the calendar as:
- Phase 1: Pre-Launch (Days 1-14) — 5 entries covering teaser, behind-the-scenes, educational, social proof, launch announcement
- Phase 2: Launch Week (Days 15-21) — 4 entries covering launch day, feature deep-dive, user testimonial, launch recap
- Phase 3: Post-Launch (Days 22-30) — 4 entries covering onboarding nudge, case study, community building, growth milestone

Each entry row: | Day | Channel | Content Type | Topic/Theme | CTA | Linked Artifact |

All CTAs use `[YOUR_CTA_URL]` or `[YOUR_SIGNUP_URL]` format. Channel slots use `[YOUR_CHANNEL_1]`, `[YOUR_CHANNEL_2]` etc. or the actual channel names from GTM if available.

Track depth for calendar density:
- solo_founder: 10-12 entries (lean calendar)
- startup_team: 13-15 entries (standard calendar)
- product_leader: 15-18 entries (enterprise calendar with internal comms entries)

**4l-iii. OTR outreach sequences content:**

Build the OTR artifact with Resend-compatible spec tables.

**Onboarding sequence** (track-dependent length):
- solo_founder: 5 emails
- startup_team: 5-7 emails
- product_leader: 7 emails

Each row: | # | Trigger | Delay | From | Subject | Body Summary | Primary CTA | Resend `tags` |

ALL fields use structural placeholders:
- From: `[YOUR_FROM_ADDRESS]`
- Subject: uses `[YOUR_PRODUCT_NAME]`, `[YOUR_KEY_FEATURE]`, `[YOUR_QUICK_WIN]`
- Body Summary: uses `[YOUR_PRODUCT_NAME]`, `[YOUR_COMPANY_NAME]`, `[YOUR_ACTION]`
- CTA: uses `[YOUR_FIRST_CTA]`, `[YOUR_FEATURE_URL]`, `[YOUR_CTA_2]`, `[YOUR_REENGAGEMENT_CTA]`, `[YOUR_UPGRADE_OR_SHARE_CTA]`
- Tags: `{sequence: "onboarding", step: "N"}`

NEVER use actual company names, product names, or partner names from the brief. KIT-06 is a hard requirement.

**Investor outreach sequence** (3 emails, gated on DPD):
IF DPD_AVAILABLE is false: emit note "Investor outreach sequence requires pitch deck (DPD artifact). Run /pde:wireframe first." Set INVESTOR_SEQUENCE_GENERATED=false. Skip investor table.

IF DPD_AVAILABLE is true:
Generate 3-email investor sequence. Each row: | # | Trigger | Delay | From | Subject | Body Summary | Primary CTA |
- Email 1: manual send, day of outreach, problem+solution+traction pitch, deck review request
- Email 2: +5 days after email 1, brief re-pitch + new signal, intro call request
- Email 3: +7 days after email 2, final follow-up, connect on [YOUR_NETWORK]

All use `[YOUR_FROM_ADDRESS]`, `[YOUR_PRODUCT_NAME]`, `[YOUR_PROBLEM_STATEMENT]`, `[YOUR_NETWORK]` placeholders.

Display: `Step 4/7 (4l): Launch kit content synthesized. LKT manifest + CNT calendar + OTR sequences ready.`
```

**4. Step 4m: Domain strategy extraction — insert AFTER Step 4l:**

```markdown
#### 4m. Extract domain strategy from brief (business mode only)

IF `$BM != "true"`: skip Step 4m entirely.

IF `$BM == "true"`:

The brief artifact (BRF) loaded in Step 4a contains a domain strategy section (generated by Phase 85 BRIEF-05). Search BRIEF_CONTENT for a "Domain Strategy" heading or "domain" subsection.

If found: extract the domain strategy text verbatim and insert it into the LKT manifest's "## Domain Strategy" section (replacing the placeholder from Step 4l).

If not found (brief was generated before Phase 85 or domain section is absent): insert "Domain strategy not captured in brief. Re-run /pde:brief to generate domain naming and availability notes."

Display: `Step 4/7 (4m): Domain strategy extracted from brief.`
```

**5. Step 5e: Write LKT/CNT/OTR files — insert AFTER Step 5c, BEFORE Step 5d (lock release):**

```markdown
#### 5e. Write launch kit artifacts (business mode only — under existing lock from Step 5a)

IF `LKT_GENERATED != true`: skip Step 5e entirely.

IF `LKT_GENERATED == true`:

Determine versions via Glob (find highest existing version N, use N+1; if none exist, use v1):
- LKT_VERSION from `.planning/design/launch/LKT-launch-kit-v*.md`
- CNT_VERSION from `.planning/design/launch/CNT-content-calendar-v*.md`
- OTR_VERSION from `.planning/design/launch/OTR-outreach-sequences-v*.md`

Use the Write tool three times:
1. `.planning/design/launch/LKT-launch-kit-v{LKT_VERSION}.md` — full launch kit manifest from 4l (with domain strategy from 4m merged)
2. `.planning/design/launch/CNT-content-calendar-v{CNT_VERSION}.md` — 30-day content calendar from 4l
3. `.planning/design/launch/OTR-outreach-sequences-v{OTR_VERSION}.md` — email sequences from 4l

Display:
`Step 5/7 (5e): LKT-launch-kit-v{LKT_VERSION}.md written.`
`Step 5/7 (5e): CNT-content-calendar-v{CNT_VERSION}.md written.`
`Step 5/7 (5e): OTR-outreach-sequences-v{OTR_VERSION}.md written.`

IMPORTANT: Step 5e runs BEFORE Step 5d (lock release). The lock acquired in Step 5a covers all writes through 5e. Do NOT call lock-acquire again — the existing lock covers Steps 5b through 5e.
```

**6. Update Step 5d ordering note:**

In the existing Step 5d lock release section, add a note that the lock covers Steps 5b, 5c, AND 5e: "ALWAYS release, even if an error occurred during Steps 5b-5c-5e."
  </action>
  <verify>
    <automated>node --test ".planning/phases/91-handoff-launch-kit-assembly/tests/test-handoff-launch-kit.cjs" 2>&1 | grep -E "pass|fail" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - workflows/handoff.md contains `manifest-get-top-level businessMode`
    - workflows/handoff.md contains `#### 4k. Discover upstream business artifacts`
    - workflows/handoff.md contains `#### 4l. Synthesize launch kit content`
    - workflows/handoff.md contains `#### 4m. Extract domain strategy from brief`
    - workflows/handoff.md contains `#### 5e. Write launch kit artifacts`
    - workflows/handoff.md contains `LKT-launch-kit`
    - workflows/handoff.md contains `CNT-content-calendar`
    - workflows/handoff.md contains `OTR-outreach-sequences`
    - workflows/handoff.md contains `Artifact Registry`
    - workflows/handoff.md contains `Deployment Ready`
    - workflows/handoff.md contains `[YOUR_PRODUCT_NAME]`
    - workflows/handoff.md contains `[YOUR_FROM_ADDRESS]`
    - workflows/handoff.md contains `[YOUR_COMPANY_NAME]`
    - workflows/handoff.md contains `Resend-compatible` (case insensitive)
    - workflows/handoff.md contains `DPD_AVAILABLE` or `DPD-pitch-deck`
    - workflows/handoff.md contains `Pre-Launch`
    - workflows/handoff.md contains `Post-Launch`
    - workflows/handoff.md contains `30-day`
    - workflows/handoff.md contains `Domain Strategy`
    - workflows/handoff.md contains `Investor Outreach`
    - workflows/handoff.md contains `LKT_GENERATED`
    - Step 5e appears BEFORE Step 5d in the file (verify with grep -n)
    - KIT-01 through KIT-06 Nyquist tests pass (majority GREEN)
  </acceptance_criteria>
  <done>handoff.md contains business mode detection, Steps 4k/4l/4m for LKT/CNT/OTR synthesis, Step 5e for file writes under existing lock; all OTR content uses structural placeholders; investor sequence gated on DPD; majority of 21 Nyquist tests GREEN</done>
</task>

</tasks>

<verification>
- `node --test ".planning/phases/91-handoff-launch-kit-assembly/tests/test-handoff-launch-kit.cjs"` — majority GREEN after Task 2
- `grep -c "YOUR_PRODUCT_NAME" workflows/handoff.md` returns >= 1
- `grep -c "LKT-launch-kit" workflows/handoff.md` returns >= 1
- `grep -n "5e\." workflows/handoff.md` shows line number < `grep -n "5d\." workflows/handoff.md` line number
</verification>

<success_criteria>
- 21 Nyquist structural assertions exist in test file
- handoff.md extended with Steps 4k, 4l, 4m, 5e (business mode launch kit assembly)
- LKT manifest template includes 11-row Artifact Registry with status/readiness columns
- CNT calendar uses 30-day relative offsets with channel slots from GTM
- OTR sequences use Resend-compatible spec format with structural placeholders only
- Investor outreach gated on DPD availability
- Domain strategy extracted from brief into LKT
</success_criteria>

<output>
After completion, create `.planning/phases/91-handoff-launch-kit-assembly/91-01-SUMMARY.md`
</output>
