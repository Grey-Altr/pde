---
phase: 91-handoff-launch-kit-assembly
plan: "02"
type: execute
wave: 2
depends_on: ["91-01"]
files_modified:
  - workflows/handoff.md
autonomous: true
requirements: [KIT-01, KIT-05]

must_haves:
  truths:
    - "handoff.md Step 7b-lkt registers LKT/CNT/OTR artifacts in manifest with 7-call pattern"
    - "handoff.md Step 7c writes all 20 designCoverage fields including hasLaunchKit"
    - "handoff.md purpose tag says 20 designCoverage fields"
    - "handoff.md Anti-Patterns section lists all 20 fields"
    - "handoff.md output section references 20 fields"
    - "hasLaunchKit set to true for business mode, passed through for non-business"
  artifacts:
    - path: "workflows/handoff.md"
      provides: "20-field designCoverage write, LKT/CNT/OTR manifest registration, DESIGN-STATE wiring"
      contains: "hasLaunchKit"
  key_links:
    - from: "workflows/handoff.md Step 7b-lkt"
      to: "design-manifest.json"
      via: "manifest-update 7-call pattern"
      pattern: "manifest-update LKT"
    - from: "workflows/handoff.md Step 7c"
      to: "design-manifest.json designCoverage"
      via: "manifest-set-top-level designCoverage"
      pattern: "hasLaunchKit.*true"
---

<objective>
Complete handoff.md business infrastructure: Step 7b-lkt manifest registration for LKT/CNT/OTR, Step 7c upgrade from 16 to 20 designCoverage fields with hasLaunchKit, DESIGN-STATE rows for business artifacts, and text updates to purpose/anti-patterns/output sections.

Purpose: Ensure LKT/CNT/OTR artifacts are registered in the design manifest, hasLaunchKit gates the deploy stage, and all field count references in handoff.md are accurate (20 fields, not 14/13/16).
Output: handoff.md fully updated — all 21/21 Nyquist tests GREEN.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-handoff-launch-kit-assembly/91-RESEARCH.md
@.planning/phases/91-handoff-launch-kit-assembly/91-01-SUMMARY.md
</context>

<interfaces>
<!-- Current state of handoff.md sections that need updating (from RESEARCH.md) -->

Step 7c currently at line 1194-1216: writes 16 fields, must become 20
- Current: "Extract ALL sixteen current flag values: hasDesignSystem...hasProductionBible"
- Target: "Extract ALL twenty current flag values: hasDesignSystem...hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit"

Purpose tag line 2: currently says "14 designCoverage fields"
- Target: "20 designCoverage fields"

Anti-Patterns line 1271: currently says "13 fields (hasDesignSystem...hasRecommendations)"
- Target: "20 fields (hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit)"

Output section line 1308: currently says "all 13 fields preserved"
- Target: "all 20 fields preserved"

Step 7b-bib pattern (line 1170-1194): reference for how to structure 7b-lkt registration block

20-field coverage JSON (from research Pattern 7):
```json
{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":true,"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":{current},"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{current},"hasLaunchKit":true}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Step 7b-lkt manifest registration + Step 7c 20-field upgrade + DESIGN-STATE rows</name>
  <files>workflows/handoff.md</files>
  <read_first>
    - workflows/handoff.md (full file — find Step 7b, 7b-bib, 7c, 7d sections; find purpose tag line 1-2; find Anti-Patterns section; find output section)
    - .planning/phases/91-handoff-launch-kit-assembly/91-RESEARCH.md (Patterns 6, 7, 8; Examples 3-4)
  </read_first>
  <action>
Edit `workflows/handoff.md` with the following changes:

**1. Add Step 7b-lkt — insert AFTER Step 7b-bib (experience products), BEFORE Step 7c:**

```markdown
#### 7b-lkt. Register LKT/CNT/OTR artifacts in manifest (business mode only)

IF LKT_GENERATED is NOT true: SKIP Step 7b-lkt entirely. LKT/CNT/OTR artifacts are only registered for business mode runs. Non-business products must never reach this step.

IF LKT_GENERATED is true:

Register LKT artifact (7 calls):
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LKT code LKT
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LKT name "Launch Kit Manifest"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LKT type launch-kit
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LKT domain launch
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LKT path ".planning/design/launch/"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LKT status complete
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LKT version ${LKT_VERSION}
```

Register CNT artifact (7 calls):
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update CNT code CNT
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update CNT name "Content Calendar"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update CNT type content-calendar
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update CNT domain launch
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update CNT path ".planning/design/launch/"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update CNT status complete
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update CNT version ${CNT_VERSION}
```

Register OTR artifact (7 calls):
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update OTR code OTR
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update OTR name "Outreach Sequences"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update OTR type outreach-sequences
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update OTR domain launch
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update OTR path ".planning/design/launch/"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update OTR status complete
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update OTR version ${OTR_VERSION}
```

Display: `Step 7/7 (7b-lkt): LKT, CNT, OTR registered in manifest.`

Also update Step 7a DESIGN-STATE.md for business mode products — add rows under IF LKT_GENERATED guard:

```
| LKT | Launch Kit Manifest | /pde:handoff | complete | v{LKT_VERSION} | none | BTH,LCV,CMP,MLS,OPP,SBP,GTM,MKT,LDP,STR,DPD | {YYYY-MM-DD} |
| CNT | Content Calendar | /pde:handoff | complete | v{CNT_VERSION} | none | GTM | {YYYY-MM-DD} |
| OTR | Outreach Sequences | /pde:handoff | complete | v{OTR_VERSION} | none | BTH,MKT,DPD | {YYYY-MM-DD} |
```
```

**2. Replace Step 7c — upgrade from 16-field to 20-field designCoverage write:**

Replace the ENTIRE Step 7c section (from `#### 7c.` through the `Replace each {current}` line) with:

```markdown
#### 7c. Set coverage flag (CRITICAL — read-before-set to prevent clobber)

```bash
COV=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design coverage-check)
if [[ "$COV" == @file:* ]]; then COV=$(cat "${COV#@file:}"); fi
```

Parse the JSON output from coverage-check. Extract ALL twenty current flag values: `hasDesignSystem`, `hasWireframes`, `hasFlows`, `hasHardwareSpec`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit`, `hasRecommendations`, `hasStitchWireframes`, `hasPrintCollateral`, `hasProductionBible`, `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`. Default any absent field to `false`.

IF PRODUCT_TYPE is "experience" (non-hybrid-event): merge `hasProductionBible: true`, preserve all other nineteen values. Then write the full merged twenty-field object:

```bash
# For experience products:
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":{current},"hasProductionBible":true,"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{current},"hasLaunchKit":{current}}'

# For hybrid-event (both BIB and HND flags):
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":true,"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":{current},"hasProductionBible":true,"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{current},"hasLaunchKit":{current}}'
```

IF `$BM == "true"` (business mode — applies regardless of productType): merge `hasHandoff: true` AND `hasLaunchKit: true`, preserve all other eighteen values:

```bash
# For business mode runs (any productType):
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":true,"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":{current},"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{current},"hasLaunchKit":true}'
```

IF `$BM != "true"` AND productType is NOT "experience" (standard non-business products): merge `hasHandoff: true` only, preserve all other nineteen values including passing `hasLaunchKit` as current:

```bash
# For non-business, non-experience products (existing behavior, upgraded to 20 fields):
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":true,"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":{current},"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{current},"hasLaunchKit":{current}}'
```

Replace each `{current}` with the actual value read from coverage-check output (true or false).
```

**3. Update purpose tag (line 2) — change "14 designCoverage fields" to "20 designCoverage fields":**

Find `preserving all 14 designCoverage fields` in the purpose tag and replace with `preserving all 20 designCoverage fields`.

**4. Update Anti-Patterns section — change "13 fields" to "20 fields":**

Find `preserving all 13 fields (hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations)` and replace with:

`preserving all 20 fields (hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit)`

**5. Update output section (last lines) — change "13 fields" to "20 fields":**

Find `all 13 fields preserved via read-before-set` and replace with `all 20 fields preserved via read-before-set`.

Also add three new output lines to the `<output>` section for the business-mode artifacts:

```
- `.planning/design/launch/LKT-launch-kit-v{N}.md` — launch kit manifest with artifact registry, deployment readiness, and domain strategy (business mode only)
- `.planning/design/launch/CNT-content-calendar-v{N}.md` — 30-day pre-launch/launch/post-launch content calendar skeleton (business mode only)
- `.planning/design/launch/OTR-outreach-sequences-v{N}.md` — Resend-compatible onboarding and investor outreach email sequences (business mode only)
```

**6. Update Step 7d summary table — add business mode summary variant:**

After the existing experience summary table in Step 7d, add a business mode summary table:

```
IF `$BM == "true"`:
  Append to summary table:
  | Launch Kit | .planning/design/launch/LKT-launch-kit-v{LKT_VERSION}.md |
  | Content Calendar | .planning/design/launch/CNT-content-calendar-v{CNT_VERSION}.md |
  | Outreach Sequences | .planning/design/launch/OTR-outreach-sequences-v{OTR_VERSION}.md |
  | Business Track | {$BT} |
  | Artifacts Catalogued | {BUSINESS_ARTIFACT_COUNT}/11 |

  Display: `Step 7/7: Manifest updated. Coverage: hasHandoff = true, hasLaunchKit = true. Done.`
```

**7. Add anti-pattern for business mode:**

Append to Anti-Patterns section:

```
- **Generate LKT/CNT/OTR for non-business products:** Steps 4k, 4l, 4m, 5e, and 7b-lkt are gated on `$BM == "true"`. Non-business products (businessMode false or absent) must NEVER generate launch kit artifacts. The independent IF block pattern (not ELSE IF from experience/hardware gates) ensures business:experience compositions run both paths correctly.

- **Use real company names in OTR email sequences:** All email content must use structural placeholders (`[YOUR_PRODUCT_NAME]`, `[YOUR_COMPANY_NAME]`, `[YOUR_FROM_ADDRESS]`). Never extract actual names from the brief for email body content.

- **Generate investor outreach without pitch deck:** The investor outreach sequence (3 emails) in OTR is gated on DPD artifact availability. If DPD-pitch-deck is not found via Glob, emit a note and skip investor sequence generation.

- **Re-acquire write lock in Step 5e:** Step 5e runs under the existing lock from Step 5a. Do NOT call lock-acquire again — this will deadlock. The lock window covers Steps 5b through 5e, with 5d (release) always running last.
```
  </action>
  <verify>
    <automated>node --test ".planning/phases/91-handoff-launch-kit-assembly/tests/test-handoff-launch-kit.cjs" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - workflows/handoff.md contains `#### 7b-lkt. Register LKT/CNT/OTR`
    - workflows/handoff.md contains `manifest-update LKT code LKT`
    - workflows/handoff.md contains `manifest-update CNT code CNT`
    - workflows/handoff.md contains `manifest-update OTR code OTR`
    - workflows/handoff.md contains `Extract ALL twenty current flag values`
    - workflows/handoff.md contains `hasBusinessThesis` in the Step 7c coverage write JSON
    - workflows/handoff.md contains `hasMarketLandscape` in the Step 7c coverage write JSON
    - workflows/handoff.md contains `hasServiceBlueprint` in the Step 7c coverage write JSON
    - workflows/handoff.md contains `hasLaunchKit` in the Step 7c coverage write JSON (at least 4 occurrences across business/non-business/experience paths)
    - workflows/handoff.md purpose tag contains "20 designCoverage fields" (not 14)
    - workflows/handoff.md Anti-Patterns contains "20 fields" (not 13)
    - workflows/handoff.md Anti-Patterns lists `hasLaunchKit` in the field enumeration
    - workflows/handoff.md output section contains "20 fields" (not 13)
    - workflows/handoff.md output section contains `LKT-launch-kit-v{N}.md`
    - workflows/handoff.md output section contains `CNT-content-calendar-v{N}.md`
    - workflows/handoff.md output section contains `OTR-outreach-sequences-v{N}.md`
    - All 21 Nyquist tests pass: `node --test .planning/phases/91-handoff-launch-kit-assembly/tests/test-handoff-launch-kit.cjs` exits 0
  </acceptance_criteria>
  <done>handoff.md Step 7b-lkt registers LKT/CNT/OTR in manifest; Step 7c upgraded to 20-field designCoverage write with hasLaunchKit; purpose/anti-patterns/output sections updated to 20 fields; all 21/21 Nyquist tests GREEN</done>
</task>

</tasks>

<verification>
- `node --test ".planning/phases/91-handoff-launch-kit-assembly/tests/test-handoff-launch-kit.cjs"` — all 21 tests GREEN
- `grep -c "hasLaunchKit" workflows/handoff.md` returns >= 4
- `grep "20 designCoverage" workflows/handoff.md` returns match in purpose tag
- `grep "20 fields" workflows/handoff.md` returns matches in Anti-Patterns and output sections
- `grep "manifest-update LKT" workflows/handoff.md` returns >= 7 (7-call pattern)
- `grep "manifest-update CNT" workflows/handoff.md` returns >= 7
- `grep "manifest-update OTR" workflows/handoff.md` returns >= 7
</verification>

<success_criteria>
- Step 7b-lkt registers all three business artifacts (LKT, CNT, OTR) using 7-call pattern
- Step 7c upgraded from 16 to 20 fields with hasLaunchKit:true for business mode
- All four field-count references updated: purpose (14->20), Step 7c (sixteen->twenty), Anti-Patterns (13->20), output (13->20)
- DESIGN-STATE rows for LKT/CNT/OTR under LKT_GENERATED guard
- Business-specific anti-patterns added
- Summary table extended for business mode
- All 21/21 Nyquist tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/91-handoff-launch-kit-assembly/91-02-SUMMARY.md`
</output>
