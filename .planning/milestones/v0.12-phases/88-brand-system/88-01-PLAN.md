---
phase: 88-brand-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/88-brand-system/tests/test-brand-system.cjs
  - workflows/system.md
  - references/launch-frameworks.md
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03

must_haves:
  truths:
    - "system.md 20-field designCoverage write prevents clobbering business flags set by earlier skills"
    - "launch-frameworks.md has a Brand System section that system.md can reference during MKT generation"
    - "system.md has businessMode/businessTrack detection and brand token generation steps gated on businessMode"
    - "8 structural Nyquist tests exist and run green against the modified files"
  artifacts:
    - path: ".planning/phases/88-brand-system/tests/test-brand-system.cjs"
      provides: "Structural assertions for BRAND-01, BRAND-02, BRAND-03"
      min_lines: 80
    - path: "workflows/system.md"
      provides: "Business brand system generation (Steps 5c, 5d) and 20-field coverage write"
      contains: "SYS-brand-tokens.json"
    - path: "references/launch-frameworks.md"
      provides: "Brand System template section for MKT artifact generation"
      contains: "## Brand System"
  key_links:
    - from: "workflows/system.md"
      to: "references/launch-frameworks.md"
      via: "required_reading reference"
      pattern: "@references/launch-frameworks.md"
    - from: "workflows/system.md"
      to: "references/business-track.md"
      via: "required_reading reference"
      pattern: "@references/business-track.md"
    - from: ".planning/phases/88-brand-system/tests/test-brand-system.cjs"
      to: "workflows/system.md"
      via: "fs.readFileSync structural assertions"
      pattern: "readFileSync.*system\\.md"
---

<objective>
Create the test scaffold, upgrade system.md designCoverage to 20 fields, add Brand System section to launch-frameworks.md, and insert business mode detection + brand token generation (Step 5c) + MKT artifact write (Step 5d) into system.md.

Purpose: This is the only plan for Phase 88 — it delivers all three requirements (BRAND-01, BRAND-02, BRAND-03) in a single execution window. The test scaffold runs first (Wave 0 pattern), then the workflow modifications are applied and verified.

Output: Modified system.md with business brand system support, updated launch-frameworks.md, and green Nyquist tests.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-brand-system/88-RESEARCH.md
@.planning/phases/88-brand-system/88-VALIDATION.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From workflows/system.md (lines 1742-1820):
Step 5b experience token block — independent IF gate on PRODUCT_TYPE == "experience".
Steps 5c/5d must be INDEPENDENT conditional blocks (not ELSE IF), so business:experience compositions run both.

From workflows/system.md (lines 1862-1945):
Step 7 acquires write lock, updates root DESIGN-STATE (4 sections), registers SYS artifact via 7-call manifest-update, writes 16-field designCoverage (must become 20-field).

From workflows/system.md (lines 1-12):
required_reading block currently has 6 entries — business-track.md and launch-frameworks.md must be added conditionally for business mode.

From workflows/flows.md (lines 157-159):
Business mode detection pattern:
```bash
BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
BT=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessTrack 2>/dev/null)
```

From .planning/phases/87-flows-stage/tests/test-flows-sbp.cjs (lines 1-28):
Test scaffold pattern: require node:test + node:assert, resolve ROOT from __dirname, readFileSync the workflow file, describe/it blocks per requirement ID.

From references/launch-frameworks.md (full file, 170 lines):
Currently has: Lean Canvas, Pitch Deck Formats, Service Blueprint, Pricing Config Schema, Consumers section. NO Brand System section exists. Must add Brand System section before Consumers section.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Nyquist test scaffold + add Brand System section to launch-frameworks.md</name>
  <files>.planning/phases/88-brand-system/tests/test-brand-system.cjs, references/launch-frameworks.md</files>
  <read_first>
    - .planning/phases/87-flows-stage/tests/test-flows-sbp.cjs (test pattern to follow)
    - references/launch-frameworks.md (current content — Brand System section will be appended before Consumers)
    - workflows/system.md (target file the tests assert against — read to confirm current state)
  </read_first>
  <behavior>
    - Test 1 (BRAND-01): system.md contains "MKT-brand-system" artifact filename pattern
    - Test 2 (BRAND-01): system.md contains businessMode gate (manifest-get-top-level businessMode) before MKT generation
    - Test 3 (BRAND-01): system.md contains "Positioning Statement", "Tone of Voice Spectrum", and "Visual Differentiation Rationale" section markers
    - Test 4 (BRAND-02): system.md contains "SYS-brand-tokens.json" filename pattern
    - Test 5 (BRAND-02): system.md contains "brand-marketing" DTCG group key
    - Test 6 (BRAND-02): system.md designCoverage write contains all 20 fields including "hasBusinessThesis", "hasMarketLandscape", "hasServiceBlueprint", "hasLaunchKit"
    - Test 7 (BRAND-03): system.md contains MKT manifest-update with dependsOn including "BRF" and "BTH"
    - Test 8 (BRAND-03): references/launch-frameworks.md contains "## Brand System" section
  </behavior>
  <action>
**Part A: Create test file** at `.planning/phases/88-brand-system/tests/test-brand-system.cjs`

Follow the exact pattern from `.planning/phases/87-flows-stage/tests/test-flows-sbp.cjs`:
- `'use strict';` header
- Comment block listing BRAND-01, BRAND-02, BRAND-03 coverage
- `require('node:test')` for describe/it, `require('node:assert')` for assert
- `ROOT = path.resolve(__dirname, '..', '..', '..', '..')` (4 levels up from tests dir)
- `const systemContent = fs.readFileSync(path.join(ROOT, 'workflows', 'system.md'), 'utf-8');`
- `const frameworksContent = fs.readFileSync(path.join(ROOT, 'references', 'launch-frameworks.md'), 'utf-8');`

8 tests in 3 describe blocks:

```javascript
describe('BRAND-01: MKT artifact generation', () => {
  it('system.md contains "MKT-brand-system" artifact filename pattern', () => {
    assert.ok(systemContent.includes('MKT-brand-system'), 'system.md must contain "MKT-brand-system" artifact filename pattern');
  });
  it('system.md contains businessMode gate before MKT generation', () => {
    const bmIdx = systemContent.indexOf('manifest-get-top-level businessMode');
    const mktIdx = systemContent.indexOf('MKT-brand-system');
    assert.ok(bmIdx !== -1, '"manifest-get-top-level businessMode" must be present in system.md');
    assert.ok(mktIdx !== -1, '"MKT-brand-system" must be present in system.md');
    assert.ok(bmIdx < mktIdx, 'businessMode detection must appear before MKT artifact generation');
  });
  it('system.md contains positioning, tone of voice, and visual differentiation sections', () => {
    assert.ok(systemContent.includes('Positioning Statement'), 'system.md must contain "Positioning Statement"');
    assert.ok(systemContent.includes('Tone of Voice Spectrum'), 'system.md must contain "Tone of Voice Spectrum"');
    assert.ok(systemContent.includes('Visual Differentiation Rationale'), 'system.md must contain "Visual Differentiation Rationale"');
  });
});

describe('BRAND-02: Brand token DTCG extension', () => {
  it('system.md contains "SYS-brand-tokens.json" filename pattern', () => {
    assert.ok(systemContent.includes('SYS-brand-tokens.json'), 'system.md must contain "SYS-brand-tokens.json"');
  });
  it('system.md contains "brand-marketing" DTCG group key', () => {
    assert.ok(systemContent.includes('brand-marketing'), 'system.md must contain "brand-marketing" DTCG group key');
  });
  it('system.md designCoverage write contains all 20 fields', () => {
    assert.ok(systemContent.includes('hasBusinessThesis'), 'designCoverage must include hasBusinessThesis');
    assert.ok(systemContent.includes('hasMarketLandscape'), 'designCoverage must include hasMarketLandscape');
    assert.ok(systemContent.includes('hasServiceBlueprint'), 'designCoverage must include hasServiceBlueprint');
    assert.ok(systemContent.includes('hasLaunchKit'), 'designCoverage must include hasLaunchKit');
  });
});

describe('BRAND-03: Downstream reference wiring', () => {
  it('system.md MKT manifest-update dependsOn includes BRF and BTH', () => {
    assert.ok(systemContent.includes('manifest-update MKT dependsOn'), 'system.md must contain MKT dependsOn manifest-update call');
    const depLine = systemContent.split('\n').find(l => l.includes('manifest-update MKT dependsOn'));
    assert.ok(depLine && depLine.includes('BRF'), 'MKT dependsOn must include BRF');
    assert.ok(depLine && depLine.includes('BTH'), 'MKT dependsOn must include BTH');
  });
  it('launch-frameworks.md contains Brand System section', () => {
    assert.ok(frameworksContent.includes('## Brand System'), 'launch-frameworks.md must contain "## Brand System" section');
  });
});
```

**Part B: Add Brand System section to `references/launch-frameworks.md`**

Insert the following new section BEFORE the existing `## Consumers` section (line 164). Use the Edit tool.

```markdown
## Brand System

Source: Geoffrey Moore positioning statement format + brand strategy best practices

The Brand System (MKT artifact) captures marketing positioning, tone of voice, and visual differentiation rationale. It is generated by `/pde:system` when `businessMode == true` and feeds into Phase 89 landing page wireframe and pitch deck content.

**Artifact code:** MKT
**File path:** `.planning/design/strategy/MKT-brand-system-v{N}.md`
**Domain:** strategy

### Positioning Statement Format

Use the Geoffrey Moore template:

> For [target customer], [product name] is the [category] that [primary benefit] because [unique differentiator].

Derive from:
- **Target customer** from LCV Customer Segments box
- **Product name** from BRF project name
- **Category** from BTH market section
- **Primary benefit** from BTH solution section
- **Unique differentiator** from BTH unfair advantage section + LCV UVP box

### Tone of Voice Spectrum

| Dimension | Warm End | Authoritative End |
|-----------|----------|-------------------|
| Formality | Conversational | Professional |
| Energy | Calm | Dynamic |
| Expertise | Accessible | Expert |
| Personality | Playful | Serious |

Position the brand on each dimension based on the business thesis audience and unfair advantage. Include:
- **Primary tone descriptor** — single most important voice characteristic
- **Secondary tone descriptor** — supporting voice characteristic
- **Voice to avoid** — tone that would undermine brand positioning

### Visual Differentiation Rationale

Document how the design system's visual choices deliberately support brand positioning:
- **Category visual conventions** — what competitors typically look like (derive from CMP positioning matrix)
- **Our differentiation** — how visual choices depart from category conventions
- **Primary palette role** — how the primary color supports brand positioning
- **Typography personality** — how typeface reinforces brand character

### Brand Voice Examples

Track-depth controlled:
- `solo_founder`: 2 examples (headline, CTA)
- `startup_team`: 4 examples (headline, CTA, feature description, email subject)
- `product_leader`: 6 examples (headline, CTA, feature description, email subject, executive summary, internal memo)

| Context | Avoid | Prefer |
|---------|-------|--------|
| Headline | [generic category language] | [differentiating brand language] |
| CTA | [commodity phrasing] | [brand-specific action language] |
```

Also update the `## Consumers` section to add system.md:
```
- `workflows/system.md` — Phase 88: brand system MKT artifact generation
```

And update the header metadata to include SYS in Ownership:
```
**Ownership:** Shared (BRF, SYS, WFR, FLW, HND)
```

And update the Scope line to include brand system:
```
**Scope:** Lean canvas schema, pitch deck slide formats, service blueprint lanes, pricing config schema, brand system template
```
  </action>
  <verify>
    <automated>node --test .planning/phases/88-brand-system/tests/test-brand-system.cjs 2>&1 | grep -E "(pass|fail|#)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/88-brand-system/tests/test-brand-system.cjs exists and contains `require('node:test')`
    - .planning/phases/88-brand-system/tests/test-brand-system.cjs contains 8 `it(` test declarations
    - .planning/phases/88-brand-system/tests/test-brand-system.cjs contains `readFileSync` calls for both `system.md` and `launch-frameworks.md`
    - references/launch-frameworks.md contains `## Brand System`
    - references/launch-frameworks.md contains `Geoffrey Moore`
    - references/launch-frameworks.md contains `workflows/system.md` in Consumers section
    - references/launch-frameworks.md Ownership line contains `SYS`
    - Tests for BRAND-01 (tests 1-3) should FAIL at this point (system.md not yet modified) — this confirms RED state
    - Test 8 (BRAND-03 launch-frameworks) should PASS (Brand System section added)
    - Test 6 (BRAND-02 20-field coverage) should FAIL (system.md still has 16-field write)
  </acceptance_criteria>
  <done>8 Nyquist tests exist in test file; launch-frameworks.md has Brand System section; tests confirm RED state for system.md assertions</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade system.md — 20-field coverage, business detection, Steps 5c/5d, DESIGN-STATE wiring</name>
  <files>workflows/system.md</files>
  <read_first>
    - workflows/system.md (FULL file — executor needs to see current Step 5b, Step 7, required_reading, anti-patterns, output sections)
    - .planning/phases/88-brand-system/88-RESEARCH.md (architecture patterns, code examples, pitfalls)
    - workflows/flows.md (lines 157-159 for businessMode detection pattern; lines 750-763 for SBP manifest registration pattern; lines 1062 for 20-field coverage write)
    - references/business-track.md (track vocabulary and depth thresholds for brand voice calibration)
  </read_first>
  <action>
Apply 6 edits to `workflows/system.md` using the Edit tool. Each edit is described with exact content and location.

**Edit 1: Add business-mode required_reading entries (after line 11)**

After the existing `@references/motion-design.md` line (line 11) and before `@references/composition-typography.md` (line 12 — keep this), insert:

```markdown
@references/business-track.md
@references/launch-frameworks.md
```

Note: These references are loaded regardless of business mode — the workflow agent reads all required_reading at init. The Step 5c gate controls whether brand artifacts are generated, not whether references are loaded.

**Edit 2: Add comment in process header about business mode (after the existing experience comment, around line 71)**

After the existing `<!-- Experience product type -->` comment block (line 71), add:

```markdown
  <!-- Business product type (Phase 88): business brand system and marketing tokens generated in Steps 5c/5d — see businessMode == "true" block below. Step 5b (experience) and Steps 5c/5d (business) are independent conditional blocks — both run for business:experience compositions. -->
```

**Edit 3: Insert Steps 5c and 5d after Step 5b experience token block (after line 1835, before Step 6)**

After the Step 5b block ends (the `---` separator before Step 6/7 on line 1835-1836), insert the following content:

```markdown

#### Step 5c: Brand token generation (business mode only)

BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
BT=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessTrack 2>/dev/null)

**IF `$BM == "true"`:** proceed to Steps 5c and 5d below before continuing to Step 6.

**ELSE (`$BM != "true"`):** Skip silently. Set BRAND_TOKENS_GENERATED=false. Set MKT_WRITTEN=false. Continue to Step 6.

---

Generate `SYS-brand-tokens.json` as a DTCG 2025.10 compliant JSON file with 1 top-level category key: `brand-marketing`. Two sub-groups: `brand-voice` (5 tokens) and `campaign-palette-variants` (4 tokens). Total: 9 leaf nodes.

Each leaf node MUST have `$value`, `$type`, and `$description` fields per DTCG 2025.10.

**DTCG structure:**

```json
{
  "brand-marketing": {
    "$description": "Marketing-specific brand tokens. Extends SYS-tokens.json without modifying core design system categories.",
    "brand-voice": {
      "tone-primary": {
        "$value": "[BRAND_VOICE_PRIMARY_DERIVED_FROM_UVP]",
        "$type": "string",
        "$description": "Primary tone descriptor derived from UVP and business thesis"
      },
      "tone-spectrum-warm": {
        "$value": "[WARM_END_OF_TONE_SPECTRUM]",
        "$type": "string",
        "$description": "Warm/approachable end of the tone of voice spectrum"
      },
      "tone-spectrum-authoritative": {
        "$value": "[AUTHORITATIVE_END_OF_TONE_SPECTRUM]",
        "$type": "string",
        "$description": "Authoritative/expert end of the tone of voice spectrum"
      },
      "positioning-statement": {
        "$value": "[POSITIONING_STATEMENT_FROM_MKT_ARTIFACT]",
        "$type": "string",
        "$description": "Reference to MKT artifact positioning statement — for downstream consumption by wireframe and pitch deck"
      },
      "audience-voice-descriptor": {
        "$value": "[HOW_AUDIENCE_TALKS_DERIVED_FROM_LEAN_CANVAS_CUSTOMER_SEGMENTS]",
        "$type": "string",
        "$description": "Language register of target audience — calibrates copy tone"
      }
    },
    "campaign-palette-variants": {
      "primary-campaign": {
        "$value": "{color.primitive.primary.500}",
        "$type": "color",
        "$description": "Primary campaign color — references core design system primary via token alias"
      },
      "accent-campaign": {
        "$value": "{color.harmony.analogous-warm.500}",
        "$type": "color",
        "$description": "Accent campaign color — warm harmony from design system"
      },
      "neutral-campaign": {
        "$value": "{color.primitive.neutral.100}",
        "$type": "color",
        "$description": "Campaign background neutral — light surface from design system"
      },
      "cta-campaign": {
        "$value": "{color.semantic.action}",
        "$type": "color",
        "$description": "Campaign CTA color — semantic action alias from design system"
      }
    }
  }
}
```

**CRITICAL:** Campaign palette variant `$value` fields use `{token.path}` alias syntax pointing into `SYS-tokens.json` categories — never raw oklch values. This ensures they inherit dark mode overrides and stay coherent with the core palette.

**CRITICAL:** Derive `brand-voice` token `$value` fields from the loaded brief's `## Domain Strategy` section (which contains brand positioning seeds from BRIEF-05). The `$value` strings above are STRUCTURAL PLACEHOLDERS — the agent fills them with content derived from the brief context at generation time.

Write to: `.planning/design/visual/SYS-brand-tokens.json`

Display: `  -> Created: visual/SYS-brand-tokens.json`

**Generate CSS custom properties from brand tokens:**

```bash
CSS_BRAND=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design tokens-to-css ".planning/design/visual/SYS-brand-tokens.json" --raw)
if [[ "$CSS_BRAND" == @file:* ]]; then CSS_BRAND=$(cat "${CSS_BRAND#@file:}"); fi
```

Write `$CSS_BRAND` to `.planning/design/visual/SYS-brand-tokens.css`

Display: `  -> Created: visual/SYS-brand-tokens.css`

**Register brand tokens in manifest:**

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-BRAND code SYS-BRAND
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-BRAND name "Brand Marketing Tokens"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-BRAND type brand-tokens
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-BRAND domain visual
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-BRAND path ".planning/design/visual/SYS-brand-tokens.json"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-BRAND status draft
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-BRAND dependsOn '["SYS","BRF","BTH"]'
```

Set BRAND_TOKENS_GENERATED=true.

---

#### Step 5d: MKT brand system artifact (business mode only)

**IF `$BM == "true"` AND BRAND_TOKENS_GENERATED == true:**

Read the brief's `## Domain Strategy` section for brand positioning seeds (project name, category, audience descriptor, brand personality seeds). Also read BTH artifact sections (Problem, Solution, Market, Unfair Advantage) and LCV UVP box from the brief context loaded in Step 2.

Generate the MKT artifact at `.planning/design/strategy/MKT-brand-system-v{N}.md` using the template from `@references/launch-frameworks.md` `## Brand System` section.

The MKT artifact content follows this structure:

```markdown
---
artifact: MKT-brand-system
version: v{N}
Skill: /pde:system (MKT)
businessTrack: {$BT}
dependsOn: BRF, BTH, LCV
---

# Brand & Marketing System

*Generated by /pde:system (business mode) v{N} | {ISO date}*

## Positioning Statement

For [target customer from LCV Customer Segments], [product name from BRF] is the [category from BTH Market] that [primary benefit from BTH Solution] because [unique differentiator from BTH Unfair Advantage + LCV UVP].

## Tone of Voice Spectrum

| Dimension | Warm End | Authoritative End | Primary Position |
|-----------|----------|-------------------|-----------------|
| Formality | Conversational | Professional | [position derived from audience archetype] |
| Energy | Calm | Dynamic | [position derived from product category] |
| Expertise | Accessible | Expert | [position derived from businessTrack — solo_founder leans Accessible, product_leader leans Expert] |
| Personality | Playful | Serious | [position derived from brand personality seeds] |

**Primary tone descriptor:** [Single most important voice characteristic]
**Secondary tone descriptor:** [Supporting voice characteristic]
**Voice to avoid:** [Tone that would undermine brand positioning]

## Visual Differentiation Rationale

**Category visual conventions:** [What competitors in this space typically look like — derive from CMP competitive positioning matrix if available]
**Our differentiation:** [How visual choices deliberately depart from category conventions]
**Primary palette role:** [How the design system's primary color supports brand positioning]
**Typography personality:** [How typeface selection reinforces brand character]

## Brand Voice Examples

*Track-depth controlled: solo_founder = 2 examples, startup_team = 4 examples, product_leader = 6 examples*

| Context | Avoid | Prefer |
|---------|-------|--------|
| Headline | [generic category language] | [differentiating brand language] |
| CTA | [commodity phrasing] | [brand-specific action language] |

## Downstream References

- Landing page wireframe (Phase 89): Use Positioning Statement for hero headline framing, Tone of Voice for CTA and feature copy
- Pitch deck (Phase 89): Use Positioning Statement for solution slide, Visual Differentiation for competition slide

---
*Generated by /pde:system (MKT) v{N} | {ISO date}*
```

**Financial content check (mandatory):**

```bash
if grep -qE '\$[0-9]' ".planning/design/strategy/MKT-brand-system-v${N}.md" 2>/dev/null; then
  echo "ERROR: Dollar amount detected in MKT artifact. Use [YOUR_X] placeholders only."
  grep -nE '\$[0-9]' ".planning/design/strategy/MKT-brand-system-v${N}.md"
  exit 1
fi
```

Display: `  -> Created: strategy/MKT-brand-system-v{N}.md`

**Register MKT artifact in manifest:**

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MKT code MKT
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MKT name "Brand Marketing System"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MKT type brand-system
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MKT domain strategy
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MKT path ".planning/design/strategy/MKT-brand-system-v${N}.md"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MKT status draft
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MKT dependsOn '["BRF","BTH","LCV"]'
```

Set MKT_WRITTEN=true.

**ELSE:** Set MKT_WRITTEN=false. Skip silently.

**Edit 4: Add MKT DESIGN-STATE wiring to Step 7 (inside the lock window, after the existing Iteration History update)**

After the existing Step 7 Iteration History append entry (line ~1911), add:

```markdown

**IF `MKT_WRITTEN == true`:**

Add MKT row to root DESIGN-STATE.md (inside the existing write lock window):

1. **Cross-Domain Dependency Map** — add MKT row:
   ```
   | MKT | strategy | BRF,BTH | current |
   ```

2. **Quick Reference section** — add row:
   ```
   | Brand System | v{N} |
   ```

3. **Decision Log** — append entry:
   ```
   | MKT | brand system generated, {businessTrack} track | {YYYY-MM-DD} |
   ```

4. **Iteration History** — append entry:
   ```
   | MKT-brand-system-v{N}.md | v{N} | Created by /pde:system | {YYYY-MM-DD} |
   ```
```

**Edit 5: Upgrade designCoverage write from 16 fields to 20 fields (lines 1931-1945)**

Replace the existing 16-field designCoverage block. The current text says "ALL sixteen current flag values" and lists 16 fields. Replace with:

Old text (lines 1938): `Parse the JSON output from coverage-check. Extract ALL sixteen current flag values: `hasDesignSystem`, `hasWireframes`, `hasFlows`, `hasHardwareSpec`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit`, `hasRecommendations`, `hasStitchWireframes`, `hasPrintCollateral`, `hasProductionBible`. Default any absent field to `false`. Merge `hasDesignSystem: true` while preserving all other fifteen values. Then write the full merged sixteen-field object:`

New text: `Parse the JSON output from coverage-check. Extract ALL twenty current flag values: `hasDesignSystem`, `hasWireframes`, `hasFlows`, `hasHardwareSpec`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit`, `hasRecommendations`, `hasStitchWireframes`, `hasPrintCollateral`, `hasProductionBible`, `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`. Default any absent field to `false`. Merge `hasDesignSystem: true` while preserving all other nineteen values. Then write the full merged twenty-field object:`

Old code block (line 1942): the 16-field JSON write
New code block:
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":true,"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":{current},"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{current},"hasLaunchKit":{current}}'
```

Old closing text (line 1945): `Use the actual values read from coverage-check — do not hardcode false for fields that may have been set by other skills. The key invariant: `manifest-set-top-level` performs FLAT key assignment, so `designCoverage` must be set as the FULL sixteen-field JSON object every time. All 16 fields: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible.`

New closing text: `Use the actual values read from coverage-check — do not hardcode false for fields that may have been set by other skills. The key invariant: `manifest-set-top-level` performs FLAT key assignment, so `designCoverage` must be set as the FULL twenty-field JSON object every time. All 20 fields: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit.`

**Edit 6: Update output section and summary (lines 2017-2033)**

In the `<output>` section, add after the existing SYS-experience-tokens.json line (or after the manifest line):
```
- `.planning/design/visual/SYS-brand-tokens.json` — DTCG brand marketing token extension (business mode only)
- `.planning/design/visual/SYS-brand-tokens.css` — CSS custom properties from brand tokens (business mode only)
- `.planning/design/strategy/MKT-brand-system-v{N}.md` — Brand marketing system artifact (business mode only)
```

In the Summary table, add to "Files created" conditionally:
```
| Files created (business mode) | visual/SYS-brand-tokens.json, visual/SYS-brand-tokens.css, strategy/MKT-brand-system-v{N}.md |
```

Also add to the anti-patterns section (after line 2013):
```
- NEVER merge brand tokens into SYS-tokens.json. Write a separate SYS-brand-tokens.json file. The manifest-set-top-level command has no deep merge — writing a new root key into SYS-tokens.json risks corrupting all 7 existing categories.
- NEVER use raw oklch values in campaign-palette-variants tokens. Campaign palette tokens MUST use {token.path} alias syntax pointing into SYS-tokens.json (e.g., {color.primitive.primary.500}). Raw values drift out of sync when the core system is regenerated.
- NEVER use ELSE IF or ELSE branching between Step 5b (experience tokens) and Steps 5c/5d (business brand tokens). Both are independent conditional blocks — a business:experience composition must run BOTH.
```
  </action>
  <verify>
    <automated>node --test .planning/phases/88-brand-system/tests/test-brand-system.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All 8 Nyquist tests pass (exit code 0)
    - workflows/system.md contains `manifest-get-top-level businessMode` (businessMode detection)
    - workflows/system.md contains `SYS-brand-tokens.json` (brand token file reference)
    - workflows/system.md contains `MKT-brand-system` (MKT artifact filename)
    - workflows/system.md contains `brand-marketing` (DTCG group key)
    - workflows/system.md contains `Positioning Statement` section marker
    - workflows/system.md contains `Tone of Voice Spectrum` section marker
    - workflows/system.md contains `Visual Differentiation Rationale` section marker
    - workflows/system.md contains `manifest-update MKT dependsOn` with `BRF` and `BTH` in the value
    - workflows/system.md designCoverage write contains `hasBusinessThesis` and `hasMarketLandscape` and `hasServiceBlueprint` and `hasLaunchKit`
    - workflows/system.md contains `@references/business-track.md` in required_reading
    - workflows/system.md contains `@references/launch-frameworks.md` in required_reading
    - workflows/system.md contains `Step 5c` and `Step 5d` headers
    - workflows/system.md contains `BRAND_TOKENS_GENERATED` flag variable
    - workflows/system.md contains `MKT_WRITTEN` flag variable
    - workflows/system.md contains `tokens-to-css` call for brand tokens
    - workflows/system.md anti-patterns section contains warning about merging into SYS-tokens.json
    - workflows/system.md output section lists SYS-brand-tokens.json and MKT-brand-system
  </acceptance_criteria>
  <done>All 8 Nyquist tests green; system.md has Steps 5c/5d with business brand system generation; 20-field designCoverage write; DESIGN-STATE wiring for MKT; anti-patterns updated; output section updated</done>
</task>

</tasks>

<verification>
All 8 structural assertions pass:
```bash
node --test .planning/phases/88-brand-system/tests/test-brand-system.cjs
```

Manual grep verification:
```bash
grep -c "hasBusinessThesis" workflows/system.md  # should be >= 2 (coverage list + write)
grep -c "MKT-brand-system" workflows/system.md   # should be >= 2
grep -c "SYS-brand-tokens" workflows/system.md    # should be >= 2
grep "## Brand System" references/launch-frameworks.md  # should match
grep "manifest-get-top-level businessMode" workflows/system.md  # should match
```
</verification>

<success_criteria>
- 8/8 Nyquist tests green
- system.md has businessMode detection before MKT generation
- system.md Steps 5c/5d are independent from Step 5b (no ELSE IF between them)
- system.md designCoverage write has exactly 20 fields
- launch-frameworks.md has Brand System section with positioning statement template
- MKT artifact template includes Positioning Statement, Tone of Voice Spectrum, Visual Differentiation Rationale
- Brand tokens use {token.path} alias syntax for campaign palette variants
- DESIGN-STATE wiring adds MKT rows conditionally under MKT_WRITTEN gate
</success_criteria>

<output>
After completion, create `.planning/phases/88-brand-system/88-01-SUMMARY.md`
</output>
