---
phase: 90-critique-hig-extensions
plan: 02
type: execute
wave: 2
depends_on: [90-01]
files_modified:
  - workflows/hig.md
autonomous: true
requirements: [QUAL-03, QUAL-04]

must_haves:
  truths:
    - "hig.md detects businessMode and adds business communications section when true and LIGHT_MODE is false"
    - "Business communications HIG covers pitch deck readability, email cadence, and content calendar structure"
    - "HIG business findings use critical/major/minor/nit severity only — no 'info' severity"
    - "hig.md uses 20-field designCoverage write including hasBusinessThesis"
    - "Business communications block does NOT run in --light mode"
  artifacts:
    - path: "workflows/hig.md"
      provides: "Business communications HIG section with 3 domain checks"
      contains: "Pitch Deck Readability"
  key_links:
    - from: "workflows/hig.md"
      to: "references/business-track.md"
      via: "Load reference for track-specific email count expectations"
      pattern: "business-track.md"
    - from: "workflows/hig.md"
      to: ".planning/design/launch/DPD-pitch-deck-outline-v*.md"
      via: "Glob discovery for pitch deck readability check"
      pattern: "DPD-pitch-deck-outline"
---

<objective>
Extend hig.md with a business communications section and upgrade its designCoverage write from 16 to 20 fields.

Purpose: Business-mode users running /pde:hig get pitch deck readability, email cadence structure, and content calendar guidelines evaluated — plus the coverage write no longer clobbers the 4 new business fields.
Output: All Phase 90 Nyquist tests pass GREEN across both critique.md and hig.md.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-critique-hig-extensions/90-RESEARCH.md
@.planning/phases/90-critique-hig-extensions/90-01-SUMMARY.md

<interfaces>
<!-- hig.md Step 4 structure (from research lines 216-248): -->
<!-- IF productType === "experience": [7 physical HIG domains] -->
<!-- ELSE: -->
<!--   IF LIGHT_MODE=true: [5 mandatory checks only] STOP -->
<!--   IF full mode: [4a through 4i] -->
<!--   <<< Step 4-BUSINESS inserts after 4i >>> -->

<!-- Business mode detection (same pattern as critique.md / wireframe.md): -->
BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
if [[ "$BM" == @file:* ]]; then BM=$(cat "${BM#@file:}"); fi

<!-- Severity: critical | major | minor | nit (never "info") -->
<!-- Finding format: | {severity} | {effort} | {description} | {guideline} | -->

<!-- OTR artifact: .planning/design/launch/OTR-email-sequence-v*.md (Phase 91 — may not exist yet) -->
<!-- CNT artifact: .planning/design/launch/CNT-content-calendar-v*.md (Phase 91 — may not exist yet) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Business communications HIG section + 20-field coverage upgrade in hig.md</name>
  <files>workflows/hig.md</files>
  <read_first>
    - workflows/hig.md (FULL file — understand Step 4 ELSE clause structure, --light mode handling, Step 7 coverage write)
    - .planning/phases/90-critique-hig-extensions/90-RESEARCH.md (Pattern 3: Business Communications HIG, lines 211-271; Pattern 4: 16->20 field upgrade, lines 272-298; anti-patterns lines 299-311)
    - references/business-track.md (track vocabulary — email count expectations per track)
    - workflows/critique.md (reference: how Plan 01 implemented the $BM/$BT detection pattern, for consistency)
  </read_first>
  <action>
Make TWO modifications to hig.md:

**Modification 1: Step 4-BUSINESS block in the ELSE clause (after Step 4i, before "Display: Step 4/7 complete")**

First, add $BM/$BT detection in the ELSE clause preamble (same location pattern as critique.md):
```
BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
if [[ "$BM" == @file:* ]]; then BM=$(cat "${BM#@file:}"); fi
BT=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessTrack 2>/dev/null)
if [[ "$BT" == @file:* ]]; then BT=$(cat "${BT#@file:}"); fi
```

Then insert the business communications block:

```
#### Step 4-BUSINESS: Business Communications HIG

IF `$LIGHT_MODE == true`: skip this entire block. (Defense-in-depth: business communications MUST NOT run in --light mode, which is used by /pde:critique delegation.)

IF `$BM != "true"`: skip this entire block.

IF `$BM == "true"` AND `$LIGHT_MODE == false`:

Load @references/business-track.md for track-specific expectations.

##### Domain 1: Pitch Deck Readability

Use Glob to find `.planning/design/launch/DPD-pitch-deck-outline-v*.md`. Take highest version.

IF DPD absent: note "DPD pitch deck outline not yet generated; run /pde:wireframe in business mode to create." — do NOT halt.

IF DPD present:
- Check slide count against track format:
  - solo_founder: expect ~10 slides (YC format)
  - startup_team: expect 10-15 slides (YC or Sequoia depending on funding signal)
  - product_leader: expect internal business case format
- Check each slide headline/question is concise (< 12 words). Severity: minor if > 12 words.
- Check solution slide contains UVP from LCV Box 3 (search for `LCV.box3.UVP` anchor). Severity: major if UVP absent from solution slide.

##### Domain 2: Email Cadence Structure

Use Glob to find `.planning/design/launch/OTR-email-sequence-v*.md`. Take highest version.

IF OTR absent: note "OTR email sequence not yet generated; run /pde:handoff to create." — do NOT halt. Skip to Domain 3.

IF OTR present:
- Check onboarding email count matches track expectation:
  - solo_founder: 5 emails
  - startup_team: 5-7 emails
  - product_leader: 7 emails
  Severity: minor for count mismatch.
- Check each email has trigger, delay, and CTA fields populated. Severity: major for missing CTA fields.

##### Domain 3: Content Calendar Structure

Use Glob to find `.planning/design/launch/CNT-content-calendar-v*.md`. Take highest version.

IF CNT absent: note "CNT content calendar not yet generated; run /pde:handoff to create." — do NOT halt.

IF CNT present:
- Check 30-day skeleton with pre-launch, launch, and post-launch phases present. Severity: minor if any phase missing.
- Check content slots derive from GTM channel priorities (look for GTM/channel references). Severity: major if no GTM cross-reference.
```

Use the EXACT finding format from existing hig.md Step 4f: `| {severity} | {effort} | {description} | {guideline} |`. Severity values: critical, major, minor, nit ONLY.

**CRITICAL placement rules:**
- The LIGHT_MODE check MUST be the FIRST guard in the block (before $BM check) — defense-in-depth prevents business HIG running in --light delegation mode.
- The block MUST be INSIDE the ELSE clause (not outside it) — prevents running for experience products.
- This is an INDEPENDENT IF block (not ELSE IF from the experience gate).

**Modification 2: Step 7 designCoverage write upgrade (16 -> 20 fields)**

Find the Step 7 designCoverage write section. Replace the 16-field pattern:
```
Extract ALL 16 current coverage flag values (default absent fields to false): hasDesignSystem, hasWireframes, ...hasProductionBible
```

With the 20-field read-before-set pattern:
```
Extract ALL TWENTY current coverage flag values (default absent fields to false):
hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit

Set `hasHigAudit: true`, pass all 19 other fields through with their current values.
```

Ensure the coverage write command includes ALL 20 field names including `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`.
  </action>
  <verify>
    <automated>node --test .planning/phases/90-critique-hig-extensions/tests/test-critique-hig-business.cjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - hig.md contains string `manifest-get-top-level businessMode`
    - hig.md contains string `LIGHT_MODE` check BEFORE `$BM` check in the business communications block
    - hig.md contains string `Pitch Deck Readability` (case-sensitive)
    - hig.md contains string (case-insensitive) `email cadence`
    - hig.md contains string (case-insensitive) `content calendar`
    - hig.md contains string `DPD-pitch-deck-outline`
    - hig.md contains string `OTR-email-sequence`
    - hig.md contains string `CNT-content-calendar`
    - hig.md does NOT contain string `| info |`
    - hig.md contains string `hasBusinessThesis` in the designCoverage write section
    - hig.md contains string `hasMarketLandscape`
    - hig.md contains string `hasServiceBlueprint`
    - hig.md contains string `hasLaunchKit`
    - hig.md contains string `business-track.md` (reference load)
    - ALL tests in test-critique-hig-business.cjs pass GREEN (0 failures)
  </acceptance_criteria>
  <done>hig.md has business communications section (3 domains: pitch deck readability, email cadence, content calendar) with LIGHT_MODE defense-in-depth guard, and 20-field designCoverage write. All Phase 90 Nyquist tests pass GREEN.</done>
</task>

</tasks>

<verification>
Run full Nyquist suite — ALL tests must pass:
```bash
node --test .planning/phases/90-critique-hig-extensions/tests/test-critique-hig-business.cjs
```

Confirm:
- All QUAL-01 tests pass (business perspectives in critique.md)
- All QUAL-02 tests pass (pitch coherence cross-check in critique.md)
- All QUAL-03 tests pass (business communications in hig.md)
- All QUAL-04 tests pass (no "info" severity in either file)
- All INTG-02 tests pass (20-field coverage writes in both files)
- Zero test failures
</verification>

<success_criteria>
- hig.md contains business communications section gated on $BM == "true" AND LIGHT_MODE == false
- hig.md designCoverage write upgraded to 20 fields
- Full Nyquist test suite passes with 0 failures
- No "info" severity introduced in hig.md
- OTR and CNT checks degrade gracefully when artifacts absent (Phase 91 not yet run)
</success_criteria>

<output>
After completion, create `.planning/phases/90-critique-hig-extensions/90-02-SUMMARY.md`
</output>
