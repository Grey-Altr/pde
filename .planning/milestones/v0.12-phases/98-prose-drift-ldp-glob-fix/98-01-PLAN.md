---
phase: 98-prose-drift-ldp-glob-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/critique.md
  - workflows/competitive.md
  - workflows/opportunity.md
  - workflows/system.md
  - workflows/hig.md
  - workflows/handoff.md
autonomous: true
gap_closure: true
requirements:
  - QUAL-01
  - INTG-01
  - FOUND-02

must_haves:
  truths:
    - "critique.md BIZ-3 evaluation source globs LDP-landing-page-v*.md (not LDP-landing-page-spec-v*)"
    - "All 6 workflow anti-pattern/prose sections say 21 fields, not 20 fields"
    - "system.md, critique.md, hig.md, and handoff.md prose field lists include hasDeployStaging"
    - "competitive.md and opportunity.md anti-pattern prose matches the 21-field runtime reality"
    - "235/235 Nyquist tests remain GREEN after all edits"
  artifacts:
    - path: "workflows/critique.md"
      provides: "Fixed LDP glob stem at line 796 and updated prose at line 1224"
      contains: "LDP-landing-page-v*.md"
    - path: "workflows/competitive.md"
      provides: "Updated anti-pattern prose at line 754"
      contains: "fewer than 21 fields"
    - path: "workflows/opportunity.md"
      provides: "Updated anti-pattern prose at line 584"
      contains: "fewer than 21 fields"
    - path: "workflows/system.md"
      provides: "Updated prose at line 2176 with twenty-one and hasDeployStaging in field list"
      contains: "twenty-one-field"
    - path: "workflows/hig.md"
      provides: "Updated prose at lines 853-854 with TWENTY-ONE and hasDeployStaging in field list"
      contains: "ALL TWENTY-ONE"
    - path: "workflows/handoff.md"
      provides: "Updated prose at line 1456 with twenty-one and hasDeployStaging in field list"
      contains: "ALL twenty-one"
  key_links:
    - from: "workflows/critique.md:796"
      to: ".planning/design/launch/LDP-landing-page-v*.md"
      via: "BIZ-3 evaluation source glob"
      pattern: "LDP-landing-page-v\\*\\.md"
---

<objective>
Fix 7 accumulated tech debt items from the v0.12 milestone audit: 1 stale glob stem in critique.md that causes BIZ-3 to silently miss the LDP artifact, and 6 stale "20 fields" prose references across 5 workflows that lag behind the 21-field runtime reality (hasDeployStaging was added in Phase 95 but prose was not updated).

Purpose: All runtime write commands are already correct — only prose descriptions and one evaluation source glob lag. These fixes eliminate the last 7 tech debt items from the v0.12 audit, closing integration gap LDP-glob-stem-mismatch (QUAL-01) and aligning all prose with the 21-field designCoverage schema (INTG-01, FOUND-02).

Output: 6 modified workflow files, 235/235 Nyquist tests still GREEN.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.12-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix LDP glob stem in critique.md BIZ-3 (QUAL-01)</name>
  <files>workflows/critique.md</files>

  <read_first>
    - workflows/critique.md (lines 790-810 to see the full BIZ-3 evaluation source block)
  </read_first>

  <action>
Edit workflows/critique.md line 796. The current text reads:

  - LDP artifact (`.planning/design/launch/LDP-landing-page-spec-v*.md`) — pricing table section

Change it to:

  - LDP artifact (`.planning/design/launch/LDP-landing-page-v*.md`) — pricing table section

This is a one-character-range deletion: remove `-spec` from the glob stem. The producer (wireframe.md) writes `LDP-landing-page-v{N}.md` — the consumer glob must match. This is the only change to this task scope; do NOT touch the prose field list fix in this task (that is Task 2).
  </action>

  <verify>
    <automated>grep -n "LDP-landing-page-spec-v" "/Users/greyaltaer/code/projects/Platform Development Engine/workflows/critique.md" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - grep for "LDP-landing-page-spec-v" in workflows/critique.md returns 0 matches
    - grep for "LDP-landing-page-v\*.md" in workflows/critique.md returns at least 1 match at line ~796
    - No other lines in critique.md are modified (diff shows exactly 1 line changed)
  </acceptance_criteria>

  <done>critique.md line 796 contains LDP-landing-page-v*.md (no "-spec" in the glob stem). BIZ-3 pricing psychology evaluation will now find the correct artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Update 6 stale "20 fields" prose references to "21 fields" (INTG-01, FOUND-02)</name>
  <files>
    workflows/competitive.md,
    workflows/opportunity.md,
    workflows/system.md,
    workflows/critique.md,
    workflows/hig.md,
    workflows/handoff.md
  </files>

  <read_first>
    - workflows/competitive.md (lines 748-756 — the anti-pattern prose block)
    - workflows/opportunity.md (lines 578-585 — the anti-pattern prose block)
    - workflows/system.md (lines 2168-2177 — the coverage-check prose block)
    - workflows/critique.md (lines 1218-1230 — the coverage-check prose block)
    - workflows/hig.md (lines 847-866 — the coverage-check prose block)
    - workflows/handoff.md (lines 1450-1458 — the coverage-check prose block)
  </read_first>

  <action>
Make the following 6 targeted edits. Each edit is described with the exact before and after text. Do NOT touch runtime bash commands (those are already correct with all 21 fields). Only touch prose descriptions.

---

**FIX 1 — competitive.md line 754**

BEFORE (exact):
  NEVER write designCoverage with fewer than 20 fields. The 16-field version is a regression from Phase 84. Always include hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit alongside the original 16 fields.

AFTER:
  NEVER write designCoverage with fewer than 21 fields. The 16-field version is a regression from Phase 84. Always include hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit, hasDeployStaging alongside the original 16 fields.

---

**FIX 2 — opportunity.md line 584**

BEFORE (exact):
  NEVER write designCoverage with fewer than 20 fields. The 16-field version is a regression from Phase 84. Always include hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit alongside the original 16 fields.

AFTER:
  NEVER write designCoverage with fewer than 21 fields. The 16-field version is a regression from Phase 84. Always include hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit, hasDeployStaging alongside the original 16 fields.

---

**FIX 3 — system.md line 2176**

BEFORE (exact):
  Use the actual values read from coverage-check — do not hardcode false for fields that may have been set by other skills. The key invariant: `manifest-set-top-level` performs FLAT key assignment, so `designCoverage` must be set as the FULL twenty-field JSON object every time. All 20 fields: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit.

AFTER:
  Use the actual values read from coverage-check — do not hardcode false for fields that may have been set by other skills. The key invariant: `manifest-set-top-level` performs FLAT key assignment, so `designCoverage` must be set as the FULL twenty-one-field JSON object every time. All 21 fields: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit, hasDeployStaging.

Also on the line above (line 2169), the sentence beginning "Parse the JSON output from coverage-check" contains:

  Extract ALL twenty current flag values: `hasDesignSystem`, ... `hasLaunchKit`. Default any absent field to `false`. Merge `hasDesignSystem: true` while preserving all other nineteen values. Then write the full merged twenty-field object:

Change it to:

  Extract ALL twenty-one current flag values: `hasDesignSystem`, ... `hasLaunchKit`, `hasDeployStaging`. Default any absent field to `false`. Merge `hasDesignSystem: true` while preserving all other twenty values. Then write the full merged twenty-one-field object:

(The field list in that sentence must also have `hasDeployStaging` appended after `hasLaunchKit`.)

---

**FIX 4 — critique.md line 1224**

BEFORE (exact):
  Parse the JSON output from coverage-check. Extract ALL TWENTY current flag values (default absent fields to `false`): hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit. Merge `hasCritique: true` while preserving all other nineteen values. Then write the full merged twenty-field object:

AFTER:
  Parse the JSON output from coverage-check. Extract ALL TWENTY-ONE current flag values (default absent fields to `false`): hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit, hasDeployStaging. Merge `hasCritique: true` while preserving all other twenty values. Then write the full merged twenty-one-field object:

---

**FIX 5 — hig.md lines 853-854**

BEFORE (exact — two lines):
  Parse the JSON result. Extract ALL TWENTY current coverage flag values (default absent fields to `false`):
  - `hasDesignSystem`, `hasWireframes`, `hasFlows`, `hasHardwareSpec`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit`, `hasRecommendations`, `hasStitchWireframes`, `hasPrintCollateral`, `hasProductionBible`, `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`

AFTER (two lines):
  Parse the JSON result. Extract ALL TWENTY-ONE current coverage flag values (default absent fields to `false`):
  - `hasDesignSystem`, `hasWireframes`, `hasFlows`, `hasHardwareSpec`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit`, `hasRecommendations`, `hasStitchWireframes`, `hasPrintCollateral`, `hasProductionBible`, `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`, `hasDeployStaging`

Also update the sentence immediately after the field list (line 856):

  BEFORE: Set `hasHigAudit: true`, pass all 19 other fields through with their current values.
  AFTER:  Set `hasHigAudit: true`, pass all 20 other fields through with their current values.

And update line 858:

  BEFORE: Then write the FULL 20-field JSON, setting `hasHigAudit` to `true` and passing all other flags through unchanged:
  AFTER:  Then write the FULL 21-field JSON, setting `hasHigAudit` to `true` and passing all other flags through unchanged:

---

**FIX 6 — handoff.md line 1456**

BEFORE (exact):
  Parse the JSON output from coverage-check. Extract ALL twenty current flag values: `hasDesignSystem`, `hasWireframes`, `hasFlows`, `hasHardwareSpec`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit`, `hasRecommendations`, `hasStitchWireframes`, `hasPrintCollateral`, `hasProductionBible`, `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`. Default any absent field to `false`.

AFTER:
  Parse the JSON output from coverage-check. Extract ALL twenty-one current flag values: `hasDesignSystem`, `hasWireframes`, `hasFlows`, `hasHardwareSpec`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit`, `hasRecommendations`, `hasStitchWireframes`, `hasPrintCollateral`, `hasProductionBible`, `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`, `hasDeployStaging`. Default any absent field to `false`.

---

IMPORTANT constraints:
- Do NOT modify any bash command blocks (lines with `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs"`). Those are already correct.
- Do NOT modify the runtime JSON object strings (those already include all 21 fields with hasDeployStaging).
- Only modify prose/description sentences as specified above.
- Do NOT modify critique.md line 796 (that was Task 1).
  </action>

  <verify>
    <automated>grep -c "fewer than 20 fields" "/Users/greyaltaer/code/projects/Platform Development Engine/workflows/competitive.md" "/Users/greyaltaer/code/projects/Platform Development Engine/workflows/opportunity.md" && grep -c "twenty-field\|TWENTY current\|twenty current" "/Users/greyaltaer/code/projects/Platform Development Engine/workflows/system.md" "/Users/greyaltaer/code/projects/Platform Development Engine/workflows/critique.md" "/Users/greyaltaer/code/projects/Platform Development Engine/workflows/hig.md" "/Users/greyaltaer/code/projects/Platform Development Engine/workflows/handoff.md"</automated>
  </verify>

  <acceptance_criteria>
    - grep for "fewer than 20 fields" in competitive.md returns 0 matches
    - grep for "fewer than 21 fields" in competitive.md returns 1 match
    - grep for "fewer than 20 fields" in opportunity.md returns 0 matches
    - grep for "fewer than 21 fields" in opportunity.md returns 1 match
    - grep for "twenty-field JSON object" in system.md returns 0 matches (was changed to "twenty-one-field")
    - grep for "twenty-one-field JSON object" in system.md returns at least 1 match
    - grep for "ALL twenty current flag values" in system.md returns 0 matches (was changed to "twenty-one")
    - grep for "ALL TWENTY current flag values" in critique.md returns 0 matches
    - grep for "ALL TWENTY-ONE current flag values" in critique.md returns 1 match
    - grep for "ALL TWENTY current" in hig.md returns 0 matches
    - grep for "ALL TWENTY-ONE current" in hig.md returns 1 match
    - grep for "ALL twenty current flag values" in handoff.md returns 0 matches
    - grep for "ALL twenty-one current flag values" in handoff.md returns 1 match
    - grep for "hasDeployStaging" in competitive.md prose (not bash block) returns at least 1 match
    - grep for "hasDeployStaging" in opportunity.md prose (not bash block) returns at least 1 match
    - grep for "hasDeployStaging" in system.md prose (not bash block) returns at least 2 matches (prose field list + summary sentence)
    - grep for "hasDeployStaging" in critique.md prose (not bash block) returns at least 1 match at line ~1224
    - grep for "hasDeployStaging" in hig.md prose (not bash block) returns at least 1 match at line ~854
    - grep for "hasDeployStaging" in handoff.md prose (not bash block) returns at least 1 match at line ~1456
  </acceptance_criteria>

  <done>All 6 stale "20 fields" prose references updated to "21 fields". hasDeployStaging added to prose field lists in system.md, critique.md, hig.md, and handoff.md. competitive.md and opportunity.md anti-pattern prose updated to say 21. No runtime bash commands modified.</done>
</task>

<task type="auto">
  <name>Task 3: Verify 235/235 Nyquist tests GREEN after edits</name>
  <files></files>

  <read_first>
    - (No files to read — run the test suite directly)
  </read_first>

  <action>
Run the Nyquist regression test suite to confirm all 235 tests remain GREEN after the prose edits.

```bash
cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --experimental-vm-modules node_modules/.bin/jest tests/nyquist/ --no-coverage 2>&1 | tail -20
```

If any tests fail, read the failing test output carefully. Prose-only edits should not affect Nyquist tests (which test structural assertions, not prose content). If a test does fail, it means one of the task edits inadvertently modified a line that a test grep pattern targets — read the test file to identify the pattern, then restore the minimal correct text.

Do not proceed to SUMMARY creation if any test is RED.
  </action>

  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --experimental-vm-modules node_modules/.bin/jest tests/nyquist/ --no-coverage 2>&1 | grep -E "Tests:|passed|failed"</automated>
  </verify>

  <acceptance_criteria>
    - Jest output shows "235 passed" (or equivalent total pass count)
    - Jest output shows "0 failed"
    - No test suite errors or crashes
  </acceptance_criteria>

  <done>235/235 Nyquist tests GREEN. All prose edits confirmed safe — no structural regressions introduced.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, confirm these conditions hold:

1. `grep -n "LDP-landing-page-spec-v" workflows/critique.md` returns empty (0 matches)
2. `grep -n "LDP-landing-page-v\*.md" workflows/critique.md` returns a match at line ~796
3. `grep -c "fewer than 21 fields" workflows/competitive.md workflows/opportunity.md` returns 1:1
4. `grep -c "twenty-one-field" workflows/system.md` returns >= 1
5. `grep -c "TWENTY-ONE" workflows/critique.md workflows/hig.md` returns >= 1 each
6. `grep -c "twenty-one" workflows/handoff.md` returns >= 1
7. Nyquist test suite: 235 passed, 0 failed
</verification>

<success_criteria>
1. critique.md line 796 globs LDP-landing-page-v*.md — BIZ-3 pricing evaluation finds LDP artifact correctly
2. All 6 workflow prose sections say "21 fields" (not "20 fields")
3. system.md, critique.md, hig.md, and handoff.md prose field lists include hasDeployStaging
4. competitive.md and opportunity.md anti-pattern rules reference 21 fields with hasDeployStaging included
5. 235/235 Nyquist tests GREEN — no regressions from prose edits
</success_criteria>

<output>
After completion, create `.planning/phases/98-prose-drift-ldp-glob-fix/98-01-SUMMARY.md` following the standard summary template from @/Users/greyaltaer/.claude/get-shit-done/templates/summary.md.

The summary frontmatter must include:
- requirements: [QUAL-01, INTG-01, FOUND-02]
- files_modified: all 6 workflow files
- gaps_closed: [LDP-glob-stem-mismatch, competitive-prose-20fields, opportunity-prose-20fields, system-prose-20fields, critique-prose-20fields, hig-prose-20fields, handoff-prose-20fields]
</output>
