---
phase: 85-brief-extensions-detection
plan: 02
type: execute
wave: 2
depends_on: [85-01]
files_modified:
  - workflows/brief.md
  - .planning/phases/85-brief-extensions-detection/tests/test-brief-artifacts.cjs
autonomous: true
requirements: [BRIEF-03, BRIEF-04, BRIEF-06]

must_haves:
  truths:
    - "When businessMode is true, brief.md generates a BTH artifact in strategy/ with problem/solution/market/unfair-advantage sections"
    - "When businessMode is true, brief.md generates an LCV artifact in strategy/ with all 9 lean canvas boxes and confidence statuses"
    - "BTH and LCV use the same version number N as the BRF from the same run"
    - "The LCV artifact has dependsOn: BTH in frontmatter and manifest"
    - "The designCoverage write uses all 20 fields with hasBusinessThesis set to true"
    - "Financial content in BTH and LCV uses [YOUR_X] placeholders only — no dollar amounts"
    - "When businessMode is false, no BTH or LCV artifacts are generated"
  artifacts:
    - path: "workflows/brief.md"
      provides: "Steps 5b and 5c for BTH/LCV generation, 20-field coverage write in Step 7"
      contains: "Step 5b"
    - path: ".planning/phases/85-brief-extensions-detection/tests/test-brief-artifacts.cjs"
      provides: "Structural tests for BRIEF-03, BRIEF-04, BRIEF-06"
      min_lines: 60
  key_links:
    - from: "workflows/brief.md"
      to: "references/launch-frameworks.md"
      via: "LCV 9-box schema source"
      pattern: "Lean Canvas"
    - from: "workflows/brief.md"
      to: "pde-tools.cjs design manifest-update BTH"
      via: "artifact registration"
      pattern: "manifest-update BTH"
    - from: "workflows/brief.md"
      to: "pde-tools.cjs design manifest-set-top-level designCoverage"
      via: "20-field coverage write"
      pattern: "hasBusinessThesis"
---

<objective>
Add BTH (business thesis) and LCV (lean canvas) artifact generation to brief.md, plus the 20-field designCoverage write.

Purpose: When business mode is active, the brief skill produces two additional strategy artifacts that anchor all downstream business workflows (competitive, flows, brand, wireframe, critique, handoff).
Output: Modified workflows/brief.md with Steps 5b (BTH) and 5c (LCV) conditionally inserted after BRF generation, plus 20-field designCoverage write in Step 7. Test scaffold for artifact structure.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-brief-extensions-detection/85-RESEARCH.md
@.planning/phases/85-brief-extensions-detection/85-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: brief.md now has business detection in Step 4, Domain Strategy in Step 5, manifest writes in Step 7 -->
<!-- Plan 02 inserts Steps 5b/5c BETWEEN Step 5 BRF write and Step 6 DESIGN-STATE update -->
<!-- Plan 02 extends Step 7 with 20-field designCoverage write AFTER businessMode/businessTrack writes -->

From references/launch-frameworks.md (lean canvas schema):
- 9-box table: Problem, Solution, UVP, Unfair Advantage, Customer Segments, Key Metrics, Channels, Cost Structure, Revenue Streams
- Confidence: validated (user evidence) | assumed (BRF-derived) | unknown (synthesized/inferred)
- Financial boxes 8+9: always [YOUR_X] placeholders, never validated without user evidence

From templates/design-manifest.json (20-field designCoverage order):
1. hasDesignSystem, 2. hasWireframes, 3. hasFlows, 4. hasHardwareSpec, 5. hasCritique,
6. hasIterate, 7. hasHandoff, 8. hasIdeation, 9. hasCompetitive, 10. hasOpportunity,
11. hasMockup, 12. hasHigAudit, 13. hasRecommendations, 14. hasStitchWireframes,
15. hasPrintCollateral, 16. hasProductionBible, 17. hasBusinessThesis, 18. hasMarketLandscape,
19. hasServiceBlueprint, 20. hasLaunchKit

Artifact registration pattern (7 manifest-update commands per artifact):
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} code {CODE}
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} name "{Name}"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} type {type}
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} domain {domain}
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} path "{file_path}"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} status draft
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update {CODE} version {N}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffold for BTH/LCV artifact requirements</name>
  <files>.planning/phases/85-brief-extensions-detection/tests/test-brief-artifacts.cjs</files>
  <read_first>
    - .planning/phases/85-brief-extensions-detection/tests/test-brief-detection.cjs
    - .planning/phases/84-foundation/tests/test-foundation.cjs
    - workflows/brief.md
    - references/launch-frameworks.md
    - templates/design-manifest.json
  </read_first>
  <action>
Create a node:test structural test file at `.planning/phases/85-brief-extensions-detection/tests/test-brief-artifacts.cjs` that verifies brief.md contains the BTH/LCV generation steps and 20-field coverage write.

Test blocks (all should FAIL initially — RED state):

**BRIEF-03 tests (BTH artifact generation):**
- Test: brief.md contains "Step 5b" or "BTH" generation step
- Test: brief.md contains BTH artifact file path pattern `BTH-thesis-v`
- Test: brief.md contains BTH content sections: "## Problem", "## Solution", "## Market", "## Unfair Advantage" in the BTH template
- Test: brief.md contains BTH manifest registration commands (`manifest-update BTH code BTH`)
- Test: brief.md contains BTH DESIGN-STATE update instruction (check for "BTH" and "DESIGN-STATE" in proximity)
- Test: brief.md contains businessMode gate for BTH (check for conditional logic gating BTH on businessMode)
- Test: brief.md contains BTH frontmatter template with `businessTrack:` field

**BRIEF-04 tests (LCV artifact generation):**
- Test: brief.md contains "Step 5c" or "LCV" generation step
- Test: brief.md contains LCV artifact file path pattern `LCV-lean-canvas-v`
- Test: brief.md contains all 9 lean canvas box names: "Problem", "Solution", "Unique Value Proposition", "Unfair Advantage", "Customer Segments", "Key Metrics", "Channels", "Cost Structure", "Revenue Streams"
- Test: brief.md contains confidence status labels: "validated", "assumed", "unknown"
- Test: brief.md contains LCV manifest registration commands (`manifest-update LCV code LCV`)
- Test: brief.md contains `dependsOn` field for LCV (check for "dependsOn" and "BTH" in the LCV context)
- Test: brief.md contains LCV financial placeholders: `[YOUR_CAC_CEILING]`, `[YOUR_ARR_TARGET]`, `[YOUR_LTV_ESTIMATE]`

**BRIEF-06 tests (20-field designCoverage write):**
- Test: brief.md contains `manifest-set-top-level designCoverage` command
- Test: brief.md contains all 20 designCoverage field names in the coverage write section (check for `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit` alongside the 16 existing fields)
- Test: brief.md contains `hasBusinessThesis` set to `true` in the coverage write
- Test: brief.md contains `coverage-check` command to read existing coverage before writing

Use `node:test` built-in with `describe`/`it` blocks. Read brief.md content once at top. All tests must FAIL against the brief.md produced by Plan 01 (which does not yet have Steps 5b/5c or coverage write).

```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert');
const fs = require('fs');
const path = require('path');

const ROOT = path.resolve(__dirname, '..', '..', '..', '..');
const briefContent = fs.readFileSync(path.join(ROOT, 'workflows', 'brief.md'), 'utf-8');

describe('BRIEF-03: BTH artifact generation', () => {
  // tests...
});
// etc.
```
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test .planning/phases/85-brief-extensions-detection/tests/test-brief-artifacts.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/85-brief-extensions-detection/tests/test-brief-artifacts.cjs exists and is valid JavaScript
    - File contains `describe('BRIEF-03` block
    - File contains `describe('BRIEF-04` block
    - File contains `describe('BRIEF-06` block
    - Running `node --test` exits non-zero (tests fail against Plan-01-modified brief.md)
    - File uses `require('node:test')` — no external dependencies
  </acceptance_criteria>
  <done>Test scaffold exists with structural assertions for BRIEF-03, BRIEF-04, BRIEF-06. All tests fail (RED state).</done>
</task>

<task type="auto">
  <name>Task 2: Add BTH generation (Step 5b), LCV generation (Step 5c), and 20-field coverage write to brief.md</name>
  <files>workflows/brief.md</files>
  <read_first>
    - workflows/brief.md
    - references/launch-frameworks.md
    - references/business-track.md
    - references/business-financial-disclaimer.md
    - .planning/phases/85-brief-extensions-detection/85-RESEARCH.md
    - templates/design-manifest.json
  </read_first>
  <action>
Modify `workflows/brief.md` to add Steps 5b and 5c (conditionally gated on businessMode == true), and extend Step 7 with the 20-field designCoverage write. Read the full file first to identify exact insertion points.

**CHANGE 1: Insert Step 5b — BTH Artifact Generation (after BRF Write, before Step 5 display)**

After the BRF artifact is written via Write tool (and after the Domain Strategy section from Plan 01), insert:

```markdown
#### Step 5b: Generate Business Thesis (BTH) — BRIEF-03

IF businessMode == false: SKIP this step entirely. Do not generate any BTH content.

IF businessMode == true:

Generate the Business Thesis artifact. The BTH is DERIVED from the BRF content just written — do not introduce information not present in the BRF.

**BTH content sources (read from the BRF-brief-v{N}.md just written):**

## Problem section:
  Source: BRF "## Problem Statement" — condense to 1-2 paragraphs focused on the specific pain, who has it, and why now.
  Do NOT introduce new problems not in the BRF.
  Track depth: solo_founder: 2-3 sentences. startup_team: 1-2 paragraphs. product_leader: 2-3 paragraphs with executive framing.

## Solution section:
  Source: BRF "## Product Type" (rationale) + "## Scope Boundaries" (in-scope list) — synthesize into 1-2 paragraphs.
  Track depth: solo_founder: 2-3 sentences. startup_team: 1-2 paragraphs. product_leader: 2-3 paragraphs.

## Market section:
  Source: BRF "## Target Users" + "## Competitive Context" — describe market context.
  ALWAYS use [YOUR_TAM_SIZE] [VERIFY FINANCIAL ASSUMPTIONS] for any market size reference.
  NEVER write dollar amounts. NEVER write specific percentages as facts.
  Track vocabulary: solo_founder: "customers". startup_team: "ICP". product_leader: "key accounts".

## Unfair Advantage section:
  Source: BRF "## Key Assumptions" + "## Competitive Context" (differentiation) — synthesize what cannot be easily copied.
  Be specific; avoid generic phrases like "first-mover advantage" unless the BRF specifically supports that claim.

Write the BTH artifact file using Write tool:

File path: `.planning/design/strategy/BTH-thesis-v{N}.md`
(where N matches the BRF version number from this run)

```markdown
---
Generated: "{ISO 8601 timestamp}"
Skill: /pde:brief (BTH)
Version: v{N}
businessTrack: {confirmed_track}
---

# Business Thesis

## Problem
[derived content per instructions above]

## Solution
[derived content per instructions above]

## Market
[derived content per instructions above — uses [YOUR_TAM_SIZE] [VERIFY FINANCIAL ASSUMPTIONS] for sizing]

## Unfair Advantage
[derived content per instructions above]

---
*Generated by PDE-OS /pde:brief | {ISO 8601 date}*
```

**Post-write verification (BRIEF-07):**
```bash
if grep -qE '\$[0-9]' ".planning/design/strategy/BTH-thesis-v${N}.md" 2>/dev/null; then
  echo "ERROR: Dollar amount detected in BTH artifact. Financial content must use [YOUR_X] placeholders."
  echo "Offending lines:"
  grep -nE '\$[0-9]' ".planning/design/strategy/BTH-thesis-v${N}.md"
fi
```

**Register BTH in manifest:**
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BTH code BTH
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BTH name "Business Thesis"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BTH type thesis
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BTH domain strategy
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BTH path ".planning/design/strategy/BTH-thesis-v${N}.md"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BTH status draft
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BTH version ${N}
```

**Update strategy/DESIGN-STATE.md** (use Edit tool to append):
- Artifact Index table: `| BTH | Business Thesis | /pde:brief | draft | v{N} | none | BRF | {date} |`
- Cross-Domain Dependency Map: `| BTH | strategy | BRF | current |`

Display: `  -> BTH-thesis-v{N}.md created.`
```

**CHANGE 2: Insert Step 5c — LCV Artifact Generation (after Step 5b, before Step 5 display)**

```markdown
#### Step 5c: Generate Lean Canvas (LCV) — BRIEF-04

IF businessMode == false: SKIP this step entirely.

IF BTH generation failed in Step 5b: SKIP LCV. Log: `  Warning: LCV skipped — BTH generation failed.`

IF businessMode == true AND BTH was successfully written:

Generate the Lean Canvas artifact. Content is derived from BRF and BTH artifacts just written.

Use the 9-box schema from `@references/launch-frameworks.md`. Apply track vocabulary from `@references/business-track.md`.

Write the LCV artifact using Write tool:

File path: `.planning/design/strategy/LCV-lean-canvas-v{N}.md`
(where N matches BRF and BTH version numbers)

```markdown
---
Generated: "{ISO 8601 timestamp}"
Skill: /pde:brief (LCV)
Version: v{N}
businessTrack: {confirmed_track}
dependsOn: BTH
---

# Lean Canvas

*Source: Ash Maurya, Running Lean*
*Status key: validated | assumed | unknown*

| # | Box | Content | Status | Evidence |
|---|-----|---------|--------|----------|
| 1 | Problem | {2-3 specific problems from BRF Problem Statement} | assumed | BRF-brief-v{N}.md |
| 2 | Solution | {2-3 solution features from BRF scope} | assumed | BRF-brief-v{N}.md |
| 3 | Unique Value Proposition | {one compelling sentence from BTH Solution + Unfair Advantage} | unknown | none yet |
| 4 | Unfair Advantage | {from BTH Unfair Advantage section} | unknown | none yet |
| 5 | Customer Segments | {from BRF Target Users — primary and secondary} | assumed | BRF-brief-v{N}.md |
| 6 | Key Metrics | {from BRF Success Criteria — 2-3 measurable KPIs} | unknown | none yet |
| 7 | Channels | {synthesized from BRF Competitive Context and product type} | unknown | none yet |
| 8 | Cost Structure | [YOUR_CAC_CEILING] [VERIFY FINANCIAL ASSUMPTIONS], [YOUR_HOSTING_COST] [VERIFY FINANCIAL ASSUMPTIONS] | unknown | none yet |
| 9 | Revenue Streams | [YOUR_ARR_TARGET] [VERIFY FINANCIAL ASSUMPTIONS], [YOUR_LTV_ESTIMATE] [VERIFY FINANCIAL ASSUMPTIONS] | unknown | none yet |

---
*Generated by PDE-OS /pde:brief | {ISO 8601 date}*
```

Track vocabulary substitutions for LCV non-financial cells:
- Box 5 (Customer Segments): solo_founder: "customers". startup_team: "ICP". product_leader: "key accounts".
- Box 6 (Key Metrics): solo_founder: "what you measure". startup_team: "key metrics / KPIs". product_leader: "OKRs / P&L impact".
- Box 9 (Revenue label only): solo_founder: "what you charge". startup_team: "ARR target / pricing tiers". product_leader: "monetization strategy / packaging".

Confidence status rules:
- `validated` — ONLY when user provides explicit evidence (never auto-assigned by this workflow)
- `assumed` — when derived from BRF content the user authored (boxes 1, 2, 5)
- `unknown` — default for all synthesized or inferred values (boxes 3, 4, 6, 7, 8, 9)
- Financial boxes (8, 9) — NEVER mark as `validated` without explicit user evidence

**Post-write verification (BRIEF-07):**
```bash
if grep -qE '\$[0-9]' ".planning/design/strategy/LCV-lean-canvas-v${N}.md" 2>/dev/null; then
  echo "ERROR: Dollar amount detected in LCV artifact. Financial content must use [YOUR_X] placeholders."
  echo "Offending lines:"
  grep -nE '\$[0-9]' ".planning/design/strategy/LCV-lean-canvas-v${N}.md"
fi
```

**Register LCV in manifest:**
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LCV code LCV
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LCV name "Lean Canvas"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LCV type canvas
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LCV domain strategy
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LCV path ".planning/design/strategy/LCV-lean-canvas-v${N}.md"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LCV status draft
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LCV version ${N}
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update LCV dependsOn '["BTH"]'
```

**Update strategy/DESIGN-STATE.md** (use Edit tool to append):
- Artifact Index table: `| LCV | Lean Canvas | /pde:brief | draft | v{N} | none | BTH | {date} |`
- Cross-Domain Dependency Map: `| LCV | strategy | BTH | current |`

Display: `  -> LCV-lean-canvas-v{N}.md created.`
```

**CHANGE 3: Extend Step 7 with 20-field designCoverage write (BRIEF-06)**

After the businessMode/businessTrack manifest writes (added by Plan 01), add the 20-field designCoverage write:

```markdown
#### 20-Field designCoverage Write (BRIEF-06)

Read current designCoverage state:

```bash
COV=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design coverage-check)
if [[ "$COV" == @file:* ]]; then COV=$(cat "${COV#@file:}"); fi
```

Parse the result: if COV starts with `{`, parse it as JSON to extract current flag values. Default any absent field to `false`.

Construct the complete 20-field JSON string. Replace each field with its ACTUAL current value (true or false), except set `hasBusinessThesis` to `true` when businessMode is true:

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage \
  '{"hasDesignSystem":ACTUAL,"hasWireframes":ACTUAL,"hasFlows":ACTUAL,"hasHardwareSpec":ACTUAL,"hasCritique":ACTUAL,"hasIterate":ACTUAL,"hasHandoff":ACTUAL,"hasIdeation":ACTUAL,"hasCompetitive":ACTUAL,"hasOpportunity":ACTUAL,"hasMockup":ACTUAL,"hasHigAudit":ACTUAL,"hasRecommendations":ACTUAL,"hasStitchWireframes":ACTUAL,"hasPrintCollateral":ACTUAL,"hasProductionBible":ACTUAL,"hasBusinessThesis":true,"hasMarketLandscape":ACTUAL,"hasServiceBlueprint":ACTUAL,"hasLaunchKit":ACTUAL}'
```

Where each ACTUAL is replaced with the literal `true` or `false` value read from the parsed coverage. The `hasBusinessThesis` field is hardcoded to `true` because the BTH artifact was just created.

**CRITICAL:** This write MUST include all 20 fields. Writing fewer fields erases the omitted ones. Do NOT copy the 16-field pattern from other workflows (opportunity.md, hig.md, etc.) — those are outdated and will be fixed in Phase 93.

IF businessMode == false: Still run the coverage-check and write, but pass through all existing values unchanged (do not set hasBusinessThesis to true). This ensures the 20-field format is maintained even for non-business projects.
```

**CHANGE 4: Update Step 6 to include BTH/LCV in DESIGN-STATE updates**

In Step 6, after the BRF row is added to strategy/DESIGN-STATE.md, add conditional BTH/LCV rows:

```markdown
IF businessMode == true:
  Also add to Root DESIGN-STATE.md:
  - Cross-Domain Dependency Map: `| BTH | strategy | BRF | current |` and `| LCV | strategy | BTH | current |`
  - Decision Log: `| BTH | business thesis generated from BRF v{N} | {date} |` and `| LCV | lean canvas generated from BTH v{N} | {date} |`
  - Iteration History: `| BTH-thesis-v{N} | v{N} | Created by /pde:brief | {date} |` and `| LCV-lean-canvas-v{N} | v{N} | Created by /pde:brief | {date} |`
```

**CHANGE 5: Update Summary table for BTH/LCV**

Extend the summary display to include BTH and LCV when businessMode is true:

```markdown
IF businessMode == true:
| Files created | ... BTH-thesis-v{N}.md ..., LCV-lean-canvas-v{N}.md ... |
```
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test .planning/phases/85-brief-extensions-detection/tests/test-brief-artifacts.cjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - workflows/brief.md contains "Step 5b" or "step 5b" for BTH generation
    - workflows/brief.md contains "Step 5c" or "step 5c" for LCV generation
    - workflows/brief.md contains `BTH-thesis-v` file path pattern
    - workflows/brief.md contains `LCV-lean-canvas-v` file path pattern
    - workflows/brief.md contains "## Problem" AND "## Solution" AND "## Market" AND "## Unfair Advantage" in BTH template
    - workflows/brief.md contains all 9 lean canvas box names (Problem, Solution, Unique Value Proposition, Unfair Advantage, Customer Segments, Key Metrics, Channels, Cost Structure, Revenue Streams)
    - workflows/brief.md contains `manifest-update BTH code BTH`
    - workflows/brief.md contains `manifest-update LCV code LCV`
    - workflows/brief.md contains `manifest-update LCV dependsOn`
    - workflows/brief.md contains `manifest-set-top-level designCoverage`
    - workflows/brief.md contains all 20 designCoverage field names including `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`
    - workflows/brief.md contains `coverage-check` command
    - workflows/brief.md contains `[YOUR_CAC_CEILING]` and `[YOUR_ARR_TARGET]` and `[YOUR_LTV_ESTIMATE]`
    - workflows/brief.md contains confidence status labels "validated", "assumed", "unknown"
    - All BRIEF-03, BRIEF-04, BRIEF-06 tests pass (GREEN state)
  </acceptance_criteria>
  <done>brief.md contains Step 5b (BTH generation with problem/solution/market/unfair-advantage, registration, DESIGN-STATE update), Step 5c (LCV generation with 9-box schema, confidence statuses, financial placeholders, registration, dependency chain), and 20-field designCoverage write in Step 7. All artifact tests pass.</done>
</task>

</tasks>

<verification>
Run full test suites for both plans:
```bash
node --test .planning/phases/85-brief-extensions-detection/tests/test-brief-detection.cjs && \
node --test .planning/phases/85-brief-extensions-detection/tests/test-brief-artifacts.cjs
```
All BRIEF-01 through BRIEF-07 structural tests pass.

Verify 20-field coverage write:
```bash
grep -c "hasLaunchKit" workflows/brief.md
```
Returns at least 1.

Verify no dollar amounts in workflow templates:
```bash
grep -c '\[YOUR_' workflows/brief.md
```
Returns at least 5 (TAM_SIZE, CAC_CEILING, ARR_TARGET, LTV_ESTIMATE, HOSTING_COST, DOMAIN_NAME).

Verify artifact registration commands:
```bash
grep -c "manifest-update BTH" workflows/brief.md && grep -c "manifest-update LCV" workflows/brief.md
```
BTH: 7 commands. LCV: 8 commands (7 standard + 1 dependsOn).
</verification>

<success_criteria>
- brief.md has Step 5b generating BTH artifact conditionally when businessMode is true
- brief.md has Step 5c generating LCV artifact conditionally when businessMode is true and BTH succeeded
- BTH artifact structure includes problem/solution/market/unfair-advantage derived from BRF
- LCV artifact structure includes all 9 lean canvas boxes with correct confidence statuses
- Financial content uses [YOUR_X] placeholders only — post-write dollar verification included
- Both artifacts are registered in manifest with 7 manifest-update commands each
- LCV has dependsOn BTH in both frontmatter and manifest
- 20-field designCoverage write includes hasBusinessThesis: true
- All structural tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/85-brief-extensions-detection/85-02-SUMMARY.md`
</output>
