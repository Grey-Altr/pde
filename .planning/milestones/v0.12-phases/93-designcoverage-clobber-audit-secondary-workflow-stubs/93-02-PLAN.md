---
phase: 93-designcoverage-clobber-audit-secondary-workflow-stubs
plan: 02
type: execute
wave: 2
depends_on:
  - 93-01
files_modified:
  - workflows/mockup.md
  - workflows/ideate.md
autonomous: true
requirements:
  - INTG-01
  - INTG-08

must_haves:
  truths:
    - "mockup.md writes all 20 designCoverage fields — no business flags are clobbered when /pde:mockup runs after /pde:handoff"
    - "ideate.md writes all 20 designCoverage fields — no business flags are clobbered when /pde:ideate runs after /pde:handoff"
    - "mockup.md contains a Business product type stub comment matching the experience stub pattern"
    - "All 11 Nyquist tests pass GREEN"
  artifacts:
    - path: "workflows/mockup.md"
      provides: "20-field designCoverage write + business stub"
      contains: "hasLaunchKit"
    - path: "workflows/ideate.md"
      provides: "20-field designCoverage write (no business stub required per success criteria)"
      contains: "hasLaunchKit"
  key_links:
    - from: "tests/test-clobber-audit.cjs"
      to: "workflows/mockup.md"
      via: "readWorkflow('mockup.md') then TWENTY_FIELDS.filter"
      pattern: "TWENTY_FIELDS.filter"
    - from: "tests/test-clobber-audit.cjs"
      to: "workflows/ideate.md"
      via: "readWorkflow('ideate.md') then TWENTY_FIELDS.filter"
      pattern: "TWENTY_FIELDS.filter"
---

<objective>
Fix the 16-field designCoverage clobber regression in mockup.md and ideate.md, and add a business product type stub to mockup.md.

Purpose: Complete the 20-field audit across all 4 regression workflows. After this plan, every designCoverage-writing workflow in PDE writes all 20 fields, and all 3 secondary workflows (recommend, iterate, mockup) have business stubs.

Output: 2 patched workflows (mockup.md, ideate.md), all 11 Nyquist tests GREEN
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/93-RESEARCH.md
@.planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/93-01-SUMMARY.md

<interfaces>
<!-- Test file created in Plan 01 covers mockup.md and ideate.md assertions. -->
<!-- mockup.md uses generic {current} placeholders (same as iterate.md, ideate.md). -->
<!-- mockup.md has a real write call at line ~1432 AND a prose anti-pattern example at line ~1481. Only line ~1432 is the actual write. -->
<!-- ideate.md uses generic {current} placeholders. No business stub required per success criteria. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix mockup.md — 20-field write + business stub</name>
  <files>workflows/mockup.md</files>
  <read_first>
    - workflows/mockup.md (MUST read lines 1420-1440 for current 16-field write state, line 154 for experience stub location, line 1481 for anti-pattern example to NOT modify)
    - .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/93-RESEARCH.md (section "3. Corrected 20-Field Write for mockup.md" and "5. Business Product Type Stub Format" and Pitfall 2 about the anti-pattern line)
  </read_first>
  <behavior>
    - Test: mockup.md contains all 20 designCoverage field names
    - Test: mockup.md does not contain "ALWAYS write all 16 fields"
    - Test: mockup.md contains `<!-- Business product type -- Phase 93 stub`
  </behavior>
  <action>
Four edits to workflows/mockup.md. IMPORTANT: mockup.md has TWO matches for `manifest-set-top-level designCoverage` — only line ~1432 is the real write call (inside a bash code block). Line ~1481 is a NEVER anti-pattern example in prose — do NOT modify line ~1481.

**Edit 1 — Field extraction prose (line ~1426):**
Change `"Extract all 16 flags. Default any absent flag to false:"` to `"Extract all 20 flags. Default any absent flag to false:"`. In the dash-separated list that follows, add after hasProductionBible: `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`.

**Edit 2 — Write call (line ~1432):**
Replace the 16-field JSON string with the 20-field version. Uses generic `{current}` placeholders. The corrected write ends with:
`...,"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{current},"hasLaunchKit":{current}}'`

Keep `"hasMockup":true` as the owned flag. All other 19 fields use `{current}`.

**Edit 3 — IMPORTANT note (line ~1435):**
Change `"ALWAYS write all 16 fields. Canonical field order: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible."` to `"ALWAYS write all 20 fields. Canonical field order: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit."`

**Edit 4 — Business stub (after line 154, immediately after the experience stub):**
Insert this exact HTML comment on the line after the experience stub:
```
<!-- Business product type — Phase 93 stub: business product mockup extensions (landing page mockups, pitch deck slide renders, pricing table mockups) added in a future phase. Current behavior: proceed with software mockup path as temporary fallback for business product type. NEVER produce launch artifact mockups from this stub. -->
```

After edits, run: `node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs 2>&1 | grep -E "mockup|pass|fail"`
  </action>
  <verify>
    <automated>node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs 2>&1 | grep -E "mockup"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/mockup.md contains string `hasBusinessThesis`
    - workflows/mockup.md contains string `hasMarketLandscape`
    - workflows/mockup.md contains string `hasServiceBlueprint`
    - workflows/mockup.md contains string `hasLaunchKit`
    - workflows/mockup.md does NOT contain string `ALWAYS write all 16 fields`
    - workflows/mockup.md contains string `ALWAYS write all 20 fields`
    - workflows/mockup.md contains string `<!-- Business product type`
    - Line ~1481 anti-pattern example is UNTOUCHED (still says `manifest-set-top-level designCoverage.hasMockup true`)
    - All 3 mockup.md Nyquist tests pass (GREEN)
  </acceptance_criteria>
  <done>mockup.md writes all 20 designCoverage fields. IMPORTANT note says 20-field. Business stub comment present after experience stub. Anti-pattern example at line ~1481 unchanged. All mockup.md tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix ideate.md — 20-field write (no business stub required)</name>
  <files>workflows/ideate.md</files>
  <read_first>
    - workflows/ideate.md (MUST read lines 685-700 for current 16-field write state)
    - .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/93-RESEARCH.md (section "4. Corrected 20-Field Write for ideate.md")
  </read_first>
  <behavior>
    - Test: ideate.md contains all 20 designCoverage field names
    - Test: ideate.md does not contain "ALWAYS write all 16 fields"
  </behavior>
  <action>
Three edits to workflows/ideate.md. NOTE: ideate.md does NOT need a business stub — it is not in the Success Criterion 3 list (only recommend, iterate, mockup need stubs).

**Edit 1 — Field extraction prose (line ~687):**
Change `"Extract all 16 flags (default absent flags to false):"` to `"Extract all 20 flags (default absent flags to false):"`. Update the canonical field list to include the 4 new fields after hasProductionBible: `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`.

**Edit 2 — Write call (line ~694):**
Replace the 16-field JSON string with the 20-field version. Uses generic `{current}` placeholders. The corrected write ends with:
`...,"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{current},"hasLaunchKit":{current}}'`

Keep `"hasIdeation":true` as the owned flag. All other 19 fields use `{current}`.

**Edit 3 — IMPORTANT note (line ~697):**
Change `"ALWAYS write all 16 fields. The canonical field order is: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible."` to `"ALWAYS write all 20 fields. The canonical field order is: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit."`

After edits, run the full test suite:
```bash
node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs
```
ALL 11 tests must pass GREEN.
  </action>
  <verify>
    <automated>node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs</automated>
  </verify>
  <acceptance_criteria>
    - workflows/ideate.md contains string `hasBusinessThesis`
    - workflows/ideate.md contains string `hasMarketLandscape`
    - workflows/ideate.md contains string `hasServiceBlueprint`
    - workflows/ideate.md contains string `hasLaunchKit`
    - workflows/ideate.md does NOT contain string `ALWAYS write all 16 fields`
    - workflows/ideate.md contains string `ALWAYS write all 20 fields`
    - All 11 Nyquist tests pass GREEN (full suite: recommend x3 + iterate x3 + mockup x3 + ideate x2 = 11)
  </acceptance_criteria>
  <done>ideate.md writes all 20 designCoverage fields. IMPORTANT note says 20-field. All 11 Phase 93 Nyquist tests GREEN. Phase 93 complete.</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm all 11 assertions pass:
```bash
node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs
```
Expected: 11/11 tests pass. Zero failures.

Additional structural verification:
```bash
# Confirm no workflow still says "16 fields" in its IMPORTANT note
grep -rn "write all 16 fields" workflows/ | wc -l
# Expected: 0

# Confirm all 4 regression workflows now contain hasLaunchKit
grep -l "hasLaunchKit" workflows/recommend.md workflows/iterate.md workflows/mockup.md workflows/ideate.md | wc -l
# Expected: 4

# Confirm 3 business stubs exist
grep -l "Business product type" workflows/recommend.md workflows/iterate.md workflows/mockup.md | wc -l
# Expected: 3
```
</verification>

<success_criteria>
- mockup.md and ideate.md each write all 20 designCoverage fields
- mockup.md contains a `<!-- Business product type` stub comment
- IMPORTANT notes updated from "16" to "20" in both files
- All 11 Nyquist tests GREEN
- No workflow in the codebase still references "write all 16 fields" in IMPORTANT notes
- Phase 93 requirements INTG-01 and INTG-08 fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/93-02-SUMMARY.md`
</output>
