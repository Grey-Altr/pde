---
phase: 94-nyquist-regression-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs
autonomous: true
requirements:
  - INTG-02
  - INTG-03
  - INTG-04
  - INTG-05
  - INTG-06
  - INTG-07

must_haves:
  truths:
    - "Non-business product types do not trigger business artifact generation (BTH/LDP/STR/DPD/LKT are gated on businessMode == true)"
    - "business:software composition produces both WFR software artifacts and LDP/STR/DPD business artifacts via independent IF blocks"
    - "business:hardware composition produces both hardware coverage flags and business artifacts via independent IF blocks"
    - "business:experience composition produces both experience artifacts (BIB/FLY/FLP/TML) and business artifacts (BTH/LDP/LKT) via independent IF blocks"
    - "Deploy workflow halts at each of 4 approval gates when user declines — no fallthrough between gates"
    - "All 9 v0.12 coverage-writing workflows contain all 20 designCoverage fields"
  artifacts:
    - path: ".planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs"
      provides: "All INTG-02 through INTG-07 structural assertions"
      min_lines: 150
  key_links:
    - from: "test-regression-matrix.cjs"
      to: "workflows/brief.md, wireframe.md, flows.md, system.md, handoff.md, deploy.md"
      via: "fs.readFileSync structural pattern checks"
      pattern: "readFileSync.*workflows"
---

<objective>
Create the Phase 94 Nyquist regression test file covering all 6 INTG requirements (INTG-02 through INTG-07) for v0.12 Business Product Type composition validation and non-regression verification.

Purpose: Terminal validation phase for the v0.12 milestone — proves that business mode additions do not regress non-business product types, that all composition cases (business:software, business:hardware, business:experience) produce correct artifact sets via independent IF blocks, that deploy approval gates halt on decline, and that all 20 designCoverage fields are present in every coverage-writing workflow.

Output: One test file (`test-regression-matrix.cjs`) with ~30 structural assertions, all GREEN.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-nyquist-regression-tests/94-RESEARCH.md

<interfaces>
<!-- Executor needs these exact patterns from existing tests to maintain consistency -->

From .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs:
```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert');
const fs = require('node:fs');
const path = require('node:path');
const ROOT = path.resolve(__dirname, '..', '..', '..', '..');

const TWENTY_FIELDS = [
  'hasDesignSystem', 'hasWireframes', 'hasFlows', 'hasHardwareSpec',
  'hasCritique', 'hasIterate', 'hasHandoff', 'hasIdeation',
  'hasCompetitive', 'hasOpportunity', 'hasMockup', 'hasHigAudit',
  'hasRecommendations', 'hasStitchWireframes', 'hasPrintCollateral',
  'hasProductionBible', 'hasBusinessThesis', 'hasMarketLandscape',
  'hasServiceBlueprint', 'hasLaunchKit'
];

function readWorkflow(name) {
  return fs.readFileSync(path.join(ROOT, 'workflows', name), 'utf-8');
}
```

Confirmed workflow string markers (from 94-RESEARCH.md direct file reads):

brief.md: `businessMode == false`, `businessMode == true`, `manifest-set-top-level businessMode`, `BTH-business-thesis`, `hasBusinessThesis`
wireframe.md: `WFR`, `LDP-landing-page`, `manifest-get-top-level businessMode`
flows.md: `SBP-service-blueprint`, `GTM-channel-flow`, `PRODUCT_TYPE == "experience"`, `$BM == "true"`, `experience flow artifacts AND business flow artifacts`
system.md: `Step 5b`, `Step 5c` or `Steps 5c`, `independent conditional blocks`, `SYS-experience-tokens.json`, `SYS-brand-tokens.json`
handoff.md: `TypeScript`, `LKT`, `BIB`, `independent IF block`
deploy.md: `Gate 1/4` through `Gate 4/4`, `Halt -- stop deployment`, `Deploy halted at Gate 1/4. No files written.`, `Deploy halted at Gate 2/4.`, `Deploy halted at Gate 3/4.`, `Deploy halted at Gate 4/4.`, `NEVER skip an approval gate`, `Stage 14`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test-regression-matrix.cjs with all 6 INTG requirement assertions</name>
  <files>.planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs</files>
  <read_first>
    - .planning/phases/94-nyquist-regression-tests/94-RESEARCH.md (complete test patterns, confirmed markers, code examples)
    - .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs (canonical test pattern reference)
    - workflows/brief.md (verify markers: businessMode == false, businessMode == true, BTH-business-thesis, manifest-set-top-level businessMode)
    - workflows/deploy.md (verify markers: Gate 1/4 through Gate 4/4, Halt -- stop deployment, Deploy halted at Gate N/4)
    - workflows/wireframe.md (verify markers: WFR, LDP-landing-page)
    - workflows/flows.md (verify markers: SBP-service-blueprint, PRODUCT_TYPE == "experience", $BM == "true")
    - workflows/system.md (verify markers: Step 5b, Step 5c, independent conditional blocks)
    - workflows/handoff.md (verify markers: TypeScript, LKT, BIB, independent IF block)
  </read_first>
  <behavior>
    INTG-02 (Non-business regression — ~5 assertions):
    - brief.md contains "businessMode == false" path (non-business skip)
    - brief.md writes "manifest-set-top-level businessMode" (ensures field exists for non-business)
    - brief.md contains hasBusinessThesis in 20-field coverage write (pass-through)
    - BTH-business-thesis appears AFTER businessMode == true gate (ordering check)
    - Non-business paths do not generate business artifacts

    INTG-03 (business:software — ~4 assertions):
    - wireframe.md contains both WFR (software) and LDP-landing-page (business)
    - wireframe.md does NOT contain "ELSE IF businessMode" or "else.*businessMode" after WFR block
    - handoff.md contains both TypeScript (software) and LKT (business)
    - handoff.md does NOT gate LKT inside ELSE IF from software path

    INTG-04 (business:hardware — ~3 assertions):
    - hasHardwareSpec is in the TWENTY_FIELDS array (coverage field exists)
    - flows.md contains businessMode read before SBP-service-blueprint generation
    - handoff.md does not gate business paths on PRODUCT_TYPE === "software"

    INTG-05 (business:experience — ~4 assertions):
    - flows.md has both PRODUCT_TYPE == "experience" gate AND $BM == "true" gate
    - flows.md contains "experience flow artifacts AND business flow artifacts"
    - system.md contains both "Step 5b" and "Step 5c" with "independent conditional blocks"
    - handoff.md contains "independent IF block" documenting non-ELSE-IF relationship

    INTG-06 (Deploy approval gates — ~5 assertions):
    - deploy.md contains all 4 gate labels Gate 1/4 through Gate 4/4 in ascending order
    - deploy.md contains "Halt -- stop deployment" option text
    - deploy.md contains "Deploy halted at Gate 1/4. No files written."
    - deploy.md contains "Deploy halted at Gate 4/4."
    - deploy.md contains "NEVER skip an approval gate"

    INTG-07 (20-field coverage across all v0.12 workflows — ~9 assertions):
    - Each of 9 V012_COVERAGE_WRITERS (brief.md, competitive.md, opportunity.md, flows.md, wireframe.md, critique.md, hig.md, handoff.md, system.md) contains all 20 TWENTY_FIELDS
  </behavior>
  <action>
Create `.planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` following the exact pattern from test-clobber-audit.cjs (Phase 93).

File structure:
1. Module header: `'use strict'` + `require('node:test')` / `require('node:assert')` / `require('node:fs')` / `require('node:path')`
2. ROOT resolution: `path.resolve(__dirname, '..', '..', '..', '..')`
3. `TWENTY_FIELDS` array (copy exact 20-element array from test-clobber-audit.cjs)
4. `readWorkflow(name)` helper function (same pattern)
5. Module-level file reads for all tested workflow files
6. Six `describe` blocks, one per INTG requirement:

**describe('INTG-02: Non-business product types produce byte-identical manifest output'):**
- Read `briefContent` from `workflows/brief.md`
- Assert `briefContent.includes('businessMode == false')` — non-business skip path exists
- Assert `briefContent.includes('manifest-set-top-level businessMode')` — field always written
- Assert `briefContent.includes('hasBusinessThesis')` — 20-field coverage pass-through
- Assert ordering: `briefContent.indexOf('businessMode == true')` < `briefContent.indexOf('BTH-business-thesis')` — BTH gated on businessMode
- Assert `wireframeContent.includes('manifest-get-top-level businessMode')` — wireframe reads businessMode before deciding LDP

**describe('INTG-03: business:software composition — independent IF blocks'):**
- Assert `wireframeContent.includes('WFR')` AND `wireframeContent.includes('LDP-landing-page')` — both artifact paths exist
- Assert `!wireframeContent.match(/ELSE\s+IF.*businessMode/)` — no ELSE IF pattern (use regex test, not includes)
- Assert `handoffContent.includes('TypeScript')` AND `handoffContent.includes('LKT')` — both paths exist
- Assert `!handoffContent.match(/ELSE\s+IF.*businessMode/)` — no ELSE IF pattern

**describe('INTG-04: business:hardware composition — hardware + business coexist'):**
- Assert `TWENTY_FIELDS.includes('hasHardwareSpec')` — field exists in canonical list
- Assert ordering in `flowsContent`: `indexOf('manifest-get-top-level businessMode')` < `indexOf('SBP-service-blueprint')` — businessMode read before SBP
- Assert `!handoffContent.match(/PRODUCT_TYPE\s*===?\s*"software".*LKT/)` — LKT not gated on software-only

**describe('INTG-05: business:experience composition — both artifact sets produced'):**
- Assert `flowsContent.includes('PRODUCT_TYPE == "experience"')` AND `flowsContent.includes('$BM == "true"')` — both gates exist
- Assert `flowsContent.includes('experience flow artifacts AND business flow artifacts')` — composition documented
- Assert `systemContent.includes('Step 5b')` AND `(systemContent.includes('Steps 5c') || systemContent.includes('Step 5c'))` — both blocks exist
- Assert `systemContent.includes('independent conditional blocks')` — documents non-ELSE-IF
- Assert `handoffContent.includes('independent IF block')` — documents non-ELSE-IF in handoff

**describe('INTG-06: deploy.md halts at each gate on user decline'):**
- Read `deployContent` from `workflows/deploy.md`
- Assert ordering: indexOf Gate 1/4 < Gate 2/4 < Gate 3/4 < Gate 4/4 — all 4 exist in order
- Assert `deployContent.includes('Halt -- stop deployment')` — halt option text
- Assert `deployContent.includes('Deploy halted at Gate 1/4. No files written.')` — Gate 1 halt message
- Assert `deployContent.includes('Deploy halted at Gate 4/4.')` — Gate 4 halt message
- Assert `deployContent.includes('NEVER skip an approval gate')` — enforcement instruction

**describe('INTG-07: all v0.12 coverage-writing workflows contain all 20 designCoverage fields'):**
- Define `V012_COVERAGE_WRITERS` array: `['brief.md', 'competitive.md', 'opportunity.md', 'flows.md', 'wireframe.md', 'critique.md', 'hig.md', 'handoff.md', 'system.md']`
- Loop: for each filename, read file and assert `TWENTY_FIELDS.filter(f => !content.includes(f)).length === 0`
- Each loop iteration is a separate `it()` block: `it('${filename} contains all 20 designCoverage field names', ...)`

Use `require('node:assert')` (not strict) per Phase 93 convention. Use `assert.ok()` for all assertions with descriptive failure messages. Do NOT use try/catch for file reads — all workflow files exist. Total: approximately 30 `it()` blocks across 6 `describe()` blocks.

IMPORTANT: Verify each string marker by reading the actual workflow file BEFORE writing the assertion. The markers listed above are from research — confirm they are still present. If any marker has changed, use the actual current string from the file.
  </action>
  <verify>
    <automated>node --test ".planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs`
    - File starts with `'use strict';`
    - File contains `const ROOT = path.resolve(__dirname, '..', '..', '..', '..');`
    - File contains `describe('INTG-02` (non-business regression block)
    - File contains `describe('INTG-03` (business:software composition block)
    - File contains `describe('INTG-04` (business:hardware composition block)
    - File contains `describe('INTG-05` (business:experience composition block)
    - File contains `describe('INTG-06` (deploy approval gates block)
    - File contains `describe('INTG-07` (20-field coverage block)
    - File contains `TWENTY_FIELDS` array with exactly 20 elements
    - File contains `V012_COVERAGE_WRITERS` or equivalent array listing 9 workflow filenames
    - `node --test` on the file exits with code 0 (all tests pass)
    - Test count is >= 25 assertions (it blocks)
  </acceptance_criteria>
  <done>test-regression-matrix.cjs exists with 6 describe blocks covering all INTG requirements, all assertions GREEN on `node --test`</done>
</task>

<task type="auto">
  <name>Task 2: Run full Nyquist regression suite and verify no prior test breakage</name>
  <files>.planning/phases/94-nyquist-regression-tests/94-VALIDATION.md</files>
  <read_first>
    - .planning/phases/94-nyquist-regression-tests/94-VALIDATION.md (update status fields)
    - .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs (just created — verify it exists)
  </read_first>
  <action>
Run the full CJS Nyquist test suite across ALL phases to confirm no regressions:

```bash
cd "/Users/greyaltaer/code/projects/Platform Development Engine" && find .planning/phases -name "*.cjs" -path "*/tests/*" | sort | xargs node --test
```

Expected: All tests GREEN (189 prior tests + ~30 new Phase 94 tests = ~219 total).

If any prior test fails, investigate whether it is the known Phase 64 ESM issue (which is NOT in the CJS tree) or an actual regression. If an actual regression, fix the Phase 94 test file — do NOT modify workflow files (Phase 94 is read-only on workflows).

After full suite GREEN, update `.planning/phases/94-nyquist-regression-tests/94-VALIDATION.md`:

1. Set frontmatter `nyquist_compliant: true` and `wave_0_complete: true`
2. Update the Per-Task Verification Map table: set all 6 rows (94-01-01 through 94-01-06) to `File Exists: yes` and `Status: GREEN`
3. Check all Validation Sign-Off boxes
4. Set `Approval: APPROVED`
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && find .planning/phases -name "*.cjs" -path "*/tests/*" | sort | xargs node --test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Full CJS test suite exits with code 0
    - 94-VALIDATION.md frontmatter contains `nyquist_compliant: true`
    - 94-VALIDATION.md frontmatter contains `wave_0_complete: true`
    - 94-VALIDATION.md contains `Approval: APPROVED`
    - All 6 Per-Task Verification Map rows show `Status` as GREEN (not pending)
  </acceptance_criteria>
  <done>Full Nyquist CJS test suite (all phases 84-94) passes with zero failures; 94-VALIDATION.md updated to APPROVED</done>
</task>

</tasks>

<verification>
1. `node --test .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` exits 0
2. Full suite: `find .planning/phases -name "*.cjs" -path "*/tests/*" | sort | xargs node --test` exits 0
3. All 6 INTG requirements (INTG-02 through INTG-07) have at least one GREEN assertion in the test file
4. No workflow files modified (Phase 94 is read-only on production code)
</verification>

<success_criteria>
- test-regression-matrix.cjs contains >= 25 test assertions across 6 describe blocks
- All tests GREEN on `node --test`
- Full CJS suite (~219 tests across phases 84-94) all GREEN
- 94-VALIDATION.md updated to nyquist_compliant: true
</success_criteria>

<output>
After completion, create `.planning/phases/94-nyquist-regression-tests/94-01-SUMMARY.md`
</output>
