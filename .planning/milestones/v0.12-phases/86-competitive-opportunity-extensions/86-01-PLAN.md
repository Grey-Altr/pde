---
phase: 86-competitive-opportunity-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/competitive.md
  - .planning/phases/86-competitive-opportunity-extensions/tests/test-competitive-mls.cjs
autonomous: true
requirements: [MRKT-01, MRKT-02, MRKT-04, MRKT-05]

must_haves:
  truths:
    - "competitive.md produces an MLS artifact with TAM/SAM/SOM sizing using [Source required] placeholders when businessMode is true"
    - "competitive.md generates a Mermaid quadrantChart positioning matrix when businessMode is true"
    - "competitive.md sets hasMarketLandscape to true in a 20-field designCoverage write"
    - "Market landscape depth differs by track: solo_founder gets 1-page summary, startup_team gets competitive deep-dive, product_leader gets build-vs-buy analysis"
    - "Non-business projects produce byte-identical CMP output — no MLS artifact, no business sections"
  artifacts:
    - path: "workflows/competitive.md"
      provides: "MLS artifact generation, Mermaid quadrant chart, track-differentiated depth, 20-field coverage write"
      contains: "quadrantChart"
    - path: ".planning/phases/86-competitive-opportunity-extensions/tests/test-competitive-mls.cjs"
      provides: "Structural tests for MRKT-01, MRKT-02, MRKT-04, MRKT-05"
  key_links:
    - from: "workflows/competitive.md"
      to: "references/business-track.md"
      via: "@references/business-track.md in required_reading"
      pattern: "@references/business-track.md"
    - from: "workflows/competitive.md"
      to: "references/business-financial-disclaimer.md"
      via: "@references/business-financial-disclaimer.md in required_reading"
      pattern: "@references/business-financial-disclaimer.md"
    - from: "workflows/competitive.md"
      to: "designCoverage 20-field write"
      via: "manifest-set-top-level designCoverage with hasMarketLandscape"
      pattern: "hasMarketLandscape"
---

<objective>
Extend competitive.md with business-mode market landscape sizing (MLS artifact), Mermaid quadrant chart positioning matrix, track-differentiated depth, and 20-field designCoverage write.

Purpose: When businessMode is true, /pde:competitive produces a separate MLS artifact with TAM/SAM/SOM placeholder tables and a Mermaid quadrant chart alongside the existing CMP artifact. The designCoverage write is upgraded from 16 to 20 fields for all projects (business or not).
Output: Modified workflows/competitive.md + structural test file
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-competitive-opportunity-extensions/86-RESEARCH.md
@.planning/phases/85-brief-extensions-detection/85-02-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns the executor needs from the codebase -->

From workflows/competitive.md (current structure):
- Step 4/7 contains sub-steps 4a through 4h (analysis steps)
- Step 5/7 writes CMP artifact to .planning/design/strategy/CMP-competitive-v{N}.md
- Step 7/7 writes designCoverage with 16 fields (must become 20)
- required_reading block contains @references/skill-style-guide.md, @references/mcp-integration.md, @references/strategy-frameworks.md

From workflows/brief.md (established pattern for businessMode gating):
- businessMode read: `BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)`
- businessTrack read: `BT=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessTrack 2>/dev/null)`
- Conditional: `IF businessMode == true AND businessTrack is not null:`

From Phase 85 test pattern:
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert');
const fs = require('node:fs');
const path = require('node:path');
const ROOT = path.resolve(__dirname, '..', '..', '..', '..');
const content = fs.readFileSync(path.join(ROOT, 'workflows', 'competitive.md'), 'utf-8');
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create structural test scaffold for MRKT-01, MRKT-02, MRKT-04, MRKT-05</name>
  <files>.planning/phases/86-competitive-opportunity-extensions/tests/test-competitive-mls.cjs</files>
  <read_first>
    - workflows/competitive.md (current state — understand what must change)
    - .planning/phases/85-brief-extensions-detection/tests/test-brief-artifacts.cjs (test pattern reference)
    - .planning/phases/86-competitive-opportunity-extensions/86-RESEARCH.md (requirement details)
    - references/business-track.md (track depth specifications)
    - references/business-financial-disclaimer.md (placeholder format rules)
  </read_first>
  <behavior>
    MRKT-01 tests (MLS artifact write):
    - competitive.md contains "MLS-market-landscape" artifact filename pattern
    - competitive.md contains "TAM" AND "SAM" AND "SOM" text
    - competitive.md contains "[Source required]" placeholder text
    - competitive.md contains "[YOUR_TAM_SIZE]" placeholder
    - competitive.md contains "[VERIFY FINANCIAL ASSUMPTIONS]" inline flag
    - MLS artifact write is gated on businessMode == true conditional

    MRKT-02 tests (Mermaid quadrant chart):
    - competitive.md contains "quadrantChart" Mermaid type declaration
    - competitive.md contains "x-axis" and "y-axis" Mermaid axis labels
    - competitive.md contains quadrant coordinate pattern [x, y] with 0-1 range notation

    MRKT-04 tests (20-field designCoverage):
    - competitive.md contains "hasMarketLandscape" in designCoverage write
    - competitive.md designCoverage write contains ALL 20 field names: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit
    - competitive.md no longer contains a 16-field-only designCoverage write (the old pattern with only 16 fields is replaced)

    MRKT-05 tests (track depth differentiation):
    - competitive.md contains "solo_founder" track conditional
    - competitive.md contains "startup_team" track conditional
    - competitive.md contains "product_leader" track conditional
    - product_leader section contains "build-vs-buy" or "Build vs. Buy" language
    - solo_founder section references 1-page or summary format
  </behavior>
  <action>
    Create `.planning/phases/86-competitive-opportunity-extensions/tests/test-competitive-mls.cjs` using the identical node:test pattern from Phase 85 test files.

    File structure:
    1. 'use strict' + requires for node:test, node:assert, node:fs, node:path
    2. ROOT = path.resolve(__dirname, '..', '..', '..', '..') — goes up 4 levels from tests/ to project root
    3. competitiveContent = fs.readFileSync(path.join(ROOT, 'workflows', 'competitive.md'), 'utf-8')
    4. Four describe blocks: MRKT-01 (6 tests), MRKT-02 (3 tests), MRKT-04 (3 tests), MRKT-05 (5 tests) = 17 tests total

    Test assertions use `assert.ok(content.includes('X'), 'message')` or `assert.ok(/pattern/.test(content), 'message')` for regex checks.

    For the 20-field completeness test (MRKT-04), check that all 20 canonical field names appear in the file:
    ```javascript
    const TWENTY_FIELDS = [
      'hasDesignSystem', 'hasWireframes', 'hasFlows', 'hasHardwareSpec',
      'hasCritique', 'hasIterate', 'hasHandoff', 'hasIdeation',
      'hasCompetitive', 'hasOpportunity', 'hasMockup', 'hasHigAudit',
      'hasRecommendations', 'hasStitchWireframes', 'hasPrintCollateral',
      'hasProductionBible', 'hasBusinessThesis', 'hasMarketLandscape',
      'hasServiceBlueprint', 'hasLaunchKit'
    ];
    ```
    Assert each field name appears at least once in the file content.

    For the 16-field regression test (MRKT-04), verify the old "16 flags" or "16-field" instruction text is replaced — look for absence of the text "16 flags" and "all 16 fields" (the new text should say "20 fields" or "all 20 fields").

    All tests MUST fail in RED state (competitive.md not yet modified). Run and confirm failure.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && node --test .planning/phases/86-competitive-opportunity-extensions/tests/test-competitive-mls.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/86-competitive-opportunity-extensions/tests/test-competitive-mls.cjs exists
    - File contains `require('node:test')` and `require('node:assert')`
    - File contains `describe('MRKT-01` and `describe('MRKT-02` and `describe('MRKT-04` and `describe('MRKT-05`
    - File contains all 20 field names in the TWENTY_FIELDS array
    - File contains at least 17 `it(` test declarations
    - Running `node --test` reports all tests as FAILING (RED state) — this is expected and correct
  </acceptance_criteria>
  <done>17 structural tests exist covering MRKT-01, MRKT-02, MRKT-04, MRKT-05. All tests fail against unmodified competitive.md (RED state confirmed).</done>
</task>

<task type="auto">
  <name>Task 2: Extend competitive.md with MLS artifact, Mermaid quadrant, track depth, and 20-field coverage</name>
  <files>workflows/competitive.md</files>
  <read_first>
    - workflows/competitive.md (full file — current state before modification)
    - references/business-track.md (track depth specifications — solo_founder, startup_team, product_leader)
    - references/business-financial-disclaimer.md (placeholder format: [YOUR_X] + [VERIFY FINANCIAL ASSUMPTIONS])
    - references/launch-frameworks.md (lean canvas schema for cross-reference)
    - .planning/phases/86-competitive-opportunity-extensions/86-RESEARCH.md (architecture patterns, code examples, anti-patterns)
    - workflows/brief.md (businessMode gating pattern reference — search for "businessMode" to see established conditional structure)
  </read_first>
  <action>
    Modify workflows/competitive.md with the following changes. The file is currently 601 lines. Approximately 120 lines will be added.

    **Change 1: Add required_reading entries (in the existing required_reading block near line 55-59)**

    Add these two lines after the existing @references/strategy-frameworks.md entry:
    ```
    @references/business-track.md
    @references/business-financial-disclaimer.md
    ```

    **Change 2: Add businessMode/businessTrack read at the START of Step 4 (before sub-step 4a, around line 230)**

    Insert before the "#### 4a" line:
    ```markdown
    **Business Mode Detection:**

    Before beginning analysis, read business mode status from manifest:

    ```bash
    BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
    BT=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessTrack 2>/dev/null)
    ```

    Cache `$BM` and `$BT` for use in Steps 4i, 4j, 5, and 7.
    ```

    **Change 3: Add Step 4i — Market Landscape Sizing (after Step 4h, before the Step 4 display line)**

    Insert new sub-section `#### 4i. Market Landscape Sizing (business mode only)`:

    ```markdown
    #### 4i. Market Landscape Sizing (business mode only)

    IF businessMode == true AND businessTrack is not null:

    Generate a TAM/SAM/SOM market landscape sizing section. This content will be written to a SEPARATE MLS artifact (not inside the CMP artifact).

    **All market sizing values MUST use structural placeholders per @references/business-financial-disclaimer.md. NEVER generate specific dollar amounts for TAM, SAM, or SOM.**

    IF businessTrack == "solo_founder":
      Generate a 1-page summary format:
      - Simple 3-row TAM/SAM/SOM table with [YOUR_TAM_SIZE], [YOUR_SAM_SIZE], [YOUR_SOM_SIZE] placeholders
      - Each row has: Market Level, Definition (1 sentence), Size Estimate (placeholder), Sources ([Source required])
      - 1 paragraph of qualitative market trend context (no dollar amounts)
      - Bottom-up methodology note: [YOUR_UNIT_PRICE] x [YOUR_ADDRESSABLE_USERS] = SOM baseline
      - Limit to 3 competing tools in analysis context

    IF businessTrack == "startup_team":
      Generate a competitive deep-dive format:
      - Full TAM/SAM/SOM table with methodology columns (top-down AND bottom-up)
      - Each row has: Market Level, Definition, Size Estimate ([YOUR_X] [VERIFY FINANCIAL ASSUMPTIONS]), Methodology, Sources ([Source required])
      - Top-down methodology section: Industry total -> segment filter -> geographic scope -> product category
      - Bottom-up methodology section: [YOUR_UNIT_PRICE] x [YOUR_ADDRESSABLE_USERS] = SOM, extrapolate to SAM/TAM
      - Reconciliation note: "Credible investor presentations show both approaches"
      - Market trend context with segment analysis (no dollar amounts)
      - 5-8 competitors in competitive landscape context

    IF businessTrack == "product_leader":
      Generate a build-vs-buy analysis format:
      - Full TAM/SAM/SOM table (same as startup_team depth)
      - Build vs. Buy Decision Matrix table:
        | Alternative | Category | Core Capability | Integration Effort | TCO [VERIFY FINANCIAL ASSUMPTIONS] | Differentiating? |
        Rows for each major competitor + "Build in-house" row
        TCO values use [YOUR_TCO_VENDOR_X] placeholders
      - Recommendation Framework:
        "Build when: Core to competitive differentiation AND in-house expertise exists AND long-term maintenance cost < vendor TCO"
        "Buy when: Commodity capability AND vendor roadmap aligns AND integration < build cost"
      - Initiative ROI Placeholder table:
        | Metric | Value |
        Expected ROI: [YOUR_INITIATIVE_ROI] [VERIFY FINANCIAL ASSUMPTIONS]
        Payback Period: [YOUR_PAYBACK_PERIOD] months [VERIFY FINANCIAL ASSUMPTIONS]
        P&L Impact: [YOUR_PL_IMPACT] [VERIFY FINANCIAL ASSUMPTIONS]
        Success Metric: [YOUR_OKR_METRIC]
      - 8+ market alternatives in stakeholder vocabulary, OKR framing

    Set flag: `MLS_CONTENT_GENERATED=true`

    ELSE (businessMode != true):
      Skip silently. Display nothing. Continue to next step.
      Set flag: `MLS_CONTENT_GENERATED=false`
    ```

    **Change 4: Add Step 4j — Competitive Positioning Matrix (after Step 4i)**

    ```markdown
    #### 4j. Competitive Positioning Matrix — Mermaid quadrant chart (business mode only)

    IF businessMode == true AND MLS_CONTENT_GENERATED == true:

    Generate a Mermaid `quadrantChart` using the SAME axis pairs already determined in Step 4d (do not select new axes).

    **Coordinate normalization:** Divide the 0-10 SVG scores from Step 4d by 10 to get 0-1 Mermaid coordinates: `mermaid_coord = svg_score / 10`.

    **Exact Mermaid syntax (verified against official docs):**

    ```mermaid
    quadrantChart
        title Competitive Positioning: {product_name}
        x-axis {Low Axis Label} --> {High Axis Label}
        y-axis {Low Axis Label} --> {High Axis Label}
        quadrant-1 {top-right label}
        quadrant-2 {top-left label}
        quadrant-3 {bottom-left label}
        quadrant-4 {bottom-right label}
        Our Product: [{x/10}, {y/10}] radius: 14, color: #2563eb
        {Competitor A}: [{x/10}, {y/10}]
        {Competitor B}: [{x/10}, {y/10}]
    ```

    **Quadrant layout:** quadrant-1=top-right, quadrant-2=top-left, quadrant-3=bottom-left, quadrant-4=bottom-right.

    **Number of competitors on chart by track:**
    - solo_founder: 3 competitors (minimal labeling)
    - startup_team: 5-8 competitors (quadrant interpretation per segment)
    - product_leader: 8+ competitors (stakeholder vocabulary)

    After the Mermaid chart, include a **Differentiation Analysis** section interpreting:
    - Which quadrant "Our Product" occupies and why
    - Whitespace opportunities (empty or sparse quadrants)
    - Competitive cluster analysis (where competitors group)
    - Strategic positioning recommendation

    ELSE:
      Skip silently. Continue to next step.
    ```

    **Change 5: Extend Step 5 — MLS artifact write (in the Step 5/7 section, after the CMP artifact write)**

    After the existing CMP artifact write instructions but before the Step 5 display line, add:

    ```markdown
    **MLS Artifact Write (business mode only):**

    IF businessMode == true AND MLS_CONTENT_GENERATED == true:

    Write the market landscape analysis to `.planning/design/strategy/MLS-market-landscape-v{N}.md` where {N} matches the CMP version number (locked versioning — MLS version always equals CMP version).

    **YAML Frontmatter:**
    ```yaml
    ---
    Generated: "{ISO 8601 date}"
    Skill: /pde:competitive (MLS)
    Version: v{N}
    businessTrack: {solo_founder|startup_team|product_leader}
    dependsOn: CMP
    ---
    ```

    **Sections in order:**
    1. `# Market Landscape Sizing: {product_name}`
    2. `## TAM/SAM/SOM Overview` — the content generated in Step 4i
    3. `## Competitive Positioning Matrix` — the Mermaid quadrantChart from Step 4j
    4. `## Differentiation Analysis` — per-quadrant interpretation from Step 4j

    **Post-write verification (MANDATORY):**
    ```bash
    if grep -qE '\$[0-9]' ".planning/design/strategy/MLS-market-landscape-v${N}.md" 2>/dev/null; then
      echo "ERROR: Dollar amount detected in MLS artifact. Use [YOUR_X] placeholders only."
      grep -nE '\$[0-9]' ".planning/design/strategy/MLS-market-landscape-v${N}.md"
    fi
    ```

    **Register MLS artifact in manifest (7 calls):**
    ```bash
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MLS name "Market Landscape Sizing"
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MLS type market-landscape
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MLS domain strategy
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MLS path ".planning/design/strategy/MLS-market-landscape-v{N}.md"
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MLS status draft
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MLS version {N}
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update MLS dependsOn '["CMP"]'
    ```

    Set flag: `MLS_WRITTEN=true`

    ELSE:
      Set flag: `MLS_WRITTEN=false`
    ```

    **Change 6: Update Step 7 — designCoverage write from 16 fields to 20 fields**

    In Step 7/7 (around line 540-557), replace the existing 16-field designCoverage instructions with 20-field instructions.

    Replace the text "Parse the JSON result. Extract all 16 flags (default absent flags to `false`):" and the 16-field list with:
    ```
    Parse the JSON result. Extract all 20 flags (default absent flags to `false`):
    - `hasDesignSystem`, `hasWireframes`, `hasFlows`, `hasHardwareSpec`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit`, `hasRecommendations`, `hasStitchWireframes`, `hasPrintCollateral`, `hasProductionBible`, `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`
    ```

    Replace the 16-field JSON write command with 20 fields:
    ```bash
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage \
      '{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":true,"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":{current},"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{MLS_WRITTEN_VALUE},"hasServiceBlueprint":{current},"hasLaunchKit":{current}}'
    ```

    Where `{MLS_WRITTEN_VALUE}` is: `true` if MLS_WRITTEN == true, else pass through the current value from coverage-check.

    Replace the text "ALWAYS write all 16 fields" with "ALWAYS write all 20 fields".
    Replace "The canonical field order is: hasDesignSystem, ... hasProductionBible." with the full 20-field canonical order including hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit.

    **Change 7: Update Step 6 — Add MLS row to domain DESIGN-STATE (in Step 6/7)**

    In the Step 6/7 section where CMP is added to the cross-domain dependency map, add a conditional MLS row:

    ```markdown
    IF MLS_WRITTEN == true:
      Add MLS entry to strategy domain DESIGN-STATE.md alongside CMP entry:
      - Cross-Domain Dependency Map: `| MLS | Market Landscape Sizing | v{N} | CMP |`
      - Iteration History: `| MLS | v{N} | Business market landscape sizing |`
    ```

    **Change 8: Update Summary table (at the end of the file)**

    In the Summary section, update the "Files created" row to conditionally include MLS:

    ```
    | Files created | .planning/design/strategy/CMP-competitive-v{N}.md{, .planning/design/strategy/MLS-market-landscape-v{N}.md if businessMode} |
    ```

    **Change 9: Add Anti-Pattern entries**

    In the Anti-Patterns section at the bottom of the file, add:
    - NEVER insert MLS content inside the CMP artifact. MLS is a SEPARATE artifact file (MLS-market-landscape-v{N}.md). CMP and MLS have different lifecycles.
    - NEVER generate specific dollar amounts for TAM, SAM, or SOM. Use [YOUR_TAM_SIZE] [Source required] for every sizing cell. Post-write verification bash check is MANDATORY.
    - NEVER write designCoverage with fewer than 20 fields. The 16-field version is a regression from Phase 84. Always include hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit alongside the original 16 fields.
    - NEVER create MLS artifact when businessMode is false. The MLS_WRITTEN flag must gate both the artifact write AND the manifest registration AND the designCoverage hasMarketLandscape value.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && node --test .planning/phases/86-competitive-opportunity-extensions/tests/test-competitive-mls.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - workflows/competitive.md contains "@references/business-track.md" in required_reading
    - workflows/competitive.md contains "@references/business-financial-disclaimer.md" in required_reading
    - workflows/competitive.md contains "manifest-get-top-level businessMode"
    - workflows/competitive.md contains "#### 4i" section header for market landscape sizing
    - workflows/competitive.md contains "#### 4j" section header for Mermaid quadrant chart
    - workflows/competitive.md contains "quadrantChart" (Mermaid type declaration)
    - workflows/competitive.md contains "x-axis" and "y-axis" (Mermaid axis syntax)
    - workflows/competitive.md contains "[YOUR_TAM_SIZE]" and "[YOUR_SAM_SIZE]" and "[YOUR_SOM_SIZE]"
    - workflows/competitive.md contains "[Source required]"
    - workflows/competitive.md contains "[VERIFY FINANCIAL ASSUMPTIONS]"
    - workflows/competitive.md contains "MLS-market-landscape-v"
    - workflows/competitive.md contains "solo_founder" AND "startup_team" AND "product_leader" track conditionals
    - workflows/competitive.md contains "build-vs-buy" or "Build vs. Buy" (product_leader track)
    - workflows/competitive.md contains "hasMarketLandscape" in designCoverage write
    - workflows/competitive.md contains "hasBusinessThesis" AND "hasServiceBlueprint" AND "hasLaunchKit" (20-field completeness)
    - workflows/competitive.md contains "all 20 fields" or "20 fields" (replacing old "16 fields" language)
    - workflows/competitive.md does NOT contain "all 16 fields" (old pattern replaced)
    - All 17 tests in test-competitive-mls.cjs pass (GREEN state)
  </acceptance_criteria>
  <done>competitive.md extended with MLS artifact generation, Mermaid quadrant chart, three-track depth differentiation, and 20-field designCoverage write. All 17 structural tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
node --test .planning/phases/86-competitive-opportunity-extensions/tests/test-competitive-mls.cjs
```
All 17 tests must pass. Zero failures, zero errors.

Verify no dollar amounts in workflow:
```bash
grep -nE '\$[0-9]' workflows/competitive.md || echo "No dollar amounts found (correct)"
```

Verify 20-field coverage:
```bash
grep -c 'hasLaunchKit' workflows/competitive.md  # Must be >= 1
grep -c 'hasBusinessThesis' workflows/competitive.md  # Must be >= 1
grep -c 'hasServiceBlueprint' workflows/competitive.md  # Must be >= 1
```
</verification>

<success_criteria>
- competitive.md contains business-mode MLS artifact generation with TAM/SAM/SOM placeholder tables
- competitive.md contains Mermaid quadrantChart with normalized 0-1 coordinates
- competitive.md differentiates depth by track (solo=summary, startup=deep-dive, leader=build-vs-buy)
- competitive.md writes designCoverage with all 20 fields including hasMarketLandscape
- No dollar amounts appear in any placeholder template
- All 17 structural tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/86-competitive-opportunity-extensions/86-01-SUMMARY.md`
</output>
