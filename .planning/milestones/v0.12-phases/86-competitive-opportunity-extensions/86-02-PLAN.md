---
phase: 86-competitive-opportunity-extensions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/opportunity.md
  - .planning/phases/86-competitive-opportunity-extensions/tests/test-opportunity-rice.cjs
autonomous: true
requirements: [MRKT-03]

must_haves:
  truths:
    - "opportunity.md extends RICE scoring with business initiative framing when businessMode is true"
    - "Unit economics section contains LTV formula, CAC ceiling, and payback period at 3 churn scenarios as structural placeholders"
    - "Unit economics section is gated on businessMode == true — non-business projects are unaffected"
    - "opportunity.md writes designCoverage with 20 fields (upgraded from 16)"
  artifacts:
    - path: "workflows/opportunity.md"
      provides: "Business initiative framing with unit economics placeholders in RICE scoring"
      contains: "businessMode"
    - path: ".planning/phases/86-competitive-opportunity-extensions/tests/test-opportunity-rice.cjs"
      provides: "Structural tests for MRKT-03"
  key_links:
    - from: "workflows/opportunity.md"
      to: "references/business-financial-disclaimer.md"
      via: "@references/business-financial-disclaimer.md in required_reading"
      pattern: "@references/business-financial-disclaimer.md"
    - from: "workflows/opportunity.md"
      to: "designCoverage 20-field write"
      via: "manifest-set-top-level designCoverage with all 20 fields"
      pattern: "hasLaunchKit"
---

<objective>
Extend opportunity.md with business initiative framing in RICE scoring, adding unit economics structural inputs (LTV formula, CAC ceiling, payback period at 3 churn scenarios) as placeholders when businessMode is true. Also upgrade designCoverage write from 16 to 20 fields.

Purpose: When businessMode is true, /pde:opportunity contextualizes RICE scoring with business initiative framing — unit economics inputs help users understand how feature opportunities map to business viability.
Output: Modified workflows/opportunity.md + structural test file
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-competitive-opportunity-extensions/86-RESEARCH.md
@.planning/phases/85-brief-extensions-detection/85-02-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns the executor needs from the codebase -->

From workflows/opportunity.md (current structure):
- Step 4/7 contains interactive RICE scoring with 7 dimensions
- Step 5/7 writes OPP artifact to .planning/design/strategy/OPP-opportunity-v{N}.md
- Step 7/7 writes designCoverage with 16 fields (must become 20)
- required_reading block contains @references/skill-style-guide.md, @references/mcp-integration.md, @references/strategy-frameworks.md

From workflows/brief.md (established pattern for businessMode gating):
- businessMode read: `BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)`
- businessTrack read: `BT=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessTrack 2>/dev/null)`
- Conditional: `IF businessMode == true AND businessTrack is not null:`

From Phase 85 test pattern:
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert');
const fs = require('node:fs');
const path = require('node:path');
const ROOT = path.resolve(__dirname, '..', '..', '..', '..');
const content = fs.readFileSync(path.join(ROOT, 'workflows', 'opportunity.md'), 'utf-8');
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create structural test scaffold for MRKT-03</name>
  <files>.planning/phases/86-competitive-opportunity-extensions/tests/test-opportunity-rice.cjs</files>
  <read_first>
    - workflows/opportunity.md (current state — understand what must change)
    - .planning/phases/85-brief-extensions-detection/tests/test-brief-artifacts.cjs (test pattern reference)
    - .planning/phases/86-competitive-opportunity-extensions/86-RESEARCH.md (requirement details, unit economics template)
    - references/business-financial-disclaimer.md (placeholder format rules)
  </read_first>
  <behavior>
    MRKT-03 tests (unit economics in RICE scoring):
    - opportunity.md contains "businessMode" conditional check
    - opportunity.md contains "Business Initiative Framing" section header
    - opportunity.md contains LTV formula text: "Customer Lifetime Value" or "LTV"
    - opportunity.md contains "CAC" (Customer Acquisition Cost) reference
    - opportunity.md contains "[YOUR_LTV_ESTIMATE]" or "[YOUR_ARPU]" placeholder
    - opportunity.md contains "[YOUR_CAC_CEILING]" placeholder
    - opportunity.md contains "[YOUR_PAYBACK_PERIOD]" placeholder
    - opportunity.md contains "[VERIFY FINANCIAL ASSUMPTIONS]" inline flag
    - opportunity.md contains three churn scenario rows: "Optimistic" AND "Base Case" AND "Pessimistic"
    - opportunity.md contains "[YOUR_CHURN_RATE_LOW]" or similar churn rate placeholder
    - Unit economics section is gated on businessMode == true (contains IF businessMode conditional before the section)
    - opportunity.md designCoverage write contains all 20 field names (hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit alongside original 16)
  </behavior>
  <action>
    Create `.planning/phases/86-competitive-opportunity-extensions/tests/test-opportunity-rice.cjs` using the identical node:test pattern from Phase 85 test files.

    File structure:
    1. 'use strict' + requires for node:test, node:assert, node:fs, node:path
    2. ROOT = path.resolve(__dirname, '..', '..', '..', '..') — goes up 4 levels from tests/ to project root
    3. opportunityContent = fs.readFileSync(path.join(ROOT, 'workflows', 'opportunity.md'), 'utf-8')
    4. Two describe blocks: MRKT-03 (10 tests), 20-field designCoverage (2 tests) = 12 tests total

    MRKT-03 describe block tests:
    1. opportunity.md contains "businessMode" text
    2. opportunity.md contains "Business Initiative Framing" section
    3. opportunity.md contains LTV formula reference (check for "Lifetime Value" or "LTV")
    4. opportunity.md contains "[YOUR_CAC_CEILING]" placeholder
    5. opportunity.md contains "[YOUR_PAYBACK_PERIOD]" placeholder
    6. opportunity.md contains "[VERIFY FINANCIAL ASSUMPTIONS]"
    7. opportunity.md contains "Optimistic" churn scenario
    8. opportunity.md contains "Base Case" churn scenario
    9. opportunity.md contains "Pessimistic" churn scenario
    10. opportunity.md contains "[YOUR_CHURN_RATE" (partial match for any churn rate placeholder)

    20-field designCoverage describe block tests:
    11. opportunity.md contains all 20 field names (same TWENTY_FIELDS array check as Plan 01)
    12. opportunity.md does NOT contain "all 16 fields" (old pattern replaced)

    All tests MUST fail in RED state (opportunity.md not yet modified). Run and confirm failure.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && node --test .planning/phases/86-competitive-opportunity-extensions/tests/test-opportunity-rice.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/86-competitive-opportunity-extensions/tests/test-opportunity-rice.cjs exists
    - File contains `require('node:test')` and `require('node:assert')`
    - File contains `describe('MRKT-03` text
    - File contains at least 12 `it(` test declarations
    - File contains "[YOUR_CAC_CEILING]" and "[YOUR_PAYBACK_PERIOD]" as test assertion strings
    - Running `node --test` reports all tests as FAILING (RED state) — this is expected and correct
  </acceptance_criteria>
  <done>12 structural tests exist covering MRKT-03 and 20-field designCoverage. All tests fail against unmodified opportunity.md (RED state confirmed).</done>
</task>

<task type="auto">
  <name>Task 2: Extend opportunity.md with business initiative framing and 20-field coverage</name>
  <files>workflows/opportunity.md</files>
  <read_first>
    - workflows/opportunity.md (full file — current state before modification)
    - references/business-financial-disclaimer.md (placeholder format: [YOUR_X] + [VERIFY FINANCIAL ASSUMPTIONS])
    - references/business-track.md (track depth specifications — not heavily used here but for vocabulary reference)
    - .planning/phases/86-competitive-opportunity-extensions/86-RESEARCH.md (architecture patterns, unit economics template, anti-patterns)
    - workflows/brief.md (businessMode gating pattern reference — search for "businessMode")
  </read_first>
  <action>
    Modify workflows/opportunity.md with the following changes. The file is currently 523 lines. Approximately 60 lines will be added.

    **Change 1: Add required_reading entries (in the existing required_reading block near line 49-53)**

    Add these two lines after the existing @references/strategy-frameworks.md entry:
    ```
    @references/business-track.md
    @references/business-financial-disclaimer.md
    ```

    **Change 2: Add businessMode read at the START of Step 4 (before the interactive scoring begins, around line 214)**

    Insert before the "For each confirmed candidate" line:
    ```markdown
    **Business Mode Detection:**

    Before beginning scoring, read business mode status from manifest:

    ```bash
    BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
    BT=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessTrack 2>/dev/null)
    ```

    Cache `$BM` for use after RICE scoring and in Step 5 artifact write.
    ```

    **Change 3: Add Business Initiative Framing section at END of Step 4 (after the Now/Next/Later bucket assignment, before the Step 4 display line)**

    Insert new section after the priority buckets and before `Display: Step 4/7: RICE scoring complete.`:

    ```markdown
    **Business Initiative Framing (business mode only):**

    IF businessMode == true:

    After RICE scoring and bucket assignment, generate a Business Initiative Framing section that contextualizes opportunities through unit economics.

    **All financial values MUST use structural placeholders per @references/business-financial-disclaimer.md. NEVER generate specific dollar amounts.**

    Generate the following content (this will be included in the OPP artifact in Step 5):

    ## Business Initiative Framing
    *(Generated when businessMode = true)*

    > [VERIFY FINANCIAL ASSUMPTIONS] — All unit economics below are structural placeholders.
    > Replace [YOUR_X] values with your own researched figures before financial modeling.

    ### Core Unit Economics Inputs

    | Metric | Formula | Placeholder Value |
    |--------|---------|-------------------|
    | Customer Lifetime Value (LTV) | (ARPU x Gross Margin) / Churn Rate | ([YOUR_ARPU] x [YOUR_GROSS_MARGIN]) / [YOUR_CHURN_RATE] [VERIFY FINANCIAL ASSUMPTIONS] |
    | CAC Ceiling | LTV / [YOUR_LTV_CAC_RATIO_TARGET] | [YOUR_CAC_CEILING] [VERIFY FINANCIAL ASSUMPTIONS] |
    | Payback Period | CAC / (Monthly Revenue per Customer x Gross Margin) | [YOUR_PAYBACK_PERIOD] months [VERIFY FINANCIAL ASSUMPTIONS] |

    ### Payback Period at 3 Churn Scenarios

    | Churn Scenario | Monthly Churn | LTV Impact | Payback Period |
    |----------------|--------------|-----------|----------------|
    | Optimistic | [YOUR_CHURN_RATE_LOW] | [YOUR_LTV_HIGH] [VERIFY FINANCIAL ASSUMPTIONS] | [YOUR_PAYBACK_OPTIMISTIC] months |
    | Base Case | [YOUR_CHURN_RATE_BASE] | [YOUR_LTV_BASE] [VERIFY FINANCIAL ASSUMPTIONS] | [YOUR_PAYBACK_BASE] months |
    | Pessimistic | [YOUR_CHURN_RATE_HIGH] | [YOUR_LTV_LOW] [VERIFY FINANCIAL ASSUMPTIONS] | [YOUR_PAYBACK_PESSIMISTIC] months |

    **Standard SaaS benchmarks (for calibration only — do not use as your numbers):**
    - Healthy LTV/CAC ratio: 3:1 or higher
    - Target payback period: < 12 months (B2C), < 18 months (B2B)
    - Gross margin target: 70%+ (SaaS)

    ### RICE Score Business Context

    | RICE Dimension | Standard Interpretation | Business Initiative Lens |
    |---------------|------------------------|--------------------------|
    | Reach | Users affected per quarter | [YOUR_ICP_SEGMENT_SIZE] potential customers |
    | Impact | Per-user value delta | Revenue impact per account x LTV multiplier |
    | Confidence | Data quality level | Higher if supported by pricing validation data |
    | Effort | Person-months | Include GTM effort (sales cycles, onboarding) for business initiatives |

    Set flag: `BUSINESS_FRAMING_GENERATED=true`

    ELSE (businessMode != true):
      Skip silently. Display nothing. Continue to next step.
      Set flag: `BUSINESS_FRAMING_GENERATED=false`
    ```

    **Change 4: Extend Step 5 — Add business initiative framing to OPP artifact (in Step 5/7)**

    In the Step 5/7 artifact sections list (around line 337-349), add a conditional section after the existing sections:

    ```markdown
    8. IF businessMode == true AND BUSINESS_FRAMING_GENERATED == true:
       `## Business Initiative Framing` — the full unit economics content generated in Step 4 (Core Unit Economics Inputs table, Payback Period at 3 Churn Scenarios table, RICE Score Business Context table)
    ```

    **Change 5: Update Step 7 — designCoverage write from 16 fields to 20 fields**

    In Step 7/7 (around line 470-481), replace the existing 16-field designCoverage instructions with 20-field instructions.

    Replace the text "Parse the JSON result. Extract all 16 flags (default absent flags to `false`):" and the 16-field list with:
    ```
    Parse the JSON result. Extract all 20 flags (default absent flags to `false`):
    - `hasDesignSystem`, `hasWireframes`, `hasFlows`, `hasHardwareSpec`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit`, `hasRecommendations`, `hasStitchWireframes`, `hasPrintCollateral`, `hasProductionBible`, `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`
    ```

    Replace the 16-field JSON write command with 20 fields:
    ```bash
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage \
      '{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":true,"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":{current},"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{current},"hasLaunchKit":{current}}'
    ```

    Replace the text "ALWAYS write all 16 fields" with "ALWAYS write all 20 fields".
    Replace "The canonical field order is: hasDesignSystem, ... hasProductionBible." with the full 20-field canonical order including hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit.

    **Change 6: Add Anti-Pattern entries**

    In the Anti-Patterns section at the bottom of the file, add:
    - NEVER generate specific dollar amounts in the Business Initiative Framing section. Use [YOUR_X] [VERIFY FINANCIAL ASSUMPTIONS] format for all financial values.
    - NEVER include the Business Initiative Framing section when businessMode is false. The BUSINESS_FRAMING_GENERATED flag must gate both the Step 4 generation AND the Step 5 artifact section inclusion.
    - NEVER write designCoverage with fewer than 20 fields. The 16-field version is a regression from Phase 84. Always include hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit alongside the original 16 fields.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && node --test .planning/phases/86-competitive-opportunity-extensions/tests/test-opportunity-rice.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - workflows/opportunity.md contains "@references/business-track.md" in required_reading
    - workflows/opportunity.md contains "@references/business-financial-disclaimer.md" in required_reading
    - workflows/opportunity.md contains "manifest-get-top-level businessMode"
    - workflows/opportunity.md contains "Business Initiative Framing" section header
    - workflows/opportunity.md contains "Customer Lifetime Value" or "LTV"
    - workflows/opportunity.md contains "[YOUR_CAC_CEILING]"
    - workflows/opportunity.md contains "[YOUR_PAYBACK_PERIOD]"
    - workflows/opportunity.md contains "[VERIFY FINANCIAL ASSUMPTIONS]"
    - workflows/opportunity.md contains "Optimistic" AND "Base Case" AND "Pessimistic" churn scenarios
    - workflows/opportunity.md contains "[YOUR_CHURN_RATE_LOW]" or "[YOUR_CHURN_RATE"
    - workflows/opportunity.md contains "hasBusinessThesis" AND "hasMarketLandscape" AND "hasServiceBlueprint" AND "hasLaunchKit" (20-field completeness)
    - workflows/opportunity.md contains "all 20 fields" or "20 fields" (replacing old "16 fields" language)
    - workflows/opportunity.md does NOT contain "all 16 fields" (old pattern replaced)
    - All 12 tests in test-opportunity-rice.cjs pass (GREEN state)
  </acceptance_criteria>
  <done>opportunity.md extended with business initiative framing (LTV, CAC, payback at 3 churn scenarios) and 20-field designCoverage write. All 12 structural tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
node --test .planning/phases/86-competitive-opportunity-extensions/tests/test-opportunity-rice.cjs
```
All 12 tests must pass. Zero failures, zero errors.

Verify no dollar amounts in workflow:
```bash
grep -nE '\$[0-9]' workflows/opportunity.md || echo "No dollar amounts found (correct)"
```

Verify 20-field coverage:
```bash
grep -c 'hasLaunchKit' workflows/opportunity.md  # Must be >= 1
grep -c 'hasBusinessThesis' workflows/opportunity.md  # Must be >= 1
```
</verification>

<success_criteria>
- opportunity.md contains business initiative framing section gated on businessMode
- Unit economics section has LTV formula, CAC ceiling, payback period at 3 churn scenarios
- All financial values use [YOUR_X] [VERIFY FINANCIAL ASSUMPTIONS] placeholders
- opportunity.md writes designCoverage with all 20 fields
- Non-business projects are unaffected (businessMode gate prevents unit economics section)
- All 12 structural tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/86-competitive-opportunity-extensions/86-02-SUMMARY.md`
</output>
