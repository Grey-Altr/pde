---
phase: 96-21-field-cascade-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/84-foundation/tests/test-foundation.cjs
  - workflows/recommend.md
  - workflows/ideate.md
  - workflows/iterate.md
  - workflows/mockup.md
  - .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs
autonomous: true
requirements: [FOUND-02, INTG-01]

must_haves:
  truths:
    - "FOUND-02 test asserts exactly 21 designCoverage fields and passes"
    - "recommend.md writes all 21 fields including hasDeployStaging in its designCoverage JSON blob"
    - "ideate.md writes all 21 fields including hasDeployStaging in its designCoverage JSON blob"
    - "iterate.md writes all 21 fields including hasDeployStaging in its designCoverage JSON blob"
    - "mockup.md writes all 21 fields including hasDeployStaging in its designCoverage JSON blob"
    - "Full Nyquist suite passes with 0 failures"
  artifacts:
    - path: ".planning/phases/84-foundation/tests/test-foundation.cjs"
      provides: "FOUND-02 21-field assertion"
      contains: "21 designCoverage fields"
    - path: "workflows/recommend.md"
      provides: "21-field designCoverage write with hasDeployStaging pass-through"
      contains: "hasDeployStaging"
    - path: "workflows/ideate.md"
      provides: "21-field designCoverage write with hasDeployStaging pass-through"
      contains: "hasDeployStaging"
    - path: "workflows/iterate.md"
      provides: "21-field designCoverage write with hasDeployStaging pass-through"
      contains: "hasDeployStaging"
    - path: "workflows/mockup.md"
      provides: "21-field designCoverage write with hasDeployStaging pass-through"
      contains: "hasDeployStaging"
    - path: ".planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs"
      provides: "Updated clobber audit with 21-field TWENTY_ONE_FIELDS constant"
      contains: "hasDeployStaging"
  key_links:
    - from: "workflows/recommend.md"
      to: "templates/design-manifest.json"
      via: "manifest-set-top-level designCoverage JSON blob"
      pattern: "hasDeployStaging.*current"
    - from: ".planning/phases/84-foundation/tests/test-foundation.cjs"
      to: "templates/design-manifest.json"
      via: "coverageKeys.length assertion"
      pattern: "strictEqual.*21"
---

<objective>
Fix the last 2 open v0.12 requirement gaps: update FOUND-02 test assertion from 20 to 21 fields and add hasDeployStaging to 4 secondary workflow designCoverage writes.

Purpose: Eliminates the clobber scenario where `build --from recommend` after deploy reverts hasDeployStaging from true to absent, and makes the full Nyquist suite pass 235/235.
Output: 5 updated workflow/test files, 1 updated clobber-audit test, full green test suite.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-21-field-cascade-fix/96-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix FOUND-02 test assertion — 20 to 21 fields</name>
  <files>.planning/phases/84-foundation/tests/test-foundation.cjs</files>
  <read_first>
    - .planning/phases/84-foundation/tests/test-foundation.cjs (current state of FOUND-02 describe block)
    - .planning/phases/96-21-field-cascade-fix/96-RESEARCH.md (exact edit targets in "File 1" section)
    - templates/design-manifest.json (source of truth for 21 field names)
  </read_first>
  <action>
Update the FOUND-02 describe block in test-foundation.cjs with these exact changes:

1. **Line 8 (file header comment):** Change `FOUND-02: 20 designCoverage fields (16 existing + 4 new)` to `FOUND-02: 21 designCoverage fields (16 existing + 5 new)`

2. **Line 68 (describe title):** Change `'FOUND-02: 20 designCoverage fields in design-manifest.json'` to `'FOUND-02: 21 designCoverage fields in design-manifest.json'`

3. **Lines 75-77 (NEW_4 array):** Rename `NEW_4` to `NEW_5` and add `'hasDeployStaging'` as the 5th element:
   ```javascript
   const NEW_5 = [
     'hasBusinessThesis', 'hasMarketLandscape', 'hasServiceBlueprint', 'hasLaunchKit', 'hasDeployStaging'
   ];
   ```

4. **All NEW_4 references (lines ~95, ~111):** Replace `NEW_4` with `NEW_5` in every `for (const field of NEW_4)` loop

5. **Line ~91 (describe text):** Change `'all 4 new designCoverage field names appear in the manifest'` to `'all 5 new designCoverage field names appear in the manifest'`

6. **Line ~103 (describe text):** Change `'4 new fields appear AFTER hasProductionBible in the file'` to `'5 new fields appear AFTER hasProductionBible in the file'`

7. **Line ~127 (count assertion):** Change `coverageKeys.length, 20,` to `coverageKeys.length, 21,`

8. **Line ~128 (error message):** Change `'exactly 20 fields, found:'` to `'exactly 21 fields, found:'`

Do NOT modify EXISTING_16 array. Do NOT modify any other describe blocks.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test .planning/phases/84-foundation/tests/test-foundation.cjs</automated>
  </verify>
  <acceptance_criteria>
    - test-foundation.cjs contains string `FOUND-02: 21 designCoverage fields`
    - test-foundation.cjs contains string `const NEW_5`
    - test-foundation.cjs does NOT contain string `const NEW_4`
    - test-foundation.cjs contains string `coverageKeys.length, 21,`
    - test-foundation.cjs does NOT contain string `coverageKeys.length, 20,`
    - test-foundation.cjs contains string `'hasDeployStaging'` inside NEW_5 array
    - `node --test .planning/phases/84-foundation/tests/test-foundation.cjs` exits 0 with all tests passing
  </acceptance_criteria>
  <done>FOUND-02 test passes asserting exactly 21 designCoverage fields including hasDeployStaging</done>
</task>

<task type="auto">
  <name>Task 2: Add hasDeployStaging to 4 secondary workflow writes + update clobber audit test</name>
  <files>workflows/recommend.md, workflows/ideate.md, workflows/iterate.md, workflows/mockup.md, .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs</files>
  <read_first>
    - workflows/recommend.md (find the designCoverage JSON write near line 595 and surrounding prose)
    - workflows/ideate.md (find the designCoverage JSON write near line 694 and surrounding prose)
    - workflows/iterate.md (find the designCoverage JSON write near line 456 and surrounding prose)
    - workflows/mockup.md (find the designCoverage JSON write near line 1434 and surrounding prose)
    - .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs (current TWENTY_FIELDS constant)
    - .planning/phases/96-21-field-cascade-fix/96-RESEARCH.md (exact edit targets in Files 2-5 sections)
  </read_first>
  <action>
**For each of the 4 workflow files, make these changes:**

### recommend.md (uses per-field `{current_hasFieldName}` placeholders):
1. Near line 566: Add `| hasDeployStaging | false |` row after the `hasLaunchKit` row in the coverage table
2. Near line 591: Change `20-field JSON` to `21-field JSON`
3. Near line 595 (JSON string): Append `,"hasDeployStaging":{current_hasDeployStaging}` immediately before the closing `}'`
4. Near line 598: Change `20-field JSON object` to `21-field JSON object`
5. Near line 600: Change `complete 20-field JSON object` to `complete 21-field JSON object`
6. Update any other prose references from "20" to "21" that refer to field count (not step numbers)

### ideate.md (uses generic `{current}` placeholders):
1. Near line 687: Change `Extract all 20 flags` to `Extract all 21 flags`
2. Near line 688: Append `, hasDeployStaging` to the canonical order field list
3. Near line 690: Change `20-field JSON` to `21-field JSON`
4. Near line 694 (JSON string): Append `,"hasDeployStaging":{current}` immediately before the closing `}'`
5. Near line 697 (IMPORTANT): Update field count references from 20 to 21; append `hasDeployStaging` to canonical order list

### iterate.md (uses generic `{current}` placeholders):
1. Near line 453: Change `ALL twenty current flag values` to `ALL twenty-one current flag values`; append `hasDeployStaging` to field list
2. Near line 453: Change `preserving all other nineteen values` to `twenty values`
3. Near line 453: Change `full merged twenty-field object` to `twenty-one-field object`
4. Near line 456 (JSON string): Append `,"hasDeployStaging":{current}` immediately before the closing `}'`

### mockup.md (uses generic `{current}` placeholders):
1. Near line 1427: Change `Extract all 20 flags` to `Extract all 21 flags`
2. Near line 1428: Append `, hasDeployStaging` to field list
3. Near line 1430: Change `20-field JSON` to `21-field JSON`
4. Near line 1434 (JSON string): Append `,"hasDeployStaging":{current}` immediately before the closing `}'`
5. Near line 1437 (IMPORTANT): Append `hasDeployStaging` to canonical field order list

**CRITICAL: In all 4 workflows, hasDeployStaging MUST be `{current}` (or `{current_hasDeployStaging}` for recommend.md). NEVER set it to `true`. Only deploy.md owns the true write.**

### test-clobber-audit.cjs:
1. Find the `TWENTY_FIELDS` constant (around lines 24-31)
2. Rename to `TWENTY_ONE_FIELDS`
3. Add `'hasDeployStaging'` as the 21st element
4. Update all references from `TWENTY_FIELDS` to `TWENTY_ONE_FIELDS`
5. Update the describe/it text from "20" to "21" where it refers to field count
6. Update any assertion count from 20 to 21
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep -c 'hasDeployStaging' workflows/recommend.md && grep -c 'hasDeployStaging' workflows/ideate.md && grep -c 'hasDeployStaging' workflows/iterate.md && grep -c 'hasDeployStaging' workflows/mockup.md && node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs && node --test .planning/phases/84-foundation/tests/test-foundation.cjs && node --test .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'hasDeployStaging' workflows/recommend.md` returns at least 2 (table row + JSON blob)
    - `grep -c 'hasDeployStaging' workflows/ideate.md` returns at least 2 (field list + JSON blob)
    - `grep -c 'hasDeployStaging' workflows/iterate.md` returns at least 2 (field list + JSON blob)
    - `grep -c 'hasDeployStaging' workflows/mockup.md` returns at least 2 (field list + JSON blob)
    - workflows/recommend.md JSON blob contains `"hasDeployStaging":{current_hasDeployStaging}`
    - workflows/ideate.md JSON blob contains `"hasDeployStaging":{current}`
    - workflows/iterate.md JSON blob contains `"hasDeployStaging":{current}`
    - workflows/mockup.md JSON blob contains `"hasDeployStaging":{current}`
    - NO workflow file contains `"hasDeployStaging":true` (only deploy.md owns that)
    - `grep '20-field' workflows/recommend.md workflows/ideate.md workflows/iterate.md workflows/mockup.md` returns empty (all updated to 21)
    - test-clobber-audit.cjs contains `TWENTY_ONE_FIELDS` and `hasDeployStaging`
    - test-clobber-audit.cjs does NOT contain `TWENTY_FIELDS` (old constant name)
    - `node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs` exits 0
    - `node --test .planning/phases/84-foundation/tests/test-foundation.cjs` exits 0
    - `node --test .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` exits 0
  </acceptance_criteria>
  <done>All 4 secondary workflows write 21-field designCoverage including hasDeployStaging as pass-through. Clobber audit test updated to 21 fields. Full Nyquist suite green.</done>
</task>

</tasks>

<verification>
1. `node --test .planning/phases/84-foundation/tests/test-foundation.cjs` — all FOUND-02 subtests pass with 21-field assertions
2. `node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs` — all INTG-01 subtests pass with 21-field checks
3. `node --test .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` — INTG-07 regression suite stays green (46/46)
4. `grep -rn 'hasDeployStaging' workflows/recommend.md workflows/ideate.md workflows/iterate.md workflows/mockup.md` — each file shows at least 2 hits
5. `grep '"hasDeployStaging":true' workflows/recommend.md workflows/ideate.md workflows/iterate.md workflows/mockup.md` — returns EMPTY (only deploy.md writes true)
</verification>

<success_criteria>
- Full Nyquist suite: 235/235 PASS, 0 FAIL (FOUND-02 no longer failing)
- All 4 secondary workflows include hasDeployStaging in their 21-field designCoverage JSON write
- test-clobber-audit.cjs uses TWENTY_ONE_FIELDS constant with hasDeployStaging
- No workflow other than deploy.md sets hasDeployStaging to true
- `build --from recommend` after deploy would preserve hasDeployStaging flag (structural verification via field presence in JSON blob)
</success_criteria>

<output>
After completion, create `.planning/phases/96-21-field-cascade-fix/96-01-SUMMARY.md`
</output>
