---
phase: 89-wireframe-stage-launch-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/wireframe.md
  - references/launch-frameworks.md
  - .planning/phases/89-wireframe-stage-launch-artifacts/tests/test-wireframe-launch.cjs
autonomous: true
requirements: [LAUNCH-01, LAUNCH-04, LAUNCH-06]

must_haves:
  truths:
    - "wireframe.md detects businessMode and businessTrack in Step 2e"
    - "wireframe.md generates an LDP landing page deployable spec artifact in launch/ directory"
    - "wireframe.md writes 20-field designCoverage (not 16) to prevent clobbering business flags"
    - "LDP artifact references MKT brand tokens and GTM channel flow for copy framing"
    - "launch-frameworks.md contains the Landing Page Wireframe Spec template"
  artifacts:
    - path: "workflows/wireframe.md"
      provides: "businessMode detection, LDP generation step 4h, 20-field designCoverage"
      contains: "manifest-get-top-level businessMode"
    - path: "references/launch-frameworks.md"
      provides: "LDP artifact schema and section block template"
      contains: "Landing Page Wireframe Spec"
    - path: ".planning/phases/89-wireframe-stage-launch-artifacts/tests/test-wireframe-launch.cjs"
      provides: "Nyquist structural tests for LAUNCH-01 through LAUNCH-06"
      exports: ["node --test"]
  key_links:
    - from: "workflows/wireframe.md"
      to: "references/launch-frameworks.md"
      via: "required_reading block"
      pattern: "@references/launch-frameworks.md"
    - from: "workflows/wireframe.md"
      to: ".planning/design/launch/LDP-landing-page"
      via: "Step 4h artifact write"
      pattern: "LDP-landing-page"
    - from: "workflows/wireframe.md"
      to: "designCoverage 20-field write"
      via: "Step 7d upgrade"
      pattern: "hasBusinessThesis"
---

<objective>
Add business mode infrastructure to wireframe.md: businessMode/businessTrack detection, LDP (Landing Page) artifact generation as a deployable-spec with Next.js component mapping, 20-field designCoverage upgrade, and the LDP schema template in launch-frameworks.md. Also create the Nyquist test scaffold covering all LAUNCH-01 through LAUNCH-06 requirements.

Purpose: wireframe.md currently has ZERO businessMode detection and writes only 16-field designCoverage. This plan adds the foundation (detection + coverage upgrade) and the largest artifact (LDP) so Plan 02 can focus on STR + DPD.
Output: Modified wireframe.md with Steps 2e (detection), 4h (LDP generation), 7d (20-field coverage); updated launch-frameworks.md with LDP spec section; test scaffold.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-wireframe-stage-launch-artifacts/89-RESEARCH.md
@.planning/phases/89-wireframe-stage-launch-artifacts/89-VALIDATION.md
@references/launch-frameworks.md
@references/business-track.md
@references/business-financial-disclaimer.md
@references/business-legal-disclaimer.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nyquist test scaffold + LDP schema in launch-frameworks.md</name>
  <files>
    .planning/phases/89-wireframe-stage-launch-artifacts/tests/test-wireframe-launch.cjs,
    references/launch-frameworks.md
  </files>
  <read_first>
    .planning/phases/88-brand-system/tests/test-brand-system.cjs,
    references/launch-frameworks.md,
    workflows/wireframe.md
  </read_first>
  <action>
**Part A — Test scaffold:**

Create `.planning/phases/89-wireframe-stage-launch-artifacts/tests/test-wireframe-launch.cjs` following the exact pattern of Phase 88's test-brand-system.cjs (node:test, describe/it, fs.readFileSync from project root).

Read `workflows/wireframe.md` once and `references/launch-frameworks.md` once. Assert ALL of the following structural properties:

**LAUNCH-01 tests (4 assertions):**
1. wireframe.md contains `manifest-get-top-level businessMode` (businessMode detection present)
2. wireframe.md contains `LDP-landing-page` (LDP artifact filename pattern present)
3. wireframe.md contains `HeroSection` AND `FeaturesGrid` AND `PricingTable` AND `CTABanner` AND `SiteFooter` (5 required section component names)
4. wireframe.md's designCoverage write contains `hasBusinessThesis` (confirms 20-field upgrade, not 16)

**LAUNCH-02 tests (2 assertions):**
5. wireframe.md contains `STR-stripe-pricing` (STR artifact filename pattern)
6. wireframe.md contains the string `YOUR_PRICE_IN_CENTS` (financial placeholder pattern, not numeric unit_amount)

**LAUNCH-03 tests (2 assertions):**
7. wireframe.md contains `DPD-pitch-deck-outline` (DPD artifact filename pattern)
8. wireframe.md contains `solo_founder` AND `startup_team` AND `product_leader` in proximity to pitch deck format selection (track branching present)

**LAUNCH-04 test (1 assertion):**
9. wireframe.md contains `MKT-brand-system` OR `MKT` reference in the LDP generation section (brand token cross-reference)

**LAUNCH-05 test (1 assertion):**
10. wireframe.md contains `LCV` reference in proximity to STR generation (lean canvas revenue streams reference)

**LAUNCH-06 test (1 assertion):**
11. wireframe.md contains `launch/LDP` AND `launch/STR` AND `launch/DPD` path patterns (all three artifacts route to launch/ directory)

Total: 11 structural assertions. All tests read files once at module top; no runtime execution.

File header comment must list: requirements covered (LAUNCH-01 through LAUNCH-06), run command (`node --test .planning/phases/89-wireframe-stage-launch-artifacts/tests/test-wireframe-launch.cjs`), RED/GREEN state description.

**Part B — LDP schema in launch-frameworks.md:**

Read `references/launch-frameworks.md` (currently ~230 lines). Append a new `## Landing Page Wireframe Spec` section AFTER the existing `## Brand System` section (the last section added in Phase 88). The new section contains:

1. Section header: `## Landing Page Wireframe Spec`
2. Intro paragraph: "Landing page deployable spec format for Phase 89 wireframe.md LDP artifact. Consumed by Phase 92 deploy skill to scaffold Next.js landing page."
3. **Section Block Template** — the canonical per-section format (Component, Next.js path, Server/Client, Responsive layout, Props, Brand tokens, GTM stage, Copy register) exactly as specified in the research deep dive Pattern 2 and the Deployable Spec Section Block Template.
4. **Section Map** — the 11-section table (SiteNav, HeroSection, LogoBar, ProblemStatement, FeaturesGrid, HowItWorks, TestimonialsBlock, PricingTable, FAQAccordion, CTABanner, SiteFooter) with Component names, Next.js paths (`app/(marketing)/_components/[component-name].tsx` for section components, `app/(marketing)/layout.tsx` for SiteNav and SiteFooter), and Server/Client designation.
5. **Track Depth Table** — abbreviated version of the track-specific section depth from research (solo_founder 5 sections minimum, startup_team all 11, product_leader all 11 adapted).
6. **Consumers** list: `wireframe.md (Phase 89), deploy.md (Phase 92)`

Do NOT duplicate the Pricing Config Schema or Pitch Deck sections — those already exist in launch-frameworks.md.
  </action>
  <verify>
    <automated>node --test .planning/phases/89-wireframe-stage-launch-artifacts/tests/test-wireframe-launch.cjs 2>&1 | grep -E "fail|pass" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `test-wireframe-launch.cjs` exists with 11 `it()` blocks covering LAUNCH-01 through LAUNCH-06
    - `test-wireframe-launch.cjs` line 1 contains `'use strict'`
    - `test-wireframe-launch.cjs` contains `require('node:test')`
    - `references/launch-frameworks.md` contains `## Landing Page Wireframe Spec`
    - `references/launch-frameworks.md` contains `HeroSection` and `FeaturesGrid` and `PricingTable`
    - `references/launch-frameworks.md` contains `app/(marketing)/_components/`
    - `references/launch-frameworks.md` contains `Section Block Template` or `Section Map`
    - Tests 5-8 and 10-11 are in RED state (wireframe.md not yet modified); tests that read launch-frameworks.md pass
  </acceptance_criteria>
  <done>Test scaffold with 11 assertions exists and runs (most in RED state). launch-frameworks.md has LDP spec section with section map, template, and track depth table.</done>
</task>

<task type="auto">
  <name>Task 2: wireframe.md businessMode detection, LDP generation, 20-field coverage upgrade</name>
  <files>workflows/wireframe.md</files>
  <read_first>
    workflows/wireframe.md,
    references/launch-frameworks.md,
    references/business-track.md,
    references/business-financial-disclaimer.md,
    references/business-legal-disclaimer.md,
    .planning/phases/88-brand-system/88-01-SUMMARY.md,
    .planning/phases/87-flows-stage/87-01-SUMMARY.md
  </read_first>
  <action>
Modify `workflows/wireframe.md` with three changes. Read the FULL file first (2100 lines).

**Change 1 — required_reading block (lines 5-10):** Add four entries after the existing `@references/composition-typography.md` line:
```
@references/business-track.md
@references/business-financial-disclaimer.md
@references/business-legal-disclaimer.md
@references/launch-frameworks.md
```

**Change 2 — Step 2e businessMode detection (after line 148, after `Store as PRODUCT_NAME, PRODUCT_TYPE.`):**

Insert the following business mode detection block. This is an ADDITION — it goes after the PRODUCT_TYPE storage line and before the experience product type comment:

```markdown
<!-- Business product type — Phase 89 architecture: LDP landing page spec, STR Stripe pricing config, DPD pitch deck outline. -->
<!-- Depends on: businessMode === true from design-manifest.json -->

Read businessMode and businessTrack from manifest:
```bash
BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
BT=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessTrack 2>/dev/null)
```

Store as `$BM` (string "true"/"false") and `$BT` (string "solo_founder"/"startup_team"/"product_leader"). These are consumed by Steps 4h, 4i, 4j, and 7e-launch.
```

**Change 3 — Steps 4h (LDP generation) — insert AFTER Step 4-EXP block (after the experience wireframe generation section, before Step 4/7 "Generate wireframe HTML per screen"):**

Note: Steps 4h/4i/4j are INDEPENDENT from Step 4-EXP. A business:experience product runs BOTH 4-EXP AND 4h/4i/4j. Use `IF $BM == "true"` gate, NOT `ELSE IF`.

Insert Step 4h — LDP landing page spec generation:

```markdown
#### Step 4h: LDP landing page spec generation (business mode only)

IF `$BM != "true"`: skip Step 4h entirely.

**Read upstream artifacts (soft dependencies — degrade gracefully if absent):**

1. **MKT brand system artifact (Phase 88):**
   ```bash
   MKT_FILE=$(ls .planning/design/strategy/MKT-brand-system-v*.md 2>/dev/null | tail -1)
   ```
   - If present: extract Positioning Statement, Tone of Voice spectrum, Brand Voice Examples (CTA prefer/avoid).
   - If absent: emit warning: `"Warning: MKT brand system artifact not found. LDP hero content derived from BRF Domain Strategy section. Run /pde:system in business mode for richer brand-aligned copy framing."` Set `MKT_AVAILABLE=false`.

2. **GTM channel flow artifact (Phase 87):**
   ```bash
   GTM_FILE=$(ls .planning/design/strategy/GTM-channel-flow-v*.md 2>/dev/null | tail -1)
   ```
   - If present: extract ACQ subgraph highest-priority channel node and channel framing approach (direct response / educational / social proof).
   - If absent: emit warning. Default to "content marketing" framing — educational hero headline direction.

3. **LCV lean canvas artifact (Phase 85):**
   ```bash
   LCV_FILE=$(ls .planning/design/strategy/LCV-lean-canvas-v*.md 2>/dev/null | tail -1)
   ```
   - If present: extract box 2 (Solution — features), box 3 (UVP), box 5 (Customer Segments), box 9 (Revenue Streams).
   - If absent: emit warning. Derive features and audience from BRF product description.

4. **SYS-brand-tokens.json (Phase 88):**
   ```bash
   BRAND_TOKENS_FILE=$(ls .planning/design/visual/SYS-brand-tokens.json 2>/dev/null | tail -1)
   ```
   - If present: reference `brand-marketing.campaign-palette-variants.*` and `brand-marketing.brand-voice.*` token paths in LDP spec.
   - If absent: omit brand token references from LDP spec. Add note: `[Run /pde:system in business mode for brand token integration]`.

**Generate LDP artifact:**

Write `.planning/design/launch/LDP-landing-page-v${N}.md` where `${N}` is the wireframe version number from Step 2d.

The LDP artifact follows the Landing Page Wireframe Spec template in `references/launch-frameworks.md`:

- **Frontmatter:** artifact: LDP-landing-page, version: v{N}, skill: /pde:wireframe (LDP), businessTrack: {$BT}, audience: external/public (or internal/enterprise for product_leader), dependsOn: BRF, MKT, GTM, LCV, STR
- **Section Map table:** Track-depth-aware — solo_founder gets 5 core sections (Hero, Features, Pricing, CTA, Footer), startup_team gets all 11 sections, product_leader gets all 11 with adapted vocabulary per `references/business-track.md`
- **Per-section spec blocks:** Each section uses the Section Block Template from launch-frameworks.md: Component name, Next.js path (`app/(marketing)/_components/[component-name].tsx`), Server/Client designation, Responsive layout (mobile/tablet/desktop), Props with content slot values derived from upstream artifacts, Brand token references (`brand-marketing.campaign-palette-variants.*`, `brand-marketing.brand-voice.*`), GTM stage mapping, Copy register instruction
- **Hero section:** Headline derived from MKT positioning statement (primary benefit clause, NOT full Geoffrey Moore sentence). Subheadline from GTM ACQ highest-priority channel message. CTA text from MKT Brand Voice CTA example (avoid commodity phrasing). If MKT absent: derive from BRF Domain Strategy section.
- **Features section:** Features array derived from BTH solution block top 3-4 features (solo_founder: 3, startup_team: 6, product_leader: 4 with ROI callouts). Content sourced from LCV box 2 (Solution).
- **Pricing section:** Plan names reference STR artifact nicknames. Price display values: `[YOUR_PRICE]` — never populate with dollar amounts. Include `[VERIFY FINANCIAL ASSUMPTIONS]` tag.
- **Footer section:** Legal checklist items from `references/business-legal-disclaimer.md` — Terms of Service, Privacy Policy, Cookie Consent with `[CONSULT LEGAL COUNSEL]` tags.
- **Upstream References block:** List all 4 upstream artifact paths with description of what was consumed from each.

Display: `Step 4/7 (4h): Generated LDP landing page spec — {section_count} sections, {businessTrack} track.`

LDP_WRITTEN=true
```

**Change 4 — Step 7d designCoverage upgrade (line 2015-2025):**

Replace the 16-field designCoverage description and ALL THREE write commands (lines 2015-2025) with a 20-field version. The narrative text at line 2015 must read:

```
Parse the JSON output. Extract ALL twenty current flag values: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit. Default any absent field to `false`. Merge `hasWireframes: true` while preserving all other nineteen values. [rest of existing conditional logic for hasStitchWireframes and hasPrintCollateral unchanged]. Then write the full merged twenty-field object:
```

The three bash write commands become (preserving the same conditional structure — base, stitch variant, print variant):

Base write:
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":{current},"hasWireframes":true,"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":{current},"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{current},"hasLaunchKit":{current}}'
```

Stitch variant (hasStitchWireframes:true):
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":{current},"hasWireframes":true,"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":true,"hasPrintCollateral":{current},"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{current},"hasLaunchKit":{current}}'
```

Print variant (hasPrintCollateral:true):
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":{current},"hasWireframes":true,"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":true,"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{current},"hasLaunchKit":{current}}'
```

**CRITICAL:** Do NOT set `hasLaunchKit: true` in wireframe.md. This flag is owned by Phase 91 handoff.md. wireframe.md passes through the current value via `{current}`.

**Important placement notes:**
- Step 4h goes AFTER Step 4-EXP (experience block) and BEFORE the main `### Step 4/7: Generate wireframe HTML per screen` section
- The experience block and business block are INDEPENDENT conditional blocks (not ELSE IF). A business:experience product runs both.
- Step 7d changes are an IN-PLACE replacement of lines 2015-2025 (the narrative + 3 bash commands).
  </action>
  <verify>
    <automated>node --test .planning/phases/89-wireframe-stage-launch-artifacts/tests/test-wireframe-launch.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - wireframe.md contains `manifest-get-top-level businessMode` (grep returns match)
    - wireframe.md contains `manifest-get-top-level businessTrack` (grep returns match)
    - wireframe.md contains `LDP-landing-page` (grep returns match)
    - wireframe.md contains `HeroSection` (grep returns match)
    - wireframe.md contains `FeaturesGrid` (grep returns match)
    - wireframe.md contains `PricingTable` (grep returns match)
    - wireframe.md contains `CTABanner` (grep returns match)
    - wireframe.md contains `SiteFooter` (grep returns match)
    - wireframe.md contains `MKT-brand-system` or `MKT` in proximity to LDP generation
    - wireframe.md contains `hasBusinessThesis` in a designCoverage write command
    - wireframe.md contains `hasMarketLandscape` in a designCoverage write command
    - wireframe.md contains `hasServiceBlueprint` in a designCoverage write command
    - wireframe.md contains `hasLaunchKit` in a designCoverage write command
    - wireframe.md required_reading contains `@references/launch-frameworks.md`
    - wireframe.md required_reading contains `@references/business-track.md`
    - Nyquist tests for LAUNCH-01 test 1 (businessMode detection) passes
    - Nyquist tests for LAUNCH-01 test 4 (20-field coverage) passes
    - Nyquist test for LAUNCH-06 (launch/ directory) passes for LDP path
  </acceptance_criteria>
  <done>wireframe.md has businessMode/businessTrack detection in Step 2e, LDP landing page spec generation in Step 4h with MKT/GTM/LCV/brand-token cross-references, 20-field designCoverage write in Step 7d, and 4 new required_reading entries. All LAUNCH-01, LAUNCH-04, LAUNCH-06 tests for LDP pass green.</done>
</task>

</tasks>

<verification>
After both tasks complete:
```bash
node --test .planning/phases/89-wireframe-stage-launch-artifacts/tests/test-wireframe-launch.cjs
```
Tests 1-4 (LAUNCH-01) should be GREEN. Tests 5-8 (LAUNCH-02, LAUNCH-03) remain RED until Plan 02. Test 9 (LAUNCH-04) GREEN. Tests 10-11 partial (LAUNCH-05 RED, LAUNCH-06 partially GREEN).
</verification>

<success_criteria>
- wireframe.md has businessMode detection (0 matches before -> matches after)
- wireframe.md writes 20-field designCoverage (16 fields before -> 20 after)
- wireframe.md generates LDP artifact to launch/ directory
- LDP generation references MKT, GTM, LCV artifacts with soft dependency fallbacks
- launch-frameworks.md has Landing Page Wireframe Spec section
- Nyquist test scaffold exists with 11 assertions
- All LAUNCH-01, LAUNCH-04, LAUNCH-06 assertions pass
</success_criteria>

<output>
After completion, create `.planning/phases/89-wireframe-stage-launch-artifacts/89-01-SUMMARY.md`
</output>
