---
phase: 108-playwright-mcp-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - references/mcp-integration.md
autonomous: false
requirements: [PLAY-04, PLAY-06, PLAY-07]

must_haves:
  truths:
    - "mcp-integration.md Playwright section documents --headless and --allow-unrestricted-file-access flags"
    - "mcp-integration.md install command includes full flag set"
    - "mcp-integration.md Playwright probe uses browser_snapshot (not browser_navigate)"
    - "Live tool name verification confirms mcp__playwright__* prefix is correct (or corrects it)"
  artifacts:
    - path: "references/mcp-integration.md"
      provides: "Updated Playwright MCP documentation with flags and verification status"
      contains: "--allow-unrestricted-file-access"
  key_links:
    - from: "references/mcp-integration.md"
      to: "bin/lib/mcp-bridge.cjs"
      via: "Install command and tool name documentation must match APPROVED_SERVERS/TOOL_MAP"
      pattern: "playwright.*headless"
---

<objective>
Update mcp-integration.md with complete Playwright documentation (install flags, probe correction, file:// access notes) and perform live MCP-08 gate verification of tool name prefix.

Purpose: Ensures all downstream phases (109-117) have accurate documentation, and confirms the mcp__playwright__* tool name prefix is correct before workflows depend on it.
Output: Updated mcp-integration.md, live verification result recorded.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-playwright-mcp-infrastructure/108-RESEARCH.md
@.planning/phases/108-playwright-mcp-infrastructure/108-01-SUMMARY.md

<interfaces>
<!-- Current Playwright section in mcp-integration.md (lines 282-347) -->

From references/mcp-integration.md (current Playwright section):
```markdown
### Playwright MCP

**Purpose:** Browser automation for screenshot capture, responsive validation, link testing, interaction testing.
**Package:** `@playwright/mcp@latest`
**Transport:** stdio (via npx)
**Install:** `claude mcp add playwright -- npx @playwright/mcp@latest`
**Stability:** STABLE

#### Probe

Attempt: browser_navigate to about:blank
Result:
  - Navigation succeeds: PLAYWRIGHT_AVAILABLE = true
  - Tool not found or browser launch fails: PLAYWRIGHT_AVAILABLE = false

Timeout: 30 seconds
Retry: 0
```

From references/mcp-integration.md (Installation Commands section, line 744-745):
```
# Playwright:
claude mcp add playwright -- npx @playwright/mcp@latest
```

From references/mcp-integration.md (Stability table, line 765):
```
| Playwright MCP | STABLE | stdio | 2026-03-11 | Official Microsoft, actively maintained |
```

From references/mcp-integration.md (Troubleshooting, lines 691-697):
```
### Playwright MCP

| Tool not found | MCP not installed | `claude mcp add playwright -- npx @playwright/mcp@latest` |
| Browser launch fails | Chromium not downloaded | Run `npx playwright install chromium` |
| Screenshot blank | Page not fully loaded | Playwright waits for load event; may need longer for complex pages |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mcp-integration.md Playwright documentation with full flags and corrected probe</name>
  <files>references/mcp-integration.md</files>
  <read_first>
    - references/mcp-integration.md (full file -- multiple sections need updates)
    - bin/lib/mcp-bridge.cjs (verify install command and probe tool match documentation)
    - .planning/phases/108-playwright-mcp-infrastructure/108-RESEARCH.md (flag details and pitfalls)
  </read_first>
  <action>
    Update 5 sections of `references/mcp-integration.md`:

    **Section 1: Playwright MCP block (around line 282)**

    Change the Install line from:
    ```
    **Install:** `claude mcp add playwright -- npx @playwright/mcp@latest`
    ```
    To:
    ```
    **Install:** `claude mcp add playwright -- npx @playwright/mcp@latest --headless --allow-unrestricted-file-access`
    ```

    Change the Probe block from:
    ```
    Attempt: browser_navigate to about:blank
    ```
    To:
    ```
    Attempt: mcp__playwright__browser_snapshot
    ```
    (browser_snapshot is the probe tool, not browser_navigate -- browser_navigate requires a URL arg)

    Add a new subsection after the Probe block and before Enhancement Recipes:
    ```markdown
    #### Flags

    | Flag | Purpose | Required By |
    |------|---------|-------------|
    | `--headless` | Prevents visible browser windows during autonomous execution | All phases (108+) |
    | `--allow-unrestricted-file-access` | Enables file:// URL navigation for local wireframe/mockup HTML | Phases 109, 110, 111 |

    **Note:** The `--` separator before `npx` is critical. Without it, `--headless` and `--allow-unrestricted-file-access` are parsed by `claude mcp add` instead of being passed to the Playwright server process.

    **file:// URL encoding:** The project directory contains a space ("Platform Development Engine"). When passing file:// URLs to browser_navigate, encode the space as `%20`: `file:///Users/.../Platform%20Development%20Engine/.planning/...`

    **Version fallback:** If `@playwright/mcp@latest` produces "No such tool available" errors, pin to `@playwright/mcp@0.0.41`.
    ```

    **Section 2: Installation Commands (around line 744-745)**

    Change:
    ```
    # Playwright:
    claude mcp add playwright -- npx @playwright/mcp@latest
    ```
    To:
    ```
    # Playwright (headless + file:// access):
    claude mcp add playwright -- npx @playwright/mcp@latest --headless --allow-unrestricted-file-access
    ```

    **Section 3: Stability table (around line 765)**

    Update the Playwright row's "Last Verified" column from `2026-03-11` to `2026-03-23` and add note about Phase 108:
    ```
    | Playwright MCP | STABLE | stdio | 2026-03-23 | Official Microsoft; Phase 108 registered as 7th APPROVED_SERVER |
    ```

    **Section 4: Troubleshooting Playwright section (around line 691)**

    Add two new rows to the troubleshooting table:
    ```
    | file:// navigation fails | --allow-unrestricted-file-access missing | Add flag to install: `claude mcp add playwright -- npx @playwright/mcp@latest --headless --allow-unrestricted-file-access` |
    | "No such tool available" | Version incompatibility | Pin version: `npx @playwright/mcp@0.0.41` instead of `@latest` |
    ```

    **Section 5: Scope line (line 8)**

    The scope line already says "7 MCP servers (2 universal + 5 targeted)" which is correct. Verify it matches -- if it says a different number, update to 7.
  </action>
  <verify>
    <automated>grep -c "allow-unrestricted-file-access" references/mcp-integration.md | xargs -I{} test {} -ge 3 && grep "browser_snapshot" references/mcp-integration.md | head -1 && grep "headless" references/mcp-integration.md | grep -c "Install\|claude mcp add" | xargs -I{} test {} -ge 2 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - references/mcp-integration.md Install line contains `--headless --allow-unrestricted-file-access`
    - references/mcp-integration.md Probe section contains `browser_snapshot` (not `browser_navigate`)
    - references/mcp-integration.md contains `#### Flags` subsection
    - references/mcp-integration.md Flags table contains `--headless` row and `--allow-unrestricted-file-access` row
    - references/mcp-integration.md contains `%20` URL encoding note
    - references/mcp-integration.md contains `0.0.41` version fallback note
    - references/mcp-integration.md Installation Commands section contains `--headless --allow-unrestricted-file-access`
    - references/mcp-integration.md Stability table Playwright row contains `2026-03-23`
    - references/mcp-integration.md Troubleshooting contains `file:// navigation fails` row
    - references/mcp-integration.md scope line says `7 MCP servers`
  </acceptance_criteria>
  <done>mcp-integration.md fully documents Playwright with --headless, --allow-unrestricted-file-access, corrected probe tool, file:// URL encoding note, version fallback, and updated troubleshooting.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: MCP-08 Live Verification Gate -- Confirm Playwright tool name prefix</name>
  <files>bin/lib/mcp-bridge.cjs</files>
  <action>
    This is a human verification checkpoint. The executor presents the verification steps below
    and waits for the user to confirm. If the user reports "wrong-prefix", the executor must
    update all 10 TOOL_MAP entries in bin/lib/mcp-bridge.cjs and the probeTool in APPROVED_SERVERS
    to use the correct prefix, then re-run tests/phase-108/mcp-bridge-playwright.test.mjs.
    If the user reports "skip", no changes are needed -- TOOL_MAP_VERIFY_REQUIRED markers remain.
    If the user reports "verified", no changes needed -- the prefix is confirmed correct.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification of MCP-08 gate"</automated>
  </verify>
  <acceptance_criteria>
    - User has attempted to register Playwright MCP via `claude mcp add playwright -- npx @playwright/mcp@latest --headless --allow-unrestricted-file-access`
    - User reports one of: "verified" (prefix correct), "wrong-prefix: {actual}" (prefix needs correction), or "skip" (deferred)
    - If "wrong-prefix": all 10 TOOL_MAP entries and probeTool updated to correct prefix, tests re-run and passing
    - If "verified" or "skip": no code changes needed for this task
  </acceptance_criteria>
  <done>MCP-08 live verification gate completed. Tool name prefix either confirmed correct, corrected, or deferred with VERIFY_REQUIRED markers preserved.</done>
</task>

</tasks>

<verification>
- `grep -c "allow-unrestricted-file-access" references/mcp-integration.md` returns >= 3
- `grep "browser_snapshot" references/mcp-integration.md` appears in Probe section
- `grep "headless" references/mcp-integration.md | grep -c "Install\|claude mcp"` returns >= 2
- MCP-08 gate result recorded (verified / wrong-prefix / skip)
</verification>

<success_criteria>
- mcp-integration.md contains complete Playwright documentation with all flags
- Probe section corrected from browser_navigate to browser_snapshot
- file:// URL encoding note present
- Version fallback (0.0.41) documented
- MCP-08 live verification attempted (result recorded regardless of outcome)
</success_criteria>

<output>
After completion, create `.planning/phases/108-playwright-mcp-infrastructure/108-02-SUMMARY.md`
</output>
