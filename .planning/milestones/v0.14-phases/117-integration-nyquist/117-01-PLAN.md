---
phase: 117-integration-nyquist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-117/integration-nyquist.test.mjs
autonomous: true
requirements: [INTG-01]

must_haves:
  truths:
    - "All 76 v0.14 requirements have explicit Nyquist structural test coverage"
    - "The 11 gap requirements (PLAY-04, EXP-01 through EXP-09, INTG-01) each have a dedicated describe block"
    - "Phase 117 test file passes with zero failures"
  artifacts:
    - path: "tests/phase-117/integration-nyquist.test.mjs"
      provides: "Integration tests covering all 76 v0.14 requirements"
      contains: "PLAY-04"
  key_links:
    - from: "tests/phase-117/integration-nyquist.test.mjs"
      to: "bin/lib/mcp-bridge.cjs"
      via: "createRequire for TOOL_MAP assertions"
      pattern: "require.*mcp-bridge"
    - from: "tests/phase-117/integration-nyquist.test.mjs"
      to: "bin/lib/experiment-schema.cjs"
      via: "createRequire for parseExperimentFile"
      pattern: "require.*experiment-schema"
    - from: "tests/phase-117/integration-nyquist.test.mjs"
      to: "references/experiments/*.md"
      via: "fs.existsSync and readFileSync"
      pattern: "references/experiments"
---

<objective>
Write the Phase 117 integration Nyquist test file that fills all 11 coverage gaps identified in research, ensuring every v0.14 requirement has an explicit named test block.

Purpose: INTG-01 requires structural regression tests for all 76 v0.14 requirements. Research identified 65/76 already covered across phase-108 through phase-116 tests. This plan fills the 11 gaps: PLAY-04, EXP-01 through EXP-09, and the INTG-01 meta-test.

Output: `tests/phase-117/integration-nyquist.test.mjs` with all gaps closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-integration-nyquist/117-RESEARCH.md

<interfaces>
<!-- Key modules the executor needs to import/require -->

From bin/lib/mcp-bridge.cjs (CJS):
```javascript
// Exported: TOOL_MAP, APPROVED_SERVERS, AUTH_INSTRUCTIONS, probe(), call()
const bridge = require('../../bin/lib/mcp-bridge.cjs');
const { TOOL_MAP, APPROVED_SERVERS } = bridge;
// TOOL_MAP is an object: { 'playwright:navigate': 'mcp__playwright__browser_navigate', ... }
// Playwright entries all start with 'playwright:' key and 'mcp__playwright__' value
```

From bin/lib/experiment-schema.cjs (CJS):
```javascript
// Exported: parseExperimentFile(filePath) => { valid: boolean, errors?: string[] }
const { parseExperimentFile } = require('../../bin/lib/experiment-schema.cjs');
```

Experiment template files (all in references/experiments/):
```
wireframe.md, mockup.md, system.md, flows.md, critique.md,
hig.md, iterate.md, handoff.md, brief.md,
recommend.md, competitive.md, opportunity.md, ideate.md, deploy.md
```

Test file pattern (from tests/phase-116/pressure-test-visual.test.mjs):
```javascript
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import fs from 'node:fs';
import { fileURLToPath } from 'node:url';
import { createRequire } from 'node:module';

const ROOT = fileURLToPath(new URL('../../', import.meta.url));
const require = createRequire(import.meta.url);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration-nyquist.test.mjs covering all 11 requirement gaps</name>
  <files>tests/phase-117/integration-nyquist.test.mjs</files>

  <read_first>
    - .planning/phases/117-integration-nyquist/117-RESEARCH.md (gap analysis table, code examples)
    - tests/phase-112/experiment-templates.test.mjs (EXP-11/12 pattern for parseExperimentFile)
    - tests/phase-108/mcp-bridge-playwright.test.mjs (PLAY-04 pattern for TOOL_MAP assertions)
    - tests/phase-116/pressure-test-visual.test.mjs (recent test file structure convention)
  </read_first>

  <action>
Create `tests/phase-117/integration-nyquist.test.mjs` with the following structure:

**File header comment:**
```
/**
 * integration-nyquist.test.mjs — Phase 117 Integration & Nyquist
 *
 * Fills coverage gaps for v0.14 requirements not explicitly tested in phases 108-116.
 * Covers: PLAY-04, EXP-01 through EXP-09, INTG-01
 *
 * Run: node --test tests/phase-117/integration-nyquist.test.mjs
 */
```

**Imports (ESM with createRequire):**
- `import { describe, it } from 'node:test'`
- `import assert from 'node:assert/strict'`
- `import fs from 'node:fs'`
- `import path from 'node:path'`
- `import { fileURLToPath } from 'node:url'`
- `import { createRequire } from 'node:module'`
- `const ROOT = fileURLToPath(new URL('../../', import.meta.url))`
- `const require = createRequire(import.meta.url)`
- `const { TOOL_MAP } = require('../../bin/lib/mcp-bridge.cjs')`
- `const { parseExperimentFile } = require('../../bin/lib/experiment-schema.cjs')`

**Section 1 — PLAY-04 (1 describe block):**
```javascript
describe('PLAY-04: Live tool name verification confirms mcp__playwright__* prefix', () => {
  const playwrightEntries = Object.entries(TOOL_MAP)
    .filter(([k]) => k.startsWith('playwright:'));

  it('at least 10 playwright:* entries exist in TOOL_MAP', () => {
    assert.ok(playwrightEntries.length >= 10,
      `Expected >= 10 playwright entries, got ${playwrightEntries.length}`);
  });

  it('every playwright:* TOOL_MAP entry maps to mcp__playwright__* target', () => {
    for (const [key, value] of playwrightEntries) {
      assert.ok(
        value.startsWith('mcp__playwright__'),
        `TOOL_MAP['${key}'] must map to mcp__playwright__* prefix, got: ${value}`
      );
    }
  });
});
```

**Section 2 — EXP-01 through EXP-09 (9 describe blocks):**
For each of these 9 skill-to-template mappings, create a describe block:

| Req ID | Template File | Skill Name | Mutable file substring |
|--------|--------------|------------|----------------------|
| EXP-01 | wireframe.md | wireframe | wireframe.md |
| EXP-02 | mockup.md | mockup | mockup.md |
| EXP-03 | critique.md | critique | critique.md |
| EXP-04 | system.md | system | system.md |
| EXP-05 | brief.md | brief | brief.md |
| EXP-06 | flows.md | flows | flows.md |
| EXP-07 | iterate.md | iterate | iterate.md |
| EXP-08 | hig.md | hig | hig.md |
| EXP-09 | handoff.md | handoff | handoff.md |

Each describe block follows this pattern (use the exact code from the research Per-EXP Requirement Test Pattern):
```javascript
describe('EXP-NN: SKILLNAME skill optimization experiment template', () => {
  const templatePath = path.join(ROOT, 'references', 'experiments', 'SKILLNAME.md');

  it('references/experiments/SKILLNAME.md exists', () => {
    assert.ok(fs.existsSync(templatePath), 'SKILLNAME.md experiment template must exist');
  });

  it('SKILLNAME.md template is schema-valid (EXP-11 contract)', () => {
    const result = parseExperimentFile(templatePath);
    assert.equal(result.valid, true,
      `SKILLNAME.md must be valid: ${JSON.stringify(result.errors || [])}`);
  });

  it('SKILLNAME.md template targets SKILLNAME.md as a mutable file', () => {
    const content = fs.readFileSync(templatePath, 'utf-8');
    assert.ok(content.includes('SKILLNAME.md'),
      'SKILLNAME experiment must reference SKILLNAME.md');
  });
});
```

**Section 3 — INTG-01 meta-test (1 describe block):**
```javascript
describe('INTG-01: Nyquist structural tests exist for all 76 v0.14 requirements', () => {
  const V014_TEST_FILES = [
    'tests/phase-108/mcp-bridge-playwright.test.mjs',
    'tests/phase-109/wireframe-mockup-screenshots.test.mjs',
    'tests/phase-110/critique-a11y-aom.test.mjs',
    'tests/phase-110/deploy-smoke-test.test.mjs',
    'tests/phase-111/a11y-metric.test.mjs',
    'tests/phase-111/contrast-metric.test.mjs',
    'tests/phase-111/dom-metric.test.mjs',
    'tests/phase-111/mermaid-metric.test.mjs',
    'tests/phase-111/responsive-metric.test.mjs',
    'tests/phase-112/experiment-templates.test.mjs',
    'tests/phase-113/pipeline-iterate-experiments.test.mjs',
    'tests/phase-114/visual-regression.test.mjs',
    'tests/phase-115/multi-candidate.test.mjs',
    'tests/phase-116/brief-reference.test.mjs',
    'tests/phase-116/ideation-visual.test.mjs',
    'tests/phase-116/meta-optimization.test.mjs',
    'tests/phase-116/pressure-test-visual.test.mjs',
    'tests/phase-117/integration-nyquist.test.mjs',
  ];

  it('all 18 v0.14 test files exist', () => {
    for (const f of V014_TEST_FILES) {
      const fullPath = path.join(ROOT, f);
      assert.ok(fs.existsSync(fullPath), `${f} must exist`);
    }
  });

  it('18 test files cover all 76 v0.14 requirements', () => {
    // Meta-assertion: if any gap requirement is missing a describe block,
    // this file's own tests above would fail.
    // This test verifies the file count matches the expected total.
    assert.equal(V014_TEST_FILES.length, 18,
      'Expected 18 v0.14 test files (17 from phases 108-116 + 1 from phase 117)');
  });
});
```

**Total tests:** approximately 31 assertions (2 for PLAY-04, 27 for EXP-01..09 at 3 each, 2 for INTG-01).

Use `it()` (not `test()`) to match the Phase 112-116 convention as noted in research.
  </action>

  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-117/integration-nyquist.test.mjs</automated>
  </verify>

  <acceptance_criteria>
    - File `tests/phase-117/integration-nyquist.test.mjs` exists
    - grep "PLAY-04" tests/phase-117/integration-nyquist.test.mjs returns matches
    - grep "EXP-01" tests/phase-117/integration-nyquist.test.mjs returns matches
    - grep "EXP-09" tests/phase-117/integration-nyquist.test.mjs returns matches
    - grep "INTG-01" tests/phase-117/integration-nyquist.test.mjs returns matches
    - `node --test tests/phase-117/integration-nyquist.test.mjs` exits 0 with all tests passing
  </acceptance_criteria>

  <done>
    - tests/phase-117/integration-nyquist.test.mjs exists with describe blocks for PLAY-04, EXP-01 through EXP-09, and INTG-01
    - All ~31 assertions pass
    - Every v0.14 requirement now has an explicit named test block across the 18 test files
  </done>
</task>

</tasks>

<verification>
```bash
# Phase 117 test passes
node --test tests/phase-117/integration-nyquist.test.mjs

# All 18 v0.14 test files still pass together
node --test \
  tests/phase-108/mcp-bridge-playwright.test.mjs \
  tests/phase-109/wireframe-mockup-screenshots.test.mjs \
  tests/phase-110/critique-a11y-aom.test.mjs \
  tests/phase-110/deploy-smoke-test.test.mjs \
  tests/phase-111/a11y-metric.test.mjs \
  tests/phase-111/contrast-metric.test.mjs \
  tests/phase-111/dom-metric.test.mjs \
  tests/phase-111/mermaid-metric.test.mjs \
  tests/phase-111/responsive-metric.test.mjs \
  tests/phase-112/experiment-templates.test.mjs \
  tests/phase-113/pipeline-iterate-experiments.test.mjs \
  tests/phase-114/visual-regression.test.mjs \
  tests/phase-115/multi-candidate.test.mjs \
  tests/phase-116/brief-reference.test.mjs \
  tests/phase-116/ideation-visual.test.mjs \
  tests/phase-116/meta-optimization.test.mjs \
  tests/phase-116/pressure-test-visual.test.mjs \
  tests/phase-117/integration-nyquist.test.mjs
```
</verification>

<success_criteria>
- tests/phase-117/integration-nyquist.test.mjs exists and passes
- All 11 gap requirements (PLAY-04, EXP-01..09, INTG-01) have named describe blocks
- All v0.14 test files (18 total) pass together
</success_criteria>

<output>
After completion, create `.planning/phases/117-integration-nyquist/117-01-SUMMARY.md`
</output>
