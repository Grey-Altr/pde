---
phase: 113-cross-skill-pipeline-iterate-effectiveness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/pipeline-brief-wireframe-metric.cjs
  - references/experiments/pipeline-brief-to-wireframe.md
  - references/experiments/pipeline-upstream-isolation.md
  - tests/phase-113/pipeline-iterate-experiments.test.mjs
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04

must_haves:
  truths:
    - "Pipeline experiment template exists that measures upstream brief.md prose change impact on downstream wireframe visual quality"
    - "Separate upstream isolation templates exist (one per upstream skill: brief, system) enabling attribution of which skill drove improvement"
    - "All pipeline templates conform to experiment-schema.cjs contract (metric, direction, verify, mutable_files)"
    - "All pipeline template verify commands use bin/pipeline-brief-wireframe-metric.cjs which chains skill invocations (PIPE-04)"
    - "pipeline-brief-wireframe-metric.cjs internally chains dom-metric.cjs invocation on fixture (multi-stage wrapper satisfying PIPE-04)"
  artifacts:
    - path: "bin/pipeline-brief-wireframe-metric.cjs"
      provides: "PIPE-04 multi-stage pipeline metric wrapper"
      contains: "dom-metric.cjs"
    - path: "references/experiments/pipeline-brief-to-wireframe.md"
      provides: "PIPE-01/02 pipeline experiment template"
      contains: "slug: pipeline-brief-wireframe"
    - path: "references/experiments/pipeline-upstream-isolation.md"
      provides: "PIPE-03 upstream isolation template"
      contains: "slug: pipeline-upstream"
    - path: "tests/phase-113/pipeline-iterate-experiments.test.mjs"
      provides: "Nyquist test coverage for PIPE-01 through PIPE-04"
      contains: "PIPE-01"
  key_links:
    - from: "references/experiments/pipeline-brief-to-wireframe.md"
      to: "bin/pipeline-brief-wireframe-metric.cjs"
      via: "verify field"
      pattern: "node bin/pipeline-brief-wireframe-metric.cjs"
    - from: "references/experiments/pipeline-upstream-isolation.md"
      to: "bin/pipeline-brief-wireframe-metric.cjs"
      via: "verify field"
      pattern: "node bin/pipeline-brief-wireframe-metric.cjs"
    - from: "bin/pipeline-brief-wireframe-metric.cjs"
      to: "bin/dom-metric.cjs"
      via: "spawnSync child process call"
      pattern: "dom-metric.cjs"
    - from: "tests/phase-113/pipeline-iterate-experiments.test.mjs"
      to: "bin/lib/experiment-schema.cjs"
      via: "parseExperimentFile import"
      pattern: "parseExperimentFile"
---

<objective>
Create cross-skill pipeline experiment templates that measure how upstream prose changes (e.g., brief.md mutations) impact downstream visual quality (wireframe DOM structure score). Include a multi-stage pipeline metric wrapper (PIPE-04) and upstream isolation templates for attributing which skill drove the largest improvement.

Purpose: Enable AutoResearch to optimize upstream design skills (brief, system) based on their downstream visual impact, not just local text quality.
Output: 1 pipeline metric wrapper in bin/, 2 experiment templates in references/experiments/, + Nyquist test file covering PIPE-01 through PIPE-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-cross-skill-pipeline-iterate-effectiveness/113-RESEARCH.md

@references/experiments/brief.md
@references/experiments/iterate.md
@tests/phase-112/experiment-templates.test.mjs
@bin/lib/experiment-schema.cjs
@bin/dom-metric.cjs

<interfaces>
From bin/lib/experiment-schema.cjs:
- parseExperimentFile(filePath) returns { valid, errors, metric, direction, verify, mutable_files, immutable_files, iteration_budget, time_budget_minutes, slug }
- REQUIRED_FIELDS: metric, direction, verify, mutable_files
- VALID_DIRECTIONS: max, min

From tests/phase-112/experiment-templates.test.mjs (assertions Phase 113 templates must also satisfy):
- result.verify.startsWith('node bin/')
- result.direction === 'max'
- result.mutable_files entries start with 'workflows/'
- mutable_files do not target protected prefixes: bin/, tests/, references/, .planning/, agents/, .claude/

Existing template pattern (references/experiments/brief.md):
  slug: brief-downstream
  metric: dom_structure_score
  direction: max
  verify: node bin/dom-metric.cjs references/experiments/fixtures/good-wireframe.html
  mutable_files: [workflows/brief.md]
  immutable_files: []
  iteration_budget: 30
  time_budget_minutes: 60

From bin/dom-metric.cjs (the metric this wrapper calls):
- Usage: node bin/dom-metric.cjs path-to-html-file
- Contract: exit 0 always, last line stdout = numeric score 0-100
- Degrades to 0 when Playwright unavailable or no file arg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pipeline-brief-wireframe-metric.cjs wrapper (PIPE-04)</name>
  <files>
    bin/pipeline-brief-wireframe-metric.cjs
  </files>
  <read_first>
    bin/dom-metric.cjs
    bin/a11y-metric.cjs
    bin/lib/experiment-schema.cjs
  </read_first>
  <action>
Create bin/pipeline-brief-wireframe-metric.cjs as a CJS Node.js script that wraps a multi-stage pipeline measurement. This satisfies PIPE-04's "multi-stage verify commands chaining skill invocations" requirement by providing a single entry point that internally orchestrates multiple stages.

**File structure (follow dom-metric.cjs pattern exactly):**

Line 1: #!/usr/bin/env node
Line 2: 'use strict';

JSDoc comment block:
- pipeline-brief-wireframe-metric.cjs -- Multi-stage pipeline metric.
- Chains skill invocations to measure downstream visual quality from upstream prose.
- PIPE-04: Multi-stage verify wrapper that chains: upstream skill measurement -> downstream DOM metric.
- Phase 113 mode: fixture-based (upstream measurement is a passthrough, downstream uses dom-metric.cjs on fixture).
- Future phases: replace Stage 1 with live skill invocation (pde-tools.cjs invoke brief && pde-tools.cjs invoke wireframe).
- Contract: exit 0 always, last line of stdout = numeric score 0-100.
- Usage: node bin/pipeline-brief-wireframe-metric.cjs <path-to-html-file>

**Required imports (CJS pattern from dom-metric.cjs):**
- const path = require('path');
- const { spawnSync } = require('child_process');

**Timeout guard (VIS-06 pattern from dom-metric.cjs):**
- TIMEOUT_MS = 30000
- setTimeout that writes '0\n' to stdout and calls process.exit(0) on timeout
- Store the timeoutId for clearTimeout later

**Argument parsing:**
- const targetFile = process.argv[2];
- If no targetFile: write '0\n' to stdout and exit 0 (graceful degradation)

**Stage 1: Upstream skill chain (placeholder for live pipeline):**
- Write a comment: // Stage 1: Upstream skill chain
- Write a comment: // Phase 113 fixture mode: upstream measurement is passthrough (no live skill invocation)
- Write a comment: // Future: spawnSync('node', [path.join(__dirname, 'pde-tools.cjs'), 'invoke', 'brief'], ...)
- Write a comment: // Future: spawnSync('node', [path.join(__dirname, 'pde-tools.cjs'), 'invoke', 'wireframe'], ...)
- Write to stderr: 'Stage 1: upstream skill chain (fixture passthrough)\n'

**Stage 2: Terminal DOM metric measurement:**
- Write to stderr: 'Stage 2: terminal DOM metric measurement\n'
- const result = spawnSync('node', [path.join(__dirname, 'dom-metric.cjs'), targetFile], { encoding: 'utf-8', timeout: 20000 })
- If result.status !== 0 or stdout is empty: clearTimeout, write '0\n' to stdout, exit 0
- Extract last non-empty line from result.stdout
- Parse as parseFloat; if NaN: clearTimeout, write '0\n', exit 0
- clearTimeout(timeoutId)
- process.stdout.write(lastLine.trim() + '\n')
- process.exit(0)

**Error handling:**
- Wrap entire logic in try/catch
- Catch block: clearTimeout(timeoutId), write '0\n' to stdout, exit 0

**Make executable:** chmod +x after creation

**DO NOT:**
- Import any npm packages (zero-dep constraint)
- Use ESM syntax (bin/ uses CJS only)
- Invoke live skill chains (Phase 113 uses fixture mode only)
  </action>
  <verify>
    <automated>node bin/pipeline-brief-wireframe-metric.cjs references/experiments/fixtures/good-wireframe.html 2>/dev/null | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - bin/pipeline-brief-wireframe-metric.cjs exists
    - bin/pipeline-brief-wireframe-metric.cjs first line is "#!/usr/bin/env node"
    - bin/pipeline-brief-wireframe-metric.cjs contains "'use strict'"
    - bin/pipeline-brief-wireframe-metric.cjs contains "PIPE-04"
    - bin/pipeline-brief-wireframe-metric.cjs contains "dom-metric.cjs"
    - bin/pipeline-brief-wireframe-metric.cjs contains "Stage 1" and "Stage 2"
    - bin/pipeline-brief-wireframe-metric.cjs contains "spawnSync"
    - Running "node bin/pipeline-brief-wireframe-metric.cjs" with no args exits 0 and last stdout line is "0"
    - Running "node bin/pipeline-brief-wireframe-metric.cjs references/experiments/fixtures/good-wireframe.html" exits 0 and last stdout line is a parseable number
  </acceptance_criteria>
  <done>pipeline-brief-wireframe-metric.cjs exists, follows _evalMetric contract (exit 0 always, last line = numeric), chains Stage 1 (upstream passthrough) and Stage 2 (dom-metric.cjs), satisfying PIPE-04 multi-stage requirement</done>
</task>

<task type="auto">
  <name>Task 2: Create pipeline experiment templates (PIPE-01, PIPE-02, PIPE-03)</name>
  <files>
    references/experiments/pipeline-brief-to-wireframe.md
    references/experiments/pipeline-upstream-isolation.md
  </files>
  <read_first>
    references/experiments/brief.md
    references/experiments/wireframe.md
    references/experiments/system.md
    bin/lib/experiment-schema.cjs
    bin/pipeline-brief-wireframe-metric.cjs
    references/experiment-boundaries.md
  </read_first>
  <action>
Create two new experiment template files following the exact YAML frontmatter pattern from existing templates. Both templates use the pipeline-brief-wireframe-metric.cjs wrapper (created in Task 1) as their verify command, satisfying PIPE-04 multi-stage chaining.

**File 1: references/experiments/pipeline-brief-to-wireframe.md**

YAML frontmatter (between --- delimiters):
- slug: pipeline-brief-wireframe
- metric: dom_structure_score
- direction: max
- verify: node bin/pipeline-brief-wireframe-metric.cjs references/experiments/fixtures/good-wireframe.html
- mutable_files: [workflows/brief.md]  (YAML array format, one entry)
- immutable_files: []
- iteration_budget: 20
- time_budget_minutes: 90

Body content must include three markdown H2 sections:

## Search Space
Content: "Optimize prose in workflows/brief.md within OPTIMIZABLE markers. Focus on brief generation prompts, output structure guidance, downstream wireframe quality hints. Do NOT modify: Step 1 (init), artifact schema writes, error messages, LOCKED sections."

## Proxy Rationale
Content: Explain this is a pipeline proxy experiment (PIPE-01, PIPE-02). The verify command uses pipeline-brief-wireframe-metric.cjs which chains: Stage 1 (upstream skill passthrough in fixture mode) then Stage 2 (dom-metric.cjs on fixture wireframe). This multi-stage wrapper (PIPE-04) enables future live pipeline measurement by replacing Stage 1 with actual skill invocations. brief.md mutations that improve downstream wireframe structure are captured by this pipeline metric.

## Stopping Rationale
Content: "Halt at consecutive_failure_limit (3), no_progress_limit (8), or iteration_budget (20)."

**File 2: references/experiments/pipeline-upstream-isolation.md**

This template targets workflows/system.md (not brief.md) to enable comparison against the brief.md pipeline template. Comparing metric_delta values across both templates' JSONL histories reveals which upstream mutation (brief vs system) had the highest downstream wireframe quality impact. This is the PIPE-03 mechanism.

YAML frontmatter:
- slug: pipeline-upstream-system
- metric: dom_structure_score
- direction: max
- verify: node bin/pipeline-brief-wireframe-metric.cjs references/experiments/fixtures/good-wireframe.html
- mutable_files: [workflows/system.md]  (YAML array format, one entry)
- immutable_files: []
- iteration_budget: 20
- time_budget_minutes: 90

Body content must include three markdown H2 sections:

## Search Space
Content: "Optimize prose in workflows/system.md within OPTIMIZABLE markers. Focus on design token generation, color palette guidance, typography scale, spacing system definition. Do NOT modify: Step 1 (init), artifact schema writes, error messages, LOCKED sections."

## Upstream Isolation
Content: Explain this is one leg of a PIPE-03 upstream isolation experiment. Compare metric_delta from this template's results.jsonl against pipeline-brief-to-wireframe.md results to attribute which upstream skill (brief vs system) drives larger downstream wireframe quality improvement. Both templates use the same pipeline-brief-wireframe-metric.cjs wrapper (PIPE-04 multi-stage chain) ensuring apples-to-apples comparison.

## Stopping Rationale
Content: "Halt at consecutive_failure_limit (3), no_progress_limit (8), or iteration_budget (20)."

**Critical constraints (DO NOT VIOLATE):**
- verify MUST start with "node bin/" (Nyquist assertion from Phase 112 tests)
- verify MUST use "node bin/pipeline-brief-wireframe-metric.cjs" (NOT dom-metric.cjs directly) to satisfy PIPE-04 multi-stage chaining
- direction MUST be "max" (Phase 112 convention continued in Phase 113)
- mutable_files entries MUST start with "workflows/" (Nyquist assertion)
- No protected directory paths (bin/, tests/, references/, .planning/, agents/, .claude/) in mutable_files
  </action>
  <verify>
    <automated>node -e "const s=require('./bin/lib/experiment-schema.cjs');const r1=s.parseExperimentFile('references/experiments/pipeline-brief-to-wireframe.md');const r2=s.parseExperimentFile('references/experiments/pipeline-upstream-isolation.md');console.log('r1.valid='+r1.valid,'r2.valid='+r2.valid);if(!r1.valid||!r2.valid){console.log(r1.errors,r2.errors);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - references/experiments/pipeline-brief-to-wireframe.md contains "slug: pipeline-brief-wireframe"
    - references/experiments/pipeline-brief-to-wireframe.md contains "direction: max"
    - references/experiments/pipeline-brief-to-wireframe.md contains "node bin/pipeline-brief-wireframe-metric.cjs"
    - references/experiments/pipeline-brief-to-wireframe.md contains "workflows/brief.md"
    - references/experiments/pipeline-brief-to-wireframe.md contains "## Proxy Rationale"
    - references/experiments/pipeline-upstream-isolation.md contains "slug: pipeline-upstream-system"
    - references/experiments/pipeline-upstream-isolation.md contains "workflows/system.md"
    - references/experiments/pipeline-upstream-isolation.md contains "## Upstream Isolation"
    - references/experiments/pipeline-upstream-isolation.md contains "node bin/pipeline-brief-wireframe-metric.cjs"
    - parseExperimentFile returns valid=true for both files
  </acceptance_criteria>
  <done>Both pipeline experiment templates exist, pass schema validation, and use pipeline-brief-wireframe-metric.cjs wrapper for PIPE-04 multi-stage verify</done>
</task>

<task type="auto">
  <name>Task 3: Create Nyquist tests for pipeline templates (PIPE-01 through PIPE-04)</name>
  <files>
    tests/phase-113/pipeline-iterate-experiments.test.mjs
  </files>
  <read_first>
    tests/phase-112/experiment-templates.test.mjs
    bin/lib/experiment-schema.cjs
    references/experiments/pipeline-brief-to-wireframe.md
    references/experiments/pipeline-upstream-isolation.md
    bin/pipeline-brief-wireframe-metric.cjs
  </read_first>
  <action>
Create tests/phase-113/pipeline-iterate-experiments.test.mjs following the exact pattern from tests/phase-112/experiment-templates.test.mjs.

The file uses ESM imports (import from 'node:test', 'node:assert/strict', 'node:fs', 'node:path', 'node:url', 'node:module'). It uses createRequire(import.meta.url) to load the CJS experiment-schema.cjs module. ROOT is derived from fileURLToPath(new URL('../../', import.meta.url)). Also import { spawnSync } from 'node:child_process' (needed for PIPE-04 wrapper test and later ITER tests).

**Define PIPELINE_TEMPLATES array:**
  ['pipeline-brief-to-wireframe.md', 'pipeline-upstream-isolation.md']

**Test blocks to create (use describe/it from node:test):**

1. describe('PIPE-01/04: pipeline templates exist and validate')
   For each template in PIPELINE_TEMPLATES:
   - it: "references/experiments/{name} exists" -- assert.ok(fs.existsSync(...))
   - it: "references/experiments/{name} passes parseExperimentFile() validation" -- const result = parseExperimentFile(fullPath); assert.equal(result.valid, true, ...)
   - it: "references/experiments/{name} verify starts with 'node bin/'" -- assert.ok(result.verify.startsWith('node bin/'), ...)
   - it: "references/experiments/{name} direction is max" -- assert.equal(result.direction, 'max')
   - it: "references/experiments/{name} mutable_files start with 'workflows/'" -- for (const entry of result.mutable_files) assert.ok(entry.startsWith('workflows/'), ...)

2. describe('PIPE-02: pipeline templates use dom-metric.cjs as terminal metric')
   For each template in PIPELINE_TEMPLATES:
   - it: "references/experiments/{name} verify command uses pipeline-brief-wireframe-metric.cjs" -- read file content, assert.ok(content.includes('pipeline-brief-wireframe-metric.cjs'))

3. describe('PIPE-03: upstream isolation templates target different upstream skills')
   - it: "pipeline-brief-to-wireframe.md targets workflows/brief.md" -- read file, assert.ok(content.includes('workflows/brief.md'))
   - it: "pipeline-upstream-isolation.md targets workflows/system.md" -- read file, assert.ok(content.includes('workflows/system.md'))
   - it: "both templates use the same terminal metric for comparable results" -- parse both, assert.equal(r1.metric, r2.metric)

4. describe('PIPE-04: pipeline metric wrapper chains multi-stage invocation')
   - it: "bin/pipeline-brief-wireframe-metric.cjs exists" -- assert.ok(fs.existsSync(path.join(ROOT, 'bin', 'pipeline-brief-wireframe-metric.cjs')))
   - it: "bin/pipeline-brief-wireframe-metric.cjs outputs numeric with fixture arg" -- spawnSync('node', [path.join(ROOT, 'bin', 'pipeline-brief-wireframe-metric.cjs'), path.join(ROOT, 'references', 'experiments', 'fixtures', 'good-wireframe.html')], { encoding: 'utf-8', timeout: 15000 }); assert proc.status === 0; parse last line; assert numeric
   - it: "bin/pipeline-brief-wireframe-metric.cjs outputs 0 with no args (graceful degradation)" -- spawnSync with no args; assert proc.status === 0; last line is '0'
   - it: "bin/pipeline-brief-wireframe-metric.cjs contains multi-stage structure (Stage 1, Stage 2)" -- read file content, assert.ok(content.includes('Stage 1') && content.includes('Stage 2'))
   PROTECTED_PREFIXES = ['bin/', 'tests/', 'references/', '.planning/', 'agents/', '.claude/']
   For each template in PIPELINE_TEMPLATES:
   - it: "references/experiments/{name} mutable_files do not target protected directories" -- for each entry, for each prefix, assert.ok(!entry.startsWith(prefix))

**Important:** This test file ONLY covers the PIPELINE_TEMPLATES (2 templates). The ITER templates will be tested in Plan 02. However, to avoid having TWO separate test files in tests/phase-113/, include a placeholder comment at the bottom: "// ITER-01..04 tests: see iterate-effectiveness section below" -- Plan 02 will EXTEND this file by adding ITER test blocks.

Actually, per the VALIDATION.md, there should be one test file: tests/phase-113/pipeline-iterate-experiments.test.mjs. So this task creates the file with PIPE tests only, and Plan 02 Task 2 will add ITER tests to the SAME file. To enable this, end the file with a comment: // ITER-01..04 tests added by 113-02-PLAN.md
  </action>
  <verify>
    <automated>node --test tests/phase-113/pipeline-iterate-experiments.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-113/pipeline-iterate-experiments.test.mjs exists
    - tests/phase-113/pipeline-iterate-experiments.test.mjs contains "PIPE-01"
    - tests/phase-113/pipeline-iterate-experiments.test.mjs contains "PIPE-02"
    - tests/phase-113/pipeline-iterate-experiments.test.mjs contains "PIPE-03"
    - tests/phase-113/pipeline-iterate-experiments.test.mjs contains "PIPE-04"
    - tests/phase-113/pipeline-iterate-experiments.test.mjs contains "parseExperimentFile"
    - tests/phase-113/pipeline-iterate-experiments.test.mjs contains "pipeline-brief-to-wireframe.md"
    - tests/phase-113/pipeline-iterate-experiments.test.mjs contains "pipeline-upstream-isolation.md"
    - tests/phase-113/pipeline-iterate-experiments.test.mjs contains "pipeline-brief-wireframe-metric.cjs"
    - tests/phase-113/pipeline-iterate-experiments.test.mjs contains "spawnSync"
    - node --test tests/phase-113/pipeline-iterate-experiments.test.mjs exits 0 (all tests pass)
  </acceptance_criteria>
  <done>Nyquist tests for PIPE-01 through PIPE-04 exist and pass, covering template existence, schema validation, verify format, direction, mutable_files, pipeline metric wrapper multi-stage structure, and upstream isolation</done>
</task>

</tasks>

<verification>
node --test tests/phase-113/pipeline-iterate-experiments.test.mjs
node --test tests/phase-112/experiment-templates.test.mjs (no regressions on Phase 112)
node bin/pipeline-brief-wireframe-metric.cjs references/experiments/fixtures/good-wireframe.html (exits 0, numeric output)
</verification>

<success_criteria>
- pipeline-brief-wireframe-metric.cjs exists in bin/, chains Stage 1 + Stage 2
- 2 pipeline experiment templates created in references/experiments/
- Both templates use pipeline-brief-wireframe-metric.cjs (not dom-metric.cjs directly) in verify
- Both pass parseExperimentFile validation
- Nyquist tests cover PIPE-01, PIPE-02, PIPE-03, PIPE-04 including wrapper script tests
- All tests pass green
- Phase 112 tests unaffected (no regressions)
</success_criteria>

<output>
After completion, create .planning/phases/113-cross-skill-pipeline-iterate-effectiveness/113-01-SUMMARY.md
</output>
