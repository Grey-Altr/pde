---
phase: 112-experiment-templates
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - references/experiments/recommend.md
  - references/experiments/competitive.md
  - references/experiments/opportunity.md
  - references/experiments/ideate.md
autonomous: true
requirements: [EXP-10, EXP-11, EXP-12]

must_haves:
  truths:
    - "All 4 non-browser skills have experiment templates using nyquist-metric.cjs"
    - "All 14 eligible design skills have at least one experiment template (EXP-12)"
    - "Every template passes experiment-schema.cjs parseExperimentFile() validation"
  artifacts:
    - path: "references/experiments/recommend.md"
      provides: "Recommend skill experiment template"
      contains: "nyquist-metric.cjs"
    - path: "references/experiments/competitive.md"
      provides: "Competitive skill experiment template"
      contains: "nyquist-metric.cjs"
    - path: "references/experiments/opportunity.md"
      provides: "Opportunity skill experiment template"
      contains: "nyquist-metric.cjs"
    - path: "references/experiments/ideate.md"
      provides: "Ideate skill experiment template"
      contains: "nyquist-metric.cjs"
  key_links:
    - from: "references/experiments/{recommend,competitive,opportunity,ideate}.md"
      to: "bin/nyquist-metric.cjs"
      via: "verify field shell command"
      pattern: "verify: node bin/nyquist-metric.cjs"
    - from: "references/experiments/*.md (all 14)"
      to: "bin/lib/experiment-schema.cjs"
      via: "parseExperimentFile() frontmatter validation"
      pattern: "REQUIRED_FIELDS.*metric.*direction.*verify.*mutable_files"
---

<objective>
Create experiment.md templates for all 4 non-browser design skills (recommend, competitive, opportunity, ideate) and validate all 14 templates pass schema validation.

Purpose: Complete the experiment template set so every eligible design skill has a runnable AutoResearch experiment, and confirm the full set conforms to the experiment-schema.cjs contract.

Output: 4 experiment template files in `references/experiments/` using nyquist-metric.cjs. Combined with Plan 01's 9 templates, this delivers EXP-12 (all 14 skills covered).
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@references/experiment-boundaries.md
@templates/experiment.md
@bin/lib/experiment-schema.cjs
@.planning/phases/112-experiment-templates/112-01-SUMMARY.md

<interfaces>
<!-- Non-browser skill metric -->
nyquist-metric.cjs — Nyquist pass count (integer), direction: max, no argv[2] needed

<!-- 4 non-browser authorized workflow files -->
workflows/recommend.md
workflows/competitive.md
workflows/opportunity.md
workflows/ideate.md

<!-- experiment-schema.cjs REQUIRED_FIELDS -->
['metric', 'direction', 'verify', 'mutable_files']

<!-- parseExperimentFile(filePath) returns -->
{ valid: true, metric, direction, verify, mutable_files, ... } on success
{ valid: false, errors: [...] } on failure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 non-browser skill experiment templates (recommend, competitive, opportunity, ideate)</name>
  <files>
    references/experiments/recommend.md
    references/experiments/competitive.md
    references/experiments/opportunity.md
    references/experiments/ideate.md
  </files>
  <read_first>
    templates/experiment.md
    references/experiment-boundaries.md
    .planning/phases/112-experiment-templates/112-RESEARCH.md
  </read_first>
  <action>
Create 4 experiment template files for non-browser skills. All use `nyquist-metric.cjs` with `direction: max` since these skills produce markdown-only output with no renderable artifacts.

**recommend.md** (EXP-10a):
```yaml
---
slug: recommend-quality
metric: nyquist_pass_count
direction: max
verify: node bin/nyquist-metric.cjs
mutable_files:
  - workflows/recommend.md
immutable_files: []
iteration_budget: 30
time_budget_minutes: 60
---
```
Search Space: Optimize prose in `workflows/recommend.md` within `<!-- OPTIMIZABLE -->` markers. Focus on recommendation framing, context analysis instructions, output structure guidance, technology evaluation criteria. Do NOT modify: Step 1 (init), artifact schema writes (REC code, file path patterns), error messages, `<!-- LOCKED -->` sections.
Constraints: Only modify `<!-- OPTIMIZABLE -->` sections. Nyquist pass count measures structural quality of recommendation output.
Stopping: Halt at consecutive_failure_limit (5), no_progress_limit (10), or iteration_budget (30).

**competitive.md** (EXP-10b):
```yaml
---
slug: competitive-quality
metric: nyquist_pass_count
direction: max
verify: node bin/nyquist-metric.cjs
mutable_files:
  - workflows/competitive.md
immutable_files: []
iteration_budget: 30
time_budget_minutes: 60
---
```
Search Space: Optimize prose in `workflows/competitive.md` within `<!-- OPTIMIZABLE -->` markers. Focus on competitive analysis guidance, comparison framing, feature matrix instructions, market positioning prose. Do NOT modify: Step 1 (init), artifact schema writes (CMP code), error messages, `<!-- LOCKED -->` sections.
Constraints: Only modify `<!-- OPTIMIZABLE -->` sections. Nyquist pass count measures structural quality.
Stopping: Same circuit breakers.

**opportunity.md** (EXP-10c):
```yaml
---
slug: opportunity-quality
metric: nyquist_pass_count
direction: max
verify: node bin/nyquist-metric.cjs
mutable_files:
  - workflows/opportunity.md
immutable_files: []
iteration_budget: 30
time_budget_minutes: 60
---
```
Search Space: Optimize prose in `workflows/opportunity.md` within `<!-- OPTIMIZABLE -->` markers. Focus on opportunity framing, analysis structure, gap identification instructions, prioritization guidance. Do NOT modify: Step 1 (init), artifact schema writes (OPP code), error messages, `<!-- LOCKED -->` sections.
Constraints: Only modify `<!-- OPTIMIZABLE -->` sections. Nyquist pass count measures structural quality.
Stopping: Same circuit breakers.

**ideate.md** (EXP-10d):
```yaml
---
slug: ideate-quality
metric: nyquist_pass_count
direction: max
verify: node bin/nyquist-metric.cjs
mutable_files:
  - workflows/ideate.md
immutable_files: []
iteration_budget: 30
time_budget_minutes: 60
---
```
Search Space: Optimize prose in `workflows/ideate.md` within `<!-- OPTIMIZABLE -->` markers. Focus on ideation prompts, concept framing, divergent thinking guidance, output format diversity. Do NOT modify: Step 1 (init), artifact schema writes (IDT code), error messages, `<!-- LOCKED -->` sections.
Constraints: Only modify `<!-- OPTIMIZABLE -->` sections. Nyquist pass count measures structural quality.
Stopping: Same circuit breakers.

Each file must have three prose sections after frontmatter: `## Search Space`, `## Constraints`, `## Stopping Rationale`.
  </action>
  <verify>
    <automated>for f in references/experiments/recommend.md references/experiments/competitive.md references/experiments/opportunity.md references/experiments/ideate.md; do echo "--- $f ---"; head -10 "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - references/experiments/recommend.md contains "slug: recommend-quality" AND "metric: nyquist_pass_count" AND "direction: max" AND "verify: node bin/nyquist-metric.cjs" AND "workflows/recommend.md"
    - references/experiments/competitive.md contains "slug: competitive-quality" AND "metric: nyquist_pass_count" AND "direction: max" AND "workflows/competitive.md"
    - references/experiments/opportunity.md contains "slug: opportunity-quality" AND "metric: nyquist_pass_count" AND "direction: max" AND "workflows/opportunity.md"
    - references/experiments/ideate.md contains "slug: ideate-quality" AND "metric: nyquist_pass_count" AND "direction: max" AND "workflows/ideate.md"
    - All 4 files have `## Search Space`, `## Constraints`, and `## Stopping Rationale` sections
  </acceptance_criteria>
  <done>4 non-browser skill experiment templates exist with nyquist-metric.cjs verify commands and valid frontmatter</done>
</task>

<task type="auto">
  <name>Task 2: Validate all 14 experiment templates against experiment-schema.cjs and create Nyquist tests</name>
  <files>
    tests/phase-112/experiment-templates.test.mjs
  </files>
  <read_first>
    bin/lib/experiment-schema.cjs
    references/experiment-boundaries.md
    tests/phase-107/experiment-infrastructure.test.mjs
  </read_first>
  <action>
Create a Nyquist test file `tests/phase-112/experiment-templates.test.mjs` that validates all 14 experiment templates exist and conform to the experiment-schema.cjs contract. Follow the existing test patterns from `tests/phase-107/experiment-infrastructure.test.mjs`.

The test file must:

1. Import `parseExperimentFile` from `bin/lib/experiment-schema.cjs` using `createRequire`.
2. Import `fs` and `path`.
3. Define the 14 expected template files:
   ```javascript
   const EXPECTED_TEMPLATES = [
     'wireframe.md', 'mockup.md', 'system.md', 'flows.md',
     'critique.md', 'hig.md', 'iterate.md', 'handoff.md',
     'brief.md', 'recommend.md', 'competitive.md',
     'opportunity.md', 'ideate.md'
   ];
   // Note: deploy.md is experiment-eligible but not a design skill — 14 design skills, not 15
   ```

4. Test: **EXP-12 -- All 14 templates exist**
   - For each name in EXPECTED_TEMPLATES, assert `fs.existsSync(path.join('references/experiments', name))` is true.

5. Test: **EXP-11 -- Each template validates against experiment-schema.cjs**
   - For each template, call `parseExperimentFile(fullPath)` and assert `result.valid === true`.
   - Assert `result.metric` is a non-empty string.
   - Assert `result.direction` is `'max'` (all Phase 112 templates use direction: max).
   - Assert `result.verify` starts with `'node bin/'`.
   - Assert `result.mutable_files` is an array with length >= 1.
   - Assert each entry in `result.mutable_files` starts with `'workflows/'`.

6. Test: **Browser-backed templates use visual metric scripts**
   - For wireframe.md, mockup.md, handoff.md, brief.md: verify contains `dom-metric.cjs`
   - For system.md: verify contains `contrast-metric.cjs`
   - For flows.md: verify contains `mermaid-metric.cjs`
   - For iterate.md, hig.md: verify contains `a11y-metric.cjs`

7. Test: **Non-browser templates use nyquist-metric.cjs**
   - For recommend.md, competitive.md, opportunity.md, ideate.md: verify contains `nyquist-metric.cjs`

8. Test: **No template targets protected files**
   - For each template, assert no entry in mutable_files starts with `bin/`, `tests/`, `references/`, `.planning/`, `agents/`, or `.claude/`.

Use `describe()` and `it()` from `node:test`. Use `assert` from `node:assert`. Follow exact patterns from existing Phase 107 tests.
  </action>
  <verify>
    <automated>node --test tests/phase-112/experiment-templates.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-112/experiment-templates.test.mjs exists
    - File imports parseExperimentFile from bin/lib/experiment-schema.cjs
    - File contains at least 14 assertions for template existence (one per skill)
    - File contains at least 14 assertions for schema validation (one per skill)
    - File contains assertions checking verify command contains correct metric script name
    - File contains assertions checking mutable_files do not target protected directories
    - `node --test tests/phase-112/experiment-templates.test.mjs` exits 0
  </acceptance_criteria>
  <done>Nyquist test file validates all 14 experiment templates exist, pass schema validation, use correct metrics, and respect protected boundaries</done>
</task>

</tasks>

<verification>
- All 14 experiment templates exist in `references/experiments/` (9 from Plan 01 + 4 from this plan + critique in Plan 01 = 13 unique files, 14 design skills covered)
- All templates validate via `parseExperimentFile()`
- Nyquist tests pass: `node --test tests/phase-112/experiment-templates.test.mjs` exits 0
- No template targets protected files or directories
</verification>

<success_criteria>
- 4 non-browser experiment templates created with nyquist-metric.cjs verify commands
- Combined with Plan 01, all 14 eligible design skills have templates (EXP-12)
- Nyquist test file validates the complete set
- All tests pass green
</success_criteria>

<output>
After completion, create `.planning/phases/112-experiment-templates/112-02-SUMMARY.md`
</output>
