---
phase: 131-mcp-write-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pde-mcp-server/src/index.ts
  - packages/pde-mcp-server/src/write-tools.ts
  - packages/pde-mcp-server/src/tools/update-constraints.ts
  - packages/pde-mcp-server/src/tools/update-tech-stack.ts
  - packages/pde-mcp-server/handlers.cjs
  - tests/phase-131/test-mcp-write-tools.cjs
autonomous: true
requirements:
  - INF-01
  - INF-02
  - INF-03

must_haves:
  truths:
    - "Starting pde-mcp-server without --enable-writes produces read-only behavior — no write tools registered"
    - "Starting with --enable-writes registers 4 write tools and logs to stderr"
    - "pde_update_constraints overwrites the Constraints section in PROJECT.md, calls emitAll, logs to mcp-writes.ndjson"
    - "pde_update_tech_stack overwrites the Tech Stack section in PROJECT.md, calls emitAll, logs to mcp-writes.ndjson"
    - "Both tools reject content outside 1-4000 chars or containing HTML comment markers"
  artifacts:
    - path: "packages/pde-mcp-server/src/index.ts"
      provides: "--enable-writes flag parsing and conditional write tool registration"
      contains: "enableWrites"
    - path: "packages/pde-mcp-server/src/write-tools.ts"
      provides: "registerWriteTools function importing 4 tool factories"
      exports: ["registerWriteTools"]
    - path: "packages/pde-mcp-server/src/tools/update-constraints.ts"
      provides: "pde_update_constraints tool factory"
      exports: ["updateConstraintsTool"]
    - path: "packages/pde-mcp-server/src/tools/update-tech-stack.ts"
      provides: "pde_update_tech_stack tool factory"
      exports: ["updateTechStackTool"]
    - path: "packages/pde-mcp-server/handlers.cjs"
      provides: "handleUpdateConstraints, handleUpdateTechStack, appendMcpWriteLog handlers"
      exports: ["handleUpdateConstraints", "handleUpdateTechStack", "appendMcpWriteLog"]
    - path: "tests/phase-131/test-mcp-write-tools.cjs"
      provides: "Nyquist tests for INF-01, INF-02, INF-03"
      min_lines: 100
  key_links:
    - from: "packages/pde-mcp-server/src/index.ts"
      to: "packages/pde-mcp-server/src/write-tools.ts"
      via: "conditional import when enableWrites"
      pattern: "enableWrites.*registerWriteTools"
    - from: "packages/pde-mcp-server/handlers.cjs"
      to: "bin/lib/context-sync.cjs"
      via: "require for replaceSectionInFile and emitAll"
      pattern: "require.*context-sync"
---

<objective>
Add the --enable-writes flag gate to the MCP server and implement the two PROJECT.md section-overwrite tools (pde_update_constraints, pde_update_tech_stack) with shared NDJSON audit logging infrastructure.

Purpose: Enables MCP clients to write Constraints and Tech Stack sections to PROJECT.md through validated write tools, with full audit trail and automatic editor context re-emission.
Output: Modified index.ts with flag gate, new write-tools.ts entry point, two tool factories, two handler functions, appendMcpWriteLog utility, and Nyquist tests covering INF-01 through INF-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-mcp-write-tools/131-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/pde-mcp-server/src/index.ts (current tool registration pattern):
```typescript
const tools = [getProjectTool(planningDir), /* ... */];
for (const tool of tools) {
  server.registerTool(tool.name, { description: tool.description, inputSchema: tool.inputSchema }, tool.handler);
}
// All registerTool calls MUST precede server.connect(transport)
```

From packages/pde-mcp-server/src/tools/get-handoff.ts (tool factory pattern):
```typescript
import { createRequire } from 'node:module';
import { z } from 'zod';
const require = createRequire(import.meta.url);
export function getHandoffTool(planningDir: string) {
  const handlers = require('../../handlers.cjs');
  return { name: 'get-handoff', description: '...', inputSchema: { name: z.string().optional().describe('...') }, handler: (params) => handlers.handleGetHandoff(planningDir, params) };
}
```

From packages/pde-mcp-server/handlers.cjs (handler return shape):
```javascript
// Success: { content: [{ type: 'text', text: string }] }
// Error:   { content: [{ type: 'text', text: string }], isError: true }
```

From bin/lib/context-sync.cjs (available exports):
```javascript
module.exports = {
  // ... existing exports ...
  replaceSectionInFile,  // (filePath, sectionName, newContent) => boolean
  emitAll,               // (cwd) => { emitted, skipped, errors }
  appendConflictLog,     // (planningDir, entry) => void — NDJSON append pattern
};
```

CRITICAL: emitAll(cwd) takes project root, NOT planningDir. Derive cwd = path.dirname(planningDir).
CRITICAL: replaceSectionInFile returns false when section not found (does not throw).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Handlers + NDJSON audit log + Nyquist test scaffold</name>
  <files>
    packages/pde-mcp-server/handlers.cjs
    tests/phase-131/test-mcp-write-tools.cjs
  </files>
  <behavior>
    - INF-02-valid: handleUpdateConstraints with valid 100-char content replaces Constraints section in PROJECT.md, returns success text
    - INF-02-overflow: handleUpdateConstraints with 4001-char content returns isError: true
    - INF-02-underflow: handleUpdateConstraints with empty string returns isError: true
    - INF-02-injection: handleUpdateConstraints with content containing '<!--' returns isError: true
    - INF-02-injection2: handleUpdateConstraints with content containing 'PDE-GENERATED' returns isError: true
    - INF-02-missing-section: handleUpdateConstraints when PROJECT.md has no Constraints heading returns isError: true
    - INF-02-ndjson: handleUpdateConstraints writes an NDJSON entry to logs/mcp-writes.ndjson with ts, tool, section, contentLen, emitResult fields
    - INF-03-valid: handleUpdateTechStack with valid content replaces Tech Stack section in PROJECT.md
    - INF-03-overflow: handleUpdateTechStack with 4001-char content returns isError: true
    - INF-01-flag-off: When process.argv does not include --enable-writes, enableWrites is false (test the argv check pattern directly)
    - INF-01-flag-on: When process.argv includes --enable-writes, enableWrites is true
  </behavior>
  <action>
    1. Create tests/phase-131/test-mcp-write-tools.cjs with the test scaffold:
       - Use 'node:test' describe/test + 'node:assert/strict' (same pattern as tests/phase-130/)
       - Create makeTmpDir() and makePlanningDir(baseDir) helpers that create .planning/ with a minimal PROJECT.md containing `## Constraints\nExisting constraints\n\n## Tech Stack\nExisting tech stack\n\n## Summary\nTest project.\n`
       - Import handlers directly from handlers.cjs via require('../../packages/pde-mcp-server/handlers.cjs')
       - Write all 11 tests from the behavior block as RED (failing) tests first

    2. Add to handlers.cjs (above the exports block):
       - Lazy-load context-sync.cjs: `function getContextSync() { return require(path.join(__dirname, '..', '..', 'bin', 'lib', 'context-sync.cjs')); }` — same relative path pattern as getGenerateTailwindTheme()
       - `function appendMcpWriteLog(planningDir, entry)` — creates logs/ dir with mkdirSync({recursive:true}), appends JSON.stringify(entry)+'\n' to `path.join(planningDir, 'logs', 'mcp-writes.ndjson')`, catches errors to stderr
       - `function validateWriteContent(content)` — shared validation: typeof string, 1-4000 chars, no '<!--' substring, no 'PDE-GENERATED' substring. Returns null on valid, error string on invalid.
       - `async function handleUpdateConstraints(planningDir, params)` — validates params.content with validateWriteContent(), calls getContextSync().replaceSectionInFile(projectMd, 'Constraints', content), checks return value (false = isError), derives cwd = path.dirname(planningDir), calls getContextSync().emitAll(cwd), catches emitAll errors, calls appendMcpWriteLog, returns success
       - `async function handleUpdateTechStack(planningDir, params)` — identical to handleUpdateConstraints but section='Tech Stack', tool name='pde_update_tech_stack'
       - Add handleUpdateConstraints, handleUpdateTechStack, appendMcpWriteLog to module.exports

    3. Run tests — all 11 should pass (GREEN).

    CRITICAL: emitAll(cwd) takes project root NOT planningDir. Derive: const cwd = path.dirname(planningDir);
    CRITICAL: replaceSectionInFile returns false (not throw) when section missing — check the return value.
    CRITICAL: For tests that call emitAll via handleUpdateConstraints/handleUpdateTechStack, the tmp .planning/ dir needs at minimum a PROJECT.md with the target section. emitAll may fail on minimal dirs — wrap emitAll in try/catch in the handler (emitStatus='error:...') so the handler still succeeds and logs the attempt.
  </action>
  <verify>
    <automated>node tests/phase-131/test-mcp-write-tools.cjs</automated>
  </verify>
  <done>
    - handlers.cjs exports handleUpdateConstraints, handleUpdateTechStack, appendMcpWriteLog
    - validateWriteContent rejects empty, >4000 chars, '<!--', 'PDE-GENERATED'
    - handleUpdateConstraints replaces Constraints section via replaceSectionInFile, calls emitAll(cwd), logs NDJSON
    - handleUpdateTechStack replaces Tech Stack section via same pattern
    - All 11 Nyquist tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: TypeScript tool factories + flag gate in index.ts + build</name>
  <files>
    packages/pde-mcp-server/src/tools/update-constraints.ts
    packages/pde-mcp-server/src/tools/update-tech-stack.ts
    packages/pde-mcp-server/src/write-tools.ts
    packages/pde-mcp-server/src/index.ts
  </files>
  <action>
    1. Create src/tools/update-constraints.ts:
       - Follow get-handoff.ts pattern exactly: import createRequire from 'node:module', import z from 'zod'
       - Export function updateConstraintsTool(planningDir: string) that returns { name: 'pde_update_constraints', description: 'Overwrites the Constraints section of PROJECT.md, validates content, and re-emits all editor context files', inputSchema: { content: z.string().describe('New constraints content (1-4000 characters, no HTML comment markers)') }, handler: (params) => handlers.handleUpdateConstraints(planningDir, params) }

    2. Create src/tools/update-tech-stack.ts:
       - Same pattern, name: 'pde_update_tech_stack', description: 'Overwrites the Tech Stack section of PROJECT.md, validates content, and re-emits all editor context files', inputSchema: { content: z.string().describe('New tech stack content (1-4000 characters, no HTML comment markers)') }

    3. Create src/write-tools.ts:
       - Import McpServer from '@modelcontextprotocol/sdk/server/mcp.js'
       - Import updateConstraintsTool, updateTechStackTool, appendContextNoteTool (placeholder — will be added in plan 02), flagDivergenceTool (placeholder)
       - For now, only import the two tools that exist (update-constraints, update-tech-stack). Plan 02 will add the other two imports.
       - Export function registerWriteTools(server: McpServer, planningDir: string): void — creates array of tool factories, iterates and calls server.registerTool(tool.name, {description, inputSchema}, tool.handler)

    4. Modify src/index.ts:
       - After the planningDir resolution block (line ~37), add:
         ```
         const enableWrites = process.argv.includes('--enable-writes');
         if (enableWrites) {
           process.stderr.write('pde-mcp-server: Write mode enabled — 4 write tools registered\n');
         }
         ```
       - Import registerWriteTools from './write-tools.js'
       - After the read-only tool registration loop (line ~70) and before the resource registration (line ~74), add:
         ```
         if (enableWrites) { registerWriteTools(server, planningDir); }
         ```
       - CRITICAL: This must be BEFORE server.connect(transport) — tools registered after connect may not be visible to MCP clients

    5. Build: Run `npm run build` from packages/pde-mcp-server/ to compile TypeScript to dist/

    6. Verify build succeeds with no TypeScript errors.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine/packages/pde-mcp-server && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - src/index.ts parses --enable-writes from process.argv BEFORE server.connect()
    - src/write-tools.ts exports registerWriteTools that registers update-constraints and update-tech-stack tools
    - Two tool factory files exist following the get-handoff.ts pattern exactly
    - TypeScript build succeeds with no errors
    - enableWrites=false skips registerWriteTools call, enableWrites=true calls it
  </done>
</task>

</tasks>

<verification>
- `node tests/phase-131/test-mcp-write-tools.cjs` — all 11 tests pass
- `cd packages/pde-mcp-server && npm run build` — succeeds with no errors
- `grep -c 'enableWrites' packages/pde-mcp-server/src/index.ts` returns >= 2 (declaration + usage)
- `grep -c 'handleUpdateConstraints\|handleUpdateTechStack\|appendMcpWriteLog' packages/pde-mcp-server/handlers.cjs` returns >= 6 (definition + export for each)
</verification>

<success_criteria>
- INF-01: --enable-writes flag parsed in index.ts, conditional registration in place, stderr log on activation
- INF-02: handleUpdateConstraints validates 1-4000 chars + no marker injection, calls replaceSectionInFile('Constraints'), calls emitAll(cwd), logs NDJSON
- INF-03: handleUpdateTechStack validates identically, calls replaceSectionInFile('Tech Stack'), calls emitAll(cwd), logs NDJSON
- All 11 Nyquist tests green
- TypeScript build clean
</success_criteria>

<output>
After completion, create `.planning/phases/131-mcp-write-tools/131-01-SUMMARY.md`
</output>
