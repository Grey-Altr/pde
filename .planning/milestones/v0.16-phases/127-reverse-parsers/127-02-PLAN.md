---
phase: 127-reverse-parsers
plan: 02
type: tdd
wave: 2
depends_on:
  - 127-01
files_modified:
  - bin/lib/context-sync.cjs
  - tests/phase-127/test-reverse-parsers.cjs
autonomous: true
requirements:
  - AGR-01
  - AGR-02

must_haves:
  truths:
    - "parseSkillMd() returns null for files without PDE-GENERATED marker"
    - "parseSkillMd() returns null when frontmatter name != pde-design"
    - "parseSkillMd() extracts designTokens, componentCatalog, constraints sections into partial IR"
    - "parseSkillMd() captures unknown sections as agentAdditions string"
    - "parseSkillMd() handles marker-before-frontmatter ordering (PDE-GENERATED on line 1, --- on line 2)"
    - "parseDesignMd() extracts hex colors from Color Palette using pattern - **Name** (#hex) -- role"
    - "parseDesignMd() detects pde-format-version marker and logs warning when absent"
    - "parseDesignMd() returns {} for placeholder DESIGN.md with no color entries"
    - "Round-trip fidelity: emitAll() then parse each file type produces partial IR matching original IR fields (trimmed)"
  artifacts:
    - path: "bin/lib/context-sync.cjs"
      provides: "parseSkillMd and parseDesignMd functions"
      contains: "function parseSkillMd"
    - path: "bin/lib/context-sync.cjs"
      provides: "parseDesignMd function"
      contains: "function parseDesignMd"
    - path: "tests/phase-127/test-reverse-parsers.cjs"
      provides: "AGR-01, AGR-02, and round-trip Nyquist tests"
      min_lines: 200
  key_links:
    - from: "bin/lib/context-sync.cjs"
      to: "module.exports"
      via: "parseSkillMd, parseDesignMd added to exports"
      pattern: "parseSkillMd.*parseDesignMd"
    - from: "tests/phase-127/test-reverse-parsers.cjs"
      to: "bin/lib/context-sync.cjs"
      via: "require destructuring includes parseSkillMd, parseDesignMd"
      pattern: "parseSkillMd.*parseDesignMd"
---

<objective>
Implement the SKILL.md reverse parser (parseSkillMd) and DESIGN.md reverse parser (parseDesignMd) in bin/lib/context-sync.cjs with TDD, plus round-trip integration tests confirming emit-then-parse fidelity.

Purpose: Enable Phase 128's merge engine to receive structured partial IR from Antigravity-edited SKILL.md and DESIGN.md files.
Output: parseSkillMd() and parseDesignMd() exported from context-sync.cjs, all AGR-01/AGR-02 and round-trip Nyquist tests green.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-reverse-parsers/127-CONTEXT.md
@.planning/phases/127-reverse-parsers/127-RESEARCH.md
@.planning/phases/127-reverse-parsers/127-VALIDATION.md
@.planning/phases/127-reverse-parsers/127-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 output + existing codebase -->

From bin/lib/context-sync.cjs (after Plan 01):
```javascript
// Existing exports (Plan 01 added parseMdcContent)
module.exports = {
  buildContextIR, emitAll, emitAgentsMd, emitCursorRules, emitCursorrules,
  emitGeminiMd, computeSourceHash, cmdContextSync, oklchToHex,
  isStitchSource, emitAntigravitySkill, emitDesignMd,
  writeStateFile, readStateFile, computeLoopBreak,
  parseMdcContent,
};
```

From bin/lib/context-sync.cjs (lines 68-70, 80-85):
```javascript
function makeHeader(sourceHash, generatedAt) {
  return `<!-- PDE-GENERATED | hash:${sourceHash} | generated:${generatedAt} -->`;
}
const PDE_HASH_RE = new RegExp(_escaped); // matches PDE-GENERATED marker
```

From bin/lib/context-sync.cjs (lines 112-125):
```javascript
function extractSection(content, sectionName) { /* ... returns trimmed section body or '' */ }
```

SKILL.md emitter format (lines 659-701):
- Line 1: `<!-- PDE-GENERATED | hash:... | generated:... -->`
- Lines 2-4: YAML frontmatter `---\nname: pde-design\ndescription: ...\n---`
- Body: `# PDE Design System`, then `## Goal`, `## Instructions`, `## Design Tokens Available`, `## Component Catalog`, `## Constraints`

DESIGN.md emitter color entry format (line 775):
```javascript
return `- **${role}** (${hex}) -- ${name} color role`;
```

Partial IR contract (D-02):
```javascript
// { techStack?, constraints?, componentCatalog?, designTokens?, agentAdditions? }
// null = not PDE-authored / corrupt; {} = parsed but empty
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing AGR-01, AGR-02, and round-trip tests</name>
  <files>tests/phase-127/test-reverse-parsers.cjs</files>
  <read_first>
    - tests/phase-127/test-reverse-parsers.cjs (current state from Plan 01 — append to this file)
    - bin/lib/context-sync.cjs lines 659-701 (emitAntigravitySkill — SKILL.md format to build fixtures)
    - bin/lib/context-sync.cjs lines 733-832 (emitDesignMd — DESIGN.md format to build fixtures)
    - .planning/phases/127-reverse-parsers/127-RESEARCH.md lines 550-573 (test map: all AGR-01, AGR-02, round-trip behaviors)
  </read_first>
  <behavior>
    AGR-01 tests (parseSkillMd):
    - Test 1: parseSkillMd(null) returns null
    - Test 2: parseSkillMd('no marker') returns null (no PDE-GENERATED marker)
    - Test 3: parseSkillMd(valid_with_wrong_name) returns null (frontmatter name != pde-design)
    - Test 4: parseSkillMd(valid_skill_md) extracts designTokens, componentCatalog, constraints fields
    - Test 5: parseSkillMd(skill_md_with_unknown_section) captures unknown section as agentAdditions
    - Test 6: parseSkillMd handles marker-before-frontmatter ordering (PDE-GENERATED on line 1)

    AGR-02 tests (parseDesignMd):
    - Test 7: parseDesignMd(null) returns null
    - Test 8: parseDesignMd('no marker') returns null
    - Test 9: parseDesignMd(valid_design_md) extracts hex colors from Color Palette entries
    - Test 10: parseDesignMd(design_md_without_format_version) still extracts colors (lenient fallback), logs version warning
    - Test 11: parseDesignMd(placeholder_design_md) returns {} (no color entries, valid empty partial)

    Round-trip tests:
    - Test 12: emitAll() -> read pde-project.mdc -> parseMdcContent() -> partial.constraints matches original IR constraints (trimmed)
    - Test 13: emitAll() -> read SKILL.md -> parseSkillMd() -> partial.designTokens matches original IR designTokens (trimmed)
    - Test 14: parseMdcContent, parseSkillMd, parseDesignMd all present in module.exports
  </behavior>
  <action>
    APPEND new tests to `tests/phase-127/test-reverse-parsers.cjs` (do NOT overwrite Plan 01's CUR-01/CUR-02 tests).

    Add `parseSkillMd, parseDesignMd, emitAll` to the require destructuring at the top of the file.

    Synthetic SKILL.md fixture (matches emitAntigravitySkill format exactly):
    ```
    <!-- PDE-GENERATED | hash:aaaa...64...aaaa | generated:2025-01-01T00:00:00.000Z -->
    ---
    name: pde-design
    description: PDE design system context
    ---

    # PDE Design System

    ## Goal
    Provide design context.

    ## Instructions
    Follow the design system.

    ## Design Tokens Available
    Token data here

    ## Component Catalog
    Component data here

    ## Constraints
    - Use hex color values
    - Follow typography hierarchy
    ```

    For agentAdditions test, add an extra section `## Custom Agent Notes\nAgent wrote this.` after Constraints.

    Synthetic DESIGN.md fixture:
    ```
    <!-- PDE-GENERATED | hash:aaaa...64...aaaa | generated:2025-01-01T00:00:00.000Z -->
    <!-- pde-format-version: 1.0 -->
    # Design System: Test Project
    **Source Hash:** aaaaaaaaaaaa

    ## 2. Color Palette & Roles
    - **Primary** (#3b82f6) -- primary color role
    - **Secondary** (#8b5cf6) -- secondary color role
    ```

    For round-trip tests (Test 12, 13), use makeTmpDir()/makePlanningDir(), call emitAll(baseDir), read generated files with fs.readFileSync, parse with the reverse parsers, compare trimmed fields.

    RED phase: Run tests — new tests MUST fail (parseSkillMd, parseDesignMd not yet exported). CUR-01/CUR-02 tests from Plan 01 should still pass. Commit with message `test(127-02): add failing AGR-01/AGR-02 and round-trip tests`.
  </action>
  <verify>
    <automated>node --test tests/phase-127/test-reverse-parsers.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/phase-127/test-reverse-parsers.cjs` contains `parseSkillMd` and `parseDesignMd` in require destructuring
    - File contains at minimum 14 total `test(` calls (11 from Plan 01 + at least 3 new)
    - File contains `'AGR-01:` and `'AGR-02:` in test names
    - File contains `pde-design` string (SKILL.md frontmatter fixture)
    - File contains `pde-format-version` string (DESIGN.md version marker fixture)
    - File contains `#3b82f6` or similar hex color in fixture
    - File contains `agentAdditions` assertion
    - File contains `emitAll` in require destructuring (for round-trip tests)
    - CUR-01/CUR-02 tests from Plan 01 are unchanged and still pass
  </acceptance_criteria>
  <done>14+ tests total in the file. CUR-01/CUR-02 tests still green. AGR-01/AGR-02 and round-trip tests fail because parseSkillMd and parseDesignMd are not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement parseSkillMd and parseDesignMd, make all tests green</name>
  <files>bin/lib/context-sync.cjs</files>
  <read_first>
    - bin/lib/context-sync.cjs (full file — current state after Plan 01, need to add two functions + update exports)
    - tests/phase-127/test-reverse-parsers.cjs (the failing tests — understand what must pass)
    - .planning/phases/127-reverse-parsers/127-RESEARCH.md lines 390-483 (parseSkillMd + parseDesignMd skeletons)
    - .planning/phases/127-reverse-parsers/127-RESEARCH.md lines 278-327 (pitfalls: marker ordering, section names, Constraints hardcoded)
  </read_first>
  <behavior>
    parseSkillMd:
    - parseSkillMd(null|''|no-marker) -> null
    - parseSkillMd(wrong_name_frontmatter) -> null
    - parseSkillMd(valid) -> { designTokens, componentCatalog, constraints } with correct values
    - parseSkillMd(valid_with_unknown_sections) -> { ..., agentAdditions: 'unknown section content' }
    - Handles PDE-GENERATED marker on line 1 before YAML frontmatter
    - On exception: returns null + stderr, never throws

    parseDesignMd:
    - parseDesignMd(null|''|no-marker) -> null
    - parseDesignMd(valid_with_version) -> { designTokens: 'color list string' }
    - parseDesignMd(valid_without_version) -> { designTokens: 'color list string' } + stderr warning
    - parseDesignMd(placeholder_no_colors) -> {}
    - Color regex: /^-\s+\*\*([^*]+)\*\*\s+\(#([a-fA-F0-9]{3,6})\)\s+--\s+(.+)$/gm
    - On exception: returns null + stderr, never throws
  </behavior>
  <action>
    Add `parseSkillMd(content)` and `parseDesignMd(content)` functions to `bin/lib/context-sync.cjs` BEFORE the exports block, after the parseMdcContent function from Plan 01.

    **parseSkillMd implementation:**
    1. Null/empty gate: `if (!content) return null;`
    2. PDE marker gate: `if (!PDE_HASH_RE.test(content)) return null;`
    3. Try/catch wrapping, catch returns null + stderr log `[context-sync] skill.md parse error: ${err.message}`
    4. Strip PDE-GENERATED header line: `const withoutHeader = content.replace(/^<!--[^>]+-->\n/, '');` (Pitfall 1: marker-before-frontmatter)
    5. Match YAML frontmatter: `/^---\n([\s\S]*?)\n---/`
    6. Validate name: `if (!/^name:\s*pde-design$/m.test(fmMatch[1])) return null;` (D-12)
    7. Extract body: `withoutHeader.slice(fmMatch[0].length).trim()`
    8. Extract known sections using existing `extractSection()` helper:
       - `extractSection(body, 'Design Tokens Available')` -> `designTokens` (NOTE: "Design Tokens Available", NOT "Design Tokens" — Pitfall 3)
       - `extractSection(body, 'Component Catalog')` -> `componentCatalog`
       - `extractSection(body, 'Constraints')` -> `constraints` (NOTE: this is hardcoded content per Pitfall 4, not ir.constraints)
    9. Only add field to partial if non-empty: `if (val !== '') partial.fieldName = val;`
    10. agentAdditions: split body on `/^(?=## )/m`, filter out KNOWN sections `['Goal', 'Instructions', 'Design Tokens Available', 'Component Catalog', 'Constraints']`, join remainder trimmed. Set `partial.agentAdditions` if non-empty.
    11. Return partial (may be `{}` — valid)

    JSDoc: `@param {string} content - Raw SKILL.md file content` / `@returns {object|null}`

    **parseDesignMd implementation:**
    1. Null/empty gate: `if (!content) return null;`
    2. PDE marker gate: `if (!PDE_HASH_RE.test(content)) return null;`
    3. Try/catch wrapping, catch returns null + stderr log `[context-sync] design.md parse error: ${err.message}`
    4. Format version check: `const isV1 = /<!--\s*pde-format-version:\s*1\.0\s*-->/.test(content);`
       - If not v1: `process.stderr.write('[context-sync] design.md: no pde-format-version marker, using lenient fallback\n');`
    5. Color extraction regex applied to entire document (Pitfall 5: don't section-gate):
       `const colorPattern = /^-\s+\*\*([^*]+)\*\*\s+\(#([a-fA-F0-9]{3,6})\)\s+--\s+(.+)$/gm;`
    6. Collect matches into array: `{ name, hex: '#' + match[2], role }`
    7. If no colors found: return `{}` (valid empty partial for placeholder DESIGN.md)
    8. Reconstruct designTokens string: `colors.map(c => '- **' + c.name + '** (' + c.hex + ') -- ' + c.role).join('\n')`
    9. Return `{ designTokens }`

    JSDoc: `@param {string} content - Raw DESIGN.md file content` / `@returns {object|null}`

    **Update module.exports** to include `parseSkillMd, parseDesignMd`.

    GREEN phase: Run ALL tests — all 14+ MUST pass (CUR + AGR + round-trip). Commit with message `feat(127-02): implement parseSkillMd and parseDesignMd reverse parsers`.
  </action>
  <verify>
    <automated>node --test tests/phase-127/test-reverse-parsers.cjs && node --test tests/phase-126/test-sync-foundation.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `bin/lib/context-sync.cjs` contains `function parseSkillMd(content)`
    - `bin/lib/context-sync.cjs` contains `function parseDesignMd(content)`
    - `bin/lib/context-sync.cjs` contains `parseSkillMd` and `parseDesignMd` in module.exports
    - `bin/lib/context-sync.cjs` contains `'Design Tokens Available'` string (correct section name per Pitfall 3)
    - `bin/lib/context-sync.cjs` contains `pde-format-version` string for version detection
    - `bin/lib/context-sync.cjs` contains color regex pattern with `\*\*` and `#` hex matching
    - `bin/lib/context-sync.cjs` parseSkillMd has `content.replace(/^<!--[^>]+-->\n/, '')` for marker-before-frontmatter handling
    - `bin/lib/context-sync.cjs` parseSkillMd has `agentAdditions` logic with KNOWN sections filter
    - `node --test tests/phase-127/test-reverse-parsers.cjs` exits with code 0 (all tests pass)
    - `node --test tests/phase-126/test-sync-foundation.cjs` exits with code 0 (no regression)
  </acceptance_criteria>
  <done>parseSkillMd() and parseDesignMd() are implemented, exported, and all Nyquist tests green. Round-trip tests confirm emit-then-parse fidelity. Phase 126 regression tests pass. All 3 reverse parsers (parseMdcContent, parseSkillMd, parseDesignMd) are exported and ready for Phase 128's merge engine.</done>
</task>

</tasks>

<verification>
- `node --test tests/phase-127/test-reverse-parsers.cjs` — all CUR-01/CUR-02/AGR-01/AGR-02 + round-trip tests green
- `node --test tests/phase-126/test-sync-foundation.cjs` — Phase 126 regression check
- `grep 'parseSkillMd\|parseDesignMd\|parseMdcContent' bin/lib/context-sync.cjs` — all 3 parsers defined and exported
- `node -e "const m = require('./bin/lib/context-sync.cjs'); console.log(typeof m.parseMdcContent, typeof m.parseSkillMd, typeof m.parseDesignMd)"` — prints "function function function"
</verification>

<success_criteria>
- parseSkillMd() and parseDesignMd() exported from bin/lib/context-sync.cjs
- All Nyquist tests green: AGR-01 (marker gate, name validation, section extraction, agentAdditions, marker-before-frontmatter), AGR-02 (marker gate, color extraction, version detection, lenient fallback, placeholder handling)
- Round-trip integration tests confirm emit-then-parse produces matching partial IR fields
- Phase 126 tests still pass (no regression)
- All 3 parsers follow non-fatal resilience pattern: never throw, return null on error, log to stderr
</success_criteria>

<output>
After completion, create `.planning/phases/127-reverse-parsers/127-02-SUMMARY.md`
</output>
