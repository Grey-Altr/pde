---
phase: 164-cli-wrapping-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/cli-anything/model.cjs
  - tests/phase-164/help-parser.test.mjs
  - tests/phase-164/server-gen.test.mjs
  - tests/phase-164/skill-gen.test.mjs
  - tests/phase-164/registry.test.mjs
  - tests/phase-164/fixtures/git-help.txt
  - tests/phase-164/fixtures/gh-help.txt
  - tests/phase-164/fixtures/simple-help.txt
autonomous: true
requirements:
  - CLI-07
  - CLI-08
  - CLI-09
  - CLI-10
  - CLI-11

must_haves:
  truths:
    - "model.cjs CapabilityModelSchema type enum accepts 'cli' as a valid type"
    - "Test scaffolds exist for all four new modules with failing placeholder tests"
    - "Fixture files contain realistic --help output for parser testing"
  artifacts:
    - path: "bin/lib/cli-anything/model.cjs"
      provides: "Extended type enum including 'cli'"
      contains: "'cli'"
    - path: "tests/phase-164/help-parser.test.mjs"
      provides: "Test scaffold for help parser"
      contains: "parseSubcommands"
    - path: "tests/phase-164/server-gen.test.mjs"
      provides: "Test scaffold for server generator"
      contains: "generateServerSource"
    - path: "tests/phase-164/skill-gen.test.mjs"
      provides: "Test scaffold for SKILL.md generator"
      contains: "generateSkillMd"
    - path: "tests/phase-164/registry.test.mjs"
      provides: "Test scaffold for registry operations"
      contains: "upsertEntry"
    - path: "tests/phase-164/fixtures/git-help.txt"
      provides: "Real git --help output for testing"
      min_lines: 10
  key_links:
    - from: "tests/phase-164/help-parser.test.mjs"
      to: "bin/lib/cli-anything/help-parser.cjs"
      via: "import"
      pattern: "help-parser"
    - from: "bin/lib/cli-anything/model.cjs"
      to: "CapabilityModelSchema"
      via: "type enum"
      pattern: "'cli'"
---

<objective>
Wave 0 infrastructure: extend the capability model type enum to accept 'cli', create test scaffolds for all four new modules, and populate fixture files with real --help output samples for deterministic parser testing.

Purpose: Unblock all subsequent plans by establishing the test infrastructure and the schema extension that cmdWrap depends on.
Output: Extended model.cjs, 4 test files, 3 fixture files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-cli-wrapping-publishing/164-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From bin/lib/cli-anything/model.cjs:
```javascript
const CapabilitySchema = z.object({
  name: z.string(),
  description: z.string(),
  inputSchema: z.record(z.string(), z.unknown()),
  outputSchema: z.record(z.string(), z.unknown()).nullable(),
  method: z.string().nullable(),
  path: z.string().nullable(),
  extensions: z.record(z.string(), z.unknown()),
});

const CapabilityModelSchema = z.object({
  meta: z.object({
    source: z.string(),
    type: z.enum(['openapi', 'jsonschema', 'graphql', 'mcp']),
    version: z.string(),
    auth: z.record(z.string(), z.unknown()),
    generatedAt: z.string(),
  }),
  capabilities: z.array(CapabilitySchema),
});

module.exports = { CapabilitySchema, CapabilityModelSchema, validateCapabilityModel };
```

From bin/lib/cli-anything/ingest.cjs:
```javascript
function slugify(source) { ... }
async function cmdIngest(cwd, args) { ... }
module.exports = { loadSource, slugify, cmdIngest };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend model.cjs type enum and create test fixtures</name>
  <read_first>
    - bin/lib/cli-anything/model.cjs
    - .planning/phases/164-cli-wrapping-publishing/164-RESEARCH.md
  </read_first>
  <files>
    bin/lib/cli-anything/model.cjs,
    tests/phase-164/fixtures/git-help.txt,
    tests/phase-164/fixtures/gh-help.txt,
    tests/phase-164/fixtures/simple-help.txt
  </files>
  <action>
    1. In bin/lib/cli-anything/model.cjs, change the type enum from:
       `z.enum(['openapi', 'jsonschema', 'graphql', 'mcp'])`
       to:
       `z.enum(['openapi', 'jsonschema', 'graphql', 'mcp', 'cli'])`

    2. Create tests/phase-164/fixtures/ directory.

    3. Create tests/phase-164/fixtures/git-help.txt by running `git --help` and capturing the output. This provides a real GNU-style help output with subcommands section.

    4. Create tests/phase-164/fixtures/gh-help.txt by running `gh --help` (if available) or writing a realistic gh-style help output with USAGE, CORE COMMANDS, and FLAGS sections.

    5. Create tests/phase-164/fixtures/simple-help.txt with a minimal synthetic help output:
       ```
       Usage: mytool <command> [options]

       Commands:
         init        Initialize a new project
         build       Build the project
         test        Run tests
         deploy      Deploy to production

       Options:
         --verbose   Enable verbose output
         --json      Output as JSON
         -h, --help  Show this help
       ```

    IMPORTANT: The fixture files must be plain text, not wrapped in any template. They are read by tests as raw help output.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node -e "const {validateCapabilityModel} = require('./bin/lib/cli-anything/model.cjs'); validateCapabilityModel({meta:{source:'test',type:'cli',version:'1.0',auth:{},generatedAt:'2026-01-01'},capabilities:[{name:'test',description:'test',inputSchema:{},outputSchema:null,method:null,path:null,extensions:{}}]}); console.log('cli type validated OK')"</automated>
  </verify>
  <acceptance_criteria>
    - model.cjs contains the string `'cli'` in the type enum
    - tests/phase-164/fixtures/git-help.txt exists and contains "usage:" or "These are common Git commands"
    - tests/phase-164/fixtures/simple-help.txt exists and contains "Commands:" and "init" and "build"
    - validateCapabilityModel accepts type:'cli' without throwing
  </acceptance_criteria>
  <done>model.cjs accepts 'cli' type; 3 fixture files exist with realistic --help samples</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test scaffolds for all four new modules</name>
  <read_first>
    - tests/phase-164/fixtures/simple-help.txt
    - .planning/phases/164-cli-wrapping-publishing/164-RESEARCH.md
  </read_first>
  <files>
    tests/phase-164/help-parser.test.mjs,
    tests/phase-164/server-gen.test.mjs,
    tests/phase-164/skill-gen.test.mjs,
    tests/phase-164/registry.test.mjs
  </files>
  <behavior>
    help-parser.test.mjs:
    - parseSubcommands extracts subcommands from simple-help.txt (expects init, build, test, deploy)
    - parseSubcommands returns empty array for empty string
    - parseFlags extracts --verbose and --json from simple-help.txt
    - parseSubcommands skips ALL_CAPS section headers
    - parseSubcommands skips lines starting with dashes (flags)
    - spawnHelpText uses stdout || stderr fallback

    server-gen.test.mjs:
    - generateServerSource produces valid JS containing McpServer
    - generateServerSource includes BINARY constant with source path
    - generated source includes DRY_RUN check
    - generated source includes JSON.parse(stdout) with envelope fallback
    - generated tool includes useJson input field that appends --json
    - generated source uses spawnSync (not exec/spawn)

    skill-gen.test.mjs:
    - generateSkillMd produces string starting with <!-- PDE-GENERATED
    - generateSkillMd includes YAML frontmatter with name and binary
    - generateSkillMd lists all capabilities as ## sections
    - generateSkillMd includes --dry-run and --json in Flags section

    registry.test.mjs:
    - upsertEntry creates registry.json if it does not exist
    - upsertEntry adds new entry to empty registry
    - upsertEntry replaces existing entry with same name (upsert)
    - loadRegistry returns { version: '1.0', entries: [] } for missing file
    - cmdPublish fails if capability-model.json does not exist
  </behavior>
  <action>
    Create 4 test files in tests/phase-164/. Each test file imports from the corresponding module path (which does not exist yet -- tests are written RED first).

    Use vitest (import { describe, it, expect, vi } from 'vitest').

    For help-parser.test.mjs: read fixtures via fs.readFileSync relative to import.meta.url. Import { parseSubcommands, parseFlags, spawnHelpText } from '../../bin/lib/cli-anything/help-parser.cjs'.

    For server-gen.test.mjs: import { generateServerSource } from '../../bin/lib/cli-anything/server-gen.cjs'. Test that the returned string contains expected patterns.

    For skill-gen.test.mjs: import { generateSkillMd } from '../../bin/lib/cli-anything/skill-gen.cjs'. Pass a mock capability model and check output string.

    For registry.test.mjs: import { upsertEntry, loadRegistry } from '../../bin/lib/cli-anything/registry.cjs'. Use vi.mock or tmp directories for fs isolation. Import { cmdPublish } from same module.

    All tests should be runnable (they will FAIL because modules don't exist yet -- that's correct RED state).
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && npx vitest run tests/phase-164/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-164/help-parser.test.mjs exists and contains "parseSubcommands"
    - tests/phase-164/server-gen.test.mjs exists and contains "generateServerSource" and "useJson"
    - tests/phase-164/skill-gen.test.mjs exists and contains "generateSkillMd" and "PDE-GENERATED"
    - tests/phase-164/registry.test.mjs exists and contains "upsertEntry" and "cmdPublish"
    - Running vitest shows test files discovered (tests fail because modules don't exist -- expected RED state)
  </acceptance_criteria>
  <done>4 test scaffolds exist with meaningful assertions covering all 5 requirements; tests are in RED state awaiting module implementation</done>
</task>

</tasks>

<verification>
- `node -e "require('./bin/lib/cli-anything/model.cjs').validateCapabilityModel({meta:{source:'x',type:'cli',version:'1',auth:{},generatedAt:'x'},capabilities:[]})"` succeeds
- `ls tests/phase-164/` shows 4 test files and fixtures/ directory
- `npx vitest run tests/phase-164/ --reporter=verbose` discovers all test files
</verification>

<success_criteria>
- model.cjs type enum includes 'cli' alongside existing types
- All 4 test files exist with behavioral tests matching the requirements
- 3 fixture files exist with realistic --help output
- Existing Phase 163 tests still pass (model.cjs change is backward-compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/164-cli-wrapping-publishing/164-01-SUMMARY.md`
</output>
