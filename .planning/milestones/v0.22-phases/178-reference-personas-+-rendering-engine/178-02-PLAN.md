---
phase: 178-reference-personas-+-rendering-engine
plan: "02"
type: execute
wave: 2
depends_on: ["178-01"]
files_modified:
  - workflows/present.md
autonomous: false
requirements: [CLU-01, CLR-01, RND-06, RND-07]

must_haves:
  truths:
    - "Running /pde:present executive-summary produces a real HTML+MD presentation (not a stub) in .planning/presentations/"
    - "Running /pde:present case-study produces a real HTML+MD presentation (not a stub) in .planning/presentations/"
    - "Output files follow [persona]-[date].html and [persona]-[date].md naming convention"
    - "Regenerating overwrites prior output with current project state"
  artifacts:
    - path: "workflows/present.md"
      provides: "Updated Step 6 with pde-tools presentation render call replacing stub"
      contains: "pde-tools.*presentation render"
  key_links:
    - from: "workflows/present.md"
      to: "bin/pde-tools.cjs"
      via: "node pde-tools.cjs presentation render"
      pattern: "presentation render"
    - from: "bin/pde-tools.cjs"
      to: "bin/lib/render-presentation.cjs"
      via: "require('./lib/render-presentation.cjs')"
      pattern: "render-presentation"
---

<objective>
Update workflows/present.md Step 6 to replace the generation stub with a real pde-tools presentation render call, then verify the full end-to-end pipeline works via human checkpoint.

Purpose: Connect the rendering engine (Plan 01) to the workflow shell (Phase 177) so that /pde:present executive-summary and /pde:present case-study produce real presentations.

Output: Updated workflow file + human-verified end-to-end pipeline
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-reference-personas-+-rendering-engine/178-RESEARCH.md
@.planning/phases/178-reference-personas-+-rendering-engine/178-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: render-presentation.cjs CLI interface -->
CLI signature:
  node bin/pde-tools.cjs presentation render <persona-slug> <html-path> <md-path> [ir-file-path]

Example:
  node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" presentation render executive-summary ".planning/presentations/executive-summary-2026-03-30.html" ".planning/presentations/executive-summary-2026-03-30.md"

<!-- From Phase 177: workflows/present.md Step 6 is the stub to replace -->
Step 6 currently writes placeholder HTML+MD files inline in the workflow.
Replace with a single pde-tools CLI call that delegates to the rendering engine.
The IR is already acquired in Step 4 as $IR and saved via @file: pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace workflow Step 6 stub with render CLI call</name>
  <files>workflows/present.md</files>
  <read_first>
    - workflows/present.md (full file — understand Step 4 IR acquisition, Step 5 path setup, Step 6 current stub, Step 7 completion)
    - bin/lib/render-presentation.cjs (cmdPresentationRender signature and behavior from Plan 01)
  </read_first>
  <action>
    Replace Step 6/7 ("GENERATE MODE — Dispatch to persona generator") in workflows/present.md.

    The current Step 6 contains a placeholder that writes stub HTML and MD files inline. Replace the ENTIRE Step 6 content (after the heading) with:

    ```
    ### Step 6/7: GENERATE MODE — Render presentation

    Run the rendering engine to produce HTML and Markdown output:

    ```bash
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" presentation render "${PERSONA_SLUG}" "${HTML_PATH}" "${MD_PATH}"
    ```

    If the command fails (non-zero exit), display error:

    ```
    Error: Rendering failed for persona "${PERSONA_SLUG}".
      Check the IR extraction output and ensure .planning/ artifacts are intact.
      Re-run /pde:present ${PERSONA_SLUG} after resolving any errors.
    ```

    HALT on error.

    Log output:

    ```
    Presentation rendered:
      HTML: ${HTML_PATH}
      Markdown: ${MD_PATH}
    ```
    ```

    Key changes:
    - Remove ALL placeholder HTML template strings from Step 6
    - Remove ALL placeholder Markdown template strings from Step 6
    - Replace with single `pde-tools presentation render` CLI call
    - Keep the step heading format consistent (Step 6/7)
    - Keep error handling pattern consistent with Step 4 (HALT on error)
    - The render command re-extracts IR internally (no need to pass IR file — simpler, avoids arg escaping issues with large JSON paths)

    Do NOT modify Steps 0-5 or Step 7. Only Step 6 changes.

    Also update the Step 6 comment from "This step is the generation stub. Phase 178 will replace this with actual rendering logic." to remove the stub reference — it is no longer a stub.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && grep -c "presentation render" workflows/present.md && grep -c "Generation stub" workflows/present.md | grep "0" && npx vitest run tests/phase-177/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep "presentation render" workflows/present.md returns at least 1 match
    - grep "Generation stub" workflows/present.md returns 0 matches (stub text removed)
    - grep "Phase 178 will replace" workflows/present.md returns 0 matches (stub comment removed)
    - grep "pde-tools.cjs" workflows/present.md shows the render call
    - All Phase 177 tests still pass (workflow structure tests)
    - Step 7 (completion banner) is unchanged
  </acceptance_criteria>
  <done>
    - workflows/present.md Step 6 calls pde-tools presentation render instead of writing stubs
    - No placeholder HTML or MD template strings remain in Step 6
    - Workflow file passes Phase 177 lint/structure tests
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end presentation generation</name>
  <files>workflows/present.md, .planning/presentations/</files>
  <action>
    Human verifies the full rendering pipeline end-to-end by running /pde:present for both reference personas and inspecting the generated output in a browser and text editor.
  </action>
  <read_first>
    - .planning/presentations/ (check generated files exist)
  </read_first>
  <what-built>
    Complete rendering pipeline: /pde:present now produces real self-contained HTML and Markdown presentations for executive-summary and case-study personas. The HTML includes embedded PDE-branded CSS, auto-generated table of contents with anchor navigation, design artifact embedding (base64 when images exist), and a metadata footer. The Markdown is a portable companion file.
  </what-built>
  <how-to-verify>
    1. Run `/pde:present executive-summary` — should produce HTML+MD files in .planning/presentations/
    2. Open the generated HTML file in a browser — verify:
       - Dark-themed design (PDE tokens: dark background, blue accents)
       - Table of contents with clickable anchor links
       - Sections: Project Overview, Progress, Requirements, Blockers and Risks, Key Decisions, Timeline and Velocity, Design Artifacts
       - Footer with IR metadata
       - No broken images (should show "no artifacts" notice since project has no design screenshots)
       - No external resource loading (check browser dev tools Network tab)
    3. Open the generated MD file — verify it has matching section structure in plain text
    4. Run `/pde:present case-study` — should produce HTML+MD files
    5. Open the case-study HTML — verify problem-approach-outcome-lessons structure
    6. Run `/pde:present executive-summary` again — verify it overwrites the previous file (same filename, updated content)
  </how-to-verify>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && ls .planning/presentations/*.html 2>/dev/null | head -2</automated>
  </verify>
  <done>
    - Human confirmed executive-summary HTML renders correctly with PDE branding, TOC, and all sections
    - Human confirmed case-study HTML has problem-approach-outcome-lessons narrative structure
    - Human confirmed Markdown companions match HTML section structure
    - Human confirmed overwrite behavior works on re-run
  </done>
  <resume-signal>Type "approved" if presentations look correct, or describe any issues with layout, content, or missing sections</resume-signal>
</task>

</tasks>

<verification>
1. `grep "presentation render" workflows/present.md` — render call present
2. `grep -c "Generation stub" workflows/present.md` — returns 0
3. `npx vitest run tests/phase-177/ --reporter=verbose` — Phase 177 tests pass
4. `npx vitest run tests/phase-178/ --reporter=verbose` — Phase 178 tests pass
5. End-to-end: `/pde:present executive-summary` produces real HTML+MD
6. End-to-end: `/pde:present case-study` produces real HTML+MD
7. HTML files are self-contained, branded, with TOC and no JS
</verification>

<success_criteria>
- Workflow Step 6 delegates to pde-tools presentation render (no inline stubs)
- /pde:present executive-summary produces a self-contained HTML presentation with PDE branding
- /pde:present case-study produces a self-contained HTML presentation with problem-approach-outcome-lessons structure
- Both personas produce Markdown companion files
- Human verifies visual quality and content accuracy
</success_criteria>

<output>
After completion, create `.planning/phases/178-reference-personas-+-rendering-engine/178-02-SUMMARY.md`
</output>
