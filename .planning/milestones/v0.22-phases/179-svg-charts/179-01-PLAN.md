---
phase: 179-svg-charts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/charts.cjs
  - bin/lib/render-presentation.cjs
  - tests/phase-179/charts.test.mjs
autonomous: true
requirements: [CHT-01, CHT-02, CHT-03, CHT-04, CHT-05, CHT-06]

must_haves:
  truths:
    - "burndownChart(ir) returns valid SVG with line chart showing remaining requirements"
    - "velocityChart(ir) returns valid SVG with bar chart showing completion per phase"
    - "phaseTimelineChart(ir) returns valid SVG with horizontal bars showing phase durations"
    - "effortBreakdownChart(ir) returns valid SVG with bars showing task count by category"
    - "Every chart SVG contains role=img, aria-labelledby, and <title> element"
    - "Every chart output includes a <details> fallback data table with <table> element"
    - "No chart output contains <script> tags or external href/src URLs"
    - "Charts with unavailable IR data return a placeholder SVG instead of throwing"
    - "Charts are embedded in executive-summary and case-study persona sections via render-presentation.cjs"
  artifacts:
    - path: "bin/lib/charts.cjs"
      provides: "Four SVG chart generator functions"
      exports: ["burndownChart", "velocityChart", "phaseTimelineChart", "effortBreakdownChart"]
    - path: "tests/phase-179/charts.test.mjs"
      provides: "Unit tests for all four chart generators"
      min_lines: 80
    - path: "bin/lib/render-presentation.cjs"
      provides: "Updated renderer with chart section entries and chart CSS"
      contains: "require('./charts.cjs')"
  key_links:
    - from: "bin/lib/charts.cjs"
      to: "IR object from presentation.cjs"
      via: "function parameter"
      pattern: "function burndownChart\\(ir\\)"
    - from: "bin/lib/render-presentation.cjs"
      to: "bin/lib/charts.cjs"
      via: "require"
      pattern: "require\\('./charts\\.cjs'\\)"
    - from: "bin/lib/render-presentation.cjs"
      to: "charts in buildExecutiveSummary"
      via: "section entries"
      pattern: "burndownChart\\(ir\\)"
---

<objective>
Create four parametric SVG chart generators (burndown, velocity, phase timeline, effort breakdown) and wire them into the presentation rendering engine.

Purpose: Presentations need inline data visualizations that work in self-contained HTML (no JS, no external deps) and remain accessible via aria-labels and fallback data tables.
Output: bin/lib/charts.cjs module with four exported functions, unit tests, and updated render-presentation.cjs with chart sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/179-svg-charts/179-RESEARCH.md

<interfaces>
<!-- From bin/lib/render-presentation.cjs — existing section model -->
Section shape: { id: string, title: string, level: number, content: string }

buildExecutiveSummary(ir) returns array of section objects.
buildCaseStudy(ir) returns array of section objects.

sentinelHtml(irField, label) returns HTML string if field.unavailable, else null.

PDE_CSS constant contains CSS custom properties at :root level.

module.exports = { escHtml, embedImage, buildTOC, personaDisplayName, buildExecutiveSummary, buildCaseStudy, renderHTML, renderMarkdown, render, cmdPresentationRender }

<!-- From tests/phase-178 — MOCK_IR shape for test fixtures -->
MOCK_IR.requirements = { total: 58, completed: 12, blocked: 2, categories: [{ name: 'Rendering', total: 7, completed: 3 }, ...] }
MOCK_IR.phases = { total: 9, completed: 2, completion_pct: 22, phase_list: [{ name: '176-...', completed: true }, ...] }
MOCK_IR.cost_timing = { total_duration_min: 180, phases_with_timing: 6, avg_duration_min: 30 }
MOCK_IR.git_velocity = { total_commits: 42, commits_per_phase: 3.5, recent_activity: '2026-03-30' }

<!-- SVG design tokens (hardcoded hex, NOT CSS variables) -->
SVG_COLORS = { bg: '#161b22', text: '#e6edf3', muted: '#8b949e', accent: '#58a6ff', success: '#3fb950', warning: '#d29922', danger: '#f85149', border: '#30363d' }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create charts.cjs with four SVG generators and unit tests</name>
  <files>bin/lib/charts.cjs, tests/phase-179/charts.test.mjs</files>
  <read_first>
    - bin/lib/render-presentation.cjs (lines 130-160 for sentinelHtml pattern, lines 400-440 for section model, lines 440-460 for PDE_CSS tokens)
    - tests/phase-178/render-presentation.test.mjs (lines 40-100 for MOCK_IR fixture shape)
    - bin/lib/presentation.cjs (grep for extractRequirements, extractPhaseCompletion, extractCostTiming to confirm IR field names)
  </read_first>
  <behavior>
    - burndownChart(ir) with valid IR returns string containing `<svg` with `role="img"`, `aria-labelledby="burndown-title"`, a `<title id="burndown-title">` element, a `<polyline` for the data line, and a trailing `<details` with `<table>` fallback
    - burndownChart(ir) with { unavailable: true } returns placeholder SVG with "unavailable" text, no throw
    - burndownChart(ir) with zero total requirements returns "insufficient data" placeholder
    - velocityChart(ir) with valid IR returns `<svg` with `role="img"`, `aria-labelledby="velocity-title"`, `<rect` bars, and `<details` fallback table
    - velocityChart(ir) with phases.unavailable returns placeholder SVG
    - phaseTimelineChart(ir) with valid IR returns `<svg` with `aria-labelledby="timeline-title"`, horizontal `<rect` bars, and fallback table
    - phaseTimelineChart(ir) with cost_timing.unavailable returns placeholder
    - effortBreakdownChart(ir) with valid categories returns `<svg` with `aria-labelledby="effort-title"`, bars per category, and fallback table
    - effortBreakdownChart(ir) with empty categories returns placeholder
    - NO chart output contains `<script` tag
    - NO chart output contains `href="http` or `src="http` (no external URLs)
    - ALL chart SVGs include `xmlns="http://www.w3.org/2000/svg"` and `viewBox`
  </behavior>
  <action>
    Write tests/phase-179/charts.test.mjs FIRST (RED phase). Create a MOCK_IR fixture matching the shape from Phase 178 tests. Write describe blocks for each chart function testing: valid data output, unavailable data placeholder, empty/zero data placeholder, accessibility attributes, no-script/no-external-url constraints.

    Then create bin/lib/charts.cjs (GREEN phase):

    1. Define SVG_COLORS constant with hardcoded PDE hex values (bg:#161b22, text:#e6edf3, muted:#8b949e, accent:#58a6ff, success:#3fb950, warning:#d29922, danger:#f85149, border:#30363d).

    2. Create helper functions:
       - `mapY(value, minVal, maxVal, yTop, yBottom)` — maps data value to SVG y coordinate (inverted: higher value = lower y)
       - `mapX(index, count, xLeft, xRight)` — maps index to SVG x coordinate
       - `unavailableSvg(message)` — returns a 600x100 placeholder SVG with centered message text
       - `escAttr(str)` — escape &, <, >, " for SVG attribute context

    3. `burndownChart(ir)` (CHT-01):
       - Guard: if `!ir.requirements || ir.requirements.unavailable` return unavailableSvg
       - Guard: if `ir.requirements.total === 0` return unavailableSvg('insufficient data')
       - Derive synthetic burndown: since per-phase snapshots aren't in IR, create a linear approximation from total down to pending using phases.completed/phases.total as progress indicator. Data points: [total at start, interpolated points, pending now].
       - viewBox="0 0 600 300", axes as `<line>` elements, data as `<polyline>`, axis labels as `<text>`.
       - title id="burndown-title", aria-labelledby="burndown-title"
       - Append `<details class="chart-data-table"><summary>Data table: Burndown</summary><table>` with thead/tbody showing phase vs remaining count

    4. `velocityChart(ir)` (CHT-02):
       - Guard: unavailable or missing phases
       - If `ir.phases.phase_list` exists and is array with length > 0, show one bar per phase (completed=green bar, incomplete=muted bar). If phase_list absent, show single summary bar of completed/total.
       - viewBox="0 0 600 300", vertical bars as `<rect>`, labels as `<text>`
       - title id="velocity-title"
       - Fallback data table

    5. `phaseTimelineChart(ir)` (CHT-03):
       - Guard: if `!ir.cost_timing || ir.cost_timing.unavailable || ir.cost_timing.phases_with_timing === 0` return unavailableSvg
       - Horizontal bar chart. If detailed per-phase timing exists, show one bar per phase. Otherwise show single summary bar with total_duration_min and label with average.
       - viewBox="0 0 600 300"
       - title id="timeline-title"
       - Fallback data table

    6. `effortBreakdownChart(ir)` (CHT-04):
       - Guard: requirements unavailable or categories empty/missing
       - Horizontal bars, one per requirement category from `ir.requirements.categories`. Each bar shows completed (accent color) vs remaining (muted color) stacked.
       - viewBox="0 0 600 N" where N scales with category count (min 200, 40px per category + padding)
       - title id="effort-title"
       - Fallback data table

    7. `module.exports = { burndownChart, velocityChart, phaseTimelineChart, effortBreakdownChart }`

    Run tests until GREEN. All 4 generators must pass all assertions.
  </action>
  <verify>
    <automated>npx vitest run tests/phase-179/charts.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "burndownChart" bin/lib/charts.cjs
    - grep -q "velocityChart" bin/lib/charts.cjs
    - grep -q "phaseTimelineChart" bin/lib/charts.cjs
    - grep -q "effortBreakdownChart" bin/lib/charts.cjs
    - grep -q "module.exports" bin/lib/charts.cjs
    - grep -q "role=\"img\"" bin/lib/charts.cjs
    - grep -q "aria-labelledby" bin/lib/charts.cjs
    - grep -q "chart-data-table" bin/lib/charts.cjs
    - grep -q "unavailableSvg" bin/lib/charts.cjs
    - grep -q "xmlns=\"http://www.w3.org/2000/svg\"" bin/lib/charts.cjs
    - grep -q "burndownChart" tests/phase-179/charts.test.mjs
    - grep -q "velocityChart" tests/phase-179/charts.test.mjs
    - grep -q "phaseTimelineChart" tests/phase-179/charts.test.mjs
    - grep -q "effortBreakdownChart" tests/phase-179/charts.test.mjs
  </acceptance_criteria>
  <done>All four chart generators return valid SVG strings with accessibility attributes. Unavailable/empty data returns placeholder SVG without throwing. Tests pass for all generators covering valid data, unavailable data, accessibility, and security constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Wire charts into render-presentation.cjs and add chart CSS</name>
  <files>bin/lib/render-presentation.cjs, tests/phase-178/render-presentation.test.mjs</files>
  <read_first>
    - bin/lib/render-presentation.cjs (full file — need to understand buildExecutiveSummary, buildCaseStudy, PDE_CSS, module.exports)
    - bin/lib/charts.cjs (just created — confirm exports)
    - tests/phase-178/render-presentation.test.mjs (understand existing test structure for adding chart integration tests)
  </read_first>
  <action>
    1. At the top of render-presentation.cjs (after existing requires), add:
       `const charts = require('./charts.cjs');`

    2. In `buildExecutiveSummary(ir)` (around line 408), add TWO new chart sections after the 'timeline' section and before 'artifacts':
       `{ id: 'burndown', title: 'Burndown', level: 2, content: charts.burndownChart(ir) }`
       `{ id: 'velocity', title: 'Velocity', level: 2, content: charts.velocityChart(ir) }`

    3. In `buildCaseStudy(ir)` (around line 429), add TWO new chart sections after 'outcome' and before 'lessons':
       `{ id: 'timeline-chart', title: 'Phase Timeline', level: 2, content: charts.phaseTimelineChart(ir) }`
       `{ id: 'effort', title: 'Effort Breakdown', level: 2, content: charts.effortBreakdownChart(ir) }`

    4. In the PDE_CSS constant (around line 442), add chart-specific CSS rules BEFORE the closing backtick:
       ```
       .chart-data-table { margin-top: 0.5rem; font-size: 0.85rem; }
       .chart-data-table summary { cursor: pointer; color: var(--pde-text-muted); }
       .chart-data-table table { margin-top: 0.25rem; }
       svg { display: block; }
       ```

    5. In tests/phase-178/render-presentation.test.mjs, add a new describe block 'Chart integration (CHT-05)' that:
       - Calls buildExecutiveSummary(MOCK_IR) and verifies returned sections array contains entries with id 'burndown' and 'velocity'
       - Calls buildCaseStudy(MOCK_IR) and verifies returned sections array contains entries with id 'timeline-chart' and 'effort'
       - Verifies the content field of chart sections contains '<svg'
       - Verifies renderHTML output for executive-summary contains 'burndown-title' (proving chart SVG made it into final HTML)

    6. Run both test suites to confirm no regressions:
       - `npx vitest run tests/phase-179/charts.test.mjs` (chart unit tests)
       - `npx vitest run tests/phase-178/render-presentation.test.mjs` (renderer integration)
  </action>
  <verify>
    <automated>npx vitest run tests/phase-179/ tests/phase-178/</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "require('./charts.cjs')" bin/lib/render-presentation.cjs
    - grep -q "burndownChart" bin/lib/render-presentation.cjs
    - grep -q "velocityChart" bin/lib/render-presentation.cjs
    - grep -q "phaseTimelineChart" bin/lib/render-presentation.cjs
    - grep -q "effortBreakdownChart" bin/lib/render-presentation.cjs
    - grep -q "chart-data-table" bin/lib/render-presentation.cjs
    - grep -q "Chart integration" tests/phase-178/render-presentation.test.mjs
  </acceptance_criteria>
  <done>Charts are embedded in executive-summary (burndown + velocity) and case-study (timeline + effort) persona sections. Chart CSS is in PDE_CSS. Integration tests confirm chart SVGs appear in rendered HTML output. Both phase-178 and phase-179 test suites pass with no regressions.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/phase-179/charts.test.mjs` — all chart unit tests pass
2. `npx vitest run tests/phase-178/render-presentation.test.mjs` — renderer tests pass including new chart integration tests
3. `npx vitest run` — full suite green, no regressions
4. `node -e "const c = require('./bin/lib/charts.cjs'); console.log(Object.keys(c))"` — prints four function names
5. `node -e "const c = require('./bin/lib/charts.cjs'); const svg = c.burndownChart({requirements:{unavailable:true}}); console.log(svg.includes('unavailable'))"` — prints true
</verification>

<success_criteria>
- Four chart generators (burndown, velocity, phaseTimeline, effortBreakdown) are exported from bin/lib/charts.cjs
- Each generator accepts an IR object and returns a string containing valid inline SVG
- Each SVG includes role="img", aria-labelledby, xmlns, viewBox, and a <title> element
- Each generator output includes a <details> fallback data table for accessibility
- Unavailable or empty IR data produces a placeholder SVG, never throws
- No chart output contains <script> tags or external URLs
- Charts are wired into buildExecutiveSummary (burndown + velocity) and buildCaseStudy (timeline + effort)
- Chart CSS (.chart-data-table styles) added to PDE_CSS
- All tests pass: phase-179 unit tests + phase-178 integration tests + full suite
</success_criteria>

<output>
After completion, create `.planning/phases/179-svg-charts/179-01-SUMMARY.md`
</output>
