---
phase: 182-remaining-cluster-b-personas
plan: 03
type: execute
wave: 3
depends_on: ["182-02"]
files_modified:
  - bin/lib/render-presentation.cjs
  - tests/phase-182/render-presentation-cluster-b.test.mjs
autonomous: true
requirements:
  - CLR-07
  - CLR-08
must_haves:
  truths:
    - "buildLaunchAnnouncement(ir) returns a sections array with what-it-is, who-its-for, and how-to-start narrative"
    - "buildPortfolioOverview(ir) returns a sections array with cross-project patterns and skills demonstrated"
    - "Both builders handle unavailable IR fields gracefully via sentinelHtml()"
    - "Both slugs are registered in render() switch, personaDisplayName(), and module.exports"
    - "All 15 personas are listed in personaDisplayName() with display names"
    - "render() switch has 15 cases (no unknown persona throws for any valid slug)"
  artifacts:
    - path: "bin/lib/render-presentation.cjs"
      provides: "buildLaunchAnnouncement + buildPortfolioOverview functions, complete 15-persona registration"
      contains: "function buildLaunchAnnouncement"
    - path: "tests/phase-182/render-presentation-cluster-b.test.mjs"
      provides: "Full tests for all 7 Cluster B personas (CLR-02 through CLR-08)"
      contains: "buildLaunchAnnouncement"
  key_links:
    - from: "bin/lib/render-presentation.cjs"
      to: "render() switch statement"
      via: "case 'launch-announcement' and case 'portfolio-overview'"
      pattern: "case 'launch-announcement'"
    - from: "bin/lib/render-presentation.cjs"
      to: "personaDisplayName()"
      via: "15 entries in the names map"
      pattern: "'portfolio-overview'"
---

<objective>
Add launch-announcement (CLR-07) and portfolio-overview (CLR-08) persona builders to render-presentation.cjs. Upgrade remaining test scaffolds to full implementations. Verify the complete 15-persona suite is registered.

Purpose: Final two Cluster B personas, completing the full 15-persona suite.
Output: Two working persona builders with tests, full 15-persona registration confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/181-remaining-cluster-a-personas/181-RESEARCH.md
@.planning/phases/182-remaining-cluster-b-personas/182-01-SUMMARY.md
@.planning/phases/182-remaining-cluster-b-personas/182-02-SUMMARY.md

@bin/lib/render-presentation.cjs
@tests/phase-182/render-presentation-cluster-b.test.mjs

<interfaces>
<!-- After Plan 02, render-presentation.cjs has 13 builders registered. -->

From bin/lib/render-presentation.cjs (after Plan 02):
```javascript
// All registered personas (13 of 15):
// executive-summary, case-study (Phase 178)
// investor-update, sprint-review, client-deliverable, stakeholder-status, pm-view, project-manager-view (Phase 181)
// agile-report, design-report, research-report (Plan 01)
// post-mortem, adr-summary (Plan 02)

// Still missing (this plan adds them):
// launch-announcement (CLR-07)
// portfolio-overview (CLR-08)

// Available helpers (cumulative from all prior plans):
function buildOverview(ir), buildProgress(ir), buildRequirements(ir)
function buildBlockers(ir), buildDecisions(ir), buildTimeline(ir)
function buildArtifacts(ir), buildTechnical(ir)
function buildRetroNarrative(ir), buildDesignDecisions(ir), buildTokenEvolution(ir)
function buildResearchFindings(ir), buildResearchRecommendations(ir), buildCompetitiveLandscape(ir)
function buildWhatBroke(ir), buildRootCause(ir), buildAdrDecisions(ir)
function sentinelHtml(irField, label), escHtml(str)

// Charts:
charts.burndownChart(ir), charts.velocityChart(ir)
charts.phaseTimelineChart(ir), charts.effortBreakdownChart(ir)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement launch-announcement and portfolio-overview builders + complete tests</name>
  <files>bin/lib/render-presentation.cjs, tests/phase-182/render-presentation-cluster-b.test.mjs</files>
  <read_first>
    - bin/lib/render-presentation.cjs (full file — need current line numbers after Plan 02 modifications)
    - tests/phase-182/render-presentation-cluster-b.test.mjs (find the CLR-07 and CLR-08 it.skip scaffolds)
    - workflows/present.md (verify the 15-persona registry list for slug accuracy)
  </read_first>
  <action>
**Part A: Add 2 builder functions to bin/lib/render-presentation.cjs**

**1. buildLaunchAnnouncement(ir) — CLR-07: Launch announcement**
Target audience: Users, community, marketing, press.
Sections:
- `{ id: 'headline', title: 'Launch Announcement', level: 1, content: buildLaunchHeadline(ir) }` — NEW helper: reads `ir.project.name`, `ir.project.goal`, `ir.phases.milestone_name`. Formats as a "what it is" announcement: "[Project] [milestone] is here — [goal]". Sentinel-check `ir.project`.
- `{ id: 'whats-new', title: "What's New", level: 2, content: buildWhatsNew(ir) }` — NEW helper: reads `ir.phases.phase_list` (filter completed), summarizes as feature bullets. Sentinel-check `ir.phases`. Use escHtml. If no phases completed, show "Launch details coming soon."
- `{ id: 'who-its-for', title: "Who It's For", level: 2, content: buildAudience(ir) }` — NEW helper: reads `ir.project.target_users` and `ir.project.core_value`. Formats as audience statement. Sentinel-check `ir.project`.
- `{ id: 'how-to-start', title: 'Getting Started', level: 2, content: buildGettingStarted(ir) }` — NEW helper: reads `ir.project.name`. Provides a generic "getting started" section referencing the project. Sentinel-check `ir.project`.
- `{ id: 'metrics', title: 'By the Numbers', level: 2, content: buildProgress(ir) }`

**2. buildPortfolioOverview(ir) — CLR-08: Portfolio overview**
Target audience: Hiring managers, portfolio reviewers, self-assessment.
Sections:
- `{ id: 'overview', title: 'Portfolio Overview', level: 1, content: buildOverview(ir) }`
- `{ id: 'patterns', title: 'Cross-Project Patterns', level: 2, content: buildPatterns(ir) }` — NEW helper: reads `ir.phases` (total, completed), `ir.requirements` (total, completed), `ir.git_velocity`. Summarizes as quantitative patterns: "X phases delivered, Y requirements met, Z commits". Sentinel-check each field.
- `{ id: 'skills', title: 'Skills Demonstrated', level: 2, content: buildSkillsDemonstrated(ir) }` — NEW helper: reads `ir.requirements.categories` if available, `ir.decisions` for technology choices. Lists skill areas derived from requirement categories and technology decisions. Sentinel-check each field.
- `{ id: 'outcomes', title: 'Cumulative Outcomes', level: 2, content: buildRequirements(ir) }` — reuse (requirement coverage = outcomes achieved)
- `{ id: 'velocity', title: 'Development Velocity', level: 2, content: charts.velocityChart(ir) }`
- `{ id: 'effort', title: 'Effort Distribution', level: 2, content: charts.effortBreakdownChart(ir) }`

**Registration (3 locations):**

1. `personaDisplayName()` — add:
   - `'launch-announcement': 'Launch Announcement'`
   - `'portfolio-overview': 'Portfolio Overview'`

2. `render()` switch — add 2 cases AFTER the Plan 02 cases and BEFORE default:
   ```javascript
   case 'launch-announcement':
     sections = buildLaunchAnnouncement(ir);
     break;
   case 'portfolio-overview':
     sections = buildPortfolioOverview(ir);
     break;
   ```

3. `module.exports` — add `buildLaunchAnnouncement`, `buildPortfolioOverview`.

4. Update `default` throw message to list ALL 15 persona slugs: `executive-summary, case-study, investor-update, sprint-review, client-deliverable, stakeholder-status, pm-view, project-manager-view, agile-report, design-report, research-report, post-mortem, adr-summary, launch-announcement, portfolio-overview`.

**Part B: Complete test suite**

In `tests/phase-182/render-presentation-cluster-b.test.mjs`:
- Replace `it.skip` blocks for CLR-07 (buildLaunchAnnouncement) with full tests:
  - Returns array with expected section IDs: `headline`, `whats-new`, `who-its-for`, `how-to-start`, `metrics`
  - Handles unavailable `ir.project` and `ir.phases` gracefully
  - Integration: `render(MOCK_IR, 'launch-announcement', tmpHtml, tmpMd)` writes files
- Replace `it.skip` blocks for CLR-08 (buildPortfolioOverview) with full tests:
  - Returns array with expected section IDs: `overview`, `patterns`, `skills`, `outcomes`, `velocity`, `effort`
  - Handles unavailable `ir.phases`, `ir.requirements`, `ir.git_velocity` gracefully
  - Integration: `render(MOCK_IR, 'portfolio-overview', tmpHtml, tmpMd)` writes files

**Part C: Full suite verification**

Add a final describe block: `describe('Complete 15-persona suite')`:
- Test that `personaDisplayName()` returns non-slug display names for all 15 slugs: `executive-summary, case-study, investor-update, sprint-review, client-deliverable, stakeholder-status, pm-view, project-manager-view, agile-report, design-report, research-report, post-mortem, adr-summary, launch-announcement, portfolio-overview`
- Test that `render()` does not throw "Unknown persona" for any of the 15 slugs (call with MOCK_IR + tmp paths)

Run `npx vitest run tests/phase-182/` — ALL tests should pass including the 15-persona completeness check.
Then run `npx vitest run` — full suite should pass (no regressions).
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && npx vitest run tests/phase-182/ 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "function buildLaunchAnnouncement" bin/lib/render-presentation.cjs returns 0
    - grep -q "function buildPortfolioOverview" bin/lib/render-presentation.cjs returns 0
    - grep -q "'launch-announcement'" bin/lib/render-presentation.cjs returns 0 (in personaDisplayName)
    - grep -q "'portfolio-overview'" bin/lib/render-presentation.cjs returns 0
    - grep -q "case 'launch-announcement'" bin/lib/render-presentation.cjs returns 0
    - grep -q "case 'portfolio-overview'" bin/lib/render-presentation.cjs returns 0
    - grep -c "case '" bin/lib/render-presentation.cjs shows 15 switch cases
    - npx vitest run tests/phase-182/ passes for ALL 7 Cluster B personas
    - npx vitest run passes (full suite, no regressions)
    - No it.skip remains in tests/phase-182/render-presentation-cluster-b.test.mjs
  </acceptance_criteria>
  <done>All 7 Cluster B persona builders implemented and tested. Full 15-persona suite registered in personaDisplayName(), render() switch, and module.exports. Complete test suite green.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/phase-182/` passes all tests (0 skipped)
- `npx vitest run` passes full suite (no regressions from Phase 178, 180, 181)
- `grep -c "case '" bin/lib/render-presentation.cjs` shows exactly 15 switch cases
- personaDisplayName() has 15 entries
- module.exports lists all 15 builder functions
</verification>

<success_criteria>
- launch-announcement produces sections with headline, what's-new, audience, getting-started
- portfolio-overview produces sections with patterns, skills, outcomes, velocity chart, effort chart
- All 15 persona slugs resolve without "Unknown persona" error
- All 15 persona slugs have human-readable display names
- Full test suite green with zero skipped tests
</success_criteria>

<output>
After completion, create `.planning/phases/182-remaining-cluster-b-personas/182-03-SUMMARY.md`
</output>
