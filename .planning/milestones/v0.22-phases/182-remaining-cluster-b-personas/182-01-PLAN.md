---
phase: 182-remaining-cluster-b-personas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/render-presentation.cjs
  - tests/phase-182/render-presentation-cluster-b.test.mjs
autonomous: true
requirements:
  - CLR-02
  - CLR-03
  - CLR-04
must_haves:
  truths:
    - "buildAgileReport(ir) returns a sections array with retro narrative, burndown chart, and velocity chart"
    - "buildDesignReport(ir) returns a sections array with design decisions, artifact inventory, and visual direction"
    - "buildResearchReport(ir) returns a sections array with findings summary, tech evaluations, and recommendations"
    - "All three builders handle unavailable IR fields gracefully via sentinelHtml()"
    - "All three slugs are registered in render() switch, personaDisplayName(), and module.exports"
  artifacts:
    - path: "bin/lib/render-presentation.cjs"
      provides: "buildAgileReport + buildDesignReport + buildResearchReport functions, switch cases, display names"
      contains: "function buildAgileReport"
    - path: "tests/phase-182/render-presentation-cluster-b.test.mjs"
      provides: "Unit tests for CLR-02, CLR-03, CLR-04 plus scaffolds for CLR-05 through CLR-08"
      contains: "buildAgileReport"
  key_links:
    - from: "bin/lib/render-presentation.cjs"
      to: "render() switch statement"
      via: "case 'agile-report', case 'design-report', case 'research-report'"
      pattern: "case 'agile-report'"
---

<objective>
Add agile-report (CLR-02), design-report (CLR-03), and research-report (CLR-04) persona builders to render-presentation.cjs. Create the test file scaffold covering all 7 Cluster B personas (CLR-02 through CLR-08), with full test implementations for the three personas built here.

Purpose: First three of seven Cluster B personas, plus Wave 0 test scaffold for the phase.
Output: Three working persona builders with tests, test file scaffold for remaining four personas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/181-remaining-cluster-a-personas/181-RESEARCH.md

@bin/lib/render-presentation.cjs
@bin/lib/charts.cjs
@tests/phase-181/render-presentation-cluster-a.test.mjs

<interfaces>
<!-- Key patterns the executor needs from render-presentation.cjs -->

From bin/lib/render-presentation.cjs (persona builder contract):
```javascript
// Every builder follows this signature:
function buildPersonaName(ir, opts) {
  return [
    { id: 'section-slug', title: 'Section Title', level: 1, content: buildSomething(ir) },
    // ...
  ];
}

// Sentinel check (REQUIRED for every IR field access):
function sentinelHtml(irField, label) // returns HTML string or null

// HTML escaping (REQUIRED for all user strings):
function escHtml(str) // returns escaped string

// Existing reusable section helpers:
function buildOverview(ir)      // project name, goal, core value
function buildProgress(ir)      // phases completed (caps at 10 items)
function buildRequirements(ir)  // requirement coverage table
function buildBlockers(ir)      // blocker + risk items
function buildDecisions(ir)     // decision items
function buildTimeline(ir)      // git velocity + cost timing
function buildArtifacts(ir)     // design artifact listing
function buildTechnical(ir)     // technical decisions list

// Charts (from charts.cjs):
charts.burndownChart(ir)        // remaining tasks SVG
charts.velocityChart(ir)        // tasks per phase SVG
charts.phaseTimelineChart(ir)   // planned vs actual SVG
charts.effortBreakdownChart(ir) // effort distribution SVG

// Registration points (current state after Phase 181):
// 1. render() switch statement — currently 8 cases (executive-summary through project-manager-view), default throws
// 2. personaDisplayName() map — currently 8 entries
// 3. module.exports — currently exports 8 builders
```

From tests/phase-181/render-presentation-cluster-a.test.mjs (test pattern):
```javascript
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
let renderMod;
try { renderMod = require('../../bin/lib/render-presentation.cjs'); } catch (_) { renderMod = null; }

// Each persona: one describe block with section-id assertions + sentinel handling
describe('buildInvestorUpdate (CLU-02)', () => {
  it('returns expected section IDs', () => {
    const sections = renderMod.buildInvestorUpdate(MOCK_IR);
    const ids = sections.map(s => s.id);
    expect(ids).toContain('vision');
    // ...
  });
  it('handles unavailable phases gracefully', () => {
    const ir = { ...MOCK_IR, phases: { unavailable: true, reason: 'test' } };
    // must not throw
    const sections = renderMod.buildInvestorUpdate(ir);
    expect(sections.length).toBeGreaterThan(0);
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffold + 3 builder test suites</name>
  <files>tests/phase-182/render-presentation-cluster-b.test.mjs</files>
  <read_first>
    - tests/phase-181/render-presentation-cluster-a.test.mjs (full file — copy MOCK_IR fixture and test pattern)
    - bin/lib/render-presentation.cjs (lines 117-128 for personaDisplayName, lines 1028-1056 for switch, lines 1143-1161 for exports)
  </read_first>
  <action>
Create `tests/phase-182/render-presentation-cluster-b.test.mjs` following the Phase 181 test file pattern exactly.

1. Copy the MOCK_IR fixture from the Phase 181 test file (same structure).
2. Create 7 describe blocks — one per Cluster B persona slug:
   - `buildAgileReport (CLR-02)` — FULL test implementation
   - `buildDesignReport (CLR-03)` — FULL test implementation
   - `buildResearchReport (CLR-04)` — FULL test implementation
   - `buildPostMortem (CLR-05)` — scaffold with `it.skip` placeholder
   - `buildAdrSummary (CLR-06)` — scaffold with `it.skip` placeholder
   - `buildLaunchAnnouncement (CLR-07)` — scaffold with `it.skip` placeholder
   - `buildPortfolioOverview (CLR-08)` — scaffold with `it.skip` placeholder

For each FULL test (CLR-02, CLR-03, CLR-04), assert:
- Builder function exists on renderMod
- Returns an array of section objects
- Each section has `id`, `title`, `level`, `content` properties
- Expected section IDs are present (derive from the builder's audience needs):
  - agile-report: expect IDs including `overview`, `burndown`, `velocity`, `retrospective`
  - design-report: expect IDs including `overview`, `design-decisions`, `artifacts`
  - research-report: expect IDs including `overview`, `findings`, `recommendations`
- Handles unavailable IR fields gracefully (phases, requirements, design_artifacts, research — each set to `{ unavailable: true, reason: 'test' }`)
- Does not throw when called with full MOCK_IR

Also add an integration describe block: for each of the 3 implemented slugs, call `renderMod.render(MOCK_IR, slug, htmlPath, mdPath)` with tmp paths and assert files are written without throwing. Use `os.tmpdir()` + `beforeAll/afterAll` cleanup pattern from Phase 181 tests.

Run `npx vitest run tests/phase-182/` — tests should FAIL (builders do not exist yet). This is the RED step.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && npx vitest run tests/phase-182/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-182/render-presentation-cluster-b.test.mjs exists
    - File contains "buildAgileReport" in a describe block
    - File contains "buildDesignReport" in a describe block
    - File contains "buildResearchReport" in a describe block
    - File contains "it.skip" for CLR-05, CLR-06, CLR-07, CLR-08 scaffolds
    - File imports from ../../bin/lib/render-presentation.cjs
  </acceptance_criteria>
  <done>Test file exists with 7 describe blocks (3 full + 4 scaffold), tests fail because builders do not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Implement agile-report, design-report, research-report builders + registration</name>
  <files>bin/lib/render-presentation.cjs</files>
  <read_first>
    - bin/lib/render-presentation.cjs (lines 665-770 for Phase 181 builder examples — buildInvestorUpdate through buildProjectManager)
    - bin/lib/render-presentation.cjs (lines 1028-1060 for render() switch)
    - bin/lib/render-presentation.cjs (lines 117-128 for personaDisplayName)
    - bin/lib/render-presentation.cjs (lines 1143-1161 for module.exports)
  </read_first>
  <action>
Add 3 builder functions to bin/lib/render-presentation.cjs, following the EXACT pattern of the Phase 181 builders (buildInvestorUpdate, etc.).

**1. buildAgileReport(ir) — CLR-02: Agile project report**
Target audience: Scrum masters, agile coaches, retrospective participants.
Sections:
- `{ id: 'overview', title: 'Project Overview', level: 1, content: buildOverview(ir) }`
- `{ id: 'retrospective', title: 'Retrospective Narrative', level: 2, content: buildRetroNarrative(ir) }` — NEW helper: lists completed phases as "what went well", blockers as "what could improve", decisions as "action items taken". Use `ir.phases.phase_list` (filter completed), `ir.blockers`, `ir.decisions`. Sentinel-check each field. Use `escHtml()` on all strings.
- `{ id: 'burndown', title: 'Burndown Chart', level: 2, content: charts.burndownChart(ir) }`
- `{ id: 'velocity', title: 'Velocity Chart', level: 2, content: charts.velocityChart(ir) }`
- `{ id: 'metrics', title: 'Sprint Metrics', level: 2, content: buildProgress(ir) }`

**2. buildDesignReport(ir) — CLR-03: Design persona report**
Target audience: Design leads, UX reviewers, brand stakeholders.
Sections:
- `{ id: 'overview', title: 'Design Overview', level: 1, content: buildOverview(ir) }`
- `{ id: 'design-decisions', title: 'Design Decisions', level: 2, content: buildDesignDecisions(ir) }` — NEW helper: filters `ir.decisions` items for design-related keywords (design, token, color, typography, wireframe, mockup, layout, CSS, theme, visual, UX, UI). Falls back to buildDecisions(ir) if no design decisions found. Sentinel-check `ir.decisions`.
- `{ id: 'artifacts', title: 'Design Artifacts', level: 2, content: buildArtifacts(ir) }`
- `{ id: 'tokens', title: 'Design Token Evolution', level: 2, content: buildTokenEvolution(ir) }` — NEW helper: if `ir.design_artifacts` has token-related entries, list them; otherwise show sentinel "no token data available". Use `sentinelHtml(ir.design_artifacts, 'Design artifacts')`.
- `{ id: 'direction', title: 'Visual Direction Rationale', level: 2, content: buildTechnical(ir) }`

**3. buildResearchReport(ir) — CLR-04: Research persona report**
Target audience: Technical leads, architecture reviewers, research consumers.
Sections:
- `{ id: 'overview', title: 'Research Overview', level: 1, content: buildOverview(ir) }`
- `{ id: 'findings', title: 'Research Findings', level: 2, content: buildResearchFindings(ir) }` — NEW helper: reads `ir.research` (sentinel-check first). If available, iterate items and render title + summary + source for each. Use `escHtml()`.
- `{ id: 'recommendations', title: 'Recommendations', level: 2, content: buildResearchRecommendations(ir) }` — NEW helper: reads `ir.research` items, extracts any with recommendation-like fields. Falls back to "No specific recommendations extracted" message.
- `{ id: 'tech-eval', title: 'Technology Evaluations', level: 2, content: buildTechnical(ir) }`
- `{ id: 'landscape', title: 'Competitive Landscape', level: 2, content: buildCompetitiveLandscape(ir) }` — NEW helper: reads `ir.research` for competitive/landscape items. Sentinel-check. Falls back gracefully.

**Registration (3 locations):**

1. `personaDisplayName()` map — add 3 entries:
   - `'agile-report': 'Agile Project Report'`
   - `'design-report': 'Design Persona Report'`
   - `'research-report': 'Research Persona Report'`

2. `render()` switch statement — add 3 cases AFTER the existing `case 'project-manager-view':` block and BEFORE the `default:`:
   ```javascript
   case 'agile-report':
     sections = buildAgileReport(ir);
     break;
   case 'design-report':
     sections = buildDesignReport(ir);
     break;
   case 'research-report':
     sections = buildResearchReport(ir);
     break;
   ```

3. `module.exports` — add 3 entries:
   - `buildAgileReport`
   - `buildDesignReport`
   - `buildResearchReport`

4. Update the `default` throw message to include all 11 persona slugs (8 existing + 3 new).

**New helper functions** (buildRetroNarrative, buildDesignDecisions, buildTokenEvolution, buildResearchFindings, buildResearchRecommendations, buildCompetitiveLandscape) should be placed AFTER the existing section helpers and BEFORE the builder functions, following the file's organizational pattern.

Run `npx vitest run tests/phase-182/` — all 3 implemented persona tests should PASS. This is the GREEN step.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && npx vitest run tests/phase-182/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "function buildAgileReport" bin/lib/render-presentation.cjs returns 0
    - grep -q "function buildDesignReport" bin/lib/render-presentation.cjs returns 0
    - grep -q "function buildResearchReport" bin/lib/render-presentation.cjs returns 0
    - grep -q "'agile-report'" bin/lib/render-presentation.cjs returns 0 (in personaDisplayName)
    - grep -q "'design-report'" bin/lib/render-presentation.cjs returns 0
    - grep -q "'research-report'" bin/lib/render-presentation.cjs returns 0
    - grep -q "case 'agile-report'" bin/lib/render-presentation.cjs returns 0 (in switch)
    - grep -q "buildAgileReport" bin/lib/render-presentation.cjs (in module.exports)
    - npx vitest run tests/phase-182/ passes for CLR-02, CLR-03, CLR-04 tests
  </acceptance_criteria>
  <done>All 3 builders implemented, registered in switch + displayName + exports, tests pass. CLR-05 through CLR-08 test scaffolds still skipped.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/phase-182/` passes for buildAgileReport, buildDesignReport, buildResearchReport tests
- `npx vitest run tests/phase-178/ tests/phase-181/` still passes (no regressions)
- `grep -c "case '" bin/lib/render-presentation.cjs` shows 11 switch cases (8 existing + 3 new)
</verification>

<success_criteria>
- agile-report, design-report, research-report produce valid sections arrays with correct IDs
- All 3 slugs registered in personaDisplayName(), render() switch, and module.exports
- Unavailable IR fields produce sentinel HTML, not crashes
- Burndown and velocity charts embedded in agile-report sections
- Test scaffold exists for remaining 4 Cluster B personas (CLR-05 through CLR-08)
</success_criteria>

<output>
After completion, create `.planning/phases/182-remaining-cluster-b-personas/182-01-SUMMARY.md`
</output>
