---
phase: 57-workflow-integration
plan: 03
type: execute
wave: 2
depends_on: [57-02]
files_modified:
  - workflows/check-readiness.md
  - commands/check-readiness.md
autonomous: true
requirements: [WIRE-02, WIRE-04, INTG-02]

must_haves:
  truths:
    - "check-readiness.md has a run_integration_checks step after run_semantic_checks"
    - "run_integration_checks reads frontmatter from RESEARCH-VALIDATION.md, DEPENDENCY-GAPS.md, EDGE-CASES.md, and INTEGRATION-CHECK.md when present"
    - "Absent artifact files are reported as 'not present' in the table, not as errors"
    - "Mode B codebase-time verification checks function signatures and module exports for @-referenced code files"
    - "The Verification Artifacts section is appended to READINESS.md with severity mapping that updates the frontmatter result field"
    - "Only RESEARCH-VALIDATION.md with contradicted_count > 0 maps to FAIL; all other artifacts map to CONCERNS"
  artifacts:
    - path: "workflows/check-readiness.md"
      provides: "run_integration_checks step with artifact consumption and Mode B verification"
      contains: "run_integration_checks"
    - path: "commands/check-readiness.md"
      provides: "Command copy of check-readiness.md with run_integration_checks"
      contains: "run_integration_checks"
  key_links:
    - from: "workflows/check-readiness.md run_integration_checks"
      to: "RESEARCH-VALIDATION.md frontmatter"
      via: "extractFrontmatter parse of contradicted_count and unverifiable_count"
      pattern: "RESEARCH-VALIDATION"
    - from: "workflows/check-readiness.md run_integration_checks"
      to: "READINESS.md"
      via: "read-modify-write append of Verification Artifacts section"
      pattern: "Verification Artifacts"
---

<objective>
Add run_integration_checks step to check-readiness.md that consumes all four verification artifacts (RESEARCH-VALIDATION.md, DEPENDENCY-GAPS.md, EDGE-CASES.md, INTEGRATION-CHECK.md), runs Mode B codebase-time verification on @-referenced code files, and appends findings to READINESS.md.

Purpose: Unified readiness gate output reflects all pipeline verification signals, not just structural and semantic checks.
Output: Both copies of check-readiness.md updated with run_integration_checks step.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-workflow-integration/57-RESEARCH.md

<interfaces>
<!-- Artifact frontmatter formats the step must parse -->

RESEARCH-VALIDATION.md frontmatter:
```yaml
result: FAIL          # PASS / FAIL
contradicted_count: 5
unverifiable_count: 1
```

DEPENDENCY-GAPS.md frontmatter:
```yaml
result: pass          # pass / concerns / fail
gap_count: 0
```

EDGE-CASES.md frontmatter:
```yaml
finding_count: 3
high_count: 1
has_bdd_candidates: true
```

INTEGRATION-CHECK.md frontmatter:
```yaml
result: pass          # pass / concerns / fail
issues_found: 0
```

From workflows/check-readiness.md current step structure:
```
load_context
run_structural_checks
run_semantic_checks    (appends ## Semantic Checks to READINESS.md, updates frontmatter result)
report_result
```

From workflows/check-readiness.md run_semantic_checks pattern:
```
1. Read current READINESS.md
2. Append new section
3. If result needs updating, edit frontmatter result: field
4. Re-write using Edit or Write tool
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add run_integration_checks step to project check-readiness.md</name>
  <files>workflows/check-readiness.md</files>
  <read_first>
    - workflows/check-readiness.md (MUST read full file to see current step structure and run_semantic_checks pattern)
    - .planning/phases/57-workflow-integration/57-RESEARCH.md (Pattern 2: run_integration_checks structure, severity mapping, Pitfall 6: absent artifact handling)
  </read_first>
  <action>
Insert a new `<step name="run_integration_checks">` BETWEEN the closing `</step>` of `run_semantic_checks` and the opening `<step name="report_result">`.

The new step must contain:

**1. Artifact detection and frontmatter parsing:**

For each of the four artifacts, detect and parse:
```bash
RV_FILE=$(ls "${PHASE_DIR}"/*-RESEARCH-VALIDATION.md 2>/dev/null | head -1)
DG_FILE=$(ls "${PHASE_DIR}"/*-DEPENDENCY-GAPS.md 2>/dev/null | head -1)
EC_FILE=$(ls "${PHASE_DIR}"/*-EDGE-CASES.md 2>/dev/null | head -1)
IC_FILE=$(ls "${PHASE_DIR}"/*-INTEGRATION-CHECK.md 2>/dev/null | head -1)
```

If a file exists, read it and extract its frontmatter fields. If absent, record "not present" (per Pitfall 6 -- absent artifacts are normal, not errors).

**2. Verification Artifacts table:**

Append to READINESS.md:
```markdown
## Verification Artifacts

| Artifact | Result | Key Metric | Notes |
|----------|--------|------------|-------|
| RESEARCH-VALIDATION.md | {result or n/a} | {contradicted_count}/{unverifiable_count} claims | {present or not present} |
| DEPENDENCY-GAPS.md | {result or n/a} | {gap_count} gaps | {present or not present} |
| EDGE-CASES.md | CONCERNS | {finding_count} findings ({high_count} HIGH) | {present or not present} |
| INTEGRATION-CHECK.md | {result or n/a} | {issues_found} issues | {present or not present} |
```

**3. Mode B codebase-time verification:**

Read each PLAN.md in the phase directory. For each plan:
- Extract the `<context>...</context>` block
- Find @-references to code files (.cjs, .js, .mjs, .ts, .tsx, .jsx) that are relative paths
- For each existing code file:
  - Read the file
  - Extract function signatures: grep for `function\s+(\w+)` and `exports\.(\w+)\s*=\s*function`
  - Extract module exports: grep for `exports\.(\w+)` and `export\s+(?:function|const|class)\s+(\w+)`
  - Verify these names appear in the plan tasks (check `<action>` and `<files>` content)
  - Record as PASS if at least one export/function is referenced, CONCERNS if none are

Append to READINESS.md:
```markdown
## Mode B: Codebase-Time Verification

| File | Check | Status | Notes |
|------|-------|--------|-------|
| @{path} | function_signature | PASS/CONCERNS | {function} found/not found |
| @{path} | module_export | PASS/CONCERNS | {export} verified/missing |
```

If no code files in @-references, append: "No code file @-references found -- Mode B checks skipped."

**4. Severity mapping and READINESS.md frontmatter update:**

Apply severity mapping to determine if overall result needs updating:
- RESEARCH-VALIDATION.md with result=FAIL (contradicted_count > 0) --> update READINESS.md result to 'fail'
- DEPENDENCY-GAPS.md with result=fail --> update to 'concerns' (not fail -- dependency gaps are actionable)
- EDGE-CASES.md present --> always CONCERNS (per EDGE-04 decision)
- INTEGRATION-CHECK.md with issues_found > 0 --> update to 'concerns'
- Mode B findings with any CONCERNS --> update to 'concerns'
- Never downgrade: if current result is already 'fail', do not change it to 'concerns'

Read-modify-write pattern (same as run_semantic_checks):
1. Read current READINESS.md (just written/updated by prior steps)
2. Append the two new sections (Verification Artifacts + Mode B)
3. If overall result needs updating, edit the frontmatter `result:` field
4. Update `checks_failed` count in frontmatter if new failures found
5. Re-write the file using Edit or Write tool
  </action>
  <verify>
    <automated>grep "run_integration_checks" workflows/check-readiness.md && grep "Verification Artifacts" workflows/check-readiness.md && grep "Mode B" workflows/check-readiness.md && grep "RESEARCH-VALIDATION" workflows/check-readiness.md && grep "DEPENDENCY-GAPS" workflows/check-readiness.md && grep "EDGE-CASES" workflows/check-readiness.md && grep "INTEGRATION-CHECK" workflows/check-readiness.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/check-readiness.md contains `<step name="run_integration_checks">`
    - workflows/check-readiness.md contains "Verification Artifacts" table template
    - workflows/check-readiness.md contains "Mode B" section template
    - workflows/check-readiness.md references all four artifacts: RESEARCH-VALIDATION.md, DEPENDENCY-GAPS.md, EDGE-CASES.md, INTEGRATION-CHECK.md
    - The step appears AFTER run_semantic_checks and BEFORE report_result
    - workflows/check-readiness.md contains "not present" handling for absent artifacts
    - Severity mapping section contains "Never downgrade" or equivalent logic
  </acceptance_criteria>
  <done>run_integration_checks step exists in project check-readiness.md with artifact consumption, Mode B verification, and severity mapping</done>
</task>

<task type="auto">
  <name>Task 2: Replicate run_integration_checks to command copy of check-readiness.md</name>
  <files>commands/check-readiness.md</files>
  <read_first>
    - workflows/check-readiness.md (source of truth -- just updated with run_integration_checks)
    - commands/check-readiness.md (target -- command wrapper that delegates to the workflow)
  </read_first>
  <action>
Read commands/check-readiness.md to understand its structure. It is a command wrapper with frontmatter (name, description, allowed-tools) that delegates to the workflow.

Two possibilities:
1. If commands/check-readiness.md contains an `@workflows/check-readiness.md` reference or delegates entirely -- no changes needed (it inherits the workflow update automatically)
2. If commands/check-readiness.md has its own inline process steps (like the diff showed it has its own frontmatter and objective) -- insert the same run_integration_checks step content between run_semantic_checks and report_result, matching the exact same logic from Task 1

Read the file first, determine which case applies, then act accordingly.

If the command copy has its own inline steps, the run_integration_checks step content must be identical to the workflow copy. The only differences should be path conventions if the command copy uses different tool path patterns.
  </action>
  <verify>
    <automated>grep "run_integration_checks\|Verification Artifacts\|@.*check-readiness" commands/check-readiness.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - commands/check-readiness.md either contains run_integration_checks step OR delegates to workflows/check-readiness.md (which has it)
    - If inline steps exist: contains "Verification Artifacts" and "Mode B" sections
    - If delegation: the delegation reference points to the updated workflow file
  </acceptance_criteria>
  <done>Command copy of check-readiness.md reflects run_integration_checks capability either directly or via delegation</done>
</task>

</tasks>

<verification>
Full suite verification:
```bash
grep "run_integration_checks" workflows/check-readiness.md && \
grep "Verification Artifacts" workflows/check-readiness.md && \
grep "Mode B" workflows/check-readiness.md && \
grep "RESEARCH-VALIDATION" workflows/check-readiness.md && \
grep "DEPENDENCY-GAPS" workflows/check-readiness.md && \
echo "FULL SUITE PASS"
```
</verification>

<success_criteria>
- run_integration_checks step exists between run_semantic_checks and report_result
- All four verification artifacts are referenced in the Verification Artifacts table
- Mode B codebase-time verification section exists
- Absent artifacts handled gracefully (not errors)
- Severity mapping correctly applied (only RESEARCH-VALIDATION FAIL is overall FAIL)
- Both copies (workflow and command) are consistent
</success_criteria>

<output>
After completion, create `.planning/phases/57-workflow-integration/57-03-SUMMARY.md`
</output>
