---
phase: 56-plan-checker-enhancement
plan: 03
type: execute
wave: 3
depends_on: ["56-02"]
files_modified:
  - ~/.claude/agents/pde-plan-checker.md
autonomous: true
requirements:
  - INTG-01
  - INTG-03
  - INTG-05
  - INTG-06

must_haves:
  truths:
    - "Plan checker detects orphan exports and name mismatches in plan declarations"
    - "Plan checker verifies that files named in <context> @-references exist on disk"
    - "TOOL_MAP_PREREGISTERED entries are excluded from orphan detection"
    - "Integration check scope is strictly bounded to @-referenced files only"
    - "INTEGRATION-CHECK.md is produced with a check table showing each reference, check type, and result"
  artifacts:
    - path: "~/.claude/agents/pde-plan-checker.md"
      provides: "Dimension 11: Integration Mode A analysis pass"
      contains: "Dimension 11"
  key_links:
    - from: "pde-plan-checker.md Dimension 11"
      to: "PLAN.md <context> blocks"
      via: "grep @-references from context blocks"
      pattern: "@-ref"
    - from: "pde-plan-checker.md Dimension 11"
      to: "bin/lib/mcp-bridge.cjs"
      via: "Read TOOL_MAP_PREREGISTERED annotations"
      pattern: "TOOL_MAP_PREREGISTERED"
    - from: "pde-plan-checker.md Dimension 11"
      to: "INTEGRATION-CHECK.md"
      via: "Write via Bash tool after analysis"
      pattern: "INTEGRATION-CHECK.md"
---

<objective>
Add Dimension 11 (Integration Mode A) to pde-plan-checker agent. The checker extracts @-references from PLAN.md `<context>` blocks, verifies file existence, scans for orphan exports and name mismatches in task declarations, skips TOOL_MAP_PREREGISTERED entries, and writes INTEGRATION-CHECK.md.

Purpose: Catch broken @-references and orphan exports at declaration time before the executor burns context trying to read non-existent files. Mode A is purely declaration-time; Mode B (codebase-time) is Phase 57 scope.
Output: Updated pde-plan-checker.md with Dimension 11, INTEGRATION-CHECK.md artifact written by checker.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-plan-checker-enhancement/56-RESEARCH.md
@.planning/phases/56-plan-checker-enhancement/56-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dimension 11 (Integration Mode A) to pde-plan-checker</name>
  <files>~/.claude/agents/pde-plan-checker.md</files>
  <read_first>
    - ~/.claude/agents/pde-plan-checker.md (current checker — now has Dimensions 9 and 10; understand placement)
    - .planning/phases/56-plan-checker-enhancement/56-RESEARCH.md (Pattern 3: @-Reference Extraction, Pattern 4: Artifact File Format for INTEGRATION-CHECK.md, TOOL_MAP_PREREGISTERED extraction example, Pitfall 2 and Pitfall 5)
    - bin/lib/mcp-bridge.cjs (lines 85-95 — verify TOOL_MAP_PREREGISTERED annotation format and exact entries)
  </read_first>
  <action>
Add a new `## Dimension 11: Integration Mode A` section to pde-plan-checker.md, placed after Dimension 10 (Edge Cases) and before `</verification_dimensions>`.

The dimension content must include:

**Question:** "Do plan @-references point to existing files, and are declared exports consumed by plan tasks?"

**Process (step-by-step):**

**Step 1: Build TOOL_MAP_PREREGISTERED exclusion set**
1. Read `bin/lib/mcp-bridge.cjs` (relative to project root, which is the working directory)
2. Find all lines containing `TOOL_MAP_PREREGISTERED` annotation
3. Extract the tool name from each line (pattern: `'tool-name': '...',  // TOOL_MAP_PREREGISTERED`)
4. Build an exclusion set of these tool names (currently: `github:update-pr`, `github:search-issues`)
5. Entries in the exclusion set are NEVER flagged as orphan exports

**Step 2: Extract @-references from all plan <context> blocks**
1. For each PLAN.md in the phase directory, extract content between `<context>` and `</context>` tags
2. From that content, find all lines starting with `@` (pattern: `grep -E "^@"`)
3. Strip the leading `@` to get the file path
4. Normalize paths: if path starts with `/`, it's absolute; otherwise it's relative to project root
5. Build a deduplicated allowlist of all referenced file paths across all plans
6. INTG-05 scope constraint: this allowlist is the ONLY set of files inspected. No other files are touched.

**Step 3: Check file existence for each @-reference**
1. For each path in the allowlist, check if the file exists on disk using `test -f`
2. Record result: EXISTS or MISSING
3. MISSING files are flagged as issues with severity "concerns"

**Step 4: Check for orphan exports in @-referenced .cjs and .ts files**
1. For each @-referenced file that exists AND is a code file (.cjs, .ts, .js, .mjs):
   - Extract exported names from the file (grep for `exports.`, `module.exports`, `export function`, `export const`)
   - For each exported name, check if it appears in any task `<files>` or `<action>` block in the phase plans
   - If an export is not referenced by any task AND not in the TOOL_MAP_PREREGISTERED exclusion set → flag as orphan export with severity "concerns"
2. Skip orphan detection for non-code files (.md, .json, .yaml, etc.)

**Step 5: Check for name mismatches**
1. For each task `<files>` entry, check if the filename matches a file in the @-reference allowlist
2. If a task references a function name (in `<action>`) that appears in a different file than declared in `<files>`, flag as name mismatch

**Step 6: Write INTEGRATION-CHECK.md to phase directory**

**INTEGRATION-CHECK.md artifact format:**
```markdown
---
phase: {phase-slug}
generated: "{ISO timestamp}"
mode: A
result: {pass | concerns | fail}
checks_run: {N}
issues_found: {N}
---

# Phase {N}: Integration Check (Mode A)

**Generated:** {timestamp}
**Mode:** A -- Declaration-time
**Result:** {PASS | CONCERNS | FAIL}
**Checks run:** {N}
**Issues found:** {N}

## Check Table

| Task | Reference | Check Type | Result | Details |
|------|-----------|------------|--------|---------|
| Task 1 | @.planning/STATE.md | file_exists | PASS | --- |
| Task 1 | @bin/lib/mcp-bridge.cjs | file_exists | PASS | --- |
| Task 1 | github:update-pr | tool_map_orphan | SKIPPED | TOOL_MAP_PREREGISTERED |
| Task 2 | @bin/lib/readiness.cjs | orphan_export | PASS | All declared exports consumed |
| Task 2 | @src/missing-file.ts | file_exists | CONCERNS | File not found on disk |

## Issues

{Only present when issues_found > 0}

### {N}. {check_type}: {reference}

**Severity:** CONCERNS
**Details:** {description}
```

**Issue output format:**
```yaml
# File existence check
issue:
  plan: "{phase}-{plan_number}"
  dimension: "integration_mode_a"
  severity: "concerns"
  description: "File referenced in @-context does not exist: {path}"
  reference: "@{path}"
  check_type: "file_exists"

# Orphan export check
issue:
  plan: "{phase}-{plan_number}"
  dimension: "integration_mode_a"
  severity: "concerns"
  description: "Export '{name}' declared in {file} but no consumer found in task declarations"
  reference: "@{file}"
  check_type: "orphan_export"
  declared_name: "{name}"

# TOOL_MAP_PREREGISTERED skip
# No issue emitted — logged as SKIPPED in check table only
```

**Severity rules:**
- Missing @-referenced file → severity: "concerns"
- Orphan export (not pre-registered) → severity: "concerns"
- TOOL_MAP_PREREGISTERED entry → SKIPPED (no issue emitted)
- Name mismatch → severity: "concerns"
  </action>
  <verify>
    <automated>grep -c "Dimension 11" ~/.claude/agents/pde-plan-checker.md | grep -q "^[1-9]" && grep "integration_mode_a" ~/.claude/agents/pde-plan-checker.md | head -1 && grep "INTEGRATION-CHECK.md" ~/.claude/agents/pde-plan-checker.md | head -1 && grep "TOOL_MAP_PREREGISTERED" ~/.claude/agents/pde-plan-checker.md | head -1 && grep "file_exists\|orphan_export" ~/.claude/agents/pde-plan-checker.md | head -1 && grep "@-ref\|@-reference" ~/.claude/agents/pde-plan-checker.md | head -1 && grep "INTG-05\|allowlist\|bounded" ~/.claude/agents/pde-plan-checker.md | head -1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "## Dimension 11: Integration Mode A" ~/.claude/agents/pde-plan-checker.md returns a match
    - grep "integration_mode_a" ~/.claude/agents/pde-plan-checker.md returns at least 1 match (dimension name in issue structure)
    - grep "INTEGRATION-CHECK.md" ~/.claude/agents/pde-plan-checker.md returns at least 2 matches (artifact write + reference)
    - grep "TOOL_MAP_PREREGISTERED" ~/.claude/agents/pde-plan-checker.md returns at least 2 matches (exclusion set building + skip handling)
    - grep "file_exists" ~/.claude/agents/pde-plan-checker.md returns at least 1 match (check type)
    - grep "orphan_export" ~/.claude/agents/pde-plan-checker.md returns at least 1 match (check type)
    - grep "allowlist" ~/.claude/agents/pde-plan-checker.md returns at least 1 match (INTG-05 scope constraint)
    - grep "mcp-bridge.cjs" ~/.claude/agents/pde-plan-checker.md returns at least 1 match (pre-registered source)
    - grep 'mode.*A\|Mode A' ~/.claude/agents/pde-plan-checker.md returns at least 1 match
    - The dimension is placed AFTER Dimension 10 and BEFORE </verification_dimensions>
  </acceptance_criteria>
  <done>pde-plan-checker.md contains Dimension 11 with @-reference extraction, file existence checks, orphan export detection with TOOL_MAP_PREREGISTERED exclusion, INTG-05 scope constraint via allowlist, and INTEGRATION-CHECK.md artifact specification</done>
</task>

</tasks>

<verification>
- Dimension 11 section exists in pde-plan-checker.md between Dimension 10 and </verification_dimensions>
- All INTG requirement IDs for Phase 56 (INTG-01, INTG-03, INTG-05, INTG-06) are addressed
- TOOL_MAP_PREREGISTERED exclusion set is built from mcp-bridge.cjs (not hardcoded list)
- Scope is explicitly bounded to @-referenced files only (INTG-05)
- INTEGRATION-CHECK.md artifact format matches RESEARCH.md Pattern 4
</verification>

<success_criteria>
- pde-plan-checker.md has Dimension 11 with complete specification
- @-reference extraction process documented step-by-step
- TOOL_MAP_PREREGISTERED exclusion prevents false positives (INTG-03)
- Scope bounded to plan @-references only (INTG-05)
- INTEGRATION-CHECK.md produced with check table (INTG-06)
- Orphan export and file existence checks documented (INTG-01)
</success_criteria>

<output>
After completion, create `.planning/phases/56-plan-checker-enhancement/56-03-SUMMARY.md`
</output>
