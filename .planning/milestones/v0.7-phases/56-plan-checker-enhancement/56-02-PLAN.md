---
phase: 56-plan-checker-enhancement
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - ~/.claude/agents/pde-plan-checker.md
autonomous: true
requirements:
  - EDGE-01
  - EDGE-02
  - EDGE-03
  - EDGE-04
  - EDGE-05
  - EDGE-06

must_haves:
  truths:
    - "Plan checker surfaces uncovered error paths, empty states, and boundary conditions from task descriptions"
    - "Each edge case finding references a specific plan element (file path, function name, or state field)"
    - "Findings are capped at 5-8 per plan with a relevance filter"
    - "All edge case findings are CONCERNS severity in the checker issue output, never FAIL/blocker"
    - "HIGH severity edge cases include BDD acceptance criteria candidates (Given/When/Then)"
    - "The orchestrator (plan-phase.md) handles user approval of BDD ACs, not the checker"
  artifacts:
    - path: "~/.claude/agents/pde-plan-checker.md"
      provides: "Dimension 10: Edge Cases analysis pass"
      contains: "Dimension 10"
  key_links:
    - from: "pde-plan-checker.md Dimension 10"
      to: "EDGE-CASES.md"
      via: "Write via Bash tool after LLM reasoning pass"
      pattern: "EDGE-CASES.md"
    - from: "pde-plan-checker.md Dimension 10"
      to: "plan-phase.md orchestrator"
      via: "high_severity_acs field in checker return structure"
      pattern: "high_severity_acs"
---

<objective>
Add Dimension 10 (Edge Cases) to pde-plan-checker agent and add the EDGE-06 AC approval gate to plan-phase.md orchestrator. The checker uses LLM reasoning to identify uncovered error paths, empty states, and boundary conditions in task descriptions, caps findings at 5-8 per plan, classifies severity, generates BDD candidates for HIGH findings, and writes EDGE-CASES.md. The orchestrator handles user approval of generated ACs.

Purpose: Surface edge cases at planning time (as CONCERNS, never FAIL) so users can add acceptance criteria before execution discovers gaps. HIGH severity findings get BDD AC candidates that the user explicitly approves.
Output: Updated pde-plan-checker.md with Dimension 10, updated plan-phase.md with AC approval gate.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-plan-checker-enhancement/56-RESEARCH.md
@.planning/phases/56-plan-checker-enhancement/56-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dimension 10 (Edge Cases) to pde-plan-checker</name>
  <files>~/.claude/agents/pde-plan-checker.md</files>
  <read_first>
    - ~/.claude/agents/pde-plan-checker.md (current checker — now has Dimension 9 from plan 01; understand placement pattern)
    - .planning/phases/56-plan-checker-enhancement/56-RESEARCH.md (Pattern 2: Edge Case LLM Reasoning Pass, severity classification, BDD candidate format, Pitfall 1 and Pitfall 6)
  </read_first>
  <action>
Add a new `## Dimension 10: Edge Cases` section to pde-plan-checker.md, placed after Dimension 9 (Cross-Phase Dependencies) and before `</verification_dimensions>`.

The dimension content must include:

**Question:** "Are there uncovered error paths, empty states, or boundary conditions in the plan tasks?"

**Process (step-by-step):**
1. For each plan in the phase, read all `<task>` elements
2. Extract the `<action>`, `<acceptance_criteria>`, `<files>`, and `<done>` content from each task
3. Apply LLM reasoning to identify edge cases in these categories:
   - **Error paths:** State transitions that can visibly fail, write operations without rollback, file operations without error handling
   - **Empty states:** Missing handling for zero-item lists, null/undefined inputs, first-run scenarios
   - **Boundary conditions:** Input validation gaps, size/length limits not specified, concurrent access patterns
4. For each candidate finding, verify it references a SPECIFIC plan element — one of:
   - A file path from `<files>` (e.g., `bin/lib/readiness.cjs`)
   - A function name mentioned in `<action>` (e.g., `writeReadinessMd()`)
   - A state field referenced in `<action>` or `<done>` (e.g., `disk_status`)
   - If the finding can only reference "the plan in general" or a generic category, DROP IT (EDGE-02 compliance)
5. Classify each valid finding by severity:
   - **HIGH:** Missing error handling for a state transition that can visibly fail; missing validation for an input that could corrupt state; missing rollback for a multi-step write operation
   - **MEDIUM:** Missing empty-state handling; missing rate limit / retry logic; missing auth check on a write path
   - **LOW:** Missing logging; missing optional validation (advisory only); performance edge case with no correctness impact
6. Apply cap: rank by severity (HIGH > MEDIUM > LOW), take top 5-8 findings (EDGE-03 hard cap). If more than 8 found, drop lowest-severity items first.
7. For each HIGH severity finding, generate BDD acceptance criteria candidates inline (same reasoning pass, no second agent call):
   ```
   Given [precondition from task context]
   When [action from task or adjacent task]
   Then [expected behavior that is currently unspecified]
   ```
8. Write EDGE-CASES.md to the phase directory

**CRITICAL CONSTRAINT (EDGE-04):** ALL edge case findings MUST use `severity: "concerns"` in the checker issue_structure. The HIGH/MEDIUM/LOW classification describes the edge case risk level and is stored in the finding payload (`severity_level` field), NOT in the checker issue severity. This prevents edge cases from triggering mandatory revision loops.

**EDGE-CASES.md artifact format:**
```markdown
---
phase: {phase-slug}
generated: "{ISO timestamp}"
finding_count: {N}
high_count: {N}
has_bdd_candidates: {true | false}
---

# Phase {N}: Edge Cases

**Generated:** {timestamp}
**Findings:** {N} (cap: 8)
**HIGH severity:** {N}
**BDD candidates:** {yes | no}

## Findings

### {N}. [{severity_level}] {description}

**Plan element:** `{file_path | function_name() | state.field_name}`
**Category:** {error_path | empty_state | boundary_condition}

{description of the uncovered edge case}

{If HIGH severity:}
**BDD Acceptance Criteria Candidate:**
```
Given {precondition}
When {action}
Then {expected behavior}
```

{Repeat for each finding}
```

**Issue output format (for each finding):**
```yaml
issue:
  plan: "{phase}-{plan_number}"
  dimension: "edge_cases"
  severity: "concerns"          # ALWAYS "concerns" — NEVER "blocker" (EDGE-04)
  description: "{description of uncovered edge case}"
  finding:
    severity_level: "{HIGH | MEDIUM | LOW}"   # Edge case risk, NOT checker issue severity
    plan_element: "{specific file/function/state reference}"
    category: "{error_path | empty_state | boundary_condition}"
    bdd_candidates:             # Only present for HIGH severity_level
      - "Given {precondition} When {action} Then {expected behavior}"
```

**Return structure addition:** When HIGH severity findings exist, include a `high_severity_acs` field in the checker's structured return:
```json
{
  "high_severity_acs": [
    {
      "plan": "{phase}-{plan_number}",
      "task_name": "{task name from plan}",
      "plan_element": "{file/function/state}",
      "bdd_candidates": [
        "Given ... When ... Then ..."
      ]
    }
  ]
}
```
This field is consumed by plan-phase.md orchestrator for the EDGE-06 approval gate.
  </action>
  <verify>
    <automated>grep -c "Dimension 10" ~/.claude/agents/pde-plan-checker.md | grep -q "^[1-9]" && grep "edge_cases" ~/.claude/agents/pde-plan-checker.md | head -1 && grep "EDGE-CASES.md" ~/.claude/agents/pde-plan-checker.md | head -1 && grep "severity.*concerns.*ALWAYS\|ALWAYS.*concerns" ~/.claude/agents/pde-plan-checker.md | head -1 && grep "severity_level.*HIGH\|HIGH.*severity_level" ~/.claude/agents/pde-plan-checker.md | head -1 && grep "bdd_candidates" ~/.claude/agents/pde-plan-checker.md | head -1 && grep "5-8\|cap.*8\|8.*cap" ~/.claude/agents/pde-plan-checker.md | head -1 && grep "plan_element" ~/.claude/agents/pde-plan-checker.md | head -1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "## Dimension 10: Edge Cases" ~/.claude/agents/pde-plan-checker.md returns a match
    - grep "edge_cases" ~/.claude/agents/pde-plan-checker.md returns at least 1 match (dimension name in issue structure)
    - grep "EDGE-CASES.md" ~/.claude/agents/pde-plan-checker.md returns at least 2 matches (artifact write + reference)
    - grep 'severity.*"concerns"' ~/.claude/agents/pde-plan-checker.md shows the ALWAYS concerns constraint (EDGE-04)
    - grep "severity_level" ~/.claude/agents/pde-plan-checker.md returns at least 1 match (distinct from checker issue severity)
    - grep "bdd_candidates" ~/.claude/agents/pde-plan-checker.md returns at least 1 match
    - grep "plan_element" ~/.claude/agents/pde-plan-checker.md returns at least 1 match (specific element reference requirement)
    - grep "5-8" ~/.claude/agents/pde-plan-checker.md returns at least 1 match (cap documented)
    - grep "high_severity_acs" ~/.claude/agents/pde-plan-checker.md returns at least 1 match (return structure for orchestrator)
    - grep "EDGE-04" ~/.claude/agents/pde-plan-checker.md returns at least 1 match (constraint reference)
    - The dimension is placed AFTER Dimension 9 and BEFORE </verification_dimensions>
  </acceptance_criteria>
  <done>pde-plan-checker.md contains Dimension 10 with LLM reasoning process, severity classification (HIGH/MEDIUM/LOW in payload, always "concerns" in issue), 5-8 cap, BDD candidate generation for HIGH findings, plan_element requirement, EDGE-CASES.md artifact format, and high_severity_acs return field</done>
</task>

<task type="auto">
  <name>Task 2: Add EDGE-06 AC approval gate to plan-phase.md orchestrator</name>
  <files>~/.claude/pde-os/engines/gsd/workflows/plan-phase.md</files>
  <read_first>
    - ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md (current orchestrator — understand Step 11 Handle Checker Return and Step 12 Revision Loop structure)
    - .planning/phases/56-plan-checker-enhancement/56-RESEARCH.md (Pattern 5: Interactive AC Approval Gate, Pitfall 4: AC Approval Loop Triggers Full Checker Re-Run)
  </read_first>
  <action>
Add a new step "## 11.5. Edge Case AC Approval Gate" to plan-phase.md, placed BETWEEN Step 11 (Handle Checker Return) and Step 12 (Revision Loop). This step runs OUTSIDE the revision loop to prevent Pitfall 4 (infinite re-check after AC append).

The step content must be:

```markdown
## 11.5. Edge Case AC Approval Gate

**Skip if:** Checker return does not contain `high_severity_acs` field, or `high_severity_acs` array is empty.

**If `high_severity_acs` present and non-empty:**

1. Display banner:
```bash
node "$HOME/.claude/pde-os/lib/ui/render.cjs" banner "EDGE CASE REVIEW"
```

2. For each entry in `high_severity_acs`, display:

```
**Edge Case:** {plan_element}
**Plan:** {plan}
**Task:** {task_name}

Suggested acceptance criteria:

{For each bdd_candidate:}
  {N}. Given {precondition}
     When {action}
     Then {expected behavior}

```

3. Prompt user for approval:

Use AskUserQuestion:
- header: "Edge Case ACs"
- question: "Which acceptance criteria would you like to add to the plan? (Enter numbers like '1,3' or 'all' or 'none')"
- options: free-form input

4. Parse user response:
   - "none" or empty → skip, proceed to Step 12
   - "all" → approve all candidates
   - Comma-separated numbers → approve only those candidates

5. For each approved AC, append to the relevant PLAN.md task's `<acceptance_criteria>` block using the Edit tool:
   - Find the task by matching `task_name` in the plan file
   - Append the BDD AC as a new bullet: `- Given {precondition} When {action} Then {expected behavior}`

6. If any ACs were appended, commit:
```bash
node "$HOME/.claude/pde-os/engines/gsd/bin/gsd-tools.cjs" commit "docs(${padded_phase}): append approved edge case ACs" --files ${PHASE_DIR}/*-PLAN.md
```

**CRITICAL:** This step does NOT re-invoke the checker. The AC append is additive-only. After this step, proceed directly to Step 12 (or Step 13 if checker already passed).
```

Also update Step 11 to extract `high_severity_acs` from the checker return and pass it to Step 11.5.

In Step 11, after the existing logic for detecting "## ISSUES FOUND", add:
```
# Extract edge case ACs for Step 11.5
HIGH_ACS={extract high_severity_acs from checker return if present}
```

Ensure the flow is: Step 10 (checker) -> Step 11 (parse return) -> Step 11.5 (AC approval, if applicable) -> Step 12 (revision loop, if issues found) or Step 13 (if passed).

**Important ordering note:** The AC approval gate runs AFTER the checker returns but BEFORE the revision loop. If the checker found both blockers AND edge case ACs, the user sees and approves ACs first, then the revision loop handles blockers. This prevents the planner from being unaware of user-approved ACs during revision.
  </action>
  <verify>
    <automated>grep -c "11.5" ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md | grep -q "^[1-9]" && grep "high_severity_acs" ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md | head -1 && grep "EDGE CASE REVIEW\|Edge Case AC\|AC Approval" ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md | head -1 && grep "does NOT re-invoke\|NOT.*re-invoke\|not.*re-invoke\|additive-only" ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md | head -1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "11.5" ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md returns at least 1 match (step number)
    - grep "high_severity_acs" ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md returns at least 2 matches (extraction + processing)
    - grep "EDGE CASE REVIEW\|Edge Case" ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md returns at least 1 match (banner text)
    - grep "additive-only\|does NOT re-invoke" ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md returns at least 1 match (Pitfall 4 prevention documented)
    - grep "AskUserQuestion\|approval\|approve" ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md returns at least 1 match for the AC approval prompt
    - Step 11.5 appears BETWEEN Step 11 and Step 12 in the document structure
  </acceptance_criteria>
  <done>plan-phase.md contains Step 11.5 with AC approval gate that displays HIGH edge case BDD candidates, prompts user for selection, appends approved ACs to PLAN.md, and explicitly does NOT re-invoke the checker (preventing Pitfall 4 infinite loop)</done>
</task>

</tasks>

<verification>
- Dimension 10 section exists in pde-plan-checker.md between Dimension 9 and </verification_dimensions>
- All EDGE requirement IDs (EDGE-01 through EDGE-06) are addressed
- EDGE-04 constraint (always CONCERNS, never FAIL) is explicitly documented
- EDGE-06 AC approval gate exists in plan-phase.md as Step 11.5
- AC approval gate runs OUTSIDE the revision loop (Pitfall 4 prevention)
- high_severity_acs bridges checker output to orchestrator input
</verification>

<success_criteria>
- pde-plan-checker.md has Dimension 10 with complete specification
- plan-phase.md has Step 11.5 with AC approval gate
- Dimension 10 findings are always CONCERNS severity (EDGE-04)
- BDD candidates generated inline for HIGH severity findings (EDGE-05)
- User approval required before ACs are appended (EDGE-06)
- Cap of 5-8 findings enforced (EDGE-03)
</success_criteria>

<output>
After completion, create `.planning/phases/56-plan-checker-enhancement/56-02-SUMMARY.md`
</output>
