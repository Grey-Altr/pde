---
phase: 55-research-validation-agent
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
files_modified:
  - .planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md
autonomous: true
requirements:
  - RVAL-03
  - RVAL-06

must_haves:
  truths:
    - "Running the agent against 54-RESEARCH.md produces a RESEARCH-VALIDATION.md with claims labeled VERIFIED, UNVERIFIABLE, or CONTRADICTED"
    - "RESEARCH-VALIDATION.md has validated_at_phase in YAML frontmatter"
    - "Each claim in the output has a tier classification (T1, T2, or T3)"
    - "The agent did not write any file directly — the orchestrating task wrote the artifact"
  artifacts:
    - path: ".planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md"
      provides: "Proof-of-concept validation artifact"
      contains: "validated_at_phase"
  key_links:
    - from: "agents/pde-research-validator.md"
      to: ".planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md"
      via: "agent returns artifact_content, task writes to disk"
      pattern: "validated_at_phase"
---

<objective>
Smoke-test pde-research-validator against Phase 54's RESEARCH.md to produce a real RESEARCH-VALIDATION.md artifact, proving the agent works end-to-end and the output format is correct.

Purpose: Validate that the agent definition from Plan 01 produces correct output when invoked against real research content. This also produces the first RESEARCH-VALIDATION.md artifact in the project, proving RVAL-03 (produces output) and RVAL-06 (validated_at_phase in frontmatter).

Output: .planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md (proof-of-concept validation)
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-research-validation-agent/55-RESEARCH.md
@.planning/phases/55-research-validation-agent/55-01-SUMMARY.md

<interfaces>
<!-- Agent file created in Plan 01 -->
From agents/pde-research-validator.md:
- 7-step process: parse context, read research, extract claims, classify tiers, verify, classify results, build return
- Return format: JSON with status, summary, claims[], artifact_content
- Constraint: READ-ONLY, no Write/Edit/Bash-redirection

<!-- Research file to validate against -->
From .planning/phases/54-tech-debt-closure/54-RESEARCH.md:
- Phase 54 research about tech debt closure
- Contains structural claims (file exists), content claims (pattern present), potentially behavioral claims
- Good test candidate because it references codebase internals that can be verified
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke pde-research-validator against 54-RESEARCH.md and write output</name>
  <files>.planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md</files>
  <read_first>
    - agents/pde-research-validator.md (the agent definition to follow)
    - templates/research-validation.md (the output template to match)
    - .planning/phases/54-tech-debt-closure/54-RESEARCH.md (the research file to validate)
  </read_first>
  <action>
This task simulates the orchestrator role for the research validator agent. The executor MUST:

1. **Read the agent definition** at `agents/pde-research-validator.md` to understand the 7-step process.

2. **Execute the agent's 7-step process manually** (since we are proving the agent works, not yet wiring it into plan-phase.md — that is Phase 57 scope):

   **Step 1 — Context:** `research_file_path = .planning/phases/54-tech-debt-closure/54-RESEARCH.md`, `validated_at_phase = 55` (the phase at which validation is being run).

   **Step 2 — Read:** Load the full 54-RESEARCH.md file.

   **Step 3 — Extract claims:** Using the agent's three-criteria filter (assertion about codebase, specific enough to check, factual finding), identify 5-15 verifiable claims from the research content. Focus on Standard Stack, Architecture Patterns, Code Examples sections.

   **Step 4 — Classify tiers:** Assign T1 (structural/file existence), T2 (content/pattern presence), or T3 (behavioral/logic) to each claim.

   **Step 5 — Verify:** For each claim, use the tier-appropriate tools:
   - T1: Use Glob or `test -f` to check file existence
   - T2: Use Grep to check for patterns in files
   - T3: Use Read to inspect logic, or classify as UNVERIFIABLE if runtime-dependent

   **Step 6 — Classify results:** Apply the three-state labels:
   - VERIFIED: positive evidence found
   - UNVERIFIABLE: no codebase evidence possible (external/runtime claims)
   - CONTRADICTED: positive evidence of CONFLICT (not just absence)

   **Step 7 — Build output:** Construct the RESEARCH-VALIDATION.md content following `templates/research-validation.md` format exactly.

3. **Write the artifact** to `.planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md` — this simulates the orchestrator writing the agent's `artifact_content` to disk.

   The file MUST have:
   - YAML frontmatter with: `phase: 54-tech-debt-closure`, `research_file: .planning/phases/54-tech-debt-closure/54-RESEARCH.md`, `validated_at_phase: 55`, `validated_at: {current ISO timestamp}`, `total_claims: N`, `verified_count: N`, `unverifiable_count: N`, `contradicted_count: N`, `result: {PASS|CONCERNS|FAIL}`
   - Claims table with all extracted claims
   - Detail section with per-claim evidence
   - Summary section with counts

4. **Result aggregation:** If contradicted_count > 0: FAIL. If unverifiable_count > 0: CONCERNS. Else: PASS.

IMPORTANT: The executor writes the file directly (simulating the orchestrator role). The agent definition itself is read-only — this task proves that the orchestrator-writes-artifact pattern works.
  </action>
  <verify>
    <automated>grep "validated_at_phase: 55" .planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md && grep -c "VERIFIED\|UNVERIFIABLE\|CONTRADICTED" .planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md | grep -v "^0$" && grep "result:" .planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md exists
    - File has YAML frontmatter containing `validated_at_phase: 55`
    - File has YAML frontmatter containing `result:` with value PASS, CONCERNS, or FAIL
    - File has YAML frontmatter containing `total_claims:` with a number >= 3
    - File contains `## Claims` section with a markdown table
    - File contains at least 3 claim rows in the table (grep -c "| T[123] |" returns >= 3)
    - File contains `## Detail` section with per-claim breakdowns
    - File contains `## Summary` section with metric counts
    - Each claim row has one of: VERIFIED, UNVERIFIABLE, or CONTRADICTED
    - File contains `pde-research-validator` as the validator attribution
    - grep "validated_at_phase" on the first 10 lines confirms it is in frontmatter (not just body)
  </acceptance_criteria>
  <done>A real RESEARCH-VALIDATION.md exists for Phase 54's research, with validated_at_phase: 55 in frontmatter, all claims labeled with tier and three-state status, and correct result aggregation</done>
</task>

</tasks>

<verification>
1. `test -f .planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md` exits 0
2. `head -10 .planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md | grep "validated_at_phase: 55"` matches
3. `grep -c "| T[123] |" .planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md` shows 3+
4. `grep "result:" .planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md` shows PASS, CONCERNS, or FAIL
5. `grep -c "VERIFIED\|UNVERIFIABLE\|CONTRADICTED" .planning/phases/54-tech-debt-closure/54-RESEARCH-VALIDATION.md` shows >= 3
</verification>

<success_criteria>
- RESEARCH-VALIDATION.md exists with correct YAML frontmatter including validated_at_phase
- At least 3 claims extracted, classified by tier, and verified with three-state labels
- Result aggregation logic applied correctly (FAIL if contradicted, CONCERNS if unverifiable, PASS otherwise)
- Output format matches templates/research-validation.md structure
- The artifact proves RVAL-03 (produces RESEARCH-VALIDATION.md) and RVAL-06 (validated_at_phase field)
</success_criteria>

<output>
After completion, create `.planning/phases/55-research-validation-agent/55-02-SUMMARY.md`
</output>
