---
phase: 54-tech-debt-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/critique.md
  - workflows/iterate.md
  - workflows/handoff.md
  - bin/lib/mcp-bridge.cjs
  - bin/pde-tools.cjs
autonomous: true
requirements: [DEBT-04, DEBT-06, DEBT-07]

must_haves:
  truths:
    - "All lock-release bash code blocks in workflow files have no trailing arguments"
    - "TOOL_MAP pre-registered entries have TOOL_MAP_PREREGISTERED annotation comments"
    - "pde-tools.cjs help text lists manifest, readiness, and tracking commands"
  artifacts:
    - path: "workflows/critique.md"
      provides: "Normalized lock-release call"
      contains: 'design lock-release"'
    - path: "workflows/iterate.md"
      provides: "Normalized lock-release call"
      contains: 'design lock-release"'
    - path: "workflows/handoff.md"
      provides: "Normalized lock-release call"
      contains: 'design lock-release"'
    - path: "bin/lib/mcp-bridge.cjs"
      provides: "TOOL_MAP_PREREGISTERED annotations"
      contains: "TOOL_MAP_PREREGISTERED"
    - path: "bin/pde-tools.cjs"
      provides: "Complete help text with v0.6 commands"
      contains: "manifest init"
  key_links: []
---

<objective>
Normalize cosmetic inconsistencies in workflow files, annotate pre-registered TOOL_MAP entries, and update pde-tools.cjs help text to include v0.6 commands.

Purpose: Clean up 3 of 7 tech debt items that are quick code edits with zero functional change.
Output: 5 files updated with cosmetic normalizations and documentation improvements.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-tech-debt-closure/54-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize lock-release trailing arguments in workflow files</name>
  <files>workflows/critique.md, workflows/iterate.md, workflows/handoff.md</files>
  <read_first>workflows/critique.md, workflows/iterate.md, workflows/handoff.md</read_first>
  <action>
In three workflow files, remove the trailing argument from the lock-release bash code block call. Each file has exactly one bash code block with a trailing arg. The change is purely cosmetic -- `cmdLockRelease` in pde-tools.cjs ignores trailing args.

1. `workflows/critique.md` line 614: Change
   `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design lock-release critique`
   to
   `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design lock-release`

2. `workflows/iterate.md` line 355: Change
   `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design lock-release iterate`
   to
   `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design lock-release`

3. `workflows/handoff.md` line 611: Change
   `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design lock-release pde-handoff`
   to
   `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design lock-release`

IMPORTANT: Only modify the bash code block call sites. Do NOT alter prose references like "via `lock-release critique`" or "via `lock-release iterate`" in guidelines/process sections -- those are descriptive text, not executable code.
  </action>
  <verify>
    <automated>grep -n "lock-release [a-z]" workflows/critique.md workflows/iterate.md workflows/handoff.md | grep -v "^.*:.*- " | grep -v "^.*:.*via " | grep -v "^.*:.*Skip " | grep -v "^.*:.*Always " || echo "PASS: no trailing args in code blocks"</automated>
  </verify>
  <acceptance_criteria>
- `grep 'lock-release critique' workflows/critique.md` returns only prose lines (not inside bash code blocks)
- `grep 'lock-release iterate' workflows/iterate.md` returns only prose lines (not inside bash code blocks)
- `grep 'lock-release pde-handoff' workflows/handoff.md` returns only prose lines (not inside bash code blocks)
- Each file contains exactly one line matching `design lock-release"` (the normalized form ending with quote)
  </acceptance_criteria>
  <done>All 3 workflow files have lock-release bash code blocks with no trailing arguments. Prose references unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate TOOL_MAP pre-registered entries</name>
  <files>bin/lib/mcp-bridge.cjs</files>
  <read_first>bin/lib/mcp-bridge.cjs</read_first>
  <action>
Add `// TOOL_MAP_PREREGISTERED` inline comments to the two pre-registered TOOL_MAP entries that have no consumers in any operational file.

In `bin/lib/mcp-bridge.cjs`, change lines 90 and 93:

Line 90 -- change:
```javascript
'github:update-pr':           'mcp__github__update_pull_request',
```
to:
```javascript
'github:update-pr':           'mcp__github__update_pull_request',  // TOOL_MAP_PREREGISTERED
```

Line 93 -- change:
```javascript
'github:search-issues':       'mcp__github__search_issues',
```
to:
```javascript
'github:search-issues':       'mcp__github__search_issues',         // TOOL_MAP_PREREGISTERED
```

Do NOT modify any other TOOL_MAP entries. Do NOT remove these entries.
  </action>
  <verify>
    <automated>grep -c "TOOL_MAP_PREREGISTERED" bin/lib/mcp-bridge.cjs | grep -q "2" && echo "PASS: 2 annotations" || echo "FAIL: expected 2 annotations"</automated>
  </verify>
  <acceptance_criteria>
- `grep "TOOL_MAP_PREREGISTERED" bin/lib/mcp-bridge.cjs` returns exactly 2 lines
- `grep "github:update-pr" bin/lib/mcp-bridge.cjs` contains `// TOOL_MAP_PREREGISTERED`
- `grep "github:search-issues" bin/lib/mcp-bridge.cjs` contains `// TOOL_MAP_PREREGISTERED`
  </acceptance_criteria>
  <done>Both pre-registered TOOL_MAP entries annotated with TOOL_MAP_PREREGISTERED comment. Future integration checks will not flag these as orphans.</done>
</task>

<task type="auto">
  <name>Task 3: Add missing v0.6 commands to pde-tools.cjs help text</name>
  <files>bin/pde-tools.cjs</files>
  <read_first>bin/pde-tools.cjs</read_first>
  <action>
Add three missing command sections to the header comment block in `bin/pde-tools.cjs`. The command `shard-plan` is already documented at line 67. The three missing commands are `manifest`, `readiness`, and `tracking`.

Insert the following sections into the header comment block. Place them after the `Sharding:` section (which ends at line 68) and before the `Validation:` section (which starts at line 70):

```
 *
 * File Manifest:
 *   manifest init                      Build files-manifest.csv from plugin root
 *   manifest check                     Check manifest entries against disk hashes
 *
 * Readiness:
 *   readiness check <phase> [plan]     Run readiness checks against a plan
 *   readiness result <phase>           Read last readiness result
 *
 * Task Tracking:
 *   tracking init <phase> <plan>       Initialize workflow-status.md for a plan
 *     [--names 'Task A|Task B']
 *   tracking set-status <phase> <plan> Update task status in workflow-status.md
 *   tracking read <phase> <plan>       Read current workflow status
```

This adds documentation for commands that are already implemented:
- `manifest` at case 'manifest' in the main switch
- `readiness` at case 'readiness' in the main switch
- `tracking` at case 'tracking' in the main switch

No implementation changes -- help text only.
  </action>
  <verify>
    <automated>grep -c "manifest init\|manifest check\|readiness check\|readiness result\|tracking init\|tracking set-status\|tracking read" bin/pde-tools.cjs | grep -q "7" && echo "PASS: 7 help entries" || echo "FAIL: expected 7 help entries"</automated>
  </verify>
  <acceptance_criteria>
- `grep "manifest init" bin/pde-tools.cjs` returns a match in the comment block
- `grep "manifest check" bin/pde-tools.cjs` returns a match in the comment block
- `grep "readiness check" bin/pde-tools.cjs` returns a match in the comment block
- `grep "readiness result" bin/pde-tools.cjs` returns a match in the comment block
- `grep "tracking init" bin/pde-tools.cjs` returns a match in the comment block
- `grep "tracking set-status" bin/pde-tools.cjs` returns a match in the comment block
- `grep "tracking read" bin/pde-tools.cjs` returns a match in the comment block
  </acceptance_criteria>
  <done>pde-tools.cjs help text includes all v0.6 commands: manifest (init, check), readiness (check, result), and tracking (init, set-status, read).</done>
</task>

</tasks>

<verification>
- `grep "lock-release [a-z]" workflows/critique.md workflows/iterate.md workflows/handoff.md` returns only prose references, not bash code blocks
- `grep -c "TOOL_MAP_PREREGISTERED" bin/lib/mcp-bridge.cjs` returns 2
- `grep -c "manifest\|readiness\|tracking" bin/pde-tools.cjs` returns more than 3 matches in comment block area (lines 1-143)
</verification>

<success_criteria>
- Zero lock-release bash code blocks with trailing arguments across all workflow files
- Exactly 2 TOOL_MAP_PREREGISTERED annotations in mcp-bridge.cjs
- All 3 missing v0.6 commands documented in pde-tools.cjs help text (7 new help entries total)
</success_criteria>

<output>
After completion, create `.planning/phases/54-tech-debt-closure/54-01-SUMMARY.md`
</output>
