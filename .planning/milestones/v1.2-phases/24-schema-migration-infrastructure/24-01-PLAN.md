---
phase: 24-schema-migration-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/design-manifest.json
  - bin/lib/design.cjs
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "design-manifest.json template contains all 13 designCoverage flags in canonical order"
    - "ensureDesignDirs creates ux/mockups subdirectory on initialization"
    - "Self-test passes at 20/20 with updated label reflecting 8 domain directories"
  artifacts:
    - path: "templates/design-manifest.json"
      provides: "13-field designCoverage schema template for new projects"
      contains: "hasRecommendations"
    - path: "bin/lib/design.cjs"
      provides: "Updated DOMAIN_DIRS with ux/mockups; updated self-test label"
      contains: "ux/mockups"
  key_links:
    - from: "bin/lib/design.cjs"
      to: "templates/design-manifest.json"
      via: "ensureDesignDirs reads template to initialize new projects"
      pattern: "design-manifest\\.json"
---

<objective>
Extend the manifest template with 6 new v1.2 coverage flags and add the ux/mockups output directory to ensureDesignDirs.

Purpose: Provides the schema foundation that all v1.2 skills will write into, and creates the mockup output directory that Phase 26 needs.
Output: Updated templates/design-manifest.json (13 flags), updated bin/lib/design.cjs (DOMAIN_DIRS + self-test label)
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-schema-migration-infrastructure/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend manifest template and design.cjs infrastructure</name>
  <files>templates/design-manifest.json, bin/lib/design.cjs</files>
  <read_first>
    - templates/design-manifest.json (current 7-field designCoverage block at lines 107-116)
    - bin/lib/design.cjs (DOMAIN_DIRS at line 17, self-test label at line 388)
  </read_first>
  <action>
**File 1: templates/design-manifest.json**

Replace the `designCoverage` block (lines 107-116) with the full 13-field version. Keep the existing `_comment` key. Add 6 new fields after `hasHandoff`, all defaulting to `false`. The exact replacement block:

```json
"designCoverage": {
  "_comment": "Boolean flags indicating which design artifact types exist. PDE uses this to determine what design context is available for engineering planning.",
  "hasDesignSystem": false,
  "hasWireframes": false,
  "hasFlows": false,
  "hasHardwareSpec": false,
  "hasCritique": false,
  "hasIterate": false,
  "hasHandoff": false,
  "hasIdeation": false,
  "hasCompetitive": false,
  "hasOpportunity": false,
  "hasMockup": false,
  "hasHigAudit": false,
  "hasRecommendations": false
}
```

Do NOT add `hasBrief` -- brief completion is tracked via `artifacts.BRF` presence, not a coverage flag.

**File 2: bin/lib/design.cjs**

1. Line 17: Replace `DOMAIN_DIRS` array. Current value:
   ```javascript
   const DOMAIN_DIRS = ['assets', 'strategy', 'ux', 'visual', 'review', 'handoff', 'hardware'];
   ```
   New value:
   ```javascript
   const DOMAIN_DIRS = ['assets', 'strategy', 'ux', 'ux/mockups', 'visual', 'review', 'handoff', 'hardware'];
   ```
   `ux/mockups` must come AFTER `ux` in the array. `fs.mkdirSync` with `recursive: true` handles nested paths.

2. Line 388: Replace self-test label string. Current:
   ```javascript
   check('creates all 7 domain subdirectories', () => {
   ```
   New:
   ```javascript
   check('creates all 8 domain subdirectories (including ux/mockups)', () => {
   ```
   The loop body (`for (const domain of DOMAIN_DIRS)`) is unchanged -- it automatically iterates the updated array.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node bin/lib/design.cjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - templates/design-manifest.json contains `"hasIdeation": false`
    - templates/design-manifest.json contains `"hasCompetitive": false`
    - templates/design-manifest.json contains `"hasOpportunity": false`
    - templates/design-manifest.json contains `"hasMockup": false`
    - templates/design-manifest.json contains `"hasHigAudit": false`
    - templates/design-manifest.json contains `"hasRecommendations": false`
    - templates/design-manifest.json does NOT contain `hasBrief`
    - bin/lib/design.cjs line 17 contains `'ux/mockups'`
    - bin/lib/design.cjs contains `creates all 8 domain subdirectories (including ux/mockups)`
    - `node bin/lib/design.cjs --self-test` outputs `20 passed, 0 failed`
  </acceptance_criteria>
  <done>Template has exactly 13 designCoverage flags in canonical order. DOMAIN_DIRS has 8 entries including ux/mockups. Self-test passes 20/20 with correct label.</done>
</task>

</tasks>

<verification>
- `node bin/lib/design.cjs --self-test` passes 20/20
- `grep -c "has[A-Z]" templates/design-manifest.json` returns 13 (exactly 13 flag lines)
- `grep "ux/mockups" bin/lib/design.cjs` returns a match on the DOMAIN_DIRS line
</verification>

<success_criteria>
- Template schema extended: 13 coverage flags present in canonical order
- Directory infrastructure updated: ux/mockups in DOMAIN_DIRS
- Self-test green: 20/20 with updated label
- No regressions: existing 7 flags unchanged, existing 7 domain dirs unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/24-schema-migration-infrastructure/24-01-SUMMARY.md`
</output>
