---
phase: 119-antigravity-context-+-stitch-bridge
plan: 02
type: execute
wave: 2
depends_on:
  - 119-01
files_modified:
  - tests/phase-119/test-antigravity-stitch.cjs
autonomous: false
requirements:
  - CTX-05
  - STH-01
  - STH-02
  - STH-03

must_haves:
  truths:
    - "Generated SKILL.md is readable and contains meaningful PDE workflow instructions"
    - "Generated DESIGN.md looks like a valid Antigravity Design DNA document with proper section structure"
    - "All Phase 119 and Phase 118 tests pass together with zero regressions"
  artifacts:
    - path: "tests/phase-119/test-antigravity-stitch.cjs"
      provides: "Complete structural tests for all Phase 119 requirements"
      min_lines: 150
  key_links:
    - from: "tests/phase-119/test-antigravity-stitch.cjs"
      to: "bin/lib/context-sync.cjs"
      via: "require imports"
      pattern: "require.*context-sync"
---

<objective>
Run full test suite to verify all Phase 119 requirements are satisfied, generate sample output files for human inspection, and confirm zero regressions against Phase 118.

Purpose: Final verification that Antigravity integration works end-to-end.
Output: Verified test results, sample generated files for human review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-antigravity-context-+-stitch-bridge/119-01-SUMMARY.md
@bin/lib/context-sync.cjs
@tests/phase-119/test-antigravity-stitch.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite verification + sample output generation</name>
  <files>tests/phase-119/test-antigravity-stitch.cjs</files>
  <read_first>
    bin/lib/context-sync.cjs
    tests/phase-119/test-antigravity-stitch.cjs
    tests/phase-118/test-context-sync.cjs
  </read_first>
  <action>
    1. Run the complete test suite to verify all requirements:
       - `node --test tests/phase-119/test-antigravity-stitch.cjs` (Phase 119 tests)
       - `node --test tests/phase-118/test-context-sync.cjs` (Phase 118 regression check)

    2. If any tests fail, diagnose and fix in the test file or context-sync.cjs as needed. Common issues:
       - YAML frontmatter delimiter matching (check for --- on its own line)
       - Hex color format (ensure # prefix + exactly 6 hex chars)
       - Directory creation (recursive mkdir for .agent/skills/pde-design/)

    3. Generate sample output files for human inspection:
       - Create a temp directory with mock .planning/ data including DTCG tokens
       - Run emitAll() against it
       - Copy the generated SKILL.md and DESIGN.md to a temporary location
       - Print their contents to stdout for the human checkpoint to review

    4. Verify the 119-VALIDATION.md can be updated:
       - Confirm all Wave 0 gaps are now closed (test file exists)
       - Confirm sampling rate commands work
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && node --test tests/phase-119/test-antigravity-stitch.cjs tests/phase-118/test-context-sync.cjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/phase-119/test-antigravity-stitch.cjs exits 0
    - node --test tests/phase-118/test-context-sync.cjs exits 0
    - Both test files run together without conflicts
  </acceptance_criteria>
  <done>All Phase 119 tests pass. All Phase 118 tests pass (zero regression). Sample output files generated for review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of generated Antigravity files</name>
  <files>bin/lib/context-sync.cjs</files>
  <action>
    Present generated SKILL.md and DESIGN.md sample outputs to the user for visual verification.
    Run oklchToHex and isStitchSource smoke tests and display results.
  </action>
  <verify>Human confirms output quality and correctness.</verify>
  <done>User approves SKILL.md format, DESIGN.md format, oklchToHex accuracy, and isStitchSource behavior.</done>
  <what-built>Antigravity Agent Manager integration: SKILL.md (PDE design skill) and DESIGN.md (Design DNA format from DTCG tokens) emitters added to context-sync.cjs. Stitch source detection extended to support "antigravity-stitch" origin. cmdContextSync --editor antigravity flag added.</what-built>
  <how-to-verify>
    1. Review the sample SKILL.md output printed above -- confirm it has:
       - YAML frontmatter with name: pde-design
       - Sections: Goal, Instructions, Design Tokens Available, Component Catalog, Constraints
       - PDE-GENERATED marker in header

    2. Review the sample DESIGN.md output -- confirm it has:
       - 5 sections matching Antigravity Design DNA format
       - Hex color codes in Color Palette section (not OKLCH)
       - Typography and spacing sections populated
       - PDE-GENERATED marker in header

    3. Verify test results show all passing (0 failures across both Phase 118 and 119)

    4. Run: `cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && node -e "const cs = require('./bin/lib/context-sync.cjs'); console.log('oklchToHex black:', cs.oklchToHex('oklch(0 0 0)')); console.log('oklchToHex white:', cs.oklchToHex('oklch(1 0 0)')); console.log('isStitchSource stitch:', cs.isStitchSource('stitch')); console.log('isStitchSource antigravity-stitch:', cs.isStitchSource('antigravity-stitch')); console.log('isStitchSource pde:', cs.isStitchSource('pde'));"`
       - Expect: black=#000000, white=#ffffff, stitch=true, antigravity-stitch=true, pde=false
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `node --test tests/phase-119/test-antigravity-stitch.cjs tests/phase-118/test-context-sync.cjs` -- full suite green
2. Sample SKILL.md and DESIGN.md visually reviewed by human
3. oklchToHex and isStitchSource produce correct output via quick smoke test
</verification>

<success_criteria>
- All automated tests pass with zero failures
- Zero regressions on Phase 118 tests
- Human confirms SKILL.md and DESIGN.md output quality
- oklchToHex and isStitchSource produce correct values
</success_criteria>

<output>
After completion, create `.planning/phases/119-antigravity-context-+-stitch-bridge/119-02-SUMMARY.md`
</output>
