---
phase: 119-antigravity-context-+-stitch-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/context-sync.cjs
  - tests/phase-119/test-antigravity-stitch.cjs
autonomous: true
requirements:
  - CTX-05
  - STH-01
  - STH-02
  - STH-03

must_haves:
  truths:
    - "emitAll() produces .agent/skills/pde-design/SKILL.md with YAML frontmatter (name, description) and PDE workflow instructions"
    - "emitAll() produces DESIGN.md with 5+ sections, hex colors converted from OKLCH, typography and spacing from IR"
    - "oklchToHex() converts OKLCH color strings to valid 7-char hex codes with gamut clamping"
    - "Manifest source 'antigravity-stitch' is distinguishable from 'stitch' via isStitchSource() helper"
    - "cmdContextSync --editor antigravity emits only Antigravity files (SKILL.md + DESIGN.md)"
    - "emitDesignMd() gracefully handles missing DTCG tokens with placeholder content"
  artifacts:
    - path: "bin/lib/context-sync.cjs"
      provides: "emitAntigravitySkill, emitDesignMd, oklchToHex, isStitchSource"
      exports: ["emitAntigravitySkill", "emitDesignMd", "oklchToHex", "isStitchSource"]
    - path: "tests/phase-119/test-antigravity-stitch.cjs"
      provides: "Structural tests for CTX-05, STH-01, STH-02, STH-03"
      min_lines: 100
  key_links:
    - from: "bin/lib/context-sync.cjs:emitAll"
      to: "emitAntigravitySkill, emitDesignMd"
      via: "function call in emitAll()"
      pattern: "emitAntigravitySkill\\(ir"
    - from: "bin/lib/context-sync.cjs:cmdContextSync"
      to: "emitAntigravitySkill, emitDesignMd"
      via: "editor === 'antigravity' branch"
      pattern: "editor === 'antigravity'"
    - from: "bin/lib/context-sync.cjs:emitDesignMd"
      to: "oklchToHex"
      via: "color conversion in DESIGN.md generation"
      pattern: "oklchToHex"
---

<objective>
Add Antigravity emitter (SKILL.md) and Design DNA emitter (DESIGN.md) to context-sync.cjs, plus oklchToHex conversion, Stitch source detection helper, and structural tests for all four Phase 119 requirements.

Purpose: Enable Antigravity Agent Manager to consume PDE design state as skills and Design DNA, and support bidirectional Stitch artifact flow detection.
Output: Extended context-sync.cjs with 4 new exports, new test file covering all requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-antigravity-context-+-stitch-bridge/119-RESEARCH.md
@bin/lib/context-sync.cjs
@tests/phase-118/test-context-sync.cjs

<interfaces>
<!-- Key types and contracts the executor needs from context-sync.cjs -->

From bin/lib/context-sync.cjs:
```javascript
// IR object shape (returned by buildContextIR):
// {
//   projectName: string,
//   productType: string,
//   techStack: string,
//   projectSummary: string,
//   designTokens: string,
//   componentCatalog: string,
//   pipelineStatus: string,
//   constraints: string,
//   sourceHash: string (64-char hex SHA-256),
//   generatedAt: string (ISO 8601),
// }

function makeHeader(sourceHash, generatedAt) // returns "<!-- PDE-GENERATED | hash:... | generated:... -->"
function buildContextIR(planningDir) // returns IR object
function emitAll(cwd) // returns { agentsMd, cursorRules, cursorrules, geminiMd, sourceHash, generatedAt }
function cmdContextSync(cwd, args, raw) // CLI entry point, --editor flag selects emitters

// Current emitAll return keys: agentsMd, cursorRules, cursorrules, geminiMd
// Phase 119 adds: antigravitySkill, designMd

// Current module.exports:
module.exports = {
  buildContextIR, emitAll, emitAgentsMd, emitCursorRules,
  emitCursorrules, emitGeminiMd, computeSourceHash, cmdContextSync,
};
```

From tests/phase-118/test-context-sync.cjs (test fixture pattern):
```javascript
// Uses node:test, node:assert/strict
// createMockPlanning(tmpDir) creates .planning/ with PROJECT.md, STATE.md,
// design/DESIGN-STATE.md, design/design-manifest.json, design/handoff/*.md
// Temp dirs via os.tmpdir() + fs.mkdtempSync
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffold + oklchToHex + isStitchSource</name>
  <files>tests/phase-119/test-antigravity-stitch.cjs, bin/lib/context-sync.cjs</files>
  <read_first>
    bin/lib/context-sync.cjs
    tests/phase-118/test-context-sync.cjs
    .planning/phases/119-antigravity-context-+-stitch-bridge/119-RESEARCH.md
  </read_first>
  <behavior>
    - oklchToHex("oklch(0.7 0.15 150)") returns a valid 7-char hex string starting with #
    - oklchToHex("oklch(0 0 0)") returns "#000000" (black)
    - oklchToHex("oklch(1 0 0)") returns "#ffffff" (white)
    - oklchToHex("not-oklch") returns "not-oklch" (passthrough for non-OKLCH values)
    - oklchToHex clamps out-of-gamut values (no hex digits outside 00-ff)
    - isStitchSource("stitch") returns true
    - isStitchSource("antigravity-stitch") returns true
    - isStitchSource("pde") returns false
    - isStitchSource(undefined) returns false
  </behavior>
  <action>
    1. Create tests/phase-119/test-antigravity-stitch.cjs following the Phase 118 test pattern:
       - Import from ../../bin/lib/context-sync.cjs
       - Use node:test describe/it blocks, node:assert/strict
       - Create createMockPlanningWithTokens(tmpDir) fixture helper that extends the Phase 118 pattern with DTCG token data in design-manifest.json (add a tokens.color object with OKLCH values)
       - Write describe("oklchToHex") with the behavior tests above
       - Write describe("isStitchSource") with the behavior tests above
       - Leave placeholder describes for emitAntigravitySkill and emitDesignMd (filled in Task 2)

    2. Add oklchToHex() to context-sync.cjs:
       - Place after the "Markdown extraction helpers" section
       - Parse oklch(L C H) where L is lightness [0-1], C is chroma, H is hue in degrees
       - Convert OKLCH -> OKLAB -> linear sRGB -> gamma-encoded sRGB -> hex
       - Clamp linear RGB to [0,1] before gamma encoding (gamut safety)
       - Use the exact matrix constants from the research (Pattern 2 in 119-RESEARCH.md)
       - Passthrough non-OKLCH strings unchanged

    3. Add isStitchSource(source) to context-sync.cjs:
       - Returns true if source === "stitch" || source === "antigravity-stitch"
       - Returns false for anything else including undefined/null
       - Per STH-02: use exact equality, NOT includes() or startsWith()

    4. Export both new functions from module.exports.

    Run tests to confirm oklchToHex and isStitchSource pass.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && node --test tests/phase-119/test-antigravity-stitch.cjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "oklchToHex" bin/lib/context-sync.cjs
    - grep -q "isStitchSource" bin/lib/context-sync.cjs
    - grep -q "module.exports" bin/lib/context-sync.cjs && grep -q "oklchToHex" bin/lib/context-sync.cjs
    - grep -q "isStitchSource" bin/lib/context-sync.cjs && grep -q "antigravity-stitch" bin/lib/context-sync.cjs
    - node --test tests/phase-119/test-antigravity-stitch.cjs exits 0
    - grep -c "describe\|it(" tests/phase-119/test-antigravity-stitch.cjs shows 10+ test cases
  </acceptance_criteria>
  <done>oklchToHex converts OKLCH to hex with gamut clamping. isStitchSource detects both "stitch" and "antigravity-stitch". All tests pass. Test scaffold ready for Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: emitAntigravitySkill + emitDesignMd + emitAll/cmdContextSync extension</name>
  <files>bin/lib/context-sync.cjs, tests/phase-119/test-antigravity-stitch.cjs</files>
  <read_first>
    bin/lib/context-sync.cjs
    tests/phase-119/test-antigravity-stitch.cjs
    .planning/phases/119-antigravity-context-+-stitch-bridge/119-RESEARCH.md
  </read_first>
  <behavior>
    - emitAntigravitySkill(ir, projectRoot) creates .agent/skills/pde-design/SKILL.md
    - SKILL.md contains YAML frontmatter with "name: pde-design" and "description:" fields
    - SKILL.md contains PDE-GENERATED header comment with hash
    - SKILL.md body has sections: Goal, Instructions, Design Tokens Available, Component Catalog, Constraints
    - SKILL.md references ir.designTokens and ir.componentCatalog content
    - emitDesignMd(ir, projectRoot, planningDir) creates DESIGN.md at project root
    - DESIGN.md has PDE-GENERATED header, 5+ sections (Visual Theme, Color Palette, Typography, Component Stylings, Layout Principles)
    - DESIGN.md Color Palette section contains hex codes (not OKLCH) when DTCG tokens have OKLCH values
    - emitDesignMd gracefully handles missing tokens (produces placeholder DESIGN.md)
    - emitAll() return object includes antigravitySkill and designMd keys
    - cmdContextSync with --editor antigravity emits only SKILL.md and DESIGN.md
    - cmdContextSync with --editor all includes antigravity files
    - Existing Phase 118 tests still pass (no regression)
  </behavior>
  <action>
    1. Complete the test file (tests/phase-119/test-antigravity-stitch.cjs):
       - Fill in describe("emitAntigravitySkill") tests:
         * Creates .agent/skills/pde-design/SKILL.md
         * SKILL.md contains "name: pde-design" in YAML frontmatter (between --- delimiters)
         * SKILL.md contains "description:" field
         * SKILL.md contains PDE-GENERATED marker
         * SKILL.md body contains "## Goal", "## Instructions", "## Design Tokens Available", "## Component Catalog", "## Constraints"
       - Fill in describe("emitDesignMd") tests:
         * Creates DESIGN.md at project root
         * DESIGN.md contains PDE-GENERATED marker
         * Contains "## 1. Visual Theme", "## 2. Color Palette", "## 3. Typography Rules", "## 4. Component Stylings", "## 5. Layout Principles"
         * When manifest has OKLCH tokens, Color Palette section contains hex codes (match /#[0-9a-f]{6}/i)
         * When no tokens exist, DESIGN.md still generates with placeholder text ("not yet generated" or similar)
       - Fill in describe("emitAll extension") tests:
         * emitAll returns object with antigravitySkill key
         * emitAll returns object with designMd key
         * Both .agent/skills/pde-design/SKILL.md and DESIGN.md exist after emitAll
       - Fill in describe("cmdContextSync --editor antigravity") tests:
         * Calling cmdContextSync with --editor antigravity produces SKILL.md and DESIGN.md
         * Does NOT produce .cursor/rules/ or GEMINI.md

    2. Add emitAntigravitySkill(ir, projectRoot) to context-sync.cjs:
       - Create .agent/skills/pde-design/ directory (fs.mkdirSync recursive)
       - Generate SKILL.md with:
         * PDE-GENERATED header from makeHeader()
         * YAML frontmatter: name: pde-design, description: "PDE design system context -- query palette colors, typography rules, spacing scale, and component patterns for the current project"
         * Markdown body sections: Goal (referencing ir.projectName), Instructions (3 numbered steps about DESIGN.md, SYS-tokens.json, handoff specs), Design Tokens Available (ir.designTokens), Component Catalog (ir.componentCatalog), Constraints (use hex from DESIGN.md, follow typography hierarchy, use spacing base unit)
       - Return { written: true, path: '.agent/skills/pde-design/SKILL.md' }

    3. Add emitDesignMd(ir, projectRoot, planningDir) to context-sync.cjs:
       - Read design-manifest.json for token data: look for manifest.tokens.color entries with OKLCH values
       - Also check for SYS-tokens.json at planningDir/design/SYS-tokens.json for raw DTCG data
       - If tokens found:
         * Section 1 (Visual Theme): brief mood paragraph from ir.projectSummary
         * Section 2 (Color Palette): convert each OKLCH token value to hex via oklchToHex(), list with semantic role names (Primary, Secondary, Surface, etc.)
         * Section 3 (Typography Rules): extract font family and hierarchy from token data or ir.designTokens
         * Section 4 (Component Stylings): list components from ir.componentCatalog with style hints
         * Section 5 (Layout Principles): spacing scale from tokens
       - If NO tokens found: emit a minimal DESIGN.md with placeholder text "Design tokens not yet generated -- run the PDE design pipeline to populate this file."
       - Always include PDE-GENERATED header
       - Return { written: true, path: 'DESIGN.md' } or { written: true, path: 'DESIGN.md', placeholder: true }

    4. Update emitAll():
       - Add: const antigravitySkill = emitAntigravitySkill(ir, projectRoot);
       - Add: const designMd = emitDesignMd(ir, projectRoot, planningDir);
       - Add both to return object

    5. Update cmdContextSync():
       - Add 'antigravity' branch: emits emitAntigravitySkill + emitDesignMd only
       - Update 'all' branch to use emitAll (already calls both via emitAll)
       - Update error message to include 'antigravity' in available editors list

    6. Update module.exports to include emitAntigravitySkill, emitDesignMd (oklchToHex and isStitchSource already added in Task 1)

    7. Run both Phase 119 AND Phase 118 tests to confirm no regressions.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && node --test tests/phase-119/test-antigravity-stitch.cjs tests/phase-118/test-context-sync.cjs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "emitAntigravitySkill" bin/lib/context-sync.cjs
    - grep -q "emitDesignMd" bin/lib/context-sync.cjs
    - grep -q "antigravitySkill" bin/lib/context-sync.cjs && grep -q "designMd" bin/lib/context-sync.cjs
    - grep -q "editor === 'antigravity'" bin/lib/context-sync.cjs
    - grep -q "Visual Theme" bin/lib/context-sync.cjs
    - grep -q "Color Palette" bin/lib/context-sync.cjs
    - grep -q "Typography Rules" bin/lib/context-sync.cjs
    - grep -q "name: pde-design" bin/lib/context-sync.cjs
    - grep -q "antigravity-stitch" bin/lib/context-sync.cjs
    - node --test tests/phase-119/test-antigravity-stitch.cjs exits 0
    - node --test tests/phase-118/test-context-sync.cjs exits 0 (no regression)
  </acceptance_criteria>
  <done>emitAntigravitySkill produces SKILL.md with YAML frontmatter and PDE instructions. emitDesignMd produces DESIGN.md with 5 sections and hex-converted colors. emitAll and cmdContextSync include both new emitters. All Phase 119 and 118 tests pass.</done>
</task>

</tasks>

<verification>
1. `node --test tests/phase-119/test-antigravity-stitch.cjs` -- all Phase 119 structural tests pass
2. `node --test tests/phase-118/test-context-sync.cjs` -- zero regressions on Phase 118
3. `grep -c "emitAntigravitySkill\|emitDesignMd\|oklchToHex\|isStitchSource" bin/lib/context-sync.cjs` -- all 4 new functions present
4. `grep "module.exports" bin/lib/context-sync.cjs` -- all 4 new functions exported
</verification>

<success_criteria>
- emitAll() produces both .agent/skills/pde-design/SKILL.md and DESIGN.md
- SKILL.md has valid YAML frontmatter with name: pde-design and description
- DESIGN.md has 5+ sections with hex color codes (not OKLCH) when tokens available
- DESIGN.md gracefully handles missing tokens with placeholder content
- isStitchSource() correctly identifies both "stitch" and "antigravity-stitch"
- cmdContextSync --editor antigravity emits only Antigravity files
- Phase 118 tests still pass (zero regression)
- All 4 requirement IDs (CTX-05, STH-01, STH-02, STH-03) have test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/119-antigravity-context-+-stitch-bridge/119-01-SUMMARY.md`
</output>
