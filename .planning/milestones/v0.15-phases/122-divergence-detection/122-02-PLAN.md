---
phase: 122-divergence-detection
plan: 02
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - commands/check-divergence.md
  - workflows/check-divergence.md
autonomous: false
requirements:
  - DIV-05

must_haves:
  truths:
    - "/pde:check-divergence command exists and delegates to workflows/check-divergence.md"
    - "Workflow invokes divergence.cjs runDivergenceCheck and writes DIVERGENCE.md to project root"
    - "Running the command on a project with handoff specs produces a valid DIVERGENCE.md report"
  artifacts:
    - path: "commands/check-divergence.md"
      provides: "Slash command definition for /pde:check-divergence"
      contains: "pde:check-divergence"
    - path: "workflows/check-divergence.md"
      provides: "Workflow that runs divergence detection and writes output"
      contains: "divergence.cjs"
  key_links:
    - from: "commands/check-divergence.md"
      to: "workflows/check-divergence.md"
      via: "Follow @workflows/check-divergence.md"
      pattern: "@workflows/check-divergence"
    - from: "workflows/check-divergence.md"
      to: "bin/lib/divergence.cjs"
      via: "require + runDivergenceCheck call"
      pattern: "divergence\\.cjs"
---

<objective>
Wire the divergence detection engine into a /pde:check-divergence slash command with workflow, completing the user-facing interface.

Purpose: Users need an on-demand command to detect handoff-to-code drift. The core engine exists from Plan 01; this plan creates the command entry point and workflow orchestration.
Output: commands/check-divergence.md, workflows/check-divergence.md (functional command)
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-divergence-detection/122-01-SUMMARY.md
@.planning/phases/122-divergence-detection/122-RESEARCH.md

@commands/pipeline-status.md
@workflows/mcp-status.md
@bin/lib/divergence.cjs
</context>

<interfaces>
<!-- From Plan 01 divergence.cjs exports -->

```javascript
module.exports = {
  extractAnnotations,        // (specContent: string) => ComponentSpec[]
  loadHandoffSpecs,          // (planningDir: string) => ComponentSpec[]
  findComponentFile,         // (projectRoot: string, name: string) => string|null
  extractPropsFromFile,      // (fileContent: string, propsName: string) => string[]|null
  checkTokenUsage,           // (fileContent: string, tokens: string[]) => string[]
  loadIgnoreList,            // (projectRoot: string) => Set<string>
  runDivergenceCheck,        // (projectRoot: string) => DivergenceResult
  buildDivergenceReport,     // (result: DivergenceResult) => string  (Markdown)
};
```

<!-- Command pattern to follow (from commands/pipeline-status.md) -->
<!-- Frontmatter: name, description, argument-hint, allowed-tools -->
<!-- Body: objective + process referencing @workflows/{name}.md -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create command and workflow files</name>
  <files>commands/check-divergence.md, workflows/check-divergence.md</files>
  <action>
    Create commands/check-divergence.md following the pipeline-status.md command pattern exactly:
    - name: pde:check-divergence
    - description: "Detect drift between handoff specs and implemented components"
    - argument-hint: '[--verbose]'
    - allowed-tools: Read, Write, Bash, Glob, Grep
    - Process: Follow @workflows/check-divergence.md exactly, pass $ARGUMENTS

    Create workflows/check-divergence.md:
    - Objective: Run 3-tier divergence detection between handoff specs and codebase, output DIVERGENCE.md
    - Steps:
      1. Invoke bin/lib/divergence.cjs via Node one-liner (same pattern as mcp-status.md uses):
         node -e "const d = require('$CLAUDE_PLUGIN_ROOT/bin/lib/divergence.cjs'); const r = d.runDivergenceCheck(process.cwd()); const md = d.buildDivergenceReport(r); require('fs').writeFileSync('DIVERGENCE.md', md); console.log(JSON.stringify(r));"
      2. If result.noSpecs is true, output message: "No handoff specs found -- run /pde:handoff first" and exit without writing DIVERGENCE.md
      3. If result has components, write DIVERGENCE.md to project root and display summary to user
      4. If --verbose flag passed, display per-component details in addition to summary
    - Include clear instructions about interpreting each status (ALIGNED, DRIFTED, MISSING, EXTRA)
    - Reference .pde-divergence-ignore for suppressing known-acceptable divergences
  </action>
  <verify>
    <automated>test -f commands/check-divergence.md && test -f workflows/check-divergence.md && grep -q "pde:check-divergence" commands/check-divergence.md && grep -q "divergence.cjs" workflows/check-divergence.md && echo "PASS"</automated>
  </verify>
  <done>Both command and workflow files exist with correct structure, command delegates to workflow, workflow invokes divergence.cjs</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete divergence detection system: bin/lib/divergence.cjs module with T1/T2/T3 detection, commands/check-divergence.md command, workflows/check-divergence.md workflow, full test suite</what-built>
  <how-to-verify>
    1. Review bin/lib/divergence.cjs -- confirm it exports all 8 functions and follows zero-dep CJS pattern
    2. Review commands/check-divergence.md -- confirm it follows the pipeline-status command pattern
    3. Review workflows/check-divergence.md -- confirm it invokes divergence.cjs and handles noSpecs case
    4. Run: node --test tests/phase-122/test-divergence.cjs -- all tests should pass
    5. Run: node --test tests/phase-120/test-artifact-format.cjs -- zero regressions
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- commands/check-divergence.md exists with correct frontmatter
- workflows/check-divergence.md exists and invokes divergence.cjs
- node --test tests/phase-122/test-divergence.cjs passes
- node --test tests/phase-120/test-artifact-format.cjs passes (no regressions)
</verification>

<success_criteria>
- /pde:check-divergence command file exists following established pattern
- Workflow correctly invokes runDivergenceCheck and buildDivergenceReport
- Workflow handles noSpecs case gracefully without writing DIVERGENCE.md
- Workflow writes DIVERGENCE.md to project root on success
- Human verification confirms all components work together
</success_criteria>

<output>
After completion, create `.planning/phases/122-divergence-detection/122-02-SUMMARY.md`
</output>
