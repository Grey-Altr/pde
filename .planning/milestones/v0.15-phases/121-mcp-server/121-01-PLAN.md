---
phase: 121-mcp-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pde-mcp-server/package.json
  - packages/pde-mcp-server/tsconfig.json
  - packages/pde-mcp-server/src/index.ts
  - packages/pde-mcp-server/src/discover.ts
  - packages/pde-mcp-server/src/tools/get-project.ts
  - packages/pde-mcp-server/src/tools/get-design-state.ts
  - packages/pde-mcp-server/src/tools/get-manifest.ts
  - packages/pde-mcp-server/src/tools/get-tokens.ts
  - packages/pde-mcp-server/src/tools/get-handoff.ts
  - packages/pde-mcp-server/src/tools/get-artifact.ts
  - packages/pde-mcp-server/src/tools/get-roadmap.ts
  - packages/pde-mcp-server/src/tools/get-requirements.ts
  - packages/pde-mcp-server/src/tools/get-pipeline-status.ts
  - packages/pde-mcp-server/src/tools/list-artifacts.ts
  - packages/pde-mcp-server/src/resources/pipeline-status.ts
  - tests/phase-121/test-mcp-server.cjs
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-04
  - MCP-05

must_haves:
  truths:
    - "MCP server package exists in isolated subdirectory with its own dependencies"
    - "Server registers exactly 10 read-only tools with correct names"
    - "Server registers pipeline-status resource for passive editor consumption"
    - "get-tokens tool returns Tailwind v4 @theme block from DTCG tokens"
    - "discoverPlanningDir walks up from cwd to find .planning/"
  artifacts:
    - path: "packages/pde-mcp-server/package.json"
      provides: "Isolated package with SDK dependency, bin field, type module"
      contains: "pde-mcp-server"
    - path: "packages/pde-mcp-server/src/index.ts"
      provides: "Entry point registering all tools and resources"
      contains: "McpServer"
    - path: "packages/pde-mcp-server/src/discover.ts"
      provides: "Walk-up .planning/ directory discovery"
      exports: ["discoverPlanningDir"]
    - path: "tests/phase-121/test-mcp-server.cjs"
      provides: "Tests for MCP-01 through MCP-05"
      min_lines: 50
  key_links:
    - from: "packages/pde-mcp-server/src/index.ts"
      to: "packages/pde-mcp-server/src/discover.ts"
      via: "import discoverPlanningDir"
      pattern: "discoverPlanningDir"
    - from: "packages/pde-mcp-server/src/tools/get-tokens.ts"
      to: "bin/lib/artifact-format.cjs"
      via: "createRequire for generateTailwindTheme"
      pattern: "generateTailwindTheme"
    - from: "packages/pde-mcp-server/src/index.ts"
      to: "@modelcontextprotocol/sdk"
      via: "McpServer + StdioServerTransport"
      pattern: "McpServer|StdioServerTransport"
---

<objective>
Build the PDE MCP server package with all 10 read-only tools, pipeline resource, and structural tests.

Purpose: Enable any MCP-compatible editor to query PDE project state programmatically through a standalone server.
Output: Complete MCP server source in packages/pde-mcp-server/, test file covering all requirements.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-mcp-server/121-RESEARCH.md
@.planning/phases/121-mcp-server/121-CONTEXT.md

<interfaces>
<!-- Key exports the executor needs from existing codebase -->

From bin/lib/artifact-format.cjs:
```javascript
module.exports = {
  generateFileAnnotations,
  generateTailwindTheme,     // DTCG tokens object -> Tailwind v4 @theme CSS string
  generateCssVarsFromTheme,
  dtcgToThemeLines,
  typeToNamespace,
  detectFramework,
  generateComponentStub,
};
```

From .planning/design/design-manifest.json (schema):
```json
{
  "schemaVersion": "1.0.0",
  "projectName": "string",
  "productType": "software | hardware | hybrid | experience",
  "outputRoot": "string",
  "artifacts": {},
  "designCoverage": { "hasDesignSystem": false, ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Package scaffold and discovery module</name>
  <files>
    packages/pde-mcp-server/package.json,
    packages/pde-mcp-server/tsconfig.json,
    packages/pde-mcp-server/src/discover.ts,
    tests/phase-121/test-mcp-server.cjs
  </files>
  <behavior>
    - Test: packages/pde-mcp-server/package.json exists with name "pde-mcp-server", bin field, @modelcontextprotocol/sdk dep, type "module"
    - Test: discoverPlanningDir finds .planning/ in current dir
    - Test: discoverPlanningDir finds .planning/ in ancestor dir (walk-up)
    - Test: discoverPlanningDir returns null when no .planning/ exists
    - Test: tsconfig.json exists with outDir dist, rootDir src
  </behavior>
  <action>
    Create `packages/pde-mcp-server/` directory structure.

    **package.json:** Use ESM ("type": "module") per research recommendation. Fields:
    - name: "pde-mcp-server"
    - version: "0.1.0"
    - type: "module"
    - bin: { "pde-mcp-server": "./dist/index.js" }
    - files: ["dist"]
    - scripts: { "build": "tsc", "prepublishOnly": "npm run build" }
    - dependencies: { "@modelcontextprotocol/sdk": "^1.26.0", "zod": "^3.25.0" }
    - devDependencies: { "typescript": "^5.0.0", "@types/node": "^20.0.0" }

    **tsconfig.json:** target ES2022, module Node16, moduleResolution Node16, outDir ./dist, rootDir ./src, strict true, declaration true, sourceMap true.

    **src/discover.ts:** Export `discoverPlanningDir(startDir: string): string | null` that walks up from startDir checking for `.planning/` directory. Max 10 levels. Returns the `.planning/` absolute path or null.

    **tests/phase-121/test-mcp-server.cjs:** Create test scaffold using node:test. Tests for:
    - MCP-01: package.json structure validation (read and parse the file, assert fields)
    - MCP-03: discoverPlanningDir behavior with temp dir fixtures (create temp .planning/ dirs, test walk-up and null cases)
    - Stub describe blocks for MCP-02, MCP-04, MCP-05 (filled in Task 2)

    Run `cd packages/pde-mcp-server && npm install` to install dependencies.

    IMPORTANT: Do NOT install dependencies at the project root. Only inside packages/pde-mcp-server/.
  </action>
  <verify>
    <automated>node --test tests/phase-121/test-mcp-server.cjs</automated>
  </verify>
  <done>
    - packages/pde-mcp-server/package.json exists with correct fields
    - discoverPlanningDir works for direct, ancestor, and missing cases
    - npm install completes inside packages/pde-mcp-server/
    - Test scaffold runs with MCP-01 and MCP-03 tests passing
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: All 10 tools, pipeline resource, and complete tests</name>
  <files>
    packages/pde-mcp-server/src/index.ts,
    packages/pde-mcp-server/src/tools/get-project.ts,
    packages/pde-mcp-server/src/tools/get-design-state.ts,
    packages/pde-mcp-server/src/tools/get-manifest.ts,
    packages/pde-mcp-server/src/tools/get-tokens.ts,
    packages/pde-mcp-server/src/tools/get-handoff.ts,
    packages/pde-mcp-server/src/tools/get-artifact.ts,
    packages/pde-mcp-server/src/tools/get-roadmap.ts,
    packages/pde-mcp-server/src/tools/get-requirements.ts,
    packages/pde-mcp-server/src/tools/get-pipeline-status.ts,
    packages/pde-mcp-server/src/tools/list-artifacts.ts,
    packages/pde-mcp-server/src/resources/pipeline-status.ts,
    tests/phase-121/test-mcp-server.cjs
  </files>
  <behavior>
    - Test: Server registers exactly 10 tools with correct names (get-project, get-design-state, get-manifest, get-tokens, get-handoff, get-artifact, get-roadmap, get-requirements, get-pipeline-status, list-artifacts)
    - Test: Each tool handler returns content from the correct .planning/ file when given a mock planningDir
    - Test: get-tokens returns string containing "@theme" when given valid DTCG token fixture
    - Test: get-handoff with no name param lists available handoff files
    - Test: get-handoff with name param returns that file's content
    - Test: get-artifact with name param reads from manifest artifacts map
    - Test: get-pipeline-status returns JSON with designState and manifest keys
    - Test: pipeline-status resource returns JSON with same structure as get-pipeline-status
    - Test: Tools return isError: true when files are missing
    - Test: No write tools registered (verify tool list is exactly 10 read-only)
  </behavior>
  <action>
    Each tool file exports a factory function: `(planningDir: string) => { name, description, inputSchema, handler }`. This allows the test file to call handlers directly with a temp planningDir without needing the full MCP server.

    **Tool-to-file mapping (from research):**
    | Tool | Reads | Params |
    |------|-------|--------|
    | get-project | PROJECT.md | none |
    | get-design-state | design/DESIGN-STATE.md | none |
    | get-manifest | design/design-manifest.json | none |
    | get-tokens | design/design-manifest.json -> tokens path, then generateTailwindTheme() | none |
    | get-handoff | design/handoff/*.md | optional `name` (string) |
    | get-artifact | design/design-manifest.json -> artifacts[name] | required `name` (string) |
    | get-roadmap | ROADMAP.md | none |
    | get-requirements | REQUIREMENTS.md | none |
    | get-pipeline-status | design/DESIGN-STATE.md + design/design-manifest.json | none |
    | list-artifacts | design/design-manifest.json -> Object.keys(artifacts) | none |

    **get-tokens (MCP-05):** Use `createRequire(import.meta.url)` to import `generateTailwindTheme` from `../../../bin/lib/artifact-format.cjs`. Read manifest to find tokens path, read tokens file, pass to generateTailwindTheme(), return the @theme CSS string. If no tokens artifact in manifest, return informative message (not error).

    **get-handoff:** If `name` param provided, read `design/handoff/{name}.md`. If not provided, read the `design/handoff/` directory and return list of available handoff files.

    **pipeline-status resource (MCP-04):** Register at URI `pde://pipeline-status` with mimeType `application/json`. Returns JSON with `{ designState, manifest }` from DESIGN-STATE.md and design-manifest.json.

    **src/index.ts entry point:**
    - Line 1: `#!/usr/bin/env node`
    - Import McpServer from `@modelcontextprotocol/sdk/server/mcp.js`
    - Import StdioServerTransport from `@modelcontextprotocol/sdk/server/stdio.js`
    - Import discoverPlanningDir from `./discover.js`
    - Import all tool factories
    - Discover planningDir, exit with stderr message if null
    - Support `--planning-dir` CLI arg as override
    - Register all 10 tools via server.registerTool()
    - Register pipeline-status resource via server.registerResource()
    - Connect to StdioServerTransport

    **Tests:** Update test file to add tests for MCP-02 (all 10 tools registered, each handler returns correct content from fixture dirs), MCP-04 (resource handler), MCP-05 (get-tokens returns @theme). Create temp .planning/ directories with fixture files for each test. Test handlers directly (import the tool factory, call handler with temp planningDir) -- do NOT start the actual MCP server in tests.

    NOTE on test approach: Since the MCP server is TypeScript ESM but tests are CJS, export tool handler logic as pure functions that can be tested independently. Create a `packages/pde-mcp-server/src/tool-handlers.ts` that exports all handler functions taking `planningDir` as first arg. The test file can either:
    (a) Test by compiling first and importing from dist/, or
    (b) Test the compiled JS directly.
    Recommended: After `npm run build`, test against `packages/pde-mcp-server/dist/tool-handlers.js` using dynamic import in the test or use createRequire. Alternatively, write a thin CJS test helper that spawns the server and sends JSON-RPC messages via stdin/stdout.

    Simpler approach: Write tool handler logic in a separate `packages/pde-mcp-server/handlers.cjs` file (plain CJS, no TypeScript) that the TypeScript index.ts imports via createRequire. Tests import handlers.cjs directly. This avoids the TS compilation dependency in tests.

    FINAL DECISION: Use the simpler approach. Create `packages/pde-mcp-server/handlers.cjs` with all tool handler functions as plain CJS. The TypeScript `src/index.ts` imports it via createRequire. Tests import handlers.cjs directly. This matches the project's CJS test pattern (node:test with .cjs files).
  </action>
  <verify>
    <automated>node --test tests/phase-121/test-mcp-server.cjs</automated>
  </verify>
  <done>
    - All 10 tool handlers return correct content from fixture .planning/ dirs
    - get-tokens returns @theme CSS block from DTCG fixture
    - Pipeline resource returns JSON with designState + manifest
    - Missing file cases return isError: true
    - Zero write tools registered
    - All MCP-01 through MCP-05 tests pass
  </done>
</task>

</tasks>

<verification>
- `node --test tests/phase-121/test-mcp-server.cjs` — all tests green
- `ls packages/pde-mcp-server/package.json` — exists with correct structure
- `ls packages/pde-mcp-server/src/index.ts` — entry point with shebang
- `grep -c "registerTool" packages/pde-mcp-server/src/index.ts` — returns 10
- `grep "registerResource" packages/pde-mcp-server/src/index.ts` — pipeline-status registered
</verification>

<success_criteria>
- packages/pde-mcp-server/ is a self-contained package with its own node_modules
- 10 read-only tools registered, zero write tools
- Pipeline resource registered at pde://pipeline-status
- get-tokens serves Tailwind v4 @theme format
- discoverPlanningDir walks up from cwd
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/121-mcp-server/121-01-SUMMARY.md`
</output>
