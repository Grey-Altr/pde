---
phase: 124-integration-and-nyquist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-124/test-integration-nyquist.cjs
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03, CTX-04, CTX-05, CTX-06, CTX-07, CTX-08, MCP-01, MCP-02, MCP-03, MCP-04, MCP-05, STH-01, STH-02, STH-03, FMT-01, FMT-02, FMT-03, DIV-01, DIV-02, DIV-03, DIV-04, DIV-05, DIV-06]

must_haves:
  truths:
    - "All 25 v0.15 requirements have explicit Nyquist structural test coverage across 8 test files"
    - "MCP-03 npx distribution structure is structurally gated (dist/index.js exists, has shebang, bin field correct)"
    - "Meta-test verifies all 8 v0.15 test files exist"
    - "Phase 124 test file passes with zero failures"
  artifacts:
    - path: "tests/phase-124/test-integration-nyquist.cjs"
      provides: "Integration Nyquist tests for MCP-03 gap + INTG-01 meta-test"
      contains: "MCP-03"
  key_links:
    - from: "tests/phase-124/test-integration-nyquist.cjs"
      to: "packages/pde-mcp-server/package.json"
      via: "JSON.parse for bin field assertion"
      pattern: "package\\.json"
    - from: "tests/phase-124/test-integration-nyquist.cjs"
      to: "packages/pde-mcp-server/dist/index.js"
      via: "fs.existsSync and readFileSync for shebang check"
      pattern: "dist/index\\.js"
---

<objective>
Write the Phase 124 integration Nyquist test file that fills the MCP-03 coverage gap and provides the INTG-01 meta-test asserting all 8 v0.15 test files exist.

Purpose: 24 of 25 v0.15 requirements already have structural tests in phases 118-123. MCP-03 (npx distribution structure) needs a dist/index.js existence + shebang + bin field gate. The meta-test registers all 8 test files as the structural coverage proof.

Output: `tests/phase-124/test-integration-nyquist.cjs` with MCP-03 gap closed and INTG-01 meta-test.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-integration-and-nyquist/124-RESEARCH.md

<interfaces>
<!-- Key paths and patterns the executor needs -->

MCP server package structure:
```
packages/pde-mcp-server/
  package.json          # bin field points to dist/index.js
  dist/index.js         # Built TypeScript output with #!/usr/bin/env node shebang
  src/handlers.cjs      # CJS handler implementations
  src/discover.cjs      # Planning dir discovery
```

Test file convention (v0.15 = .cjs):
```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const PROJECT_ROOT = path.join(__dirname, '..', '..');
```

All 8 v0.15 test files:
```
tests/phase-118/test-context-sync.cjs        (31 tests: CTX-01,02,03,04,08)
tests/phase-119/test-antigravity-stitch.cjs   (32 tests: CTX-05, STH-01,02,03)
tests/phase-120/test-artifact-format.cjs      (41 tests: FMT-01,02,03)
tests/phase-121/test-mcp-server.cjs           (27 tests: MCP-01,02,04,05)
tests/phase-122/test-divergence.cjs           (38 tests: DIV-01,02,03,04,05,06)
tests/phase-123/test-context-sync-hook.cjs    (7 tests: CTX-06)
tests/phase-123/test-editor-sync-command.cjs  (9 tests: CTX-07)
tests/phase-124/test-integration-nyquist.cjs  (THIS FILE: MCP-03, INTG-01)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test-integration-nyquist.cjs with MCP-03 gap fill and INTG-01 meta-test</name>
  <files>tests/phase-124/test-integration-nyquist.cjs</files>

  <read_first>
    - .planning/phases/124-integration-and-nyquist/124-RESEARCH.md (code examples for MCP-03 and INTG-01)
    - tests/phase-121/test-mcp-server.cjs (existing MCP test patterns, first 30 lines for header convention)
    - packages/pde-mcp-server/package.json (confirm bin field structure)
  </read_first>

  <action>
Create `tests/phase-124/test-integration-nyquist.cjs` with the following structure:

**File header:**
```javascript
'use strict';

/**
 * test-integration-nyquist.cjs -- Phase 124 Integration & Nyquist
 *
 * Fills coverage gaps for v0.15 requirements not explicitly tested in phases 118-123.
 * Covers: MCP-03 (npx dist structure), INTG-01 (meta-test file registration)
 *
 * Run: node --test tests/phase-124/test-integration-nyquist.cjs
 */

const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const PROJECT_ROOT = path.join(__dirname, '..', '..');
```

**Section 1 -- MCP-03 describe block (3 tests):**
- Test 1: `dist/index.js exists (TypeScript build artifact)` -- assert `fs.existsSync` of `packages/pde-mcp-server/dist/index.js`
- Test 2: `dist/index.js has executable shebang line` -- read first line, assert it includes `#!/usr/bin/env node`
- Test 3: `package.json bin field points to dist/index.js` -- JSON.parse the package.json, extract the bin value (handle both string and object forms), assert it includes `dist/index.js`

Use the exact code pattern from 124-RESEARCH.md "MCP-03 Dist Structure Assertions" section.

**Section 2 -- INTG-01 meta-test describe block (2 tests):**
- Define `V015_TEST_FILES` array with all 8 test file paths (relative to PROJECT_ROOT)
- Test 1: `all 8 v0.15 test files exist` -- loop through array, assert each exists
- Test 2: `8 test files cover all 25 v0.15 requirements` -- assert array length equals 8

Use the exact code pattern from 124-RESEARCH.md "Meta-Test File Registration" section.

**Total:** 5 assertions in 2 describe blocks. Use `it()` not `test()` to match v0.15 convention.
  </action>

  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-124/test-integration-nyquist.cjs</automated>
  </verify>

  <done>
    - tests/phase-124/test-integration-nyquist.cjs exists with describe blocks for MCP-03 and INTG-01
    - All 5 assertions pass
    - MCP-03 gap closed (dist/index.js existence, shebang, bin field)
    - INTG-01 meta-test confirms all 8 v0.15 test files exist
  </done>
</task>

</tasks>

<verification>
```bash
# Phase 124 test passes
node --test tests/phase-124/test-integration-nyquist.cjs

# All 8 v0.15 test files pass together
node --test \
  tests/phase-118/test-context-sync.cjs \
  tests/phase-119/test-antigravity-stitch.cjs \
  tests/phase-120/test-artifact-format.cjs \
  tests/phase-121/test-mcp-server.cjs \
  tests/phase-122/test-divergence.cjs \
  tests/phase-123/test-context-sync-hook.cjs \
  tests/phase-123/test-editor-sync-command.cjs \
  tests/phase-124/test-integration-nyquist.cjs
```
</verification>

<success_criteria>
- tests/phase-124/test-integration-nyquist.cjs exists and passes
- MCP-03 and INTG-01 have named describe blocks
- All 8 v0.15 test files pass together (185+ assertions total)
</success_criteria>

<output>
After completion, create `.planning/phases/124-integration-and-nyquist/124-01-SUMMARY.md`
</output>
