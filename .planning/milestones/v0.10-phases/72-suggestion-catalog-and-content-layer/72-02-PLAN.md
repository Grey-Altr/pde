---
phase: 72-suggestion-catalog-and-content-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/context-notes/README.md
  - workflows/plan-phase.md
  - workflows/brief.md
autonomous: true
requirements: [CONT-04]

must_haves:
  truths:
    - "Placing a markdown file in .planning/context-notes/ and running /pde:plan causes that file's content to appear in the planning context under a Context Notes section"
    - "Running /pde:brief with context-notes present causes the brief to incorporate them as upstream context"
    - "README.md in context-notes/ is excluded from injection into planner prompts"
  artifacts:
    - path: ".planning/context-notes/README.md"
      provides: "User-facing documentation for the context-notes directory"
      contains: "Place markdown files here"
    - path: "workflows/plan-phase.md"
      provides: "Context-notes probe and injection into planner prompt"
      contains: "context-notes"
    - path: "workflows/brief.md"
      provides: "Context-notes probe in Sub-step 2c upstream injection"
      contains: "NOTES_CONTEXT"
  key_links:
    - from: "workflows/plan-phase.md"
      to: ".planning/context-notes/"
      via: "Glob probe for *.md files excluding README.md"
      pattern: "context-notes"
    - from: "workflows/brief.md"
      to: ".planning/context-notes/"
      via: "Glob probe for *.md files excluding README.md in Sub-step 2c"
      pattern: "context-notes"
---

<objective>
Initialize the .planning/context-notes/ directory with a README and inject user-authored context notes into both /pde:plan and /pde:brief workflows.

Purpose: CONT-04 requires user-authored markdown files in .planning/context-notes/ to be consumed by planning and brief workflows. This plan creates the directory, documents its purpose, and adds soft-probe injection logic to both workflow files.

Output: context-notes directory ready for user content, both workflows reading and injecting notes into their prompts.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-suggestion-catalog-and-content-layer/72-RESEARCH.md

<interfaces>
<!-- Key workflow sections the executor needs to understand. -->

From workflows/plan-phase.md — Step 7 (lines 390-402):
```markdown
## 7. Use Context Paths from INIT
Extract from INIT JSON:
STATE_PATH, ROADMAP_PATH, REQUIREMENTS_PATH, RESEARCH_PATH, VERIFICATION_PATH, UAT_PATH, CONTEXT_PATH
```

From workflows/plan-phase.md — Step 8 planner prompt (lines 473-500):
```markdown
## 8. Spawn pde-planner Agent
Planner prompt:
<files_to_read>
- .planning/project-context.md
- references/workflow-methodology.md
- {state_path}
- {roadmap_path}
- {requirements_path}
- {context_path}
- {research_path}
- {verification_path}
- {uat_path}
- {UI_SPEC_PATH}
- .planning/agent-memory/planner/memories.md
</files_to_read>
```

From workflows/brief.md — Sub-step 2c (lines 87-122):
```markdown
**Sub-step 2c: Upstream context injection (all soft — never halt)**
Probes for: IDT, CMP, OPP, ANL artifacts
Pattern: Glob -> sort -> read highest version -> store as *_CONTEXT -> use in Step 5
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create context-notes directory and inject into plan-phase and brief workflows</name>
  <files>.planning/context-notes/README.md, workflows/plan-phase.md, workflows/brief.md</files>
  <read_first>
    - workflows/plan-phase.md (full file — locate Step 7 at ~line 390, Step 7.5 at ~line 404, Step 8 planner prompt at ~line 473, and the files_to_read block at ~line 488)
    - workflows/brief.md (full file — locate Sub-step 2c at ~line 87, and Step 5 upstream enrichment at ~line 240)
    - .planning/phases/72-suggestion-catalog-and-content-layer/72-RESEARCH.md (Pattern 5: README content, Pattern 6: injection logic)
  </read_first>
  <action>
**Part A: Create .planning/context-notes/README.md**

Create the directory and README with this exact content:

```markdown
# Context Notes

Place markdown files here to inject domain knowledge into PDE's planning cycles.

## How it works

Any `.md` file in this directory will be read by `/pde:plan` and `/pde:brief` and appear
in the planner's context under a **Context Notes** section. Use this to capture:

- Business rules PDE cannot infer from code
- Edge cases you've discovered that aren't in the codebase yet
- User stories and acceptance criteria drafts
- Domain constraints and regulatory requirements

## Naming convention

Files are sorted alphabetically and read in order. Use date-prefixed filenames
to control order:

    2026-03-21-payment-edge-cases.md
    2026-03-21-user-auth-constraints.md

## Format

Plain markdown. No special frontmatter required. Write naturally.
PDE reads the full file content and includes it verbatim in planning context.
```

**Part B: Add Step 7.2 to workflows/plan-phase.md**

Insert a new step between Step 7 (ends at line ~402 with closing ```) and Step 7.5 (starts at ~line 404). Add:

```markdown
## 7.2. Probe Context Notes

Soft probe — never halt. If `.planning/context-notes/` does not exist or has no `.md` files, skip silently.

Use the Glob tool to find `.planning/context-notes/*.md` files. Exclude `README.md` from results.

If matching files found:
1. Sort alphabetically
2. Read each file's content
3. Build CONTEXT_NOTES_CONTENT by concatenating each file's content under a `### {filename}` subheading
4. Log: `"  -> Context notes found: {N} file(s) — will inject into planner prompt"`

If no matching files (or directory absent):
- SET CONTEXT_NOTES_CONTENT = "" (empty string). Continue silently.
```

**Part C: Inject context notes into Step 8 planner prompt in workflows/plan-phase.md**

In Step 8, after the `</files_to_read>` closing tag in the planner prompt (approximately line 500), and before the `**Phase requirement IDs**` line (approximately line 502), add:

```markdown
{If CONTEXT_NOTES_CONTENT is non-empty, include this block:}
<context_notes>
## Context Notes

The user has authored the following domain knowledge notes. Honor these during planning —
they represent business rules, edge cases, and constraints PDE cannot infer from code.

{CONTEXT_NOTES_CONTENT}
</context_notes>
```

**Part D: Add context-notes probe to workflows/brief.md Sub-step 2c**

In `brief.md`, after the **Analyst context (ANL)** block (ends at ~line 121 with `SET ANL_CONTEXT = null. Continue normally.`) and before the `---` separator (line 123), add a new context-notes probe following the same pattern:

```markdown

**Context notes (NOTES):**
Use the Glob tool to find `.planning/context-notes/*.md` files. Exclude `README.md` from results.
- If found: Sort alphabetically, read each file. Concatenate content under `### {filename}` subheadings. Store as NOTES_CONTEXT for use in Step 5.
  Log: `"  -> Context notes found: {N} file(s) — enriching brief with user-authored domain knowledge"`
- If not found:
  Log: `"  -> No context notes — continuing without user-authored enrichment"`
  SET NOTES_CONTEXT = null. Continue normally.
```

**Part E: Use NOTES_CONTEXT in brief.md Step 5**

In the "Upstream context enrichment" section of Step 5 (after the line about ANL_CONTEXT enriching Assumptions & Risks, approximately line 252), add:

```markdown
- **Domain Context:** If NOTES_CONTEXT is available, incorporate user-authored domain knowledge as supplementary context across all relevant sections. Context notes provide business rules, edge cases, and constraints that PROJECT.md may not contain. Treat them as high-confidence user-provided facts.
```

Also add NOTES_CONTEXT to the "Upstream context" row in the summary table at the end (approximately line 524). After `ANL v{N} (analyst)`, add `, NOTES ({N} files)` when NOTES_CONTEXT is non-null.
  </action>
  <verify>
    <automated>grep -c "context-notes" workflows/plan-phase.md && grep -c "context-notes" workflows/brief.md && grep -q "README.md" workflows/plan-phase.md && grep -q "CONTEXT_NOTES_CONTENT" workflows/plan-phase.md && grep -q "7.2" workflows/plan-phase.md && test -f .planning/context-notes/README.md && echo "ALL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/context-notes/README.md exists and contains "Place markdown files here to inject domain knowledge"
    - .planning/context-notes/README.md contains "## How it works" and "## Naming convention" and "## Format"
    - workflows/plan-phase.md contains `## 7.2. Probe Context Notes`
    - workflows/plan-phase.md contains `context-notes/*.md`
    - workflows/plan-phase.md contains `Exclude \`README.md\``
    - workflows/plan-phase.md contains `<context_notes>` block in Step 8
    - workflows/plan-phase.md contains `CONTEXT_NOTES_CONTENT`
    - workflows/brief.md contains `**Context notes (NOTES):**`
    - workflows/brief.md contains `NOTES_CONTEXT`
    - workflows/brief.md contains `context-notes/*.md`
    - workflows/brief.md Step 5 section references `NOTES_CONTEXT` for Domain Context
    - grep -c "context-notes" workflows/plan-phase.md returns at least 4
    - grep -c "context-notes" workflows/brief.md returns at least 3
  </acceptance_criteria>
  <done>context-notes directory initialized with README, /pde:plan probes and injects notes into planner prompt, /pde:brief probes and injects notes as upstream context alongside IDT/CMP/OPP/ANL</done>
</task>

</tasks>

<verification>
1. `test -d .planning/context-notes && echo "directory exists"` — confirms directory created
2. `grep "context-notes" workflows/plan-phase.md | wc -l` — at least 4 references
3. `grep "context-notes" workflows/brief.md | wc -l` — at least 3 references
4. `grep "NOTES_CONTEXT" workflows/brief.md | wc -l` — at least 3 references
5. `grep "README.md" workflows/plan-phase.md` — README exclusion present
6. `grep "README.md" workflows/brief.md` — README exclusion present
7. `grep "7.2" workflows/plan-phase.md` — new step present
</verification>

<success_criteria>
- .planning/context-notes/ directory exists with README.md documenting purpose, format, and naming convention
- plan-phase.md has Step 7.2 that probes context-notes and Step 8 that injects content into planner prompt
- brief.md has context-notes probe in Sub-step 2c and uses NOTES_CONTEXT in Step 5
- README.md is excluded from injection in both workflows
- Empty context-notes directory causes no change to workflow behavior (soft probe)
</success_criteria>

<output>
After completion, create `.planning/phases/72-suggestion-catalog-and-content-layer/72-02-SUMMARY.md`
</output>
