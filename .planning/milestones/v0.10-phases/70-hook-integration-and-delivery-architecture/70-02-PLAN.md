---
phase: 70-hook-integration-and-delivery-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - GETTING-STARTED.md
autonomous: true
requirements:
  - DLVR-05

must_haves:
  truths:
    - "Getting Started documentation contains a messageIdleNotifThresholdMs: 5000 recommendation with the correct ~/.CLAUDE.json key"
  artifacts:
    - path: "GETTING-STARTED.md"
      provides: "Idle suggestion threshold configuration documentation"
      contains: "messageIdleNotifThresholdMs"
  key_links:
    - from: "GETTING-STARTED.md"
      to: "~/.CLAUDE.json"
      via: "Configuration key documentation"
      pattern: "messageIdleNotifThresholdMs.*5000"
---

<objective>
Add idle suggestion threshold configuration documentation to GETTING-STARTED.md.

Purpose: Users need to know how to configure `messageIdleNotifThresholdMs` in `~/.CLAUDE.json` to receive idle suggestions within 5 seconds instead of the default 60 seconds. Without this documentation, the idle suggestion system works but users may not see suggestions for a full minute after Claude Code becomes idle.

Output: GETTING-STARTED.md updated with a new section documenting the threshold setting.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-hook-integration-and-delivery-architecture/70-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add idle suggestion threshold configuration to Getting Started</name>
  <files>GETTING-STARTED.md</files>
  <read_first>GETTING-STARTED.md, .planning/phases/70-hook-integration-and-delivery-architecture/70-RESEARCH.md</read_first>
  <action>
Add a new section to GETTING-STARTED.md titled "## Idle Suggestion Threshold (Optional)" AFTER the "## What's Next" section and BEFORE the "## Command Cheat Sheet" section (between lines ~263 and ~267 of the current file).

Insert exactly this content:

```markdown
## Idle Suggestion Threshold (Optional)

PDE surfaces productivity suggestions when Claude Code is idle between operations. These suggestions appear in the tmux dashboard (Pane 7 of `/pde:monitor`) and are accessible via `/pde:suggestions`.

By default, Claude Code waits 60 seconds before firing the idle notification. To see suggestions sooner, add this to `~/.CLAUDE.json`:

```json
{
  "messageIdleNotifThresholdMs": 5000
}
```

This makes suggestions appear after 5 seconds of idle time instead of 60 seconds. The setting lives in `~/.CLAUDE.json` (your global Claude Code config), not in the project's `.claude/settings.json`.

---
```

Key constraints:
- The file is `~/.CLAUDE.json` — NOT `settings.json`, NOT `.claude/settings.json`
- The key is `messageIdleNotifThresholdMs` (exact casing, one word)
- The value is 5000 (milliseconds)
- The JSON example must be valid JSON (not JSONC — no comments)
- Do NOT modify any existing content in GETTING-STARTED.md — only insert the new section
  </action>
  <verify>
    <automated>node -e "const md=require('fs').readFileSync('GETTING-STARTED.md','utf-8'); if(!md.includes('messageIdleNotifThresholdMs')) throw 'missing key'; if(!md.includes('5000')) throw 'missing 5000 value'; if(!md.includes('~/.CLAUDE.json')) throw 'missing file path'; if(!md.includes('Idle Suggestion Threshold')) throw 'missing section title'; console.log('PASS: GETTING-STARTED.md threshold config documented')"</automated>
  </verify>
  <acceptance_criteria>
    - GETTING-STARTED.md contains the string "messageIdleNotifThresholdMs"
    - GETTING-STARTED.md contains the string "5000"
    - GETTING-STARTED.md contains the string "~/.CLAUDE.json"
    - GETTING-STARTED.md contains the heading "## Idle Suggestion Threshold"
    - The section appears AFTER "## What's Next" and BEFORE "## Command Cheat Sheet"
    - All existing content in GETTING-STARTED.md is preserved unchanged
    - The JSON code block in the new section is valid JSON
  </acceptance_criteria>
  <done>GETTING-STARTED.md contains messageIdleNotifThresholdMs: 5000 recommendation with ~/.CLAUDE.json as the correct config file location</done>
</task>

</tasks>

<verification>
1. `grep 'messageIdleNotifThresholdMs' GETTING-STARTED.md` returns a match
2. `grep '~/.CLAUDE.json' GETTING-STARTED.md` returns a match
3. `grep '5000' GETTING-STARTED.md` returns a match
4. The section order in GETTING-STARTED.md is: "What's Next" ... "Idle Suggestion Threshold" ... "Command Cheat Sheet"
</verification>

<success_criteria>
- GETTING-STARTED.md has the idle threshold configuration section with the correct key, value, and file path
- Existing documentation content is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/70-hook-integration-and-delivery-architecture/70-02-SUMMARY.md`
</output>
