---
phase: 71-suggestion-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/idle-suggestions.cjs
  - hooks/tests/verify-phase-71.cjs
autonomous: true
requirements: [ENGN-01, ENGN-02, ENGN-03, ENGN-04, ENGN-05, ENGN-06]

must_haves:
  truths:
    - "rankSuggestions() places blocker-category candidates before all other categories"
    - "classifyPhase() returns design when DESIGN-STATE.md has incomplete stages"
    - "classifyPhase() returns plan when event_type is plan_started"
    - "classifyPhase() returns null when no state data exists (zero-state)"
    - "generateSuggestions() returns formatted markdown with all four category headers visible"
    - "Suggestions exceeding time budget are filtered out and counted in stats footer cut:N"
    - "Max 7 suggestions returned regardless of candidate count"
    - "Empty categories show -- none"
    - "generateSuggestions() completes in under 2 seconds with zero LLM calls"
  artifacts:
    - path: "bin/lib/idle-suggestions.cjs"
      provides: "Phase-aware suggestion engine"
      exports: ["generateSuggestions", "rankSuggestions"]
      min_lines: 150
    - path: "hooks/tests/verify-phase-71.cjs"
      provides: "Unit tests for ENGN-01 through ENGN-06"
      min_lines: 80
  key_links:
    - from: "bin/lib/idle-suggestions.cjs"
      to: "bin/lib/frontmatter.cjs"
      via: "require('./frontmatter.cjs')"
      pattern: "extractFrontmatter"
    - from: "bin/lib/idle-suggestions.cjs"
      to: "bin/lib/state.cjs"
      via: "require('./state.cjs')"
      pattern: "stateExtractField"
    - from: "bin/lib/idle-suggestions.cjs"
      to: "bin/lib/core.cjs"
      via: "require('./core.cjs')"
      pattern: "stripShippedMilestones"
    - from: "bin/lib/idle-suggestions.cjs"
      to: "bin/lib/design.cjs"
      via: "require('./design.cjs')"
      pattern: "readManifest"
---

<objective>
Build the suggestion engine module and its test suite using TDD.

Purpose: Create `bin/lib/idle-suggestions.cjs` — a standalone, unit-testable CJS module that classifies the current phase, gathers suggestion candidates from STATE.md/ROADMAP.md/DESIGN-STATE.md/design-manifest.json, ranks them by fixed priority order, filters by time budget, and renders them in the locked tech-noir visual format. All six ENGN requirements are satisfied by this plan.

Output: Two files — `bin/lib/idle-suggestions.cjs` (engine module) and `hooks/tests/verify-phase-71.cjs` (test suite covering ENGN-01 through ENGN-06).
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-suggestion-engine/71-CONTEXT.md
@.planning/phases/71-suggestion-engine/71-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From bin/lib/frontmatter.cjs:
```javascript
// extractFrontmatter(rawMarkdown: string) => object|null
// Returns parsed YAML frontmatter as a JS object, or null if none found.
// Safe — no process.exit().
module.exports = { extractFrontmatter, reconstructFrontmatter, spliceFrontmatter, ... };
```

From bin/lib/state.cjs:
```javascript
// stateExtractField(body: string, fieldName: string) => string
// Extracts a named field section from STATE.md body text.
// Safe — no process.exit(). DO NOT call cmd* wrappers.
module.exports = { stateExtractField, stateReplaceField, writeStateMd, ... };
```

From bin/lib/core.cjs:
```javascript
// stripShippedMilestones(rawRoadmap: string) => string
// Removes <details>...</details> blocks (shipped milestones) from ROADMAP.md content.
// Safe — no process.exit().
module.exports = { output, error, safeReadFile, loadConfig, stripShippedMilestones, ... };
```

From bin/lib/design.cjs:
```javascript
// readManifest(cwd: string) => object|null
// Reads design-manifest.json, returns parsed object or null if file missing.
// Safe — no process.exit().
module.exports = { readManifest, writeManifest, updateManifestArtifact, ... };
```

From hooks/idle-suggestions.cjs (Phase 70 hook handler):
```javascript
// Lines 66-75: Placeholder block to be replaced by Plan 02.
// The hook resolves: cwd, sessionId, ndjsonPath, lastMeaningful event, suggPath, markerPath
// Engine will be called as: generateSuggestions({ cwd, event: lastMeaningful })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — Test suite and engine module for suggestion ranking, classification, and formatting</name>
  <files>hooks/tests/verify-phase-71.cjs, bin/lib/idle-suggestions.cjs</files>
  <read_first>
    - hooks/tests/verify-phase-70.cjs (test pattern to follow — manual runner, PASS/FAIL format, process.exit)
    - hooks/idle-suggestions.cjs (hook handler — understand the integration contract, lines 66-75 placeholder)
    - bin/lib/frontmatter.cjs (extractFrontmatter export — verify signature)
    - bin/lib/state.cjs (stateExtractField export — verify signature, understand blockers regex)
    - bin/lib/core.cjs (stripShippedMilestones export — verify it exists in module.exports)
    - bin/lib/design.cjs (readManifest export — verify it exists in module.exports)
    - .planning/phases/71-suggestion-engine/71-CONTEXT.md (locked decisions)
    - .planning/phases/71-suggestion-engine/71-RESEARCH.md (architecture patterns, code examples, pitfalls)
  </read_first>
  <behavior>
    - ENGN-01 classification: classifyPhase({ event_type: 'plan_started' }, ...) returns 'plan'; classifyPhase with DESIGN-STATE.md containing '[ ]' returns 'design'; classifyPhase with no data returns null
    - ENGN-02 performance: generateSuggestions() with fixture cwd completes in under 2000ms; source has zero 'await' keywords; source has zero 'http' or 'fetch' calls
    - ENGN-03 priority: rankSuggestions([{category:'next_phase',...},{category:'blocker',...}], 'execute') returns shown[0].category === 'blocker'
    - ENGN-04 next-phase: given ROADMAP.md fixture with unchecked Phase 72, output contains 'phase 72' or 'suggestion catalog'
    - ENGN-05 artifacts: given design-manifest.json fixture with completed artifact at path 'design/wireframe.md', output contains 'wireframe.md'
    - ENGN-06 filtering: given candidates with minutes > budget, rankSuggestions filters them out; stats footer shows correct cut:N count
    - Format: output contains all four category headers ('// blocker', '// next_phase', '// review', '// think'); empty categories show '-- none'; max 7 suggestions; stats footer matches pattern 'gen:N shown:N cut:N budget:~Nmin'
    - Resumption cost: block char maps correctly — low='░', med='▒', high='█'
    - Zero-state: when classifyPhase returns null, output is '// awaiting phase data...'
  </behavior>
  <action>
    Follow RED-GREEN-REFACTOR TDD cycle.

    **RED — Create test file first (`hooks/tests/verify-phase-71.cjs`):**

    Follow the Phase 70 test pattern exactly: `let passed = 0; let failed = 0;` counter, `function test(name, fn)` runner, PASS/FAIL console output, `process.exit(failed > 0 ? 1 : 0)`.

    Create tests that exercise `rankSuggestions()` and `generateSuggestions()` with in-memory fixture data. Use `node:assert` (`assert.strictEqual`, `assert.ok`, `assert.throws`). Tests should NOT read real STATE.md or ROADMAP.md — use temp directories with fixture files via `os.tmpdir()` + `fs.mkdtempSync`.

    Required test cases (minimum):
    1. `ENGN-01: classifyPhase returns plan for plan_started event` — call classifyPhase with `{ event_type: 'plan_started' }` and assert result is `'plan'`
    2. `ENGN-01: classifyPhase returns design when DESIGN-STATE.md has incomplete stages` — create temp dir with DESIGN-STATE.md containing `- [ ] wireframe`, call classifyPhase, assert `'design'`
    3. `ENGN-01: classifyPhase returns null on zero-state` — no STATE.md, no event → null
    4. `ENGN-02: no async/await in source` — read source file, assert no `await ` or `async ` keywords (except in comments)
    5. `ENGN-03: blockers sort above all other categories` — rankSuggestions with mixed candidates, assert shown[0].category === 'blocker'
    6. `ENGN-03: multiple blockers all appear before next_phase` — 2 blockers + 1 next_phase, assert shown[0] and shown[1] are blockers
    7. `ENGN-04: next-phase prep from ROADMAP.md` — create temp dir with ROADMAP.md fixture containing unchecked Phase 72, call generateSuggestions, assert output contains reference to phase 72
    8. `ENGN-05: completed artifact paths in suggestions` — create temp dir with design-manifest.json containing `{ "artifacts": { "wireframe": { "status": "complete", "path": "design/wireframe.md", "name": "Wireframe", "code": "WF" } } }`, call generateSuggestions, assert output contains `wireframe.md`
    9. `ENGN-06: time-bounded filtering removes over-budget suggestions` — rankSuggestions with candidate.minutes=60 and phaseType='plan' (budget 3-5min), assert candidate filtered out
    10. `ENGN-06: stats footer shows cut count` — same as above, assert output contains `cut:1` (or correct count)
    11. `FORMAT: all four category headers present` — call generateSuggestions, assert output contains `// blocker`, `// next_phase`, `// review`, `// think`
    12. `FORMAT: empty categories show -- none` — call with zero candidates for review category, assert output contains `-- none` after review header
    13. `FORMAT: max 7 suggestions` — provide 10 candidates, assert shown.length <= 7
    14. `FORMAT: resumption cost block chars` — assert low maps to `░`, med to `▒`, high to `█`
    15. `ZERO-STATE: null phase returns awaiting message` — generateSuggestions with empty cwd, assert output is `// awaiting phase data...`

    Run tests — all should FAIL (module does not exist yet).

    **GREEN — Create `bin/lib/idle-suggestions.cjs`:**

    Follow the `bin/lib/*.cjs` pattern: `'use strict';`, require Node built-ins and sibling modules, export functions.

    Module structure (all functions, no classes):

    ```
    generateSuggestions({ cwd, event, catalogPath }) → string (markdown)
    rankSuggestions(candidates, phaseType) → { shown: array, cut: number }
    classifyPhase(event, stateData, cwd) → string|null
    ```

    Only `generateSuggestions` and `rankSuggestions` are exported. `classifyPhase` is also exported for testability.

    Internal functions (not exported):
    - `loadStateData(cwd)` — reads STATE.md via `fs.readFileSync`, parses frontmatter via `extractFrontmatter()`, extracts body. Returns `{ fm, body }`. On error returns `{ fm: {}, body: '' }`.
    - `extractBlockers(body)` — regex: `/###?\s*(?:Blockers|Blockers\/Concerns|Concerns)\s*\n([\s\S]*?)(?=\n###?|\n##[^#]|$)/i`. Filters out `None` lines. Returns array of strings.
    - `getNextPhaseInfo(cwd)` — reads ROADMAP.md, calls `stripShippedMilestones()`, regex: `/-\s*\[ \]\s*\*\*Phase\s+(\d+):\s*([^*]+)\*\*/`. Returns `{ number, name }` or null.
    - `getCompletedArtifacts(cwd)` — calls `readManifest(cwd)`, filters `artifacts[*].status === 'complete'`, returns array of `{ code, path, name }`.
    - `checkDesignState(cwd)` — reads `.planning/DESIGN-STATE.md`, checks for `[ ]` pattern. Returns boolean (has incomplete stages).
    - `gatherCandidates(cwd, phaseType, stateData, catalogPath)` — assembles candidate array from all sources.
    - `slashFill(label, totalWidth)` — generates `// label ////...` separator. Default totalWidth=32.
    - `formatOutput(shown, cut, phaseType)` — renders final markdown string in locked tech-noir format.
    - `statsFooter(generated, shown, cut, budgetMin)` — returns `// gen:N shown:N cut:N budget:~Nmin`.

    **Phase classification logic (`classifyPhase`):**
    ```javascript
    const EVENT_TO_PHASE = {
      'plan_started': 'plan',
      'phase_started': null,       // resolve from STATE.md
      'phase_complete': 'review',
    };
    ```
    1. If DESIGN-STATE.md exists AND has `[ ]` → return `'design'`
    2. If event and EVENT_TO_PHASE[event.event_type] is defined → return that value
    3. If event.event_type === 'phase_started' → inspect STATE.md frontmatter `status` field, map to phase type
    4. Fallback: return `'default'`
    5. Zero-state (no event, no STATE.md): return `null`

    **Ranking logic (`rankSuggestions`):**
    ```javascript
    const CATEGORY_PRIORITY = { blocker: 0, next_phase: 1, review: 2, think: 3 };
    const PHASE_BUDGET = { research: 10, plan: 5, execute: 30, design: 15, review: 10, validation: 10, default: 10 };
    ```
    - Sort by `CATEGORY_PRIORITY[candidate.category]` ascending, then by resumption cost (low < med < high)
    - Filter out candidates where `candidate.minutes > PHASE_BUDGET[phaseType]`
    - Cap at 7 items
    - Return `{ shown: [...], cut: filteredCount }`

    **Candidate gathering (`gatherCandidates`):**
    - From blockers: each blocker string → `{ category: 'blocker', text: blocker, minutes: 5, resumption: 'high', filePath: null, source: 'blocker' }`
    - From next phase: if getNextPhaseInfo returns non-null → generate 2-3 prep suggestions: `{ category: 'next_phase', text: 'review phase N requirements', minutes: 3, resumption: 'low', ... }`
    - From artifacts: each completed artifact → `{ category: 'review', text: 'review: ' + artifact.name.toLowerCase(), minutes: 5, resumption: 'med', filePath: artifact.path, source: 'manifest' }`
    - Static fallback think entries (Phase 72 catalog not yet available):
      - `{ category: 'think', text: 'capture: what decisions shaped this phase?', minutes: 5, resumption: 'low' }`
      - `{ category: 'think', text: 'capture: what domain concepts need documentation?', minutes: 5, resumption: 'low' }`
      - `{ category: 'think', text: 'capture: what patterns emerged during this work?', minutes: 5, resumption: 'low' }`

    **3-file-read budget enforcement (ENGN-02):**
    - Read 1: STATE.md (always)
    - Read 2: ROADMAP.md (always, for next-phase)
    - Read 3: DESIGN-STATE.md if it exists, else design-manifest.json if it exists, else catalogPath if provided

    **Output format (locked by decisions):**
    ```
    // blocker ////////////////////
    █ address: {text}
      {minutes}min // resumption:{cost}

    // next_phase /////////////////
    ░ prep: {text}
      {minutes}min // resumption:{cost}

    // review /////////////////////
    -- none

    // think //////////////////////
    ░ capture: {text}
      {minutes}min // resumption:{cost}

    // gen:N shown:N cut:N budget:~Nmin
    ```

    Block char mapping: `{ low: '░', med: '▒', high: '█' }`
    Category verbs: `{ blocker: 'address', next_phase: 'prep', review: 'review', think: 'capture' }`
    Empty category: `-- none`
    All category headers ALWAYS present.
    All text lowercase.

    **Zero-state:** When classifyPhase returns null, generateSuggestions returns the string `// awaiting phase data...` (no category headers, no stats footer).

    **CRITICAL anti-patterns to avoid:**
    - NEVER call any function whose name starts with `cmd` — they call process.exit()
    - NEVER write to stdout inside the module — zero stdout constraint
    - NEVER use async/await or Promises — synchronous only
    - NEVER spawn child processes
    - NEVER call `output()` or `error()` from core.cjs

    Run tests — all should PASS.

    **REFACTOR** if needed — clean up, ensure all variable names are lowercase, ensure no dead code.
  </action>
  <verify>
    <automated>node hooks/tests/verify-phase-71.cjs</automated>
  </verify>
  <acceptance_criteria>
    - hooks/tests/verify-phase-71.cjs exits with code 0 (all tests pass)
    - hooks/tests/verify-phase-71.cjs contains at least 15 test() calls
    - bin/lib/idle-suggestions.cjs contains `module.exports = {` with `generateSuggestions` and `rankSuggestions`
    - bin/lib/idle-suggestions.cjs contains `require('./frontmatter.cjs')`
    - bin/lib/idle-suggestions.cjs contains `require('./state.cjs')`
    - bin/lib/idle-suggestions.cjs contains `require('./core.cjs')`
    - bin/lib/idle-suggestions.cjs contains `require('./design.cjs')`
    - bin/lib/idle-suggestions.cjs does NOT contain `console.log(` or `console.error(` or `process.stdout.write(`
    - bin/lib/idle-suggestions.cjs does NOT contain `async ` or `await ` (outside comments)
    - bin/lib/idle-suggestions.cjs does NOT contain `cmd` function calls (no cmdStateLoad, cmdRoadmapAnalyze, etc.)
    - bin/lib/idle-suggestions.cjs contains `// blocker` and `// next_phase` and `// review` and `// think` (category headers)
    - bin/lib/idle-suggestions.cjs contains `slashFill` function
    - bin/lib/idle-suggestions.cjs contains `CATEGORY_PRIORITY` or equivalent priority mapping
    - bin/lib/idle-suggestions.cjs contains `// awaiting phase data...` (zero-state output)
    - hooks/tests/verify-phase-71.cjs contains `ENGN-01` and `ENGN-02` and `ENGN-03` and `ENGN-04` and `ENGN-05` and `ENGN-06`
  </acceptance_criteria>
  <done>
    All 15+ tests pass. Engine module exports generateSuggestions and rankSuggestions. Module uses only safe utility functions from sibling CJS modules — no cmd* wrappers, no stdout, no async. Output matches locked tech-noir format with all four category headers, block char resumption encoding, stats footer, and 7-suggestion cap.
  </done>
</task>

</tasks>

<verification>
- `node hooks/tests/verify-phase-71.cjs` exits 0 with all tests passing
- `grep -c 'module.exports' bin/lib/idle-suggestions.cjs` returns 1
- `grep 'generateSuggestions\|rankSuggestions' bin/lib/idle-suggestions.cjs` shows both exports
- `grep -c 'console.log\|console.error\|process.stdout.write' bin/lib/idle-suggestions.cjs` returns 0
- `grep -c 'await \|async ' bin/lib/idle-suggestions.cjs` returns 0 (no async patterns)
</verification>

<success_criteria>
Engine module is independently testable with fixture data. All six ENGN requirements have dedicated test coverage. Module produces correctly formatted tech-noir output with fixed priority ranking, time-budget filtering, and graceful zero-state handling.
</success_criteria>

<output>
After completion, create `.planning/phases/71-suggestion-engine/71-01-SUMMARY.md`
</output>
