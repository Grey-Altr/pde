---
phase: 14-design-system-pde-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/system.md
  - commands/system.md
autonomous: true
requirements: [SYS-01, SYS-02, SYS-03]

must_haves:
  truths:
    - "/pde:system workflow file exists with 7-step pipeline structure matching brief.md pattern"
    - "Workflow instructs Claude to generate DTCG JSON with $value/$type leaves for all 7 token categories (color, typography, spacing, shadows, borders, motion, components)"
    - "Workflow instructs Claude to write per-category CSS files (8 files) and unified assets/tokens.css with inline content (no @import)"
    - "Workflow includes dark mode generation via two-pass strategy (light :root + @media prefers-color-scheme + [data-theme=dark])"
    - "Workflow includes SYS-preview.html with light/dark toggle and SYS-usage-guide.md from template"
    - "Workflow updates visual domain DESIGN-STATE.md, root DESIGN-STATE.md (with write-lock), and design-manifest.json"
    - "commands/system.md delegates to workflows/system.md (stub text removed)"
  artifacts:
    - path: "workflows/system.md"
      provides: "Full /pde:system skill workflow"
      min_lines: 400
    - path: "commands/system.md"
      provides: "Slash command delegation to workflow"
      contains: "@workflows/system.md"
  key_links:
    - from: "commands/system.md"
      to: "workflows/system.md"
      via: "@reference in process block"
      pattern: "@workflows/system.md"
    - from: "workflows/system.md"
      to: "bin/lib/design.cjs"
      via: "pde-tools.cjs design subcommands"
      pattern: "pde-tools.cjs.*design"
    - from: "workflows/system.md"
      to: "references/color-systems.md"
      via: "@reference for OKLCH palette algorithm"
      pattern: "@references/color-systems.md"
    - from: "workflows/system.md"
      to: "references/typography.md"
      via: "@reference for modular scale algorithm"
      pattern: "@references/typography.md"
    - from: "workflows/system.md"
      to: "templates/design-system.md"
      via: "@reference for usage guide output structure"
      pattern: "@templates/design-system.md"
---

<objective>
Create the /pde:system skill — a 7-step workflow that reads the product brief, generates a canonical DTCG 2025.10 JSON token set covering 7 categories with dark mode, derives per-category CSS files and a unified tokens.css, writes a browser-viewable preview page and usage guide, and registers all artifacts in DESIGN-STATE and the design manifest.

Purpose: This is the design token generator that all downstream pipeline skills (wireframe, critique, handoff) depend on. Without it, wireframes have no design system to consume.

Output: workflows/system.md (full skill workflow), commands/system.md (updated delegation)
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-design-system-pde-system/14-CONTEXT.md
@.planning/phases/14-design-system-pde-system/14-RESEARCH.md

@.planning/phases/12-design-pipeline-infrastructure/12-01-SUMMARY.md
@.planning/phases/13-problem-framing-pde-brief/13-01-SUMMARY.md

<interfaces>
<!-- Key infrastructure functions from design.cjs that the workflow calls via pde-tools.cjs -->

From bin/lib/design.cjs:
```javascript
// Directory init (idempotent)
function ensureDesignDirs(cwd) // Creates .planning/design/ + 6 subdirs + DESIGN-STATE.md + manifest

// Write-lock protocol
function acquireWriteLock(cwd, owner) // Returns {acquired: true/false}
function releaseWriteLock(cwd) // Always call after lock-acquire, even on error

// DTCG-to-CSS conversion
function dtcgToCssLines(tokens, prefix) // Recursively walks DTCG tree, emits "  --{prefix}{key}: {$value};" lines
// IMPORTANT: Skips keys starting with $ (so $extensions is NOT emitted). Dark mode tokens need separate pass.
function generateCssVars(tokens) // Wraps dtcgToCssLines output in ":root { ... }" — light mode ONLY

// Manifest operations
function updateManifestArtifact(cwd, code, fields) // Upserts artifact entry (Object.assign merge)
function cmdManifestSetTopLevel(cwd, field, value, raw) // Sets manifest[field] = value (FLAT key only, no dot-notation)
```

CLI invocation pattern (from pde-tools.cjs):
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design ensure-dirs
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design lock-acquire pde-system
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design lock-release
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS code SYS
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":true,"hasBrief":true}'
# Note: designCoverage must be set as a full JSON object since cmdManifestSetTopLevel does flat key assignment
```

From workflows/brief.md (7-step pattern to mirror):
```
Step 1/7: Initialize design directories (ensureDesignDirs)
Step 2/7: Check prerequisites (read brief, detect context)
Step 3/7: Probe MCP (Sequential Thinking)
Step 4/7: Core generation logic
Step 5/7: Write output artifacts
Step 6/7: Update domain DESIGN-STATE
Step 7/7: Update root DESIGN-STATE + manifest (write-lock, release, coverage flag)
```

From commands/brief.md (delegation pattern):
```markdown
---
name: pde:brief
description: ...
allowed-tools: [Read, Write, Edit, Bash, Glob, Grep, Task]
---
<process>
@workflows/brief.md
@references/skill-style-guide.md
</process>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workflows/system.md — full /pde:system skill workflow</name>
  <files>workflows/system.md</files>
  <read_first>
    - workflows/brief.md (canonical 7-step workflow pattern to mirror exactly)
    - references/color-systems.md (OKLCH palette algorithm, scale table, semantic mappings, dark mode strategy, product-type fallback palettes)
    - references/typography.md (modular scale algorithm, font pairing, preset mappings, line-height/letter-spacing derivation)
    - templates/design-system.md (output structure for SYS-usage-guide.md)
    - templates/design-manifest.json (manifest schema — SYS artifact shape, designCoverage flags)
    - templates/design-state-domain.md (domain DESIGN-STATE template for visual/ directory)
    - bin/lib/design.cjs (dtcgToCssLines, generateCssVars signatures — confirm behavior)
    - .planning/phases/14-design-system-pde-system/14-CONTEXT.md (locked decisions — token scope, dark mode, presets, output artifacts)
    - .planning/phases/14-design-system-pde-system/14-RESEARCH.md (architecture patterns, pitfalls, code examples)
  </read_first>
  <action>
Create workflows/system.md as a self-contained workflow file following the exact same structural pattern as workflows/brief.md. The file must have these sections in order:

**Top-level structure (mirrors brief.md):**

1. `<purpose>` block — one paragraph: generate DTCG design tokens from brief context, produce CSS custom properties and visual preview for downstream wireframe consumption.

2. `<required_reading>` block with these @references:
   - @references/skill-style-guide.md
   - @references/mcp-integration.md
   - @references/color-systems.md
   - @references/typography.md

3. `<flags>` section — table listing: --dry-run, --quick, --verbose, --no-mcp, --no-sequential-thinking, --preset (with values: minimal|corporate|playful|editorial). Copy flag behavior descriptions from brief.md for the shared flags. --preset is skill-specific: "Override default brief-derived token generation with a curated preset. Values: minimal, corporate, playful, editorial. Uses curated defaults from references/typography.md and references/color-systems.md."

4. `<process>` section — the 7-step pipeline:

**Step 1/7: Initialize design directories**
- Identical to brief.md Step 1: call `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design ensure-dirs`, parse JSON, halt on error, display progress.

**Step 2/7: Check prerequisites**
- Read the brief: Glob for `.planning/design/strategy/BRF-brief-v*.md`, sort by version number descending, read the highest version. If no brief exists:
  - If --preset flag is provided: warn that brief is missing but continue (preset provides defaults).
  - If no --preset: display error: "Error: No design brief found. Run /pde:brief first, or use --preset to generate tokens without a brief." HALT.
- Extract from brief: product name, product type (software/hardware/hybrid), platform, brand colors (if any), typography preferences (if any), product character (data-dense/marketing/reading).
- Determine input path: (a) brief with brand data, (b) brief without brand data (derive algorithmically), (c) --preset flag override.
- Check for existing design system: Glob for `.planning/design/visual/SYS-tokens.json`. If exists and --force not present, prompt user (same pattern as brief.md version check). If exists and --force present, auto-increment version N. If not exists, N=1.
- Display: "Step 2/7: Prerequisites satisfied. Brief: v{X} loaded. Input path: {a|b|c}. System version: v{N}."

**Step 3/7: Probe MCP (Sequential Thinking)**
- Identical to brief.md Step 3: check flags (--no-mcp, --no-sequential-thinking, --quick), probe mcp__sequential-thinking__think, set SEQUENTIAL_THINKING_AVAILABLE, display result.

**Step 4/7: Generate token data**
This is the core generation step. Write detailed instructions for Claude to:

*Color tokens (SYS-01, SYS-03):*
- If input path (a): extract brand colors from brief, convert to OKLCH if needed (or use directly if already OKLCH/hex).
- If input path (b): derive seed hue algorithmically from product name/domain keywords. Method at implementor's discretion (simple character-code hash modulo 360, keyword-to-hue lookup, etc.).
- If input path (c) --preset: use the curated seed colors from references/color-systems.md product-type fallback palettes section.
- Generate primary palette using OKLCH scale from references/color-systems.md:
  ```
  Steps: 50 (L=0.97, C*0.08), 100 (L=0.93, C*0.18), 200 (L=0.87, C*0.32),
         300 (L=0.77, C*0.55), 400 (L=0.67, C*0.80), 500 (L=0.55, C*1.00),
         600 (L=0.47, C*0.90), 700 (L=0.39, C*0.75), 800 (L=0.31, C*0.55),
         900 (L=0.23, C*0.35), 950 (L=0.15, C*0.20)
  ```
  Apply gamut clamping: `C_step = min(seedC * multiplier, C_safe_max)` where C_safe_max at L>0.90 is ~0.05, at L<0.20 is ~0.04, mid-range ~0.25.
- Generate secondary palette: complementary hue (seed_H + 180) % 360, same scale.
- Generate neutral palette: same algorithm with C=0.005-0.015, H=brand primary hue.
- Generate semantic palettes: success H~145, warning H~85, error H~25, info H~250, each with full 11-step scale.
- Generate semantic color aliases (light mode): --color-action = var(--color-primary-500), --color-bg-default = var(--color-neutral-50), --color-bg-surface = var(--color-neutral-100), --color-bg-elevated = white, --color-text-primary = var(--color-neutral-900), --color-text-secondary = var(--color-neutral-600), --color-text-muted = var(--color-neutral-400), --color-border = var(--color-neutral-200), --color-success = var(--color-success-500), --color-warning = var(--color-warning-500), --color-error = var(--color-error-500), --color-info = var(--color-info-500).
- Generate dark mode semantic overrides: --color-action = var(--color-primary-400), --color-bg-default = var(--color-neutral-900), --color-bg-surface = var(--color-neutral-800), --color-bg-elevated = var(--color-neutral-700), --color-text-primary = var(--color-neutral-50), --color-text-secondary = var(--color-neutral-300), --color-text-muted = var(--color-neutral-500), --color-border = var(--color-neutral-700).

*Typography tokens (SYS-03):*
- Determine scale ratio from brief context: data-dense -> Minor Third (1.200), marketing -> Perfect Fourth (1.333), reading -> Augmented Fourth (1.414), default -> Minor Third (1.200). If --preset flag: use preset mapping from references/typography.md (minimal=1.200, corporate=1.250, playful=1.333, editorial=1.414).
- Generate font size scale: xs (n=-2), sm (n=-1), base (n=0), lg (n=1), xl (n=2), 2xl (n=3), 3xl (n=4), 4xl (n=5). Formula: size = 1rem * ratio^n, round to 3 decimal places.
- Generate line-height per step: lineHeight = max(1.1, 1.6 - (step_index * 0.05)) where step_index is 0-7 for xs through 4xl.
- Generate letter-spacing: xs=+0.04em, sm=+0.02em, base=0, lg=-0.005em, xl=-0.01em, 2xl=-0.015em, 3xl=-0.02em, 4xl=-0.025em.
- Font families: --font-display (for headings) and --font-body (for body text). If brief specifies fonts, use those. Otherwise use system font stacks: display = "system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif", body = "system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif". If --preset, use preset recommendations from references/typography.md.
- Font weights: --font-weight-normal: 400, --font-weight-medium: 500, --font-weight-semibold: 600, --font-weight-bold: 700.

*Spacing tokens (SYS-03):*
- Standard scale with 4px base unit: 0=0px, px=1px, 0.5=2px, 1=4px, 2=8px, 3=12px, 4=16px, 5=20px, 6=24px, 8=32px, 10=40px, 12=48px, 16=64px, 20=80px, 24=96px.

*Shadow tokens:*
- 5 elevation levels (per CONTEXT.md): xs, sm, md, lg, xl.
  - --shadow-xs: 0 1px 2px 0 oklch(0 0 0 / 0.05)
  - --shadow-sm: 0 1px 3px 0 oklch(0 0 0 / 0.1), 0 1px 2px -1px oklch(0 0 0 / 0.1)
  - --shadow-md: 0 4px 6px -1px oklch(0 0 0 / 0.1), 0 2px 4px -2px oklch(0 0 0 / 0.1)
  - --shadow-lg: 0 10px 15px -3px oklch(0 0 0 / 0.1), 0 4px 6px -4px oklch(0 0 0 / 0.1)
  - --shadow-xl: 0 20px 25px -5px oklch(0 0 0 / 0.1), 0 8px 10px -6px oklch(0 0 0 / 0.1)

*Border tokens:*
- Radius: --radius-none: 0, --radius-sm: 0.125rem, --radius-md: 0.375rem, --radius-lg: 0.5rem, --radius-xl: 0.75rem, --radius-2xl: 1rem, --radius-full: 9999px.
- Width: --border-width-thin: 1px, --border-width-default: 1px, --border-width-thick: 2px.
- Style: --border-style-default: solid.

*Motion tokens (per CONTEXT.md — duration and easing only):*
- --duration-fast: 150ms, --duration-normal: 250ms, --duration-slow: 400ms.
- --easing-default: cubic-bezier(0.4, 0, 0.2, 1), --easing-in: cubic-bezier(0.4, 0, 1, 1), --easing-out: cubic-bezier(0, 0, 0.2, 1).

*Component tokens (per CONTEXT.md — .pde-btn, .pde-card, .pde-input + dark variants):*
- Define CSS classes using the generated design tokens (var() references to semantic tokens).
- .pde-btn (base), .pde-btn--primary, .pde-btn--secondary, .pde-btn--sm, .pde-btn--lg.
- .pde-card, .pde-card__header, .pde-card__body, .pde-card__footer.
- .pde-input, .pde-label, .pde-field, .pde-error.
- Dark variants: .dark .pde-btn--primary, .dark .pde-card, .dark .pde-input (explicit, not auto-adapting, per CONTEXT.md).

*Utility classes (per CONTEXT.md — spacing utilities, flex, width/gap):*
- Spacing: .mt-{scale}, .mb-{scale}, .ml-{scale}, .mr-{scale}, .mx-{scale}, .my-{scale}, .m-{scale}, .pt-{scale}, .pb-{scale}, .pl-{scale}, .pr-{scale}, .px-{scale}, .py-{scale}, .p-{scale} — for scale values 0, 1, 2, 3, 4, 5, 6, 8.
- Flex: .flex, .flex-col, .flex-row, .flex-wrap, .items-center, .items-start, .items-end, .justify-center, .justify-between, .justify-start, .justify-end.
- Width: .w-full, .w-auto, .w-1/2, .w-1/3, .w-2/3, .w-1/4, .w-3/4.
- Gap: .gap-{scale} for scale values 1, 2, 3, 4, 6, 8.
- Display: .block, .inline-block, .inline, .hidden.
- Text: .text-center, .text-left, .text-right.

Construct the full DTCG JSON token tree as a single object with all 7 top-level categories. Each leaf MUST have both $value and $type (W3C 2025.10 format). Dark mode semantics use $extensions: { "com.pde.condition": "dark" } on the semantic group.

If SEQUENTIAL_THINKING_AVAILABLE: use it for palette harmony reasoning when brand colors are absent (input path b).

**Step 5/7: Write output artifacts**

Write these 12 files using the Write tool:

1. `visual/SYS-tokens.json` — the complete DTCG JSON tree (canonical source). Pretty-printed with 2-space indent.

2. `visual/SYS-colors.css` — Color tokens. Structure:
   ```css
   /* SYS-colors.css -- Color Tokens */
   /* Generated by /pde:system | {date} */
   /* === Primitive Colors === */
   :root { --color-primary-50: oklch(...); ... all primitive palettes ... }
   /* === Semantic Colors (Light Mode) === */
   :root { --color-action: var(--color-primary-500); ... }
   /* === Dark Mode === */
   @media (prefers-color-scheme: dark) { :root { ... dark semantic overrides ... } }
   [data-theme="dark"] { ... same dark overrides ... }
   ```

3. `visual/SYS-typography.css` — Font families, sizes, weights, line-heights, letter-spacing in :root {}.

4. `visual/SYS-spacing.css` — Spacing scale in :root {}.

5. `visual/SYS-shadows.css` — 5 elevation levels in :root {}.

6. `visual/SYS-borders.css` — Radius + width + style in :root {}.

7. `visual/SYS-motion.css` — Duration + easing in :root {}.

8. `visual/SYS-components.css` — .pde-btn, .pde-card, .pde-input classes using var() references. Dark variants: .dark .pde-btn--primary { ... }.

9. `visual/SYS-utilities.css` — Spacing utilities, flex helpers, width/gap classes.

10. `assets/tokens.css` — FULL INLINE COPY of all CSS custom property declarations from files 2-9. NOT @import. Comment headers indicate category origin. This file must work standalone when opened as file:// URL.

11. `visual/SYS-preview.html` — Self-contained HTML page. Must include:
    - Inline `<style>` with all tokens (copy from assets/tokens.css) OR `<link>` to a relative path that works from visual/ to assets/.
    - Color swatches grid showing all palette steps with hex/oklch values.
    - Typography scale preview with heading/body examples.
    - Spacing scale visualization.
    - Component demos (buttons, cards, inputs).
    - Light/dark toggle: inline `<script>` (~10 lines) that toggles `document.documentElement.setAttribute('data-theme', ...)` between 'light' and 'dark'.
    - Clean layout, readable without external dependencies.

12. `visual/SYS-usage-guide.md` — Use templates/design-system.md as the output structure template. Fill all placeholders with actual generated token values, file paths, component examples, import instructions. Include actual computed token values in the Token Overview table.

Display after each file: "  -> Created: {path}"

Display: "Step 5/7: All 12 artifacts written."

**Step 6/7: Update visual domain DESIGN-STATE**
- Check if `.planning/design/visual/DESIGN-STATE.md` exists using Glob.
- If NOT exists: create from templates/design-state-domain.md, replacing {domain_name} with "visual", {Domain} with "Visual", {date} with ISO date.
- Add/update SYS artifact row in the Artifact Index table:
  ```
  | SYS | Design System | /pde:system | draft | v{N} | {MCP list or "none"} | -- | {YYYY-MM-DD} |
  ```
- Display: "Step 6/7: Visual DESIGN-STATE.md updated with SYS artifact entry."

**Step 7/7: Update root DESIGN-STATE and manifest**
- Acquire write lock: `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design lock-acquire pde-system` (same retry pattern as brief.md).
- Update root `.planning/design/DESIGN-STATE.md` using Edit:
  1. Cross-Domain Dependency Map: add row `| SYS | visual | BRF | current |`
  2. Quick Reference: add rows `| Design System | v{N} |` and `| Type Scale Ratio | {ratio_name} ({ratio_value}) |`
  3. Decision Log: append `| SYS | design system generated, {N} token categories | {date} |`
  4. Iteration History: append `| SYS-tokens.json | v{N} | Created by /pde:system | {date} |`
- ALWAYS release write lock: `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design lock-release`
- Update manifest:
  ```bash
  node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS code SYS
  node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS name "Design System"
  node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS type design-system
  node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS domain visual
  node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS path ".planning/design/visual/SYS-tokens.json"
  node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS status draft
  node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS version {N}
  ```
- Set coverage flag (CRITICAL: use full JSON object since cmdManifestSetTopLevel does flat key assignment):
  - First read current designCoverage: `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design coverage-check`
  - Then set full object: `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":true}'`
  - Note: must preserve any existing fields in designCoverage (e.g. hasBrief may already be set by /pde:brief). Read coverage-check output, merge hasDesignSystem:true, write back the full object.
- Display: "Step 7/7: Root DESIGN-STATE and manifest updated."

5. `<output>` section listing all artifacts produced (same format as brief.md).

6. Anti-patterns section (at bottom of `<process>`, before `</process>`):
   - NEVER use generateCssVars() for dark mode blocks (it only wraps in :root {})
   - NEVER use @import in assets/tokens.css (breaks file:// consumption)
   - NEVER write semantic tokens with raw oklch values — MUST use var(--color-primitive-...) aliases
   - NEVER skip write-lock for root DESIGN-STATE.md updates
   - NEVER forget visual/DESIGN-STATE.md creation (wireframe skill reads it)
   - NEVER use dot-notation with manifest-set-top-level (flat key only, pass full JSON object)
   - ALWAYS release write lock even on error
   - ALWAYS include $type on every DTCG leaf node (W3C 2025.10 requires leaf-level $type)

7. Summary display section (mirrors brief.md): table with Files created, Files modified, Next suggested skill (/pde:flows or /pde:wireframe), Elapsed time, Estimated tokens, MCP enhancements.
  </action>
  <verify>
    <automated>grep -c "Step [1-7]/7" workflows/system.md | xargs test 7 -le</automated>
    <automated>grep -q "pde-tools.cjs.*design ensure-dirs" workflows/system.md</automated>
    <automated>grep -q "pde-tools.cjs.*design lock-acquire" workflows/system.md</automated>
    <automated>grep -q "pde-tools.cjs.*design lock-release" workflows/system.md</automated>
    <automated>grep -q "pde-tools.cjs.*design manifest-update SYS" workflows/system.md</automated>
    <automated>grep -q "pde-tools.cjs.*design manifest-set-top-level" workflows/system.md</automated>
    <automated>grep -q "@references/color-systems.md" workflows/system.md</automated>
    <automated>grep -q "@references/typography.md" workflows/system.md</automated>
    <automated>grep -q "prefers-color-scheme: dark" workflows/system.md</automated>
    <automated>grep -q 'data-theme="dark"' workflows/system.md</automated>
    <automated>grep -q "SYS-tokens.json" workflows/system.md</automated>
    <automated>grep -q "assets/tokens.css" workflows/system.md</automated>
    <automated>grep -q "SYS-preview.html" workflows/system.md</automated>
    <automated>grep -q "SYS-usage-guide.md" workflows/system.md</automated>
    <automated>grep -q "dtcgToCssLines" workflows/system.md</automated>
    <automated>wc -l < workflows/system.md | xargs test 400 -le</automated>
  </verify>
  <acceptance_criteria>
    - workflows/system.md exists and contains >= 400 lines
    - Contains all 7 steps labeled "Step N/7:" (N=1 through 7)
    - Contains `<purpose>` block, `<required_reading>` block, `<flags>` section, `<process>` section, `<output>` section
    - Step 1 calls `pde-tools.cjs design ensure-dirs`
    - Step 2 globs for `BRF-brief-v*.md`, handles 3 input paths (brief+brand, brief-no-brand, preset), checks existing SYS version
    - Step 3 probes Sequential Thinking MCP with flag checks
    - Step 4 contains OKLCH scale table (50-950 with L and C multiplier values), modular type scale formula, spacing scale, shadow levels, border tokens, motion tokens, component classes (.pde-btn, .pde-card, .pde-input), utility classes
    - Step 5 writes 12 files: SYS-tokens.json, 8 per-category CSS files (colors, typography, spacing, shadows, borders, motion, components, utilities), assets/tokens.css, SYS-preview.html, SYS-usage-guide.md
    - Step 6 creates visual/DESIGN-STATE.md from template if absent, adds SYS row
    - Step 7 uses lock-acquire/lock-release protocol, updates root DESIGN-STATE.md (4 sections: Cross-Domain Map, Quick Reference, Decision Log, Iteration History), registers SYS artifact in manifest, sets designCoverage as full JSON object
    - Contains dark mode instructions: @media (prefers-color-scheme: dark) AND [data-theme="dark"]
    - Contains anti-patterns section warning about generateCssVars() for dark mode, @import in tokens.css, raw oklch in semantic tokens
    - Contains --preset flag with values minimal|corporate|playful|editorial
    - References @references/color-systems.md and @references/typography.md in required_reading
  </acceptance_criteria>
  <done>workflows/system.md is a complete, self-contained workflow file that an executor Claude can follow step-by-step to generate a full DTCG design system with 7 token categories, dark mode, per-category CSS files, unified tokens.css, preview HTML, and usage guide — mirroring the proven brief.md pattern exactly</done>
</task>

<task type="auto">
  <name>Task 2: Update commands/system.md — delegate to workflow</name>
  <files>commands/system.md</files>
  <read_first>
    - commands/system.md (current stub content to be replaced)
    - commands/brief.md (delegation pattern to follow)
  </read_first>
  <action>
Replace the stub content in commands/system.md with the standard delegation pattern used by commands/brief.md.

Keep the existing YAML frontmatter (name: pde:system, description, argument-hint, allowed-tools) unchanged.

Replace the `<objective>` and `<process>` blocks with:

```xml
<objective>
Execute the /pde:system command.
</objective>

<process>
@workflows/system.md
@references/skill-style-guide.md
</process>
```

Remove ALL of the following text that currently exists in the stub:
- "**Status:** Planned -- available in PDE v2 (design pipeline)."
- "This command generates a complete design system..."
- "Related design-pipeline commands: brief, flows, wireframe, mockup, critique, hig, iterate, handoff."
- "See references/skill-style-guide.md for documentation on the design pipeline workflow."
- The trailing `</output>` tag (malformed — brief.md does not have this)

The final file should be approximately 15-20 lines: YAML frontmatter + objective + process with two @references.
  </action>
  <verify>
    <automated>grep -q "@workflows/system.md" commands/system.md</automated>
    <automated>grep -q "@references/skill-style-guide.md" commands/system.md</automated>
    <automated>grep -qv "Planned -- available in PDE v2" commands/system.md</automated>
    <automated>grep -q "pde:system" commands/system.md</automated>
  </verify>
  <acceptance_criteria>
    - commands/system.md contains "@workflows/system.md" in the process block
    - commands/system.md contains "@references/skill-style-guide.md" in the process block
    - commands/system.md does NOT contain "Planned -- available in PDE v2"
    - commands/system.md does NOT contain "Related design-pipeline commands"
    - commands/system.md YAML frontmatter has name: pde:system
    - commands/system.md is approximately 15-20 lines (not the current 27-line stub)
  </acceptance_criteria>
  <done>commands/system.md delegates to workflows/system.md using the same pattern as commands/brief.md, with no stub/placeholder text remaining</done>
</task>

</tasks>

<verification>
1. `grep -c "Step [1-7]/7" workflows/system.md` returns >= 7 (all 7 steps present)
2. `wc -l < workflows/system.md` returns >= 400 (comprehensive workflow)
3. `grep -q "@workflows/system.md" commands/system.md` confirms delegation
4. `grep -qv "Planned" commands/system.md` confirms stub text removed
5. `node bin/lib/design.cjs --self-test` still passes (no infrastructure changes, regression check)
</verification>

<success_criteria>
- workflows/system.md exists as a 400+ line self-contained workflow mirroring the brief.md 7-step pattern
- All 7 token categories are covered: color (with OKLCH scale), typography (with modular scale), spacing, shadows, borders, motion, components
- Dark mode strategy documented: two-pass (light :root + @media + [data-theme])
- All 12 output artifacts specified: SYS-tokens.json, 8 CSS files, tokens.css, preview HTML, usage guide
- DESIGN-STATE and manifest update protocol matches brief.md pattern
- commands/system.md delegates cleanly with no stub text
- Existing self-test passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-system-pde-system/14-01-SUMMARY.md`
</output>
