---
phase: 41-linear-jira-integration
plan: "04"
type: execute
wave: 2
depends_on: ["41-01", "41-02"]
files_modified:
  - workflows/handoff-create-linear-issues.md
  - workflows/handoff-create-jira-tickets.md
autonomous: true
requirements: [LIN-03, JIRA-03]

must_haves:
  truths:
    - "handoff-create-linear-issues.md reads handoff spec, shows confirmation gate, and only creates Linear issue after explicit y/yes"
    - "handoff-create-jira-tickets.md reads handoff spec, pre-flights issue types, shows confirmation gate, and only creates Jira ticket after explicit y/yes"
    - "Both workflows refuse to write when user responds anything other than y/yes"
    - "Both workflows degrade gracefully when MCP server is unavailable"
  artifacts:
    - path: "workflows/handoff-create-linear-issues.md"
      provides: "Linear issue creation with confirmation gate"
      min_lines: 60
    - path: "workflows/handoff-create-jira-tickets.md"
      provides: "Jira ticket creation with confirmation gate and ADF description"
      min_lines: 70
  key_links:
    - from: "workflows/handoff-create-linear-issues.md"
      to: "bin/lib/mcp-bridge.cjs"
      via: "bridge.call('linear:create-issue')"
      pattern: "linear:create-issue"
    - from: "workflows/handoff-create-jira-tickets.md"
      to: "bin/lib/mcp-bridge.cjs"
      via: "bridge.call('jira:create-issue')"
      pattern: "jira:create-issue"
    - from: "workflows/handoff-create-linear-issues.md"
      to: ".planning/design/implementation/"
      via: "Glob for HND-handoff-spec-v*.md"
      pattern: "HND-handoff-spec"
---

<objective>
Create the handoff-create-linear-issues.md and handoff-create-jira-tickets.md workflow files for ticket creation with confirmation gates.

Purpose: These workflows enable creating Linear issues (LIN-03) and Jira tickets (JIRA-03) from handoff artifacts, with mandatory user confirmation before any write-back to external services (VAL-03 pattern).
Output: Two new workflow files following the handoff-create-prs.md pattern.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-linear-jira-integration/41-RESEARCH.md
@.planning/phases/40-github-integration/40-03-SUMMARY.md

<interfaces>
<!-- The executor must follow the handoff-create-prs.md pattern exactly. Key patterns: -->

From workflows/handoff-create-prs.md (reference implementation):
- Step 0: Load connection status + tool lookup via bridge, guard on connected status
- Step 1: Glob for HND-handoff-spec-v*.md, select latest version, extract title + summary
- Step 2: Gather additional details from user (branch name for PRs)
- Step 3: Display confirmation gate — show full details, prompt y/n
- Step 4: Create via MCP only after explicit y/yes — any other response = "No PRs created." + stop
- Anti-patterns: NEVER create without confirmation, NEVER hardcode raw tool names

From 41-RESEARCH.md — Linear create_issue:
```
Tool: mcp__linear__create_issue
Required: teamId (from mcp-connections.json), title (from handoff spec)
Optional: description (markdown), stateId, priority (0-4), cycleId, projectId
```

From 41-RESEARCH.md — Jira createJiraIssue:
```
Tool: mcp__atlassian__createJiraIssue
Pre-flight: call getJiraProjectIssueTypesMetadata to get valid issue types
Required: project.key, issuetype.name ("Story" or "Task"), summary
Description must be ADF format:
{"type":"doc","version":1,"content":[{"type":"paragraph","content":[{"type":"text","text":"<text>"}]}]}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create handoff-create-linear-issues.md workflow (LIN-03)</name>
  <files>workflows/handoff-create-linear-issues.md</files>
  <read_first>
    - workflows/handoff-create-prs.md
    - .planning/phases/41-linear-jira-integration/41-RESEARCH.md
    - bin/lib/mcp-bridge.cjs
  </read_first>
  <action>
Create `workflows/handoff-create-linear-issues.md` following the handoff-create-prs.md pattern, adapted for Linear issue creation.

```markdown
<purpose>
Create Linear issues from handoff artifacts. Reads the most recent HND-handoff-spec-v{N}.md, constructs issue payload with teamId from mcp-connections.json, presents a confirmation gate to the user, and only creates the issue after explicit user approval. Implements LIN-03. Satisfies VAL-03 (write-back confirmation gate).
</purpose>

<process>

## Step 0 — Initialize and check Linear availability

Load team info from mcp-connections.json and verify Linear is connected:

```bash
node --input-type=module <<'EOF'
import { createRequire } from 'module';
const req = createRequire(import.meta.url);
const b = req(`${process.env.CLAUDE_PLUGIN_ROOT}/bin/lib/mcp-bridge.cjs`);
const conn = b.loadConnections();
const lin = conn.connections && conn.connections.linear;
const teamId = lin && lin.teamId || '';
const teamName = lin && lin.teamName || '';
const status = lin && lin.status || 'not_configured';
let toolName = '';
try {
  const lookup = b.call('linear:create-issue', {});
  toolName = lookup.toolName;
} catch (err) {
  toolName = '';
}
process.stdout.write(JSON.stringify({ teamId, teamName, status, toolName }) + '\n');
EOF
```

Parse JSON. If `teamId` is empty or `status` is not `connected`, display:

```
Linear is not connected or no team configured.
Run /pde:connect linear to set up.
Issue creation skipped.
```

Stop. Do NOT proceed.

## Step 1 — Find handoff artifacts

Use the Glob tool to search for:
```
.planning/design/implementation/HND-handoff-spec-v*.md
```

If no files found:
```
No handoff specs found. Run /pde:handoff first to generate a handoff spec.
```
Stop.

Select the latest version (highest v{N} number). Read the file.

Extract:
- **Title:** First `# ` heading, or derive from filename.
- **Summary:** `## Summary` section first paragraph (up to 500 chars), or first non-heading paragraph.

## Step 2 — Construct issue payload

The issue will be created with:
- `teamId`: from mcp-connections.json (LINEAR_TEAM_ID)
- `title`: from handoff spec title
- `description`: from handoff spec summary (Linear supports markdown in description)

No additional user input needed (unlike PR creation which needs branch name). Linear issues only require teamId and title.

## Step 3 — Display confirmation gate (CRITICAL — no write without user approval)

Display the issue details for user review:

```
Linear issue to create:

  Title: <title>
  Team: <teamName> (<teamId>)
  Description: <first 100 chars of summary>...

Create this issue in Linear? (y/n)
```

Wait for user response.

**CRITICAL:** If the user responds with anything OTHER than `y` or `yes` (case-insensitive), display:

```
No issues created.
```

Stop immediately. Do NOT call any MCP tool. Do NOT create any issue.

Only proceed to Step 4 if the user explicitly responds with `y` or `yes`.

## Step 4 — Create issue via MCP (only after user confirms)

Using the tool name from Step 0 (`mcp__linear__create_issue`), call the tool in Claude Code's execution context with:
- `teamId`: the team ID from connections
- `title`: the title from handoff spec
- `description`: the summary from handoff spec

**If the call succeeds**, display:
```
Linear issue created successfully!
  Title: <title>
  Identifier: <identifier from response>
  URL: <url from response>
```

**If the call fails**, display:
```
Issue creation failed: <error message>

Common causes:
  - Linear MCP server not authenticated (run /mcp to re-authenticate)
  - Team ID '<teamId>' may be invalid (run /pde:connect linear --confirm to reselect)
```

</process>

<anti_patterns>
- NEVER create an issue without the user explicitly confirming with `y` or `yes`.
- NEVER call the raw MCP tool name without looking it up via `bridge.call('linear:create-issue')`.
- NEVER hardcode teamId — always read from mcp-connections.json.
- NEVER call any MCP tool before Step 3 confirmation is obtained. Steps 0-2 are read-only.
- NEVER proceed if Linear status is not `connected`.
</anti_patterns>
```
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && test -f workflows/handoff-create-linear-issues.md && grep -c "linear:create-issue" workflows/handoff-create-linear-issues.md && grep -c "confirmation" workflows/handoff-create-linear-issues.md && grep -c "No issues created" workflows/handoff-create-linear-issues.md && grep -c "teamId" workflows/handoff-create-linear-issues.md && grep -c "HND-handoff-spec" workflows/handoff-create-linear-issues.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/handoff-create-linear-issues.md exists
    - Contains `bridge.call('linear:create-issue'` or `b.call('linear:create-issue'`
    - Contains `mcp__linear__create_issue` (toolName reference)
    - Contains `teamId` read from mcp-connections.json (at least 4 occurrences)
    - Contains `HND-handoff-spec-v*.md` glob pattern
    - Contains confirmation gate with `Create this issue in Linear? (y/n)` prompt
    - Contains `No issues created.` message for non-yes responses
    - Contains `CRITICAL` annotation on confirmation gate step
    - Contains degraded mode message: `Linear is not connected`
    - Contains `<anti_patterns>` section matching handoff-create-prs.md pattern
    - Does NOT call any MCP tool before Step 3 confirmation
  </acceptance_criteria>
  <done>handoff-create-linear-issues.md creates Linear issues from handoff specs with mandatory y/yes confirmation gate, teamId from connections, and degraded mode handling.</done>
</task>

<task type="auto">
  <name>Task 2: Create handoff-create-jira-tickets.md workflow (JIRA-03)</name>
  <files>workflows/handoff-create-jira-tickets.md</files>
  <read_first>
    - workflows/handoff-create-prs.md
    - workflows/handoff-create-linear-issues.md
    - .planning/phases/41-linear-jira-integration/41-RESEARCH.md
    - bin/lib/mcp-bridge.cjs
  </read_first>
  <action>
Create `workflows/handoff-create-jira-tickets.md` following the same confirmation gate pattern, adapted for Jira with ADF description format and issue type pre-flight.

```markdown
<purpose>
Create Jira tickets from handoff artifacts. Reads the most recent HND-handoff-spec-v{N}.md, pre-flights valid issue types via getJiraProjectIssueTypesMetadata, constructs ticket payload with projectKey from mcp-connections.json and ADF-formatted description, presents a confirmation gate, and only creates the ticket after explicit user approval. Implements JIRA-03. Satisfies VAL-03 (write-back confirmation gate).
</purpose>

<process>

## Step 0 — Initialize and check Atlassian availability

Load project info from mcp-connections.json:

```bash
node --input-type=module <<'EOF'
import { createRequire } from 'module';
const req = createRequire(import.meta.url);
const b = req(`${process.env.CLAUDE_PLUGIN_ROOT}/bin/lib/mcp-bridge.cjs`);
const conn = b.loadConnections();
const atl = conn.connections && conn.connections.atlassian;
const projectKey = atl && atl.projectKey || '';
const projectName = atl && atl.projectName || '';
const siteUrl = atl && atl.siteUrl || '';
const status = atl && atl.status || 'not_configured';
let createToolName = '';
let typesToolName = '';
try {
  createToolName = b.call('jira:create-issue', {}).toolName;
  typesToolName = b.call('jira:get-project-types', {}).toolName;
} catch (err) {
  createToolName = '';
  typesToolName = '';
}
process.stdout.write(JSON.stringify({ projectKey, projectName, siteUrl, status, createToolName, typesToolName }) + '\n');
EOF
```

If `projectKey` is empty or `status` is not `connected`, display:

```
Atlassian (Jira) is not connected or no project configured.
Run /pde:connect atlassian to set up.
Ticket creation skipped.
```

Stop. Do NOT proceed.

## Step 1 — Find handoff artifacts

Use the Glob tool to search for:
```
.planning/design/implementation/HND-handoff-spec-v*.md
```

If no files found:
```
No handoff specs found. Run /pde:handoff first to generate a handoff spec.
```
Stop.

Select the latest version (highest v{N}). Read the file. Extract Title and Summary (same as Linear workflow).

## Step 2 — Pre-flight: Get valid issue types

Call `mcp__atlassian__getJiraProjectIssueTypesMetadata` (the typesToolName from Step 0) in Claude Code's execution context with:
- `projectKey`: from mcp-connections.json

From the response, find the first available issue type matching "Story" or "Task" (in that order of preference). If neither exists, use the first issue type in the list.

Store the selected issue type name as ISSUE_TYPE_NAME.

If the pre-flight call fails, default to `"Story"` and display:
```
Note: Could not verify issue types for project <projectKey>. Defaulting to "Story".
```

## Step 3 — Construct ticket payload

Build the Jira description in Atlassian Document Format (ADF) — Jira Cloud requires ADF, NOT plain markdown:

```json
{
  "type": "doc",
  "version": 1,
  "content": [
    {
      "type": "paragraph",
      "content": [
        {
          "type": "text",
          "text": "<summary from handoff spec, up to 500 chars>"
        }
      ]
    }
  ]
}
```

The full payload for createJiraIssue:
- `project`: `{ "key": "<projectKey>" }`
- `issuetype`: `{ "name": "<ISSUE_TYPE_NAME>" }`
- `summary`: title from handoff spec
- `description`: the ADF JSON structure above
- `priority`: `{ "name": "Medium" }`

## Step 4 — Display confirmation gate (CRITICAL — no write without user approval)

Display ticket details for user review:

```
Jira ticket to create:

  Project: <projectName> (<projectKey>)
  Type: <ISSUE_TYPE_NAME>
  Summary: <title>
  Description: <first 100 chars of summary>...
  Priority: Medium

Create this ticket in Jira? (y/n)
```

Wait for user response.

**CRITICAL:** If the user responds with anything OTHER than `y` or `yes` (case-insensitive), display:

```
No tickets created.
```

Stop immediately. Do NOT call any MCP tool. Do NOT create any ticket.

Only proceed to Step 5 if the user explicitly responds with `y` or `yes`.

## Step 5 — Create ticket via MCP (only after user confirms)

Using the createToolName from Step 0 (`mcp__atlassian__createJiraIssue`), call the tool in Claude Code's execution context with the constructed payload.

**If the call succeeds**, display:
```
Jira ticket created successfully!
  Key: <key from response>
  Summary: <title>
  URL: <siteUrl>/browse/<key>
```

**If the call fails**, display:
```
Ticket creation failed: <error message>

Common causes:
  - Atlassian MCP server not authenticated (run /mcp to re-authenticate)
  - Project key '<projectKey>' may be invalid
  - Issue type '<ISSUE_TYPE_NAME>' may not be available for this project
  - Description format may need adjustment (ADF structure issue)

Try: Run /pde:connect atlassian --confirm to verify project configuration.
```

</process>

<anti_patterns>
- NEVER create a ticket without the user explicitly confirming with `y` or `yes`.
- NEVER call the raw MCP tool name without looking it up via `bridge.call('jira:create-issue')`.
- NEVER hardcode projectKey or siteUrl — always read from mcp-connections.json.
- NEVER use plain markdown for the Jira description field — always wrap in ADF format.
- NEVER skip the pre-flight issue type check — required fields vary by project.
- NEVER call any write MCP tool before Step 4 confirmation is obtained. Steps 0-2 are read-only (pre-flight type check in Step 2 is a read operation).
- NEVER proceed if Atlassian status is not `connected`.
</anti_patterns>
```
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && test -f workflows/handoff-create-jira-tickets.md && grep -c "jira:create-issue" workflows/handoff-create-jira-tickets.md && grep -c "confirmation" workflows/handoff-create-jira-tickets.md && grep -c "No tickets created" workflows/handoff-create-jira-tickets.md && grep -c "projectKey" workflows/handoff-create-jira-tickets.md && grep -c "ADF\|Atlassian Document Format" workflows/handoff-create-jira-tickets.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/handoff-create-jira-tickets.md exists
    - Contains `bridge.call('jira:create-issue'` or `b.call('jira:create-issue'`
    - Contains `bridge.call('jira:get-project-types'` or `b.call('jira:get-project-types'`
    - Contains `mcp__atlassian__createJiraIssue` (toolName reference)
    - Contains `mcp__atlassian__getJiraProjectIssueTypesMetadata` (toolName reference)
    - Contains `projectKey` read from mcp-connections.json (at least 4 occurrences)
    - Contains ADF description format: `"type": "doc"` and `"version": 1`
    - Contains `HND-handoff-spec-v*.md` glob pattern
    - Contains confirmation gate with `Create this ticket in Jira? (y/n)` prompt
    - Contains `No tickets created.` message for non-yes responses
    - Contains `CRITICAL` annotation on confirmation gate step
    - Contains pre-flight issue type check (Step 2)
    - Contains `"Story"` or `"Task"` issue type selection logic
    - Contains `priority`: `{ "name": "Medium" }` in payload
    - Contains degraded mode message: `Atlassian (Jira) is not connected`
    - Contains `<anti_patterns>` section
    - Does NOT call createJiraIssue before Step 4 confirmation
  </acceptance_criteria>
  <done>handoff-create-jira-tickets.md creates Jira tickets from handoff specs with pre-flight issue type validation, ADF description format, mandatory y/yes confirmation gate, and degraded mode handling.</done>
</task>

</tasks>

<verification>
1. Both workflow files exist: `ls workflows/handoff-create-linear-issues.md workflows/handoff-create-jira-tickets.md`
2. Both have confirmation gates with "No issues/tickets created." on non-yes
3. Both read from mcp-connections.json (teamId for Linear, projectKey for Jira)
4. Both use bridge.call() for tool name lookup
5. Jira workflow uses ADF format for description
6. Jira workflow has pre-flight issue type check
7. Neither workflow calls write MCP tools before confirmation
</verification>

<success_criteria>
- handoff-create-linear-issues.md creates Linear issues with confirmation gate (LIN-03)
- handoff-create-jira-tickets.md creates Jira tickets with ADF description and confirmation gate (JIRA-03)
- Both follow handoff-create-prs.md pattern: glob for latest spec, extract title/summary, confirm, create
- Both refuse writes on non-yes response (VAL-03)
- Both degrade gracefully when server unavailable
</success_criteria>

<output>
After completion, create `.planning/phases/41-linear-jira-integration/41-04-SUMMARY.md`
</output>
