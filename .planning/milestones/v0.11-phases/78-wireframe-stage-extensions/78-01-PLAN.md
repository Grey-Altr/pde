---
phase: 78-wireframe-stage-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-78/wireframe-stage-extensions.test.mjs
  - workflows/wireframe.md
  - tests/phase-82/milestone-completion.test.mjs
autonomous: true
requirements: [WIRE-01, WIRE-02, WIRE-03]

must_haves:
  truths:
    - "wireframe.md contains a PRODUCT_TYPE experience guard that skips the software wireframe path and generates FLP and TML artifacts instead"
    - "wireframe.md contains FLP floor plan generation instructions referencing spaces-inventory.json with SVG zone boundaries, capacity annotations, flow arrows, accessibility routes, scale bar, and SCHEMATIC ONLY disclaimer"
    - "wireframe.md contains TML timeline generation instructions with Mermaid gantt parallel tracks and energy curve overlay"
    - "wireframe.md registers FLP and TML artifacts in design-manifest.json using pde-tools manifest-update commands"
    - "Phase 78 Nyquist test suite passes with all structural assertions green"
  artifacts:
    - path: "tests/phase-78/wireframe-stage-extensions.test.mjs"
      provides: "Nyquist structural assertions for WIRE-01, WIRE-02, WIRE-03"
      min_lines: 60
    - path: "workflows/wireframe.md"
      provides: "Step 4-EXP experience wireframe generation block with FLP and TML"
      contains: "Step 4-EXP"
    - path: "tests/phase-82/milestone-completion.test.mjs"
      provides: "WIRE-01/02/03 positive assertions replacing test.todo markers"
      contains: "Phase 78: WIRE-01"
  key_links:
    - from: "workflows/wireframe.md"
      to: ".planning/design/ux/spaces-inventory.json"
      via: "Glob read in Step 4-EXP"
      pattern: "spaces-inventory\\.json"
    - from: "workflows/wireframe.md"
      to: "design-manifest.json"
      via: "manifest-update FLP and manifest-update TML commands in Step 7"
      pattern: "manifest-update FLP|manifest-update TML"
---

<objective>
Add experience wireframe generation to wireframe.md — floor plan (FLP) as inline SVG and timeline (TML) as Mermaid gantt — with Wave 0 Nyquist tests written first, then workflow edits, then Phase 82 todo markers flipped to positive assertions.

Purpose: Phase 78 is the last uncompleted phase in v0.11. Enables Phase 79 critique (which HALTs without FLP) and Phase 81 production bible (which reads TML for run sheet data).
Output: Working wireframe.md experience block producing FLP-floor-plan-v1.html and TML-timeline-v1.html, registered in manifest.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-flow-diagrams/77-01-SUMMARY.md
@.planning/phases/78-wireframe-stage-extensions/78-RESEARCH.md
@.planning/phases/78-wireframe-stage-extensions/78-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From workflows/wireframe.md line 150 (Phase 74 stub to replace):
```
<!-- Experience product type — Phase 74 stub: floor plan (FLP) and timeline (TML) artifacts are added in Phase 78. Current behavior: proceed with software wireframe path as temporary fallback for experience product type. NEVER produce floor plans or timeline wireframes from this stub. -->
```

From workflows/wireframe.md line 148 (PRODUCT_TYPE variable already set):
```
Store as PRODUCT_NAME, PRODUCT_TYPE.
```

From workflows/wireframe.md line 293 (Step 4 header — insertion point for 4-EXP before software path):
```
### Step 4/7: Generate wireframe HTML per screen
```

From workflows/wireframe.md lines 1768-1801 (Step 7c-print — FLY/SIT/PRG manifest registration pattern to replicate for FLP/TML):
```bash
# FLY artifact (always for experience)
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLY code FLY
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLY name "Event Flyer"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLY type print-collateral
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLY domain physical
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLY path ".planning/design/physical/print/FLY-event-flyer-v1.html"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLY status draft
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLY version 1
```

From workflows/wireframe.md lines 1804-1818 (Step 7d — coverage flag read-merge-write pattern):
```bash
COV=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design coverage-check)
# Parse JSON, merge hasWireframes: true, preserve all 15 fields
```

From tests/phase-77/experience-flows.test.mjs lines 1-16 (test file structure pattern):
```javascript
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'fs';
import { resolve, join } from 'path';
import { fileURLToPath } from 'url';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');

function readWorkflow(name) {
  return readFileSync(join(ROOT, name), 'utf8');
}
```

From tests/phase-82/milestone-completion.test.mjs lines 329-332 (todo markers to replace):
```javascript
  // Phase 78: WIRE — experience wireframes
  test.todo('Phase 78: WIRE-01 — floor plan wireframe generated as SVG-in-HTML');
  test.todo('Phase 78: WIRE-02 — timeline wireframe generated as gantt-style HTML');
  test.todo('Phase 78: WIRE-03 — floor plan and timeline registered in manifest');
```

From .planning/phases/78-wireframe-stage-extensions/78-RESEARCH.md — spaces-inventory.json schema (Phase 77 output):
```json
{
  "schemaVersion": "1.0",
  "venueCapacity": 600,
  "zones": [
    {
      "id": "zone-main-floor",
      "name": "Main Floor",
      "capacity": 400,
      "densityTarget": "high",
      "mood": "peak energy",
      "adjacentTo": ["zone-secondary", "zone-egress"],
      "sightlines": "stage-facing"
    }
  ],
  "bottlenecks": [{ "location": "Entry Funnel", "type": "ingress" }],
  "emergencyEgress": [{ "zoneId": "zone-main-floor", "exitPath": "north wall" }]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 Nyquist test suite for WIRE-01 through WIRE-03</name>
  <files>tests/phase-78/wireframe-stage-extensions.test.mjs</files>
  <read_first>
    - tests/phase-77/experience-flows.test.mjs (test file structure pattern — replicate imports, readWorkflow helper, describe/test blocks)
    - workflows/wireframe.md (pre-edit state — tests must FAIL against current file to confirm Wave 0 contract)
    - .planning/phases/78-wireframe-stage-extensions/78-RESEARCH.md (validation architecture section — test map for exact assertions)
  </read_first>
  <behavior>
    - WIRE-01a: wireframe.md contains FLP generation instruction (string 'FLP' or 'floor plan')
    - WIRE-01b: wireframe.md contains PRODUCT_TYPE experience guard before FLP block (guard index < FLP index)
    - WIRE-01c: wireframe.md contains 'SCHEMATIC ONLY' disclaimer text
    - WIRE-01d: wireframe.md contains 'spaces-inventory.json' reference (hard prerequisite)
    - WIRE-01e: wireframe.md contains font-size minimum 14 instruction for zone labels
    - WIRE-01f: wireframe.md contains stroke-width minimum 3 instruction for wall/boundary strokes
    - WIRE-02a: wireframe.md contains TML generation instruction (string 'TML' or 'timeline')
    - WIRE-02b: wireframe.md contains 'gantt' or 'mermaid' keyword for TML rendering
    - WIRE-02c: wireframe.md contains 'energy curve' or 'energy arc' reference for TML overlay
    - WIRE-03a: wireframe.md contains 'manifest-update FLP' registration instruction
    - WIRE-03b: wireframe.md contains 'manifest-update TML' registration instruction
    - Isolation: experience wireframe references appear AFTER PRODUCT_TYPE experience guard (ordering check)
    - Isolation: Phase 74 stub 'NEVER produce floor plans' text is removed from wireframe.md
  </behavior>
  <action>
    Create `tests/phase-78/wireframe-stage-extensions.test.mjs` following the exact structure pattern from `tests/phase-77/experience-flows.test.mjs`:

    1. File header comment: `// Phase 78 — Wireframe Stage Extensions` + `// Nyquist structural assertions for WIRE-01 through WIRE-03.` + `// Written BEFORE workflow edits (Wave 0) — expected to FAIL until wireframe.md is updated.`

    2. Imports: `import { test, describe } from 'node:test';` + `import assert from 'node:assert/strict';` + `import { readFileSync } from 'fs';` + `import { resolve, join } from 'path';` + `import { fileURLToPath } from 'url';`

    3. Setup: `const __dirname = fileURLToPath(new URL('.', import.meta.url));` + `const ROOT = resolve(__dirname, '..', '..');` + `function readWorkflow(name) { return readFileSync(join(ROOT, name), 'utf8'); }`

    4. Describe blocks — one per requirement:

    `describe('WIRE-01: floor plan wireframe as SVG-in-HTML', () => { ... })` with tests:
    - Test FLP generation instruction present (content.includes('FLP') || content.includes('floor plan'))
    - Test PRODUCT_TYPE experience guard precedes FLP (guardIdx < flpIdx ordering)
    - Test SCHEMATIC ONLY disclaimer text present (content.includes('SCHEMATIC ONLY'))
    - Test spaces-inventory.json hard prerequisite reference (content.includes('spaces-inventory.json'))
    - Test font-size 14 minimum instruction (content.includes('font-size') && content.includes('14'))
    - Test stroke-width 3 minimum instruction (content.includes('stroke-width') && content.includes('3'))

    `describe('WIRE-02: timeline wireframe as gantt-style HTML', () => { ... })` with tests:
    - Test TML generation instruction present (content.includes('TML') || content.includes('timeline'))
    - Test gantt/mermaid reference present (content.includes('gantt') || content.includes('mermaid'))
    - Test energy curve overlay reference present (content.includes('energy curve') || content.includes('energy arc'))

    `describe('WIRE-03: FLP and TML manifest registration', () => { ... })` with tests:
    - Test manifest-update FLP present (content.includes('manifest-update FLP'))
    - Test manifest-update TML present (content.includes('manifest-update TML'))

    `describe('Isolation: experience wireframe ordering and stub removal', () => { ... })` with tests:
    - Test experience wireframe block appears after PRODUCT_TYPE guard (indexOf ordering)
    - Test Phase 74 stub 'NEVER produce floor plans' text is removed (!content.includes('NEVER produce floor plans'))

    5. Run tests and confirm they FAIL against current wireframe.md (expected — Wave 0 contract). Commit the test file with message: `test(78-01): add failing Nyquist tests for WIRE-01 through WIRE-03`
  </action>
  <verify>
    <automated>node --test tests/phase-78/wireframe-stage-extensions.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-78/wireframe-stage-extensions.test.mjs exists and contains `import { test, describe } from 'node:test'`
    - File contains exactly 4 describe blocks: 'WIRE-01', 'WIRE-02', 'WIRE-03', 'Isolation'
    - File contains at least 13 test() calls (6 WIRE-01 + 3 WIRE-02 + 2 WIRE-03 + 2 Isolation)
    - Running `node --test tests/phase-78/wireframe-stage-extensions.test.mjs 2>&1` produces FAILING tests (expected — Wave 0)
    - File does NOT import any npm packages — only node:test, node:assert/strict, fs, path, url
  </acceptance_criteria>
  <done>Wave 0 test file committed with all 13+ structural assertions. Tests fail against current wireframe.md (pre-edit state), confirming the red-green contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add experience wireframe generation block to wireframe.md (FLP + TML + manifest registration)</name>
  <files>workflows/wireframe.md, tests/phase-82/milestone-completion.test.mjs</files>
  <read_first>
    - workflows/wireframe.md (full file — specifically lines 148-160 for stub location, lines 293-300 for Step 4 header, lines 1567-1580 for Step 5 header, lines 1708-1820 for Step 7 manifest/coverage pattern)
    - .planning/phases/78-wireframe-stage-extensions/78-RESEARCH.md (Pattern 1-7 for exact block structure, SVG specs, Mermaid config, manifest commands)
    - tests/phase-78/wireframe-stage-extensions.test.mjs (assertions that must pass after this edit)
    - tests/phase-82/milestone-completion.test.mjs (lines 329-332 for WIRE todo markers to flip)
  </read_first>
  <action>
    Make three edits to wireframe.md and one edit to milestone-completion.test.mjs:

    **Edit 1 — Replace Phase 74 stub (line 150):**

    Replace the single line:
    ```
    <!-- Experience product type — Phase 74 stub: floor plan (FLP) and timeline (TML) artifacts are added in Phase 78. Current behavior: proceed with software wireframe path as temporary fallback for experience product type. NEVER produce floor plans or timeline wireframes from this stub. -->
    ```

    With the forward-reference comment:
    ```
    <!-- Experience product type — Phase 74 architecture: floor plan (FLP) and timeline (TML) wireframes added in Phase 78. See Step 4-EXP experience block below. -->
    ```

    **Edit 2 — Insert Step 4-EXP block BEFORE existing Step 4 header (before line 293):**

    Insert a new section between line 292 (the `---` separator) and line 293 (`### Step 4/7:`). The new block must contain:

    ```markdown
    #### Step 4-EXP: Experience wireframe generation (experience products only)

    **IF `PRODUCT_TYPE == "experience"`:**

    Experience products generate a floor plan (FLP) and timeline (TML) instead of software wireframes. Skip Steps 4a through 4f (software path) and Step 4-STITCH entirely.

    **4-EXP-1. Read spaces-inventory.json (HARD prerequisite for FLP):**

    Use the Glob tool to check `.planning/design/ux/spaces-inventory.json`.

    - If **present**: load the file contents as `SPACES_INVENTORY`. Parse the JSON to extract `zones`, `venueCapacity`, `bottlenecks`, and `emergencyEgress` arrays.
    - If **absent**: HALT with error:
      ```
      HALT: spaces-inventory.json not found.
      Floor plan generation requires zone data from /pde:flows.
      Run /pde:flows first, then retry /pde:wireframe.
      ```

    **4-EXP-2. Read TFL temporal flow (SOFT prerequisite for TML):**

    Use the Glob tool to check `.planning/design/ux/TFL-temporal-flow-v*.md`. Use the highest version number if multiple exist.

    - If **present**: load contents as `TFL_CONTENT`. Extract temporal arc stages and timing data for gantt chart population.
    - If **absent**: emit WARNING: `Warning: TFL temporal flow not found. Using brief temporal data as fallback for timeline generation.` Use brief Venue Constraints (doors, curfew) and Vibe Contract (peak timing) as fallback.

    **4-EXP-3. Generate FLP floor plan HTML (WIRE-01):**

    Generate a self-contained HTML file with inline SVG. The file must open at `file://` without a server — NO external dependencies (no CDN, no images, no fonts).

    Output file: `.planning/design/ux/wireframes/FLP-floor-plan-v1.html`

    **HTML structure:**

    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>FLP — {EVENT_NAME} — Floor Plan (Schematic)</title>
      <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: system-ui, sans-serif; background: #f5f5f5; padding: 24px; }
        h1 { font-size: 18px; margin-bottom: 8px; }
        .schematic-disclaimer {
          background: #fff8dc; border: 1px solid #e0c060; border-radius: 4px;
          padding: 8px 12px; font-size: 12px; margin-bottom: 16px; max-width: 840px;
        }
        .floor-plan-container { background: white; padding: 16px; display: inline-block; }
        svg { display: block; }
      </style>
    </head>
    <body>
      <h1>{EVENT_NAME} — Floor Plan</h1>
      <aside class="schematic-disclaimer">
        <strong>SCHEMATIC ONLY.</strong> Not to scale. Zone boundaries, dimensions, and
        infrastructure placements are illustrative. Verify all measurements against venue
        technical drawings before production.
      </aside>
      <div class="floor-plan-container">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 {WIDTH} {HEIGHT}"
             width="{WIDTH}" height="{HEIGHT}" role="img"
             aria-label="Floor plan schematic">
          <title>{EVENT_NAME} Floor Plan Schematic</title>
          <!-- Content generated from SPACES_INVENTORY -->
        </svg>
      </div>
    </body>
    </html>
    ```

    **SVG content requirements (HARD MINIMUMS from success criteria):**

    - **Zone boundaries:** `<rect>` or `<polygon>` elements with `stroke="#334155" stroke-width="3"` (minimum stroke-width: 3 — NEVER thinner). Fill with semi-transparent color `rgba(R,G,B,0.15)`.
    - **Zone labels:** `<text>` elements with `font-size="16" font-weight="bold"` for zone names. Capacity annotations use `font-size="14"` (minimum font-size: 14 — NEVER smaller). Format: `Cap: {capacity}`.
    - **Flow arrows:** Use SVG `<defs><marker id="arrowhead">` pattern with `<line>` or `<path>` elements using `marker-end="url(#arrowhead)"`. Draw arrows between adjacent zones based on `adjacentTo` relationships in SPACES_INVENTORY.
    - **Infrastructure placement:** Rectangles with labels for stage, bar, toilets, medical, entry. Place based on zone types and mood (e.g., stage in "peak energy" zone).
    - **Accessibility routes:** Dashed lines `stroke="#16a34a" stroke-width="2" stroke-dasharray="8,4"` showing step-free paths.
    - **Emergency egress:** Red exit markers at edges based on `emergencyEgress` array. Use `fill="#dc2626"` rectangles with white "EXIT" text.
    - **Scale bar:** `<line>` element at bottom of SVG with text labels. Include "NOT TO SCALE" text.
    - **SCHEMATIC ONLY text in SVG:** `<text>` element: `<text x="{cx}" y="{bottom}" font-size="13" text-anchor="middle" fill="#94a3b8" font-style="italic">SCHEMATIC ONLY — NOT TO SCALE</text>`

    **Layout algorithm (Claude judgment):**
    1. Start SVG viewBox at 840x600. If zone count > 4, increase height by 100px per additional zone.
    2. Place largest-capacity zone (main floor) as central rect.
    3. Place adjacent zones around main floor based on `adjacentTo` relationships.
    4. Place entry/exit funnel zones near bottom edge.
    5. Place emergency egress markers at logical edges.

    **4-EXP-4. Generate TML timeline HTML (WIRE-02):**

    Generate a self-contained HTML file with Mermaid gantt chart. Uses CDN script tag for Mermaid rendering (internet required; raw gantt text visible as fallback).

    Output file: `.planning/design/ux/wireframes/TML-timeline-v1.html`

    **HTML structure:**

    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>TML — {EVENT_NAME} — Event Timeline</title>
      <script src="https://cdn.jsdelivr.net/npm/mermaid/dist/mermaid.min.js"></script>
      <!-- Note: Mermaid CDN requires internet. Without connectivity, gantt text is visible as structured fallback. -->
      <style>
        body { font-family: system-ui, sans-serif; background: #f5f5f5; padding: 24px; max-width: 1200px; margin: 0 auto; }
        h1 { font-size: 18px; margin-bottom: 16px; }
        .energy-curve-container { background: white; padding: 16px; margin-bottom: 16px; border-radius: 4px; }
        .gantt-container { background: white; padding: 16px; border-radius: 4px; }
        .mermaid { display: block; }
        .operational-note { font-size: 11px; color: #6b7280; margin-top: 8px; }
      </style>
    </head>
    <body>
      <h1>{EVENT_NAME} — Event Timeline</h1>
      <section class="energy-curve-container">
        <h2 style="font-size:13px; color:#6b7280; margin-bottom:8px;">Energy Arc</h2>
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 80" width="100%" height="80"
             role="img" aria-label="Energy curve from arrival to departure">
          <!-- Bezier curve derived from Vibe Contract peak timing -->
          <!-- Stage labels: Arrival, Immersion, Peak, Comedown, Departure from TFL -->
        </svg>
      </section>
      <section class="gantt-container">
        <div class="mermaid">
    gantt
        title {EVENT_NAME} — Operational Timeline
        dateFormat HH:mm
        axisFormat %H:%M
        {sections generated from brief + TFL data}
        </div>
        <p class="operational-note">
          Timeline derived from brief Venue Constraints and Vibe Contract.
          [VERIFY WITH LOCAL AUTHORITY] for curfew and licensed hours compliance.
        </p>
      </section>
      <script>
        mermaid.initialize({ startOnLoad: true, theme: 'neutral', gantt: { barHeight: 20, fontSize: 12 } });
      </script>
    </body>
    </html>
    ```

    **Mermaid gantt requirements:**
    - Use `section` headers for parallel operational tracks: Load-in/Setup, Doors/Operations, Peak Programming, Load-out (minimum 3 sections).
    - Populate timing from brief Venue Constraints (doors, curfew, load-in) and Vibe Contract (peak timing).
    - Use `crit` tag for critical path items (headliner, curfew).
    - Limit to 20 entries maximum for legibility. Add comment noting multi-stage festivals may need multiple TML artifacts.

    **Energy curve requirements:**
    - Inline SVG (no CDN dependency) with `<path>` bezier curve.
    - Map event time span (doors to curfew) to SVG viewBox width.
    - Place control points at TFL arc stage fractions (arrival low, immersion rising, peak high, comedown falling, departure low).
    - Add `<text>` labels at key points along the curve.

    **Jump instruction at end of 4-EXP:** `**Jump to Step 5-EXP** (experience file write) — skip Steps 4a through 4f and Step 4-STITCH (software wireframe path).`

    **Edit 3 — Insert Step 5-EXP block in Step 5 area (before the software file write):**

    Add a Step 5-EXP block that writes the two HTML files:

    ```markdown
    #### Step 5-EXP: Experience wireframe file write (experience products only)

    **IF `PRODUCT_TYPE == "experience"`** (arriving from Step 4-EXP):

    1. Create the wireframes directory if it does not exist:
       ```bash
       mkdir -p .planning/design/ux/wireframes
       ```

    2. Write FLP_HTML to `.planning/design/ux/wireframes/FLP-floor-plan-v1.html`
    3. Write TML_HTML to `.planning/design/ux/wireframes/TML-timeline-v1.html`

    Display: `Step 5-EXP: Wrote FLP-floor-plan-v1.html and TML-timeline-v1.html to .planning/design/ux/wireframes/`

    **Jump to Step 6** — skip software file write path.
    ```

    **Edit 4 — Insert Step 7c-exp block BEFORE Step 7c-print (experience wireframe manifest registration):**

    Add a new subsection between Step 7b (WFR registration) and Step 7c-print:

    ```markdown
    #### 7c-exp. Experience wireframe manifest registration (experience products only)

    IF `PRODUCT_TYPE !== "experience"`: skip Step 7c-exp entirely.

    ```bash
    # FLP artifact (experience floor plan)
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLP code FLP
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLP name "Floor Plan"
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLP type experience-wireframe-floorplan
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLP domain ux
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLP path ".planning/design/ux/wireframes/FLP-floor-plan-v1.html"
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLP status draft
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update FLP version 1

    # TML artifact (experience timeline)
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update TML code TML
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update TML name "Event Timeline"
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update TML type experience-wireframe-timeline
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update TML domain ux
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update TML path ".planning/design/ux/wireframes/TML-timeline-v1.html"
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update TML status draft
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update TML version 1
    ```
    ```

    **Edit 5 — Update Step 7d coverage documentation (line ~1811):**

    In the coverage merge instruction text, add: "If FLP/TML experience wireframes were generated in Step 4-EXP (PRODUCT_TYPE === 'experience'), hasWireframes is already set to true by the standard merge."

    No new coverage flag needed — `hasWireframes: true` covers FLP and TML.

    **Edit 6 — Flip Phase 82 WIRE todo markers (tests/phase-82/milestone-completion.test.mjs lines 329-332):**

    Replace the three `test.todo()` lines with positive assertions (exact replacements from 78-RESEARCH.md Pattern 7):

    ```javascript
    // Phase 78: WIRE — experience wireframes (COMPLETE)
    test('Phase 78: WIRE-01 — floor plan wireframe generated as SVG-in-HTML', () => {
      const content = readWorkflow('workflows/wireframe.md');
      assert.ok(content.includes('FLP') || content.includes('floor plan'), 'WIRE-01: FLP floor plan generation missing from wireframe.md');
      assert.ok(content.includes('PRODUCT_TYPE') && content.includes('experience'), 'WIRE-01: PRODUCT_TYPE experience guard missing from wireframe.md');
      assert.ok(content.includes('SCHEMATIC ONLY') || content.includes('spaces-inventory.json'), 'WIRE-01: schematic disclaimer or spaces-inventory reference missing');
    });
    test('Phase 78: WIRE-02 — timeline wireframe generated as gantt-style HTML', () => {
      const content = readWorkflow('workflows/wireframe.md');
      assert.ok(content.includes('TML') || content.includes('timeline'), 'WIRE-02: TML timeline generation missing from wireframe.md');
      assert.ok(content.includes('gantt') || content.includes('mermaid'), 'WIRE-02: gantt/mermaid chart reference missing from wireframe.md');
    });
    test('Phase 78: WIRE-03 — floor plan and timeline registered in manifest', () => {
      const content = readWorkflow('workflows/wireframe.md');
      assert.ok(content.includes('manifest-update FLP') || (content.includes('FLP') && content.includes('manifest')), 'WIRE-03: FLP manifest registration missing from wireframe.md');
      assert.ok(content.includes('manifest-update TML') || (content.includes('TML') && content.includes('manifest')), 'WIRE-03: TML manifest registration missing from wireframe.md');
    });
    ```

    Also update any remaining "phase 78" stale comment references in milestone-completion.test.mjs header/describe blocks to reflect Phase 78 COMPLETE status.

    After all edits, run both test suites to confirm green:
    - `node --test tests/phase-78/wireframe-stage-extensions.test.mjs 2>&1`
    - `node --test tests/phase-82/milestone-completion.test.mjs 2>&1`

    Commit with message: `feat(78-01): add experience wireframe generation to wireframe.md (FLP + TML)`
  </action>
  <verify>
    <automated>node --test tests/phase-78/wireframe-stage-extensions.test.mjs 2>&1 && node --test tests/phase-82/milestone-completion.test.mjs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - workflows/wireframe.md contains `Step 4-EXP` heading text
    - workflows/wireframe.md contains `PRODUCT_TYPE == "experience"` guard in Step 4-EXP
    - workflows/wireframe.md contains `spaces-inventory.json` as hard prerequisite with HALT instruction
    - workflows/wireframe.md contains `FLP-floor-plan-v1.html` output path
    - workflows/wireframe.md contains `TML-timeline-v1.html` output path
    - workflows/wireframe.md contains `SCHEMATIC ONLY` disclaimer text
    - workflows/wireframe.md contains `font-size` with value `14` for zone label minimum
    - workflows/wireframe.md contains `stroke-width` with value `3` for wall stroke minimum
    - workflows/wireframe.md contains `gantt` and `mermaid` keywords for TML
    - workflows/wireframe.md contains `energy curve` or `energy arc` for TML overlay
    - workflows/wireframe.md contains `manifest-update FLP` and `manifest-update TML` commands
    - workflows/wireframe.md does NOT contain `NEVER produce floor plans` (Phase 74 stub removed)
    - workflows/wireframe.md contains `Skip Steps 4a through 4f` jump instruction
    - tests/phase-82/milestone-completion.test.mjs does NOT contain `test.todo('Phase 78: WIRE` (all 3 flipped)
    - `node --test tests/phase-78/wireframe-stage-extensions.test.mjs 2>&1` exits with code 0 (all pass)
    - `node --test tests/phase-82/milestone-completion.test.mjs 2>&1` exits with code 0 (all pass including WIRE assertions, 0 todos remaining)
  </acceptance_criteria>
  <done>wireframe.md contains complete Step 4-EXP/5-EXP/7c-exp experience wireframe generation block. Phase 74 stub replaced. FLP and TML artifact generation, file write, and manifest registration all present. All Phase 78 Nyquist tests pass. All Phase 82 milestone tests pass with 0 remaining todos.</done>
</task>

</tasks>

<verification>
1. `node --test tests/phase-78/wireframe-stage-extensions.test.mjs 2>&1` — all 13+ assertions pass
2. `node --test tests/phase-82/milestone-completion.test.mjs 2>&1` — all assertions pass, 0 test.todo markers remain
3. `node --test tests/**/*.test.mjs 2>&1` — full suite green, no regressions in phases 74-77
4. `grep -c 'NEVER produce floor plans' workflows/wireframe.md` — returns 0 (stub removed)
5. `grep -c 'manifest-update FLP' workflows/wireframe.md` — returns at least 1
6. `grep -c 'manifest-update TML' workflows/wireframe.md` — returns at least 1
7. `grep -c 'Step 4-EXP' workflows/wireframe.md` — returns at least 1
8. `grep -c 'SCHEMATIC ONLY' workflows/wireframe.md` — returns at least 1
</verification>

<success_criteria>
- wireframe.md generates FLP floor plan (SVG-in-HTML) and TML timeline (Mermaid gantt) for experience products
- FLP contains zone boundaries, capacity annotations, flow arrows, infrastructure, accessibility routes, scale bar, SCHEMATIC ONLY disclaimer
- FLP has no zone label below font-size 14, no wall stroke below stroke-width 3
- TML contains parallel operational tracks and energy curve overlay
- Both FLP and TML are registered in design-manifest.json via manifest-update commands
- Phase 74 stub replaced with forward-reference comment
- Software wireframe path (Steps 4a-4f) is skipped for experience products
- All Nyquist and milestone regression tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/78-wireframe-stage-extensions/78-01-SUMMARY.md`
</output>
