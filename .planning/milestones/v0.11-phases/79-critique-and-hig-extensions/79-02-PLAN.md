---
phase: 79-critique-and-hig-extensions
plan: 02
type: execute
wave: 2
depends_on: [79-01]
files_modified:
  - workflows/hig.md
autonomous: true
requirements: [PHIG-01, PHIG-02, PHIG-03, PHIG-04, PHIG-05, PHIG-06, PHIG-07]

must_haves:
  truths:
    - "hig.md contains seven physical interface guideline domains gated by productType === experience"
    - "Software products produce standard WCAG audit with no physical HIG domains"
    - "Experience products produce zero WCAG findings — POUR analysis is fully skipped"
    - "Every numerical target in physical HIG domains carries inline [VERIFY WITH LOCAL AUTHORITY] tag"
    - "HIG --light mode respects experience gate (produces physical accessibility check, not WCAG 5 mandatory)"
  artifacts:
    - path: "workflows/hig.md"
      provides: "Seven physical HIG domains replacing WCAG for experience products"
      contains: "Physical HIG Domain 1: Wayfinding"
  key_links:
    - from: "workflows/hig.md"
      to: "references/experience-disclaimer.md"
      via: "@references/experience-disclaimer.md loaded in experience branch"
      pattern: "experience-disclaimer"
    - from: "workflows/hig.md"
      to: "pde-tools.cjs design manifest-read"
      via: "productType detection gate at Step 4 entry"
      pattern: "productType.*experience"
    - from: "workflows/hig.md"
      to: ".planning/design/ux/wireframes/FLP-floor-plan-v*.html"
      via: "Glob discovery for audit target artifact"
      pattern: "FLP-floor-plan"
---

<objective>
Fill in the hig.md experience branch with seven physical interface guideline domains that replace WCAG checks for experience products.

Purpose: Physical events need wayfinding, acoustic zoning, queue UX, transaction speed, toilet ratio, hydration, and first aid audits instead of WCAG color contrast, focus indicators, and touch targets. The experience gate must also respect --light mode delegation from critique.md.

Output: workflows/hig.md with live experience branch replacing Phase 74 stub; all 15 Phase 79 Nyquist tests passing green.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-critique-and-hig-extensions/79-RESEARCH.md
@.planning/phases/79-critique-and-hig-extensions/79-01-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns from Plan 01 that this plan must align with -->

From workflows/critique.md (after Plan 01 edits):
- Experience gate: `IF productType === "experience":` → runs seven experience perspectives
- ELSE guard: software perspectives only execute for non-experience products
- Floor plan discovery: Glob for `FLP-floor-plan-v*.html`
- Disclaimer loading: `@references/experience-disclaimer.md`
- Finding table format: `| Severity | Effort | Location | Issue | Suggestion | Reference |`

From tests/phase-79/critique-hig-extensions.test.mjs (created in Plan 01):
- PHIG-01 test asserts: `content.includes('Physical HIG Domain 1: Wayfinding')`
- PHIG-02 test asserts: `content.includes('Physical HIG Domain 2: Acoustic Zoning')`
- PHIG-03 test asserts: `content.includes('Physical HIG Domain 3: Queue UX')`
- PHIG-04 test asserts: `content.includes('Physical HIG Domain 4: Transaction Speed')`
- PHIG-05 test asserts: `content.includes('Physical HIG Domain 5: Toilet Ratio')`
- PHIG-06 test asserts: `content.includes('Physical HIG Domain 6: Hydration')`
- PHIG-07 test asserts: `content.includes('Physical HIG Domain 7: First Aid')`
- Cross-type test asserts: `productType` or `PRODUCT_TYPE` present + ELSE guard after experience block
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fill hig.md experience branch with seven physical HIG domains</name>
  <files>workflows/hig.md</files>
  <read_first>
    - workflows/hig.md (FULL file — understand Step 4/7 structure, --light mode at line 191, full WCAG mode at line 223, the Phase 74 stub at line 49, POUR criteria, finding format, Steps 5-7 artifact/manifest/coverage patterns)
    - references/experience-disclaimer.md (disclaimer content, inline tag rules)
    - .planning/phases/79-critique-and-hig-extensions/79-RESEARCH.md (Pattern 3: hig.md Experience Branch structure, Pattern 4: Seven Physical HIG Domain specs, Pattern 6: HIG Artifact Output Format, Pitfall 4: --light mode gate ordering)
    - workflows/critique.md (see how the experience gate was implemented in Plan 01 for consistency)
  </read_first>
  <action>
Two insertion points in hig.md need modification:

**Insertion 1 — Step 4/7 experience gate (the behavioral change):**

The current Step 4/7 (line 189) begins with `**IF --light mode (LIGHT_MODE=true):**`. The experience gate must be inserted BEFORE the --light check. Per Pitfall 4 in research: productType check must occur before light mode check.

Replace the current Step 4 opening structure with:

```markdown
### Step 4/7: Execute audit

**Read PRODUCT_TYPE from design manifest:**

```bash
MANIFEST=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-read)
if [[ "$MANIFEST" == @file:* ]]; then MANIFEST=$(cat "${MANIFEST#@file:}"); fi
```

Parse `productType` from the JSON result.

**IF productType === "experience":**

  **Switch audit target to floor plan:**

  Use the Glob tool to check for `.planning/design/ux/wireframes/FLP-floor-plan-v*.html`.
  - If found: SET AUDIT_ARTIFACT = floor plan path (highest version). SET FIDELITY_LEVEL = lofi (floor plans are always schematic).
  - If not found: HALT with error:
    ```
    Error: No floor plan found for experience product HIG audit.
      /pde:hig for experience products requires the floor plan artifact (FLP).
      Run /pde:wireframe first to generate the floor plan, then re-run /pde:hig.
    ```

  Load @references/experience-disclaimer.md into context.

  **IF --light mode (LIGHT_MODE=true) AND productType === "experience":**

    Run abbreviated physical accessibility check only (matching critique Perspective 2 scope):
    - Step-free access routes to all primary zones
    - BSL interpreter sightlines at performance areas
    - Quiet/sensory zone presence
    - Wheelchair viewing platform presence
    - Accessible toilet facilities count

    Output using critique Perspective 3 table format:
    | Severity | Effort | Location | Issue | Suggestion | Reference |
    |----------|--------|----------|-------|------------|-----------|

    Tag output: `[Physical HIG -- /pde:hig --light experience mode]`

    **STOP HERE for --light experience mode. Do NOT proceed to Steps 5-7.**

  **IF full mode AND productType === "experience":**

    Run seven physical interface guideline domains:

    #### Physical HIG Domain 1: Wayfinding
    [Full checklist from 79-RESEARCH.md Pattern 4 PHIG-01:
    - Signage at all decision points (entry -> main zones, zones -> toilets/bar/exit)
    - Low-light readability: retro-reflective or illuminated, 75mm letter height for 10m viewing distance [VERIFY WITH LOCAL AUTHORITY]
    - Multilingual support if international audience in brief
    - Accessibility wayfinding: step-free routes distinctly signed
    Severity: No signage at primary entry = Critical, missing at secondary = Major, no low-light for night event = Major, missing multilingual for international = Minor]

    #### Physical HIG Domain 2: Acoustic Zoning
    [Full checklist PHIG-02:
    - Conversation-possible zone at < 70dB adjacent to high-volume area [VERIFY WITH LOCAL AUTHORITY]
    - Zone transition: gradual buffer zones between high/low volume
    - Hearing protection notice for zones > 85dB sustained [VERIFY WITH LOCAL AUTHORITY]
    Severity: No conversation-possible zone = Major, abrupt adjacency = Minor]

    #### Physical HIG Domain 3: Queue UX
    [Full checklist PHIG-03:
    - Wait time communication: status board position at entry and bar
    - Weather protection: covered queue for entry gateline
    - Skip-queue tiers: separate VIP gateline if in brief
    - Queue capacity: physical space for max queue length without extending into circulation]

    #### Physical HIG Domain 4: Transaction Speed
    [Full checklist PHIG-04:
    - Bar order target < 90s per transaction [VERIFY WITH LOCAL AUTHORITY], derive from bar station count and peak hourly attendance
    - Entry processing target < 30s per person [VERIFY WITH LOCAL AUTHORITY], derive from gateline width and arrival peak
    - Cashless vs cash: bar placement supports cashless-only if indicated in brief]

    #### Physical HIG Domain 5: Toilet Ratio
    [Full checklist PHIG-05:
    - Female ratio minimum 1:75 [VERIFY WITH LOCAL AUTHORITY], use 50/50 split if not specified
    - Male ratio minimum 1:100 [VERIFY WITH LOCAL AUTHORITY]
    - Accessible facilities minimum 1 per provision unit [VERIFY WITH LOCAL AUTHORITY]
    - Distance: facilities within 150m from any zone [VERIFY WITH LOCAL AUTHORITY]
    Severity: Ratio below minimum = Critical, accessible absent = Critical, distance >150m = Major]

    #### Physical HIG Domain 6: Hydration
    [Full checklist PHIG-06:
    - Free water points minimum 1 per 500 attendees [VERIFY WITH LOCAL AUTHORITY — some jurisdictions mandate at licensed events]
    - Visibility: positioned in high-traffic areas, not obscured
    - Signage: "Free Water" conspicuous at wayfinding decision points
    Severity: No free water = Critical, not signed = Major]

    #### Physical HIG Domain 7: First Aid
    [Full checklist PHIG-07:
    - Response time: trained first aider within 2 minutes from any point (approx 170m walking) [VERIFY WITH LOCAL AUTHORITY]
    - First aid post clearly identified on floor plan with medical symbol
    - Staffing ratio 1 first aider per 100-250 attendees [VERIFY WITH LOCAL AUTHORITY]
    - Defibrillator (AED) positioned and signed [VERIFY WITH LOCAL AUTHORITY — mandatory in some jurisdictions for events over certain thresholds]
    Severity: No first aid post = Critical, response >2min = Critical, no AED for >500 = Major]

    **SKIP all WCAG criteria and POUR analysis entirely.** Do NOT produce findings for color contrast, keyboard, focus, touch targets, or any WCAG criterion.

    Tag all output: `[Physical HIG -- /pde:hig experience mode]`

    Proceed to Step 5/7 with PHYSICAL_HIG_MODE = true.

ELSE:

  **Standard WCAG/HIG audit path (existing behavior for software/hardware/hybrid products):**

  [Existing --light mode and full WCAG audit content remains here unchanged]
```

**Insertion 2 — Phase 74 stub removal (line 49):**

Remove or replace the Phase 74 stub comment at line 49 with:
```markdown
<!-- Experience product type: physical HIG domains implemented in Step 4/7 — see productType gate. Required reading for experience mode: @references/experience-disclaimer.md -->
```

**Insertion 3 — required_reading for experience mode:**

In the `<required_reading>` block (lines 51-57), add `@references/experience-disclaimer.md` if not already present. (Check — it may already be wired from Phase 74.)

**Insertion 4 — Step 5/7 artifact format for experience mode:**

In Step 5/7 (Write HIG audit artifact), add a conditional for PHYSICAL_HIG_MODE:

If PHYSICAL_HIG_MODE = true:
- Use frontmatter `Mode: physical-hig-experience` and `Artifact: Floor Plan FLP-v{N}`
- Replace WCAG section headers with physical domain headers
- Replace POUR compliance table with Physical HIG Domains summary table:
  `| Domain | Status | Findings | Priority |` with rows for all 7 domains
- Keep Findings by Severity and Recommendations sections

**Insertion 5 — Step 7/7 manifest update for experience mode:**

In Step 7/7, when updating manifest, if PHYSICAL_HIG_MODE = true:
- Set artifact type to `physical-hig-audit` instead of `hig-audit`
- Coverage flag `hasHigAudit: true` remains the same (same flag name regardless of mode)

CRITICAL structural requirements:
- The productType check MUST come BEFORE the --light mode check (Pitfall 4)
- The ELSE block MUST contain the entire existing WCAG audit path
- The experience branch MUST NOT produce any WCAG findings
- Floor plan (FLP) discovery uses Glob pattern matching critique.md
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-79/critique-hig-extensions.test.mjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - workflows/hig.md contains `Physical HIG Domain 1: Wayfinding`
    - workflows/hig.md contains `Physical HIG Domain 2: Acoustic Zoning`
    - workflows/hig.md contains `Physical HIG Domain 3: Queue UX`
    - workflows/hig.md contains `Physical HIG Domain 4: Transaction Speed`
    - workflows/hig.md contains `Physical HIG Domain 5: Toilet Ratio`
    - workflows/hig.md contains `Physical HIG Domain 6: Hydration`
    - workflows/hig.md contains `Physical HIG Domain 7: First Aid`
    - workflows/hig.md contains `productType` or `PRODUCT_TYPE` gate
    - workflows/hig.md contains `ELSE` after experience branch
    - workflows/hig.md contains at least 5 occurrences of `[VERIFY WITH LOCAL AUTHORITY]`
    - workflows/hig.md contains `FLP-floor-plan` for artifact discovery
    - workflows/hig.md contains `physical-hig-experience` in Step 5 artifact format
    - workflows/hig.md contains `physical-hig-audit` manifest type
    - The Phase 74 stub comment (`Phase 74 stub: physical interface guidelines`) is removed or replaced
    - The productType check appears BEFORE the --light mode check in Step 4 (line numbers confirm ordering)
    - All 20+ tests in critique-hig-extensions.test.mjs pass: `node --test tests/phase-79/critique-hig-extensions.test.mjs` exits 0
    - Phase 74 regression tests still pass: `node --test tests/phase-74/experience-regression.test.mjs` exits 0
  </acceptance_criteria>
  <done>hig.md contains seven physical HIG domains with productType gate before --light mode check, ELSE guard for software path, disclaimer enforcement on numerical targets, floor plan artifact detection, physical-hig-experience artifact format, and physical-hig-audit manifest type; all Phase 79 and Phase 74 tests pass green</done>
</task>

</tasks>

<verification>
After task completes:
1. `node --test tests/phase-79/critique-hig-extensions.test.mjs` — ALL 20+ tests pass green (0 failures)
2. `node --test tests/phase-74/experience-regression.test.mjs` — Phase 74 regression suite passes
3. `grep -c "Physical HIG Domain" workflows/hig.md` returns 7
4. `grep -c "VERIFY WITH LOCAL AUTHORITY" workflows/hig.md` returns >= 5
5. `grep -c "Experience Perspective" workflows/critique.md` returns 7 (from Plan 01, unmodified)
6. `grep "ELSE" workflows/hig.md` confirms experience/software path isolation
7. `grep -n "productType\|PRODUCT_TYPE" workflows/hig.md | head -1` shows line number before `--light` check line number
</verification>

<success_criteria>
- Seven physical HIG domains present in hig.md with proper productType gate
- productType gate executes BEFORE --light mode check (Pitfall 4 prevention)
- Light mode + experience = abbreviated physical accessibility only (not WCAG 5 mandatory)
- Full mode + experience = all seven physical HIG domains (no WCAG)
- Non-experience products get standard WCAG/HIG audit unchanged
- Regulatory disclaimer inline on every numerical threshold
- Floor plan (FLP) artifact detection with HALT on absence
- Physical HIG audit artifact format with Mode: physical-hig-experience
- All 15 Phase 79 Nyquist tests pass green
- Phase 74 regression unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/79-critique-and-hig-extensions/79-02-SUMMARY.md`
</output>
