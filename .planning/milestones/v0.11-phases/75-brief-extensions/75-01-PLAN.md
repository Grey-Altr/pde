---
phase: 75-brief-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-75/brief-extensions.test.mjs
  - workflows/brief.md
  - templates/design-brief.md
autonomous: true
requirements:
  - BREF-01
  - BREF-02
  - BREF-03
  - BREF-04
  - BREF-05
must_haves:
  truths:
    - "Running /pde:brief for an experience product generates Promise Statement, Vibe Contract, Audience Archetype, Venue Constraints, and Repeatability Intent sections in the brief"
    - "Running /pde:brief for a software product produces no experience-specific sections"
    - "All venue constraint regulatory values carry [VERIFY WITH LOCAL AUTHORITY] inline tags"
    - "Experience sections are guarded by a product_type == experience conditional that precedes them in the file"
  artifacts:
    - path: "tests/phase-75/brief-extensions.test.mjs"
      provides: "Nyquist structural assertions for BREF-01 through BREF-05"
      contains: "Promise Statement"
    - path: "workflows/brief.md"
      provides: "Experience-specific brief sections in Step 5"
      contains: "Promise Statement"
    - path: "templates/design-brief.md"
      provides: "Template with experience type and section stubs"
      contains: "experience"
  key_links:
    - from: "workflows/brief.md"
      to: "templates/design-brief.md"
      via: "Type enum and section structure must match"
      pattern: "software.*hardware.*hybrid.*experience"
    - from: "workflows/brief.md"
      to: "references/experience-disclaimer.md"
      via: "VERIFY WITH LOCAL AUTHORITY inline tags in Venue Constraints"
      pattern: "VERIFY WITH LOCAL AUTHORITY"
---

<objective>
Add five experience-specific brief sections to workflows/brief.md and sync the design-brief.md template — with Wave 0 Nyquist tests written first.

Purpose: Phase 75 captures the five physical design inputs (promise statement, vibe contract, audience archetype, venue constraints, repeatability intent) that feed all downstream experience artifact generation in Phases 76-81.

Output: Working brief.md experience extensions with passing Nyquist test suite.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-brief-extensions/75-RESEARCH.md

<interfaces>
<!-- Key structures the executor needs from current codebase -->

From workflows/brief.md Step 5 (lines 293-296) — Product Type section:
```markdown
**`## Product Type`**

**Type:** {software | hardware | hybrid}
**Platform:** {web | mobile | desktop | embedded | multi-platform}
```

From workflows/brief.md Step 5 (lines 306-321) — Design Constraints clauses:
```markdown
For `software` type, the Design Constraints table MUST cover:
- Screen resolution targets ...
For `hardware` type, the Design Constraints table MUST cover:
- Physical dimensions ...
For `hybrid` type, include BOTH software AND hardware constraint categories, PLUS:
- Integration protocol ...
```

From workflows/brief.md Step 5 (lines 392-404) — Last common section before footer:
```markdown
**`## Scope Boundaries`**
...
Derive in-scope items from PROJECT.md's stated goals.
```

From templates/design-brief.md (line 19):
```markdown
**Type:** {software | hardware | hybrid}
```

From tests/phase-74/experience-regression.test.mjs (lines 1-12) — test file pattern:
```javascript
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'fs';
import { resolve, join } from 'path';
import { fileURLToPath } from 'url';
const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 Nyquist test scaffold for BREF-01 through BREF-05</name>
  <files>tests/phase-75/brief-extensions.test.mjs</files>
  <read_first>
    - tests/phase-74/experience-regression.test.mjs (pattern to follow — imports, ROOT resolution, describe/test structure)
    - workflows/brief.md (current state — tests must FAIL against this state before implementation)
  </read_first>
  <action>
Create `tests/phase-75/brief-extensions.test.mjs` following the exact pattern from `tests/phase-74/experience-regression.test.mjs`.

File structure:
```javascript
// Phase 75 — Brief Extensions
// Nyquist structural assertions for BREF-01 through BREF-05.
// Written BEFORE workflow edits (Wave 0) — expected to FAIL until brief.md is updated.

import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'fs';
import { resolve, join } from 'path';
import { fileURLToPath } from 'url';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');
```

Seven describe blocks, each with one or more tests:

1. `describe('BREF-01: promise statement section in brief.md')` — test that `workflows/brief.md` contains the string `Promise Statement`.

2. `describe('BREF-02: vibe contract section in brief.md')` — test that brief.md contains `Vibe Contract` AND `emotional arc`.

3. `describe('BREF-03: audience archetype section in brief.md')` — test that brief.md contains `Audience Archetype` AND `mobility needs`.

4. `describe('BREF-04: venue constraints section in brief.md')` — test that brief.md contains `Venue Constraints` AND (`Curfew` or `curfew`) AND (`Noise limits` or `noise limits`).

5. `describe('BREF-05: repeatability intent section in brief.md')` — test that brief.md contains `Repeatability Intent` AND (`one-off` or `series`).

6. `describe('Cross-type regression: experience sections guarded by product_type conditional')` — two tests:
   - Test that a `product_type == "experience"` or `product_type === 'experience'` guard appears BEFORE the `Promise Statement` string in brief.md (guardIdx < promiseIdx).
   - Test that `VERIFY WITH LOCAL AUTHORITY` appears in brief.md.

7. `describe('Template sync: design-brief.md includes experience type')` — test that `templates/design-brief.md` contains `experience` in the Type line.

All tests read files using `readFileSync(join(ROOT, 'workflows/brief.md'), 'utf8')` or `readFileSync(join(ROOT, 'templates/design-brief.md'), 'utf8')`.

Run `node --test tests/phase-75/brief-extensions.test.mjs` to confirm tests FAIL (Wave 0 contract — tests should fail against current codebase state).
  </action>
  <verify>
    <automated>node --test tests/phase-75/brief-extensions.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-75/brief-extensions.test.mjs exists and is a valid .mjs file
    - File contains `import { test, describe } from 'node:test'`
    - File contains `describe('BREF-01` through `describe('BREF-05`
    - File contains `describe('Cross-type regression`
    - File contains `describe('Template sync`
    - Running `node --test tests/phase-75/brief-extensions.test.mjs` exits with non-zero code (tests fail against current brief.md — this is the Wave 0 contract)
  </acceptance_criteria>
  <done>Phase 75 Nyquist test file exists with 7 describe blocks covering all 5 BREF requirements plus cross-type regression and template sync. Tests fail against current codebase (Wave 0 red state).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add experience brief sections to workflows/brief.md and templates/design-brief.md</name>
  <files>workflows/brief.md, templates/design-brief.md</files>
  <read_first>
    - workflows/brief.md (FULL file — executor must see current state before editing)
    - templates/design-brief.md (FULL file — executor must see current state before editing)
    - references/experience-disclaimer.md (inline disclaimer pattern — lines 14-15)
    - .planning/phases/75-brief-extensions/75-RESEARCH.md (Pattern 1, Pattern 2, Pattern 3 — exact insertion points and content)
  </read_first>
  <behavior>
    - Test BREF-01: brief.md contains `Promise Statement`
    - Test BREF-02: brief.md contains `Vibe Contract` and `emotional arc`
    - Test BREF-03: brief.md contains `Audience Archetype` and `mobility needs`
    - Test BREF-04: brief.md contains `Venue Constraints` and `Curfew` and `Noise limits`
    - Test BREF-05: brief.md contains `Repeatability Intent` and `one-off`
    - Test cross-type: product_type guard index < Promise Statement index
    - Test cross-type: `VERIFY WITH LOCAL AUTHORITY` present
    - Test template sync: design-brief.md contains `experience`
  </behavior>
  <action>
Make three targeted edits. Do NOT touch any existing instruction text outside these three insertion points.

**Edit 1 — brief.md line 296: Update Type enum**

Change:
```
**Type:** {software | hardware | hybrid}
```
To:
```
**Type:** {software | hardware | hybrid | experience}
```

**Edit 2 — brief.md after line 321 (after the hybrid Design Constraints clause): Add experience Design Constraints clause**

Insert after the hybrid clause ending at line 321:

```markdown
For `experience` type, the Design Constraints table MUST cover:
- Venue capacity (legal maximum attendee count) — [VERIFY WITH LOCAL AUTHORITY]
- Noise ordinance (dB SPL limit at boundary / curfew time) — [VERIFY WITH LOCAL AUTHORITY]
- Egress requirement (minimum exit count, route widths, emergency egress) — [VERIFY WITH LOCAL AUTHORITY]
- Accessibility standard (step-free access routes, BSL provision, quiet zones, platform seating)
- Sub-type (single-night | multi-day | recurring-series | installation | hybrid-event) — detected in Step 4
```

**Edit 3 — brief.md after line 404 (after the Scope Boundaries derive instruction, BEFORE the Footer): Add five experience sections**

Insert after "Derive in-scope items from PROJECT.md's stated goals. Derive out-of-scope items..." and before the `**Footer:**` block:

```markdown
<!-- Experience product type — Phase 75: experience-specific sections -->
**If `product_type == "experience"`**, after writing `## Scope Boundaries`, write these five additional sections:

**`## Promise Statement`** — One sentence in the voice of an attendee: what they would tell a friend about this event. Synthesize from PROJECT.md event description, atmosphere goals, and any language about the intended attendee experience. If PROJECT.md does not provide enough context, write: `[PROVIDE: One sentence your attendee tells a friend — what makes this event unmissable?]`

**`## Vibe Contract`** — The emotional arc of the event with three components:
- **Emotional arc:** Opening mood → build → peak → comedown → departure feeling (derive from event goals in PROJECT.md)
- **Peak timing:** When does the event reach maximum energy? (derive from run-of-show signals in PROJECT.md, or `[PROVIDE: Peak timing — e.g., "midnight drop" or "golden hour finale"]`)
- **Aesthetic register:** The sensory vocabulary of the event: lighting aesthetic, sound character, material mood (derive from atmosphere signals in PROJECT.md)
- **Energy level:** Low / Medium / High / Transcendent — where does this sit on the physiological engagement scale?

**`## Audience Archetype`** — The crowd composition described in four dimensions:
| Dimension | Value |
|-----------|-------|
| Crowd composition | {demographic mix — age range, scene membership, first-timers vs regulars} |
| Mobility needs | {accessibility requirements — step-free, seating zones, companion spaces} |
| Group size | {typical party size — solo, pairs, groups of 4-6, large crews} |
| Energy profile | {physical engagement expectation — active dance floor, seated performance, mixed zones} |

Synthesize from PROJECT.md audience descriptions. Use `[PROVIDE: ...]` for any dimension with no PROJECT.md basis.

**`## Venue Constraints`** — The fixed physical parameters of the venue:
| Constraint | Value |
|------------|-------|
| Capacity | {maximum legal capacity — `[PROVIDE]` if unknown} [VERIFY WITH LOCAL AUTHORITY] |
| Curfew | {noise curfew time — `[PROVIDE]` if unknown} [VERIFY WITH LOCAL AUTHORITY] |
| Noise limits | {dB SPL limit or zone restrictions — `[PROVIDE]` if unknown} [VERIFY WITH LOCAL AUTHORITY] |
| Load-in windows | {when production can access the venue — `[PROVIDE]` if unknown} |
| Fixed infrastructure | {PA system, stage, lighting rigs, bar locations, structural restrictions} |

Synthesize from PROJECT.md venue signals. ALL numerical regulatory values MUST carry `[VERIFY WITH LOCAL AUTHORITY]` inline per `@references/experience-disclaimer.md`.

**`## Repeatability Intent`** — Whether this is a one-off or a series:

**Format:**
```
**Type:** one-off | series
**Cadence:** {if series: weekly / monthly / quarterly / annual / irregular} | N/A
**Edition naming:** {if series: how editions are distinguished — "Autumn 2026", "Vol. 3", etc.} | N/A
**Template mode:** {enabled if series — downstream artifacts will include {{variable}} slots} | disabled
```

Derive from sub_type signals: `recurring-series` → series type. `single-night`, `multi-day`, `installation` → one-off unless PROJECT.md states otherwise.

<!-- End experience-only sections -->
```

**Edit 4 — templates/design-brief.md line 19: Update Type enum**

Change:
```
**Type:** {software | hardware | hybrid}
```
To:
```
**Type:** {software | hardware | hybrid | experience}
```

**Edit 5 — templates/design-brief.md: Add experience section stubs at end (before the footer `---`)**

Insert before the final `---` / `*Generated by PDE-OS` line:

```markdown
<!-- Experience product type only — omit for software, hardware, hybrid -->
## Promise Statement

{One sentence an attendee tells a friend}

## Vibe Contract

- **Emotional arc:** {opening mood → build → peak → comedown → departure feeling}
- **Peak timing:** {when peak energy occurs}
- **Aesthetic register:** {lighting aesthetic, sound character, material mood}
- **Energy level:** {Low | Medium | High | Transcendent}

## Audience Archetype

| Dimension | Value |
|-----------|-------|
| Crowd composition | {demographic mix} |
| Mobility needs | {accessibility requirements} |
| Group size | {typical party size} |
| Energy profile | {physical engagement expectation} |

## Venue Constraints

| Constraint | Value |
|------------|-------|
| Capacity | {maximum legal capacity} [VERIFY WITH LOCAL AUTHORITY] |
| Curfew | {noise curfew time} [VERIFY WITH LOCAL AUTHORITY] |
| Noise limits | {dB SPL limit} [VERIFY WITH LOCAL AUTHORITY] |
| Load-in windows | {when production can access venue} |
| Fixed infrastructure | {PA, stage, lighting rigs, bar locations, structural restrictions} |

## Repeatability Intent

**Type:** one-off | series
**Cadence:** {weekly / monthly / quarterly / annual / irregular} | N/A
**Edition naming:** {how editions are distinguished} | N/A
**Template mode:** enabled | disabled
```

After all edits, run: `node --test tests/phase-75/brief-extensions.test.mjs` — all tests must now PASS (green state).
  </action>
  <verify>
    <automated>node --test tests/phase-75/brief-extensions.test.mjs && node --test tests/phase-74/experience-regression.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - workflows/brief.md contains `**Type:** {software | hardware | hybrid | experience}`
    - workflows/brief.md contains `For \`experience\` type, the Design Constraints table MUST cover:`
    - workflows/brief.md contains `product_type == "experience"` guard before `Promise Statement`
    - workflows/brief.md contains `## Promise Statement` section heading instruction
    - workflows/brief.md contains `## Vibe Contract` section heading instruction
    - workflows/brief.md contains `## Audience Archetype` section heading instruction
    - workflows/brief.md contains `## Venue Constraints` section heading instruction
    - workflows/brief.md contains `## Repeatability Intent` section heading instruction
    - workflows/brief.md contains `[VERIFY WITH LOCAL AUTHORITY]` inline in Venue Constraints
    - templates/design-brief.md contains `{software | hardware | hybrid | experience}`
    - templates/design-brief.md contains `## Promise Statement`
    - `node --test tests/phase-75/brief-extensions.test.mjs` exits 0
    - `node --test tests/phase-74/experience-regression.test.mjs` exits 0
  </acceptance_criteria>
  <done>All five experience brief sections present in workflows/brief.md inside a product_type guard, template synced, Phase 75 Nyquist tests all green, Phase 74 regression tests still green.</done>
</task>

</tasks>

<verification>
1. `node --test tests/phase-75/brief-extensions.test.mjs` — all BREF assertions pass
2. `node --test tests/phase-74/experience-regression.test.mjs` — no regressions from Phase 74
3. `grep -c 'Promise Statement' workflows/brief.md` returns >= 1
4. `grep -c 'experience' templates/design-brief.md` returns >= 1
5. `grep 'VERIFY WITH LOCAL AUTHORITY' workflows/brief.md` returns matches in Venue Constraints area
</verification>

<success_criteria>
- Five experience-specific sections (Promise Statement, Vibe Contract, Audience Archetype, Venue Constraints, Repeatability Intent) exist in workflows/brief.md inside a product_type == "experience" guard
- templates/design-brief.md reflects experience as a valid type with all five section stubs
- Phase 75 Nyquist test suite passes (7 describe blocks, all green)
- Phase 74 regression test suite still passes (no cross-type contamination)
</success_criteria>

<output>
After completion, create `.planning/phases/75-brief-extensions/75-01-SUMMARY.md`
</output>
