---
phase: 74-foundation-and-regression-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-74/experience-regression.test.mjs
  - workflows/brief.md
  - templates/design-manifest.json
  - templates/design-state-root.md
  - .planning/design/design-manifest.json
  - .planning/pressure-test/fixture-greenfield/design/design-manifest.json
  - .planning/pressure-test/fixture-partial/design/design-manifest.json
  - .planning/pressure-test/fixture-rerun/design/design-manifest.json
autonomous: true
requirements:
  - FNDX-01
  - FNDX-03
  - FNDX-04

must_haves:
  truths:
    - "brief.md contains 48 experience signal keywords in Step 4 detection"
    - "brief.md classifies experience type with 5 sub-types and writes experienceSubType to manifest"
    - "Software default remains as final ELSE in brief.md classification chain"
    - "Regression smoke matrix test file exists and covers FNDX-01 through FNDX-04"
    - "All 5 manifest JSON files contain experienceSubType field"
    - "DESIGN-STATE template includes experience in Product Type and Sub-type row"
  artifacts:
    - path: "tests/phase-74/experience-regression.test.mjs"
      provides: "Cross-type regression smoke matrix"
      min_lines: 80
    - path: "workflows/brief.md"
      provides: "Experience product type detection with sub-types"
      contains: "experienceSubType"
    - path: "templates/design-manifest.json"
      provides: "Manifest schema with experienceSubType field"
      contains: "experienceSubType"
    - path: "templates/design-state-root.md"
      provides: "DESIGN-STATE template with Sub-type row"
      contains: "Sub-type"
  key_links:
    - from: "workflows/brief.md"
      to: "design-manifest.json"
      via: "manifest-set-top-level experienceSubType"
      pattern: "manifest-set-top-level experienceSubType"
    - from: "tests/phase-74/experience-regression.test.mjs"
      to: "workflows/brief.md"
      via: "readFileSync structural assertions"
      pattern: "readFileSync.*brief\\.md"
---

<objective>
Write the regression smoke matrix test, add experience product type detection to brief.md, and update all manifest files with the experienceSubType field.

Purpose: Establishes the test-first guard for all Phase 74 changes and locks the experience detection architecture in brief.md before any downstream workflow modifications. The regression test must exist before workflow edits so its failing state validates the pre-state.

Output: experience-regression.test.mjs (smoke matrix), modified brief.md (experience detection + sub-types), 5 updated manifest JSONs, updated DESIGN-STATE template
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-foundation-and-regression-infrastructure/74-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs from existing codebase -->

From tests/phase-64/manifest-schema.test.mjs:
```javascript
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'fs';
import { resolve, join } from 'path';
import { fileURLToPath } from 'url';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');
```

5 manifest JSON file paths:
```javascript
const JSON_FILES = [
  'templates/design-manifest.json',
  '.planning/design/design-manifest.json',
  '.planning/pressure-test/fixture-greenfield/design/design-manifest.json',
  '.planning/pressure-test/fixture-partial/design/design-manifest.json',
  '.planning/pressure-test/fixture-rerun/design/design-manifest.json',
];
```

From workflows/brief.md Step 4 (lines 177-188) — current classification:
```
IF both software AND hardware signals are present:
  product_type = "hybrid"
ELSE IF only hardware signals present (no software signals):
  product_type = "hardware"
ELSE (only software signals, OR ambiguous/no signals):
  product_type = "software"  (default)
```

From workflows/brief.md Step 7 (lines 507-511) — manifest write:
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level productType {product_type}
```

From templates/design-state-root.md (line 17):
```
| Product Type | {software/hardware/hybrid} |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write regression smoke matrix test file</name>
  <files>tests/phase-74/experience-regression.test.mjs</files>
  <read_first>
    - tests/phase-64/manifest-schema.test.mjs (test pattern: imports, ROOT resolution, describe/test structure, readFileSync assertions)
    - .planning/phases/74-foundation-and-regression-infrastructure/74-RESEARCH.md (Pattern 3: exact test code to implement)
  </read_first>
  <action>
Create `tests/phase-74/` directory and write `experience-regression.test.mjs`.

The test file must use `node:test` with `describe()` + `test()` structure and `node:assert/strict`. Follow the exact ROOT resolution pattern from `tests/phase-64/manifest-schema.test.mjs`:

```javascript
const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');
```

Define the 14 pipeline workflow file list as a constant:
```javascript
const PIPELINE_WORKFLOW_FILES = [
  'workflows/brief.md',
  'workflows/flows.md',
  'workflows/system.md',
  'workflows/wireframe.md',
  'workflows/critique.md',
  'workflows/iterate.md',
  'workflows/handoff.md',
  'workflows/ideate.md',
  'workflows/competitive.md',
  'workflows/opportunity.md',
  'workflows/hig.md',
  'workflows/recommend.md',
  'workflows/mockup.md',
  'workflows/build.md',
];
```

Implement 4 describe blocks covering all 4 requirement IDs:

**FNDX-01** (3 tests):
- `brief.md` contains experience signal keywords (assert `content.includes('experience')` AND `content.includes('festival') || content.includes('venue')`)
- `brief.md` contains all 5 sub-type detection strings: `single-night`, `multi-day`, `recurring-series`, `installation`, `hybrid-event`
- `brief.md` contains `experienceSubType` manifest write reference

**FNDX-02** (1 test):
- All 14 pipeline workflow files contain the string `experience` (loop through PIPELINE_WORKFLOW_FILES, readFileSync each, assert includes 'experience')

**FNDX-03** (1 test):
- `brief.md` still contains `software` as a resolved type value (cross-type regression guard)
- Experience branch appears BEFORE the final software default (indexOf comparison: experience index < last software default index)

**FNDX-04** (1 test):
- No workflow file other than `brief.md` contains sub-type structural branching: regex `/IF.*single-night|ELSE IF.*single-night/i` must NOT match in any of the 13 non-brief files

Total: ~6-8 tests across 4 describe blocks, approximately 100-120 lines.

The test file includes a header comment: `// Phase 74 — Foundation and Regression Infrastructure`

Note: After creating this test file, running it will FAIL because brief.md and downstream workflows do not yet contain experience content. This is the expected Wave 0 state — the test documents the target state before implementation.
  </action>
  <verify>
    <automated>node --test tests/phase-74/experience-regression.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-74/experience-regression.test.mjs exists and is >80 lines
    - File contains `import { test, describe } from 'node:test'`
    - File contains `import assert from 'node:assert/strict'`
    - File contains `PIPELINE_WORKFLOW_FILES` array with exactly 14 entries
    - File contains describe blocks for strings `FNDX-01`, `FNDX-02`, `FNDX-03`, `FNDX-04`
    - File contains assertions for all 5 sub-type strings: `single-night`, `multi-day`, `recurring-series`, `installation`, `hybrid-event`
    - File contains `experienceSubType` string
    - `node --test tests/phase-74/experience-regression.test.mjs` exits (pass or fail — expected to fail at this stage)
  </acceptance_criteria>
  <done>Regression smoke matrix test file exists with structural assertions for all 4 FNDX requirements. Test runs (and fails as expected on clean repo).</done>
</task>

<task type="auto">
  <name>Task 2: Add experience detection to brief.md and update DESIGN-STATE template</name>
  <files>workflows/brief.md, templates/design-state-root.md</files>
  <read_first>
    - workflows/brief.md (full file — must see Step 4 detection logic at lines 170-213 and Step 7 manifest write at lines 500-514 and anti-patterns at lines 538-547)
    - templates/design-state-root.md (Quick Reference table structure)
    - .planning/phases/74-foundation-and-regression-infrastructure/74-RESEARCH.md (Pattern 1: exact detection logic; Example 2: manifest write; Example 3: DESIGN-STATE extension)
  </read_first>
  <action>
**brief.md Step 4 modifications (around line 171):**

1. Add experience signals block BEFORE the existing software/hardware signals. Insert after line 175 (after hardware signals block), before the classification logic:

```
**Experience signals:** event, festival, installation, live performance, concert, venue, capacity, attendees, run-of-show, front-of-house, FOH, backstage, production, PA system, stage, set design, programme, lineup, artist, performer, act, sponsor, volunteer, site map, floor plan, acoustic zone, wayfinding, hydration station, first aid, crowd management, egress, ticket, gateline, wristband, merchandise, bar, catering, dB, watts, lux, crowd flow, ingress, site plan, temporary structure, marquee, tent
```

2. Replace the classification logic (lines 177-186) with the 4-type chain. The experience check MUST precede the hybrid check:

```
IF experience signals present AND NOT dominated by software signals:
  product_type = "experience"
  Detect sub_type from secondary signals:
    single-night: "one night", "one-day", "24h", "evening event", single date
    multi-day: "weekend", "multi-day", "festival" (3+ days implied), numbered days
    recurring-series: "monthly", "quarterly", "series", "season", "weekly"
    installation: "installation", "exhibition", "gallery", "open-ended", "visitor-led"
    hybrid-event: both experience AND software signals present with digital product layer
  Default sub_type: "single-night" if no sub-type signals detected
ELSE IF both software AND hardware signals are present:
  product_type = "hybrid"
ELSE IF only hardware signals present (no software signals):
  product_type = "hardware"
ELSE (only software signals, OR ambiguous/no signals):
  product_type = "software"  (default)
```

3. Update the resolve line (line 188) from:
`Resolve to ONE canonical lowercase string: \`software\`, \`hardware\`, or \`hybrid\`.`
to:
`Resolve to ONE canonical lowercase string: \`software\`, \`hardware\`, \`hybrid\`, or \`experience\`.`

4. Update the Sequential Thinking prompt (line 191) to include `'experience'` in the classification options.

5. Update the display block (line 209) — no changes needed (it uses `{product_type}` variable).

**brief.md Step 7 modifications (around line 507-511):**

6. Update the product_type comment (line 507) from:
`- \`{product_type}\` — Use the product type string already resolved in Step 4 (one of: \`software\`, \`hardware\`, \`hybrid\`).`
to:
`- \`{product_type}\` — Use the product type string already resolved in Step 4 (one of: \`software\`, \`hardware\`, \`hybrid\`, \`experience\`).`

7. After the existing `manifest-set-top-level productType` line (line 511), add:

```bash
# If experience type: write sub-type to manifest
# If non-experience type: write null to clear any prior value
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level experienceSubType {sub_type_or_null}
```

Where `{sub_type_or_null}` is the detected sub-type for experience products, or `null` for non-experience products.

8. In the Quick Reference update section (around line 470-474), update the Product Type row comment from `{canonical lowercase type: software/hardware/hybrid}` to `{canonical lowercase type: software/hardware/hybrid/experience}`.

9. After the Product Type row, add a conditional Sub-type row:
```
   | Sub-type | {sub_type} |    ← only for experience product type
```

**brief.md anti-patterns section (around line 544):**

10. Update the canonical type list from `(\`software\`, \`hardware\`, or \`hybrid\`)` to `(\`software\`, \`hardware\`, \`hybrid\`, or \`experience\`)` in the anti-pattern text.

**templates/design-state-root.md modifications:**

11. Update line 17 from:
`| Product Type | {software/hardware/hybrid} |`
to:
`| Product Type | {software/hardware/hybrid/experience} |`

12. Add a new row after Product Type:
`| Sub-type | {null for non-experience / single-night, multi-day, recurring-series, installation, hybrid-event} |`
  </action>
  <verify>
    <automated>grep -c "experience" workflows/brief.md && grep "experienceSubType" workflows/brief.md | head -3 && grep "Sub-type" templates/design-state-root.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/brief.md contains the string "experience signals" (experience signal keyword block present)
    - workflows/brief.md contains all 5 sub-type strings: `single-night`, `multi-day`, `recurring-series`, `installation`, `hybrid-event`
    - workflows/brief.md contains `experienceSubType` (manifest write instruction)
    - workflows/brief.md classification logic has experience check BEFORE hybrid check (grep shows experience line number < hybrid line number)
    - workflows/brief.md still ends classification with `product_type = "software"` as the final ELSE default
    - workflows/brief.md contains `manifest-set-top-level experienceSubType`
    - templates/design-state-root.md contains `| Product Type | {software/hardware/hybrid/experience} |`
    - templates/design-state-root.md contains `| Sub-type |`
  </acceptance_criteria>
  <done>brief.md detects experience product type with 5 sub-types and writes experienceSubType to manifest. DESIGN-STATE template includes Sub-type row. Software default preserved as final ELSE.</done>
</task>

<task type="auto">
  <name>Task 3: Update all 5 manifest JSON files with experienceSubType field</name>
  <files>
    templates/design-manifest.json,
    .planning/design/design-manifest.json,
    .planning/pressure-test/fixture-greenfield/design/design-manifest.json,
    .planning/pressure-test/fixture-partial/design/design-manifest.json,
    .planning/pressure-test/fixture-rerun/design/design-manifest.json
  </files>
  <read_first>
    - templates/design-manifest.json (current schema — productType at line 7)
    - .planning/design/design-manifest.json (live manifest — productType field)
    - .planning/pressure-test/fixture-greenfield/design/design-manifest.json (productType value)
    - .planning/pressure-test/fixture-partial/design/design-manifest.json (productType value)
    - .planning/pressure-test/fixture-rerun/design/design-manifest.json (productType value)
    - tests/phase-64/manifest-schema.test.mjs (existing manifest validation — must still pass after changes)
  </read_first>
  <action>
Update all 5 manifest JSON files:

**templates/design-manifest.json:**
- Change line 7 `"productType": "software | hardware | hybrid"` to `"productType": "software | hardware | hybrid | experience"`
- Add new field immediately after productType: `"experienceSubType": "single-night | multi-day | recurring-series | installation | hybrid-event | null"`

**.planning/design/design-manifest.json:**
- Change `"productType": "software | hardware | hybrid"` to `"productType": "software | hardware | hybrid | experience"`
- Add `"experienceSubType": null` immediately after the productType field

**3 pressure-test fixture manifests** (fixture-greenfield, fixture-partial, fixture-rerun):
- Each currently has `"productType": "SaaS Platform"` — leave this value unchanged (it's a fixture-specific value, not the schema comment)
- Add `"experienceSubType": null` immediately after the productType field in each

All 5 files must have `experienceSubType` present. The template uses the schema comment string; the 4 live/fixture files use `null`.

After edits, verify existing phase-64 tests still pass:
```bash
node --test tests/phase-64/manifest-schema.test.mjs
```
This must still pass — the test checks `designCoverage` fields (14 canonical fields), not `productType`/`experienceSubType`. If it fails, the manifest JSON was malformed during editing.
  </action>
  <verify>
    <automated>grep -l "experienceSubType" templates/design-manifest.json .planning/design/design-manifest.json .planning/pressure-test/fixture-greenfield/design/design-manifest.json .planning/pressure-test/fixture-partial/design/design-manifest.json .planning/pressure-test/fixture-rerun/design/design-manifest.json | wc -l && node --test tests/phase-64/manifest-schema.test.mjs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 5 manifest JSON files contain the string `experienceSubType` (grep returns 5 matches)
    - templates/design-manifest.json contains `"productType": "software | hardware | hybrid | experience"`
    - templates/design-manifest.json contains `"experienceSubType": "single-night | multi-day | recurring-series | installation | hybrid-event | null"`
    - .planning/design/design-manifest.json contains `"experienceSubType": null`
    - All 3 pressure-test fixture manifests contain `"experienceSubType": null`
    - `node --test tests/phase-64/manifest-schema.test.mjs` exits 0 (all existing tests pass)
    - All 5 files are valid JSON (no syntax errors from parse)
  </acceptance_criteria>
  <done>All 5 manifest JSON files have experienceSubType field. Template has schema comment string, live/fixture manifests have null. Existing phase-64 manifest tests pass.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. `node --test tests/phase-74/experience-regression.test.mjs` — some tests pass (FNDX-01 assertions on brief.md should pass), FNDX-02 will still fail (downstream workflows not yet modified — that's Plan 02)
2. `node --test tests/phase-64/manifest-schema.test.mjs` — all tests pass (no regression)
3. `grep -c "experienceSubType" workflows/brief.md` returns >= 1
4. `grep -c "experience" templates/design-manifest.json` returns >= 1
</verification>

<success_criteria>
- Regression smoke matrix test exists and runs
- brief.md detects experience product type with 5 sub-types
- brief.md writes experienceSubType to manifest via manifest-set-top-level
- Software default is preserved as final ELSE in classification chain
- All 5 manifest JSON files contain experienceSubType field
- DESIGN-STATE template includes experience in Product Type and Sub-type row
- Existing phase-64 manifest tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/74-foundation-and-regression-infrastructure/74-01-SUMMARY.md`
</output>
