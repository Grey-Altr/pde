---
phase: 81-handoff-production-bible
plan: 02
type: execute
wave: 2
depends_on:
  - 81-01
files_modified:
  - workflows/handoff.md
  - tests/phase-81/handoff-production-bible.test.mjs
autonomous: true
requirements:
  - HDOF-01
  - HDOF-02
  - HDOF-03
  - HDOF-04
  - HDOF-05
  - HDOF-06

must_haves:
  truths:
    - "Running /pde:handoff for a software product produces only TypeScript interfaces and component specs — no production bible sections appear"
    - "For hybrid-event sub-type, the handoff produces both software API handoff sections and production bible sections in a single output"
    - "Both BIB and HND artifact sets are registered in the manifest for hybrid-event"
    - "The Step 7d summary table shows correct output for experience, hybrid-event, and software product types"
    - "Software, hardware, and hybrid product handoff output is unchanged after experience branch hardening — no regression in existing functionality"
  artifacts:
    - path: "workflows/handoff.md"
      provides: "Refined hybrid-event dual-surface logic and software guard enforcement"
      contains: "hybrid-event"
    - path: "tests/phase-81/handoff-production-bible.test.mjs"
      provides: "Additional tests for software isolation and hybrid-event dual output"
      contains: "software-guard"
  key_links:
    - from: "workflows/handoff.md Step 4i"
      to: "workflows/handoff.md Step 5"
      via: "HND_GENERATES_SOFTWARE flag controls dual output"
      pattern: "HND_GENERATES_SOFTWARE"
    - from: "workflows/handoff.md Step 7b-bib"
      to: "workflows/handoff.md Step 7b"
      via: "Both BIB and HND registered when hybrid-event"
      pattern: "manifest-update BIB"
---

<objective>
Harden the experience branch with explicit software product guard, verify hybrid-event dual-surface output produces both BIB and HND artifacts, and add Nyquist tests confirming product type isolation and cross-regression safety.

Purpose: Success criteria 4 (software produces no BIB) and 5 (hybrid-event produces both BIB and HND) require explicit verification and workflow hardening beyond the base experience branch. This plan adds guard rails and integration tests.

Output: Hardened handoff.md with explicit guards, expanded Nyquist test suite
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-handoff-production-bible/81-RESEARCH.md
@.planning/phases/81-handoff-production-bible/81-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — the experience branch structure added to handoff.md -->

Step 4i experience branch (added in Plan 01):
```markdown
- "experience":
  4. Set BIB_GENERATES_SECTIONS = true.
     Set HND_GENERATES_SOFTWARE = false (unless experienceSubType is "hybrid-event").
     IF experienceSubType is "hybrid-event": ALSO set HND_GENERATES_SOFTWARE = true.
  7. Proceed to Step 5 BIB generation (four-pass split).
     IF HND_GENERATES_SOFTWARE is true (hybrid-event only): ALSO execute Steps 4b through 4h for the software layer
```

Step 7b-bib (added in Plan 01):
```markdown
#### 7b-bib. Register BIB artifact in manifest (experience products only)
IF BIB_GENERATES_SECTIONS is true:
  [manifest-update commands for BIB]
```

Step 7c (updated in Plan 01 to 16 flags):
```markdown
Extract ALL sixteen current flag values: hasDesignSystem, ..., hasPrintCollateral, hasProductionBible.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add software guard and hybrid-event integration tests</name>
  <files>tests/phase-81/handoff-production-bible.test.mjs</files>
  <read_first>
    - tests/phase-81/handoff-production-bible.test.mjs (current test suite from Plan 01 — append new tests without breaking existing)
    - workflows/handoff.md (verify experience branch is in place from Plan 01)
  </read_first>
  <behavior>
    - Software guard: handoff.md software branch does NOT contain "Production Bible" or "BIB" artifact generation instructions
    - Software guard: handoff.md "software" branch at Step 4i still says "include software component API sections"
    - Hybrid-event dual: handoff.md contains explicit instruction that hybrid-event writes BOTH BIB-*.md AND HND-handoff-spec-*.md
    - Hybrid-event manifest: handoff.md Step 7b-bib registers BIB, AND Step 7b registers HND, for hybrid-event
    - Hybrid-event STACK.md: handoff.md Step 2a bypass does NOT skip STACK.md for hybrid-event (only for pure experience)
    - Experience-only isolation: handoff.md pure experience branch sets HND_GENERATES_SOFTWARE = false
    - No "Phase 74 stub" remaining anywhere in handoff.md
  </behavior>
  <action>
Append new describe blocks to the existing `tests/phase-81/handoff-production-bible.test.mjs` file:

1. `describe('SC-4: Software product isolation — no BIB generation')` — Read handoff.md. Extract the Step 4i "software" branch text (the line starting with `- "software":`). Assert it includes "software component API" and does NOT include "Production Bible" or "BIB_GENERATES_SECTIONS". Also assert that the software branch does not contain "Advance Document" or "Run Sheet" (these are BIB sections).

2. `describe('SC-5: Hybrid-event dual-surface output')` — Assert handoff.md contains ALL of these strings:
   - `hybrid-event` (sub-type name)
   - `HND_GENERATES_SOFTWARE = true` (or `HND_GENERATES_SOFTWARE is true`)
   - `BIB_GENERATES_SECTIONS = true` (set for all experience products)
   - String indicating both files are written: look for "HND-handoff-spec" AND "BIB-" appearing in the same Step 5 section
   - String indicating both are registered in manifest: look for "manifest-update BIB" AND "manifest-update HND" both present

3. `describe('Hybrid-event STACK.md check NOT skipped')` — Assert handoff.md contains text indicating hybrid-event does NOT skip STACK.md (e.g., `hybrid-event` appears near "Proceed with STACK.md check" or the bypass condition explicitly excludes hybrid-event)

4. `describe('No Phase 74 stubs remaining')` — Assert handoff.md does NOT contain the string "Phase 74 stub" anywhere in the file. Use `assert.ok(!content.includes('Phase 74 stub'))`.

5. `describe('Step 7d summary handles experience products')` — Assert handoff.md contains "Production Bible" in the Step 7d summary table area (near "Summary" and "Property" and "Value")

Run all tests — the new tests should PASS since Plan 01 already implemented the experience branch. If any fail, they indicate gaps in Plan 01's implementation that need fixing in Task 2.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-81/handoff-production-bible.test.mjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-81/handoff-production-bible.test.mjs contains `describe('SC-4: Software product isolation`
    - tests/phase-81/handoff-production-bible.test.mjs contains `describe('SC-5: Hybrid-event dual-surface`
    - tests/phase-81/handoff-production-bible.test.mjs contains `describe('No Phase 74 stubs remaining`
    - tests/phase-81/handoff-production-bible.test.mjs contains `!content.includes('Phase 74 stub')`
    - Running `node --test tests/phase-81/handoff-production-bible.test.mjs` has 0 failures (all tests pass including new ones)
  </acceptance_criteria>
  <done>Extended test suite with software guard, hybrid-event dual-surface, STACK.md bypass exclusion, stub removal, and summary table tests. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Harden handoff.md guards and fix any test failures</name>
  <files>workflows/handoff.md</files>
  <read_first>
    - workflows/handoff.md (full file — verify Plan 01 changes are in place, identify any gaps found by Task 1 tests)
    - tests/phase-81/handoff-production-bible.test.mjs (read test assertions to understand exact string expectations)
    - .planning/phases/81-handoff-production-bible/81-01-SUMMARY.md (understand what Plan 01 delivered)
  </read_first>
  <action>
Review the test results from Task 1. If all tests pass, this task focuses on hardening. If any tests fail, fix the gaps first.

**Hardening edits to workflows/handoff.md:**

1. **Explicit software guard comment at Step 4i:** Ensure the `"software"` branch at Step 4i has an explicit comment:
   ```markdown
   - "software": include software component API sections, omit Hardware Handoff sections. NEVER generate Production Bible (BIB) sections for software products.
   ```

2. **Explicit hardware guard:** Ensure the `"hardware"` branch also has:
   ```markdown
   - "hardware": omit software component API sections, include Hardware Handoff sections. NEVER generate Production Bible (BIB) sections for hardware products.
   ```

3. **Explicit hybrid guard:** Ensure the `"hybrid"` branch (non-hybrid-event) has:
   ```markdown
   - "hybrid": include both software and hardware sections. NEVER generate Production Bible (BIB) sections for hybrid products (only hybrid-event sub-type produces BIB — see "experience" branch).
   ```

4. **Step 5 guard — BIB generation conditional:** At the top of the BIB generation block in Step 5 section 11, ensure there is an explicit guard:
   ```markdown
   IF PRODUCT_TYPE is NOT "experience": SKIP this entire BIB generation block. This block ONLY executes for experience products.
   ```

5. **Step 7b-bib guard:** Ensure the BIB manifest registration has an explicit guard:
   ```markdown
   IF BIB_GENERATES_SECTIONS is NOT true: SKIP Step 7b-bib entirely. BIB artifact is only registered for experience products.
   ```

6. **Anti-Patterns — add hybrid-event clarity:**
   ```markdown
   - **Skip software handoff for hybrid-event:** When experienceSubType is "hybrid-event", BOTH the production bible AND the software handoff spec must be generated. The hybrid-event is the ONLY experience sub-type that produces HND-handoff-spec and HND-types files alongside the BIB document.
   ```

7. **Verify the coverage flag write at Step 7c includes conditional logic:**
   - For pure experience: `hasHandoff: {current}` (do NOT set to true — no software handoff was generated), `hasProductionBible: true`
   - For hybrid-event: `hasHandoff: true` (software handoff WAS generated), `hasProductionBible: true`
   - For software/hardware/hybrid: `hasHandoff: true`, `hasProductionBible: {current}`

After all edits, run the full test suite to confirm all tests still pass.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-81/handoff-production-bible.test.mjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - workflows/handoff.md contains "NEVER generate Production Bible (BIB) sections for software products" (or equivalent explicit guard)
    - workflows/handoff.md contains "NEVER generate Production Bible (BIB) sections for hardware products" (or equivalent)
    - workflows/handoff.md "hybrid" branch contains "only hybrid-event sub-type produces BIB" (or equivalent)
    - workflows/handoff.md Step 5 BIB block contains explicit "ONLY executes for experience products" guard
    - workflows/handoff.md Step 7b-bib contains "SKIP" guard for non-experience products
    - workflows/handoff.md does NOT contain "Phase 74 stub" anywhere
    - Running `node --test tests/phase-81/handoff-production-bible.test.mjs` produces 0 failures
    - Running `node --test tests/phase-80/print-collateral.test.mjs` still passes (no Phase 80 regression)
  </acceptance_criteria>
  <done>handoff.md has explicit product type guards on all branches, BIB generation is gated to experience-only, hybrid-event dual-surface is hardened, all Phase 81 and Phase 80 tests pass.</done>
</task>

</tasks>

<verification>
1. `node --test tests/phase-81/handoff-production-bible.test.mjs 2>&1` — all tests pass (Plan 01 + Plan 02 tests)
2. `node --test tests/phase-80/print-collateral.test.mjs 2>&1` — Phase 80 tests still pass (no regression)
3. `grep -c "Phase 74 stub" workflows/handoff.md` — returns 0
4. `grep "NEVER generate Production Bible" workflows/handoff.md` — returns matches for software, hardware, hybrid branches
5. `grep "BIB_GENERATES_SECTIONS" workflows/handoff.md` — returns matches in Step 4i and Step 5 and Step 7b-bib
6. `grep "HND_GENERATES_SOFTWARE" workflows/handoff.md` — returns matches showing hybrid-event = true, others = false
</verification>

<success_criteria>
- Software products are explicitly guarded from receiving BIB sections (SC-4)
- Hybrid-event produces both BIB and HND outputs with both registered in manifest (SC-5)
- All Phase 74 stubs are removed (zero occurrences)
- All Nyquist tests pass (Plan 01 + Plan 02)
- No Phase 80 regressions
</success_criteria>

<output>
After completion, create `.planning/phases/81-handoff-production-bible/81-02-SUMMARY.md`
</output>
