---
phase: 81-handoff-production-bible
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-81/handoff-production-bible.test.mjs
  - workflows/handoff.md
  - templates/design-manifest.json
autonomous: true
requirements:
  - HDOF-01
  - HDOF-02
  - HDOF-03
  - HDOF-04
  - HDOF-05
  - HDOF-06

must_haves:
  truths:
    - "handoff.md experience branch generates an advance document section with load-in, sound check, doors, curfew, load-out, contact sheet, and rider fulfillment"
    - "handoff.md experience branch generates a run sheet as a markdown table with columns time, duration, activity, responsible, technical cue, contingency"
    - "handoff.md experience branch generates a staffing plan with roles, headcount, shifts, briefing time, door policy, bar menu framework"
    - "handoff.md experience branch generates a budget framework with line-item costs and revenue at 60%/80%/100% capacity scenarios"
    - "handoff.md experience branch generates a post-event template with feedback, financial reconciliation, and retrospective sections"
    - "handoff.md experience branch generates a print spec output table referencing FLY, SIT, and PRG artifacts with bleed, safe area, DPI, color mode, and trim size"
    - "Every regulatory value in the BIB generation instructions carries [VERIFY WITH LOCAL AUTHORITY] inline"
    - "Pure experience products skip STACK.md check at Step 2a without halting"
    - "BIB generation uses four-pass split to avoid token truncation"
  artifacts:
    - path: "tests/phase-81/handoff-production-bible.test.mjs"
      provides: "Nyquist structural tests for HDOF-01 through HDOF-06"
      min_lines: 80
    - path: "workflows/handoff.md"
      provides: "Experience branch with six BIB sections replacing Phase 74 stubs"
      contains: "Production Bible"
    - path: "templates/design-manifest.json"
      provides: "hasProductionBible and hasPrintCollateral flags in designCoverage"
      contains: "hasProductionBible"
  key_links:
    - from: "workflows/handoff.md"
      to: "references/experience-disclaimer.md"
      via: "required_reading already wired in Phase 74"
      pattern: "experience-disclaimer"
    - from: "workflows/handoff.md"
      to: "templates/design-manifest.json"
      via: "manifest-set-top-level designCoverage"
      pattern: "hasProductionBible"
    - from: "tests/phase-81/handoff-production-bible.test.mjs"
      to: "workflows/handoff.md"
      via: "readFileSync structural assertions"
      pattern: "readFileSync.*handoff\\.md"
---

<objective>
Write the Nyquist test suite, then implement the full experience branch in handoff.md — replacing both Phase 74 stubs with six-section production bible generation instructions, adding the Step 2a STACK.md bypass for pure experience products, and updating the manifest template with new coverage flags.

Purpose: This is the core implementation for Phase 81. All six HDOF requirements are addressed by inserting the experience branch into the existing handoff workflow. The BIB artifact is the completion artifact for the experience pipeline.

Output: Nyquist test file, modified handoff.md with experience branch, updated design-manifest.json template
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-handoff-production-bible/81-RESEARCH.md
@.planning/phases/81-handoff-production-bible/81-VALIDATION.md

<interfaces>
<!-- Key locations in handoff.md that this plan modifies -->

From workflows/handoff.md line 68-82 (Step 2a — STACK.md hard dependency to modify):
```markdown
#### 2a. Read STACK.md (hard dependency)
Use the Read tool to read `.planning/research/STACK.md`.
If the file does not exist (Read returns an error or empty result): HALT with this exact error message:
```

From workflows/handoff.md line 511-520 (Step 4i — Phase 74 stub to replace):
```markdown
#### 4i. Apply productType conditional for hardware sections
Based on PRODUCT_TYPE:
- "software": include software component API sections, omit Hardware Handoff sections
- "hardware": omit software component API sections, include Hardware Handoff sections
- "hybrid": include both software and hardware sections
- "experience":  Phase 74 stub — experience-specific content added in subsequent phases.
                 Current behavior: proceed with software path as temporary fallback.
                 NEVER produce floor plans, production bibles, or experience token files from this stub.
```

From workflows/handoff.md line 655-660 (Step 5 section 11 — second Phase 74 stub to replace):
```markdown
    If PRODUCT_TYPE is "experience": Phase 74 stub — production bible and experience handoff sections added in Phase 81.
                 Current behavior: proceed with software path as temporary fallback.
                 NEVER produce floor plans, production bibles, or experience token files from this stub.
```

From workflows/handoff.md line 911-924 (Step 7c — coverage flag write pattern):
```markdown
#### 7c. Set coverage flag (CRITICAL — read-before-set to prevent clobber)
```

From templates/design-manifest.json (current designCoverage — 14 flags, missing hasPrintCollateral and hasProductionBible):
```json
"designCoverage": {
    "hasDesignSystem": false,
    "hasWireframes": false,
    "hasFlows": false,
    "hasHardwareSpec": false,
    "hasCritique": false,
    "hasIterate": false,
    "hasHandoff": false,
    "hasIdeation": false,
    "hasCompetitive": false,
    "hasOpportunity": false,
    "hasMockup": false,
    "hasHigAudit": false,
    "hasRecommendations": false,
    "hasStitchWireframes": false
}
```

From tests/phase-80/print-collateral.test.mjs (test pattern to follow):
```javascript
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'fs';
import { resolve, join } from 'path';
import { fileURLToPath } from 'url';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Nyquist test suite for HDOF-01 through HDOF-06</name>
  <files>tests/phase-81/handoff-production-bible.test.mjs</files>
  <read_first>
    - tests/phase-80/print-collateral.test.mjs (test pattern — import style, ROOT resolution, describe/test structure, assertion style)
    - workflows/handoff.md (current state — tests must fail against current stubs, pass after Task 2)
    - templates/design-manifest.json (current designCoverage object — test must assert hasProductionBible presence)
  </read_first>
  <behavior>
    - HDOF-01: handoff.md contains "Advance Document" string in experience branch, plus "load-in", "sound check", "doors", "curfew", "load-out", "contact sheet", "rider" keywords
    - HDOF-02: handoff.md contains run sheet table instruction with all six column headers: "Time", "Duration", "Activity", "Responsible", "Technical Cue", "Contingency"
    - HDOF-03: handoff.md contains staffing plan section with "roles", "headcount", "shifts", "briefing", "door policy", "bar menu" keywords
    - HDOF-04: handoff.md contains budget framework section with "60%", "80%", "100%" capacity scenario references and "break-even" keyword
    - HDOF-05: handoff.md contains post-event template section with "feedback", "financial reconciliation", "retrospective" keywords
    - HDOF-06: handoff.md contains print spec output section referencing "FLY", "SIT", "PRG", "bleed", "DPI", "CMYK" keywords
    - SC-3 disclaimer: handoff.md experience branch contains "[VERIFY WITH LOCAL AUTHORITY]" tag
    - SC-4 isolation: handoff.md experience branch does NOT produce TypeScript interfaces for pure experience (contains "HND_GENERATES_SOFTWARE = false" or equivalent)
    - SC-5 hybrid: handoff.md hybrid-event branch sets HND_GENERATES_SOFTWARE = true
    - Manifest template: templates/design-manifest.json contains "hasProductionBible" in designCoverage
    - Manifest template: templates/design-manifest.json contains "hasPrintCollateral" in designCoverage
    - Step 2a bypass: handoff.md contains STACK.md skip condition for non-hybrid experience products
    - Four-pass: handoff.md contains "Pass A", "Pass B", "Pass C", "Pass D" (or equivalent four-pass references)
  </behavior>
  <action>
Create `tests/phase-81/handoff-production-bible.test.mjs` following the exact pattern from `tests/phase-80/print-collateral.test.mjs`:

```javascript
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'fs';
import { resolve, join } from 'path';
import { fileURLToPath } from 'url';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');
```

Create these describe blocks with specific test assertions:

1. `describe('HDOF-01: Advance document in handoff.md')` — read `workflows/handoff.md`, assert content includes "Advance Document", "load-in", "sound check", "doors", "curfew", "load-out", "Contact Sheet", "Rider"

2. `describe('HDOF-02: Run sheet table format')` — assert content includes "Run Sheet" and all six column headers: "Time", "Duration", "Activity", "Responsible", "Technical Cue", "Contingency" (each as a separate test)

3. `describe('HDOF-03: Staffing plan')` — assert content includes "Staffing Plan", "Role", "Headcount", "Shift", "Briefing", "Door Policy", "Bar Menu"

4. `describe('HDOF-04: Budget framework')` — assert content includes "Budget Framework", "60%", "80%", "100%", "break-even" (case-insensitive check via toLowerCase())

5. `describe('HDOF-05: Post-event template')` — assert content includes "Post-Event Template", "feedback", "reconciliation", "retrospective"

6. `describe('HDOF-06: Print spec output')` — assert content includes "Print Spec", "FLY", "SIT", "PRG", "bleed", "DPI", "CMYK"

7. `describe('SC-3: Disclaimer enforcement')` — assert handoff.md experience branch contains `[VERIFY WITH LOCAL AUTHORITY]` at least twice (once in advance doc, once in staffing)

8. `describe('SC-4/5: Product type isolation')` — assert content includes string indicating software handoff is skipped for pure experience (`HND_GENERATES_SOFTWARE`) and that hybrid-event enables software generation

9. `describe('Step 2a bypass: STACK.md skip for pure experience')` — assert content includes a conditional that skips STACK.md for non-hybrid-event experience products

10. `describe('Four-pass split generation')` — assert content includes references to four generation passes (Pass A, Pass B, Pass C, Pass D)

11. `describe('Manifest template: hasProductionBible flag')` — read `templates/design-manifest.json`, assert it includes `"hasProductionBible"` and `"hasPrintCollateral"`

Run these tests after creation — they MUST all FAIL (red phase) since the stubs are still in place.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-81/handoff-production-bible.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-81/handoff-production-bible.test.mjs exists and has more than 80 lines
    - File contains `import { test, describe } from 'node:test'`
    - File contains `describe('HDOF-01` through `describe('HDOF-06`
    - File contains `describe('SC-3` and `describe('SC-4` or `describe('SC-4/5`
    - File contains `readFileSync(join(ROOT, 'workflows/handoff.md')`
    - File contains `readFileSync(join(ROOT, 'templates/design-manifest.json')`
    - Running `node --test tests/phase-81/handoff-production-bible.test.mjs` produces FAILING tests (all or most tests fail — this is the RED phase)
  </acceptance_criteria>
  <done>Nyquist test suite exists with structural assertions for all six HDOF requirements plus isolation, disclaimer, bypass, four-pass, and manifest flag tests. All tests fail against current codebase (red phase).</done>
</task>

<task type="auto">
  <name>Task 2: Replace Phase 74 stubs with full experience branch in handoff.md</name>
  <files>workflows/handoff.md, templates/design-manifest.json</files>
  <read_first>
    - workflows/handoff.md (full file — understand current structure, locate both stubs at lines ~517 and ~658, Step 2a at line ~68, Step 7c at line ~911, Anti-Patterns at line ~953)
    - templates/design-manifest.json (current designCoverage object — must add hasPrintCollateral and hasProductionBible)
    - references/experience-disclaimer.md (disclaimer block content — already in required_reading)
    - .planning/phases/81-handoff-production-bible/81-RESEARCH.md (Pattern 1 through Pattern 6 — exact replacement content)
  </read_first>
  <action>
Make these five edits to `workflows/handoff.md`:

**Edit 1 — Step 2a STACK.md bypass (around line 68-82):**

After the line `#### 2a. Read STACK.md (hard dependency)` and before the `Use the Read tool to read` instruction, insert a new conditional block:

```markdown
**Experience product bypass:** Before reading STACK.md, check the design manifest for PRODUCT_TYPE:

```bash
MANIFEST_RAW=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-read)
if [[ "$MANIFEST_RAW" == @file:* ]]; then MANIFEST_RAW=$(cat "${MANIFEST_RAW#@file:}"); fi
```

Parse `productType` and `experienceSubType` from the manifest JSON.

IF PRODUCT_TYPE is "experience" AND experienceSubType is NOT "hybrid-event":
  SKIP STACK.md check — production bible does not require framework detection.
  Set FRAMEWORK = "none", TYPESCRIPT = false.
  Display: `Step 2/7 (2a): STACK.md check skipped — experience product (non-hybrid). Framework: none.`
  Proceed directly to Step 2b.

IF PRODUCT_TYPE is "experience" AND experienceSubType is "hybrid-event":
  Proceed with STACK.md check below (framework detection needed for digital layer).

For all other product types: proceed with STACK.md check below (existing behavior unchanged).
```

**Edit 2 — Step 4i experience branch (replace lines ~517-519):**

Replace the three-line Phase 74 stub:
```
- "experience":  Phase 74 stub — experience-specific content added in subsequent phases.
                 Current behavior: proceed with software path as temporary fallback.
                 NEVER produce floor plans, production bibles, or experience token files from this stub.
```

With the full experience branch (from Research Pattern 1):
```markdown
- "experience":
  1. STACK.md check status: IF experienceSubType is "hybrid-event": STACK.md check was REQUIRED
     (framework detection needed for digital layer). IF experienceSubType is any other value
     (single-night, multi-day, recurring-series, installation): STACK.md check was SKIPPED.
     FRAMEWORK = "none", TYPESCRIPT = false for pure experience products.

  2. Discover upstream experience artifacts from manifest:
     - FLP: Glob `.planning/design/ux/wireframes/FLP-floor-plan-v*.html` — highest version
     - TML: Glob `.planning/design/ux/wireframes/TML-timeline-v*.html` — highest version
     - FLY: Glob `.planning/design/physical/print/FLY-event-flyer-v*.html` — highest version
     - SIT: Glob `.planning/design/physical/print/SIT-series-identity-v*.html` — highest version
     - PRG: Glob `.planning/design/physical/print/PRG-festival-program-v*.html` — highest version
     Store each as: FLP_PATH, TML_PATH, FLY_PATH, SIT_PATH, PRG_PATH (null if not found).
     WARN if FLP_PATH is null ("Floor plan artifact not found — production bible will omit site plan reference").
     WARN if TML_PATH is null ("Timeline artifact not found — run sheet will use brief temporal data only").

  3. Load @references/experience-disclaimer.md into context before generating any section.

  4. Set BIB_GENERATES_SECTIONS = true.
     Set HND_GENERATES_SOFTWARE = false (unless experienceSubType is "hybrid-event").
     IF experienceSubType is "hybrid-event": ALSO set HND_GENERATES_SOFTWARE = true.

  5. Extract from BRIEF_CONTENT (if BRIEF_AVAILABLE):
     - event_name, venue_name, venue_location, venue_capacity
     - experienceSubType
     - curfew (from venue constraints)
     - load_in_window (from venue constraints)
     - artist_lineup (if present)
     Set BIB_CONTEXT from these extracted fields.

  6. Determine BIB_VERSION: Use the Glob tool to find all files matching `.planning/design/handoff/BIB-*-v*.md`. Parse the `v{N}` suffix. If no BIB files exist: set BIB_VERSION = 1. If files exist: set BIB_VERSION = max(N) + 1.

  7. Proceed to Step 5 BIB generation (four-pass split).
     IF HND_GENERATES_SOFTWARE is true (hybrid-event only): ALSO execute Steps 4b through 4h for the software layer, then proceed to Step 5 which generates BOTH BIB and HND outputs.
```

**Edit 3 — Step 5 section 11 experience branch (replace lines ~658-660):**

Replace the three-line Phase 74 stub:
```
    If PRODUCT_TYPE is "experience": Phase 74 stub — production bible and experience handoff sections added in Phase 81.
                 Current behavior: proceed with software path as temporary fallback.
                 NEVER produce floor plans, production bibles, or experience token files from this stub.
```

With the full BIB generation instructions. This is the largest insertion — the four-pass split generation with all six sections of the production bible. Insert:

```markdown
    If PRODUCT_TYPE is "experience" (or "hybrid-event" sub-type producing BIB):

    **BIB Generation — Four-Pass Split**

    > CRITICAL: Generate the production bible across four separate generation passes to avoid token budget truncation. This is a performance requirement — single-pass generation truncates at the staffing plan section for venues above 500 capacity.

    **Pass A — Section 1: Advance Document**

    Generate the advance document section with this structure:

    ```markdown
    ## 1. Advance Document

    ### 1.1 Day-of Schedule
    | Time | Activity | Owner | Notes |
    |------|----------|-------|-------|
    | {load_in_window from BIB_CONTEXT} | Load-in begins | Production Manager | [VERIFY WITH LOCAL AUTHORITY] |
    | {sound_check time — derive from load-in + 2hrs} | Sound check | FOH Engineer | |
    | {doors time — derive from event start - 30min} | Doors open | Door Manager | |
    | {event start} | Event begins | Stage Manager | |
    | {curfew from BIB_CONTEXT} | Curfew [VERIFY WITH LOCAL AUTHORITY] | Venue Liaison | Local noise ordinance |
    | {curfew + 1hr} | Load-out complete | Production Manager | |

    Populate times from BIB_CONTEXT. If times are unavailable, use placeholder format `HH:MM` with note "(confirm with venue)".

    ### 1.2 Contact Sheet
    | Role | Name | Phone | Email | On-site From |
    |------|------|-------|-------|-------------|
    | Production Manager | TBD | TBD | TBD | {load_in_window} |
    | FOH Engineer | TBD | TBD | TBD | {sound_check} |
    | Door Manager | TBD | TBD | TBD | {doors - 30min} |
    | Venue Liaison | TBD | TBD | TBD | {load_in_window} |
    | Security Lead | TBD | TBD | TBD | {doors - 1hr} |

    ### 1.3 Rider Fulfillment Checklist
    - [ ] Technical rider items confirmed with venue
    - [ ] Hospitality rider items sourced
    - [ ] Dietary requirements confirmed
    - [ ] Backline confirmed or hired
    ```

    Store output as BIB_SECTION_ADVANCE.

    **Pass B — Section 2: Run Sheet**

    Generate the run sheet section. Source: TML timeline artifact ({TML_PATH}) if available, otherwise derive from brief temporal data.

    ```markdown
    ## 2. Run Sheet

    > Source: TML timeline artifact ({TML_PATH or "not found — derived from brief"})
    > All times in {venue_timezone or "local time — confirm venue timezone"}.

    | Time | Duration | Activity | Responsible | Technical Cue | Contingency |
    |------|----------|----------|-------------|---------------|-------------|
    ```

    Populate the run sheet table from TML_PATH content (parse the gantt chart timeline data) or from BRIEF_CONTENT temporal data. Each row must have all six columns filled. Use "–" for empty Technical Cue or Contingency cells. Every time value that references a curfew or legally mandated deadline must include `[VERIFY WITH LOCAL AUTHORITY]`.

    Store output as BIB_SECTION_RUNSHEET.

    **Pass C — Section 3: Staffing Plan + Section 4: Budget Framework**

    Generate the staffing plan and budget framework together (they share capacity-derived calculations).

    ```markdown
    ## 3. Staffing Plan

    ### 3.1 Role Matrix
    | Role | Headcount | Shift Start | Shift End | Zone | Briefing Time | Notes |
    |------|-----------|-------------|-----------|------|---------------|-------|
    | Door Manager | {derive from capacity} | {doors - 1hr} | {curfew} | Entry | {doors - 1.5hr} | |
    | Bar Staff | {derive: 1 per 75 attendees} [VERIFY WITH LOCAL AUTHORITY] | {doors - 30min} | {close} | Bar Zone | {doors - 1hr} | Ratio is guidance only |
    | Security | {derive: 1 per 100 attendees} [VERIFY WITH LOCAL AUTHORITY] | {doors - 1hr} | {load_out} | All zones | {doors - 1.5hr} | SIA licensed where required |
    | FOH Engineer | 1 | {sound_check} | {curfew + 30min} | FOH/Stage | {sound_check} | |
    | Stage Manager | 1 | {load_in} | {load_out} | Stage | {load_in} | |
    | First Aid | {derive: minimum 2 for <500, 4 for 500-2000} [VERIFY WITH LOCAL AUTHORITY] | {doors - 30min} | {curfew + 30min} | Medical Point | {doors - 1hr} | |

    Derive headcounts from venue_capacity in BIB_CONTEXT. All ratios are industry guidance ranges — every ratio line must include `[VERIFY WITH LOCAL AUTHORITY]`.

    ### 3.2 Door Policy
    {Derive from brief audience archetype field or use placeholder: "Door policy TBD — confirm age restriction, dress code, and re-entry rules with promoter."}

    ### 3.3 Bar Menu Framework
    | Item | Price Point | Notes |
    |------|------------|-------|
    | {item} | TBD — set based on venue agreement | Populate from venue bar contract |

    Do NOT include specific pricing. Use "TBD — set based on venue agreement" for all price points.

    ## 4. Budget Framework

    ### 4.1 Line Items
    | Category | Line Item | Estimated Cost | Notes |
    |----------|-----------|----------------|-------|
    | Venue | Hire fee | TBD — obtain from venue | |
    | Technical | PA/AV — preferred vendor TBD (obtain 3 quotes) | TBD | |
    | Staffing | Door, bar, security (see Staffing Plan) | TBD | |
    | Marketing | Print collateral (see Print Spec) | TBD | |
    | Artist | Fees and hospitality | TBD | |
    | Insurance | Public liability [VERIFY WITH LOCAL AUTHORITY] | TBD | |
    | Contingency | 10% on total production costs | TBD | Industry standard buffer |

    Do NOT include specific vendor names or dollar/pound amounts. Use "TBD" placeholders throughout.

    ### 4.2 Revenue Scenarios
    | Capacity | Ticket Price | Gross Revenue | Net (after 15% fees) |
    |----------|-------------|---------------|---------------------|
    | 60% ({venue_capacity * 0.6} attendees) | TBD | TBD | TBD |
    | 80% ({venue_capacity * 0.8} attendees) | TBD | TBD | TBD |
    | 100% ({venue_capacity} attendees) | TBD | TBD | TBD |

    ### 4.3 Break-even Analysis
    Break-even ticket count = Total Costs / (Ticket Price x (1 - fee%))
    [Populate once ticket price and total cost estimates are confirmed]
    ```

    Store output as BIB_SECTION_STAFFING_BUDGET.

    **Pass D — Section 5: Post-Event Template + Section 6: Print Spec Output**

    ```markdown
    ## 5. Post-Event Template

    ### 5.1 Feedback Collection
    - [ ] Attendee survey (method: TBD — email/QR code at exit)
    - [ ] Artist / performer debrief
    - [ ] Venue staff debrief
    - [ ] Social media sentiment capture (24-hour window)

    ### 5.2 Financial Reconciliation
    | Category | Budget | Actual | Variance |
    |----------|--------|--------|---------|
    | [All line items from Section 4] | | | |

    ### 5.3 Retrospective Template
    **What went well:**
    **What to improve:**
    **Key decisions for next edition:**
    **Regulatory findings to address:** [note any AHJ feedback received]
    ```

    For Section 6 — Print Spec Output: only generate if at least one of FLY_PATH, SIT_PATH, PRG_PATH is non-null. If all are null, include: "No print collateral artifacts found. Run `/pde:wireframe` to generate event flyer and program."

    If at least one print artifact exists:

    ```markdown
    ## 6. Print Spec Output

    | Artifact | Code | Path | Format | Dimensions | Bleed | Safe Area | DPI | Color Mode | Trim Size |
    |----------|------|------|--------|------------|-------|-----------|-----|-----------|-----------|
    | Event Flyer | FLY | {FLY_PATH or "not generated"} | HTML to PDF | A5 / A4 / 1080px sq / 1080x1920 story | 3mm | 5mm | 300 DPI min | sRGB (CMYK approx. in file) | A5 |
    | Series Identity Template | SIT | {SIT_PATH or "not generated — recurring-series only"} | HTML to PDF | A5 | 3mm | 5mm | 300 DPI min | sRGB | A5 |
    | Festival Program | PRG | {PRG_PATH or "not generated — multi-day only"} | HTML to PDF | A5 multi-page | 3mm | 5mm | 300 DPI min | sRGB | A5 |

    > **Print Scope Disclaimer:** All print artifacts are composition reference guides only.
    > They are not production print files. Professional prepress conversion (PDF/X-1a or PDF/X-4)
    > and CMYK profile conversion by a qualified printer are required before commercial printing.
    ```

    Store output as BIB_SECTION_POST_PRINT.

    **BIB Assembly**

    After all four passes complete, assemble the final BIB document by writing a single file using the Write tool:

    File: `.planning/design/handoff/BIB-{event-slug}-v{BIB_VERSION}.md`

    Where `event-slug` is derived from `event_name` in BIB_CONTEXT using kebab-case (lowercase, spaces replaced with hyphens, special characters removed).

    The assembled document structure:

    ```markdown
    ---
    Generated: "{YYYY-MM-DD}"
    Skill: /pde:handoff (HND)
    Version: v{BIB_VERSION}
    Status: draft
    Product Type: "experience"
    Experience Sub-type: "{experienceSubType}"
    Event: "{event_name}"
    Venue: "{venue_name}"
    ---

    # Production Bible: {event_name}

    > **Regulatory Notice:** All regulatory values in this document (capacity limits, egress distances,
    > staffing ratios, timing requirements) are industry guidance ranges only.
    > [VERIFY WITH LOCAL AUTHORITY] before finalizing any operational plan.
    > See: `references/experience-disclaimer.md`

    {BIB_SECTION_ADVANCE}

    {BIB_SECTION_RUNSHEET}

    {BIB_SECTION_STAFFING_BUDGET}

    {BIB_SECTION_POST_PRINT}

    ---
    *Generated by PDE-OS /pde:handoff | {date} | Product Type: experience | Sub-type: {experienceSubType}*
    ```

    Display: `Step 5/7 (5-bib): BIB-{event-slug}-v{BIB_VERSION}.md written ({N} sections across 4 passes).`

    IF HND_GENERATES_SOFTWARE is true (hybrid-event only):
      ALSO write HND-handoff-spec-v{HND_VERSION}.md and HND-types-v{HND_VERSION}.ts using existing Steps 5b and 5c.
      Display: `Step 5/7 (5b): HND-handoff-spec-v{HND_VERSION}.md written (hybrid-event software layer).`
      Display: `Step 5/7 (5c): HND-types-v{HND_VERSION}.ts written (hybrid-event software layer).`
```

**Edit 4 — Step 7 manifest registration and coverage flags (after Step 7b, around line 911):**

After the existing Step 7b (HND artifact registration), add a new sub-section 7b-bib:

```markdown
#### 7b-bib. Register BIB artifact in manifest (experience products only)

IF BIB_GENERATES_SECTIONS is true:

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BIB code BIB
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BIB name "Production Bible"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BIB type production-bible
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BIB domain handoff
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BIB path ".planning/design/handoff/"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BIB status complete
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update BIB version ${BIB_VERSION}
```

Display: `Step 7/7 (7b-bib): BIB artifact registered in manifest. Version: v{BIB_VERSION}.`
```

Then update Step 7c to add `hasProductionBible` to the coverage flag merge string. The current Step 7c reads 14 flags and writes `hasHandoff: true`. Update it to:

- Read ALL current flag values (now 16 fields including `hasPrintCollateral` and `hasProductionBible`)
- IF PRODUCT_TYPE is "experience": merge `hasProductionBible: true` (and `hasHandoff: true` only if hybrid-event)
- IF PRODUCT_TYPE is NOT "experience": merge `hasHandoff: true` only (existing behavior), preserve `hasProductionBible: {current}`

Update the coverage-check read instruction to list all 16 flags:
```
Parse the JSON output from coverage-check. Extract ALL sixteen current flag values: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible.
```

Update the write command to include all 16 flags:
```bash
# For experience products:
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":{current},"hasProductionBible":true}'

# For hybrid-event (both flags):
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":{current},...,"hasHandoff":true,...,"hasProductionBible":true}'

# For non-experience products (existing behavior, updated field count):
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":{current},...,"hasHandoff":true,...,"hasPrintCollateral":{current},"hasProductionBible":{current}}'
```

Also update the Step 7a DESIGN-STATE.md update to handle experience products:

```markdown
IF PRODUCT_TYPE is "experience":
  Decision Log row: `| BIB | production bible v{BIB_VERSION} generated, experience sub-type: {experienceSubType} | {YYYY-MM-DD} |`
  Iteration History row: `| BIB-{event-slug}-v{BIB_VERSION}.md | v{BIB_VERSION} | Created by /pde:handoff | {YYYY-MM-DD} |`
```

Update the Step 7d summary table to handle experience products:

```markdown
IF PRODUCT_TYPE is "experience":
  | Property | Value |
  |----------|-------|
  | Production Bible | .planning/design/handoff/BIB-{event-slug}-v{BIB_VERSION}.md |
  | Sections | 6 (Advance, Run Sheet, Staffing, Budget, Post-Event, Print Spec) |
  | Product Type | experience |
  | Sub-type | {experienceSubType} |
  | Generation Passes | 4 |
  | Upstream Artifacts | FLP: {found/missing}, TML: {found/missing}, FLY/SIT/PRG: {found/missing} |

IF PRODUCT_TYPE is "experience" AND experienceSubType is "hybrid-event":
  Also show HND spec and types rows from the standard summary.
```

**Edit 5 — Anti-Patterns section (around line 953):**

Add these anti-patterns to the existing list:

```markdown
- **Generate all BIB sections in a single pass:** The four-pass split (Pass A through Pass D) is mandatory. Single-pass generation truncates at the staffing plan section for venues above 500 capacity. Each pass generates specific named sections; the final assembly step collects all four outputs.

- **Include specific vendor names or pricing in budget:** Never generate actual vendor names or specific monetary amounts. Use placeholder categories: "preferred AV vendor — obtain 3 quotes", "TBD — set based on venue agreement".

- **State regulatory thresholds without disclaimer:** Never state a specific ratio, distance, or occupancy limit in the production bible without `[VERIFY WITH LOCAL AUTHORITY]` on the same line or immediately following. This applies to every numerical regulatory value in Sections 1, 3, and 4.

- **Generate BIB for software products:** The experience branch at Step 4i must ONLY execute when PRODUCT_TYPE is "experience". Software, hardware, and hybrid (non-hybrid-event) products must NEVER receive production bible sections.

- **Require STACK.md for pure experience products:** Step 2a bypass must be in place for non-hybrid-event experience products. The STACK.md hard-stop error must not trigger for single-night, multi-day, recurring-series, or installation sub-types.
```

**Edit 6 — templates/design-manifest.json:**

Add `"hasPrintCollateral": false` and `"hasProductionBible": false` to the `designCoverage` object, after `"hasStitchWireframes": false`:

```json
"designCoverage": {
    ...
    "hasStitchWireframes": false,
    "hasPrintCollateral": false,
    "hasProductionBible": false
}
```
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-81/handoff-production-bible.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - workflows/handoff.md contains "Advance Document" (not just as a stub comment)
    - workflows/handoff.md contains "Run Sheet" with "| Time | Duration | Activity | Responsible | Technical Cue | Contingency |"
    - workflows/handoff.md contains "Staffing Plan" with "Role Matrix"
    - workflows/handoff.md contains "Budget Framework" with "60%" and "80%" and "100%"
    - workflows/handoff.md contains "Post-Event Template" with "Feedback Collection"
    - workflows/handoff.md contains "Print Spec Output" with "FLY" and "SIT" and "PRG" and "DPI" and "CMYK"
    - workflows/handoff.md contains "[VERIFY WITH LOCAL AUTHORITY]" at least 5 times in experience branch sections
    - workflows/handoff.md contains "BIB_GENERATES_SECTIONS" and "HND_GENERATES_SOFTWARE"
    - workflows/handoff.md contains "Pass A" and "Pass B" and "Pass C" and "Pass D"
    - workflows/handoff.md contains "SKIP STACK.md check" conditional for non-hybrid experience
    - workflows/handoff.md does NOT contain "Phase 74 stub" in Step 4i or Step 5 section 11 (both stubs removed)
    - workflows/handoff.md contains "hasProductionBible" in Step 7c
    - workflows/handoff.md contains "7b-bib" sub-section for BIB manifest registration
    - templates/design-manifest.json contains "hasPrintCollateral" and "hasProductionBible" in designCoverage
    - Running `node --test tests/phase-81/handoff-production-bible.test.mjs` produces ALL PASSING tests (green phase)
  </acceptance_criteria>
  <done>Both Phase 74 stubs replaced with full experience branch. handoff.md contains six BIB sections across four-pass generation, Step 2a bypass for pure experience, Step 7 BIB registration + 16-flag coverage write, updated anti-patterns. Manifest template has both new flags. All Nyquist tests pass.</done>
</task>

</tasks>

<verification>
1. `node --test tests/phase-81/handoff-production-bible.test.mjs 2>&1` — all tests pass
2. `grep -c "VERIFY WITH LOCAL AUTHORITY" workflows/handoff.md` — returns >= 5 (experience branch sections)
3. `grep -c "Phase 74 stub" workflows/handoff.md` — returns 0 (both stubs removed)
4. `grep "hasProductionBible" templates/design-manifest.json` — returns a match
5. `grep "hasPrintCollateral" templates/design-manifest.json` — returns a match
6. `grep "Pass A" workflows/handoff.md && grep "Pass D" workflows/handoff.md` — both match (four-pass split)
7. `grep "BIB_GENERATES_SECTIONS" workflows/handoff.md` — returns a match
8. `grep "SKIP STACK.md check" workflows/handoff.md` — returns a match
</verification>

<success_criteria>
- All HDOF-01 through HDOF-06 requirements have corresponding workflow instructions in handoff.md
- The Nyquist test suite passes with all structural assertions green
- Both Phase 74 stubs are fully replaced (zero occurrences of "Phase 74 stub" in handoff.md)
- The four-pass split generation pattern is documented in the workflow
- The STACK.md bypass is in place for non-hybrid experience products
- The manifest template includes all 16 coverage flags
- The Step 7 manifest registration and coverage write handle experience products correctly
</success_criteria>

<output>
After completion, create `.planning/phases/81-handoff-production-bible/81-01-SUMMARY.md`
</output>
