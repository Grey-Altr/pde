---
phase: 57-workflow-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/plan-phase.md
  - ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md
autonomous: true
requirements: [RVAL-07, RVAL-08, WIRE-01]

must_haves:
  truths:
    - "When plan-phase detects RESEARCH.md but no RESEARCH-VALIDATION.md, it spawns pde-research-validator automatically"
    - "If contradicted_count > 0 the user sees a blocking choice prompt before planning continues"
    - "If unverifiable_count > 0 a non-blocking warning is displayed and planning continues"
    - "Step 5.7 runs between Step 5 and Step 5.5 in the pipeline"
    - "When --research flag is active and new RESEARCH.md was written, stale RESEARCH-VALIDATION.md is deleted before Step 5.7"
  artifacts:
    - path: "workflows/plan-phase.md"
      provides: "Step 5.7 Research Validation Gate"
      contains: "5.7"
    - path: "~/.claude/pde-os/engines/gsd/workflows/plan-phase.md"
      provides: "Step 5.7 Research Validation Gate (system copy)"
      contains: "5.7"
  key_links:
    - from: "workflows/plan-phase.md Step 5.7"
      to: "agents/pde-research-validator.md"
      via: "Task spawn with research_file_path and validated_at_phase"
      pattern: "pde-research-validator"
    - from: "workflows/plan-phase.md Step 5.7"
      to: "AskUserQuestion"
      via: "contradicted_count > 0 gate"
      pattern: "contradicted_count"
---

<objective>
Wire research validation into plan-phase.md as automatic Step 5.7 — spawns pde-research-validator when RESEARCH.md exists but RESEARCH-VALIDATION.md does not, blocks on contradicted claims, surfaces unverifiable claims as non-blocking warnings.

Purpose: Eliminates manual invocation of research validation; contradicted research is caught before plans are built from it.
Output: Both copies of plan-phase.md updated with Step 5.7 Research Validation Gate.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-workflow-integration/57-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs from plan-phase.md and the research validator agent -->

From agents/pde-research-validator.md — agent return format:
```json
{
  "status": "COMPLETE",
  "validated_at_phase": "57",
  "summary": {
    "total_claims": 9,
    "verified_count": 6,
    "unverifiable_count": 2,
    "contradicted_count": 1,
    "result": "FAIL"
  },
  "artifact_content": "---\nphase: 57-workflow-integration\n..."
}
```

From workflows/plan-phase.md — existing artifact detection pattern (Step 7.5):
```bash
VALIDATION_EXISTS=$(ls "${PHASE_DIR}"/*-VALIDATION.md 2>/dev/null | head -1)
```

From workflows/plan-phase.md — existing AskUserQuestion pattern (Step 5.6):
```markdown
Use AskUserQuestion:
- header: "UI Design Contract"
- question: "Phase {N} has frontend indicators..."
- options:
  - "Generate UI-SPEC first" -> Display: "Run /pde:ui-phase..."
  - "Continue without UI-SPEC" -> Continue to step 6.
```

From workflows/plan-phase.md — existing banner pattern:
```bash
node "${CLAUDE_PLUGIN_ROOT}/lib/ui/render.cjs" banner "RESEARCHING PHASE {X}"
```

From workflows/plan-phase.md — existing commit pattern:
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" commit "docs(phase-${PHASE}): add validation strategy"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Step 5.7 Research Validation Gate into project plan-phase.md</name>
  <files>workflows/plan-phase.md</files>
  <read_first>
    - workflows/plan-phase.md (MUST read current Step 5, Step 5.5, Step 5.6 to understand exact insertion point)
    - agents/pde-research-validator.md (agent definition — confirms return format and read-only constraint)
    - .planning/phases/57-workflow-integration/57-RESEARCH.md (Pattern 1 and Pitfall 2/4/5 details)
  </read_first>
  <action>
Insert a new `## 5.7. Research Validation Gate` section in `workflows/plan-phase.md` BETWEEN the end of Step 5 (Handle Research, which ends with "### Handle Researcher Return") and Step 5.5 (Create Validation Strategy).

The new section must contain:

**Skip conditions block:**
```
Skip if: `--gaps` flag, `--skip-research` flag, `research_enabled` is false, `has_research` is false, or RESEARCH-VALIDATION.md already present.
```

**Stale artifact deletion (per open question 3 decision):**
```markdown
**If `--research` flag was active AND Step 5 just wrote a new RESEARCH.md:**
Delete stale RESEARCH-VALIDATION.md before detection:
```bash
rm -f "${PHASE_DIR}"/*-RESEARCH-VALIDATION.md
```
```

**Detection block:**
```bash
RV_ARTIFACT=$(ls "${PHASE_DIR}"/*-RESEARCH-VALIDATION.md 2>/dev/null | head -1)
```
If `RV_ARTIFACT` is non-empty, skip — validation already exists.

**Banner:**
```bash
node "${CLAUDE_PLUGIN_ROOT}/lib/ui/render.cjs" banner "VALIDATING RESEARCH"
```

**Agent spawn:**
```markdown
### Spawn pde-research-validator

Research validation prompt:

<objective>
Validate the research claims in {research_path} against the current codebase.
</objective>

<files_to_read>
- {research_path}
</files_to_read>

<additional_context>
**validated_at_phase:** {phase_number}
</additional_context>

Task(
  prompt=validation_prompt,
  subagent_type="pde-research-validator",
  model="{researcher_model}",
  description="Validate research for Phase {phase}"
)
```

**Post-spawn orchestrator responsibilities (4 steps):**
1. Parse JSON return from agent — extract `summary` and `artifact_content`
2. Write `artifact_content` to `${PHASE_DIR}/${PADDED_PHASE}-RESEARCH-VALIDATION.md` using the Write tool (agent is read-only per RVAL-05 — orchestrator MUST write)
3. If `commit_docs`: `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" commit "docs(phase-${PHASE}): add research validation" --files ${PHASE_DIR}/${PADDED_PHASE}-RESEARCH-VALIDATION.md`
4. Parse `summary.contradicted_count` and `summary.unverifiable_count`

**Blocking gate (RVAL-08):**
```markdown
IF summary.contradicted_count > 0:
  Use AskUserQuestion:
  - header: "Research Validation: FAIL"
  - question: "Research validation found {contradicted_count} contradicted claims. Plans built on contradicted claims risk incorrect implementation."
  - options:
    - "View contradictions and fix research" -> Display RESEARCH-VALIDATION.md contents. Exit workflow.
    - "Proceed anyway" -> Continue to Step 5.5.

ELIF summary.unverifiable_count > 0:
  Display non-blocking warning: "{unverifiable_count} unverifiable claims will appear as CONCERNS in READINESS.md."
  Continue to Step 5.5.

ELSE:
  Display: "Research validation: PASS — all claims verified."
  Continue to Step 5.5.
```

**Critical ordering:** Step 5.7 MUST appear BEFORE Step 5.5. If research is contradicted and user chooses to exit, no VALIDATION.md is created from bad research.

Use `${CLAUDE_PLUGIN_ROOT}` for all tool paths (matching project copy convention).
  </action>
  <verify>
    <automated>grep -c "5\.7\.\|Research Validation Gate" workflows/plan-phase.md | grep -q "^[2-9]" && grep "pde-research-validator" workflows/plan-phase.md | head -1 && grep "contradicted_count" workflows/plan-phase.md | head -1 && grep "unverifiable_count" workflows/plan-phase.md | head -1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/plan-phase.md contains the string "## 5.7. Research Validation Gate"
    - workflows/plan-phase.md contains "pde-research-validator" in the Step 5.7 section
    - workflows/plan-phase.md contains "contradicted_count > 0" in the blocking gate
    - workflows/plan-phase.md contains "unverifiable_count > 0" in the non-blocking warning
    - workflows/plan-phase.md contains "artifact_content" in the post-spawn section (orchestrator writes, not agent)
    - workflows/plan-phase.md contains "rm -f" for stale RESEARCH-VALIDATION.md deletion when --research flag active
    - Step 5.7 appears AFTER "Handle Researcher Return" and BEFORE "## 5.5"
  </acceptance_criteria>
  <done>Step 5.7 exists in project plan-phase.md with detection, spawn, write, blocking gate, and non-blocking warning</done>
</task>

<task type="auto">
  <name>Task 2: Replicate Step 5.7 to system copy of plan-phase.md</name>
  <files>~/.claude/pde-os/engines/gsd/workflows/plan-phase.md</files>
  <read_first>
    - workflows/plan-phase.md (source of truth — just updated with Step 5.7)
    - ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md (target — system copy uses $HOME paths and gsd-* agent names)
  </read_first>
  <action>
Insert the same Step 5.7 Research Validation Gate into `~/.claude/pde-os/engines/gsd/workflows/plan-phase.md`.

**Key differences from the project copy that MUST be preserved:**
- Tool paths use `$HOME/.claude/pde-os/engines/gsd/bin/gsd-tools.cjs` instead of `${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs`
- UI render uses `$HOME/.claude/pde-os/lib/ui/render.cjs` instead of `${CLAUDE_PLUGIN_ROOT}/lib/ui/render.cjs`
- Agent names may use `gsd-*` prefix instead of `pde-*` (check current file — if system copy uses `gsd-phase-researcher`, then use `gsd-research-validator` for subagent_type; if it uses `pde-phase-researcher`, keep `pde-research-validator`)

Read the system copy first to identify the exact naming convention, then translate Step 5.7 accordingly. The logic (skip conditions, detection, spawn, blocking gate, non-blocking warning) must be identical to the project copy.
  </action>
  <verify>
    <automated>grep -c "5\.7\.\|Research Validation Gate" ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md | grep -q "^[2-9]" && grep "research-validator" ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md | head -1 && grep "contradicted_count" ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md | head -1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md contains "## 5.7. Research Validation Gate"
    - ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md contains "research-validator" in the Step 5.7 section
    - ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md contains "contradicted_count > 0"
    - ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md contains "unverifiable_count > 0"
    - Tool paths in Step 5.7 match the system copy convention (NOT ${CLAUDE_PLUGIN_ROOT})
  </acceptance_criteria>
  <done>System copy of plan-phase.md has identical Step 5.7 logic with correct path conventions</done>
</task>

</tasks>

<verification>
Both copies of plan-phase.md contain Step 5.7 Research Validation Gate with:
- Automatic detection via shell glob
- pde-research-validator agent spawn
- Orchestrator-side artifact writing
- Blocking gate on contradicted_count > 0
- Non-blocking warning on unverifiable_count > 0
- Stale artifact deletion on --research flag
</verification>

<success_criteria>
- `grep "5.7" workflows/plan-phase.md` returns the Research Validation Gate heading
- `grep "5.7" ~/.claude/pde-os/engines/gsd/workflows/plan-phase.md` returns the Research Validation Gate heading
- Both files contain contradicted_count and unverifiable_count gate logic
- Step 5.7 appears before Step 5.5 in both files
</success_criteria>

<output>
After completion, create `.planning/phases/57-workflow-integration/57-01-SUMMARY.md`
</output>
