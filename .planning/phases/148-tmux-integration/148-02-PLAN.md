---
phase: 148-tmux-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/tmux-cycle-session.cjs
  - tests/dispatcher/tmux-cycle-session.test.cjs
autonomous: true
requirements: [TMX-04, TMX-05]
must_haves:
  truths:
    - "Pressing s cycles through running sessions then back to all"
    - "Pressing a resets filter to all sessions"
    - "Filter state persists in a file readable by pane scripts"
  artifacts:
    - path: "bin/lib/tmux-cycle-session.cjs"
      provides: "Node script that reads dispatcher.pids registry and cycles session filter file"
      min_lines: 25
    - path: "tests/dispatcher/tmux-cycle-session.test.cjs"
      provides: "Unit tests for session cycling logic"
      min_lines: 30
  key_links:
    - from: "bin/lib/tmux-cycle-session.cjs"
      to: ".planning/dispatcher.pids"
      via: "JSON.parse(fs.readFileSync(PIDS_FILE))"
      pattern: "dispatcher\\.pids"
    - from: "bin/lib/tmux-cycle-session.cjs"
      to: "$TMPDIR/pde-tmux-filter.txt"
      via: "fs.writeFileSync(FILTER_FILE, next)"
      pattern: "pde-tmux-filter\\.txt"
---

<objective>
Create the tmux-cycle-session.cjs script that tmux's `run-shell` invokes when the user presses `s`. It reads the dispatcher registry to find running sessions, reads the current filter from a state file, advances to the next session (or wraps to "all"), and writes the new filter.

Purpose: TMX-04 and TMX-05 require session switching via `s` key (cycle) and `a` key (all). The `a` key is a simple file write handled in monitor-dashboard.sh. The `s` key needs this helper to read the registry and compute the next filter value.
Output: tmux-cycle-session.cjs, unit tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-tmux-integration/148-RESEARCH.md

@packages/dispatcher/lib/registry.cjs
</context>

<interfaces>
<!-- dispatcher.pids JSON format from registry.cjs -->
From packages/dispatcher/lib/registry.cjs (saveToDisk output):
```json
{
  "sessions": {
    "p148-1-abc12345": {
      "phase": "148",
      "plan": "1",
      "status": "running",
      "backend": "local",
      "worktree": "/path/to/worktree",
      "branch": "pde/session/p148-1-abc12345"
    },
    "p148-2-def67890": {
      "status": "failed",
      "backend": "ssh"
    }
  }
}
```

Filter file (pde-tmux-filter.txt) contains either:
- "all" (show all sessions)
- A session ID string like "p148-1-abc12345" (show only that session)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tmux-cycle-session.cjs and tests</name>
  <files>bin/lib/tmux-cycle-session.cjs, tests/dispatcher/tmux-cycle-session.test.cjs</files>
  <read_first>
    - packages/dispatcher/lib/registry.cjs (saveToDisk format — the JSON structure of dispatcher.pids)
    - bin/lib/event-bus.cjs (example of CJS module in bin/lib/ — follow shebang and style conventions)
  </read_first>
  <behavior>
    - Test 1: When filter is "all" and 2 running sessions exist (sorted: s1, s2), next filter is s1
    - Test 2: When filter is s1 and sessions are [s1, s2], next filter is s2
    - Test 3: When filter is s2 (last session) and sessions are [s1, s2], next filter wraps to "all"
    - Test 4: When filter is "all" and no running sessions exist, filter stays "all"
    - Test 5: When filter is a sessionId not in running sessions, filter resets to "all"
    - Test 6: Only sessions with status "running" are included (failed/completed filtered out)
    - Test 7: When filter file does not exist, defaults to "all" then cycles to first running session
  </behavior>
  <action>
    Create `bin/lib/tmux-cycle-session.cjs`:

    ```
    #!/usr/bin/env node
    'use strict';
    const fs = require('node:fs');
    const path = require('node:path');
    const os = require('node:os');

    const FILTER_FILE = path.join(os.tmpdir(), 'pde-tmux-filter.txt');

    function cycleSession(pidsFile, filterFile) {
      const filterPath = filterFile || FILTER_FILE;

      // Read running sessions from dispatcher.pids
      let sessions = [];
      try {
        const raw = JSON.parse(fs.readFileSync(pidsFile, 'utf8'));
        sessions = Object.entries(raw.sessions || {})
          .filter(([, entry]) => entry.status === 'running')
          .map(([id]) => id)
          .sort();
      } catch (_) {}

      // Read current filter
      let current;
      try { current = fs.readFileSync(filterPath, 'utf8').trim(); } catch { current = 'all'; }

      // Compute next
      let next;
      if (current === 'all') {
        next = sessions[0] || 'all';
      } else {
        const idx = sessions.indexOf(current);
        if (idx === -1 || idx === sessions.length - 1) {
          next = 'all';
        } else {
          next = sessions[idx + 1];
        }
      }

      fs.writeFileSync(filterPath, next, 'utf8');
      return next;
    }

    // CLI entry point: node tmux-cycle-session.cjs <pids-file>
    if (require.main === module) {
      const pidsFile = process.argv[2];
      if (!pidsFile) {
        process.stderr.write('Usage: tmux-cycle-session.cjs <pids-file>\n');
        process.exit(1);
      }
      cycleSession(pidsFile);
    }

    module.exports = { cycleSession, FILTER_FILE };
    ```

    Create `tests/dispatcher/tmux-cycle-session.test.cjs` with vitest tests for all 7 behavior cases. Use temp files for both pidsFile and filterFile (pass both args to `cycleSession`). Write the dispatcher.pids JSON structure to the temp pidsFile before each test. Read the filterFile after each call to verify the written value.
  </action>
  <verify>
    <automated>npx vitest run tests/dispatcher/tmux-cycle-session.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/tmux-cycle-session.cjs contains `function cycleSession`
    - bin/lib/tmux-cycle-session.cjs contains `module.exports = { cycleSession, FILTER_FILE }`
    - bin/lib/tmux-cycle-session.cjs contains `status === 'running'`
    - bin/lib/tmux-cycle-session.cjs contains `pde-tmux-filter.txt`
    - bin/lib/tmux-cycle-session.cjs contains `.sort()`
    - tests/dispatcher/tmux-cycle-session.test.cjs contains at least 6 `it(` or `test(` blocks
    - `npx vitest run tests/dispatcher/tmux-cycle-session.test.cjs` exits 0
  </acceptance_criteria>
  <done>tmux-cycle-session.cjs correctly cycles through running sessions and back to "all", with all 7 behavior cases passing.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/dispatcher/tmux-cycle-session.test.cjs` — all cycle tests green
- `npx vitest run tests/dispatcher/` — full dispatcher suite green (regression)
</verification>

<success_criteria>
- tmux-cycle-session.cjs exists in bin/lib/ and exports cycleSession function
- Cycling logic: all -> session1 -> session2 -> ... -> all (wrap)
- Only "running" sessions from dispatcher.pids are candidates
- Filter state written to $TMPDIR/pde-tmux-filter.txt
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/148-tmux-integration/148-02-SUMMARY.md`
</output>
