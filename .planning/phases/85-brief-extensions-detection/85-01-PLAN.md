---
phase: 85-brief-extensions-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/brief.md
  - .planning/phases/85-brief-extensions-detection/tests/test-brief-detection.cjs
autonomous: true
requirements: [BRIEF-01, BRIEF-02, BRIEF-05, BRIEF-07]

must_haves:
  truths:
    - "brief.md scans PROJECT.md for business signals and classifies businessMode as true when 3+ signals across 2+ categories are found"
    - "brief.md prompts user to confirm or change the auto-detected business track when businessMode is true"
    - "brief.md adds a Domain Strategy section to the BRF artifact when businessMode is true"
    - "brief.md writes businessMode and businessTrack to manifest via manifest-set-top-level in Step 7"
    - "All financial references in the Domain Strategy section use [YOUR_X] placeholders, never dollar amounts"
  artifacts:
    - path: "workflows/brief.md"
      provides: "Business detection logic in Step 4, Domain Strategy in Step 5, manifest writes in Step 7"
      contains: "Business intent detection"
    - path: ".planning/phases/85-brief-extensions-detection/tests/test-brief-detection.cjs"
      provides: "Structural tests for BRIEF-01, BRIEF-02, BRIEF-05, BRIEF-07"
      min_lines: 50
  key_links:
    - from: "workflows/brief.md"
      to: "references/business-track.md"
      via: "required_reading reference"
      pattern: "@references/business-track.md"
    - from: "workflows/brief.md"
      to: "references/business-financial-disclaimer.md"
      via: "required_reading reference"
      pattern: "@references/business-financial-disclaimer.md"
---

<objective>
Add business intent detection, track selection, and domain strategy capture to the brief workflow.

Purpose: Enable PDE to detect when a user describes a business idea (not just a product) and activate the business pipeline with track-specific vocabulary and depth.
Output: Modified workflows/brief.md with business detection in Step 4, domain strategy section in Step 5, and businessMode/businessTrack manifest writes in Step 7. Test scaffold covering detection assertions.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-brief-extensions-detection/85-RESEARCH.md
@.planning/phases/84-foundation/84-01-SUMMARY.md
@.planning/phases/84-foundation/84-02-SUMMARY.md

<interfaces>
<!-- Key references the executor needs -->

From workflows/brief.md (insertion points):
- INSERTION POINT 1: After Step 4 display line (~line 224), before Step 5 header — business detection + track prompt
- INSERTION POINT 2: After BRF Write tool call (~line 274), before Step 5 display line — Domain Strategy section (conditional)
- INSERTION POINT 3: After existing manifest-set-top-level commands in Step 7 (~line 579) — businessMode/businessTrack writes

From references/business-track.md:
- Track signal lists: solo_founder, startup_team, product_leader (detection order: product_leader first, solo_founder default)
- Vocabulary substitution tables for downstream use

From references/business-financial-disclaimer.md:
- Placeholder format: [YOUR_X] with [VERIFY FINANCIAL ASSUMPTIONS] inline tag
- Prohibited: $[digit] patterns, specific percentages as facts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffold for brief detection requirements</name>
  <files>.planning/phases/85-brief-extensions-detection/tests/test-brief-detection.cjs</files>
  <read_first>
    - .planning/phases/84-foundation/tests/test-foundation.cjs
    - workflows/brief.md
    - references/business-track.md
    - references/business-financial-disclaimer.md
  </read_first>
  <action>
Create a node:test structural test file at `.planning/phases/85-brief-extensions-detection/tests/test-brief-detection.cjs` that verifies the brief.md workflow file contains the correct business detection infrastructure. These are STRUCTURAL tests that grep/read the workflow file content — not runtime tests.

Test blocks to create (all should FAIL initially — RED state):

**BRIEF-01 tests (business intent detection):**
- Test: brief.md contains "Business intent detection" section header or comment
- Test: brief.md contains the 5-category signal taxonomy (check for "Model signals", "Market signals", "Launch signals", "Metrics signals", "Positioning signals" — at least 3 of 5)
- Test: brief.md contains the threshold logic "3" and "2+" categories (check for string "BUSINESS_SIGNAL_COUNT >= 3" or "3+" and "2+" in proximity)
- Test: brief.md contains the strong single signal override (check for "business model" and "revenue model" and "go-to-market" as override signals)
- Test: brief.md contains "businessMode = false" as default

**BRIEF-02 tests (track selection):**
- Test: brief.md contains track selection prompt text (check for "solo_founder" AND "startup_team" AND "product_leader" appearing in a prompt context)
- Test: brief.md contains track detection order (product_leader checked first)
- Test: brief.md contains --force flag handling for track prompt (check for "--force" in proximity to "track")
- Test: brief.md contains default track fallback to solo_founder

**BRIEF-05 tests (domain strategy):**
- Test: brief.md contains "## Domain Strategy" section template
- Test: brief.md contains "Naming Direction" within Domain Strategy context
- Test: brief.md contains "Domain Availability Notes" within Domain Strategy context
- Test: brief.md contains "Brand Positioning Seeds" within Domain Strategy context
- Test: brief.md contains businessMode gate for Domain Strategy (check for "businessMode" in proximity to "Domain Strategy")

**BRIEF-07 tests (financial placeholder enforcement):**
- Test: brief.md contains reference to `@references/business-financial-disclaimer.md` in required_reading block
- Test: brief.md contains `[YOUR_` placeholder pattern instruction text
- Test: brief.md contains post-write verification for dollar amounts (check for `$[0-9]` or `\$[0-9]` grep pattern in the workflow text)

Use `node:test` built-in with `describe`/`it` blocks. Read the workflow file once at top of test, then run all assertions against the content string. Use `assert.ok()` for pattern matches. Use `fs.readFileSync` to load `workflows/brief.md`.

Structure:
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert');
const fs = require('fs');
const path = require('path');

const ROOT = path.resolve(__dirname, '..', '..', '..', '..');
const briefContent = fs.readFileSync(path.join(ROOT, 'workflows', 'brief.md'), 'utf-8');

describe('BRIEF-01: Business intent detection', () => {
  // tests...
});
// etc.
```

All tests must FAIL when run against the current brief.md (Wave 0 RED state). Verify by running `node --test` and confirming failures.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test .planning/phases/85-brief-extensions-detection/tests/test-brief-detection.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/85-brief-extensions-detection/tests/test-brief-detection.cjs exists and is valid JavaScript
    - File contains `describe('BRIEF-01` block
    - File contains `describe('BRIEF-02` block
    - File contains `describe('BRIEF-05` block
    - File contains `describe('BRIEF-07` block
    - Running `node --test` exits non-zero (all tests fail against unmodified brief.md)
    - File uses `require('node:test')` and `require('node:assert')` — no external dependencies
  </acceptance_criteria>
  <done>Test scaffold exists with structural assertions for BRIEF-01, BRIEF-02, BRIEF-05, BRIEF-07. All tests fail (RED state) against unmodified brief.md.</done>
</task>

<task type="auto">
  <name>Task 2: Add business detection, track selection, domain strategy, and manifest writes to brief.md</name>
  <files>workflows/brief.md</files>
  <read_first>
    - workflows/brief.md
    - references/business-track.md
    - references/business-financial-disclaimer.md
    - references/launch-frameworks.md
    - .planning/phases/85-brief-extensions-detection/85-RESEARCH.md
  </read_first>
  <action>
Modify `workflows/brief.md` at three insertion points. Read the full file first to identify exact line numbers.

**CHANGE 1: Add required_reading references (lines 5-8 area)**

Add 3 new references to the `<required_reading>` block:
```
@references/business-track.md
@references/launch-frameworks.md
@references/business-financial-disclaimer.md
```

**CHANGE 2: Add business detection to Step 4 (after Step 4 display, before Step 5)**

Insert after the Step 4 display line (`Step 4/7: Product type detected.`) and before the `### Step 5/7` header. Add this content:

```markdown
#### Business Intent Detection (BRIEF-01)

After product_type detection, scan PROJECT.md content for business-specific signals:

BUSINESS_SIGNAL_COUNT = 0
BUSINESS_CATEGORIES_HIT = set()

Check each category against PROJECT.md content (case-insensitive):
  Model signals: "business model", "revenue", "monetize", "SaaS", "subscription", "pricing", "freemium", "B2B", "B2C"
    -> if any found: BUSINESS_SIGNAL_COUNT += count of matches, add "model" to BUSINESS_CATEGORIES_HIT
  Market signals: "go-to-market", "GTM", "acquisition", "churn", "LTV", "CAC", "market fit", "target market"
    -> if any found: BUSINESS_SIGNAL_COUNT += count of matches, add "market" to BUSINESS_CATEGORIES_HIT
  Launch signals: "startup", "found", "venture", "bootstrap", "seed", "funding", "investor", "pitch deck"
    -> if any found: BUSINESS_SIGNAL_COUNT += count of matches, add "launch" to BUSINESS_CATEGORIES_HIT
  Metrics signals: "ARR", "MRR", "burn rate", "runway", "unit economics", "profit", "margin"
    -> if any found: BUSINESS_SIGNAL_COUNT += count of matches, add "metrics" to BUSINESS_CATEGORIES_HIT
  Positioning signals: "competitive advantage", "differentiation", "unfair advantage", "market position"
    -> if any found: BUSINESS_SIGNAL_COUNT += count of matches, add "positioning" to BUSINESS_CATEGORIES_HIT

Classification logic:
IF BUSINESS_SIGNAL_COUNT >= 3 AND len(BUSINESS_CATEGORIES_HIT) >= 2:
  SET businessMode = true
ELSE IF strong single signal found (exact phrase match for "business model", "revenue model", "go-to-market strategy"):
  SET businessMode = true
ELSE IF SEQUENTIAL_THINKING_AVAILABLE AND BUSINESS_SIGNAL_COUNT == 2 AND len(BUSINESS_CATEGORIES_HIT) == 1:
  Use Sequential Thinking to analyze: "Is the primary intent of this project description business design (requires business model, market strategy, or financial planning artifacts) or product/software design? Business signals found: {list}. Content: {first 300 chars}. Return: BUSINESS_INTENT or PRODUCT_INTENT with rationale."
  SET businessMode = result == "BUSINESS_INTENT"
ELSE:
  SET businessMode = false

Default: businessMode = false when signals are absent or below threshold.

#### Track Detection (BRIEF-02)

IF businessMode == true:

  Scan PROJECT.md content for track signals (check in this order — most specific first):

  product_leader signals: "product leader", "PM", "head of product", "enterprise", "director", "VP", "product manager"
  startup_team signals: "startup", "seed", "early stage", "founding team", "co-founder", "pre-seed", "Series A"
  solo_founder signals: "solo", "indie", "solo founder", "one person", "bootstrapped", "side project", "just me"

  Default: solo_founder when no track signals detected or signals are ambiguous.

  **Track selection prompt (interactive):**

  IF --force OR --quick is present in $ARGUMENTS:
    Use auto-detected track without prompting. Log:
    `  -> --force mode: using detected track {track} without confirmation prompt.`
  ELSE IF --dry-run is present:
    Display: `  -> Business track: {detected_track} (dry-run — no prompt)`
    (Do not prompt — dry-run halts after Step 4)
  ELSE:
    Present to user:
    ```
    Business intent detected. Select your track:

      1. solo_founder — individual builder or bootstrapped maker
      2. startup_team — early-stage startup with co-founders or investors
      3. product_leader — PM or product director within an organization

    Detected from your description: {detected_track}
    Press Enter to confirm or type 1/2/3 to change.
    ```
    Store user-confirmed value as businessTrack.

ELSE (businessMode == false):
  SET businessTrack = null

Display (appended to Step 4 output):
```
  -> Business mode: {true/false}. Track: {businessTrack or N/A}.
```

IF --dry-run: Display business detection results and HALT (do not proceed to Step 5). Include in dry-run output: `Business mode: {value}, Track: {value}`.
```

**CHANGE 3: Add Domain Strategy section to Step 5 BRF content (BRIEF-05)**

In Step 5, after the existing BRF content sections (after `## Scope Boundaries` and after any experience-type sections like `## Repeatability Intent`), add a conditional business-mode section:

```markdown
#### Domain Strategy Section (BRIEF-05)

IF businessMode == true, append this section to the BRF artifact content AFTER `## Scope Boundaries` (and after any experience-type sections if product_type == "experience"):

```markdown
## Domain Strategy
*(Generated when businessMode = true)*

**Naming Direction:**
[2-3 candidate naming directions derived from the UVP — descriptive, evocative, or coined word approaches. No dollar amounts. No specific pricing references.]

**Domain Availability Notes:**
[YOUR_DOMAIN_NAME].com — [verify availability]
[Placeholder notes — user must verify actual domain availability before proceeding]

**Brand Positioning Seeds:**
[3-5 positioning phrases derived from UVP and competitive differentiation for downstream brand system consumption in Phase 88. Qualitative language only — no dollar amounts, no specific revenue targets.]
```

**Financial placeholder enforcement (BRIEF-07):** The Domain Strategy section must use qualitative language only. Brand positioning seeds must NOT contain dollar amounts, specific percentages, or revenue targets. If any financial context is needed, use `[YOUR_X]` format from `@references/business-financial-disclaimer.md`.
```

**CHANGE 4: Add manifest writes to Step 7 (after existing manifest-set-top-level calls)**

After the existing `manifest-set-top-level` commands for `projectName`, `productType`, and `experienceSubType` in Step 7, add:

```markdown
#### Business Mode Manifest Writes

Write businessMode and businessTrack to manifest (always — even when false/null):

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level businessMode {businessMode_value}
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level businessTrack {businessTrack_value_or_null}
```

Where {businessMode_value} is the literal `true` or `false` determined in Step 4, and {businessTrack_value_or_null} is the confirmed track string (e.g., `solo_founder`) or `null`.

**Financial content verification (BRIEF-07):** After writing the BRF artifact in Step 5, verify no dollar amounts leaked into the Domain Strategy section:

```bash
if grep -qE '\$[0-9]' ".planning/design/strategy/BRF-brief-v${N}.md" 2>/dev/null; then
  echo "WARNING: Dollar amount detected in BRF artifact. Financial content must use [YOUR_X] placeholders per @references/business-financial-disclaimer.md."
  echo "Offending lines:"
  grep -nE '\$[0-9]' ".planning/design/strategy/BRF-brief-v${N}.md"
fi
```
```

**CHANGE 5: Update Summary table (at end of brief.md)**

In the Summary display block, add a conditional row when businessMode is true:

```markdown
IF businessMode == true, add to summary table:
| Business mode | {businessMode} — track: {businessTrack} |
```

**CHANGE 6: Update `<output>` block**

Add conditional business output files:
```markdown
When businessMode = true, the following additional files are created in Plan 02:
- `.planning/design/strategy/BTH-thesis-v{N}.md` — Business thesis
- `.planning/design/strategy/LCV-lean-canvas-v{N}.md` — Lean canvas
```

IMPORTANT: Do NOT add Steps 5b/5c (BTH/LCV generation) in this plan. Those are Plan 02's responsibility. This plan only adds detection, domain strategy, and manifest writes.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test .planning/phases/85-brief-extensions-detection/tests/test-brief-detection.cjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - workflows/brief.md contains `@references/business-track.md` in required_reading block
    - workflows/brief.md contains `@references/launch-frameworks.md` in required_reading block
    - workflows/brief.md contains `@references/business-financial-disclaimer.md` in required_reading block
    - workflows/brief.md contains "Business intent detection" or "Business Intent Detection"
    - workflows/brief.md contains "BUSINESS_SIGNAL_COUNT >= 3" or equivalent threshold logic
    - workflows/brief.md contains "solo_founder" AND "startup_team" AND "product_leader" track names
    - workflows/brief.md contains "## Domain Strategy" section template
    - workflows/brief.md contains "Naming Direction" in Domain Strategy context
    - workflows/brief.md contains "Brand Positioning Seeds" in Domain Strategy context
    - workflows/brief.md contains `manifest-set-top-level businessMode` command
    - workflows/brief.md contains `manifest-set-top-level businessTrack` command
    - workflows/brief.md contains `\$[0-9]` or `$[0-9]` dollar amount verification pattern
    - workflows/brief.md contains "--force" handling for track prompt
    - All BRIEF-01, BRIEF-02, BRIEF-05, BRIEF-07 tests in test-brief-detection.cjs pass (GREEN state)
  </acceptance_criteria>
  <done>brief.md contains business intent detection in Step 4 with 5-category signal taxonomy and 3+/2+ threshold, interactive track selection prompt with --force/--dry-run/--quick flag handling, Domain Strategy conditional section in Step 5, businessMode/businessTrack manifest writes in Step 7, and financial placeholder enforcement. All detection tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
node --test .planning/phases/85-brief-extensions-detection/tests/test-brief-detection.cjs
```
All BRIEF-01, BRIEF-02, BRIEF-05, BRIEF-07 structural tests pass.

Verify required_reading block:
```bash
grep -c "@references/business" workflows/brief.md
```
Returns 3 (business-track.md, launch-frameworks.md, business-financial-disclaimer.md).

Verify no dollar amounts in workflow template content:
```bash
grep -c '\[YOUR_' workflows/brief.md
```
Returns at least 1 (Domain Strategy placeholder).
</verification>

<success_criteria>
- brief.md has business intent detection logic with 5-category signal taxonomy
- brief.md has track selection prompt with all 3 tracks and flag handling
- brief.md has conditional Domain Strategy section in BRF output
- brief.md has businessMode and businessTrack manifest writes in Step 7
- brief.md has financial placeholder enforcement per BRIEF-07
- All structural tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/85-brief-extensions-detection/85-01-SUMMARY.md`
</output>
