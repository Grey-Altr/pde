---
phase: 123-context-sync-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/editor-sync.md
  - workflows/editor-sync.md
  - tests/phase-123/test-editor-sync-command.cjs
autonomous: true
requirements:
  - CTX-07
must_haves:
  truths:
    - "Running /pde:editor-sync regenerates all 6 editor context targets (AGENTS.md, .cursor/rules/*.mdc, .cursorrules, GEMINI.md hierarchy, .agent/skills/, DESIGN.md)"
    - "The command displays which files were written and the source hash"
    - "The command delegates to pde-tools.cjs context-sync which calls emitAll(cwd)"
  artifacts:
    - path: "commands/editor-sync.md"
      provides: "/pde:editor-sync command definition with allowed-tools"
      contains: "pde:editor-sync"
    - path: "workflows/editor-sync.md"
      provides: "Workflow logic that calls context-sync and displays results"
      contains: "context-sync"
    - path: "tests/phase-123/test-editor-sync-command.cjs"
      provides: "Tests verifying command file structure and workflow integration"
      min_lines: 30
  key_links:
    - from: "commands/editor-sync.md"
      to: "workflows/editor-sync.md"
      via: "@workflows/editor-sync.md reference"
      pattern: "workflows/editor-sync"
    - from: "workflows/editor-sync.md"
      to: "bin/pde-tools.cjs"
      via: "node pde-tools.cjs context-sync"
      pattern: "context-sync"
---

<objective>
Create the /pde:editor-sync command and workflow for on-demand regeneration of all editor context files.

Purpose: CTX-07 requires a manual command so users can force context file regeneration outside pipeline execution.
Output: commands/editor-sync.md + workflows/editor-sync.md following the check-divergence delegation pattern, with tests.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-context-sync-engine/123-RESEARCH.md

<interfaces>
<!-- Reference patterns from check-divergence command/workflow pair -->

From commands/check-divergence.md:
```markdown
---
name: pde:check-divergence
description: Detect drift between handoff specs and implemented components
argument-hint: '[--verbose]'
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
---
<objective>
Execute the /pde:check-divergence command.
</objective>
<process>
Follow @workflows/check-divergence.md exactly.
Pass all of $ARGUMENTS to the workflow.
</process>
```

From workflows/check-divergence.md (pattern -- calls pde-tools.cjs, parses JSON, displays results):
```markdown
## 1. Run Divergence Detection
node --input-type=module <<'EOF'
import { createRequire } from 'module';
const req = createRequire(import.meta.url);
const d = req(`${process.env.CLAUDE_PLUGIN_ROOT}/bin/lib/divergence.cjs`);
const r = d.runDivergenceCheck(process.cwd());
// ... process and display ...
EOF
```

From bin/pde-tools.cjs (context-sync command already exists):
```javascript
case 'context-sync': {
  const contextSync = require('./lib/context-sync.cjs');
  contextSync.cmdContextSync(cwd, args.slice(1), raw);
  break;
}
```

From bin/lib/context-sync.cjs emitAll return shape:
```javascript
// Returns: { agentsMd, cursorRules, cursorrules, geminiMd, antigravitySkill, designMd, sourceHash, generatedAt }
// Each emitter result: { written: true/false, path: string, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create editor-sync command and workflow</name>
  <files>commands/editor-sync.md, workflows/editor-sync.md</files>
  <action>
    Create commands/editor-sync.md following the exact check-divergence.md pattern:
    - name: pde:editor-sync
    - description: Regenerate all editor context files from current .planning/ state
    - argument-hint: '[--editor cursor|gemini|agents|antigravity|all]'
    - allowed-tools: Read, Write, Bash, Glob
    - objective: Execute the /pde:editor-sync command
    - process: Follow @workflows/editor-sync.md exactly. Pass all of $ARGUMENTS to the workflow.

    Create workflows/editor-sync.md with process steps:

    1. Parse Arguments: Extract optional --editor flag (default: all). Valid values: cursor, gemini, agents, antigravity, all.

    2. Run Context Sync: Execute via Bash:
    ```bash
    node --input-type=module <<'EOF'
    import { createRequire } from 'module';
    const req = createRequire(import.meta.url);
    const cs = req(`${process.env.CLAUDE_PLUGIN_ROOT}/bin/lib/context-sync.cjs`);
    const result = cs.emitAll(process.cwd());
    process.stdout.write(JSON.stringify(result) + '\n');
    EOF
    ```
    If --editor is not 'all', pass the flag through to pde-tools.cjs context-sync --editor {value} instead.

    3. Display Results: Parse the JSON output. Display a table showing each target file and whether it was written:
    ```
    Editor Context Sync

      Target               Status    Path
      ─────────────────────────────────────────────────
      AGENTS.md            written   ./AGENTS.md
      Cursor Rules         written   ./.cursor/rules/*.mdc
      .cursorrules         written   ./.cursorrules
      GEMINI.md            written   ./GEMINI.md (+ 2 more)
      Antigravity Skill    written   ./.agent/skills/pde-design/SKILL.md
      DESIGN.md            written   ./DESIGN.md
    ```

    Then display:
    ```
    Source hash: {sourceHash}
    Generated at: {generatedAt}
    All editor context files are up to date.
    ```

    4. Error handling: If the command fails, display:
    ```
    Error: Could not sync editor context files. Ensure .planning/ directory exists with PROJECT.md.
    ```
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const cmd = fs.readFileSync('commands/editor-sync.md', 'utf-8'); const wf = fs.readFileSync('workflows/editor-sync.md', 'utf-8'); if (!cmd.includes('pde:editor-sync')) throw new Error('Missing command name'); if (!cmd.includes('workflows/editor-sync')) throw new Error('Missing workflow ref'); if (!wf.includes('context-sync')) throw new Error('Missing context-sync call'); console.log('Command and workflow files valid')"</automated>
  </verify>
  <done>commands/editor-sync.md exists with pde:editor-sync name, delegates to workflow. workflows/editor-sync.md calls context-sync.cjs emitAll and displays formatted results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests for editor-sync command structure and context-sync integration</name>
  <files>tests/phase-123/test-editor-sync-command.cjs</files>
  <behavior>
    - Test: commands/editor-sync.md exists and contains 'pde:editor-sync' in name frontmatter
    - Test: commands/editor-sync.md references workflows/editor-sync.md in process
    - Test: workflows/editor-sync.md exists and contains 'context-sync' reference
    - Test: workflows/editor-sync.md contains 'emitAll' or 'context-sync' call
    - Test: pde-tools.cjs context-sync command runs without error on a minimal .planning/ fixture (calls cmdContextSync)
    - Test: emitAll returns all 6 expected keys (agentsMd, cursorRules, cursorrules, geminiMd, antigravitySkill, designMd)
  </behavior>
  <action>
    Create tests/phase-123/test-editor-sync-command.cjs using node:test:

    1. Structural tests: Read command and workflow files, verify expected content (command name, workflow reference, context-sync call).

    2. Integration test: Create a temp directory with minimal .planning/ fixtures (PROJECT.md with basic content, empty STATE.md). Call emitAll(tmpDir) directly from context-sync.cjs. Verify the returned object has all 6 editor target keys plus sourceHash and generatedAt.

    3. Idempotency test: Call emitAll(tmpDir) twice. Verify sourceHash is identical both times (same input = same hash).

    Use temp directories with cleanup. Follow the CJS test format established in phase 118-122 tests.
  </action>
  <verify>
    <automated>node --test tests/phase-123/test-editor-sync-command.cjs</automated>
  </verify>
  <done>All 6 test behaviors pass. Command structure validated. emitAll integration confirmed with all 6 targets. Idempotency verified via hash comparison.</done>
</task>

</tasks>

<verification>
1. `node --test tests/phase-123/test-editor-sync-command.cjs` -- all command/workflow tests pass
2. `node --test tests/phase-118/test-context-sync.cjs tests/phase-119/test-antigravity-stitch.cjs tests/phase-120/test-artifact-format.cjs tests/phase-121/test-mcp-server.cjs tests/phase-122/test-divergence.cjs` -- zero regressions
</verification>

<success_criteria>
- commands/editor-sync.md exists with proper frontmatter and workflow delegation
- workflows/editor-sync.md exists with context-sync call and formatted output
- All command/workflow tests pass
- All prior phase tests pass (zero regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/123-context-sync-engine/123-02-SUMMARY.md`
</output>
