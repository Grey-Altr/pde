---
phase: 52-agent-enhancements
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bin/lib/memory.cjs
  - tests/phase-52/memory.test.mjs
autonomous: true
requirements:
  - AGNT-04
  - AGNT-05

must_haves:
  truths:
    - "readMemories returns empty array when no memories.md exists"
    - "appendMemory creates memories.md with header and entry when file does not exist"
    - "appendMemory enforces 50-entry cap by archiving oldest entries before appending"
    - "splitEntries correctly parses ### heading-delimited entries"
    - "Archive file is created with date-based naming when cap exceeded"
    - "Memory entries include timestamp, phase context, and relevance tags"
  artifacts:
    - path: "bin/lib/memory.cjs"
      provides: "Agent memory CRUD + archival library"
      exports: ["ensureMemoryDir", "readMemories", "appendMemory", "splitEntries", "MEMORY_DIR", "MAX_ENTRIES"]
    - path: "tests/phase-52/memory.test.mjs"
      provides: "Unit tests for memory.cjs"
      min_lines: 80
  key_links:
    - from: "bin/lib/memory.cjs"
      to: ".planning/agent-memory/{agent-type}/memories.md"
      via: "fs read/write operations"
      pattern: "readFileSync|writeFileSync"
---

<objective>
TDD: Build the agent memory library (memory.cjs) with full test coverage for read, append, cap enforcement, and archival.

Purpose: Provides the core memory CRUD operations that all agent spawn points will use in Plan 04. Following tracking.cjs patterns.
Output: bin/lib/memory.cjs with exports, tests/phase-52/memory.test.mjs passing
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-agent-enhancements/52-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From bin/lib/core.cjs (used by all lib modules):
```javascript
function output(data) { /* writes JSON to stdout */ }
function error(msg) { /* writes error JSON to stdout, exits 1 */ }
module.exports = { output, error };
```

From bin/lib/tracking.cjs (pattern reference for markdown-based state):
```javascript
function parseStatusTable(content) { /* regex parse of markdown table rows */ }
function initWorkflowStatus(tasksDir, tasks) { /* creates/reinits workflow-status.md */ }
module.exports = { initWorkflowStatus, setTaskStatus, readWorkflowStatus, generateHandoff, parseStatusTable };
```
</interfaces>

<acceptance_criteria>
- AC-1: Given memory.cjs is required, When ensureMemoryDir is called with cwd and agentType "executor", Then .planning/agent-memory/executor/ directory exists
- AC-2: Given no memories.md exists, When readMemories is called, Then it returns { entries: [], path: "...", exists: false }
- AC-3: Given an empty agent-memory directory, When appendMemory is called with a valid entry, Then memories.md is created with a header line and the entry
- AC-4: Given memories.md has exactly 50 entries, When appendMemory is called, Then the oldest entry is archived to archive-YYYYMMDD.md and the new entry is appended (total stays at 50)
- AC-5: Given memories.md has 50 entries, When appendMemory runs, Then archive file contains the removed entries with heading "# {agentType} Memory Archive"
- AC-6: Given a memories.md with mixed entries, When splitEntries is called, Then each "### " heading starts a new entry and all entries are returned
</acceptance_criteria>

<tasks>

<feature>
  <name>Agent Memory Library (TDD)</name>
  <files>bin/lib/memory.cjs, tests/phase-52/memory.test.mjs</files>
  <read_first>
    - bin/lib/tracking.cjs (pattern reference: markdown state file CRUD, parseStatusTable, fs operations)
    - bin/lib/core.cjs (output/error helpers used by all lib modules)
    - tests/phase-51/workflow-status.test.mjs (test pattern reference: node:test, createRequire, tmp dirs)
  </read_first>
  <behavior>
    - Test: ensureMemoryDir creates nested directory .planning/agent-memory/{agentType}/
    - Test: readMemories returns { entries: [], path, exists: false } when no file
    - Test: readMemories returns parsed entries when file exists with 3 sample entries
    - Test: splitEntries splits on "### " headings, filters empty parts
    - Test: appendMemory creates file with header when memories.md missing
    - Test: appendMemory appends entry to existing file
    - Test: appendMemory archives oldest entries when at 50-entry cap (test with exactly 50)
    - Test: appendMemory archives correct count when over cap (test with 52 entries, append 1 = archive 3)
    - Test: archive file created with date-based name and header
    - Test: archive appends to existing archive file on same day
    - Test: MAX_ENTRIES equals 50
    - Test: MEMORY_DIR equals '.planning/agent-memory'
  </behavior>
  <implementation>
    Create bin/lib/memory.cjs implementing:

    Constants:
    - MEMORY_DIR = '.planning/agent-memory'
    - MAX_ENTRIES = 50
    - ENTRY_HEADING_RE = /^### .+$/gm

    Functions:
    1. ensureMemoryDir(cwd, agentType) — fs.mkdirSync with { recursive: true }, returns dir path
    2. splitEntries(content) — content.split(/(?=^### )/gm).filter(p => p.trim()), returns string[]
    3. readMemories(cwd, agentType) — returns { entries: string[], path: string, exists: boolean }
    4. appendMemory(cwd, agentType, entry) — enforces cap: if entries.length >= MAX_ENTRIES, splice oldest (entries.length - MAX_ENTRIES + 1) to archive file named `archive-${YYYYMMDD}.md`, append new entry, write header + remaining + new. Returns { appended: true, archived: number }

    Header format for memories.md:
    ```
    # {agentType} Agent Memory\n\n> Loaded at agent spawn. Append-only. Max 50 entries.\n> Oldest entries archived automatically.\n\n
    ```

    Archive file header: `# {agentType} Memory Archive — {YYYYMMDD}\n\n`
    Archive appends to existing archive file if same-day archive exists.

    Module exports: { ensureMemoryDir, readMemories, appendMemory, splitEntries, MEMORY_DIR, MAX_ENTRIES }

    Test file uses:
    - import { createRequire } from 'module'; const require = createRequire(import.meta.url);
    - import { describe, it, beforeEach, afterEach } from 'node:test';
    - import assert from 'node:assert/strict';
    - fs.mkdtempSync for isolated tmp dirs per test
    - fs.rmSync(tmpDir, { recursive: true }) in afterEach
  </implementation>
  <verify>
    <automated>node --test tests/phase-52/memory.test.mjs</automated>
  </verify>
  <done>
    - All 12 test cases pass
    - memory.cjs exports ensureMemoryDir, readMemories, appendMemory, splitEntries, MEMORY_DIR, MAX_ENTRIES
    - 50-entry cap enforcement verified with exact boundary tests
    - Archive file creation verified
  </done>
</feature>

</tasks>

<verification>
- `node --test tests/phase-52/memory.test.mjs` exits 0 with all tests passing
- `node -e "const m = require('./bin/lib/memory.cjs'); console.log(Object.keys(m))"` outputs ensureMemoryDir, readMemories, appendMemory, splitEntries, MEMORY_DIR, MAX_ENTRIES
- `grep "MAX_ENTRIES = 50" bin/lib/memory.cjs` finds the constant
</verification>

<success_criteria>
- memory.cjs exists with all 6 exports
- All unit tests pass (12 test cases covering read, append, cap, archive)
- TDD commits: failing tests first, then implementation
</success_criteria>

<output>
After completion, create `.planning/phases/52-agent-enhancements/52-01-SUMMARY.md`
</output>
