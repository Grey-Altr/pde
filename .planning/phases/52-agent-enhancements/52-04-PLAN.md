---
phase: 52-agent-enhancements
plan: 04
type: execute
wave: 2
depends_on:
  - 52-01
  - 52-02
files_modified:
  - workflows/execute-phase.md
  - workflows/plan-phase.md
  - workflows/diagnose-issues.md
  - workflows/verify-phase.md
  - bin/lib/init.cjs
autonomous: true
requirements:
  - AGNT-04

must_haves:
  truths:
    - "Executor subagent spawn includes .planning/agent-memory/executor/memories.md in files_to_read"
    - "Planner subagent spawn includes .planning/agent-memory/planner/memories.md in files_to_read"
    - "Debugger subagent spawn includes .planning/agent-memory/debugger/memories.md in files_to_read"
    - "Verifier subagent spawn includes .planning/agent-memory/verifier/memories.md in files_to_read"
    - "All four agent spawn prompts include <memory_instructions> block for post-task append"
    - "Memory writes are serialized by orchestrator post-wave (not concurrent per-agent)"
  artifacts:
    - path: "workflows/execute-phase.md"
      provides: "Executor memory load + save instructions"
      contains: "agent-memory/executor/memories.md"
    - path: "workflows/plan-phase.md"
      provides: "Planner memory load + save instructions"
      contains: "agent-memory/planner/memories.md"
    - path: "workflows/diagnose-issues.md"
      provides: "Debugger memory load + save instructions"
      contains: "agent-memory/debugger/memories.md"
    - path: "workflows/verify-phase.md"
      provides: "Verifier memory load + save instructions"
      contains: "agent-memory/verifier/memories.md"
  key_links:
    - from: "workflows/execute-phase.md"
      to: ".planning/agent-memory/executor/memories.md"
      via: "files_to_read + memory_instructions in Task() spawn"
      pattern: "agent-memory/executor"
    - from: "workflows/plan-phase.md"
      to: ".planning/agent-memory/planner/memories.md"
      via: "files_to_read + memory_instructions in Task() spawn"
      pattern: "agent-memory/planner"
---

<objective>
Wire agent memory load/save into all four core agent spawn points: executor (execute-phase.md), planner (plan-phase.md), debugger (diagnose-issues.md), verifier (verify-phase.md). Also ensure memory directory creation in init.cjs.

Purpose: Agents load cross-session memory at spawn and write new operational patterns on completion, building project-specific knowledge over time.
Output: Updated 4 workflow files with memory injection + updated init.cjs
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-agent-enhancements/52-RESEARCH.md
@.planning/phases/52-agent-enhancements/52-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01: memory.cjs exports -->
From bin/lib/memory.cjs (created in Plan 01):
```javascript
const MEMORY_DIR = '.planning/agent-memory';
const MAX_ENTRIES = 50;
function ensureMemoryDir(cwd, agentType) { /* creates dir, returns path */ }
function readMemories(cwd, agentType) { /* returns { entries, path, exists } */ }
function appendMemory(cwd, agentType, entry) { /* cap + archive + append */ }
function splitEntries(content) { /* splits on ### headings */ }
module.exports = { ensureMemoryDir, readMemories, appendMemory, splitEntries, MEMORY_DIR, MAX_ENTRIES };
```

<!-- From execute-phase.md: current executor Task() spawn (lines 246-279) -->
Current executor files_to_read block:
```markdown
<files_to_read>
Read these files at execution start using the Read tool:
- .planning/project-context.md (Project context — compact project baseline, if exists)
- {task_file_path} (Task {task_num} of {task_total} — self-contained task instructions)
- .planning/STATE.md (State — position and recent decisions)
- .planning/config.json (Config, if exists)
- ./CLAUDE.md (Project instructions, if exists)
- .claude/skills/ or .agents/skills/ (Project skills, if either exists)
</files_to_read>
```

<!-- From plan-phase.md Step 8: planner spawn files_to_read (lines 362-374) -->
Current planner files_to_read block:
```markdown
<files_to_read>
- .planning/project-context.md (Project context — compact project baseline, if exists)
- {state_path} (Project State)
- {roadmap_path} (Roadmap)
- {requirements_path} (Requirements)
- {context_path} (USER DECISIONS)
- {research_path} (Technical Research)
- {verification_path} (Verification Gaps)
- {uat_path} (UAT Gaps)
- {UI_SPEC_PATH} (UI Design Contract)
</files_to_read>
```

<!-- From diagnose-issues.md: debugger spawn (line 82-83) -->
Current debugger spawn:
```markdown
prompt=filled_debug_subagent_prompt + "\n\n<files_to_read>\n- {phase_dir}/{phase_num}-UAT.md\n- .planning/STATE.md\n</files_to_read>",
subagent_type="pde-debugger",
```

<!-- From execute-phase.md: verifier spawn (lines 596-610) -->
Current verifier spawn:
```markdown
Task(
  prompt="Verify phase {phase_number} goal achievement...",
  subagent_type="pde-verifier",
  files_to_read=["{phase_dir}/*-RECONCILIATION.md ..."]
)
```
</interfaces>

<acceptance_criteria>
- AC-1: Given execute-phase.md executor Task() spawn, When files_to_read is listed, Then ".planning/agent-memory/executor/memories.md (Agent memory — operational patterns, if exists)" appears in the list
- AC-2: Given execute-phase.md executor Task() spawn, When the prompt is built, Then a <memory_instructions> block follows the </files_to_read> or </success_criteria> tag
- AC-3: Given plan-phase.md Step 8 planner Task() spawn, When files_to_read is listed, Then ".planning/agent-memory/planner/memories.md (Agent memory, if exists)" appears
- AC-4: Given plan-phase.md Step 8 planner Task() spawn, When the prompt is built, Then a <memory_instructions> block is included
- AC-5: Given diagnose-issues.md debugger Task() spawn, When files_to_read is built, Then ".planning/agent-memory/debugger/memories.md (Agent memory, if exists)" appears
- AC-6: Given verify-phase.md or execute-phase.md verifier Task() spawn, When files_to_read is built, Then ".planning/agent-memory/verifier/memories.md (Agent memory, if exists)" appears
- AC-7: Given all four workflow files, When <memory_instructions> blocks are checked, Then each contains: entry format "### {ISO timestamp} | Phase {N} | tags: {csv}", "1-3 sentences", "append only", and the specific memories.md path for that agent type
- AC-8: Given execute-phase.md, When post-wave processing runs, Then a comment/instruction exists to serialize memory writes (not concurrent)
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Inject memory into executor and verifier spawns in execute-phase.md</name>
  <files>workflows/execute-phase.md</files>
  <read_first>
    - workflows/execute-phase.md (full file — find both executor Task() spawns at ~line 246 and ~line 329, and verifier spawn at ~line 605)
    - bin/lib/memory.cjs (memory module from Plan 01 — understand ensureMemoryDir and entry format)
  </read_first>
  <action>
    **1. Executor spawn — sharded mode (Mode A, ~line 246):**

    In the `<files_to_read>` block of the Mode A executor Task() spawn, add this line after the `.claude/skills/` line:
    ```
    - .planning/agent-memory/executor/memories.md (Agent memory — operational patterns learned in prior sessions, if exists)
    ```

    After the `</success_criteria>` closing tag (and before the closing `"` of the prompt), add:
    ```markdown
    <memory_instructions>
    After completing your task, append a memory entry to .planning/agent-memory/executor/memories.md (create file if missing, with header: "# executor Agent Memory\n\n> Loaded at agent spawn. Append-only. Max 50 entries.\n> Oldest entries archived automatically.\n\n").
    Entry format — one entry per task, append at end of file:
    ### {ISO timestamp} | Phase {phase_number} | tags: {2-4 comma-separated relevance tags}
    {1-3 sentences capturing an operational pattern learned during this task.}
    Good entries: project conventions, file patterns, gotchas encountered, tool behaviors.
    Bad entries: "task completed successfully", status updates, full context dumps.
    </memory_instructions>
    ```

    **2. Executor spawn — inline mode (Mode B, ~line 329):**

    Apply the same two additions (files_to_read line + memory_instructions block) to the Mode B executor Task() spawn.

    **3. Verifier spawn (~line 605):**

    In the verifier Task() spawn, add to files_to_read:
    ```
    ".planning/agent-memory/verifier/memories.md (Agent memory, if exists)"
    ```

    After the prompt's main instructions, add:
    ```markdown
    <memory_instructions>
    After completing verification, append a memory entry to .planning/agent-memory/verifier/memories.md (create file if missing, with header).
    Entry format: ### {ISO timestamp} | Phase {phase_number} | tags: {tags}
    {1-3 sentences on verification patterns, common gaps found, project-specific quality signals.}
    </memory_instructions>
    ```

    **4. Post-wave memory serialization comment:**

    After the wave completion handling (where plan results are collected), add a comment block:
    ```markdown
    <!-- Memory write serialization: Agent memory writes happen within each subagent's
         own execution context. Since waves run plans sequentially within a wave
         (parallelization=false default) or in parallel, and each agent type writes to
         its own memories.md file (executor/memories.md, verifier/memories.md),
         there is no write conflict. If future changes allow multiple executors to run
         in parallel within the same wave, orchestrator should collect memory entries
         and serialize writes post-wave using:
         node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" memory append {agentType} {entry}
    -->
    ```
  </action>
  <ac_refs>AC-1, AC-2, AC-6, AC-7, AC-8</ac_refs>
  <verify>
    <automated>grep -q "agent-memory/executor/memories.md" workflows/execute-phase.md && grep -q "agent-memory/verifier/memories.md" workflows/execute-phase.md && grep -q "memory_instructions" workflows/execute-phase.md && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - workflows/execute-phase.md contains "agent-memory/executor/memories.md" in at least 2 files_to_read blocks (Mode A and Mode B)
    - workflows/execute-phase.md contains "agent-memory/verifier/memories.md" in verifier spawn
    - workflows/execute-phase.md contains "<memory_instructions>" at least 3 times (2 executor + 1 verifier)
    - workflows/execute-phase.md memory_instructions blocks contain "### {ISO timestamp} | Phase" format
    - workflows/execute-phase.md contains "1-3 sentences" guidance in memory_instructions
    - workflows/execute-phase.md contains memory serialization comment
  </acceptance_criteria>
  <done>
    Executor (both modes) and verifier spawns in execute-phase.md include memory file in files_to_read and memory_instructions block; serialization comment present
  </done>
</task>

<task type="auto">
  <name>Task 2: Inject memory into planner, debugger spawns and update init.cjs</name>
  <files>workflows/plan-phase.md, workflows/diagnose-issues.md, workflows/verify-phase.md, bin/lib/init.cjs</files>
  <read_first>
    - workflows/plan-phase.md (full file — find planner Task() spawn at Step 8, ~line 349-490)
    - workflows/diagnose-issues.md (full file — find debugger Task() spawn at ~line 82)
    - workflows/verify-phase.md (full file — check if it has its own subagent spawn or is itself the subagent)
    - bin/lib/init.cjs (understand init JSON structure — add memory_dir path)
  </read_first>
  <action>
    **1. Planner spawn in plan-phase.md Step 8 (~line 362):**

    In the `<files_to_read>` block of the planner Task() spawn prompt, add after the UI_SPEC_PATH line:
    ```
    - .planning/agent-memory/planner/memories.md (Agent memory — planning patterns from prior phases, if exists)
    ```

    After the `</quality_gate>` closing tag (end of planner prompt, before the closing quote), add:
    ```markdown
    <memory_instructions>
    After completing the plan, append a memory entry to .planning/agent-memory/planner/memories.md (create file if missing, with header: "# planner Agent Memory\n\n> Loaded at agent spawn. Append-only. Max 50 entries.\n> Oldest entries archived automatically.\n\n").
    Entry format:
    ### {ISO timestamp} | Phase {phase_number} | tags: {2-4 relevance tags}
    {1-3 sentences on planning patterns: task sizing insights, dependency patterns, scope decisions, what worked/didn't.}
    </memory_instructions>
    ```

    **2. Debugger spawn in diagnose-issues.md (~line 82):**

    The debugger spawn currently has an inline files_to_read string:
    ```
    prompt=filled_debug_subagent_prompt + "\n\n<files_to_read>\n- {phase_dir}/{phase_num}-UAT.md\n- .planning/STATE.md\n</files_to_read>"
    ```

    Add the memory file to the files_to_read:
    ```
    prompt=filled_debug_subagent_prompt + "\n\n<files_to_read>\n- {phase_dir}/{phase_num}-UAT.md\n- .planning/STATE.md\n- .planning/agent-memory/debugger/memories.md (Agent memory — debugging patterns, if exists)\n</files_to_read>\n\n<memory_instructions>\nAfter completing diagnosis, append a memory entry to .planning/agent-memory/debugger/memories.md (create file if missing, with header).\nEntry format: ### {ISO timestamp} | Phase {phase_number} | tags: {tags}\n{1-3 sentences on debugging patterns: common root causes, diagnostic approaches that worked, project-specific failure modes.}\n</memory_instructions>"
    ```

    **3. Verify-phase.md context check:**

    verify-phase.md is executed BY the verifier subagent (not an orchestrator that spawns one). The verifier is spawned from execute-phase.md (handled in Task 1). However, verify-phase.md's `<required_reading>` or `load_context` step should acknowledge that memories.md may be available:

    In the `load_context` step, after loading phase context, add:
    ```markdown
    **Agent memory (if available):**
    If `.planning/agent-memory/verifier/memories.md` was provided in files_to_read at spawn, it is already loaded. Use any relevant patterns from prior verification sessions to inform this verification. Do NOT re-read the file — it was loaded at spawn.
    ```

    **4. Update bin/lib/init.cjs:**

    In the init function for `execute-phase` and `plan-phase` commands, add a `memory_dir` field to the JSON output:
    ```javascript
    memory_dir: '.planning/agent-memory'
    ```

    This allows orchestrators to reference the memory directory path from init context. Add it alongside existing path fields (like `phase_dir`, `state_path`, etc.).

    Also add an `ensure_memory_dirs` step: before returning init JSON for execute-phase, create the four agent memory directories if they don't exist:
    ```javascript
    const memoryTypes = ['executor', 'planner', 'debugger', 'verifier'];
    for (const type of memoryTypes) {
      const dir = path.join(cwd, '.planning', 'agent-memory', type);
      fs.mkdirSync(dir, { recursive: true });
    }
    ```

    This ensures directories exist before any agent tries to read memories.md.
  </action>
  <ac_refs>AC-3, AC-4, AC-5, AC-6, AC-7</ac_refs>
  <verify>
    <automated>grep -q "agent-memory/planner/memories.md" workflows/plan-phase.md && grep -q "agent-memory/debugger/memories.md" workflows/diagnose-issues.md && grep -q "agent-memory/verifier" workflows/verify-phase.md && grep -q "memory_dir" bin/lib/init.cjs && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - workflows/plan-phase.md contains "agent-memory/planner/memories.md" in planner files_to_read
    - workflows/plan-phase.md contains "<memory_instructions>" block in planner spawn prompt
    - workflows/diagnose-issues.md contains "agent-memory/debugger/memories.md" in debugger files_to_read
    - workflows/diagnose-issues.md contains "memory_instructions" in debugger prompt
    - workflows/verify-phase.md contains "agent-memory/verifier" acknowledgment in load_context
    - bin/lib/init.cjs contains "memory_dir" field in output JSON
    - bin/lib/init.cjs contains "agent-memory" directory creation for all 4 types: executor, planner, debugger, verifier
  </acceptance_criteria>
  <done>
    Planner and debugger spawns include memory in files_to_read with memory_instructions; verify-phase acknowledges memory; init.cjs creates memory directories and exposes memory_dir path
  </done>
</task>

</tasks>

<verification>
- `grep -c "agent-memory" workflows/execute-phase.md` returns >= 4 (executor x2, verifier x1, comment x1)
- `grep -c "agent-memory" workflows/plan-phase.md` returns >= 2 (files_to_read + instructions)
- `grep -q "agent-memory/debugger" workflows/diagnose-issues.md` — debugger memory wired
- `grep -q "agent-memory/verifier" workflows/verify-phase.md` — verifier acknowledgment present
- `grep -q "memory_dir" bin/lib/init.cjs` — init exposes memory path
- `grep -q "ensure\|mkdirSync.*agent-memory\|memoryTypes" bin/lib/init.cjs` — directories created on init
</verification>

<success_criteria>
- All 4 agent types (executor, planner, debugger, verifier) have memory file in their spawn's files_to_read
- All 4 agent spawn prompts include memory_instructions with entry format, tag guidance, and path
- init.cjs creates .planning/agent-memory/{type}/ directories for all 4 types
- init.cjs exposes memory_dir in output JSON
- Memory write instructions specify append-only, 1-3 sentences, ISO timestamp format
</success_criteria>

<output>
After completion, create `.planning/phases/52-agent-enhancements/52-04-SUMMARY.md`
</output>
