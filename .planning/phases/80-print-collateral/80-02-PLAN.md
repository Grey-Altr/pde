---
phase: 80-print-collateral
plan: 02
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - tests/phase-80/print-collateral.test.mjs
  - workflows/wireframe.md
autonomous: false
requirements: [PRNT-03]

must_haves:
  truths:
    - "wireframe.md experience branch generates a PRG festival program HTML artifact for multi-day sub-type events"
    - "The festival program contains a schedule grid, artist bio section, venue map placeholder, and sponsors section"
    - "PRG artifact is registered in design-manifest.json with manifest-update PRG commands"
    - "PRG generation is gated on experienceSubType === multi-day only"
  artifacts:
    - path: "tests/phase-80/print-collateral.test.mjs"
      provides: "Extended Nyquist tests covering PRNT-03 festival program"
      contains: "PRNT-03"
    - path: "workflows/wireframe.md"
      provides: "PRG multi-page festival program generation block"
      contains: "PRG"
  key_links:
    - from: "workflows/wireframe.md"
      to: "design-manifest.json"
      via: "manifest-update PRG artifact registration"
      pattern: "manifest-update PRG"
    - from: "workflows/wireframe.md"
      to: "temporal flow artifact"
      via: "schedule grid populated from temporal flow data"
      pattern: "temporal.*flow|schedule.*grid"
---

<objective>
Add festival program (PRG) multi-page HTML generation to the wireframe.md experience branch for multi-day sub-type events, with schedule grid, artist bios, venue map, and sponsors sections.

Purpose: Multi-day experience products (festivals) need a dense multi-page program artifact that is distinct from single-page flyers. This completes the PRNT-03 requirement.
Output: Updated wireframe.md with PRG generation block; extended Nyquist tests covering PRNT-03; visual verification checkpoint.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-print-collateral/80-RESEARCH.md
@.planning/phases/80-print-collateral/80-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — FLY/SIT blocks already in wireframe.md -->
<!-- PRG generation goes in the same Step 4g section, after SIT generation -->
<!-- PRG file write goes in Step 5b after FLY/SIT writes -->
<!-- PRG manifest registration goes in Step 7c after FLY/SIT registration -->

<!-- Pattern from Plan 01 — format variant CSS and @page approach -->
<!-- PRG uses @page { size: A4 landscape } for program pages (landscape for schedule grid legibility) -->
<!-- PRG uses CSS named-page @page rules for multi-page sections -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PRNT-03 tests and PRG generation block to wireframe.md</name>
  <files>tests/phase-80/print-collateral.test.mjs, workflows/wireframe.md</files>
  <read_first>
    - tests/phase-80/print-collateral.test.mjs (current state from Plan 01 — understand existing test structure, add new describe block)
    - workflows/wireframe.md (current state from Plan 01 — locate Step 4g print collateral section, Step 5b file writes, Step 7c manifest registration)
    - .planning/phases/80-print-collateral/80-RESEARCH.md (multi-page @page pattern, festival program structure, sub-type gate recommendations)
  </read_first>
  <action>
**Part A: Extend test file with PRNT-03 tests**

Add a new `describe('PRNT-03: PRG festival program — wireframe.md experience branch')` block to `tests/phase-80/print-collateral.test.mjs` with these tests:

1. `wireframe.md contains PRG artifact generation block` — assert `content.includes('PRG')`
2. `wireframe.md gates PRG generation on multi-day sub-type` — assert `content.includes('multi-day')` AND `content.includes('PRG')`
3. `wireframe.md PRG contains schedule grid section` — assert `content.includes('schedule') && content.includes('grid')`
4. `wireframe.md PRG contains artist bio section` — assert `content.includes('artist') && (content.includes('bio') || content.includes('lineup'))`
5. `wireframe.md PRG contains venue map placeholder` — assert `content.includes('venue') && content.includes('map')`
6. `wireframe.md PRG contains sponsors section` — assert `content.includes('sponsor')`
7. `wireframe.md contains PRG manifest registration` — assert `content.includes('manifest-update PRG')`

**Part B: Add PRG generation block to wireframe.md**

In the Step 4g section (added in Plan 01), after the SIT generation block, add the PRG generation block:

**Gate:** `IF experienceSubType !== "multi-day": SKIP PRG generation. Log: "PRG skipped — experienceSubType is {value}, not multi-day."`

Generate a multi-page self-contained HTML file for the festival program. The PRG artifact is an A4 landscape composition reference with these sections:

**Page 1: Cover**
- Festival name (from brief), dates, venue
- Hero visual area (placeholder box with gradient from brand tokens)
- Same prepress disclaimer as FLY: `<aside class="prepress-disclaimer">COMPOSITION REFERENCE -- NOT A PRODUCTION PRINT FILE...</aside>`

**Page 2: Schedule Grid**
- CSS `@page schedule { size: A4 landscape; margin: 10mm; }` with `.schedule-page { page: schedule; }`
- HTML table with columns: Time, Stage/Area 1, Stage/Area 2, Stage/Area 3 (expandable based on venue zones from spaces-inventory.json if available)
- Alternating row backgrounds for readability
- Time slots in 30-min increments
- Comment: `<!-- Schedule data populated from temporal flow artifact if available; placeholder grid if absent -->`
- Composition annotation: schedule grid uses monospace font for time column, max 2 typefaces total (body + accent), clear row separation

**Page 3: Artist/Lineup Bios**
- Grid layout: 2-3 columns of artist cards
- Each card: photo placeholder (gray box), artist name, genre tag, set time reference, 2-line bio
- Comment: `<!-- Artist data populated from brief lineup field if available; placeholder cards if absent -->`

**Page 4: Venue Map**
- Full-page venue map placeholder (references FLP floor plan artifact if available)
- Comment: `<!-- Venue map references FLP floor plan. If FLP absent, generates placeholder schematic with zone labels from spaces-inventory.json -->`
- Includes legend for zone colors, facilities icons, emergency exits

**Page 5: Sponsors & Info**
- Sponsor tier grid: headline sponsors (large logos), supporting sponsors (medium), community partners (small)
- Essential info block: address, transport, accessibility, contact, social media handles
- Comment: `<!-- Sponsor data is placeholder — populated from brief or manual entry -->`

**Multi-page CSS:**
```css
@page { size: A4 portrait; margin: 15mm; }
@page schedule { size: A4 landscape; margin: 10mm; }
@page cover { size: A4 portrait; margin: 0; }

.page { page-break-after: always; min-height: 100%; position: relative; }
.page:last-child { page-break-after: auto; }
.cover-page { page: cover; }
.schedule-page { page: schedule; }

@media print {
  .prepress-disclaimer, .page-nav { display: none !important; }
}
```

**Page navigation (screen mode only):**
Add a sticky top bar with page links (Cover, Schedule, Lineup, Map, Sponsors) for screen browsing. Hidden on print via `@media print`.

**Composition annotations (PRNT-04 compliance):**
```html
<!-- COMPOSITION: festival-program
  Awwwards-level composition rules:
  - Schedule grid: maximum 2 typefaces (mono for times, sans-serif for content)
  - Artist cards: consistent card dimensions, 8px border-radius, subtle shadow
  - Negative space: 15mm margins on all pages, 8-12px gap between grid items
  - Color hierarchy: brand primary for headings, neutral for body, accent for callouts
  - Legible at A4: minimum 10pt body text, minimum 8pt table cell text
-->
```

**File write in Step 5b:**
Add after FLY/SIT writes:
```
- PRG (if generated): `.planning/design/physical/print/PRG-festival-program-v1.html`
```

**Manifest registration in Step 7c:**
Add after FLY/SIT registration:
```bash
# PRG artifact (only if experienceSubType === "multi-day")
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update PRG code PRG
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update PRG name "Festival Program"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update PRG type print-collateral
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update PRG domain physical
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update PRG path ".planning/design/physical/print/PRG-festival-program-v1.html"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update PRG status draft
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update PRG version 1
```
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-80/print-collateral.test.mjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - workflows/wireframe.md contains string `PRG` (artifact code)
    - workflows/wireframe.md contains string `multi-day` near `PRG`
    - workflows/wireframe.md contains strings `schedule` and `grid`
    - workflows/wireframe.md contains strings `artist` and either `bio` or `lineup`
    - workflows/wireframe.md contains strings `venue` and `map`
    - workflows/wireframe.md contains string `sponsor`
    - workflows/wireframe.md contains string `manifest-update PRG`
    - workflows/wireframe.md contains `@page schedule` (named page rule for landscape schedule)
    - tests/phase-80/print-collateral.test.mjs contains `describe('PRNT-03`
    - All tests in print-collateral.test.mjs pass (including original 16 from Plan 01 + 7 new PRNT-03 tests)
    - Regression: `node --test tests/phase-74/experience-regression.test.mjs` exits 0
  </acceptance_criteria>
  <done>wireframe.md generates PRG festival program for multi-day events with schedule grid, artist bios, venue map, sponsors; all 23 Nyquist tests green; regression tests unbroken</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify print collateral workflow completeness</name>
  <files>workflows/wireframe.md, tests/phase-80/print-collateral.test.mjs</files>
  <action>
Human verifies the complete print collateral workflow added across Plans 01 and 02. Run the automated test suite first, then inspect wireframe.md for structural correctness of all three print artifact generation blocks (FLY, SIT, PRG).
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-80/print-collateral.test.mjs && node --test tests/phase-74/experience-regression.test.mjs</automated>
  </verify>
  <what-built>
    Complete print collateral generation in wireframe.md experience branch:
    - FLY event flyer with CSS @page A5, bleed/safe zones, CMYK table, format switcher, prepress disclaimer
    - SIT series identity template with {{variable}} slots (recurring-series only)
    - PRG festival program with schedule grid, artist bios, venue map, sponsors (multi-day only)
    - All three artifacts registered in manifest (FLY, SIT, PRG)
    - 23 Nyquist structural tests passing
  </what-built>
  <how-to-verify>
    1. Run full test suite: `node --test tests/phase-80/print-collateral.test.mjs` — all 23 tests should pass
    2. Run regression: `node --test tests/phase-74/experience-regression.test.mjs` — should pass
    3. Open `workflows/wireframe.md` and search for "Step 4g" — verify the print collateral section exists with FLY, SIT, PRG generation blocks
    4. Search for "manifest-update FLY" — verify manifest registration for all three artifact codes
    5. Search for "COMPOSITION REFERENCE" — verify prepress disclaimer is present
    6. Search for "recurring-series" near "SIT" — verify sub-type gate
    7. Search for "multi-day" near "PRG" — verify sub-type gate
    8. Verify no occurrence of `@page { bleed` (anti-pattern that no browser supports)
  </how-to-verify>
  <acceptance_criteria>
    - All 23 Nyquist tests pass via `node --test tests/phase-80/print-collateral.test.mjs`
    - Phase 74 regression tests pass via `node --test tests/phase-74/experience-regression.test.mjs`
    - User confirms workflow structure is correct
  </acceptance_criteria>
  <done>Human approved that all print collateral blocks (FLY, SIT, PRG) are correctly structured in wireframe.md with proper sub-type gates, composition annotations, and manifest registration</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `node --test tests/phase-80/print-collateral.test.mjs` — all 23 tests pass
2. `node --test tests/phase-74/experience-regression.test.mjs` — regression tests green
3. `grep -c 'PRG' workflows/wireframe.md` returns >= 5
4. `grep 'multi-day' workflows/wireframe.md | grep -c 'PRG'` returns >= 1
5. `grep 'manifest-update PRG' workflows/wireframe.md` returns >= 1
6. `grep '@page schedule' workflows/wireframe.md` returns >= 1
</verification>

<success_criteria>
- PRG festival program generation block in wireframe.md gated on multi-day sub-type
- Schedule grid with time/stage columns, artist bios, venue map placeholder, sponsors section
- Multi-page CSS with named @page rules (cover, schedule)
- PRG manifest registration in physical domain
- All 23 Nyquist tests green (16 from Plan 01 + 7 from Plan 02)
- Phase 74 regression tests unbroken
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/80-print-collateral/80-02-SUMMARY.md`
</output>
