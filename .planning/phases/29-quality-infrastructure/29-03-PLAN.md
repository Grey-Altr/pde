---
phase: 29-quality-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - protected-files.json
  - bin/lib/model-profiles.cjs
  - references/model-profiles.md
  - skill-registry.md
autonomous: true
requirements:
  - QUAL-04
  - QUAL-05
  - QUAL-06
must_haves:
  truths:
    - "protected-files.json exists at repo root with protected, protected_directories, allowed_write_directories, enforcement: prompt, and explanatory note"
    - "protected-files.json lists quality-standards.md, skill-style-guide.md, tooling-patterns.md, workflows/improve.md, bin/ directory, .claude/ directory, and protected-files.json itself"
    - "bin/lib/model-profiles.cjs MODEL_PROFILES object contains all 4 new agent entries: pde-output-quality-auditor, pde-skill-linter, pde-design-quality-evaluator, pde-template-auditor"
    - "references/model-profiles.md table contains all 4 new agent rows (in sync with cjs file)"
    - "skill-registry.md contains AUD, IMP, and PRT rows with correct workflow paths and status: pending"
  artifacts:
    - path: "protected-files.json"
      provides: "Protected file list for fleet agents"
      contains: "enforcement"
    - path: "bin/lib/model-profiles.cjs"
      provides: "Model profile entries for 4 new agent types"
      contains: "pde-output-quality-auditor"
    - path: "skill-registry.md"
      provides: "Skill code registrations for AUD, IMP, PRT"
      contains: "AUD"
  key_links:
    - from: "protected-files.json"
      to: "All fleet agent prompts (Phases 30+)"
      via: "Explicit prompt constraint: check protected-files.json before every Write or Edit call"
      pattern: "\"enforcement\": \"prompt\""
    - from: "bin/lib/model-profiles.cjs"
      to: "bin/lib/init.cjs (model resolution)"
      via: "MODEL_PROFILES object export"
      pattern: "pde-output-quality-auditor|pde-design-quality-evaluator"
---

<objective>
Create `protected-files.json` and register new agent types in model-profiles and skill-registry.

Three tasks delivering the infrastructure wiring for Phase 29:
1. `protected-files.json` — defines which files fleet agents must not modify (QUAL-04)
2. 4 new entries in `bin/lib/model-profiles.cjs` + `references/model-profiles.md` (QUAL-05)
3. 3 new rows in `skill-registry.md` for AUD, IMP, PRT skill codes (QUAL-06)

Purpose: These registrations and guards must exist before Phase 30 fleet agents are built. Enforcement is prompt-level (bwrap sandbox does not apply to Write/Edit tools — see 29-RESEARCH.md Pitfall 4).

Output: Three files modified/created with no runtime code changes — pure configuration and data registration.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-quality-infrastructure/29-RESEARCH.md

<interfaces>
<!-- Current MODEL_PROFILES object in bin/lib/model-profiles.cjs (lines 9-25): -->
```javascript
const MODEL_PROFILES = {
  'pde-planner': { quality: 'opus', balanced: 'opus', budget: 'sonnet' },
  'pde-roadmapper': { quality: 'opus', balanced: 'sonnet', budget: 'sonnet' },
  'pde-executor': { quality: 'opus', balanced: 'sonnet', budget: 'sonnet' },
  'pde-phase-researcher': { quality: 'opus', balanced: 'sonnet', budget: 'haiku' },
  'pde-project-researcher': { quality: 'opus', balanced: 'sonnet', budget: 'haiku' },
  'pde-research-synthesizer': { quality: 'sonnet', balanced: 'sonnet', budget: 'haiku' },
  'pde-debugger': { quality: 'opus', balanced: 'sonnet', budget: 'sonnet' },
  'pde-codebase-mapper': { quality: 'sonnet', balanced: 'haiku', budget: 'haiku' },
  'pde-verifier': { quality: 'sonnet', balanced: 'sonnet', budget: 'haiku' },
  'pde-plan-checker': { quality: 'sonnet', balanced: 'sonnet', budget: 'haiku' },
  'pde-integration-checker': { quality: 'sonnet', balanced: 'sonnet', budget: 'haiku' },
  'pde-nyquist-auditor': { quality: 'sonnet', balanced: 'sonnet', budget: 'haiku' },
  'pde-ui-researcher': { quality: 'opus', balanced: 'sonnet', budget: 'haiku' },
  'pde-ui-checker': { quality: 'sonnet', balanced: 'sonnet', budget: 'haiku' },
  'pde-ui-auditor': { quality: 'sonnet', balanced: 'sonnet', budget: 'haiku' },
};
```

<!-- New entries to add (from 29-RESEARCH.md Pattern 3): -->
'pde-output-quality-auditor': { quality: 'sonnet', balanced: 'sonnet', budget: 'haiku' }
'pde-skill-linter':           { quality: 'sonnet', balanced: 'haiku',  budget: 'haiku' }
'pde-design-quality-evaluator': { quality: 'opus', balanced: 'sonnet', budget: 'sonnet' }
'pde-template-auditor':       { quality: 'sonnet', balanced: 'haiku',  budget: 'haiku' }

<!-- Current skill-registry.md last row: -->
| IDT | /pde:ideate | workflows/ideate.md | strategy | active |

<!-- New rows to add (from 29-RESEARCH.md Pattern 4): -->
| AUD | /pde:audit | workflows/audit.md | tooling | pending |
| IMP | /pde:improve | workflows/improve.md | tooling | pending |
| PRT | /pde:pressure-test | workflows/pressure-test.md | tooling | pending |

<!-- protected-files.json format (from 29-RESEARCH.md Pattern 2): -->
Full JSON structure with: protected[], protected_directories[], allowed_write_directories[], enforcement, note
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create protected-files.json at repo root</name>
  <files>protected-files.json</files>
  <read_first>
    - .planning/phases/29-quality-infrastructure/29-RESEARCH.md (Pattern 2, Pitfall 4 — enforcement must be prompt-level)
  </read_first>
  <action>
Create `protected-files.json` at the repository root (same level as CLAUDE.md, package.json, skill-registry.md).

Write this exact JSON content:
```json
{
  "protected": [
    "references/quality-standards.md",
    "references/skill-style-guide.md",
    "references/tooling-patterns.md",
    "references/model-profiles.md",
    "workflows/improve.md",
    "bin/pde-tools.cjs",
    "bin/lib/model-profiles.cjs",
    "bin/lib/core.cjs",
    "bin/lib/init.cjs",
    "bin/lib/state.cjs",
    "bin/lib/phase.cjs",
    "bin/lib/roadmap.cjs",
    "protected-files.json",
    "CLAUDE.md",
    ".claude/settings.json",
    "skill-registry.md"
  ],
  "protected_directories": [
    "bin/",
    ".claude/"
  ],
  "allowed_write_directories": [
    ".planning/improvements/",
    ".planning/audit-report.md",
    ".planning/skill-builder-log.md",
    ".planning/pressure-test-report.md",
    "commands/",
    "workflows/"
  ],
  "enforcement": "prompt",
  "note": "Claude Code Write and Edit tools run via fs.writeFileSync and bypass bwrap filesystem sandbox. Enforcement is via explicit agent system prompt instructions only — NOT OS-level protection. All fleet agent prompts (auditor, improver, validator, skill-builder) MUST include: 'Before every Write or Edit tool call, check protected-files.json. You MUST NOT write to any path listed in protected[] or any subdirectory listed in protected_directories[].' This file itself is protected — agents cannot modify it."
}
```

Rationale for each protected entry:
- quality-standards.md: Circular evaluation prevention — the rubric must not be modified by agents it evaluates
- skill-style-guide.md, tooling-patterns.md: Format stability — generated skills must conform to these; if the standard changes, it must be a human decision
- model-profiles.md: Infrastructure stability — model routing must not change during fleet operation
- workflows/improve.md: Self-modification prevention — the improvement workflow cannot self-modify
- bin/ scripts: Runtime infrastructure — no fleet agent should modify the tools running the fleet
- protected-files.json itself: Meta-protection — agents cannot weaken their own constraints
- CLAUDE.md: Project instructions — must remain stable
- skill-registry.md: Uniqueness invariant — LINT-010 depends on this; registry changes must be intentional
  </action>
  <verify>
    <automated>node -e "const f = JSON.parse(require('fs').readFileSync('/Users/greyaltaer/code/projects/Platform Development Engine/protected-files.json')); console.log(f.enforcement, f.protected.length, f.allowed_write_directories.length)"</automated>
  </verify>
  <done>
    - protected-files.json exists at repo root (not in a subdirectory)
    - JSON is valid (node -e JSON.parse succeeds)
    - Contains "enforcement": "prompt"
    - protected array has >= 10 entries including quality-standards.md, bin/lib/model-profiles.cjs, and protected-files.json itself
    - allowed_write_directories includes .planning/improvements/ and commands/
    - note field explains prompt-enforcement limitation
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register 4 new agent types in model-profiles.cjs and references/model-profiles.md</name>
  <files>bin/lib/model-profiles.cjs, references/model-profiles.md</files>
  <read_first>
    - bin/lib/model-profiles.cjs (current MODEL_PROFILES object — lines 1-68)
    - references/model-profiles.md (current table — must stay in sync)
    - .planning/phases/29-quality-infrastructure/29-RESEARCH.md (Pattern 3 — exact entry values)
  </read_first>
  <action>
**Step 1 — Update bin/lib/model-profiles.cjs:**

In the MODEL_PROFILES object (after the existing `'pde-ui-auditor'` entry, before the closing `};`), add exactly these four entries:

```javascript
  'pde-output-quality-auditor':   { quality: 'sonnet', balanced: 'sonnet', budget: 'haiku' },
  'pde-skill-linter':             { quality: 'sonnet', balanced: 'haiku',  budget: 'haiku' },
  'pde-design-quality-evaluator': { quality: 'opus',   balanced: 'sonnet', budget: 'sonnet' },
  'pde-template-auditor':         { quality: 'sonnet', balanced: 'haiku',  budget: 'haiku' },
```

The resulting MODEL_PROFILES object must contain 19 entries (15 existing + 4 new).

**Step 2 — Update references/model-profiles.md:**

In the Profile Definitions table (which currently ends with `pde-nyquist-auditor`), add four new rows after the existing entries. NOTE: references/model-profiles.md currently only has 12 rows (pde-planner through pde-nyquist-auditor) — it is missing the pde-ui-researcher, pde-ui-checker, pde-ui-auditor rows that exist in the .cjs file. Add ALL missing rows so the table is fully in sync.

Add these rows (to bring the table in sync with the .cjs file):
```markdown
| pde-ui-researcher | opus | sonnet | haiku | inherit |
| pde-ui-checker | sonnet | sonnet | haiku | inherit |
| pde-ui-auditor | sonnet | sonnet | haiku | inherit |
| pde-output-quality-auditor | sonnet | sonnet | haiku | inherit |
| pde-skill-linter | sonnet | haiku | haiku | inherit |
| pde-design-quality-evaluator | opus | sonnet | sonnet | inherit |
| pde-template-auditor | sonnet | haiku | haiku | inherit |
```

The table should end with these 7 new rows after `pde-nyquist-auditor`.
  </action>
  <verify>
    <automated>grep "pde-output-quality-auditor" "/Users/greyaltaer/code/projects/Platform Development Engine/bin/lib/model-profiles.cjs" && grep "pde-design-quality-evaluator" "/Users/greyaltaer/code/projects/Platform Development Engine/bin/lib/model-profiles.cjs" && grep "pde-skill-linter" "/Users/greyaltaer/code/projects/Platform Development Engine/bin/lib/model-profiles.cjs" && grep "pde-template-auditor" "/Users/greyaltaer/code/projects/Platform Development Engine/bin/lib/model-profiles.cjs" && node -e "const m = require('/Users/greyaltaer/code/projects/Platform Development Engine/bin/lib/model-profiles.cjs'); console.log(Object.keys(m.MODEL_PROFILES).length)"</automated>
  </verify>
  <done>
    - bin/lib/model-profiles.cjs contains all 4 new entries with correct model values
    - Node.js require succeeds (no syntax errors)
    - Object.keys(MODEL_PROFILES).length returns 19
    - references/model-profiles.md table contains all 4 new agent rows
    - pde-design-quality-evaluator has quality: 'opus' (most expensive — design judgment task)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Register AUD, IMP, PRT skill codes in skill-registry.md</name>
  <files>skill-registry.md</files>
  <read_first>
    - skill-registry.md (current content — full file, verify last row before appending)
    - .planning/phases/29-quality-infrastructure/29-RESEARCH.md (Pattern 4 — exact row format)
  </read_first>
  <action>
Append three new rows to the skill-registry.md table. The current last row is:
`| IDT | /pde:ideate | workflows/ideate.md | strategy | active |`

Add these three rows after IDT:
```markdown
| AUD | /pde:audit | workflows/audit.md | tooling | pending |
| IMP | /pde:improve | workflows/improve.md | tooling | pending |
| PRT | /pde:pressure-test | workflows/pressure-test.md | tooling | pending |
```

Status is `pending` (not `active`) because:
- workflows/audit.md does not exist yet (Phase 30)
- workflows/improve.md does not exist yet (Phase 31)
- workflows/pressure-test.md does not exist yet (Phase 37)

The codes must be registered now so LINT-010 can enforce uniqueness from Phase 29 onward. Registering with `pending` status does not trigger workflow-path-existence validation (pending entries are excluded from that check).

Do NOT change any existing rows. Do NOT add any other rows. Verify the table has 17 rows total (14 existing + 3 new) after the edit.
  </action>
  <verify>
    <automated>grep "| AUD |" "/Users/greyaltaer/code/projects/Platform Development Engine/skill-registry.md" && grep "| IMP |" "/Users/greyaltaer/code/projects/Platform Development Engine/skill-registry.md" && grep "| PRT |" "/Users/greyaltaer/code/projects/Platform Development Engine/skill-registry.md" && grep -c "^|" "/Users/greyaltaer/code/projects/Platform Development Engine/skill-registry.md"</automated>
  </verify>
  <done>
    - skill-registry.md contains row for AUD with workflow: workflows/audit.md, status: pending
    - skill-registry.md contains row for IMP with workflow: workflows/improve.md, status: pending
    - skill-registry.md contains row for PRT with workflow: workflows/pressure-test.md, status: pending
    - No existing rows were modified
    - Table row count matches expected total (header + separator + 17 data rows)
  </done>
</task>

</tasks>

<verification>
Run after plan completes:

```bash
# protected-files.json
node -e "const f = JSON.parse(require('fs').readFileSync('/Users/greyaltaer/code/projects/Platform Development Engine/protected-files.json')); console.log('enforcement:', f.enforcement, '| protected count:', f.protected.length)"

# model-profiles.cjs
node -e "const m = require('/Users/greyaltaer/code/projects/Platform Development Engine/bin/lib/model-profiles.cjs'); console.log('total agents:', Object.keys(m.MODEL_PROFILES).length)"
grep "pde-design-quality-evaluator" "/Users/greyaltaer/code/projects/Platform Development Engine/bin/lib/model-profiles.cjs"

# skill-registry.md
grep "| AUD |" "/Users/greyaltaer/code/projects/Platform Development Engine/skill-registry.md"
grep "| IMP |" "/Users/greyaltaer/code/projects/Platform Development Engine/skill-registry.md"
grep "| PRT |" "/Users/greyaltaer/code/projects/Platform Development Engine/skill-registry.md"
```

Expected outputs:
- enforcement: prompt | protected count: >= 10
- total agents: 19
- pde-design-quality-evaluator line present with opus/sonnet/sonnet
- Three skill registry rows present
</verification>

<success_criteria>
- protected-files.json exists at repo root, JSON-valid, enforcement: prompt, note explains bwrap limitation
- bin/lib/model-profiles.cjs has 19 entries (4 new agent types added without breaking existing entries)
- references/model-profiles.md table is in sync with .cjs (all 19 agents documented)
- skill-registry.md has AUD/IMP/PRT rows with correct workflow paths and status: pending
- No existing entries in any file were modified or deleted
</success_criteria>

<output>
After completion, create `.planning/phases/29-quality-infrastructure/29-03-SUMMARY.md`
</output>
