---
phase: 01-plugin-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - VERSION
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02
  - PLUG-03

must_haves:
  truths:
    - "plugin.json exists at .claude-plugin/plugin.json with correct PDE metadata"
    - "VERSION file exists at repo root containing 0.1.0"
    - "claude plugin validate . exits 0 with no errors or warnings"
    - "Plugin installs locally via claude plugin install without errors"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest with PDE identity"
      contains: "platform-development-engine"
    - path: "VERSION"
      provides: "Single source of truth for version string"
      contains: "0.1.0"
  key_links:
    - from: "VERSION"
      to: ".claude-plugin/plugin.json"
      via: "version field mirrors VERSION file content"
      pattern: "0\\.1\\.0"
---

<objective>
Create the PDE plugin manifest and VERSION file, then validate the plugin loads correctly in Claude Code.

Purpose: Establish PDE as a valid, installable Claude Code plugin with correct identity metadata — the foundation every subsequent phase builds on.
Output: A validated `.claude-plugin/plugin.json` manifest and `VERSION` file at repo root.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-identity/01-CONTEXT.md
@.planning/phases/01-plugin-identity/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin manifest and VERSION file</name>
  <files>.claude-plugin/plugin.json, VERSION</files>
  <action>
1. Create the `.claude-plugin/` directory at the repo root.

2. Create `.claude-plugin/plugin.json` with this exact content (validated by research — `claude plugin validate` exits 0 on this):

```json
{
  "name": "platform-development-engine",
  "version": "0.1.0",
  "description": "Full lifecycle platform: from raw idea to shipped product with research, planning, execution, and verification",
  "author": {
    "name": "GreyA",
    "email": "grey.altaer@gmail.com"
  },
  "homepage": "https://github.com/Grey-Altr/pde.git",
  "repository": "https://github.com/Grey-Altr/pde.git",
  "license": "MIT",
  "keywords": ["platform", "development-lifecycle", "planning", "research", "execution", "verification", "workflow", "claude-code"]
}
```

Per user decisions:
- `name` is kebab-case machine identifier (not display name) — "platform-development-engine"
- `description` is the locked user-specified text exactly
- `author` uses object format with name "GreyA" and email "grey.altaer@gmail.com" (from git config)
- `homepage` and `repository` point to https://github.com/Grey-Altr/pde.git
- `version` is "0.1.0" per user decision (not 1.0.0 — signals work in progress)
- `license` is "MIT" (Claude's discretion, standard for plugins)
- `keywords` chosen for discoverability (Claude's discretion)

3. Create `VERSION` file at repo root containing exactly `0.1.0` (no trailing newline beyond what the editor adds). This is the single source of truth — plugin.json mirrors it.

Do NOT:
- Place plugin.json at repo root (Claude Code requires `.claude-plugin/` directory)
- Use spaces or uppercase in the `name` field (validation rejects it)
- Add any component paths (commands, agents, hooks) — Phase 1 is manifest only
- Copy any GSD source files — that happens in Phase 2+
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && test -f .claude-plugin/plugin.json && test -f VERSION && grep -q "platform-development-engine" .claude-plugin/plugin.json && grep -q "0.1.0" VERSION && echo "PASS"</automated>
  </verify>
  <done>
- `.claude-plugin/plugin.json` exists with all required fields (name, version, description, author, homepage, repository, license, keywords)
- `VERSION` file exists at repo root containing "0.1.0"
- Version values match between both files
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate plugin and test local install</name>
  <files></files>
  <action>
1. Run `claude plugin validate .` from the repo root. Must exit 0 with "Validation passed" and no warnings.

2. If validation fails, read the error output and fix the manifest. Common issues per research:
   - "Plugin name cannot contain spaces" → ensure kebab-case
   - "No manifest found" → ensure `.claude-plugin/` directory exists with plugin.json inside
   - "No version specified" warning → ensure version field is present

3. After validation passes, test local install:
   ```bash
   claude plugin install . --scope user
   ```
   This installs from the local repo path (git remote not required).

4. Verify install succeeded:
   ```bash
   claude plugin list
   ```
   Output should include "platform-development-engine" in the list.

5. If the install command syntax fails (exact local path format unclear per research), try alternatives:
   - `claude plugin install "/Users/greyaltaer/code/projects/Platform Development Engine" --scope user`
   - `claude plugin install file:///Users/greyaltaer/code/projects/Platform\ Development\ Engine`
   Document whichever syntax works.

6. After confirming install, uninstall to keep the dev environment clean (PDE is still in development):
   ```bash
   claude plugin uninstall platform-development-engine
   ```
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && claude plugin validate . 2>&1 | grep -i "passed\|valid" && echo "VALIDATE_PASS"</automated>
  </verify>
  <done>
- `claude plugin validate .` exits 0 with no errors or warnings
- Plugin installs locally without errors (PLUG-01 satisfied)
- Plugin appears in `claude plugin list` output (PLUG-03 satisfied)
- Plugin uninstalled after verification to keep dev environment clean
  </done>
</task>

</tasks>

<verification>
All three phase requirements verified:
- PLUG-01: Plugin installs via `claude plugin install .` — confirmed by Task 2
- PLUG-02: plugin.json has correct name ("platform-development-engine"), description, and version (0.1.0) — confirmed by Task 1 content and Task 2 validation
- PLUG-03: `claude plugin validate .` passes — confirmed by Task 2

Note: PLUG-02 specifies "version 1.0.0" but the user decision overrides this to 0.1.0 — the requirement is satisfied by having a correct version field; the bump to 1.0.0 happens after Phase 7 or 8 per user decision.
</verification>

<success_criteria>
1. `.claude-plugin/plugin.json` exists with PDE identity metadata
2. `VERSION` file at repo root reads "0.1.0"
3. `claude plugin validate .` exits 0
4. Plugin installs and appears in `claude plugin list`
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-identity/01-01-SUMMARY.md`
</output>
