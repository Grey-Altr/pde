---
phase: 134-relay-protocol-transport
plan: 03
type: execute
wave: 3
depends_on: [134-01, 134-02]
files_modified:
  - hooks/start-relay.cjs
  - hooks/stop-relay.cjs
  - hooks/hooks.json
  - bin/lib/relay.cjs
  - tests/phase-134/test-relay-hooks.cjs
  - tests/phase-134/test-relay-e2e.cjs
autonomous: true
requirements: [RLY-04, RLY-05]

must_haves:
  truths:
    - "With PDE_REMOTE unset, no relay process spawns and no network calls are made"
    - "With PDE_REMOTE set, relay daemon spawns as detached process on SessionStart and stops on SessionEnd"
    - "A broken relay endpoint causes zero impact on PDE session execution"
    - "Relay startup failure in hook is silently swallowed — hook exits 0"
    - "Events written during a PDE session appear at the remote ingest endpoint within 5 seconds"
  artifacts:
    - path: "hooks/start-relay.cjs"
      provides: "SessionStart hook that spawns relay daemon if PDE_REMOTE is set"
      contains: "PDE_REMOTE"
    - path: "hooks/stop-relay.cjs"
      provides: "SessionEnd hook that kills relay daemon via PID file"
      contains: "pde-relay"
    - path: "hooks/hooks.json"
      provides: "Updated hooks config with start-relay and stop-relay entries"
      contains: "start-relay.cjs"
    - path: "bin/lib/relay.cjs"
      provides: "Daemon-mode block added (require.main === module)"
      contains: "require.main === module"
    - path: "tests/phase-134/test-relay-hooks.cjs"
      provides: "Unit tests for env gate and hook behavior"
      contains: "PDE_REMOTE"
    - path: "tests/phase-134/test-relay-e2e.cjs"
      provides: "Integration test: NDJSON file -> relay -> mock HTTP server"
      contains: "createServer"
  key_links:
    - from: "hooks/start-relay.cjs"
      to: "bin/lib/relay.cjs"
      via: "spawn of relay process"
      pattern: "relay.cjs"
    - from: "hooks/hooks.json"
      to: "hooks/start-relay.cjs"
      via: "SessionStart hook entry"
      pattern: "start-relay.cjs"
    - from: "hooks/hooks.json"
      to: "hooks/stop-relay.cjs"
      via: "SessionEnd hook entry"
      pattern: "stop-relay.cjs"
---

<objective>
Wire the relay daemon into PDE's hook system with PDE_REMOTE env gate and zero-impact error isolation. Prove end-to-end flow with an integration test that writes NDJSON, runs the relay, and verifies events arrive at a mock HTTP endpoint.

Purpose: Complete the relay's integration with PDE (RLY-04 env gate, RLY-05 zero-impact) and prove the full pipeline works.
Output: Hook scripts, updated hooks.json, passing integration and unit tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-relay-protocol-transport/134-RESEARCH.md
@hooks/hooks.json
@hooks/emit-event.cjs
@bin/lib/event-bus.cjs

@.planning/phases/134-relay-protocol-transport/134-01-SUMMARY.md
@.planning/phases/134-relay-protocol-transport/134-02-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 output: bin/lib/relay-protocol.cjs -->
Expected exports:
  - WireEnvelopeSchema: zod schema for wire envelope validation
  - createEnvelope(sessionId, pdeEvent): returns envelope object with seq, session_id, machine_id, relay_ts, approval_id + pdeEvent fields
  - resetSequence(): resets seq counter to 0

<!-- From Plan 02 output: bin/lib/relay.cjs -->
Expected exports:
  - TailCursor: class(filePath, onLine) with start(intervalMs)/stop()
  - BatchQueue: class({ maxBatchSize, flushIntervalMs, maxBufferSize, onFlush }) with push(event)/start()/stop()
  - CircuitBreaker: class({ failureThreshold, cooldownMs }) with canAttempt()/recordSuccess()/recordFailure()
  - postEvents(url, bearerToken, events): Promise
  - startRelay(sessionId, opts): returns { stop() }
  - stopRelay(): stops active relay
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hook scripts with PDE_REMOTE gate and zero-impact isolation</name>
  <files>hooks/start-relay.cjs, hooks/stop-relay.cjs, hooks/hooks.json, bin/lib/relay.cjs, tests/phase-134/test-relay-hooks.cjs</files>
  <read_first>
    hooks/hooks.json (current hook configuration — must add entries without breaking existing hooks)
    hooks/emit-event.cjs (reference for hook script pattern: reads stdin JSON, uses spawnSync, exits 0 always)
    bin/lib/event-bus.cjs (session ID functions: generateSessionId, getSessionId)
    bin/lib/relay.cjs (Plan 02 output — will append daemon-mode block to bottom)
    .planning/phases/134-relay-protocol-transport/134-RESEARCH.md (Pattern 6: daemon lifecycle, Pattern 7: zero-impact isolation)
  </read_first>
  <behavior>
    - Test 1: start-relay.cjs with PDE_REMOTE unset exits 0 without spawning any child process
    - Test 2: start-relay.cjs with PDE_REMOTE set spawns a detached child process and writes PID file to /tmp/pde-relay-{sessionId}.pid
    - Test 3: stop-relay.cjs reads PID file and sends SIGTERM, then removes PID file
    - Test 4: stop-relay.cjs with no PID file exits 0 without error
    - Test 5: start-relay.cjs catches spawn errors and exits 0 (zero-impact)
    - Test 6: start-relay.cjs with existing alive PID file does not spawn duplicate
  </behavior>
  <action>
    **Create `hooks/start-relay.cjs`:**
    ```
    #!/usr/bin/env node
    'use strict';
    ```
    - Read hook JSON from stdin (same pattern as emit-event.cjs): collect chunks, parse JSON on 'end'
    - Extract `session_id` from hook payload. The hook payload for SessionStart includes a `session_id` field in `hookData.session_id`. If not available, try to get from the NDJSON filename pattern or from the event-bus module.
    - **PDE_REMOTE gate (RLY-04):** `if (!process.env.PDE_REMOTE) process.exit(0);`
    - Parse PDE_REMOTE as the ingest URL: `const ingestUrl = process.env.PDE_REMOTE;`
    - Get bearer token: `const bearerToken = process.env.PDE_RELAY_TOKEN || '';`
    - PID file path: `path.join(os.tmpdir(), 'pde-relay-' + sessionId + '.pid')`
    - **Duplicate guard:** If PID file exists, read PID, check `process.kill(pid, 0)` in try/catch. If alive, exit 0. If dead, continue (stale PID).
    - **Spawn relay daemon:**
      Create a small inline runner script approach: spawn `process.execPath` with args `[relayScript, sessionId, ingestUrl, bearerToken]` where relayScript is `path.join(__dirname, '..', 'bin', 'lib', 'relay.cjs')`.

      CHOSEN APPROACH: Add a `if (require.main === module)` block at the bottom of `bin/lib/relay.cjs` that reads `process.argv[2]` as sessionId, `process.argv[3]` as ingestUrl, `process.argv[4]` as bearerToken, and calls startRelay. The hook spawns `node bin/lib/relay.cjs <sessionId> <ingestUrl> <bearerToken>`.

    - Spawn options: `{ detached: true, stdio: 'ignore', env: { ...process.env } }`
    - Write PID: `fs.writeFileSync(pidFile, String(child.pid), 'utf8')`
    - `child.unref()` — allow hook to exit
    - **Wrap everything in try/catch that exits 0 (RLY-05):**
      ```javascript
      try {
        // ... all spawn logic ...
      } catch {
        process.exit(0); // RLY-05: relay failure never affects PDE
      }
      ```
    - Always `process.exit(0)` at the end

    **Create `hooks/stop-relay.cjs`:**
    ```
    #!/usr/bin/env node
    'use strict';
    ```
    - Read hook JSON from stdin (same pattern)
    - Extract session_id from hook payload
    - PID file: `path.join(os.tmpdir(), 'pde-relay-' + sessionId + '.pid')`
    - Try to read PID file. If not found, exit 0.
    - Parse PID as integer. Try `process.kill(pid, 'SIGTERM')`. Catch and ignore (already dead).
    - Try to unlink PID file. Catch and ignore.
    - Always exit 0.
    - Wrap everything in try/catch that exits 0.

    **Update `hooks/hooks.json`:**
    Add start-relay.cjs to SessionStart hooks array (AFTER the existing emit-event.cjs entry, AFTER context-sync-session-start.cjs). Set `"async": true` — relay start must not block session start.

    Add stop-relay.cjs to SessionEnd hooks array (BEFORE archive-session.cjs so relay stops before session archives). Set `"async": false` — must complete before archive.

    The exact additions to the SessionStart hooks array:
    ```json
    {
      "type": "command",
      "command": "${CLAUDE_PLUGIN_ROOT}/hooks/start-relay.cjs",
      "async": true
    }
    ```

    The exact addition to SessionEnd hooks array (insert before archive-session.cjs):
    ```json
    {
      "type": "command",
      "command": "${CLAUDE_PLUGIN_ROOT}/hooks/stop-relay.cjs",
      "async": false
    }
    ```

    IMPORTANT: Preserve ALL existing hooks in hooks.json. Specifically verify these remain present after editing: emit-event.cjs, cleanup-old-sessions.cjs, context-sync-session-start.cjs, archive-session.cjs, context-sync-hook.cjs, and the Notification section entry for idle-suggestions.cjs (under idle_prompt).

    **Add daemon-mode block to `bin/lib/relay.cjs`:**
    Append the `if (require.main === module)` block to the bottom of relay.cjs (before module.exports). This enables relay.cjs to run as a standalone daemon process when spawned by the hook:
    ```javascript
    // Daemon mode: run as standalone process
    if (require.main === module) {
      const sessionId = process.argv[2];
      const ingestUrl = process.argv[3];
      const bearerToken = process.argv[4] || '';
      if (!sessionId || !ingestUrl) {
        process.exit(1);
      }
      // Zero-impact error isolation (RLY-05)
      process.on('unhandledRejection', () => {});
      process.on('uncaughtException', () => { process.exit(0); });
      startRelay(sessionId, { ingestUrl, bearerToken });
      // Keep process alive
      setInterval(() => {}, 60000);
    }
    ```

    **Create `tests/phase-134/test-relay-hooks.cjs`:**
    - Test the env gate: mock process.env without PDE_REMOTE, verify no spawn occurs
    - Test PID file write: with PDE_REMOTE set, spawn creates PID file
    - Test stop cleanup: PID file is removed after stop
    - Test stale PID: if PID file points to dead process, start spawns new one
    - Test error swallowing: if spawn throws, hook still exits 0

    For hook tests, import the hook logic by extracting testable functions or by testing the hook script via spawnSync with controlled env vars. Prefer spawnSync approach to test the actual hook behavior end-to-end:
    ```javascript
    const { spawnSync } = require('node:child_process');
    const result = spawnSync(process.execPath, [hookScript], {
      input: JSON.stringify({ hook_event_name: 'SessionStart', session_id: testSessionId }),
      env: { ...process.env, PDE_REMOTE: undefined },
      timeout: 5000,
    });
    expect(result.status).toBe(0);
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/phase-134/test-relay-hooks.cjs --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - hooks/start-relay.cjs exists and contains "#!/usr/bin/env node" and "PDE_REMOTE" and "process.exit(0)" and "detached: true" and "child.unref()"
    - hooks/start-relay.cjs contains "pde-relay-" for PID file path
    - hooks/stop-relay.cjs exists and contains "#!/usr/bin/env node" and "SIGTERM" and "process.exit(0)"
    - hooks/hooks.json contains "start-relay.cjs" in SessionStart section with "async": true
    - hooks/hooks.json contains "stop-relay.cjs" in SessionEnd section
    - hooks/hooks.json still contains all original hooks (emit-event.cjs, cleanup-old-sessions.cjs, context-sync-session-start.cjs, archive-session.cjs, context-sync-hook.cjs) and the Notification section idle_prompt entry for idle-suggestions.cjs
    - bin/lib/relay.cjs contains "require.main === module" block for daemon mode
    - bin/lib/relay.cjs contains "process.on('unhandledRejection'" and "process.on('uncaughtException'"
    - tests/phase-134/test-relay-hooks.cjs contains at least 5 "it(" test cases
    - `npx vitest run tests/phase-134/test-relay-hooks.cjs` exits 0
  </acceptance_criteria>
  <done>PDE_REMOTE env gate prevents any relay activity when unset. Hook scripts spawn/stop relay daemon. All errors silently swallowed. 6 hook tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end integration test — NDJSON to mock HTTP endpoint</name>
  <files>tests/phase-134/test-relay-e2e.cjs</files>
  <read_first>
    bin/lib/relay.cjs (startRelay signature and behavior)
    bin/lib/relay-protocol.cjs (createEnvelope, WireEnvelopeSchema)
    bin/lib/event-bus.cjs (safeAppendEvent writes JSON + '\n' to /tmp/pde-session-{id}.ndjson)
  </read_first>
  <behavior>
    - Test 1: Write 3 NDJSON events to a temp file, startRelay with mock HTTP server as endpoint, verify all 3 events arrive at mock server within 5 seconds
    - Test 2: Events received at mock server are valid JSON arrays (batched)
    - Test 3: With mock server returning 500 errors, circuit breaker opens after failureThreshold failures and stops sending requests
    - Test 4: After circuit breaker opens and cooldown elapses, relay retries (HALF_OPEN probe)
    - Test 5: Relay with invalid/unreachable URL does not crash — continues running
  </behavior>
  <action>
    **Create `tests/phase-134/test-relay-e2e.cjs`:**

    Use `node:http` to create a mock ingest server on a random port (port 0 auto-assigns).
    Use the `startRelay` function from relay.cjs directly (in-process, not spawned) for testability.

    Setup:
    ```javascript
    const http = require('node:http');
    const fs = require('node:fs');
    const os = require('node:os');
    const path = require('node:path');
    const crypto = require('node:crypto');
    const { describe, it, expect, beforeEach, afterEach } = require('vitest');
    const { startRelay } = require('../../bin/lib/relay.cjs');
    ```

    For each test:
    1. Create a unique temp NDJSON file: `/tmp/pde-session-test-{randomUUID}.ndjson`
    2. Start mock HTTP server that collects received request bodies
    3. Call `startRelay(sessionId, { ingestUrl: 'http://localhost:{port}/api/ingest', bearerToken: 'test-token', pollIntervalMs: 100, flushIntervalMs: 500, batchSize: 10, failureThreshold: 3, cooldownMs: 1000 })`
    4. Write NDJSON events using `fs.appendFileSync(ndjsonPath, JSON.stringify(event) + '\n')`
    5. Wait for events to arrive at mock server (poll with timeout)
    6. Assert received events match written events
    7. Clean up: stop relay, close server, delete temp file

    Mock server for 500-error test: respond with `res.writeHead(500); res.end()` to trigger circuit breaker.

    Use short intervals for test speed: pollIntervalMs=100, flushIntervalMs=300, cooldownMs=1000.

    Test events should be realistic PDE events:
    ```javascript
    { schema_version: '1.0', ts: new Date().toISOString(), event_type: 'session_start', extensions: {} }
    ```

    IMPORTANT: Each test must have its own sessionId and temp file to avoid cross-test interference. Use `crypto.randomUUID()` for unique IDs.

    IMPORTANT: Set test timeout to 15000ms per test (`it('...', { timeout: 15000 }, async () => { ... })`) since integration tests involve real I/O and timers.
  </action>
  <verify>
    <automated>npx vitest run tests/phase-134/test-relay-e2e.cjs --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-134/test-relay-e2e.cjs exists and contains "createServer" for mock HTTP server
    - tests/phase-134/test-relay-e2e.cjs contains "startRelay" import from relay.cjs
    - tests/phase-134/test-relay-e2e.cjs contains at least 5 "it(" test cases
    - tests/phase-134/test-relay-e2e.cjs contains "pde-session-" for temp NDJSON file creation
    - `npx vitest run tests/phase-134/test-relay-e2e.cjs` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Full pipeline proven: NDJSON file write -> TailCursor reads -> createEnvelope wraps -> WireEnvelopeSchema validates -> BatchQueue batches -> postEvents sends -> mock server receives. Circuit breaker tested under failure. No crashes on unreachable endpoint. 5 integration tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/phase-134/ --reporter=verbose` — ALL phase 134 tests pass (protocol + tail + circuit + batch + hooks + e2e)
- `grep -c "PDE_REMOTE" hooks/start-relay.cjs` returns >= 1
- `node -e "const j = require('./hooks/hooks.json'); console.log(JSON.stringify(j.hooks.SessionStart))"` includes start-relay.cjs
- `PDE_REMOTE= node -e "require('./hooks/start-relay.cjs')"` exits 0 immediately (env gate)
</verification>

<success_criteria>
- PDE_REMOTE unset = zero relay activity (RLY-04)
- Relay errors fully swallowed, hook always exits 0 (RLY-05)
- hooks.json correctly wires start/stop relay into session lifecycle
- Integration test proves full NDJSON -> envelope wrapping -> HTTP pipeline works
- Circuit breaker opens under sustained failure
- All phase 134 tests GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/134-relay-protocol-transport/134-03-SUMMARY.md`
</output>
