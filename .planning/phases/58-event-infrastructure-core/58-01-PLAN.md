---
phase: 58-event-infrastructure-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/event-bus.cjs
  - bin/lib/config.cjs
  - .planning/config.json
autonomous: true
requirements:
  - EVNT-01
  - EVNT-02
  - EVNT-05
  - EVNT-06

must_haves:
  truths:
    - "bin/lib/event-bus.cjs loads without side effects via require('./bin/lib/event-bus.cjs')"
    - "dispatch() builds an envelope with schema_version='1.0', ts, event_type, session_id, extensions={} and writes NDJSON to /tmp/pde-session-{sessionId}.ndjson"
    - "setImmediate defers the dispatch so the caller is not blocked"
    - "monitoring.enabled and monitoring.session_id are accepted keys in VALID_CONFIG_KEYS"
    - ".planning/config.json contains a monitoring section with enabled and session_id fields"
  artifacts:
    - path: "bin/lib/event-bus.cjs"
      provides: "PdeEventBus class + bus singleton + generateSessionId + getSessionId + safeAppendEvent"
      exports: ["PdeEventBus", "bus", "generateSessionId", "getSessionId", "safeAppendEvent"]
    - path: "bin/lib/config.cjs"
      provides: "VALID_CONFIG_KEYS expanded with monitoring.enabled and monitoring.session_id"
      contains: "'monitoring.enabled'"
    - path: ".planning/config.json"
      provides: "monitoring section at root"
      contains: "\"monitoring\""
  key_links:
    - from: "bin/lib/event-bus.cjs"
      to: "/tmp/pde-session-{sessionId}.ndjson"
      via: "fs.appendFileSync in safeAppendEvent"
      pattern: "appendFileSync.*pde-session"
    - from: "bin/lib/event-bus.cjs"
      to: "node:events EventEmitter"
      via: "class PdeEventBus extends EventEmitter"
      pattern: "class PdeEventBus extends EventEmitter"
---

<objective>
Create the NDJSON event bus foundation: the in-process EventEmitter wrapper, session ID management, and the NDJSON append function — plus the config scaffolding to store the session ID.

Purpose: Every downstream consumer (pde-tools event-emit subcommand, dashboard, archiver) depends on event-bus.cjs providing a stable envelope schema and an atomic-safe file write. This plan builds that foundation before anything calls it.
Output: bin/lib/event-bus.cjs (new), bin/lib/config.cjs (modified), .planning/config.json (modified)
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-event-infrastructure-core/58-RESEARCH.md
@.planning/phases/58-event-infrastructure-core/58-VALIDATION.md

<interfaces>
<!-- Existing patterns the executor must replicate -->

From bin/lib/config.cjs lines 14-23 (VALID_CONFIG_KEYS set — add monitoring keys here):
```javascript
const VALID_CONFIG_KEYS = new Set([
  'mode', 'granularity', 'parallelization', 'commit_docs', 'model_profile',
  'search_gitignored', 'brave_search',
  'task_tracker',
  'workflow.research', 'workflow.plan_check', 'workflow.verifier',
  'workflow.nyquist_validation', 'workflow.ui_phase', 'workflow.ui_safety_gate',
  'workflow._auto_chain_active',
  'git.branching_strategy', 'git.phase_branch_template', 'git.milestone_branch_template',
  'planning.commit_docs', 'planning.search_gitignored',
]);
```

From bin/lib/manifest.cjs (established crypto usage pattern):
```javascript
const { randomUUID } = require('crypto');
```

Current .planning/config.json (must add monitoring section):
```json
{
  "mode": "yolo",
  "granularity": "fine",
  "parallelization": true,
  "commit_docs": true,
  "model_profile": "balanced",
  "workflow": {
    "research": true,
    "plan_check": true,
    "verifier": true,
    "nyquist_validation": true,
    "_auto_chain_active": false
  },
  "task_tracker": "linear"
}
```

From bin/lib/config.cjs lines 140-173 (setConfigValue — call this to write monitoring.session_id):
```javascript
function setConfigValue(cwd, keyPath, parsedValue) { ... }
// Usage: setConfigValue(cwd, 'monitoring.session_id', newUUID)
```

config.cjs exports: { cmdConfigEnsureSection, cmdConfigSet, cmdConfigGet, cmdConfigSetModelProfile }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create bin/lib/event-bus.cjs — EventEmitter wrapper, envelope builder, NDJSON appender</name>
  <read_first>
    - bin/lib/config.cjs (lines 1-30 — require pattern, module structure to replicate)
    - bin/lib/core.cjs (lines 1-20 — 'use strict' and CJS header style)
    - .planning/phases/58-event-infrastructure-core/58-RESEARCH.md (Pattern 3 and Pattern 4 — exact schema and EventEmitter implementation)
  </read_first>
  <files>bin/lib/event-bus.cjs</files>
  <behavior>
    - Test: node -e "const eb = require('./bin/lib/event-bus.cjs'); console.assert(typeof eb.generateSessionId === 'function'); console.assert(typeof eb.safeAppendEvent === 'function'); console.assert(typeof eb.bus === 'object'); console.log('PASS');"
    - Test: generateSessionId() returns a UUID v4 string (format: xxxxxxxx-xxxx-4xxx-xxxx-xxxxxxxxxxxx)
    - Test: safeAppendEvent(id, envelope) writes one line to /tmp/pde-session-{id}.ndjson; JSON.parse of that line yields object with schema_version='1.0', ts string, event_type, session_id, extensions={}
    - Test: safeAppendEvent with invalid sessionId containing path traversal characters does NOT throw (swallows silently)
    - Test: dispatch() on the bus singleton does not throw; setImmediate defers so calling code continues synchronously
  </behavior>
  <action>
Create `bin/lib/event-bus.cjs` at the plugin root. File must begin with `'use strict';`. Structure:

1. Require block: const { EventEmitter } = require('events'); const { randomUUID } = require('crypto'); const fs = require('fs'); const os = require('os'); const path = require('path');

2. Module-level: let _sessionId = null;

3. Function generateSessionId(): sets _sessionId = randomUUID() and returns it.

4. Function getSessionId(): returns _sessionId.

5. Function safeAppendEvent(sessionId, envelope): builds logPath = path.join(os.tmpdir(), `pde-session-${sessionId}.ndjson`); calls fs.appendFileSync(logPath, JSON.stringify(envelope) + '\n', 'utf-8') inside try-catch that swallows all errors silently (empty catch block — no console.error, no throw).

6. Class PdeEventBus extends EventEmitter: constructor calls super() then this.setMaxListeners(20). Method dispatch(eventType, payload): calls setImmediate(() => { build envelope; this.emit(eventType, envelope); this.emit('*', envelope); safeAppendEvent(sessionId, envelope); }). Envelope construction: const sessionId = payload.session_id || _sessionId || 'unknown'; const envelope = { schema_version: '1.0', ts: new Date().toISOString(), event_type: eventType, session_id: sessionId, ...payload, extensions: payload.extensions || {} };

7. Singleton: const bus = new PdeEventBus();

8. module.exports = { PdeEventBus, bus, generateSessionId, getSessionId, safeAppendEvent };

CRITICAL: Do NOT require this file at the top of pde-tools.cjs. This module is lazy-required only inside the event-emit case block. This module itself has no top-level side effects (no file I/O on require).
  </action>
  <verify>
    <automated>node -e "const eb = require('./bin/lib/event-bus.cjs'); const id = eb.generateSessionId(); eb.safeAppendEvent(id, {schema_version:'1.0',ts:new Date().toISOString(),event_type:'test',session_id:id,extensions:{}}); const fs=require('fs'),os=require('os'),p=require('path'); const line=fs.readFileSync(p.join(os.tmpdir(),'pde-session-'+id+'.ndjson'),'utf-8').trim(); const e=JSON.parse(line); console.assert(e.schema_version==='1.0','schema_version missing'); console.assert(e.extensions!==undefined,'extensions missing'); console.assert(typeof e.ts==='string','ts missing'); console.assert(e.event_type==='test','event_type missing'); console.assert(e.session_id===id,'session_id missing'); console.log('EVNT-01 PASS');"</automated>
  </verify>
  <done>
    - bin/lib/event-bus.cjs exists
    - require('./bin/lib/event-bus.cjs') exits 0 with no output (no side effects on load)
    - safeAppendEvent writes a parseable NDJSON line with all 5 required envelope fields
    - dispatch() accepts a call without throwing (setImmediate defers correctly)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add monitoring config keys and initialize .planning/config.json monitoring section</name>
  <read_first>
    - bin/lib/config.cjs (lines 14-23 — VALID_CONFIG_KEYS set, add after 'planning.search_gitignored')
    - .planning/config.json (full file — add monitoring section while preserving all existing keys)
  </read_first>
  <files>bin/lib/config.cjs, .planning/config.json</files>
  <action>
Two edits:

EDIT 1 — bin/lib/config.cjs: Add two entries to VALID_CONFIG_KEYS after 'planning.search_gitignored':
  'monitoring.enabled',
  'monitoring.session_id',

The set should end with: '...planning.search_gitignored', 'monitoring.enabled', 'monitoring.session_id',

Do NOT add a setNestedConfigValue function — config.cjs already has setConfigValue() which handles dot notation. The session-start subcommand (Plan 02, Task 1) will call setConfigValue() directly with 'monitoring.session_id'.

EDIT 2 — .planning/config.json: Add monitoring section at root level, preserving all existing keys. Result must be valid JSON with the monitoring object:
```json
"monitoring": {
  "enabled": true,
  "session_id": null
}
```

The full config.json after edit must contain all existing keys (mode, granularity, parallelization, commit_docs, model_profile, workflow, task_tracker) AND the new monitoring section. Write the complete file — do not use partial edits.
  </action>
  <verify>
    <automated>node -e "const config = require('./bin/lib/config.cjs'); const keys = config.VALID_CONFIG_KEYS || require('fs').readFileSync('./bin/lib/config.cjs','utf-8'); const src = require('fs').readFileSync('./bin/lib/config.cjs','utf-8'); console.assert(src.includes(\"'monitoring.enabled'\"), 'monitoring.enabled missing from VALID_CONFIG_KEYS'); console.assert(src.includes(\"'monitoring.session_id'\"), 'monitoring.session_id missing from VALID_CONFIG_KEYS'); const cfg = JSON.parse(require('fs').readFileSync('./.planning/config.json','utf-8')); console.assert(cfg.monitoring !== undefined, 'monitoring section missing'); console.assert(cfg.monitoring.enabled === true, 'monitoring.enabled not true'); console.assert('session_id' in cfg.monitoring, 'monitoring.session_id key missing'); console.log('config OK');"</automated>
  </verify>
  <done>
    - bin/lib/config.cjs VALID_CONFIG_KEYS contains 'monitoring.enabled' and 'monitoring.session_id'
    - .planning/config.json contains monitoring: { enabled: true, session_id: null }
    - All existing config.json keys (mode, granularity, parallelization, commit_docs, model_profile, workflow, task_tracker) are preserved
    - node bin/pde-tools.cjs config-set monitoring.enabled true exits 0 (key is now recognized)
  </done>
</task>

</tasks>

<verification>
Run both verify commands above. Then confirm pde-tools.cjs still routes all existing commands without breakage:
node bin/pde-tools.cjs state json 2>&1 | head -3 (should output JSON, not an error)
</verification>

<success_criteria>
1. bin/lib/event-bus.cjs exists and loads without side effects
2. safeAppendEvent writes valid NDJSON with all 5 required envelope fields (EVNT-01)
3. NDJSON line is written to /tmp/pde-session-{uuid}.ndjson (EVNT-02, EVNT-06 partial — unique filename per call)
4. extensions field present in every envelope (EVNT-05)
5. monitoring.enabled and monitoring.session_id accepted by config-set (VALID_CONFIG_KEYS updated)
6. .planning/config.json has monitoring section
7. All existing pde-tools.cjs commands still work (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/58-event-infrastructure-core/58-01-SUMMARY.md`
</output>
