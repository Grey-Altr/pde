---
phase: 181-remaining-cluster-a-personas
plan: 02
type: execute
wave: 2
depends_on:
  - 181-01
files_modified:
  - bin/lib/render-presentation.cjs
  - tests/phase-181/render-presentation-cluster-a.test.mjs
autonomous: true
requirements:
  - CLU-04
  - CLU-05
must_haves:
  truths:
    - "buildClientDeliverable(ir) returns sections with feature specs, ACs met, and verification evidence"
    - "buildStakeholderStatus(ir) returns sections with deterministic RAG status computed from IR, not LLM-generated"
    - "Both builders handle unavailable IR fields gracefully via sentinelHtml()"
    - "Both slugs are registered in render() switch and personaDisplayName()"
  artifacts:
    - path: "bin/lib/render-presentation.cjs"
      provides: "buildClientDeliverable + buildStakeholderStatus functions, switch cases, display names"
      contains: "function buildClientDeliverable"
    - path: "tests/phase-181/render-presentation-cluster-a.test.mjs"
      provides: "Unit tests for CLU-04 and CLU-05"
      contains: "buildClientDeliverable"
  key_links:
    - from: "bin/lib/render-presentation.cjs"
      to: "render() switch statement"
      via: "case 'client-deliverable' and case 'stakeholder-status'"
      pattern: "case 'client-deliverable'"
---

<objective>
Add client-deliverable (CLU-04) and stakeholder-status (CLU-05) persona builders to render-presentation.cjs. Unskip and implement their test blocks in the Phase 181 test file.

Purpose: Client deliverable shows feature specs and ACs met; stakeholder status provides deterministic RAG status with blockers and risks.
Output: Two working persona builders with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/181-remaining-cluster-a-personas/181-RESEARCH.md
@.planning/phases/181-remaining-cluster-a-personas/181-01-SUMMARY.md

@bin/lib/render-presentation.cjs
@tests/phase-181/render-presentation-cluster-a.test.mjs

<interfaces>
<!-- Key patterns from render-presentation.cjs (same as plan 01, plus new helpers from 181-01) -->

From bin/lib/render-presentation.cjs (reusable helpers):
```javascript
function buildOverview(ir)      // project name, goal, core value
function buildRequirements(ir)  // requirement coverage table
function buildBlockers(ir)      // blocker + risk items
function buildDecisions(ir)     // decision items
function buildTimeline(ir)      // git velocity + cost timing
function buildArtifacts(ir)     // design artifact listing
function buildTechnical(ir)     // technical decisions list

// Sentinel + escape (REQUIRED):
function sentinelHtml(irField, label) // returns HTML or null
function escHtml(str)                 // HTML entity escaping

// Charts:
charts.effortBreakdownChart(ir)       // effort distribution SVG

// RAG status helper pattern (from RESEARCH.md):
// pct >= 75 = GREEN, 40-74 = AMBER, <40 = RED
// Must be deterministic (code, not LLM narration) per extraction-first architecture
```

From MOCK_IR fixture (tests/phase-181/):
```javascript
blockers: { items: [{ text: '...', source: '...' }] },
risks: { items: [{ text: '...', source: '...' }] },
verification: { phases_verified: 2, total_phases: 2, pct: 100 },
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add buildClientDeliverable and buildStakeholderStatus persona builders</name>
  <read_first>
    - bin/lib/render-presentation.cjs (full file — see current helpers, new helpers from 181-01, switch statement, exports)
    - .planning/phases/181-remaining-cluster-a-personas/181-RESEARCH.md (IR field reference for CLU-04 and CLU-05)
  </read_first>
  <files>bin/lib/render-presentation.cjs</files>
  <action>
Add two persona builder functions to render-presentation.cjs, placed after buildSprintReview (added in 181-01):

**1. buildClientDeliverable(ir) — CLU-04:**
Sections array:
- `{id: 'scope', title: 'Project Scope', level: 1, content: buildOverview(ir)}` — reuse existing
- `{id: 'features', title: 'Features Delivered', level: 2, content: buildRequirements(ir)}` — reuse existing (shows category breakdown)
- `{id: 'verification', title: 'Verification Evidence', level: 2, content: buildVerificationEvidence(ir)}` — NEW helper
- `{id: 'artifacts', title: 'Design Artifacts', level: 2, content: buildArtifacts(ir)}` — reuse existing
- `{id: 'effort', title: 'Effort Summary', level: 2, content: buildTimeline(ir)}` — reuse existing

NEW helper `buildVerificationEvidence(ir)`:
- sentinelHtml(ir.verification, 'Verification results') check first
- Show ir.verification.phases_verified / ir.verification.total_phases verified, pct
- Render as summary paragraph + progress indicator (e.g., "N/M phases verified (X%)")
- Use escHtml() on all IR strings

**2. buildStakeholderStatus(ir) — CLU-05:**
Sections array:
- `{id: 'rag', title: 'Status Overview', level: 1, content: buildRAGStatus(ir)}` — NEW helper
- `{id: 'focus', title: 'Current Focus', level: 2, content: buildCurrentFocus(ir)}` — NEW helper
- `{id: 'blockers', title: 'Active Blockers', level: 2, content: buildBlockers(ir)}` — reuse existing
- `{id: 'risks', title: 'Risk Register', level: 2, content: buildRiskRegister(ir)}` — NEW helper (risks only, not combined with blockers)
- `{id: 'decisions', title: 'Decisions Log', level: 2, content: buildDecisions(ir)}` — reuse existing
- `{id: 'next-actions', title: 'Next Actions', level: 2, content: buildWhatsNext(ir)}` — reuse from 181-01

NEW helper `buildRAGStatus(ir)`:
- CRITICAL: Deterministic computation, no LLM involvement (per extraction-first architecture decision)
- sentinelHtml(ir.phases, 'Phase progress') check first
- Compute pct = ir.phases.total > 0 ? Math.round((ir.phases.completed / ir.phases.total) * 100) : 0
- RAG logic: pct >= 75 = GREEN (class "pde-success"), pct >= 40 = AMBER (class "pde-warning"), else RED (class "pde-danger")
- Return HTML paragraph with colored status badge and completion stats

NEW helper `buildCurrentFocus(ir)`:
- sentinelHtml(ir.phases, 'Phase progress') check first
- Show ir.phases.current_phase_name if present, else "Current phase not specified"
- Include ir.phases.milestone_name if present

NEW helper `buildRiskRegister(ir)`:
- sentinelHtml(ir.risks, 'Risk data') check first
- Access items = Array.isArray(ir.risks && ir.risks.items) ? ir.risks.items : (Array.isArray(ir.risks) ? ir.risks : [])
- Render as HTML table: Risk | Source

**Registration (3 locations):**
1. personaDisplayName() map: add `'client-deliverable': 'Client Deliverable Report'` and `'stakeholder-status': 'Stakeholder Status Update'`
2. render() switch: add both case statements
3. module.exports: add buildClientDeliverable, buildStakeholderStatus
  </action>
  <acceptance_criteria>
    - grep -c "function buildClientDeliverable" bin/lib/render-presentation.cjs returns 1
    - grep -c "function buildStakeholderStatus" bin/lib/render-presentation.cjs returns 1
    - grep -c "function buildRAGStatus" bin/lib/render-presentation.cjs returns 1
    - grep -c "function buildVerificationEvidence" bin/lib/render-presentation.cjs returns 1
    - grep -c "function buildCurrentFocus" bin/lib/render-presentation.cjs returns 1
    - grep -c "function buildRiskRegister" bin/lib/render-presentation.cjs returns 1
    - grep "case 'client-deliverable'" bin/lib/render-presentation.cjs returns a match
    - grep "case 'stakeholder-status'" bin/lib/render-presentation.cjs returns a match
    - grep "'client-deliverable': 'Client Deliverable Report'" bin/lib/render-presentation.cjs returns a match
    - grep "'stakeholder-status': 'Stakeholder Status Update'" bin/lib/render-presentation.cjs returns a match
    - node -e "require('./bin/lib/render-presentation.cjs')" exits 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const r = require('./bin/lib/render-presentation.cjs'); const s1 = r.buildClientDeliverable({project:{name:'T',goal:'G',core_value:'C',summary:'S'},requirements:{total:10,completed:5,categories:[]},verification:{phases_verified:2,total_phases:3,pct:67},design_artifacts:{unavailable:true,reason:'none'},git_velocity:{total_commits:10},cost_timing:{total_duration_min:60}}); console.assert(Array.isArray(s1), 'client-deliverable returns array'); console.assert(s1.some(s=>s.id==='verification'), 'has verification section'); const s2 = r.buildStakeholderStatus({project:{name:'T',goal:'G',core_value:'C',summary:'S'},phases:{total:10,completed:8,phase_list:[{name:'p1',completed:false}]},requirements:{total:10,completed:5,categories:[]},blockers:{items:[]},risks:{items:[]},decisions:{items:[]}}); console.assert(Array.isArray(s2), 'stakeholder-status returns array'); console.assert(s2.some(s=>s.id==='rag'), 'has rag section'); console.assert(s2[0].content.includes('GREEN'), 'RAG is GREEN at 80%'); console.log('PASS')"</automated>
  </verify>
  <done>buildClientDeliverable and buildStakeholderStatus return correct sections, RAG status is deterministic (GREEN at 80%, AMBER at 50%, RED at 20%), both registered in switch + displayName + exports</done>
</task>

<task type="auto">
  <name>Task 2: Unskip and implement CLU-04 and CLU-05 test blocks</name>
  <read_first>
    - tests/phase-181/render-presentation-cluster-a.test.mjs (current file with describe.skip blocks)
    - bin/lib/render-presentation.cjs (verify new function signatures)
  </read_first>
  <files>tests/phase-181/render-presentation-cluster-a.test.mjs</files>
  <action>
In tests/phase-181/render-presentation-cluster-a.test.mjs:

1. Change `describe.skip('buildClientDeliverable (CLU-04)')` to `describe('buildClientDeliverable (CLU-04)')`
2. Implement tests:
   - it('returns an array of sections') — check Array.isArray, length >= 4
   - it('includes scope, features, verification, artifacts, effort sections') — check section IDs
   - it('verification section shows phases verified count') — content includes phases_verified value
   - it('handles unavailable verification gracefully') — pass IR with verification: {unavailable: true, reason: 'test'}

3. Change `describe.skip('buildStakeholderStatus (CLU-05)')` to `describe('buildStakeholderStatus (CLU-05)')`
4. Implement tests:
   - it('returns an array of sections') — check Array.isArray, length >= 5
   - it('includes rag, focus, blockers, risks, decisions, next-actions sections') — check section IDs
   - it('RAG status is GREEN when >= 75% complete') — pass IR with 8/10 phases completed, check content includes 'GREEN'
   - it('RAG status is AMBER when 40-74% complete') — pass IR with 5/10, check 'AMBER'
   - it('RAG status is RED when < 40% complete') — pass IR with 2/10, check 'RED'
   - it('handles unavailable phases gracefully') — sentinel test

5. Add integration tests:
   - it('render() accepts client-deliverable persona') — write to tmp, check files exist
   - it('render() accepts stakeholder-status persona') — same pattern

Run: `npx vitest run tests/phase-181/` to verify all tests pass.
  </action>
  <acceptance_criteria>
    - grep -c "buildClientDeliverable" tests/phase-181/render-presentation-cluster-a.test.mjs returns at least 5
    - grep -c "buildStakeholderStatus" tests/phase-181/render-presentation-cluster-a.test.mjs returns at least 5
    - grep -c "GREEN" tests/phase-181/render-presentation-cluster-a.test.mjs returns at least 1
    - grep -c "AMBER" tests/phase-181/render-presentation-cluster-a.test.mjs returns at least 1
    - grep -c "RED" tests/phase-181/render-presentation-cluster-a.test.mjs returns at least 1
    - grep "describe.skip" tests/phase-181/render-presentation-cluster-a.test.mjs matches only CLU-06 and CLU-07
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && npx vitest run tests/phase-181/ 2>&1 | tail -20</automated>
  </verify>
  <done>All CLU-02 through CLU-05 tests pass, only CLU-06 and CLU-07 remain as describe.skip, integration tests confirm render() accepts both new persona slugs</done>
</task>

</tasks>

<verification>
- `node -e "require('./bin/lib/render-presentation.cjs')"` exits cleanly
- `npx vitest run tests/phase-181/` — all non-skipped tests pass
- `npx vitest run tests/phase-178/` — no regressions
- RAG status computation is deterministic: GREEN/AMBER/RED based on completion percentage thresholds
</verification>

<success_criteria>
- buildClientDeliverable returns sections with scope, features, verification, artifacts, effort IDs
- buildStakeholderStatus returns sections with rag, focus, blockers, risks, decisions, next-actions IDs
- RAG status is GREEN at 80%, AMBER at 50%, RED at 20% — computed from IR, not LLM
- render('client-deliverable') and render('stakeholder-status') produce HTML + MD files
- All tests green, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/181-remaining-cluster-a-personas/181-02-SUMMARY.md`
</output>
