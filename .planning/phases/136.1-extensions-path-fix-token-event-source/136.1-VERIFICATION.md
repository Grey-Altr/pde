---
phase: 136.1-extensions-path-fix-token-event-source
verified: 2026-03-25T20:55:00Z
status: passed
score: 8/8 must-haves verified
gaps: []
human_verification:
  - test: "CostMeter shows non-zero values during a live session"
    expected: "Token cost displayed updates from $0.00 as a real SubagentStop event flows through the relay"
    why_human: "Requires a live PDE session with actual subagent transcript; cannot verify without running Claude Code with hooks active"
  - test: "Session list shows correct phase name after a phase_started event"
    expected: "Session row in dashboard home displays e.g. '136' or 'Core Dashboard' instead of blank"
    why_human: "Requires a live relay event with phase_name at top level flowing through ingest to Redis to dashboard SSE"
---

# Phase 136.1: Extensions Path Fix & Token Event Source — Verification Report

**Phase Goal:** Fix the extensions field path mismatch that breaks phase/plan display across ingest, dashboard components, and live event paths, and add token usage event emission so CostMeter displays real data.
**Verified:** 2026-03-25T20:55:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (derived from ROADMAP success criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | deriveProgress reads phase_name from top-level wire envelope, not extensions sub-object | VERIFIED | `dashboard/lib/derive-progress.ts` line 12: `const phaseName = String(payload.phase_name ?? '')` — no `extensions` read anywhere in file |
| 2 | Ingest route stores non-empty phase name in Redis when event has top-level phase_name | VERIFIED | `dashboard/app/api/ingest/route.ts` lines 53-58: `lastEventPayload.phase_name` cast pattern; Test 9 asserts `{ phase: 'Core Dashboard', plan: '02' }` — 57/57 tests pass |
| 3 | Session detail client displays phase/plan from top-level envelope fields | VERIFIED | `session-detail-client.tsx` lines 48-56: `evPayload.phase_name` and `evPayload.plan_id` fallback; no `ext?.phase_name` pattern remains |
| 4 | SubagentStop hook emits token_usage event with summed input_tokens and output_tokens from transcript | VERIFIED | `hooks/emit-event.cjs` lines 33-49: `sumTranscriptTokens` function; lines 99-109: emission block with `event-emit token_usage`; syntax OK |
| 5 | All existing tests pass plus new integration tests verify field path fix | VERIFIED | `cd dashboard && npm test` — 57/57 passed; `npx vitest run tests/phase-136.1/` — 6/6 passed |
| 6 | SUMMARY frontmatter gaps filled | VERIFIED | 136-01-SUMMARY.md line 6: `requirements-completed: [MON-01, MON-02, MON-03]`; 136-02-SUMMARY.md line 6: `requirements-completed: [MON-01, MON-02, MON-03, MON-04, MON-05]` |
| 7 | REQUIREMENTS.md traceability table Plan column filled for DSH-03, DSH-04, MON-01, MON-02 | VERIFIED | Lines 82-87: all four rows show `136.1-01` in Plan column |
| 8 | ROADMAP.md Phase 136.1 shows 2 plans with plan list | VERIFIED | Lines 102-105: `**Plans**: 2 plans` with both 136.1-01 and 136.1-02 listed |

**Score:** 8/8 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `dashboard/lib/derive-progress.ts` | Top-level field access for phase_name/plan_name with plan_id fallback | VERIFIED | Contains `payload.phase_name`, `payload.plan_name`, `payload.plan_id`; 19 lines, substantive |
| `dashboard/app/api/ingest/route.ts` | Correct phase/plan storage in Redis metadata | VERIFIED | Contains `lastEventPayload.phase_name`; full route implementation, 67 lines |
| `dashboard/app/sessions/[id]/session-detail-client.tsx` | Live session phase/plan display from top-level fields | VERIFIED | Contains `evPayload.phase_name`; 77 lines, full component with SSE merge logic |
| `hooks/emit-event.cjs` | Token usage event emission on SubagentStop | VERIFIED | Contains `sumTranscriptTokens` function and `event-emit token_usage` emission; 121 lines |
| `tests/phase-136.1/extensions-path-fix.test.ts` | Integration test for field path fix | VERIFIED | Contains `extensions path fix integration` describe block; 2 tests |
| `tests/phase-136.1/token-event-emission.test.ts` | Integration test for transcript token parsing | VERIFIED | Contains `token event emission — transcript parsing` describe block; 4 tests |
| `.planning/phases/136-core-dashboard-features/136-01-SUMMARY.md` | requirements-completed frontmatter | VERIFIED | Line 6: `requirements-completed: [MON-01, MON-02, MON-03]` |
| `.planning/phases/136-core-dashboard-features/136-02-SUMMARY.md` | requirements-completed frontmatter | VERIFIED | Line 6: `requirements-completed: [MON-01, MON-02, MON-03, MON-04, MON-05]` |
| `.planning/ROADMAP.md` | Updated plan list for Phase 136.1 | VERIFIED | Contains `136.1-01-PLAN.md` and `136.1-02-PLAN.md` under Phase 136.1 |
| `.planning/REQUIREMENTS.md` | Traceability table with Plan columns filled | VERIFIED | DSH-03, DSH-04, MON-01, MON-02 all show `136.1-01` in Plan column |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `hooks/emit-event.cjs` | `bin/pde-tools.cjs event-emit token_usage` | `spawnSync` in SubagentStop handler | WIRED | Line 104: `spawnSync(process.execPath, [pdeTools, 'event-emit', 'token_usage', JSON.stringify(tokenPayload)])` — guarded by `hookName === 'SubagentStop' && hookData.agent_transcript_path` |
| `dashboard/app/api/ingest/route.ts` | Redis `pde:default:session:{id}` hash | `hset` with top-level phase_name | WIRED | Line 57: `phase: String(lastEventPayload.phase_name ?? '')` — cast is on line 53, hset call on line 54 |
| `dashboard/lib/derive-progress.ts` | WireEnvelope passthrough fields | `Record<string, unknown>` cast | WIRED | Line 11: `const payload = ev as Record<string, unknown>;` — reads `payload.phase_name` on line 12 |
| `dashboard/app/sessions/[id]/session-detail-client.tsx` | Merged events buffer | `evPayload.phase_name` in `useMemo` | WIRED | Lines 48-56: derives `phase` and `plan` from `evPayload`; rendered via `<SessionDetail session={session} .../>` |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `derive-progress.ts` | `phaseName` / `planName` | `WireEnvelope[]` parameter (caller-provided) | Yes — reads from events array passed in; no hardcoded empty | FLOWING |
| `ingest/route.ts` | `phase` / `plan` in hset | `lastEvent` from validated batch | Yes — reads from validated request body event | FLOWING |
| `session-detail-client.tsx` | `session.phase` / `session.plan` | `mergedEvents[0]` (live SSE + initial) | Yes — live events from `useEventStream`, initial from server props | FLOWING |
| `hooks/emit-event.cjs` | `inputTokens` / `outputTokens` | `sumTranscriptTokens(hookData.agent_transcript_path)` | Yes — reads real JSONL transcript file; returns zeros only if file unreadable | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All dashboard unit tests pass | `cd dashboard && npm test` | 57/57 passed, 8 test files | PASS |
| Phase 136.1 integration tests pass | `npx vitest run tests/phase-136.1/` | 6/6 passed, 2 test files | PASS |
| emit-event.cjs has no syntax errors | `node -c hooks/emit-event.cjs` | exit 0 | PASS |
| No old `extensions?.phase_name` patterns in dashboard/ | grep for old pattern | 0 matches — exit 1 (no matches) | PASS |
| Commits 4fb298f acc9994 6f3e339 d93c510 exist | `git log --oneline` | All 4 found with correct messages | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| DSH-03 | 136.1-01 | Dashboard home page shows session list with status badges and current phase name | SATISFIED | Ingest route now stores non-empty `phase` in Redis metadata hash via `lastEventPayload.phase_name`; session list query reads `phase` from this hash |
| DSH-04 | 136.1-01 | Live session status card displays current phase, plan, agent activity, and elapsed time | SATISFIED | `session-detail-client.tsx` derives `phase`/`plan` from top-level `evPayload.phase_name`/`evPayload.plan_name`/`evPayload.plan_id` and passes to `<SessionDetail>` |
| MON-01 | 136.1-01 | Phase progress display shows nested hierarchy (phase → plan → wave) with progress indicators | SATISFIED | `deriveProgress` now correctly reads `phase_name`/`plan_name` from top-level envelope; `PhaseProgress` component receives real values |
| MON-02 | 136.1-01 | Token/cost meter shows running session total visible at a glance, updated in near-real-time | SATISFIED (automated portion) | `sumTranscriptTokens` + `token_usage` emission in SubagentStop wires token data into event bus; `deriveCost` already correctly reads `input_tokens`/`output_tokens` from top-level fields. Live CostMeter display requires human verification (see below) |

All 4 requirements declared in PLAN frontmatter are accounted for. REQUIREMENTS.md traceability table confirms all 4 rows have `Plan: 136.1-01`. No orphaned requirements found for this phase.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.planning/ROADMAP.md` | 105 | `- [ ] 136.1-02-PLAN.md` checkbox unchecked; progress row shows `1/2` not `2/2` | Info | Phase 136.1 plan 02 was completed (commit d93c510) but ROADMAP checkbox and progress counter were not updated. Documentation-only issue — no code impact |

No blocker or warning anti-patterns. The ROADMAP progress tracking gap is informational only — the actual work is complete and committed.

---

### Human Verification Required

#### 1. CostMeter Live Data

**Test:** Run a real Claude Code session with PDE hooks active, dispatch a subagent, let it complete. Observe the CostMeter in the dashboard session detail page.
**Expected:** CostMeter displays a non-zero cost value after the SubagentStop hook fires and the token_usage event is received by the ingest route.
**Why human:** Requires a live Claude Code session with an active subagent and real transcript JSONL file. Cannot simulate spawnSync calling `pde-tools.cjs event-emit` without running the full PDE stack.

#### 2. Session List Phase Name Display

**Test:** From a PDE session that emits a `phase_started` or similar event with `phase_name` at top level (not in `extensions`), navigate to the dashboard home page.
**Expected:** The session row displays the correct phase name (e.g., "136" or "Core Dashboard") rather than a blank or empty string.
**Why human:** Requires a live relay event flowing through ingest to Redis to the dashboard. The unit test (Test 9) confirms the Redis `hset` receives the correct value, but end-to-end display requires visual confirmation.

---

### Gaps Summary

No automated gaps. All four must-have truths from PLAN 01 and PLAN 02 are verified at all levels (exist, substantive, wired, data-flowing). Tests pass at both the dashboard unit level (57/57) and the integration level (6/6). The only outstanding items are two human verification points that require a live session — these are expected for a dashboard phase and do not block phase completion.

**Minor doc note:** ROADMAP.md progress row for phase 136.1 still shows `1/2` and plan 02 checkbox is unchecked. This is cosmetic — the implementation is complete. Should be fixed in a subsequent tidy-up commit.

---

_Verified: 2026-03-25T20:55:00Z_
_Verifier: Claude (gsd-verifier)_
