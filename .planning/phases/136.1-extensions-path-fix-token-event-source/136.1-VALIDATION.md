---
phase: 136.1
slug: extensions-path-fix-token-event-source
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-25
---

# Phase 136.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (dashboard: latest, root: 4.x) |
| **Config file** | `dashboard/vitest.config.ts` (dashboard), `vitest.config.ts` (root) |
| **Quick run command** | `cd dashboard && npm test` |
| **Full suite command** | `cd dashboard && npm test && cd .. && npx vitest run` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd dashboard && npm test`
- **After every plan wave:** Run `cd dashboard && npm test && cd .. && npx vitest run`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD | 01 | 1 | DSH-03 | unit | `cd dashboard && npm test -- lib/__tests__/ingest.test.ts` | Needs new test case | pending |
| TBD | 01 | 1 | DSH-04 | unit | `cd dashboard && npm test -- lib/__tests__/derive-progress.test.ts` | Needs fixture update | pending |
| TBD | 01 | 1 | MON-01 | unit | `cd dashboard && npm test -- lib/__tests__/derive-progress.test.ts` | Needs fixture update | pending |
| TBD | 02 | 1 | MON-02 | integration | `npx vitest run tests/phase-136.1/` | Wave 0 | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [ ] `tests/phase-136.1/test-token-event-emission.cjs` — covers MON-02: SubagentStop reads transcript, emits token_usage event with correct input_tokens/output_tokens
- [ ] `tests/phase-136.1/test-extensions-path-fix.cjs` — covers DSH-03/DSH-04: integration test verifying field-path fix end-to-end (event with top-level phase_name flows through ingest -> stored in Redis metadata)

*(Note: derive-progress.test.ts and ingest.test.ts already exist — only need new test cases added, not new files)*

---

## Manual-Only Verifications

*All phase behaviors have automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have automated verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
