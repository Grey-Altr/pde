---
phase: 136.1
slug: extensions-path-fix-token-event-source
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-25
audited: 2026-03-25
---

# Phase 136.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (dashboard: latest, root: 4.x) |
| **Config file** | `dashboard/vitest.config.ts` (dashboard), `vitest.config.ts` (root) |
| **Quick run command** | `cd dashboard && npm test` |
| **Full suite command** | `cd dashboard && npm test && cd .. && npx vitest run` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd dashboard && npm test`
- **After every plan wave:** Run `cd dashboard && npm test && cd .. && npx vitest run`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | Test File | Status |
|---------|------|------|-------------|-----------|-------------------|-----------|--------|
| T1 | 01 | 1 | DSH-03 | unit | `cd dashboard && npm test -- lib/__tests__/ingest.test.ts` | `dashboard/lib/__tests__/ingest.test.ts` (Test 9) | green |
| T1 | 01 | 1 | DSH-04 | unit | `cd dashboard && npm test -- lib/__tests__/derive-progress.test.ts` | `dashboard/lib/__tests__/derive-progress.test.ts` (7 tests) | green |
| T1 | 01 | 1 | MON-01 | unit | `cd dashboard && npm test -- lib/__tests__/session-derive-phase.test.ts` | `dashboard/lib/__tests__/session-derive-phase.test.ts` (11 tests) | green |
| T2 | 01 | 1 | MON-02 | integration | `npx vitest run tests/phase-136.1/` | `tests/phase-136.1/token-event-emission.test.ts` (4 tests) | green |
| T3 | 01 | 1 | DSH-03/04 | integration | `npx vitest run tests/phase-136.1/` | `tests/phase-136.1/extensions-path-fix.test.ts` (2 tests) | green |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [x] `tests/phase-136.1/token-event-emission.test.ts` — covers MON-02: SubagentStop reads transcript, emits token_usage event with correct input_tokens/output_tokens
- [x] `tests/phase-136.1/extensions-path-fix.test.ts` — covers DSH-03/DSH-04: integration test verifying field-path fix end-to-end (event with top-level phase_name flows through ingest -> stored in Redis metadata)

*(Note: derive-progress.test.ts and ingest.test.ts already exist — new test cases were added, not new files)*

---

## Manual-Only Verifications

*All phase behaviors have automated verification.*

---

## Validation Sign-Off

- [x] All tasks have automated verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 15s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** complete

---

## Validation Audit 2026-03-25

| Metric | Count |
|--------|-------|
| Gaps found | 1 |
| Resolved | 1 |
| Escalated | 0 |

**Details:**
- MON-01 (session-detail-client.tsx top-level field read): Was PARTIAL — no direct test for the session derivation logic. Resolved by adding `session-derive-phase.test.ts` with 11 tests covering all branches of the phase/plan derivation at lines 42-58. Total dashboard tests: 68 (up from 57).
