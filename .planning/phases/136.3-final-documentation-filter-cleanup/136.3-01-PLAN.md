---
phase: 136.3-final-documentation-filter-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - dashboard/lib/event-types.ts
  - dashboard/lib/__tests__/event-filters.test.ts
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "REQUIREMENTS.md traceability table shows DSH-02 Plan=135-03 and Verified=Yes"
    - "REQUIREMENTS.md traceability table shows DSH-03, DSH-04, MON-01, MON-02 Verified=Yes"
    - "REQUIREMENTS.md traceability table shows RLY-01 Verified=Yes"
    - "EVENT_FILTER_GROUPS contains a tokens key with token_usage event type"
    - "filterEvents returns only token_usage events when group is tokens"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table with all Plan and Verified columns filled"
      contains: "| DSH-02 | 135 | 135-03 | Yes |"
    - path: "dashboard/lib/event-types.ts"
      provides: "EVENT_FILTER_GROUPS with tokens filter group"
      contains: "tokens: ['token_usage']"
    - path: "dashboard/lib/__tests__/event-filters.test.ts"
      provides: "Test coverage for tokens filter group"
      contains: "token_usage"
  key_links:
    - from: "dashboard/lib/event-types.ts"
      to: "dashboard/components/event-log.tsx"
      via: "Object.keys(EVENT_FILTER_GROUPS) drives tab rendering"
      pattern: "Object\\.keys\\(EVENT_FILTER_GROUPS\\)"
    - from: "dashboard/lib/__tests__/event-filters.test.ts"
      to: "dashboard/lib/event-types.ts"
      via: "import and assertion"
      pattern: "EVENT_FILTER_GROUPS\\.tokens"
---

<objective>
Close remaining v0.17 documentation traceability gaps and add token_usage to the EventLog filter taxonomy.

Purpose: SC-1 fills six incomplete cells in REQUIREMENTS.md traceability table (Plan and Verified columns for RLY-01, DSH-02, DSH-03, DSH-04, MON-01, MON-02). SC-5 adds a `tokens` filter tab to EVENT_FILTER_GROUPS so users can filter event log to show only token_usage events. SC-2, SC-3, SC-4 are already satisfied by Phase 136.2 — confirmed by research — and require no action.

Output: Updated REQUIREMENTS.md, updated event-types.ts, updated event-filters.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/136.3-final-documentation-filter-cleanup/136.3-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From dashboard/lib/event-types.ts:
```typescript
export const EVENT_FILTER_GROUPS = {
  all: null,
  tools: ['tool_called', 'bash_called', 'file_changed'],
  agents: ['subagent_start', 'subagent_stop'],
  phases: ['session_start', 'session_end'],
  errors: ['error'],
} as const;

export type FilterGroup = keyof typeof EVENT_FILTER_GROUPS;

export function filterEvents(events: WireEnvelope[], group: FilterGroup): WireEnvelope[] {
  const allowed = EVENT_FILTER_GROUPS[group];
  if (allowed === null) return events;
  return events.filter((ev) => (allowed as readonly string[]).includes(ev.event_type));
}
```

From dashboard/components/event-log.tsx (lines 57-65 — dynamic tab rendering):
```typescript
{(Object.keys(EVENT_FILTER_GROUPS) as FilterGroup[]).map((group) => (
  <TabsTrigger key={group} value={group} className="flex-1 min-h-[44px] min-w-[44px] capitalize">
    {group}
  </TabsTrigger>
))}
```
Note: Tabs render dynamically from Object.keys — no UI change needed when adding a key to EVENT_FILTER_GROUPS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md traceability table (SC-1)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current traceability table state — lines 73-105)
    - .planning/phases/136.3-final-documentation-filter-cleanup/136.3-RESEARCH.md (target row values and evidence, lines 76-99)
  </read_first>
  <action>
Edit six rows in the REQUIREMENTS.md traceability table (pipe-delimited Markdown table starting at line 73). Change each row to its target state:

Row 1 — RLY-01: Change `| RLY-01 | 134.1 | Complete | -- |` to `| RLY-01 | 134.1 | Complete | Yes |`
(Evidence: 134.1-VERIFICATION.md confirms daemon tails correct NDJSON file)

Row 2 — DSH-02: Change `| DSH-02 | 135 | — | — |` to `| DSH-02 | 135 | 135-03 | Yes |`
(Evidence: 135-03-SUMMARY.md frontmatter has requirements-completed: [DSH-02])

Row 3 — DSH-03: Change `| DSH-03 | 136.1 | 136.1-01 | Pending |` to `| DSH-03 | 136.1 | 136.1-01 | Yes |`
Row 4 — DSH-04: Change `| DSH-04 | 136.1 | 136.1-01 | Pending |` to `| DSH-04 | 136.1 | 136.1-01 | Yes |`
Row 5 — MON-01: Change `| MON-01 | 136.1 | 136.1-01 | Pending |` to `| MON-01 | 136.1 | 136.1-01 | Yes |`
Row 6 — MON-02: Change `| MON-02 | 136.1 | 136.1-01 | Pending |` to `| MON-02 | 136.1 | 136.1-01 | Yes |`
(Evidence: 136.1-01-SUMMARY.md has requirements-completed: [DSH-03, DSH-04, MON-01, MON-02])

Do NOT modify any other rows. Leave all other content in the file unchanged.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep -E "RLY-01|DSH-02|DSH-03|DSH-04|MON-01|MON-02" .planning/REQUIREMENTS.md | grep -c "Yes"</automated>
  </verify>
  <acceptance_criteria>
    - grep "RLY-01" .planning/REQUIREMENTS.md outputs: `| RLY-01 | 134.1 | Complete | Yes |`
    - grep "DSH-02" .planning/REQUIREMENTS.md outputs: `| DSH-02 | 135 | 135-03 | Yes |`
    - grep "DSH-03" .planning/REQUIREMENTS.md outputs: `| DSH-03 | 136.1 | 136.1-01 | Yes |`
    - grep "DSH-04" .planning/REQUIREMENTS.md outputs: `| DSH-04 | 136.1 | 136.1-01 | Yes |`
    - grep "MON-01" .planning/REQUIREMENTS.md outputs: `| MON-01 | 136.1 | 136.1-01 | Yes |`
    - grep "MON-02" .planning/REQUIREMENTS.md outputs: `| MON-02 | 136.1 | 136.1-01 | Yes |`
    - The grep -c "Yes" command returns 6 (matching all six updated rows)
  </acceptance_criteria>
  <done>All six traceability rows have correct Plan and Verified=Yes values</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add token_usage filter group to EVENT_FILTER_GROUPS (SC-5)</name>
  <files>dashboard/lib/event-types.ts, dashboard/lib/__tests__/event-filters.test.ts</files>
  <read_first>
    - dashboard/lib/event-types.ts (current EVENT_FILTER_GROUPS constant — 17 lines)
    - dashboard/lib/__tests__/event-filters.test.ts (existing test structure — 90 lines)
    - dashboard/components/event-log.tsx (confirm dynamic tab rendering uses Object.keys — line 57)
  </read_first>
  <behavior>
    - Test: EVENT_FILTER_GROUPS.tokens contains 'token_usage'
    - Test: filterEvents with group 'tokens' returns only token_usage events from a mixed set
    - Test: filterEvents with group 'tokens' on empty array returns empty array
  </behavior>
  <action>
RED phase — Add tests first:

In `dashboard/lib/__tests__/event-filters.test.ts`:

1. Inside the `describe('EVENT_FILTER_GROUPS')` block (after the `errors` test at line 40), add:
```typescript
  it('tokens contains token_usage', () => {
    expect(EVENT_FILTER_GROUPS.tokens).toContain('token_usage');
  });
```

2. Inside the `describe('filterEvents')` block, add `mockEnvelope({ event_type: 'token_usage' })` to the `events` array (after the `error` entry at line 52). Then add a new test case after the errors test (line 83):
```typescript
  it('returns only token events when group is "tokens"', () => {
    const result = filterEvents(events, 'tokens');
    expect(result).toHaveLength(1);
    expect(result[0].event_type).toBe('token_usage');
  });
```

3. Update the `all` filter test — `events.length` will now be 9 instead of 8 (no code change needed since it uses `events.length` dynamically).

4. Update the empty array test to include `tokens`:
```typescript
    expect(filterEvents([], 'tokens')).toEqual([]);
```

Run tests — confirm RED (TypeScript compile error: Property 'tokens' does not exist on type).

GREEN phase — Add the filter group:

In `dashboard/lib/event-types.ts`, add `tokens: ['token_usage'],` as the last entry before `} as const;` (after the `errors` line). The file should become:

```typescript
export const EVENT_FILTER_GROUPS = {
  all: null,
  tools: ['tool_called', 'bash_called', 'file_changed'],
  agents: ['subagent_start', 'subagent_stop'],
  phases: ['session_start', 'session_end'],
  errors: ['error'],
  tokens: ['token_usage'],
} as const;
```

Do NOT change filterEvents function — it already handles any key generically.
Do NOT remove `as const` — FilterGroup type derivation depends on it.

Run tests — confirm GREEN (all pass).
Run `cd dashboard && npx tsc --noEmit` to confirm no type errors.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine/dashboard" && npx vitest run lib/__tests__/event-filters.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/lib/event-types.ts contains the line `tokens: ['token_usage'],`
    - dashboard/lib/event-types.ts still contains `} as const;`
    - dashboard/lib/__tests__/event-filters.test.ts contains `EVENT_FILTER_GROUPS.tokens`
    - dashboard/lib/__tests__/event-filters.test.ts contains `filterEvents(events, 'tokens')`
    - vitest run exits with 0 and all tests pass
    - grep "token_usage" dashboard/lib/event-types.ts returns a match
  </acceptance_criteria>
  <done>EVENT_FILTER_GROUPS has tokens key with token_usage, all tests green, EventLog UI will render "tokens" tab automatically via Object.keys</done>
</task>

</tasks>

<verification>
All five success criteria verified:

SC-1: `grep -E "RLY-01|DSH-02|DSH-03|DSH-04|MON-01|MON-02" .planning/REQUIREMENTS.md` — all six rows show correct Plan and Verified=Yes
SC-2: Already satisfied (confirmed by research — 134-01-SUMMARY.md and 135-01-SUMMARY.md have requirements-completed in YAML frontmatter)
SC-3: Already satisfied (confirmed by research — ROADMAP.md Phase 136.2 checkboxes [x], progress 2/2 Complete)
SC-4: Already satisfied (confirmed by research — 136.2-VALIDATION.md has nyquist_compliant: true)
SC-5: `grep "token_usage" dashboard/lib/event-types.ts` — tokens filter group present; vitest suite green
</verification>

<success_criteria>
1. REQUIREMENTS.md has 6 updated traceability rows with correct Plan and Verified=Yes values
2. EVENT_FILTER_GROUPS in event-types.ts includes `tokens: ['token_usage']`
3. All vitest tests pass including new tokens filter group tests
4. No TypeScript errors (`tsc --noEmit` clean)
</success_criteria>

<output>
After completion, create `.planning/phases/136.3-final-documentation-filter-cleanup/136.3-01-SUMMARY.md`
</output>
