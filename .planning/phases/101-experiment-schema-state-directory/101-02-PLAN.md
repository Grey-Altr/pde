---
phase: 101-experiment-schema-state-directory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - references/experiment-phase-type.md
autonomous: true
requirements: [CMD-03]

must_haves:
  truths:
    - "A reference document defines the experiment phase type format with target metric, search space, iteration budget, and keep/discard threshold fields"
    - "The format is recognizable by downstream plan-phase tooling (fields are present and machine-parseable)"
  artifacts:
    - path: "references/experiment-phase-type.md"
      provides: "Canonical definition of experiment phase type format for ROADMAP.md"
      contains: "Target Metric"
  key_links:
    - from: "references/experiment-phase-type.md"
      to: ".planning/ROADMAP.md"
      via: "Format convention consumed by future experiment phases"
      pattern: "Type.*experiment"
---

<objective>
Document the experiment phase type format as a reference, defining the four extra fields (Type, Target Metric, Search Space, Iteration Budget) that distinguish experiment phases from standard phases in ROADMAP.md.

Purpose: CMD-03 requires "experiment phase type recognized in ROADMAP.md." This is a format convention, not a code change. The reference document establishes the contract so Phase 103+ can create experiment phases correctly.
Output: references/experiment-phase-type.md
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/101-experiment-schema-state-directory/101-RESEARCH.md

<interfaces>
<!-- Existing ROADMAP phase format (standard) -->
From .planning/ROADMAP.md:
```markdown
### Phase N: Name
**Goal**: ...
**Depends on**: Phase N-1
**Requirements**: REQ-XX
**Success Criteria** (what must be TRUE):
  1. ...
**Plans**: TBD
```

<!-- Experiment phase type extension from research Pattern 5 -->
```markdown
### Phase N: Optimize Brief Quality
**Type**: experiment
**Goal**: ...
**Target Metric**: awwwards_score (direction: max)
**Search Space**: workflows/brief.md OPTIMIZABLE sections
**Iteration Budget**: 50 iterations / 60 min
**Depends on**: Phase 103
**Requirements**: REQ-XX
**Success Criteria**: ...
**Plans**: TBD
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create experiment phase type reference document</name>
  <files>references/experiment-phase-type.md</files>
  <read_first>
    .planning/ROADMAP.md
    references/experiment-boundaries.md
    .planning/phases/101-experiment-schema-state-directory/101-RESEARCH.md
  </read_first>
  <action>
    Create `references/experiment-phase-type.md` that defines the experiment phase type format for ROADMAP.md entries.

    The document must include:

    1. **Purpose section** — Explains that experiment phases use the `/pde:optimize` loop instead of standard plan-execute, and how they differ from standard phases.

    2. **Format specification** — The four additional fields beyond standard phase format:
       - `**Type**: experiment` — Marks the phase as an experiment (standard phases omit this field)
       - `**Target Metric**: {metric_name} (direction: {min|max})` — The metric being optimized and whether lower or higher is better
       - `**Search Space**: {file_list} {section_description}` — Which files and sections are eligible for mutation
       - `**Iteration Budget**: {N} iterations / {M} min` — Maximum iterations and wall-clock time

    3. **Example** — A complete experiment phase entry showing all fields, following the pattern from 101-RESEARCH.md Pattern 5.

    4. **Relationship to experiment.md** — Note that the ROADMAP fields mirror the experiment.md frontmatter schema. The ROADMAP entry declares intent; the experiment.md file is the machine-readable config that the runner actually consumes.

    5. **Recognition rules** — How downstream tooling identifies an experiment phase:
       - Phase has `**Type**: experiment` field
       - Phase has `**Target Metric**` field
       - These two fields are sufficient to distinguish experiment from standard phases

    Follow the style of `references/experiment-boundaries.md` (YAML frontmatter with scope/status, markdown body).

    Use this frontmatter:
    ```yaml
    ---
    scope: experiment-phase-type
    status: active
    created: 2026-03-23
    ---
    ```
  </action>
  <verify>
    <automated>grep -q "Target Metric" references/experiment-phase-type.md && grep -q "Search Space" references/experiment-phase-type.md && grep -q "Iteration Budget" references/experiment-phase-type.md && grep -q "Type.*experiment" references/experiment-phase-type.md</automated>
  </verify>
  <acceptance_criteria>
    - test -f references/experiment-phase-type.md
    - grep -q "Target Metric" references/experiment-phase-type.md
    - grep -q "Search Space" references/experiment-phase-type.md
    - grep -q "Iteration Budget" references/experiment-phase-type.md
    - grep -q "Type.*experiment" references/experiment-phase-type.md
    - grep -q "direction" references/experiment-phase-type.md
  </acceptance_criteria>
  <done>
    - references/experiment-phase-type.md exists with complete format specification
    - All four experiment-specific fields documented (Type, Target Metric, Search Space, Iteration Budget)
    - Example experiment phase entry included
    - Relationship to experiment.md schema explained
    - Recognition rules defined for downstream tooling
  </done>
</task>

</tasks>

<verification>
1. `test -f references/experiment-phase-type.md` — document exists
2. `grep -c "Target Metric\|Search Space\|Iteration Budget\|Type.*experiment" references/experiment-phase-type.md` — all 4 fields present
</verification>

<success_criteria>
- Experiment phase type format is fully documented with all four distinguishing fields
- Example entry demonstrates correct usage
- Document follows existing references/ conventions
</success_criteria>

<output>
After completion, create `.planning/phases/101-experiment-schema-state-directory/101-02-SUMMARY.md`
</output>
