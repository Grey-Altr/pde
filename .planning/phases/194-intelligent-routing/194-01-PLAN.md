---
phase: 194-intelligent-routing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/dispatcher/lib/classify.cjs
  - tests/dispatcher/classify.test.cjs
autonomous: true
requirements: [RTG-01, RTG-02, RTG-03, RTG-04, RTG-06]

must_haves:
  truths:
    - "classifyTaskRouting() returns 'local' with reason 'fast_path' when isFastPath=true"
    - "classifyTaskRouting() returns the CLI-specified backend with reason 'manual_override' when dispatchOverride is set"
    - "classifyTaskRouting() returns per-phase config override with reason 'manual_override' when configOverrides.override[phase] is set"
    - "classifyTaskRouting() returns 'local' with reason 'cost_ceiling' when estimated cost exceeds ceiling"
    - "classifyTaskRouting() returns initialBackend with reason 'auto_classify' when no overrides apply"
    - "cost ceiling of 0 always routes non-local backends to local"
    - "Priority order: fast_path > CLI override > config override > cost ceiling > auto_classify"
  artifacts:
    - path: "packages/dispatcher/lib/classify.cjs"
      provides: "classifyTaskRouting pure function"
      exports: ["classifyTaskRouting"]
    - path: "tests/dispatcher/classify.test.cjs"
      provides: "Unit tests for all routing priority rules"
      min_lines: 80
  key_links:
    - from: "tests/dispatcher/classify.test.cjs"
      to: "packages/dispatcher/lib/classify.cjs"
      via: "require()"
      pattern: "require.*classify"
---

<objective>
Create the classifyTaskRouting() pure function module and its comprehensive TDD test suite.

Purpose: This is the core routing decision function for Phase 194. It encodes a priority-ordered decision tree (fast_path > CLI override > config override > cost ceiling > auto_classify) that adjusts the initial backend from routeSession() before any resources are allocated. Building it as a pure function with full DI makes it testable in isolation.

Output: packages/dispatcher/lib/classify.cjs (production), tests/dispatcher/classify.test.cjs (tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/194-intelligent-routing/194-CONTEXT.md
@.planning/phases/194-intelligent-routing/194-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create classify.test.cjs test suite (RED)</name>
  <files>tests/dispatcher/classify.test.cjs</files>
  <read_first>
    - tests/dispatcher/remote-router.test.cjs (test pattern, describe/it structure, pure function test style)
    - .planning/phases/194-intelligent-routing/194-RESEARCH.md (Pattern 1 classify.cjs signature, test examples section)
    - .planning/phases/194-intelligent-routing/194-CONTEXT.md (classifyTaskRouting() signature in specifics block)
  </read_first>
  <behavior>
    - fast_path=true, fastPathLocal=true -> backend='local', reason='fast_path', estimatedCost=0
    - fast_path=true, fastPathLocal=false -> does NOT force local (passthrough to next rule)
    - dispatchOverride='docker' -> backend='docker', reason='manual_override' (RTG-01)
    - dispatchOverride='ssh' overrides cost ceiling (priority 2 > priority 4)
    - configOverrides.override['194']='ssh' -> backend='ssh', reason='manual_override' (RTG-03)
    - CLI dispatchOverride takes priority over per-phase config override
    - costConfig.ceiling=0, initialBackend='cloud' -> backend='local', reason='cost_ceiling', events has routing_cost_ceiling event (RTG-04)
    - costConfig.ceiling=null (no ceiling) -> passthrough to auto_classify
    - costConfig.ceiling=20, cloud at $0.50/min * 30min = $15 (under ceiling) -> passthrough
    - costConfig.ceiling=10, cloud at $0.50/min * 30min = $15 (over ceiling) -> downgrade to local
    - no overrides, no ceiling -> backend=initialBackend, reason='auto_classify' (RTG-02)
    - estimatedCost is correctly computed as costPerMinute[backend] * estimated_minutes
    - missing planMetadata.estimated_minutes defaults to 30
    - events array is always returned (empty or with routing_cost_ceiling event)
    - opts={} (no isFastPath key) does NOT trigger fast_path (RTG-06 edge case from Pitfall 3)
  </behavior>
  <action>
    Create tests/dispatcher/classify.test.cjs with vitest globals (describe/it/expect).

    Define a baseOpts object matching the classifyTaskRouting() signature from CONTEXT.md:
    ```
    const baseOpts = {
      initialBackend: 'cloud',
      planMetadata: { autonomous: true, estimated_minutes: 30, agent_type: 'autonomous', wave: 1 },
      dispatchOverride: null,
      configOverrides: {},
      costConfig: { ceiling: null, costPerMinute: { cloud: 0.50, docker: 0.10, ssh: 0.05, local: 0.00 } },
      isFastPath: false,
      fastPathLocal: true,
      phase: 194,
    };
    ```

    Write 12-15 test cases organized in describe blocks by priority level:
    - describe('Priority 1: fast_path') — 2 tests (true/false fastPathLocal)
    - describe('Priority 2: CLI --dispatch override') — 2 tests (basic, overrides ceiling)
    - describe('Priority 3: per-phase config override') — 2 tests (basic, CLI takes precedence)
    - describe('Priority 4: cost ceiling') — 4 tests (ceiling=0, null, under, over)
    - describe('Priority 5: auto_classify') — 1 test (passthrough)
    - describe('edge cases') — 2 tests (missing estimated_minutes defaults to 30, opts without isFastPath)

    Each test uses spread of baseOpts with override for the specific scenario.
    All tests should FAIL because classify.cjs does not exist yet.

    Run: npx vitest run tests/dispatcher/classify.test.cjs (expect failures)
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && npx vitest run tests/dispatcher/classify.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" tests/dispatcher/classify.test.cjs returns 15+
    - grep "require.*classify" tests/dispatcher/classify.test.cjs finds the import
    - grep "fast_path" tests/dispatcher/classify.test.cjs finds fast_path test cases
    - grep "cost_ceiling" tests/dispatcher/classify.test.cjs finds cost ceiling test cases
    - grep "manual_override" tests/dispatcher/classify.test.cjs finds override test cases
    - grep "auto_classify" tests/dispatcher/classify.test.cjs finds auto-classify test case
  </acceptance_criteria>
  <done>Test file exists with 12+ test cases covering all 5 priority levels and edge cases. All tests fail because classify.cjs does not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement classify.cjs (GREEN) and refactor</name>
  <files>packages/dispatcher/lib/classify.cjs</files>
  <read_first>
    - tests/dispatcher/classify.test.cjs (the RED tests just created)
    - .planning/phases/194-intelligent-routing/194-RESEARCH.md (Pattern 1 full implementation, Pitfall 2 null vs 0, Pitfall 3 undefined isFastPath)
    - packages/dispatcher/lib/remote-router.cjs (module style: 'use strict', module.exports pattern)
  </read_first>
  <behavior>
    - All tests from Task 1 pass
    - Function is pure: no require() beyond built-ins, no I/O, no side effects
    - Returns { backend, reason, estimatedCost, events } for every code path
  </behavior>
  <action>
    Create packages/dispatcher/lib/classify.cjs implementing classifyTaskRouting().

    Use 'use strict' header. Export via module.exports = { classifyTaskRouting }.

    Priority-ordered decision tree (per CONTEXT.md locked decisions):
    1. Fast-path guard: if isFastPath AND fastPathLocal !== false -> return { backend: 'local', reason: 'fast_path', estimatedCost: 0, events: [] }
    2. CLI --dispatch override: if dispatchOverride -> return { backend: dispatchOverride, reason: 'manual_override', estimatedCost: null, events: [] }
    3. Per-phase config override: if configOverrides?.override?.[String(phase)] -> return { backend: phaseOverride, reason: 'manual_override', estimatedCost: null, events: [] }
    4. Cost ceiling: compute estimatedCost = (costPerMinute[initialBackend] || 0) * (planMetadata?.estimated_minutes || 30). If ceiling !== null && ceiling !== undefined && estimatedCost > ceiling -> return { backend: 'local', reason: 'cost_ceiling', estimatedCost, events: [{ type: 'system', subtype: 'routing_cost_ceiling', from: initialBackend, to: 'local', estimatedCost, ceiling: costConfig.ceiling }] }
    5. Auto-classify: return { backend: initialBackend, reason: 'auto_classify', estimatedCost, events: [] }

    CRITICAL per Pitfall 2: Use explicit null/undefined check for ceiling, NOT falsy check. ceiling=0 means "always route to local".

    Add JSDoc with @param and @returns documenting the full signature from CONTEXT.md.

    Run tests — all must pass.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && npx vitest run tests/dispatcher/classify.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - grep "classifyTaskRouting" packages/dispatcher/lib/classify.cjs finds the function
    - grep "module.exports" packages/dispatcher/lib/classify.cjs finds the export
    - grep "fast_path" packages/dispatcher/lib/classify.cjs finds fast_path reason
    - grep "manual_override" packages/dispatcher/lib/classify.cjs finds override reason
    - grep "cost_ceiling" packages/dispatcher/lib/classify.cjs finds cost ceiling reason
    - grep "auto_classify" packages/dispatcher/lib/classify.cjs finds auto-classify reason
    - grep -c "require(" packages/dispatcher/lib/classify.cjs returns 0 (pure function, no requires)
  </acceptance_criteria>
  <done>classify.cjs exists as a pure function module. All classify.test.cjs tests pass. Function handles all 5 priority levels with correct null/undefined handling for cost ceiling.</done>
</task>

</tasks>

<verification>
npx vitest run tests/dispatcher/classify.test.cjs — all tests green
</verification>

<success_criteria>
- classifyTaskRouting() passes all unit tests covering RTG-01 (CLI override), RTG-02 (auto-classify), RTG-03 (config override), RTG-04 (cost ceiling), RTG-06 (fast-path)
- Module is pure: zero require() calls, zero I/O, zero side effects
- Priority ordering verified: fast_path > CLI > config > cost > auto
</success_criteria>

<output>
After completion, create `.planning/phases/194-intelligent-routing/194-01-SUMMARY.md`
</output>
