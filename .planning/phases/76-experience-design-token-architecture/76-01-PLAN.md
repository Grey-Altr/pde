---
phase: 76-experience-design-token-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-76/experience-tokens.test.mjs
  - workflows/system.md
autonomous: true
requirements: [DSYS-01, DSYS-02, DSYS-03, DSYS-04, DSYS-05, DSYS-06, DSYS-07]

must_haves:
  truths:
    - "system.md instructs generation of SYS-experience-tokens.json when PRODUCT_TYPE is experience"
    - "system.md does NOT modify SYS-tokens.json generation for experience products — base file is identical across product types"
    - "Six token categories (sonic, lighting, spatial, atmospheric, wayfinding, brand-coherence) appear in system.md with 5 tokens each, 30 total"
    - "tokens-to-css invocation for experience file uses existing pde-tools.cjs command with experience file path"
    - "SYS-EXP artifact registered in manifest via pde-tools.cjs design manifest-update commands"
    - "Nyquist test suite covers all 7 DSYS requirements with structural assertions on system.md content"
  artifacts:
    - path: "tests/phase-76/experience-tokens.test.mjs"
      provides: "Nyquist structural assertions for DSYS-01 through DSYS-07"
      min_lines: 60
    - path: "workflows/system.md"
      provides: "Step 5b experience token generation block with PRODUCT_TYPE guard"
      contains: "SYS-experience-tokens.json"
  key_links:
    - from: "workflows/system.md"
      to: "SYS-experience-tokens.json"
      via: "Step 5b conditional block gated on PRODUCT_TYPE == experience"
      pattern: "PRODUCT_TYPE.*experience.*SYS-experience-tokens"
    - from: "workflows/system.md"
      to: "pde-tools.cjs design manifest-update SYS-EXP"
      via: "manifest registration commands after file write"
      pattern: "manifest-update SYS-EXP"
    - from: "workflows/system.md"
      to: "pde-tools.cjs design tokens-to-css"
      via: "CSS generation from experience JSON file"
      pattern: "tokens-to-css.*SYS-experience-tokens"
---

<objective>
Write Wave 0 Nyquist tests for all 7 DSYS requirements, then add the experience design token conditional block to system.md — generating SYS-experience-tokens.json with 6 categories (sonic, lighting, spatial, atmospheric, wayfinding, brand-coherence), CSS conversion, and manifest registration.

Purpose: This is the core implementation plan for Phase 76. Experience products need physical-domain design tokens (sound, light, space) separate from the visual design tokens all product types share. The separate file architecture (DSYS-07) is an irreversible locked decision.

Output: Passing Nyquist test suite + fully instrumented system.md with experience token generation.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-experience-design-token-architecture/76-RESEARCH.md
@.planning/phases/75-brief-extensions/75-01-SUMMARY.md

<interfaces>
<!-- Key patterns executor needs from codebase -->

From tests/phase-75/brief-extensions.test.mjs (Wave 0 TDD pattern):
```javascript
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'fs';
import { resolve, join } from 'path';
import { fileURLToPath } from 'url';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');

// Pattern: read workflow, assert on string content
```

From workflows/system.md line 71 (Phase 74 stub to replace):
```markdown
<!-- Experience product type: experience-specific design system extensions (SYS-experience-tokens.json) added in Phase 76. Phase 74 stub — proceed with software path as temporary fallback for experience product type. -->
```

From 76-RESEARCH.md Token Schema Reference (6 categories x 5 tokens = 30 total):
- sonic: bpm-range, genre-primary, volume-curve, system-spec, transition-strategy
- lighting: zone-main-color, intensity-curve, fixture-type-primary, peak-color, house-lights-protocol
- spatial: zone-count, main-floor-mood, density-target, sightlines, material-palette
- atmospheric: ventilation-type, indoor-outdoor, haze-level, temperature-target, air-movement
- wayfinding: sign-hierarchy, arrow-standard, legibility-distance, outdoor-contrast, hierarchy-levels
- brand-coherence: identity-thread, tone-of-voice, sensory-signature, collateral-sequence, wristband-color

From workflows/system.md manifest registration pattern (lines 1827-1833):
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-EXP code SYS-EXP
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-EXP name "Experience Design Tokens"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-EXP type experience-tokens
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-EXP domain visual
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-EXP path ".planning/design/visual/SYS-experience-tokens.json"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-EXP status draft
```

From workflows/system.md tokens-to-css pattern (lines 1596-1601):
```bash
CSS_EXP=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design tokens-to-css ".planning/design/visual/SYS-experience-tokens.json" --raw)
if [[ "$CSS_EXP" == @file:* ]]; then CSS_EXP=$(cat "${CSS_EXP#@file:}"); fi
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 Nyquist test suite for DSYS-01 through DSYS-07</name>
  <files>tests/phase-76/experience-tokens.test.mjs</files>
  <read_first>
    - tests/phase-75/brief-extensions.test.mjs (Wave 0 TDD pattern to replicate)
    - .planning/phases/76-experience-design-token-architecture/76-RESEARCH.md (token schema reference)
  </read_first>
  <action>
Create directory `tests/phase-76/` and file `tests/phase-76/experience-tokens.test.mjs`.

Follow the exact pattern from `tests/phase-75/brief-extensions.test.mjs`:
- Same imports: `node:test`, `node:assert/strict`, `fs`, `path`, `url`
- Same `ROOT` resolution via `fileURLToPath`
- Helper function: `function readWorkflow(name) { return readFileSync(join(ROOT, name), 'utf8'); }`

Write these describe/test blocks:

**DSYS-07 (separate file, test FIRST since it gates everything):**
- `system.md contains SYS-experience-tokens.json generation instruction` — assert `content.includes('SYS-experience-tokens.json')`
- `PRODUCT_TYPE experience guard appears before SYS-experience-tokens.json` — assert guard index < file reference index. Check for both `PRODUCT_TYPE == "experience"` and `product_type == "experience"` (case variants). Guard must come before the `SYS-experience-tokens.json` string.
- `system.md does NOT add experience categories to SYS-tokens.json instructions` — find the section between `SYS-tokens.json` generation and `SYS-experience-tokens.json` generation; assert this section does NOT contain `sonic` or `lighting` keywords (base file isolation).

**DSYS-01 (sonic):**
- `system.md includes sonic token generation with bpm-range` — assert content includes both `sonic` and `bpm-range`

**DSYS-02 (lighting):**
- `system.md includes lighting token generation with zone-main-color` — assert content includes both `lighting` and `zone-main-color`

**DSYS-03 (spatial):**
- `system.md includes spatial token generation with density-target` — assert content includes both `spatial` and `density-target`

**DSYS-04 (atmospheric):**
- `system.md includes atmospheric token generation with ventilation-type` — assert content includes both `atmospheric` and `ventilation-type`

**DSYS-05 (wayfinding):**
- `system.md includes wayfinding token generation with sign-hierarchy` — assert content includes both `wayfinding` and `sign-hierarchy`

**DSYS-06 (brand-coherence):**
- `system.md includes brand-coherence token generation with identity-thread` — assert content includes both `brand-coherence` and `identity-thread`

**30-token cap:**
- `system.md enforces 30-token cap on experience token file` — assert content includes `30` near `token` or `cap` (e.g., `must not exceed 30` or `30 token` or `30-token cap`)

Total: 7 describe blocks, ~10 tests. All tests MUST FAIL when run against current system.md (Wave 0 contract).

Commit the test file with message: `test(76-01): add failing Nyquist tests for DSYS-01 through DSYS-07`

Run `node --test tests/phase-76/experience-tokens.test.mjs` and confirm tests FAIL (this is expected and correct for Wave 0).
  </action>
  <verify>
    <automated>node --test tests/phase-76/experience-tokens.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/phase-76/experience-tokens.test.mjs` exists
    - File contains `import { test, describe } from 'node:test'`
    - File contains `SYS-experience-tokens.json` string assertion
    - File contains `PRODUCT_TYPE` guard ordering assertion
    - File contains assertions for all 6 categories: `sonic`, `lighting`, `spatial`, `atmospheric`, `wayfinding`, `brand-coherence`
    - File contains `bpm-range`, `zone-main-color`, `density-target`, `ventilation-type`, `sign-hierarchy`, `identity-thread`
    - File contains 30-token cap assertion
    - Running `node --test tests/phase-76/experience-tokens.test.mjs` exits non-zero (tests fail against pre-edit system.md — Wave 0 contract)
  </acceptance_criteria>
  <done>Wave 0 test scaffold committed with all DSYS assertions failing against current system.md</done>
</task>

<task type="auto">
  <name>Task 2: Add experience token generation block to system.md</name>
  <files>workflows/system.md</files>
  <read_first>
    - workflows/system.md (FULL file — must understand Step 2 stub at line 71, Step 5 token generation, Step 7 manifest registration, tokens-to-css at ~line 1599)
    - .planning/phases/76-experience-design-token-architecture/76-RESEARCH.md (architecture patterns, token schema, anti-patterns)
    - tests/phase-76/experience-tokens.test.mjs (the assertions this task must satisfy)
  </read_first>
  <action>
Edit `workflows/system.md` in three locations:

**Location 1 — Step 2 stub replacement (line 71):**

Replace the Phase 74 stub comment:
```
<!-- Experience product type: experience-specific design system extensions (SYS-experience-tokens.json) added in Phase 76. Phase 74 stub — proceed with software path as temporary fallback for experience product type. -->
```

With:
```
<!-- Experience product type: Phase 76 — generate SYS-experience-tokens.json after SYS-tokens.json (Step 5b) -->
```

IMPORTANT: The replacement MUST still contain `Phase 74` OR the text must be updated so the Phase 82 test at line 221 (which checks `content.includes('Phase 74')`) does NOT break. The safest approach: keep the string `Phase 74` somewhere in the replacement comment, e.g.:
```
<!-- Experience product type (Phase 74 architecture, Phase 76 implementation): generate SYS-experience-tokens.json after SYS-tokens.json (Step 5b). -->
```

**Location 2 — New Step 5b (insert AFTER existing Step 5 file generation, BEFORE Step 6):**

Add a new step titled `#### Step 5b: Experience token file (experience products only)`. The block must include:

1. Guard: `**If PRODUCT_TYPE == "experience":**`
2. Instruction to read the five Phase 75 brief sections: Vibe Contract, Venue Constraints, Audience Archetype, Promise Statement, Repeatability Intent
3. Generate `SYS-experience-tokens.json` as DTCG-compliant JSON with 6 top-level category keys:

**sonic** (5 tokens): `bpm-range` (string), `genre-primary` (string), `volume-curve` (string: gradual-ramp|drop-in|steady), `system-spec` (string), `transition-strategy` (string: beatmatch|silence|crossfade|live-segue)

**lighting** (5 tokens): `zone-main-color` (color, oklch), `intensity-curve` (string: slow-build|flash-on|dawn-ramp), `fixture-type-primary` (string), `peak-color` (color, oklch), `house-lights-protocol` (string: full-off|safety-only|dim-wash)

**spatial** (5 tokens): `zone-count` (number), `main-floor-mood` (string), `density-target` (number, people per sqm), `sightlines` (string), `material-palette` (string)

**atmospheric** (5 tokens): `ventilation-type` (string), `indoor-outdoor` (string: indoor|outdoor|hybrid), `haze-level` (string: none|light|heavy), `temperature-target` (string), `air-movement` (string)

**wayfinding** (5 tokens): `sign-hierarchy` (string), `arrow-standard` (string), `legibility-distance` (string), `outdoor-contrast` (string: day-bright|night-lit|mixed), `hierarchy-levels` (number)

**brand-coherence** (5 tokens): `identity-thread` (string), `tone-of-voice` (string), `sensory-signature` (string), `collateral-sequence` (string), `wristband-color` (color, oklch)

4. Include the constraint: `Total token count across all 6 categories MUST NOT exceed 30 leaf nodes (30-token cap).`
5. Each leaf node MUST have `$value`, `$type`, and `$description` fields per DTCG 2025.10.
6. Instruction to derive values from the brief: sonic from Vibe Contract energy + Venue Constraints infrastructure; lighting from Vibe Contract aesthetic register + Venue infrastructure; spatial from Audience Archetype + Venue Constraints capacity; atmospheric from Venue Constraints + Vibe Contract; wayfinding from Audience Archetype mobility + Venue capacity; brand-coherence from Promise Statement + Vibe Contract aesthetic register.
7. If brief fields contain `[PROVIDE: ...]` placeholders, propagate them verbatim as the `$value` string.
8. Write to: `.planning/design/visual/SYS-experience-tokens.json`

**Manifest registration block** (inside Step 5b, after file write):
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-EXP code SYS-EXP
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-EXP name "Experience Design Tokens"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-EXP type experience-tokens
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-EXP domain visual
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-EXP path ".planning/design/visual/SYS-experience-tokens.json"
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-update SYS-EXP status draft
```

**CSS generation block** (inside Step 5b, after manifest registration):
```bash
CSS_EXP=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design tokens-to-css ".planning/design/visual/SYS-experience-tokens.json" --raw)
if [[ "$CSS_EXP" == @file:* ]]; then CSS_EXP=$(cat "${CSS_EXP#@file:}"); fi
```
Write output to `.planning/design/visual/SYS-experience-tokens.css`.

**Location 3 — Closing the Step 5b guard:**

End the conditional block with: `**End experience token block.**`

CRITICAL ANTI-PATTERNS:
- Do NOT modify anything in Steps 3, 4, or 5 (base token generation). Experience tokens are ADDITIVE in Step 5b.
- Do NOT add sonic/lighting/spatial/atmospheric/wayfinding/brand-coherence anywhere in the SYS-tokens.json generation path.
- Do NOT create a new `--experience` flag on pde-tools.cjs — the existing `tokens-to-css` command already accepts any file path.

After editing, run:
```bash
node --test tests/phase-76/experience-tokens.test.mjs
```
All tests MUST now PASS (Wave 0 green).

Also run regression:
```bash
node --test tests/phase-74/experience-regression.test.mjs && node --test tests/phase-75/brief-extensions.test.mjs
```
Both MUST pass (no regressions).

Commit with message: `feat(76-01): add experience design token architecture to system.md`
  </action>
  <verify>
    <automated>node --test tests/phase-76/experience-tokens.test.mjs && node --test tests/phase-74/experience-regression.test.mjs && node --test tests/phase-75/brief-extensions.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SYS-experience-tokens.json' workflows/system.md` returns at least 3 (file write, manifest, css)
    - `grep -c 'PRODUCT_TYPE == "experience"' workflows/system.md` returns at least 1
    - `grep 'Step 5b' workflows/system.md` matches "Step 5b: Experience token file"
    - `grep 'sonic' workflows/system.md` matches inside Step 5b block only
    - `grep 'bpm-range' workflows/system.md` returns at least 1
    - `grep 'zone-main-color' workflows/system.md` returns at least 1
    - `grep 'density-target' workflows/system.md` returns at least 1
    - `grep 'ventilation-type' workflows/system.md` returns at least 1
    - `grep 'sign-hierarchy' workflows/system.md` returns at least 1
    - `grep 'identity-thread' workflows/system.md` returns at least 1
    - `grep 'brand-coherence' workflows/system.md` returns at least 1
    - `grep '30-token' workflows/system.md` OR `grep 'exceed 30' workflows/system.md` returns at least 1
    - `grep 'manifest-update SYS-EXP' workflows/system.md` returns at least 6 lines
    - `grep 'tokens-to-css.*SYS-experience-tokens' workflows/system.md` returns at least 1
    - `grep 'Phase 74' workflows/system.md` still returns at least 1 (keeps Phase 82 stub test passing)
    - `node --test tests/phase-76/experience-tokens.test.mjs` exits 0
    - `node --test tests/phase-74/experience-regression.test.mjs` exits 0
    - `node --test tests/phase-75/brief-extensions.test.mjs` exits 0
  </acceptance_criteria>
  <done>system.md contains complete Step 5b experience token block, all Phase 76 Nyquist tests pass, Phase 74 and 75 regressions clean</done>
</task>

</tasks>

<verification>
- `node --test tests/phase-76/experience-tokens.test.mjs` — all DSYS structural assertions green
- `node --test tests/phase-74/experience-regression.test.mjs` — Phase 74 regression clean
- `node --test tests/phase-75/brief-extensions.test.mjs` — Phase 75 regression clean
- `grep -c 'SYS-experience-tokens.json' workflows/system.md` >= 3
- `grep 'manifest-update SYS-EXP' workflows/system.md | wc -l` >= 6
</verification>

<success_criteria>
- system.md generates SYS-experience-tokens.json for experience products via Step 5b conditional block
- Six token categories with 5 tokens each (30 total) are specified with DTCG format
- Base SYS-tokens.json generation is untouched (no experience categories in Steps 3-5)
- Manifest registration and CSS generation commands present for experience file
- All Phase 76 Nyquist tests pass; all Phase 74 and 75 regression tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/76-experience-design-token-architecture/76-01-SUMMARY.md`
</output>
