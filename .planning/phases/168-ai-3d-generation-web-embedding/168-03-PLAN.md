---
phase: 168-ai-3d-generation-web-embedding
plan: 03
type: execute
wave: 3
depends_on: ["168-01", "168-02"]
files_modified:
  - bin/pde-tools.cjs
  - bin/lib/commands/3d.md
autonomous: true
requirements:
  - TRD-01
  - TRD-02
  - TRD-03
  - TRD-04
  - TRD-05
  - TRD-08

must_haves:
  truths:
    - "pde-tools.cjs 3d generate --prompt <text> invokes generate3D and prints JSON meta"
    - "pde-tools.cjs 3d convert --image <path> invokes convert3D and prints JSON meta"
    - "pde-tools.cjs 3d optimize --input <path> invokes optimizeGLB and prints result"
    - "pde-tools.cjs 3d embed --glb <path> invokes generateEmbed and prints embedPath"
    - "pde-tools.cjs 3d list invokes list3DAssets and prints JSON array"
    - "/pde:3d command documentation exists"
  artifacts:
    - path: "bin/pde-tools.cjs"
      provides: "3d case block with generate|convert|optimize|embed|list subcommands"
      contains: "case '3d'"
    - path: "bin/lib/commands/3d.md"
      provides: "Command documentation for /pde:3d"
  key_links:
    - from: "bin/pde-tools.cjs"
      to: "bin/lib/3d-pipeline/generate.cjs"
      via: "require in 3d generate subcommand"
      pattern: "require.*3d-pipeline/generate"
    - from: "bin/pde-tools.cjs"
      to: "bin/lib/3d-pipeline/convert.cjs"
      via: "require in 3d convert subcommand"
      pattern: "require.*3d-pipeline/convert"
    - from: "bin/pde-tools.cjs"
      to: "bin/lib/3d-pipeline/optimize.cjs"
      via: "require in 3d optimize subcommand"
      pattern: "require.*3d-pipeline/optimize"
    - from: "bin/pde-tools.cjs"
      to: "bin/lib/3d-pipeline/embed.cjs"
      via: "require in 3d embed subcommand"
      pattern: "require.*3d-pipeline/embed"
    - from: "bin/pde-tools.cjs"
      to: "bin/lib/3d-pipeline/assets.cjs"
      via: "require in 3d list subcommand"
      pattern: "require.*3d-pipeline/assets"
---

<objective>
Wire all 3D pipeline modules into pde-tools.cjs as the `3d` case block with subcommands, and create the /pde:3d command documentation.

Purpose: Make the 3D pipeline accessible as CLI commands that users invoke through PDE.
Output: Updated pde-tools.cjs with 3d routing, commands/3d.md documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-ai-3d-generation-web-embedding/168-CONTEXT.md
@.planning/phases/168-ai-3d-generation-web-embedding/168-01-SUMMARY.md
@.planning/phases/168-ai-3d-generation-web-embedding/168-02-SUMMARY.md

@bin/pde-tools.cjs (lines 836-915 for video case block pattern)

<interfaces>
<!-- All 3D pipeline modules from Plans 01 and 02 -->

From bin/lib/3d-pipeline/assets.cjs:
```javascript
function save3DAsset({ slug, glbBuffer, meta, assetsDir }) → { glbPath, metaPath, meta }
function list3DAssets({ assetsDir }) → object[]
const THREE_D_DIR; // .planning/design/3d
module.exports = { save3DAsset, list3DAssets, THREE_D_DIR };
```

From bin/lib/3d-pipeline/optimize.cjs:
```javascript
function optimizeGLB({ inputPath, outputPath, textureMaxSize }) → { outputPath, sizeMB }
function inspectGLB({ glbPath }) → { vertex_count, file_size }
module.exports = { optimizeGLB, inspectGLB };
```

From bin/lib/3d-pipeline/embed.cjs:
```javascript
function generateEmbed({ glbPath, slug, cameraOrbit, assetsDir }) → { snippet, html, embedPath }
module.exports = { generateEmbed };
```

From bin/lib/3d-pipeline/convert.cjs:
```javascript
async function convert3D({ imageBuffer, slug, assetsDir }) → { glbPath, metaPath, meta }
module.exports = { convert3D, SPACE_CHAIN };
```

From bin/lib/3d-pipeline/generate.cjs:
```javascript
async function generate3D({ prompt, slug, assetsDir }) → { glbPath, metaPath, meta }
module.exports = { generate3D };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3d case block to pde-tools.cjs</name>
  <files>bin/pde-tools.cjs</files>
  <read_first>
    bin/pde-tools.cjs
    bin/lib/3d-pipeline/generate.cjs
    bin/lib/3d-pipeline/convert.cjs
    bin/lib/3d-pipeline/optimize.cjs
    bin/lib/3d-pipeline/embed.cjs
    bin/lib/3d-pipeline/assets.cjs
  </read_first>
  <action>
    Add `case '3d':` block AFTER the `case 'video':` block and BEFORE the `case 'phase-plan-index':` block (per STATE.md decision: "3d case block placed after video, before phase-plan-index").

    Follow the exact routing pattern from the video case block:

    ```javascript
    case '3d': {
      const subcommand = args[1];
      if (subcommand === 'generate') {
        const { generate3D } = require('./lib/3d-pipeline/generate.cjs');
        const { THREE_D_DIR } = require('./lib/3d-pipeline/assets.cjs');
        const promptIdx = args.indexOf('--prompt');
        const slugIdx = args.indexOf('--slug');
        const prompt = promptIdx !== -1 ? args[promptIdx + 1] : undefined;
        if (!prompt) { console.error('Usage: 3d generate --prompt <text> [--slug <slug>]'); process.exit(1); }
        const slug = slugIdx !== -1 ? args[slugIdx + 1] : 'model';
        const result = await generate3D({ prompt, slug, assetsDir: THREE_D_DIR });
        console.log(JSON.stringify(result.meta, null, 2));
      } else if (subcommand === 'convert') {
        const { convert3D } = require('./lib/3d-pipeline/convert.cjs');
        const { THREE_D_DIR } = require('./lib/3d-pipeline/assets.cjs');
        const fs = require('fs');
        const imageIdx = args.indexOf('--image');
        const slugIdx = args.indexOf('--slug');
        const imagePath = imageIdx !== -1 ? args[imageIdx + 1] : undefined;
        if (!imagePath) { console.error('Usage: 3d convert --image <path> [--slug <slug>]'); process.exit(1); }
        const imageBuffer = fs.readFileSync(imagePath);
        const slug = slugIdx !== -1 ? args[slugIdx + 1] : 'model';
        const result = await convert3D({ imageBuffer, slug, assetsDir: THREE_D_DIR });
        console.log(JSON.stringify(result.meta, null, 2));
      } else if (subcommand === 'optimize') {
        const { optimizeGLB } = require('./lib/3d-pipeline/optimize.cjs');
        const inputIdx = args.indexOf('--input');
        const outputIdx = args.indexOf('--output');
        const texIdx = args.indexOf('--texture-max');
        const inputPath = inputIdx !== -1 ? args[inputIdx + 1] : undefined;
        if (!inputPath) { console.error('Usage: 3d optimize --input <path.glb> [--output <path.glb>] [--texture-max <px>]'); process.exit(1); }
        const outputPath = outputIdx !== -1 ? args[outputIdx + 1] : inputPath.replace('.glb', '-opt.glb');
        const textureMaxSize = texIdx !== -1 ? parseInt(args[texIdx + 1], 10) : 1024;
        const result = optimizeGLB({ inputPath, outputPath, textureMaxSize });
        console.log(JSON.stringify(result, null, 2));
      } else if (subcommand === 'embed') {
        const { generateEmbed } = require('./lib/3d-pipeline/embed.cjs');
        const { THREE_D_DIR } = require('./lib/3d-pipeline/assets.cjs');
        const glbIdx = args.indexOf('--glb');
        const slugIdx = args.indexOf('--slug');
        const orbitIdx = args.indexOf('--camera-orbit');
        const glbPath = glbIdx !== -1 ? args[glbIdx + 1] : undefined;
        if (!glbPath) { console.error('Usage: 3d embed --glb <path.glb> [--slug <slug>] [--camera-orbit <orbit>]'); process.exit(1); }
        const slug = slugIdx !== -1 ? args[slugIdx + 1] : 'model';
        const cameraOrbit = orbitIdx !== -1 ? args[orbitIdx + 1] : undefined;
        const result = generateEmbed({ glbPath, slug, cameraOrbit, assetsDir: THREE_D_DIR });
        console.log(JSON.stringify({ embedPath: result.embedPath, snippet: result.snippet }, null, 2));
      } else if (subcommand === 'list') {
        const { list3DAssets, THREE_D_DIR } = require('./lib/3d-pipeline/assets.cjs');
        const assets = list3DAssets({ assetsDir: THREE_D_DIR });
        console.log(JSON.stringify(assets, null, 2));
      } else {
        console.error('Usage: 3d <generate|convert|optimize|embed|list> [options]');
        process.exit(1);
      }
      break;
    }
    ```

    Key details:
    - All subcommands use args.indexOf() for flag parsing (consistent with video pattern)
    - generate and convert pass assetsDir: THREE_D_DIR for consistent output location
    - Missing required args print usage and exit 1
    - Output is always JSON via console.log(JSON.stringify(...))
    - convert reads image file to Buffer before passing to convert3D
  </action>
  <verify>
    <automated>node bin/pde-tools.cjs 3d 2>&1 | grep -q "Usage: 3d" && echo "OK" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "case '3d'" bin/pde-tools.cjs
    - grep -q "3d-pipeline/generate" bin/pde-tools.cjs
    - grep -q "3d-pipeline/convert" bin/pde-tools.cjs
    - grep -q "3d-pipeline/optimize" bin/pde-tools.cjs
    - grep -q "3d-pipeline/embed" bin/pde-tools.cjs
    - grep -q "3d-pipeline/assets" bin/pde-tools.cjs
    - grep -q "generate|convert|optimize|embed|list" bin/pde-tools.cjs
    - node bin/pde-tools.cjs 3d 2>&1 exits with usage message
    - node bin/pde-tools.cjs 3d generate 2>&1 exits with usage message (no --prompt)
  </acceptance_criteria>
  <done>pde-tools.cjs has a 3d case block after video and before phase-plan-index. All five subcommands route to the correct 3d-pipeline modules. Missing args produce usage errors with exit 1.</done>
</task>

<task type="auto">
  <name>Task 2: Create /pde:3d command documentation</name>
  <files>bin/lib/commands/3d.md</files>
  <read_first>
    bin/pde-tools.cjs
  </read_first>
  <action>
    Create `bin/lib/commands/3d.md` documenting the /pde:3d command. Follow the pattern of existing command docs. Include:

    - Command name: `/pde:3d`
    - Description: Generate 3D models from text or images, optimize GLB files for web, embed with model-viewer AR viewer
    - Prerequisites: HF_TOKEN environment variable (free Hugging Face access token from https://huggingface.co/settings/tokens)

    - Subcommands:
      1. `3d generate --prompt <text> [--slug <slug>]` — Text-to-3D: generates image from text via FLUX.1-schnell, then converts to GLB via HF Space
      2. `3d convert --image <path> [--slug <slug>]` — Image-to-3D: converts product image to GLB via HF Space (SF3D/InstantMesh)
      3. `3d optimize --input <path.glb> [--output <path.glb>] [--texture-max <px>]` — Optimize GLB with Draco compression and texture resize (target <5MB)
      4. `3d embed --glb <path.glb> [--slug <slug>] [--camera-orbit <orbit>]` — Generate model-viewer HTML embed with AR fallback (WebXR + iOS Quick Look)
      5. `3d list` — List all 3D assets in .planning/design/3d/ with metadata

    - Output: All commands output JSON. GLB files and metadata stored in `.planning/design/3d/`
    - Notes:
      - HF Spaces can be intermittently unavailable; the pipeline tries SF3D first, then InstantMesh
      - model-viewer handles iOS USDZ auto-generation — no server-side USDZ needed
      - For local GPU inference (optional): document Python TripoSR setup at https://github.com/VAST-AI-Research/TripoSR
  </action>
  <verify>
    <automated>test -f bin/lib/commands/3d.md && grep -q "pde:3d" bin/lib/commands/3d.md && echo "OK" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - test -f bin/lib/commands/3d.md
    - grep -q "pde:3d" bin/lib/commands/3d.md
    - grep -q "HF_TOKEN" bin/lib/commands/3d.md
    - grep -q "generate" bin/lib/commands/3d.md
    - grep -q "convert" bin/lib/commands/3d.md
    - grep -q "optimize" bin/lib/commands/3d.md
    - grep -q "embed" bin/lib/commands/3d.md
    - grep -q "model-viewer" bin/lib/commands/3d.md
  </acceptance_criteria>
  <done>/pde:3d command doc exists with all five subcommands documented, HF_TOKEN prerequisite noted, and AR fallback behavior explained.</done>
</task>

</tasks>

<verification>
node bin/pde-tools.cjs 3d 2>&1 | grep -q "Usage"
node bin/pde-tools.cjs 3d generate 2>&1 | grep -q "Usage"
node bin/pde-tools.cjs 3d convert 2>&1 | grep -q "Usage"
node bin/pde-tools.cjs 3d optimize 2>&1 | grep -q "Usage"
node bin/pde-tools.cjs 3d embed 2>&1 | grep -q "Usage"
test -f bin/lib/commands/3d.md
npx vitest run tests/phase-168/ --reporter=verbose
</verification>

<success_criteria>
- pde-tools.cjs routes 3d subcommands to correct pipeline modules
- All subcommands print usage on missing args and exit 1
- 3d list prints JSON array (empty is fine)
- /pde:3d command documentation covers all subcommands
- All existing phase-168 tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/168-ai-3d-generation-web-embedding/168-03-SUMMARY.md`
</output>
