---
phase: 87-flows-stage
plan: 02
type: execute
wave: 2
depends_on:
  - "87-01"
files_modified:
  - workflows/flows.md
autonomous: true
requirements:
  - OPS-02
  - OPS-03
must_haves:
  truths:
    - "flows.md designCoverage write includes all 20 fields, not 16"
    - "hasServiceBlueprint is set to true when SBP was generated, passed through otherwise"
    - "hasLaunchKit field is present in the coverage write (even though it defaults to current value)"
    - "Running /pde:flows after /pde:competitive does not erase hasMarketLandscape or hasBusinessThesis"
  artifacts:
    - path: "workflows/flows.md"
      provides: "20-field designCoverage write in Step 7, hasServiceBlueprint flag, strategy DESIGN-STATE update"
      contains: "hasLaunchKit"
  key_links:
    - from: "workflows/flows.md Step 7"
      to: "design-manifest.json designCoverage"
      via: "manifest-set-top-level designCoverage"
      pattern: "hasServiceBlueprint.*hasLaunchKit"
---

<objective>
Upgrade flows.md Step 7 designCoverage write from 16 fields to 20 fields, set hasServiceBlueprint conditionally, and update strategy domain DESIGN-STATE for SBP/GTM artifacts.

Purpose: OPS-03 requires hasServiceBlueprint flag in designCoverage. The existing 16-field write is a regression from pre-Phase 84 that would erase hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, and hasLaunchKit if left unfixed. This plan also completes the strategy DESIGN-STATE wiring for SBP and GTM artifacts.
Output: flows.md Step 7 upgraded to 20-field coverage write with hasServiceBlueprint. All 10 Nyquist tests pass.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-flows-stage/87-RESEARCH.md
@.planning/phases/87-flows-stage/87-01-SUMMARY.md

<interfaces>
<!-- Current 16-field coverage write in flows.md Step 7 (lines 799-802) that must be replaced -->

From workflows/flows.md (line 799-802) — the BROKEN 16-field write:
```markdown
Merge `hasFlows: true` into the existing values, then write the full sixteen-field object (all flags must be present — default any absent field to `false`):

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":true,"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":{current},"hasProductionBible":{current}}'
```

<!-- Canonical 20-field order (from competitive.md Step 7): -->
hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit

<!-- From workflows/competitive.md Step 7 — the authoritative 20-field pattern to match -->
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":true,"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":{current},"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{MLS_WRITTEN_VALUE},"hasServiceBlueprint":{current},"hasLaunchKit":{current}}'
```

<!-- Step 7 coverage-check read pattern (from flows.md line 775-778) -->
```bash
COV=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design coverage-check)
if [[ "$COV" == @file:* ]]; then COV=$(cat "${COV#@file:}"); fi
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Step 7 designCoverage write from 16 to 20 fields and add strategy DESIGN-STATE updates</name>
  <files>workflows/flows.md</files>
  <read_first>
    - workflows/flows.md (lines 670-860 — full Step 7 section including coverage-check, field list, and write command)
    - workflows/competitive.md (Step 7 — authoritative 20-field coverage write pattern, strategy DESIGN-STATE update pattern)
    - .planning/phases/87-flows-stage/87-01-SUMMARY.md (what Plan 01 changed — insertion points to avoid conflicts)
  </read_first>
  <action>
Modify `workflows/flows.md` Step 7 with three targeted edits using the Edit tool.

**Edit 1: Replace the 16-field coverage write with 20-field version**

Find the block that starts with `Merge hasFlows: true into the existing values, then write the full sixteen-field object` (approximately lines 799-802 in the original, may have shifted due to Plan 01 insertions).

Replace the entire block (the text line + the bash code block) with:

```markdown
Merge `hasFlows: true` and `hasServiceBlueprint: {SBP_WRITTEN_VALUE}` into the existing values, then write the full twenty-field object (all flags must be present — default any absent field to `false`):

Where `{SBP_WRITTEN_VALUE}` is `true` if `SBP_WRITTEN == true` (from Step 5-BIZ), else pass through the current value from COV output.

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage '{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":true,"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current},"hasStitchWireframes":{current},"hasPrintCollateral":{current},"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{SBP_WRITTEN_VALUE},"hasLaunchKit":{current}}'
```
```

**Edit 2: Add 4 new coverage fields to the field extraction list**

Find the list of fields being extracted from COV output (the bullet list starting with `- hasDesignSystem` around line 776). After the last existing field `- hasProductionBible`, add:

```markdown
- `hasBusinessThesis` — current value from COV output (default false if absent)
- `hasMarketLandscape` — current value from COV output (default false if absent)
- `hasServiceBlueprint` — current value from COV output (default false if absent)
- `hasLaunchKit` — current value from COV output (default false if absent)
```

**Edit 3: Add strategy DESIGN-STATE update for SBP and GTM artifacts**

Find the section in Step 7 that updates root DESIGN-STATE.md (the four numbered update items: Cross-Domain Dependency Map, Quick Reference, Decision Log, Iteration History). After the existing items (which handle FLW), add a business-mode conditional block:

```markdown
**IF `SBP_WRITTEN == true`:**

5. **Cross-Domain Dependency Map** — add SBP row if not already present:
   ```
   | SBP | strategy | FLW | current |
   ```

6. **Quick Reference section** — add SBP row:
   ```
   | Service Blueprint | v{N} |
   ```

7. **Decision Log** — append entry:
   ```
   | SBP | service blueprint generated, {stage_count} stages, {track} track | {YYYY-MM-DD} |
   ```

8. **Iteration History** — append entry:
   ```
   | SBP-service-blueprint-v{N}.md | v{N} | Created by /pde:flows (SBP) | {YYYY-MM-DD} |
   ```

**IF `GTM_CONTENT_GENERATED == true`:**

Append GTM rows to each of the four sections above:

- Cross-Domain: `| GTM | strategy | SBP | current |`
- Quick Reference: `| GTM Channel Flow | v{N} |`
- Decision Log: `| GTM | GTM channel flow generated, {channel_count} channels | {YYYY-MM-DD} |`
- Iteration History: `| GTM-channel-flow-v{N}.md | v{N} | Created by /pde:flows (GTM) | {YYYY-MM-DD} |`
```

Also update the experience product DESIGN-STATE block. Find `**If PRODUCT_TYPE == "experience":**` in Step 7 (around line 742). After the experience artifact registration block AND before the coverage write, add:

```markdown
**IF `PRODUCT_TYPE == "experience"` AND `$BM == "true"`:** Also register SBP and GTM artifacts (from Step 5-BIZ) using the same 7-call pattern above. Experience+business compositions produce both experience flow artifacts AND business flow artifacts.
```

After all edits, run the full test suite:
```bash
node --test .planning/phases/87-flows-stage/tests/test-flows-sbp.cjs
```

All 10 tests should now pass (GREEN state). Then run:
```bash
npm test
```

To verify no regressions in prior phase tests.
  </action>
  <verify>
    <automated>node --test .planning/phases/87-flows-stage/tests/test-flows-sbp.cjs && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - workflows/flows.md contains "twenty-field object" (not "sixteen-field")
    - workflows/flows.md contains all 20 field names in a single designCoverage write: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit
    - workflows/flows.md contains `hasServiceBlueprint` extraction from COV output
    - workflows/flows.md contains `hasMarketLandscape` extraction from COV output
    - workflows/flows.md contains `hasBusinessThesis` extraction from COV output
    - workflows/flows.md contains `hasLaunchKit` extraction from COV output
    - workflows/flows.md contains `SBP | strategy | FLW` in Cross-Domain Dependency Map
    - workflows/flows.md contains `GTM | strategy | SBP` in Cross-Domain Dependency Map
    - All 10 tests in test-flows-sbp.cjs pass (GREEN state)
    - `npm test` passes with no regressions
  </acceptance_criteria>
  <done>flows.md Step 7 upgraded from 16-field to 20-field designCoverage write. hasServiceBlueprint flag set conditionally. Strategy DESIGN-STATE updated for SBP and GTM artifacts. All 10 Nyquist tests pass. No regressions in npm test.</done>
</task>

</tasks>

<verification>
node --test .planning/phases/87-flows-stage/tests/test-flows-sbp.cjs
npm test
</verification>

<success_criteria>
- All 10 structural assertions pass (GREEN state)
- flows.md coverage write has exactly 20 fields
- npm test passes with no regressions from prior phases
- Strategy DESIGN-STATE update includes SBP and GTM artifact rows
</success_criteria>

<output>
After completion, create `.planning/phases/87-flows-stage/87-02-SUMMARY.md`
</output>
