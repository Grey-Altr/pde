---
phase: 104-self-improvement-presets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/nyquist-metric.cjs
  - workflows/optimize.md
  - tests/phase-104/nyquist-metric.test.mjs
  - tests/phase-104/experiment-self-preset.test.mjs
  - tests/phase-104/experiment-skill-preset.test.mjs
autonomous: true
requirements: [SELF-01, SELF-02, SELF-03]

must_haves:
  truths:
    - "/pde:optimize --self auto-discovers 14 OPTIMIZABLE workflow files and generates a valid experiment.md with nyquist_pass_count metric"
    - "/pde:optimize --skill brief targets workflows/brief.md and generates a valid experiment.md scoped to that single file"
    - "/pde:optimize --skill unknown aborts with a clear error listing known skill names"
    - "bin/nyquist-metric.cjs runs the Nyquist suite, extracts the pass count, prints a single integer as stdout last line, and exits 0"
    - "The generated experiment.md passes parseExperimentFile validation (all required fields present)"
  artifacts:
    - path: "bin/nyquist-metric.cjs"
      provides: "Nyquist pass count extraction for experiment metric evaluation"
      contains: "# pass"
    - path: "workflows/optimize.md"
      provides: "Preset resolution in Step 1 for --self and --skill flags"
      contains: "pde-self-improve"
    - path: "tests/phase-104/nyquist-metric.test.mjs"
      provides: "Tests for nyquist-metric.cjs"
    - path: "tests/phase-104/experiment-self-preset.test.mjs"
      provides: "Tests for --self preset resolution and OPTIMIZABLE autodiscovery"
    - path: "tests/phase-104/experiment-skill-preset.test.mjs"
      provides: "Tests for --skill preset resolution"
  key_links:
    - from: "workflows/optimize.md"
      to: "bin/nyquist-metric.cjs"
      via: "verify field in generated experiment.md frontmatter"
      pattern: "verify.*nyquist-metric"
    - from: "workflows/optimize.md"
      to: "references/experiment-boundaries.md"
      via: "auto-discovery of OPTIMIZABLE files cross-checked against authorized list"
      pattern: "OPTIMIZABLE"
    - from: "bin/nyquist-metric.cjs"
      to: "tests/"
      via: "spawnSync('node', ['--test', 'tests/'])"
      pattern: "spawnSync.*--test"
---

<objective>
Implement --self and --skill preset modes for /pde:optimize, completing the self-improvement preset infrastructure.

Purpose: Users can invoke PDE self-optimization with a single flag instead of manually authoring experiment.md files. The system auto-discovers eligible files, applies the Nyquist pass count as regression guard, and feeds a generated experiment.md into the existing 9-step loop.

Output: bin/nyquist-metric.cjs metric helper, extended workflows/optimize.md Step 1 with preset resolution, and structural+unit tests for all 3 requirements.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-self-improvement-presets/104-RESEARCH.md

@.planning/phases/103-orchestrator-command-circuit-breakers/103-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From workflows/optimize.md Step 1 (current stub, lines 14-22):
```markdown
If `--self` or `--skill` was passed WITHOUT an explicit experiment.md path:
- Display: "Preset mode (--self / --skill) is not yet implemented. Please provide an experiment.md path directly."
- Abort.
```
The executor must REPLACE this stub with the preset resolution logic.

From bin/lib/experiment-schema.cjs (required fields):
```javascript
// REQUIRED_FIELDS: metric, direction, verify, mutable_files
// parseExperimentFile(path) validates these fields
```

From bin/lib/experiment-runner.cjs (_evalMetric contract):
```javascript
// _evalMetric reads LAST LINE of stdout as a float
// Non-zero exit code = CRASH status
// The verify command MUST exit 0 and print the metric value as the last stdout line
```

From references/experiment-boundaries.md (14 authorized OPTIMIZABLE files):
```
workflows/brief.md, workflows/system.md, workflows/flows.md,
workflows/ideate.md, workflows/wireframe.md, workflows/critique.md,
workflows/hig.md, workflows/iterate.md, workflows/recommend.md,
workflows/mockup.md, workflows/competitive.md, workflows/opportunity.md,
workflows/handoff.md, workflows/deploy.md
```

From templates/experiment.md (frontmatter schema):
```yaml
slug: <string>
metric: <string>
direction: max|min
verify: <command>
mutable_files: [<paths>]
immutable_files: []
iteration_budget: <int>
time_budget_minutes: <int>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create nyquist-metric.cjs and implement --self/--skill preset resolution in optimize.md</name>
  <files>bin/nyquist-metric.cjs, workflows/optimize.md, tests/phase-104/nyquist-metric.test.mjs, tests/phase-104/experiment-self-preset.test.mjs, tests/phase-104/experiment-skill-preset.test.mjs</files>
  <read_first>
    - workflows/optimize.md (full file — understand Step 1 structure and the abort stub at lines 14-22)
    - bin/lib/experiment-runner.cjs (lines 69-106 — _evalMetric contract: last line stdout as float, exit 0 required)
    - bin/lib/experiment-schema.cjs (lines 35-44 — REQUIRED_FIELDS and parseExperimentFile contract)
    - references/experiment-boundaries.md (full file — 14 authorized OPTIMIZABLE files, infrastructure_workflows list)
    - templates/experiment.md (frontmatter schema)
    - commands/optimize.md (slash command entry point)
    - .planning/phases/104-self-improvement-presets/104-RESEARCH.md (architecture patterns and pitfalls)
  </read_first>
  <behavior>
    nyquist-metric.cjs:
    - Test: bin/nyquist-metric.cjs exists and is valid JavaScript (require does not throw)
    - Test: Script contains spawnSync call with 'node' and '--test' arguments
    - Test: Script contains regex matching '# pass (\\d+)' pattern
    - Test: Script always calls process.exit(0) — never non-zero exit
    - Test: When no match found, outputs '0' as fallback

    --self preset (experiment-self-preset.test.mjs):
    - Test: workflows/optimize.md Step 1 contains '--self' preset resolution logic (not the abort stub)
    - Test: workflows/optimize.md contains 'pde-self-improve' slug
    - Test: workflows/optimize.md contains 'nyquist_pass_count' metric reference
    - Test: workflows/optimize.md contains 'nyquist-metric' verify command reference
    - Test: workflows/optimize.md contains auto-discovery via grep for OPTIMIZABLE markers
    - Test: workflows/optimize.md references the 14 authorized workflow files from experiment-boundaries.md

    --skill preset (experiment-skill-preset.test.mjs):
    - Test: workflows/optimize.md Step 1 contains '--skill' preset resolution logic (not the abort stub)
    - Test: workflows/optimize.md contains skill name validation against known workflow names
    - Test: workflows/optimize.md contains error message for unknown skill names listing available skills
    - Test: workflows/optimize.md contains single-file targeting pattern (workflows/{name}.md)
  </behavior>
  <action>
    **Part A: Create bin/nyquist-metric.cjs**

    Create a CJS script that:
    1. Uses `const { spawnSync } = require('child_process');`
    2. Runs `spawnSync('node', ['--test', 'tests/'], { encoding: 'utf-8', stdio: 'pipe', cwd: process.cwd() })`
    3. Parses stdout for `/^# pass (\d+)/m` regex match
    4. If match found: prints the integer as last line of stdout via `process.stdout.write(String(passCount) + '\n')`
    5. If no match: prints '0'
    6. ALWAYS exits with `process.exit(0)` — critical: _evalMetric treats non-zero exit as CRASH

    Add `#!/usr/bin/env node` shebang and `'use strict';` directive.

    **Part B: Extend workflows/optimize.md Step 1 with preset resolution**

    In Step 1 of workflows/optimize.md, REPLACE the abort stub (lines 20-22 that say "Preset mode is not yet implemented") with:

    **For --self flag:**
    1. Auto-discover mutable_files by running: `grep -rl '<!-- OPTIMIZABLE -->' workflows/ | sort`
    2. Cross-reference discovered files against the 14 authorized files listed in the "Experiment-Eligible Workflow Files" section of references/experiment-boundaries.md — use ONLY the intersection
    3. Construct preset experiment.md with frontmatter:
       - slug: pde-self-improve
       - metric: nyquist_pass_count
       - direction: max
       - verify: node bin/nyquist-metric.cjs
       - mutable_files: [... 14 discovered files ...]
       - immutable_files: []
       - iteration_budget: 20 (conservative for self-improvement)
       - time_budget_minutes: 60
    4. Include search space prose documenting the optimization target (workflow prose in OPTIMIZABLE sections) and constraints (Nyquist pass count baseline ~1075, full suite ~3.5s runtime)
    5. Write the generated experiment.md to `/tmp/pde-self-improve-experiment.md`
    6. Set the experiment.md path variable to this temp file and continue to field validation

    **For --skill flag:**
    1. Extract the skill name from the flag argument
    2. Define the known skills list: the 14 workflow names derived from the authorized files (brief, system, flows, ideate, wireframe, critique, hig, iterate, recommend, mockup, competitive, opportunity, handoff, deploy)
    3. If the skill name is NOT in the known list, abort with: "Unknown skill '{name}'. Known skills: brief, system, flows, ideate, wireframe, critique, hig, iterate, recommend, mockup, competitive, opportunity, handoff, deploy"
    4. Construct preset experiment.md targeting `workflows/{name}.md` as the single mutable file
    5. Use slug: `pde-skill-{name}`
    6. Use metric: nyquist_pass_count, direction: max, verify: node bin/nyquist-metric.cjs
    7. Use iteration_budget: 20, time_budget_minutes: 30 (single file = less budget needed)
    8. Write to `/tmp/pde-skill-{name}-experiment.md`
    9. Set the experiment.md path and continue to field validation

    IMPORTANT: Do NOT touch any logic in Steps 2-9 of the workflow. The preset resolution happens entirely in Step 1 and produces a path that flows into the existing validation and loop.

    **Part C: Write tests (TDD — write tests FIRST, then implement)**

    Create `tests/phase-104/` directory with 3 test files:

    1. `nyquist-metric.test.mjs`: Structural tests confirming bin/nyquist-metric.cjs exists, contains spawnSync, contains # pass regex, contains process.exit(0).

    2. `experiment-self-preset.test.mjs`: Structural tests confirming workflows/optimize.md contains --self preset resolution (not abort stub), pde-self-improve slug, nyquist_pass_count metric, nyquist-metric verify reference, OPTIMIZABLE auto-discovery, and references to the 14 authorized workflow files.

    3. `experiment-skill-preset.test.mjs`: Structural tests confirming workflows/optimize.md contains --skill preset resolution (not abort stub), skill name validation, error message with known skills list, and single-file targeting via workflows/{name}.md pattern.

    All tests use `node:test` and `node:assert` (the project standard). Tests are structural — they read file contents and assert patterns.
  </action>
  <verify>
    <automated>node --test tests/phase-104/</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "spawnSync" bin/nyquist-metric.cjs
    - grep -q "# pass" bin/nyquist-metric.cjs
    - grep -q "process.exit(0)" bin/nyquist-metric.cjs
    - grep -q "pde-self-improve" workflows/optimize.md
    - grep -q "nyquist_pass_count" workflows/optimize.md
    - grep -q "nyquist-metric" workflows/optimize.md
    - grep -q "OPTIMIZABLE" workflows/optimize.md
    - grep -q "Unknown skill" workflows/optimize.md
    - grep -q "pde-skill-" workflows/optimize.md
    - ! grep -q "Preset mode.*is not yet implemented" workflows/optimize.md
    - node --test tests/phase-104/ exits 0
  </acceptance_criteria>
  <done>
    bin/nyquist-metric.cjs exists, runs Nyquist suite, extracts pass count, always exits 0.
    workflows/optimize.md Step 1 resolves --self to a generated experiment.md with all 14 OPTIMIZABLE files and nyquist_pass_count metric.
    workflows/optimize.md Step 1 resolves --skill {name} to a generated experiment.md scoped to a single workflow file.
    Unknown skill names produce a clear error listing all 14 known skills.
    The "Preset mode is not yet implemented" abort stub is fully replaced.
    All phase-104 structural tests pass.
  </done>
</task>

</tasks>

<verification>
1. `node --test tests/phase-104/` — all new structural tests pass
2. `node --test tests/` — full Nyquist suite shows zero regressions (pass count >= 1075)
3. `grep -q "Preset mode.*is not yet implemented" workflows/optimize.md` returns non-zero (stub removed)
4. `node bin/nyquist-metric.cjs` runs and outputs an integer on the last line with exit code 0
</verification>

<success_criteria>
- /pde:optimize --self would auto-discover 14 OPTIMIZABLE workflow files and generate a valid experiment.md with slug pde-self-improve, metric nyquist_pass_count, verify node bin/nyquist-metric.cjs
- /pde:optimize --skill brief would generate experiment.md targeting only workflows/brief.md
- /pde:optimize --skill nonexistent would abort with a clear error listing all 14 known skill names
- bin/nyquist-metric.cjs runs the Nyquist suite and outputs an integer pass count, always exiting 0
- All phase-104 tests pass, full Nyquist suite has zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/104-self-improvement-presets/104-01-SUMMARY.md`
</output>
