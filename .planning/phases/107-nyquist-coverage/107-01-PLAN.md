---
phase: 107
plan: 01
name: nyquist-coverage
type: execution
autonomous: true
wave: 1
depends_on: [106-01, 106-02]
requirements: [INTG-01, INTG-02, INTG-03, INTG-04]
---

# Phase 107 Plan 01: Nyquist Coverage for Experiment Infrastructure

## Objective

Write structural Nyquist tests for the v0.13 experiment infrastructure and confirm zero regressions in existing PDE workflows.

## Context

The experiment infrastructure (Phases 99-106) adds git state machine, boundary enforcement, circuit breakers, metric evaluation, orchestration, self-improvement presets, and observability. All must be covered by structural regression tests before the milestone ships.

## Tasks

### Task 1: Create phase-107 test directory and experiment-infrastructure Nyquist tests

**Type:** auto
**Done criteria:**
- `tests/phase-107/` directory exists
- `experiment-infrastructure.test.mjs` covers INTG-01..04:
  - Boundary enforcement: `_checkBoundaries` rejects out-of-bounds files (INTG-04)
  - Reset safety: `_reset` rejects non-experiment commits (INTG-04)
  - Circuit breaker precision: consecutive-failure breaker fires at exactly K regressions (INTG-04)
  - No-progress breaker fires at exactly M iterations without improvement (INTG-04)
  - Metric timeout produces CRASH status (INTG-04)
  - Experiment commits do not appear in main branch git log (INTG-03)
  - INTG-02: Nyquist-fail commits discarded — boundary check pattern verified structurally
  - INTG-01: nyquist-metric.cjs always exits 0 and outputs parseable float
- Minimum 20 new assertions

### Task 2: Confirm zero regressions — run full test suite

**Type:** auto
**Done criteria:**
- `node --test tests/` runs cleanly with all pre-existing tests still passing
- New phase-107 tests also pass
- Total pass count documented in SUMMARY

## Verification

- `node --test tests/phase-107/` passes with 20+ assertions
- `node --test tests/` overall — no regressions (all pre-existing tests pass)

## Success Criteria

1. Full test suite passes with experiment infrastructure present — zero regressions from v0.13 additions
2. A Nyquist test verifies that experiment commits that fail Nyquist (boundary check pattern) are discarded
3. Structural tests confirm boundary check rejects out-of-bounds file modifications, no-progress breaker halts at exactly M iterations, consecutive-failure breaker halts at exactly K regressions, experiment commits do not appear in main branch git log
4. New Nyquist assertions cover experiment infrastructure — at minimum 20 new assertions
