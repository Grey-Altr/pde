---
phase: 107-nyquist-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/optimize.md
  - tests/phase-107/experiment-nyquist-integrity.test.mjs
  - tests/phase-107/experiment-regression-guard.test.mjs
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03, INTG-04]

must_haves:
  truths:
    - "Nyquist pre-promote guard exists in optimize.md Step 9 before experiment promote"
    - "Structural tests verify Nyquist guard is present and ordered correctly"
    - "Behavioral tests verify boundary enforcement, reset guard, metric timeout, circuit breaker triggers"
    - "Full Nyquist suite passes with 235+ assertions and zero new regressions"
    - "At least 20 new assertions exist across the two test files"
  artifacts:
    - path: "workflows/optimize.md"
      provides: "Nyquist pre-promote guard in Step 9"
      contains: "nyquist-metric"
    - path: "tests/phase-107/experiment-nyquist-integrity.test.mjs"
      provides: "Structural tests for INTG-01, INTG-02"
      min_lines: 50
    - path: "tests/phase-107/experiment-regression-guard.test.mjs"
      provides: "Behavioral tests for INTG-03, INTG-04"
      min_lines: 100
  key_links:
    - from: "workflows/optimize.md"
      to: "bin/nyquist-metric.cjs"
      via: "Step 9 runs nyquist-metric before promote"
      pattern: "nyquist-metric.*experiment promote"
    - from: "tests/phase-107/experiment-nyquist-integrity.test.mjs"
      to: "workflows/optimize.md"
      via: "fs.readFileSync structural assertions"
      pattern: "readFileSync.*optimize"
    - from: "tests/phase-107/experiment-regression-guard.test.mjs"
      to: "bin/lib/experiment.cjs"
      via: "createRequire import of _checkBoundaries"
      pattern: "createRequire.*experiment\\.cjs"
---

<objective>
Add the Nyquist pre-promote guard to optimize.md and write 20+ new Nyquist assertions covering experiment infrastructure safety properties.

Purpose: Ensure experiment commits cannot be promoted to main if they regress the Nyquist suite, and structurally verify all safety-critical experiment behaviors (boundary enforcement, circuit breakers, reset guards, metric timeout).

Output: Amended optimize.md with Nyquist hard-floor check, two new test files with 20+ assertions total.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-nyquist-coverage/107-RESEARCH.md

<interfaces>
<!-- Key modules and exports the executor needs -->

From bin/lib/experiment.cjs (exports):
```javascript
module.exports = {
  _init, _commitCandidate, _resetToBaseline, _promoteBest,
  _status, _cleanup, _checkBoundaries,
  // cmd* wrappers (do NOT test directly — they call process.exit)
};
// _checkBoundaries(cwd, mutableFiles) => { valid: boolean, violations: string[] }
```

From bin/lib/experiment-report.cjs (exports):
```javascript
module.exports = {
  _generateReport, estimateCost: _estimateCost, _estimateCost,
  checkCircuitBreakers: _checkCircuitBreakers, _checkCircuitBreakers,
  _cmdDiffSummary,
};
// _checkCircuitBreakers(state) => { fired: boolean, reason: string|null }
// state requires: currentIteration, iterationBudget, elapsedMs, timeBudgetMs,
//   consecutiveFailures, consecutiveFailureLimit, iterationsSinceImprovement, noProgressLimit
```

From bin/lib/experiment-runner.cjs (exports):
```javascript
module.exports = {
  _evalMetric, _checkModifiedFiles,
  // _evalMetric(cwd, verifyCommand, timeoutMs) => { status, metricValue, stdout, stderr }
};
```

From bin/nyquist-metric.cjs:
- Always exits 0
- Last line of stdout is the integer pass count
- Parses `# pass N` from node --test TAP output

From workflows/optimize.md Step 9 (CURRENT — needs amendment):
- Lines 401-424: Goes straight to `experiment promote --slug {slug}` with NO Nyquist check
- Step 6 captures baseline metric; for --self preset, metric IS nyquist_pass_count

Established test patterns:
- import.meta.dirname for __dirname (phase 106 pattern)
- createRequire(import.meta.url) for CJS imports
- fs.mkdtempSync for temp git repos
- try/finally for cleanup
- assert `>= 235` not exact counts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Nyquist pre-promote guard to optimize.md and write structural tests</name>
  <files>workflows/optimize.md, tests/phase-107/experiment-nyquist-integrity.test.mjs</files>
  <read_first>
    - workflows/optimize.md (full file — understand Step 9 structure, lines 401-424)
    - tests/phase-106/experiment-events.test.mjs (structural test pattern reference)
    - tests/phase-104/experiment-self-preset.test.mjs (structural test pattern reference)
    - bin/nyquist-metric.cjs (understand the contract: exit 0, last line = pass count)
  </read_first>
  <action>
**Part A: Amend workflows/optimize.md Step 9**

Insert a Nyquist hard-floor check BEFORE the existing promote logic in Step 9 (between the diff-summary display and the "Merge these improvements?" question). The guard must:

1. Run `node bin/nyquist-metric.cjs` and capture the pass count from the last line of stdout
2. Compare against the Nyquist baseline (run the same command at Step 6 and store as `nyquistBaseline` alongside the experiment metric baseline — this is a SEPARATE baseline from the experiment metric for non-self experiments)
3. If pass count < nyquistBaseline: display "Nyquist hard floor failed: {passCount} < {nyquistBaseline}. Experiment improves the target metric but regresses PDE integrity. Discarding." Then run `node bin/pde-tools.cjs experiment cleanup --slug {slug}` and abort (do NOT ask the user about promotion).
4. If pass count >= nyquistBaseline: proceed with the existing diff-summary and promotion question.

Also add the Nyquist baseline capture to Step 6 (alongside the existing baseline metric capture). Add these lines after the existing baseline capture:

```
# Capture Nyquist baseline (hard floor — independent of experiment metric)
NYQUIST_BASELINE=$(node bin/nyquist-metric.cjs)
```

The Nyquist check at Step 9 should appear as:

```
# Nyquist hard-floor check (INTG-01, INTG-02)
NYQUIST_CURRENT=$(node bin/nyquist-metric.cjs)
```

Then an IF check: if NYQUIST_CURRENT < NYQUIST_BASELINE, discard via cleanup. Otherwise proceed.

**Part B: Create tests/phase-107/experiment-nyquist-integrity.test.mjs**

Structural tests (8+ assertions) covering INTG-01 and INTG-02:

1. `INTG-01: workflows/optimize.md references nyquist-metric in Step 9` — content.includes('nyquist-metric') within the step-9 section
2. `INTG-01: nyquist-metric reference appears BEFORE experiment promote` — use indexOf comparison: nyquistIdx < promoteIdx
3. `INTG-01: Nyquist baseline capture exists in Step 6` — content.includes('NYQUIST_BASELINE')
4. `INTG-01: Nyquist current capture exists in Step 9` — content.includes('NYQUIST_CURRENT')
5. `INTG-02: Step 9 describes discard on Nyquist regression` — content includes 'cleanup' after 'NYQUIST_CURRENT'
6. `INTG-02: Nyquist check uses cleanup (not promote) on failure` — assert cleanup reference exists between nyquist check and promote
7. `INTG-02: Nyquist hard floor is independent of experiment metric` — assert both NYQUIST_BASELINE and the experiment baseline are referenced (two separate baselines)
8. `INTG-02: Step 9 contains hard floor language` — content includes 'hard floor' or 'hard-floor'

Use the Phase 106 pattern: `import.meta.dirname` for ROOT resolution, `readFileSync` for content loading, describe/it blocks with requirement IDs in test names.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-107/experiment-nyquist-integrity.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "nyquist-metric" workflows/optimize.md returns 3 or more (Step 1 preset + Step 6 baseline + Step 9 check)
    - grep -c "NYQUIST_BASELINE" workflows/optimize.md returns 2 or more (capture + comparison)
    - grep -c "NYQUIST_CURRENT" workflows/optimize.md returns 1 or more
    - grep "experiment cleanup" workflows/optimize.md shows cleanup on Nyquist failure path
    - node --test tests/phase-107/experiment-nyquist-integrity.test.mjs exits 0
    - grep -c "assert\." tests/phase-107/experiment-nyquist-integrity.test.mjs returns 8 or more
  </acceptance_criteria>
  <done>optimize.md Step 9 has Nyquist pre-promote guard that discards on regression; 8+ structural tests verify the guard is present and correctly ordered</done>
</task>

<task type="auto">
  <name>Task 2: Write behavioral and regression guard tests for experiment infrastructure</name>
  <files>tests/phase-107/experiment-regression-guard.test.mjs</files>
  <read_first>
    - bin/lib/experiment.cjs (understand _checkBoundaries signature and boundary file parsing)
    - bin/lib/experiment-report.cjs (understand _checkCircuitBreakers signature and priority order)
    - bin/lib/experiment-runner.cjs (understand _evalMetric and _checkModifiedFiles)
    - tests/phase-100/experiment-state-machine.test.mjs (makeRepo pattern, temp dir creation)
    - tests/phase-103/experiment-report.test.mjs (circuit breaker test patterns)
    - references/experiment-boundaries.md (protected_directories list for test setup)
  </read_first>
  <action>
Create `tests/phase-107/experiment-regression-guard.test.mjs` with 14+ behavioral assertions covering INTG-03 and INTG-04. Use describe blocks organized by requirement ID.

**INTG-03 tests (zero regression — 3 assertions):**

1. `INTG-03: nyquist-metric.cjs exits 0 and returns integer pass count` — run `node bin/nyquist-metric.cjs` via spawnSync with cwd=ROOT, timeout 120000ms. Assert status === 0. Parse last line as integer. Assert passCount >= 235. (This is the subprocess zero-regression test confirming experiment infrastructure being present does not break existing tests.)
2. `INTG-03: nyquist-metric.cjs output is a single integer on last line` — same subprocess call, verify last non-empty line matches /^\d+$/
3. `INTG-03: experiment infrastructure files exist without breaking imports` — require each of experiment.cjs, experiment-runner.cjs, experiment-report.cjs, experiment-schema.cjs via createRequire and assert they are non-null objects with expected export keys

**INTG-04 tests (experiment infrastructure coverage — 11+ assertions):**

Boundary enforcement group:
4. `INTG-04: _checkBoundaries rejects files in protected directories` — create temp repo with minimal boundaries file (protected_directories: ['tests/', 'bin/', '.planning/', 'references/']), call _checkBoundaries(tmpDir, ['tests/some.test.mjs']), assert valid === false and violations includes the file
5. `INTG-04: _checkBoundaries allows files in non-protected directories` — same temp repo, call _checkBoundaries(tmpDir, ['workflows/brief.md']), assert valid === true
6. `INTG-04: _checkBoundaries rejects when boundaries file is missing` — create temp repo WITHOUT boundaries file, call _checkBoundaries(tmpDir, ['workflows/brief.md']), assert valid === false (missing boundaries = reject-all)

Circuit breaker group:
7. `INTG-04: _checkCircuitBreakers fires iteration_budget at exactly N` — call with currentIteration=50, iterationBudget=50, all others within limits. Assert fired === true, reason === 'iteration_budget'
8. `INTG-04: _checkCircuitBreakers fires time_budget when elapsed exceeds limit` — call with elapsedMs=3600001, timeBudgetMs=3600000. Assert fired === true, reason === 'time_budget'
9. `INTG-04: _checkCircuitBreakers fires consecutive_failures at exactly K` — call with consecutiveFailures=5, consecutiveFailureLimit=5. Assert fired === true, reason === 'consecutive_failures'
10. `INTG-04: _checkCircuitBreakers fires no_progress at exactly M` — call with iterationsSinceImprovement=10, noProgressLimit=10. Assert fired === true, reason === 'no_progress'
11. `INTG-04: _checkCircuitBreakers returns fired=false when all within limits` — call with all values within limits. Assert fired === false, reason === null
12. `INTG-04: _checkCircuitBreakers priority order is iteration > time > failures > progress` — call with ALL breakers firing simultaneously (all values at exact limits). Assert reason === 'iteration_budget' (first in priority)

Reset behavior group:
13. `INTG-04: _resetToBaseline requires experiment branch` — create temp repo on 'main' branch (no experiment branch), call _resetToBaseline. Assert it returns an error or throws (cannot reset on non-experiment branch)

Metric timeout group:
14. `INTG-04: _evalMetric returns CRASH status on timeout` — call _evalMetric with a verify command that hangs (e.g., 'sleep 30') and timeoutMs=100. Assert status === 'CRASH'

Use createRequire for all CJS module imports. Use fs.mkdtempSync + try/finally for any tests that need temp repos. Use spawnSync for subprocess tests.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-107/experiment-regression-guard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/phase-107/experiment-regression-guard.test.mjs exits 0
    - grep -c "assert\." tests/phase-107/experiment-regression-guard.test.mjs returns 14 or more
    - grep -c "INTG-03" tests/phase-107/experiment-regression-guard.test.mjs returns 3 or more
    - grep -c "INTG-04" tests/phase-107/experiment-regression-guard.test.mjs returns 11 or more
    - grep "createRequire" tests/phase-107/experiment-regression-guard.test.mjs shows CJS import pattern
    - grep "mkdtempSync" tests/phase-107/experiment-regression-guard.test.mjs shows temp repo pattern
  </acceptance_criteria>
  <done>14+ behavioral tests pass covering boundary enforcement (including missing file), circuit breaker priority ordering, reset guard, metric timeout, and zero-regression subprocess verification</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Run phase-specific tests: `node --test tests/phase-107/` — all tests pass
2. Run full Nyquist suite: `node bin/nyquist-metric.cjs` — pass count >= 235 (zero regressions)
3. Count new assertions: combined assertion count across both test files >= 20
4. Verify optimize.md has Nyquist guard before promote: grep confirms nyquist-metric appears before experiment promote in Step 9
</verification>

<success_criteria>
- Full Nyquist suite (235+) passes with zero new regressions (INTG-01, INTG-03)
- optimize.md Step 9 discards experiments that regress Nyquist (INTG-02)
- 20+ new assertions across two test files covering boundary enforcement, circuit breakers, reset guard, metric timeout (INTG-04)
- All tests in tests/phase-107/ pass green
</success_criteria>

<output>
After completion, create `.planning/phases/107-nyquist-coverage/107-01-SUMMARY.md`
</output>
