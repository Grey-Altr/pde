---
phase: 95-integration-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/deploy.md
  - workflows/handoff.md
  - workflows/wireframe.md
  - workflows/critique.md
  - workflows/brief.md
  - workflows/competitive.md
  - workflows/opportunity.md
  - workflows/flows.md
  - workflows/hig.md
  - workflows/system.md
  - templates/design-manifest.json
  - .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs
autonomous: true
requirements: [BRIEF-03, KIT-01, KIT-03, DEPLOY-04, DEPLOY-06, DEPLOY-09]
gap_closure: true

must_haves:
  truths:
    - "deploy.md preflight finds OTR artifact when handoff.md has written OTR-outreach-sequences-v*.md"
    - "handoff.md, wireframe.md, critique.md find BTH artifact when brief.md has written BTH-thesis-v*.md"
    - "build.md Stage 14 shows complete after deploy.md Gate 4 succeeds (hasDeployStaging written)"
    - "handoff.md pre-loads all 4 business reference files via required_reading"
    - "No workflow clobbers hasDeployStaging when writing designCoverage (all 10 writers carry 21 fields)"
  artifacts:
    - path: "workflows/deploy.md"
      provides: "OTR glob fix, BTH comment fix, hasDeployStaging coverage write"
      contains: "OTR-outreach-sequences-v"
    - path: "workflows/handoff.md"
      provides: "BTH glob fix, required_reading additions"
      contains: "BTH-thesis-v"
    - path: "workflows/wireframe.md"
      provides: "BTH glob fix"
      contains: "BTH-thesis-v"
    - path: "workflows/critique.md"
      provides: "BTH reference fix"
      contains: "BTH-thesis-v"
    - path: "templates/design-manifest.json"
      provides: "21st designCoverage field"
      contains: "hasDeployStaging"
    - path: ".planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs"
      provides: "Updated field count, deploy.md in coverage writers, Phase 95 assertions"
      contains: "TWENTY_ONE_FIELDS"
  key_links:
    - from: "workflows/deploy.md"
      to: "workflows/handoff.md"
      via: "OTR artifact glob must match handoff.md write target"
      pattern: "OTR-outreach-sequences-v"
    - from: "workflows/handoff.md"
      to: "workflows/brief.md"
      via: "BTH artifact glob must match brief.md write target"
      pattern: "BTH-thesis-v"
    - from: "workflows/deploy.md"
      to: "workflows/build.md"
      via: "hasDeployStaging coverage flag enables Stage 14 complete status"
      pattern: "hasDeployStaging"
---

<objective>
Close all 6 requirement gaps and 3 integration wiring bugs from the v0.12 milestone audit: fix OTR glob mismatch in deploy.md, fix BTH glob mismatch in 3 consumer workflows + 1 deploy.md comment, add hasDeployStaging as 21st designCoverage field with proper write in deploy.md and pass-through in all 9 existing coverage-writing workflows, add 4 missing business reference files to handoff.md required_reading, and update regression tests.

Purpose: Without these fixes, the E2E `/pde:build business:*` flow halts at deploy preflight (OTR miss), LKT manifest reports BTH as missing (BTH miss), Stage 14 permanently shows pending (no hasDeployStaging), and handoff.md may lack pre-loaded business context.

Output: All 14 files updated, regression suite expanded and green.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-integration-wiring-fixes/95-RESEARCH.md
@.planning/v0.12-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix OTR glob, BTH globs, handoff required_reading, and deploy.md BTH comment</name>
  <files>workflows/deploy.md, workflows/handoff.md, workflows/wireframe.md, workflows/critique.md</files>

  <read_first>
    workflows/deploy.md (lines 115-120 for OTR glob, line 150 for OTR error message, line 419 for BTH comment)
    workflows/handoff.md (lines 1-12 for required_reading block, line 594 for BTH glob)
    workflows/wireframe.md (line 624 for BTH glob)
    workflows/critique.md (line 812 for BTH reference)
  </read_first>

  <action>
    **Fix 1 — OTR glob in deploy.md (line 117):**
    Change `OTR-outreach-v*.md` to `OTR-outreach-sequences-v*.md` in the ls command.

    Before: `OTR_FILE=$(ls .planning/design/launch/OTR-outreach-v*.md 2>/dev/null | sort -V | tail -1)`
    After:  `OTR_FILE=$(ls .planning/design/launch/OTR-outreach-sequences-v*.md 2>/dev/null | sort -V | tail -1)`

    **Fix 2 — OTR error message in deploy.md (line 150):**
    Change the expected path string.

    Before: `Expected: .planning/design/launch/OTR-outreach-v{N}.md`
    After:  `Expected: .planning/design/launch/OTR-outreach-sequences-v{N}.md`

    **Fix 3 — BTH glob in handoff.md (line 594):**
    Change `BTH-business-thesis-v*.md` to `BTH-thesis-v*.md` in the Glob instruction.

    Before: `- BTH: Glob .planning/design/strategy/BTH-business-thesis-v*.md -> BTH_PATH, BTH_AVAILABLE (true/false)`
    After:  `- BTH: Glob .planning/design/strategy/BTH-thesis-v*.md -> BTH_PATH, BTH_AVAILABLE (true/false)`

    **Fix 4 — BTH glob in wireframe.md (line 624):**
    Change `BTH-business-thesis-v*.md` to `BTH-thesis-v*.md` in the ls command.

    Before: `BTH_FILE=$(ls .planning/design/strategy/BTH-business-thesis-v*.md 2>/dev/null | tail -1)`
    After:  `BTH_FILE=$(ls .planning/design/strategy/BTH-thesis-v*.md 2>/dev/null | tail -1)`

    **Fix 5 — BTH reference in critique.md (line 812):**
    Change `BTH-business-thesis-v*.md` to `BTH-thesis-v*.md` in the doc string.

    Before: `- BTH artifact (.planning/design/strategy/BTH-business-thesis-v*.md) -- problem/solution/market/unfair-advantage`
    After:  `- BTH artifact (.planning/design/strategy/BTH-thesis-v*.md) -- problem/solution/market/unfair-advantage`

    **Fix 6 — BTH comment in deploy.md (line 419):**
    Change `BTH-business-thesis-v1.md` to `BTH-thesis-v1.md` in the scaffold comment.

    Before: `{/* TODO: Map your features from .planning/design/strategy/BTH-business-thesis-v1.md */}`
    After:  `{/* TODO: Map your features from .planning/design/strategy/BTH-thesis-v1.md */}`

    **Fix 7 — handoff.md required_reading (lines 5-10):**
    Add 4 entries after `@references/experience-disclaimer.md` and before `</required_reading>`:

    ```
    @references/business-track.md
    @references/launch-frameworks.md
    @references/business-financial-disclaimer.md
    @references/business-legal-disclaimer.md
    ```

    The final required_reading block should have 8 entries total (4 existing + 4 new).
  </action>

  <verify>
    <automated>grep -c "OTR-outreach-sequences-v" workflows/deploy.md | grep -q "2" && grep -c "BTH-business-thesis" workflows/handoff.md workflows/wireframe.md workflows/critique.md workflows/deploy.md 2>/dev/null | grep -q ":0" && grep -c "business-track.md" workflows/handoff.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - `grep "OTR-outreach-sequences-v" workflows/deploy.md` returns exactly 2 matches (line 117 glob + line 150 error message)
    - `grep "OTR-outreach-v\*" workflows/deploy.md` returns 0 matches (old glob fully removed — note the exact pattern is "OTR-outreach-v*" without "sequences")
    - `grep "BTH-business-thesis" workflows/handoff.md workflows/wireframe.md workflows/critique.md workflows/deploy.md` returns 0 matches across all 4 files
    - `grep "BTH-thesis-v" workflows/handoff.md` returns at least 1 match (line 594)
    - `grep "BTH-thesis-v" workflows/wireframe.md` returns at least 1 match (line 624)
    - `grep "BTH-thesis-v" workflows/critique.md` returns at least 1 match (line 812)
    - `grep "BTH-thesis-v1" workflows/deploy.md` returns at least 1 match (line 419 comment)
    - `grep "business-track.md" workflows/handoff.md` returns 1 match in required_reading
    - `grep "launch-frameworks.md" workflows/handoff.md` returns 1 match
    - `grep "business-financial-disclaimer.md" workflows/handoff.md` returns 1 match
    - `grep "business-legal-disclaimer.md" workflows/handoff.md` returns 1 match
  </acceptance_criteria>

  <done>
    OTR glob matches producer (handoff.md). BTH glob matches producer (brief.md) in all 4 consumers. handoff.md required_reading has all 8 reference entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add hasDeployStaging as 21st designCoverage field across manifest template, deploy.md coverage write, and 9 pass-through workflows</name>
  <files>templates/design-manifest.json, workflows/deploy.md, workflows/brief.md, workflows/competitive.md, workflows/opportunity.md, workflows/flows.md, workflows/wireframe.md, workflows/critique.md, workflows/hig.md, workflows/handoff.md, workflows/system.md</files>

  <read_first>
    templates/design-manifest.json (lines 110-132 for designCoverage block — hasLaunchKit is at line 131, add hasDeployStaging after it)
    workflows/deploy.md (lines 819-827 for Gate 4 success path where coverage write goes)
    workflows/brief.md (line 888 area for coverage write pattern example)
    workflows/competitive.md (grep for "manifest-set-top-level designCoverage" to find coverage write)
    workflows/opportunity.md (grep for "manifest-set-top-level designCoverage")
    workflows/flows.md (grep for "manifest-set-top-level designCoverage")
    workflows/wireframe.md (grep for "manifest-set-top-level designCoverage")
    workflows/critique.md (grep for "manifest-set-top-level designCoverage")
    workflows/hig.md (grep for "manifest-set-top-level designCoverage")
    workflows/handoff.md (grep for "manifest-set-top-level designCoverage")
    workflows/system.md (grep for "manifest-set-top-level designCoverage")
  </read_first>

  <action>
    **Part A — Manifest template (templates/design-manifest.json):**
    After `"hasLaunchKit": false` (line 131), add a comma and new line:
    `"hasDeployStaging": false`
    This is the 21st field. Ensure the JSON remains valid (comma after hasLaunchKit, no trailing comma after hasDeployStaging).

    **Part B — deploy.md coverage write (insert between lines 823-825):**
    After the line `Store $DEPLOY_URL for the manifest. Display: ...` (line 823) and before the `---` separator (line 825) that precedes Step 5/6, insert a new designCoverage write block:

    ```
    **Write hasDeployStaging coverage flag:**

    Read current coverage state:
    ```bash
    COV=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design coverage-check)
    if [[ "$COV" == @file:* ]]; then COV=$(cat "${COV#@file:}"); fi
    ```

    Parse all 21 field values from COV JSON output (default absent fields to false). Then write the complete 21-field coverage object:

    ```bash
    node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-set-top-level designCoverage \
      '{"hasDesignSystem":ACTUAL,"hasWireframes":ACTUAL,"hasFlows":ACTUAL,"hasHardwareSpec":ACTUAL,"hasCritique":ACTUAL,"hasIterate":ACTUAL,"hasHandoff":ACTUAL,"hasIdeation":ACTUAL,"hasCompetitive":ACTUAL,"hasOpportunity":ACTUAL,"hasMockup":ACTUAL,"hasHigAudit":ACTUAL,"hasRecommendations":ACTUAL,"hasStitchWireframes":ACTUAL,"hasPrintCollateral":ACTUAL,"hasProductionBible":ACTUAL,"hasBusinessThesis":ACTUAL,"hasMarketLandscape":ACTUAL,"hasServiceBlueprint":ACTUAL,"hasLaunchKit":ACTUAL,"hasDeployStaging":true}'
    ```

    Where each ACTUAL is replaced with the literal `true` or `false` value read from the parsed coverage. `hasDeployStaging` is hardcoded to `true` — this is the flag deploy.md owns. All other 20 fields use their current values.

    **CRITICAL:** This write MUST be inside the `if $DEPLOY_EXIT == 0:` success path (after line 823), NOT before Gate 4 or at the start of Step 5. The flag should only be set when deploy actually succeeded.
    ```

    **Part C — 9 pass-through workflows:**
    For EACH of the following 9 workflows, find every `manifest-set-top-level designCoverage` call and add `"hasDeployStaging":ACTUAL` (or `"hasDeployStaging":{current}` matching the workflow's existing placeholder convention) as the 21st field at the end of the JSON object, before the closing `}`.

    The 9 workflows and their coverage write conventions:
    1. `brief.md` — uses `ACTUAL` placeholders (line 888 area)
    2. `competitive.md` — find the coverage write, add hasDeployStaging with matching placeholder
    3. `opportunity.md` — find the coverage write, add hasDeployStaging
    4. `flows.md` — find the coverage write(s), add hasDeployStaging to ALL variants
    5. `wireframe.md` — find the coverage write(s), add hasDeployStaging to ALL variants
    6. `critique.md` — find the coverage write, add hasDeployStaging
    7. `hig.md` — find the coverage write, add hasDeployStaging
    8. `handoff.md` — find the coverage write(s), add hasDeployStaging to ALL variants (has 4 variants: experience/hybrid/non-experience/business)
    9. `system.md` — find the coverage write, add hasDeployStaging

    For each workflow: read the existing coverage write, identify the placeholder convention used (ACTUAL, {current}, true/false literal), and add `"hasDeployStaging":PLACEHOLDER` using the SAME convention immediately after `"hasLaunchKit":PLACEHOLDER` and before the closing `}'`.

    **CRITICAL:** Every workflow may have MULTIPLE coverage write calls (branching for different product types). ALL variants must include hasDeployStaging. Use `grep -n "manifest-set-top-level designCoverage" workflows/{file}` to find every occurrence and update each one.
  </action>

  <verify>
    <automated>node .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep "hasDeployStaging" templates/design-manifest.json` returns 1 match with value `false`
    - `grep "hasDeployStaging" workflows/deploy.md` returns at least 1 match (the coverage write)
    - `grep -c "hasDeployStaging" workflows/brief.md` returns >= 1 (pass-through in coverage write)
    - `grep -c "hasDeployStaging" workflows/competitive.md` returns >= 1
    - `grep -c "hasDeployStaging" workflows/opportunity.md` returns >= 1
    - `grep -c "hasDeployStaging" workflows/flows.md` returns >= 1
    - `grep -c "hasDeployStaging" workflows/wireframe.md` returns >= 1
    - `grep -c "hasDeployStaging" workflows/critique.md` returns >= 1
    - `grep -c "hasDeployStaging" workflows/hig.md` returns >= 1
    - `grep -c "hasDeployStaging" workflows/handoff.md` returns >= 1
    - `grep -c "hasDeployStaging" workflows/system.md` returns >= 1
    - `grep "manifest-set-top-level designCoverage" workflows/deploy.md` returns at least 1 match
    - The coverage write in deploy.md appears AFTER the `$DEPLOY_EXIT == 0` check (line 821+) and BEFORE `Step 5/6` heading
    - `node .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` — existing 35 assertions still pass (no regressions from field addition)
  </acceptance_criteria>

  <done>
    21st designCoverage field (hasDeployStaging) exists in manifest template with default false. deploy.md writes hasDeployStaging:true after Gate 4 success. All 9 existing coverage-writing workflows pass through hasDeployStaging without clobbering it.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update regression test — rename constant, add deploy.md to writers, add Phase 95 assertions</name>
  <files>.planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs</files>

  <read_first>
    .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs (full file — 305 lines)
  </read_first>

  <action>
    **Part A — Rename TWENTY_FIELDS constant (line 22):**
    Change `const TWENTY_FIELDS = [` to `const TWENTY_ONE_FIELDS = [`

    Add `'hasDeployStaging'` after `'hasLaunchKit'` in the array (with comma after hasLaunchKit).

    **Part B — Update all references to TWENTY_FIELDS:**
    Find every occurrence of `TWENTY_FIELDS` in the file and replace with `TWENTY_ONE_FIELDS`. This includes:
    - The filter on line 298: `TWENTY_FIELDS.filter(...)` becomes `TWENTY_ONE_FIELDS.filter(...)`

    **Part C — Add deploy.md to V012_COVERAGE_WRITERS (line 282-292):**
    Add `'deploy.md'` after `'system.md'` in the array (with comma after system.md).

    **Part D — Update describe block title (line 294):**
    Change `'INTG-07: all v0.12 coverage-writing workflows contain all 20 designCoverage fields'`
    to `'INTG-07: all v0.12 coverage-writing workflows contain all 21 designCoverage fields'`

    **Part E — Update it() description (line 296):**
    Change `contains all 20 designCoverage field names`
    to `contains all 21 designCoverage field names`

    **Part F — Add new describe block for Phase 95 wiring fix assertions:**
    After the closing `});` of the INTG-07 block (line 305), add:

    ```javascript

    // ---------------------------------------------------------------------------
    // Phase 95: integration wiring fix assertions
    // ---------------------------------------------------------------------------
    describe('Phase 95: integration wiring fixes verified', () => {
      it('deploy.md globs OTR-outreach-sequences (not OTR-outreach-v)', () => {
        const content = readWorkflow('deploy.md');
        assert.ok(
          content.includes('OTR-outreach-sequences-v'),
          'deploy.md must glob OTR-outreach-sequences-v*.md (matching handoff.md producer)'
        );
        assert.ok(
          !content.includes('OTR-outreach-v*.md'),
          'deploy.md must NOT contain old glob OTR-outreach-v*.md'
        );
      });

      it('handoff.md globs BTH-thesis (not BTH-business-thesis)', () => {
        const content = readWorkflow('handoff.md');
        assert.ok(
          content.includes('BTH-thesis-v'),
          'handoff.md must glob BTH-thesis-v*.md (matching brief.md producer)'
        );
        assert.ok(
          !content.includes('BTH-business-thesis'),
          'handoff.md must NOT contain old glob BTH-business-thesis'
        );
      });

      it('wireframe.md globs BTH-thesis (not BTH-business-thesis)', () => {
        const content = readWorkflow('wireframe.md');
        assert.ok(
          content.includes('BTH-thesis-v'),
          'wireframe.md must glob BTH-thesis-v*.md (matching brief.md producer)'
        );
        assert.ok(
          !content.includes('BTH-business-thesis'),
          'wireframe.md must NOT contain old glob BTH-business-thesis'
        );
      });

      it('critique.md references BTH-thesis (not BTH-business-thesis)', () => {
        const content = readWorkflow('critique.md');
        assert.ok(
          content.includes('BTH-thesis-v'),
          'critique.md must reference BTH-thesis-v*.md (matching brief.md producer)'
        );
        assert.ok(
          !content.includes('BTH-business-thesis'),
          'critique.md must NOT contain old reference BTH-business-thesis'
        );
      });

      it('deploy.md references BTH-thesis (not BTH-business-thesis)', () => {
        const content = readWorkflow('deploy.md');
        assert.ok(
          !content.includes('BTH-business-thesis'),
          'deploy.md must NOT contain old reference BTH-business-thesis'
        );
      });

      it('handoff.md required_reading includes business-track.md', () => {
        const content = readWorkflow('handoff.md');
        assert.ok(
          content.includes('business-track.md'),
          'handoff.md required_reading must include business-track.md'
        );
      });

      it('handoff.md required_reading includes launch-frameworks.md', () => {
        const content = readWorkflow('handoff.md');
        assert.ok(
          content.includes('launch-frameworks.md'),
          'handoff.md required_reading must include launch-frameworks.md'
        );
      });

      it('handoff.md required_reading includes business-financial-disclaimer.md', () => {
        const content = readWorkflow('handoff.md');
        assert.ok(
          content.includes('business-financial-disclaimer.md'),
          'handoff.md required_reading must include business-financial-disclaimer.md'
        );
      });

      it('handoff.md required_reading includes business-legal-disclaimer.md', () => {
        const content = readWorkflow('handoff.md');
        assert.ok(
          content.includes('business-legal-disclaimer.md'),
          'handoff.md required_reading must include business-legal-disclaimer.md'
        );
      });

      it('deploy.md contains hasDeployStaging coverage write', () => {
        const content = readWorkflow('deploy.md');
        assert.ok(
          content.includes('hasDeployStaging'),
          'deploy.md must contain hasDeployStaging coverage write'
        );
      });
    });
    ```

    **Total assertions after update:** 35 existing + ~10 new Phase 95 assertions = ~45 total.
  </action>

  <verify>
    <automated>node .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep "TWENTY_ONE_FIELDS" .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` returns >= 2 matches (declaration + usage)
    - `grep "TWENTY_FIELDS" .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` returns 0 matches (fully renamed)
    - `grep "deploy.md" .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` returns >= 2 matches (V012_COVERAGE_WRITERS entry + Phase 95 assertions)
    - `grep "21 designCoverage" .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` returns >= 1 match (updated describe title)
    - `grep "Phase 95" .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` returns >= 1 match (new describe block)
    - `grep "OTR-outreach-sequences" .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` returns >= 1 match (new assertion)
    - `grep "BTH-business-thesis" .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` returns >= 1 match (negation assertion checking old pattern is gone)
    - `node .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` exits with code 0 (all assertions pass)
    - Test output shows 0 failures
  </acceptance_criteria>

  <done>
    Regression test suite updated: TWENTY_ONE_FIELDS with 21 entries, deploy.md in V012_COVERAGE_WRITERS (10 total), describe title says "21", 10 new Phase 95 assertions verify all wiring fixes. Full suite green with 0 failures.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run full verification:

1. `grep -rn "BTH-business-thesis" workflows/` — must return 0 results
2. `grep -rn "OTR-outreach-v\*" workflows/` — must return 0 results (the old pattern without "sequences")
3. `grep -c "hasDeployStaging" workflows/deploy.md workflows/brief.md workflows/competitive.md workflows/opportunity.md workflows/flows.md workflows/wireframe.md workflows/critique.md workflows/hig.md workflows/handoff.md workflows/system.md templates/design-manifest.json` — all files return >= 1
4. `grep -c "business-track.md" workflows/handoff.md` — returns 1
5. `node .planning/phases/94-nyquist-regression-tests/tests/test-regression-matrix.cjs` — full suite green, 0 failures
</verification>

<success_criteria>
- All 6 requirement gaps closed: BRIEF-03, KIT-01, KIT-03, DEPLOY-04, DEPLOY-06, DEPLOY-09
- Zero occurrences of `BTH-business-thesis` in any workflow file
- Zero occurrences of `OTR-outreach-v*` (without sequences) in any workflow file
- `hasDeployStaging` present in manifest template, deploy.md (owner write), and all 9 pass-through workflows
- handoff.md required_reading has 8 entries (4 original + 4 business)
- Regression test suite fully green with updated field count (21) and new Phase 95 assertions
</success_criteria>

<output>
After completion, create `.planning/phases/95-integration-wiring-fixes/95-01-SUMMARY.md`
</output>
