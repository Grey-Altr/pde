---
phase: 180-claim-verification-+-pdf-export
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/export-pdf.cjs
  - bin/pde-tools.cjs
  - workflows/present.md
  - tests/phase-180/export-pdf.test.mjs
autonomous: true
requirements: [PDF-01, PDF-02, PDF-03]

must_haves:
  truths:
    - "Running /pde:present executive-summary --pdf produces a PDF file alongside the HTML output"
    - "The PDF is generated by Playwright page.pdf() with no new npm dependencies"
    - "The PDF preserves inline SVG charts, base64 images, and table formatting from the HTML source"
    - "pde-tools presentation pdf <html-path> <pdf-path> is a working CLI subcommand"
  artifacts:
    - path: "bin/lib/export-pdf.cjs"
      provides: "Playwright PDF export module"
      exports: ["exportPdf", "cmdPresentationPdf"]
    - path: "tests/phase-180/export-pdf.test.mjs"
      provides: "Smoke tests for PDF export"
      contains: "exportPdf"
  key_links:
    - from: "bin/pde-tools.cjs"
      to: "bin/lib/export-pdf.cjs"
      via: "require and call cmdPresentationPdf in presentation pdf subcommand"
      pattern: "cmdPresentationPdf"
    - from: "workflows/present.md"
      to: "bin/pde-tools.cjs"
      via: "pde-tools presentation pdf CLI call when --pdf flag set"
      pattern: "presentation pdf"
---

<objective>
Add PDF export capability using Playwright page.pdf() so any HTML presentation can be exported to PDF via the --pdf flag or the `pde-tools presentation pdf` CLI subcommand.

Purpose: Users can produce PDF versions of presentations for distribution to stakeholders who prefer static documents (PDF-01/02/03).
Output: export-pdf.cjs module, pde-tools subcommand, workflow --pdf flag, smoke tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/180-claim-verification-+-pdf-export/180-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From bin/lib/image-pipeline/screenshot.cjs (established Playwright pattern):
```javascript
const { chromium } = require('playwright');
// Launch pattern: chromium.launch({ headless: true })
// Page load: page.goto(url, { waitUntil: 'networkidle' })
// Screenshot: page.screenshot({ path, fullPage: true })
```

From bin/pde-tools.cjs (presentation subcommand block, lines 1676-1688):
```javascript
case 'presentation': {
  const subcommand = args[1];
  const presentation = require('./lib/presentation.cjs');
  if (subcommand === 'artifact-read') {
    presentation.cmdPresentationArtifactRead(cwd, raw);
  } else if (subcommand === 'render') {
    const renderPresentation = require('./lib/render-presentation.cjs');
    renderPresentation.cmdPresentationRender(cwd, args[2], args[3], args[4], args[5]);
  } else {
    error('Unknown presentation subcommand. Available: artifact-read, render');
  }
  break;
}
```

From workflows/present.md (Step 0 flag parsing, Step 5 path computation, Step 6 render call):
```markdown
Step 0: Parse $ARGUMENTS — detect flags: --dry-run, --verbose
Step 5: DATE=$(date +%Y-%m-%d), HTML_PATH, MD_PATH computation
Step 6: node pde-tools.cjs presentation render ...
Step 7: Display completion with HTML + MD paths
```

PDF naming convention: .planning/presentations/{persona}-{date}.pdf
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create export-pdf.cjs + smoke tests</name>
  <files>bin/lib/export-pdf.cjs, tests/phase-180/export-pdf.test.mjs</files>
  <read_first>
    - bin/lib/image-pipeline/screenshot.cjs (full file — established chromium.launch() pattern, page.goto, waitUntil usage)
    - bin/lib/render-presentation.cjs (lines 616-670 — renderHTML function to understand HTML structure for test fixture)
    - tests/phase-179/charts.test.mjs (lines 1-30 — test convention reference)
  </read_first>
  <behavior>
    - exportPdf({ htmlPath, pdfPath }) with a valid HTML file produces a non-empty PDF file at pdfPath
    - exportPdf({ htmlPath, pdfPath, format: 'A4', printBackground: true }) uses those options in page.pdf()
    - exportPdf defaults to format 'A4' and printBackground true when not specified
    - PDF output from HTML containing inline SVG and base64 images produces a file larger than a minimal empty PDF (size > 5000 bytes as proxy for content preservation)
    - cmdPresentationPdf(cwd, htmlPath, pdfPath) calls exportPdf and writes success output
    - cmdPresentationPdf with missing htmlPath argument calls error()
  </behavior>
  <action>
    1. Create bin/lib/export-pdf.cjs with:
       - `async function exportPdf({ htmlPath, pdfPath, format = 'A4', printBackground = true })` — Launches headless Chromium via `const { chromium } = require('playwright')`. Creates browser context and page. Loads HTML via `page.goto('file://' + path.resolve(htmlPath), { waitUntil: 'networkidle' })`. Calls `await page.pdf({ path: pdfPath, format, printBackground })`. Closes browser in a finally block. Returns `{ pdfPath, bytes: fs.statSync(pdfPath).size }`.
       - `async function cmdPresentationPdf(cwd, htmlPath, pdfPath)` — CLI handler. Validates htmlPath and pdfPath are provided (call error() from core.cjs if missing). Validates htmlPath exists on disk. Ensures pdfPath parent directory exists (mkdirSync recursive). Calls exportPdf(). Outputs result via output() from core.cjs.
       - `module.exports = { exportPdf, cmdPresentationPdf }`

    2. Create tests/phase-180/export-pdf.test.mjs:
       - Use vitest + createRequire convention from phase-178/179 tests
       - "pdf smoke" test: Create a temp HTML file with minimal self-contained HTML (include an inline `<svg>` element and a small base64 `<img>` tag to exercise PDF-03). Call exportPdf({ htmlPath: tmpHtml, pdfPath: tmpPdf }). Assert PDF file exists, is non-empty, starts with `%PDF` magic bytes.
       - "pdf size" test: Same temp HTML with SVG + base64 image. Assert PDF size > 5000 bytes (proxy for SVG/image preservation — an empty page PDF is ~1500 bytes).
       - "missing html" test: Call cmdPresentationPdf with non-existent HTML path, expect it to throw or call error().
       - Use beforeAll to create temp dir, afterAll to clean up.

    Critical constraints:
    - MUST use `printBackground: true` (Pitfall 3 from research — PDE dark theme needs backgrounds preserved)
    - MUST use `page.goto('file://' + path.resolve(htmlPath))` not `page.setContent()` (matches screenshot.cjs pattern)
    - MUST close browser in finally block (prevents zombie processes)
    - Zero new npm dependencies — Playwright 1.58.2 already installed
  </action>
  <verify>
    <automated>npx vitest run tests/phase-180/export-pdf.test.mjs --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "exportPdf" bin/lib/export-pdf.cjs
    - grep -q "cmdPresentationPdf" bin/lib/export-pdf.cjs
    - grep -q "chromium" bin/lib/export-pdf.cjs
    - grep -q "printBackground" bin/lib/export-pdf.cjs
    - grep -q "page.pdf" bin/lib/export-pdf.cjs
    - npx vitest run tests/phase-180/export-pdf.test.mjs exits 0
  </acceptance_criteria>
  <done>
    exportPdf generates PDF from HTML using Playwright page.pdf(). cmdPresentationPdf provides CLI handler with input validation. Smoke tests confirm PDF generation with SVG and image content preservation. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire pde-tools subcommand + workflow --pdf flag</name>
  <files>bin/pde-tools.cjs, workflows/present.md</files>
  <read_first>
    - bin/pde-tools.cjs (lines 1676-1688 — presentation case block, understand routing pattern)
    - workflows/present.md (full file — understand all 7 steps, flag parsing in Step 0, path computation in Step 5, render call in Step 6, completion in Step 7)
    - bin/lib/export-pdf.cjs (just created in Task 1 — understand cmdPresentationPdf signature)
  </read_first>
  <action>
    1. In bin/pde-tools.cjs, modify the `case 'presentation'` block (line 1676). Add a new `else if` branch for `subcommand === 'pdf'`:
       ```javascript
       } else if (subcommand === 'pdf') {
         const exportPdf = require('./lib/export-pdf.cjs');
         await exportPdf.cmdPresentationPdf(cwd, args[2], args[3]);
       ```
       Also update the error message to include 'pdf' in available subcommands: `'Unknown presentation subcommand. Available: artifact-read, render, pdf'`

       IMPORTANT: cmdPresentationPdf is async. Check if the main() function in pde-tools.cjs already handles async (look for async main or .then/.catch). If main() is not async, wrap the pdf call: `exportPdf.cmdPresentationPdf(cwd, args[2], args[3]).catch(e => { error(e.message); })` — or make the presentation case block handle the promise appropriately.

    2. In workflows/present.md, make these changes:

       **Step 0** — Add `--pdf` to the flag detection list:
       ```
       - Detect flags: `--dry-run`, `--verbose`, `--pdf`
       ```

       **Step 5** — After MD_PATH computation, add PDF_PATH computation:
       ```
       IF --pdf flag is set:
         PDF_PATH=".planning/presentations/${PERSONA_SLUG}-${DATE}.pdf"
       ```

       **Step 5 dry-run block** — Add PDF path to dry-run output if --pdf:
       ```
       IF --pdf:
         PDF: .planning/presentations/${PERSONA_SLUG}-${DATE}.pdf
       ```

       **Step 6** — After the render command succeeds, add a conditional PDF export step:
       ```
       IF --pdf flag is set:
         node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" presentation pdf "${HTML_PATH}" "${PDF_PATH}"

         If the command fails, display error:
           Error: PDF export failed for "${HTML_PATH}".
             Ensure Playwright browsers are installed: npx playwright install chromium
           (Do NOT halt — HTML and MD are already written successfully)

         Log: PDF: ${PDF_PATH}
       ```

       **Step 7** — Add PDF to completion output if --pdf was set:
       ```
       IF --pdf:
         PDF:      {PDF_PATH}
       ```

    Critical constraints:
    - PDF export is a POST-WRITE step — it runs AFTER HTML is successfully written (never before)
    - PDF failure should NOT undo or block the already-written HTML and MD files
    - The `presentation pdf` subcommand takes exactly 2 args: html-path and pdf-path
    - Async handling in pde-tools.cjs must not break existing synchronous subcommands
  </action>
  <verify>
    <automated>node bin/pde-tools.cjs presentation pdf --help 2>&1 | head -5; echo "---"; grep -c "presentation pdf" bin/pde-tools.cjs; echo "---"; grep -c "\-\-pdf" workflows/present.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "subcommand === 'pdf'" bin/pde-tools.cjs
    - grep -q "cmdPresentationPdf" bin/pde-tools.cjs
    - grep -q "artifact-read, render, pdf" bin/pde-tools.cjs
    - grep -q "\-\-pdf" workflows/present.md
    - grep -q "PDF_PATH" workflows/present.md
    - grep -q "presentation pdf" workflows/present.md
  </acceptance_criteria>
  <done>
    `pde-tools presentation pdf <html> <pdf>` subcommand is wired and callable. present.md workflow detects --pdf flag, computes PDF_PATH, calls presentation pdf after render, and includes PDF path in completion output. PDF failure does not block HTML/MD output. PDF-01 is complete.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/phase-180/` — all phase-180 tests pass (both verify-presentation and export-pdf)
- `node bin/pde-tools.cjs presentation pdf /tmp/test.html /tmp/test.pdf` — produces a PDF file (requires an HTML file to exist at that path)
- `grep "presentation pdf" bin/pde-tools.cjs` — subcommand is wired
- `grep "\-\-pdf" workflows/present.md` — flag is documented in workflow
</verification>

<success_criteria>
- export-pdf.cjs exists with exportPdf and cmdPresentationPdf exports
- pde-tools.cjs routes `presentation pdf` to cmdPresentationPdf
- present.md workflow handles --pdf flag through Steps 0, 5, 6, 7
- PDF export uses Playwright page.pdf() with printBackground: true
- PDF preserves SVG and base64 images (smoke test confirms)
- All phase-180 and phase-178 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/180-claim-verification-+-pdf-export/180-02-SUMMARY.md`
</output>
