---
phase: 66-wireframe-mockup-stitch-integration
plan: 03
type: execute
wave: 3
depends_on: [66-01, 66-02]
files_modified:
  - tests/phase-66/wireframe-stitch-flag.test.mjs
  - tests/phase-66/mockup-stitch-flag.test.mjs
  - tests/phase-66/annotation-injection.test.mjs
  - tests/phase-66/consent-gates.test.mjs
  - tests/phase-66/fallback-behavior.test.mjs
autonomous: true
requirements: [WFR-01, WFR-02, WFR-03, WFR-04, WFR-05, WFR-06, CONSENT-01, CONSENT-02, CONSENT-03, CONSENT-04, EFF-01, EFF-02, EFF-04, EFF-05]

must_haves:
  truths:
    - "All 5 Nyquist test files exist and pass with node --test"
    - "Every Phase 66 requirement ID (WFR-01 through WFR-06, CONSENT-01 through CONSENT-04, EFF-01, EFF-02, EFF-04, EFF-05) has at least one test assertion"
    - "Tests verify workflow file structure (file parse assertions), not runtime behavior"
    - "Phase 65 tests still pass alongside Phase 66 tests"
  artifacts:
    - path: "tests/phase-66/wireframe-stitch-flag.test.mjs"
      provides: "Nyquist tests for WFR-01, WFR-02, WFR-04, WFR-06, EFF-02"
    - path: "tests/phase-66/mockup-stitch-flag.test.mjs"
      provides: "Nyquist tests for WFR-05"
    - path: "tests/phase-66/annotation-injection.test.mjs"
      provides: "Nyquist tests for WFR-03, EFF-05"
    - path: "tests/phase-66/consent-gates.test.mjs"
      provides: "Nyquist tests for CONSENT-01, CONSENT-02, CONSENT-03, CONSENT-04"
    - path: "tests/phase-66/fallback-behavior.test.mjs"
      provides: "Nyquist tests for EFF-04, WFR-06"
  key_links:
    - from: "tests/phase-66/*.test.mjs"
      to: "workflows/wireframe.md"
      via: "fs.readFileSync file parse"
      pattern: "readFileSync.*wireframe"
    - from: "tests/phase-66/*.test.mjs"
      to: "workflows/mockup.md"
      via: "fs.readFileSync file parse"
      pattern: "readFileSync.*mockup"
---

<objective>
Create Nyquist test suite covering all 14 Phase 66 requirements with file-parse assertions against the modified wireframe.md and mockup.md workflow files.

Purpose: Provides automated verification that all requirement-mandated workflow elements are present, correctly ordered, and structurally sound. Follows the Phase 65 Nyquist test pattern (node:test framework, file-parse assertions).
Output: 5 test files in tests/phase-66/ covering all requirements, all passing with `node --test tests/phase-66/*.test.mjs`.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-wireframe-mockup-stitch-integration/66-CONTEXT.md
@.planning/phases/66-wireframe-mockup-stitch-integration/66-RESEARCH.md
@.planning/phases/66-wireframe-mockup-stitch-integration/66-01-SUMMARY.md
@.planning/phases/66-wireframe-mockup-stitch-integration/66-02-SUMMARY.md

<interfaces>
<!-- Test framework pattern from Phase 65 -->

From tests/phase-65/stitch-bridge-registration.test.mjs:
```javascript
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { createRequire } from 'module';
import { fileURLToPath } from 'url';
import { resolve } from 'path';
import { readFileSync } from 'fs';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');

// File-parse pattern for workflow tests:
const wireframeMd = readFileSync(resolve(ROOT, 'workflows', 'wireframe.md'), 'utf8');
const mockupMd = readFileSync(resolve(ROOT, 'workflows', 'mockup.md'), 'utf8');
```

Requirement-to-test mapping (from RESEARCH.md):
- wireframe-stitch-flag.test.mjs: WFR-01, WFR-02, WFR-04, WFR-06, EFF-02
- mockup-stitch-flag.test.mjs: WFR-05
- annotation-injection.test.mjs: WFR-03, EFF-05
- consent-gates.test.mjs: CONSENT-01, CONSENT-02, CONSENT-03, CONSENT-04
- fallback-behavior.test.mjs: EFF-04, WFR-06 (cross-coverage with wireframe-stitch-flag)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wireframe-stitch-flag, mockup-stitch-flag, and annotation-injection test files</name>
  <files>tests/phase-66/wireframe-stitch-flag.test.mjs, tests/phase-66/mockup-stitch-flag.test.mjs, tests/phase-66/annotation-injection.test.mjs</files>
  <read_first>
    - tests/phase-65/stitch-bridge-registration.test.mjs (reference: test file structure, import pattern, describe/test blocks)
    - tests/phase-65/tool-map-stitch.test.mjs (reference: file-parse assertion patterns)
    - workflows/wireframe.md (target: verify --use-stitch sections exist)
    - workflows/mockup.md (target: verify --use-stitch sections exist)
    - .planning/REQUIREMENTS.md (WFR-01 through WFR-06, EFF-02, EFF-05 definitions)
  </read_first>
  <action>
Create `tests/phase-66/` directory and three test files:

**File 1: tests/phase-66/wireframe-stitch-flag.test.mjs**

```javascript
/**
 * wireframe-stitch-flag.test.mjs
 * Phase 66 — Wireframe + Mockup Stitch Integration
 *
 * Tests: WFR-01 (--use-stitch flag), WFR-02 (STH artifact paths),
 *        WFR-04 (manifest source:stitch), WFR-06 (fallback), EFF-02 (local paths)
 */
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'fs';
import { fileURLToPath } from 'url';
import { resolve } from 'path';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');
const wireframeMd = readFileSync(resolve(ROOT, 'workflows', 'wireframe.md'), 'utf8');
```

Tests to include:

describe('WFR-01: --use-stitch flag on wireframe'):
- test: flags table contains `--use-stitch` row with `Boolean`
- test: Step 2 contains `Parse --use-stitch flag` section
- test: `USE_STITCH = true` and `USE_STITCH = false` assignments exist
- test: Step 3 contains Stitch MCP probe section

describe('WFR-02: STH artifact paths'):
- test: wireframe.md contains `STH-{slug}.html` file path
- test: wireframe.md contains `STH-{slug}.png` file path
- test: target directory is `.planning/design/ux/wireframes/`

describe('WFR-04: manifest source:stitch metadata'):
- test: wireframe.md contains `manifest-update STH-{slug} source stitch`
- test: wireframe.md contains `manifest-update STH-{slug} stitch_annotated true`
- test: wireframe.md contains `manifest-add-artifact STH-{slug}`

describe('WFR-06: graceful fallback'):
- test: wireframe.md contains `FALLBACK_SCREENS` or fallback section
- test: wireframe.md contains `Falling back to Claude HTML/CSS` message text
- test: wireframe.md references `checkStitchQuota` for quota-based fallback

describe('EFF-02: local artifact paths in manifest'):
- test: manifest path field contains `.planning/design/ux/wireframes/STH-` (local path, not URL)
- test: wireframe.md does NOT contain any Stitch download URL in manifest path

**File 2: tests/phase-66/mockup-stitch-flag.test.mjs**

```javascript
/**
 * mockup-stitch-flag.test.mjs
 * Phase 66 — Wireframe + Mockup Stitch Integration
 *
 * Tests: WFR-05 (--use-stitch flag on mockup)
 */
```

Tests to include:

describe('WFR-05: --use-stitch flag on mockup'):
- test: flags table contains `--use-stitch` row
- test: Step 2 contains `Parse --use-stitch flag` section
- test: mockup.md contains `4-STITCH` section
- test: mockup.md contains `STH-{slug}-hifi.html` artifact name
- test: mockup.md contains `.planning/design/ux/mockups/` target directory
- test: mockup.md contains `manifest-update STH-{slug}-hifi source stitch`
- test: mockup.md does NOT contain `stitch:fetch-screen-image` in Stitch branch (no PNG for mockups)
- test: mockup.md contains `stitch:generate-screen` and `stitch:fetch-screen-code`

**File 3: tests/phase-66/annotation-injection.test.mjs**

```javascript
/**
 * annotation-injection.test.mjs
 * Phase 66 — Wireframe + Mockup Stitch Integration
 *
 * Tests: WFR-03 (annotation injection), EFF-05 (per-screen immediate injection)
 */
```

Tests to include:

describe('WFR-03: annotation injection'):
- test: wireframe.md contains `<!-- @component: Navigation -->` pattern
- test: wireframe.md contains `<!-- @component: Header -->` pattern
- test: wireframe.md contains `<!-- @component: MainContent -->` pattern
- test: wireframe.md contains `<!-- @component: Section -->` pattern
- test: wireframe.md contains `<!-- @component: Form -->` pattern
- test: wireframe.md contains all 5 semantic tag triggers (`<nav`, `<header`, `<main`, `<section`, `<form`)
- test: mockup.md also contains annotation injection section with same 5 patterns

describe('EFF-05: per-screen immediate annotation'):
- test: In wireframe.md, annotation injection appears BEFORE `manifest-add-artifact` (check indexOf ordering)
- test: In wireframe.md, annotation section appears within the per-screen loop (between fetch and persist)
- test: In mockup.md, same ordering constraint holds

All three files must use the Phase 65 import pattern (node:test, node:assert/strict, readFileSync, ROOT resolution). Each test should use `assert.match()` for regex checks and `assert.ok()` with `.includes()` for string checks. Use `assert.strictEqual()` for count checks where applicable.
  </action>
  <verify>
    <automated>mkdir -p tests/phase-66 2>/dev/null; node --test tests/phase-66/wireframe-stitch-flag.test.mjs tests/phase-66/mockup-stitch-flag.test.mjs tests/phase-66/annotation-injection.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-66/wireframe-stitch-flag.test.mjs exists with describe blocks for WFR-01, WFR-02, WFR-04, WFR-06, EFF-02
    - tests/phase-66/mockup-stitch-flag.test.mjs exists with describe block for WFR-05
    - tests/phase-66/annotation-injection.test.mjs exists with describe blocks for WFR-03, EFF-05
    - `node --test tests/phase-66/wireframe-stitch-flag.test.mjs` exits 0
    - `node --test tests/phase-66/mockup-stitch-flag.test.mjs` exits 0
    - `node --test tests/phase-66/annotation-injection.test.mjs` exits 0
    - Each test file imports from `node:test` and `node:assert/strict`
    - Each test file reads workflow files with `readFileSync`
  </acceptance_criteria>
  <done>Three Nyquist test files created covering WFR-01 through WFR-05, WFR-06 (partial), EFF-02, EFF-05, all passing</done>
</task>

<task type="auto">
  <name>Task 2: Create consent-gates and fallback-behavior test files, run full suite</name>
  <files>tests/phase-66/consent-gates.test.mjs, tests/phase-66/fallback-behavior.test.mjs</files>
  <read_first>
    - tests/phase-66/wireframe-stitch-flag.test.mjs (reference: established test patterns from Task 1)
    - workflows/wireframe.md (verify consent prompt templates, fallback sections)
    - workflows/mockup.md (verify consent prompt templates, fallback sections)
    - .planning/REQUIREMENTS.md (CONSENT-01 through CONSENT-04, EFF-04 definitions)
  </read_first>
  <action>
Create two more test files:

**File 4: tests/phase-66/consent-gates.test.mjs**

```javascript
/**
 * consent-gates.test.mjs
 * Phase 66 — Wireframe + Mockup Stitch Integration
 *
 * Tests: CONSENT-01 (outbound consent), CONSENT-02 (inbound consent),
 *        CONSENT-03 (data visibility), CONSENT-04 (batch consent)
 */
```

Tests to include:

describe('CONSENT-01: outbound consent before Stitch call'):
- test: wireframe.md contains AskUserQuestion or consent prompt BEFORE `stitch:generate-screen` (indexOf ordering: consent text index < generate-screen index)
- test: mockup.md contains outbound consent BEFORE `stitch:generate-screen`
- test: wireframe.md outbound consent section contains `Approve` or approval prompt text

describe('CONSENT-02: inbound consent before persist'):
- test: wireframe.md contains inbound consent section with `Persist` or `persist` prompt
- test: wireframe.md inbound consent appears BEFORE Write/persist step (indexOf ordering)
- test: mockup.md contains inbound consent section

describe('CONSENT-03: data visibility in consent prompts'):
- test: wireframe.md outbound consent contains `Google Stitch` service name
- test: wireframe.md outbound consent contains `stitch.withgoogle.com` URL
- test: wireframe.md inbound consent shows artifact type (`HTML`) and target path (`.planning/design/ux/wireframes/`)
- test: mockup.md consent prompts contain `Google Stitch` and show target path `mockups/`

describe('CONSENT-04: batch consent for multi-screen'):
- test: wireframe.md contains batch consent pattern — single AskUserQuestion with screen count/list BEFORE per-screen loop
- test: wireframe.md batch consent mentions `Screens ({count})` or lists multiple screens
- test: mockup.md contains similar batch consent pattern

**File 5: tests/phase-66/fallback-behavior.test.mjs**

```javascript
/**
 * fallback-behavior.test.mjs
 * Phase 66 — Wireframe + Mockup Stitch Integration
 *
 * Tests: EFF-04 (10-second timeout + single attempt), WFR-06 (fallback triggers)
 */
```

Tests to include:

describe('EFF-04: 10-second timeout with immediate fallback'):
- test: wireframe.md Step 3 Stitch probe references 10-second timeout (contains `10` near probe section)
- test: wireframe.md does NOT contain retry loop patterns for Stitch calls (no `retry` or `attempt.*2` near stitch:generate)
- test: mockup.md has same timeout pattern

describe('WFR-06: three fallback triggers'):
- test: wireframe.md contains quota exhaustion fallback (references `quota_exhausted` or `allowed: false`)
- test: wireframe.md contains MCP unavailable fallback (references `STITCH_MCP_AVAILABLE = false`)
- test: wireframe.md contains Stitch error fallback (`STITCH_FAILED` or error handling in generate call)
- test: wireframe.md fallback produces Claude artifacts (references `WFR-` prefix for fallback, not `STH-`)
- test: mockup.md contains same three fallback triggers

**After creating both files, run the full Phase 66 suite:**
```bash
node --test tests/phase-66/*.test.mjs
```

**Then run the combined Phase 65 + Phase 66 suite to confirm no regressions:**
```bash
node --test tests/phase-65/*.test.mjs tests/phase-66/*.test.mjs
```
  </action>
  <verify>
    <automated>node --test tests/phase-66/*.test.mjs && node --test tests/phase-65/*.test.mjs tests/phase-66/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-66/consent-gates.test.mjs exists with describe blocks for CONSENT-01, CONSENT-02, CONSENT-03, CONSENT-04
    - tests/phase-66/fallback-behavior.test.mjs exists with describe blocks for EFF-04, WFR-06
    - `node --test tests/phase-66/consent-gates.test.mjs` exits 0
    - `node --test tests/phase-66/fallback-behavior.test.mjs` exits 0
    - `node --test tests/phase-66/*.test.mjs` exits 0 (all 5 files pass)
    - `node --test tests/phase-65/*.test.mjs tests/phase-66/*.test.mjs` exits 0 (no regressions)
    - consent-gates.test.mjs verifies outbound consent appears before stitch:generate-screen (indexOf check)
    - consent-gates.test.mjs verifies Google Stitch and stitch.withgoogle.com appear in consent text
    - fallback-behavior.test.mjs verifies no retry loops exist
    - fallback-behavior.test.mjs verifies all three fallback triggers (quota, MCP, error)
  </acceptance_criteria>
  <done>All 5 Nyquist test files pass, full Phase 65+66 suite passes with no regressions, every Phase 66 requirement ID has at least one automated test assertion</done>
</task>

</tasks>

<verification>
1. `node --test tests/phase-66/*.test.mjs` — all 5 files pass (exit 0)
2. `node --test tests/phase-65/*.test.mjs tests/phase-66/*.test.mjs` — combined suite passes (exit 0)
3. Every requirement ID appears in at least one test file's describe() comment:
   - WFR-01 through WFR-06: wireframe-stitch-flag + mockup-stitch-flag + fallback-behavior
   - CONSENT-01 through CONSENT-04: consent-gates
   - EFF-01, EFF-02: wireframe-stitch-flag (EFF-01 is manual-only for downstream phases, Phase 66 side covered by EFF-02 local path check)
   - EFF-04: fallback-behavior
   - EFF-05: annotation-injection
</verification>

<success_criteria>
- 5 test files exist in tests/phase-66/ directory
- All tests pass with `node --test tests/phase-66/*.test.mjs`
- Phase 65 tests still pass (no regressions)
- Every Phase 66 requirement has automated test coverage
- Tests use file-parse assertions (readFileSync + string/regex checks), not runtime behavior tests
</success_criteria>

<output>
After completion, create `.planning/phases/66-wireframe-mockup-stitch-integration/66-03-SUMMARY.md`
</output>
