---
phase: 41-linear-jira-integration
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/connect.md
  - commands/sync.md
  - commands/handoff.md
autonomous: true
requirements: [LIN-01, LIN-03, JIRA-01, JIRA-03, JIRA-04]

must_haves:
  truths:
    - "connect linear --confirm prompts for team selection and stores teamId in mcp-connections.json"
    - "connect atlassian --confirm prompts for project key and site URL and stores them in mcp-connections.json"
    - "/pde:sync --linear routes to sync-linear.md workflow"
    - "/pde:sync --jira routes to sync-jira.md workflow"
    - "/pde:sync without flags reads task_tracker from config and dispatches accordingly"
    - "/pde:handoff --create-linear-issues routes to handoff-create-linear-issues.md"
    - "/pde:handoff --create-jira-tickets routes to handoff-create-jira-tickets.md"
  artifacts:
    - path: "workflows/connect.md"
      provides: "Linear and Atlassian metadata capture branches in connect flow"
      contains: "LINEAR_TEAM_ID"
    - path: "commands/sync.md"
      provides: "--linear, --jira flags and task_tracker dispatch"
      contains: "sync-linear.md"
    - path: "commands/handoff.md"
      provides: "--create-linear-issues and --create-jira-tickets flags"
      contains: "handoff-create-linear-issues.md"
  key_links:
    - from: "commands/sync.md"
      to: "workflows/sync-linear.md"
      via: "flag dispatch"
      pattern: "--linear.*sync-linear.md"
    - from: "commands/sync.md"
      to: "workflows/sync-jira.md"
      via: "flag dispatch"
      pattern: "--jira.*sync-jira.md"
    - from: "commands/sync.md"
      to: ".planning/config.json"
      via: "task_tracker read"
      pattern: "task_tracker"
---

<objective>
Update command entry points (sync.md, handoff.md) and connect workflow to support Linear and Atlassian services with proper flag dispatch and metadata capture.

Purpose: Command files must dispatch to the correct workflow based on flags (--linear, --jira) or task_tracker config. The connect workflow must capture service-specific metadata (teamId for Linear, projectKey/siteUrl for Jira) during --confirm.
Output: Updated connect.md, sync.md, handoff.md with new service routing.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-linear-jira-integration/41-RESEARCH.md

<interfaces>
<!-- Existing command and workflow patterns the executor must follow -->

From commands/sync.md (current):
```markdown
---
name: pde:sync
description: Sync external service data into PDE planning state
argument-hint: '--github [--dry-run]'
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - mcp__github__*
---
```
The process block dispatches on `--github` flag. Must add `--linear`, `--jira` flags and task_tracker-based default dispatch.

From commands/handoff.md (current):
```markdown
---
name: pde:handoff
argument-hint: "[--create-prs] [--quick] [--dry-run] [--verbose] [--no-mcp] [--no-sequential-thinking] [--force]"
allowed-tools:
  - Read, Write, Edit, Bash, Glob, Grep, Task
  - mcp__sequential-thinking__*
  - mcp__github__*
---
```
The process block routes `--create-prs` to handoff-create-prs.md. Must add `--create-linear-issues` and `--create-jira-tickets` flags.

From workflows/connect.md (current):
Step 3.5 is GitHub-specific (captures repo). Step 4 has GitHub and non-GitHub branches.
Must add Step 3.6 for Linear (team selection) and Step 3.7 for Atlassian (project key + site URL).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Linear and Atlassian metadata capture branches to connect.md</name>
  <files>workflows/connect.md</files>
  <read_first>
    - workflows/connect.md
    - .planning/phases/41-linear-jira-integration/41-RESEARCH.md
  </read_first>
  <action>
Modify `workflows/connect.md` to add Linear-specific and Atlassian-specific metadata capture steps.

**Change 1: Add Step 3.6 — Capture Linear Team (Linear only, --confirm present)**

Insert after Step 3.5 (GitHub repo capture) and before Step 4. This step runs when `--confirm` flag IS present AND `SERVICE_KEY` equals `linear`:

```markdown
## 3.6. Capture Linear Team (Linear only, --confirm present)

If `--confirm` flag IS present AND `SERVICE_KEY` equals `linear`:

Before recording the connection, the workflow needs the user's Linear team ID. Linear requires teamId for issue listing and creation.

Call the MCP tool `mcp__linear__list_teams` (looked up via bridge.call('linear:list-teams')) to fetch available teams. This call happens in Claude Code's execution context as a normal tool use.

If the MCP call succeeds and returns one or more teams:
- Display the team list:
  ```
  Available Linear teams:

    1. <team.name> (ID: <team.id>)
    2. <team.name> (ID: <team.id>)
    ...

  Select a team (enter number):
  ```
- Wait for user selection. Store the selected team's `id` as LINEAR_TEAM_ID and `name` as LINEAR_TEAM_NAME.

If the MCP call fails or returns no teams:
- Ask manually:
  ```
  Could not fetch teams automatically.
  What is your Linear team ID? (Found at linear.app/settings -> API -> Team ID, or visible in issue URLs as the prefix before the number)
  ```
- Wait for user input. Store as LINEAR_TEAM_ID. Set LINEAR_TEAM_NAME to empty string.

Then proceed to Step 4, passing teamId and teamName as extraFields.
```

**Change 2: Add Step 3.7 — Capture Jira Project (Atlassian only, --confirm present)**

Insert after Step 3.6 and before Step 4:

```markdown
## 3.7. Capture Jira Project (Atlassian only, --confirm present)

If `--confirm` flag IS present AND `SERVICE_KEY` equals `atlassian`:

Before recording the connection, the workflow needs the user's Jira project key and site URL.

Display:
```
PDE connects to Atlassian Cloud (*.atlassian.net). Jira Data Center is not supported in v0.5.

What is your Jira site URL? (e.g., https://myorg.atlassian.net)
```

Wait for user input. Store as JIRA_SITE_URL. Validate it contains `atlassian.net` — if not, display:
```
Warning: URL does not appear to be an Atlassian Cloud site. Jira Data Center is not supported in v0.5. Continue anyway? (y/n)
```
If user says no, stop.

Then call the MCP tool `mcp__atlassian__getVisibleJiraProjectsList` (looked up via bridge.call('jira:list-projects')) to fetch accessible projects. This call happens in Claude Code's execution context.

If the MCP call succeeds and returns one or more projects:
- Display the project list:
  ```
  Available Jira projects:

    1. <project.name> (<project.key>)
    2. <project.name> (<project.key>)
    ...

  Select a project (enter number):
  ```
- Wait for user selection. Store the selected project's `key` as JIRA_PROJECT_KEY and `name` as JIRA_PROJECT_NAME.

If the MCP call fails or returns no projects:
- Ask manually:
  ```
  Could not fetch projects automatically.
  What is your Jira project key? (e.g., PROJ, ENG, TEAM)
  ```
- Wait for user input. Store as JIRA_PROJECT_KEY. Set JIRA_PROJECT_NAME to empty string.

Then proceed to Step 4, passing projectKey, projectName, and siteUrl as extraFields.
```

**Change 3: Extend Step 4 to handle Linear and Atlassian connections**

In Step 4, add two new service-specific branches (keeping the existing GitHub and default branches):

For Linear connections (`SERVICE_KEY` equals `linear`):
```bash
node --input-type=module <<'EOF'
import { createRequire } from 'module';
const req = createRequire(import.meta.url);
const b = req(`${process.env.CLAUDE_PLUGIN_ROOT}/bin/lib/mcp-bridge.cjs`);
const result = b.updateConnectionStatus(process.env.SERVICE_KEY, 'connected', {
  connected_at: new Date().toISOString(),
  teamId: process.env.LINEAR_TEAM_ID,
  teamName: process.env.LINEAR_TEAM_NAME || ''
});
process.stdout.write(JSON.stringify(result) + '\n');
EOF
```

For Atlassian connections (`SERVICE_KEY` equals `atlassian`):
```bash
node --input-type=module <<'EOF'
import { createRequire } from 'module';
const req = createRequire(import.meta.url);
const b = req(`${process.env.CLAUDE_PLUGIN_ROOT}/bin/lib/mcp-bridge.cjs`);
const result = b.updateConnectionStatus(process.env.SERVICE_KEY, 'connected', {
  connected_at: new Date().toISOString(),
  projectKey: process.env.JIRA_PROJECT_KEY,
  projectName: process.env.JIRA_PROJECT_NAME || '',
  siteUrl: process.env.JIRA_SITE_URL
});
process.stdout.write(JSON.stringify(result) + '\n');
EOF
```

Keep the existing GitHub branch (with `repo: process.env.GITHUB_REPO`) and the default non-service-specific branch unchanged.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && grep -c "LINEAR_TEAM_ID" workflows/connect.md && grep -c "JIRA_PROJECT_KEY" workflows/connect.md && grep -c "JIRA_SITE_URL" workflows/connect.md && grep -c "3.6" workflows/connect.md && grep -c "3.7" workflows/connect.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/connect.md contains `## 3.6. Capture Linear Team`
    - workflows/connect.md contains `## 3.7. Capture Jira Project`
    - workflows/connect.md contains `LINEAR_TEAM_ID` at least 3 times (step 3.6 store + step 4 use + env var)
    - workflows/connect.md contains `JIRA_PROJECT_KEY` at least 3 times (step 3.7 store + step 4 use + env var)
    - workflows/connect.md contains `JIRA_SITE_URL` at least 3 times
    - workflows/connect.md contains `teamId: process.env.LINEAR_TEAM_ID`
    - workflows/connect.md contains `projectKey: process.env.JIRA_PROJECT_KEY`
    - workflows/connect.md contains `siteUrl: process.env.JIRA_SITE_URL`
    - workflows/connect.md contains `mcp__linear__list_teams` or `bridge.call('linear:list-teams')`
    - workflows/connect.md contains `mcp__atlassian__getVisibleJiraProjectsList` or `bridge.call('jira:list-projects')`
    - workflows/connect.md contains `Jira Data Center is not supported in v0.5`
    - Existing Step 3.5 (GitHub repo capture) is unchanged
    - Existing Step 4 GitHub branch (with `repo: process.env.GITHUB_REPO`) is unchanged
    - Existing Steps 0, 1, 2, 3, 5 are unchanged
  </acceptance_criteria>
  <done>connect.md captures teamId for Linear and projectKey/siteUrl for Atlassian during --confirm flow, storing them as extraFields in mcp-connections.json.</done>
</task>

<task type="auto">
  <name>Task 2: Update sync.md and handoff.md with Linear/Jira flag dispatch and task_tracker routing</name>
  <files>commands/sync.md, commands/handoff.md</files>
  <read_first>
    - commands/sync.md
    - commands/handoff.md
    - workflows/sync-github.md
    - workflows/handoff-create-prs.md
  </read_first>
  <action>
**Change 1: Update commands/sync.md**

Update the frontmatter:
- `argument-hint`: change to `'--github | --linear | --jira [--dry-run]'`
- `allowed-tools`: add `mcp__linear__*` and `mcp__atlassian__*` to the list (keep existing tools)

Update the process block. Replace the current process with:

```markdown
<process>
Parse $ARGUMENTS to determine which service to sync.

If `--github` is present in $ARGUMENTS, follow @workflows/sync-github.md exactly, passing all of $ARGUMENTS.

If `--linear` is present in $ARGUMENTS, follow @workflows/sync-linear.md exactly, passing all of $ARGUMENTS.

If `--jira` is present in $ARGUMENTS, follow @workflows/sync-jira.md exactly, passing all of $ARGUMENTS.

If no service flag is provided, check the task_tracker config to determine the default service:

```bash
node --input-type=module <<'EOF'
import { createRequire } from 'module';
const req = createRequire(import.meta.url);
const fs = req('fs');
const path = req('path');
const configPath = path.join(process.cwd(), '.planning', 'config.json');
let taskTracker = 'none';
try {
  const config = JSON.parse(fs.readFileSync(configPath, 'utf-8'));
  taskTracker = config.task_tracker || 'none';
} catch {}
process.stdout.write(JSON.stringify({ taskTracker }) + '\n');
EOF
```

Parse the JSON output. Based on `taskTracker`:
- If `taskTracker` is `"linear"`: follow @workflows/sync-linear.md, passing all of $ARGUMENTS.
- If `taskTracker` is `"jira"`: follow @workflows/sync-jira.md, passing all of $ARGUMENTS.
- If `taskTracker` is `"none"` or any other value, display usage:

```
Usage: /pde:sync --github | --linear | --jira

Sync external service data into PDE planning state.

Available flags:
  --github      Sync GitHub issues to REQUIREMENTS.md
  --linear      Sync Linear issues to REQUIREMENTS.md and cycles to ROADMAP.md
  --jira        Sync Jira issues to REQUIREMENTS.md and epics to REQUIREMENTS.md
  --dry-run     Preview changes without writing to disk

Tip: Set a default task tracker with /pde:settings task_tracker linear (or jira)
     Then just run /pde:sync without flags.
```
</process>
```

**Change 2: Update commands/handoff.md**

Update the frontmatter:
- `argument-hint`: change to `"[--create-prs] [--create-linear-issues] [--create-jira-tickets] [--quick] [--dry-run] [--verbose] [--no-mcp] [--no-sequential-thinking] [--force]"`
- `allowed-tools`: add `mcp__linear__*` and `mcp__atlassian__*` to the list (keep existing tools)

Update the process block. Replace the current process with:

```markdown
<process>
If $ARGUMENTS contains `--create-prs`, follow @workflows/handoff-create-prs.md exactly, passing all of $ARGUMENTS.
If $ARGUMENTS contains `--create-linear-issues`, follow @workflows/handoff-create-linear-issues.md exactly, passing all of $ARGUMENTS.
If $ARGUMENTS contains `--create-jira-tickets`, follow @workflows/handoff-create-jira-tickets.md exactly, passing all of $ARGUMENTS.
Otherwise, follow @workflows/handoff.md exactly (existing behavior unchanged).

Pass all of $ARGUMENTS to the workflow.
</process>
```
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && grep -c "sync-linear.md" commands/sync.md && grep -c "sync-jira.md" commands/sync.md && grep -c "task_tracker" commands/sync.md && grep -c "handoff-create-linear-issues.md" commands/handoff.md && grep -c "handoff-create-jira-tickets.md" commands/handoff.md && grep "mcp__linear__" commands/sync.md && grep "mcp__atlassian__" commands/sync.md</automated>
  </verify>
  <acceptance_criteria>
    - commands/sync.md argument-hint contains `--linear | --jira`
    - commands/sync.md allowed-tools contains `mcp__linear__*`
    - commands/sync.md allowed-tools contains `mcp__atlassian__*`
    - commands/sync.md process block contains `@workflows/sync-linear.md`
    - commands/sync.md process block contains `@workflows/sync-jira.md`
    - commands/sync.md process block contains `task_tracker` config read logic
    - commands/sync.md process block contains `Tip: Set a default task tracker`
    - commands/sync.md still contains `@workflows/sync-github.md` (existing GitHub dispatch preserved)
    - commands/handoff.md argument-hint contains `--create-linear-issues`
    - commands/handoff.md argument-hint contains `--create-jira-tickets`
    - commands/handoff.md allowed-tools contains `mcp__linear__*`
    - commands/handoff.md allowed-tools contains `mcp__atlassian__*`
    - commands/handoff.md process block contains `@workflows/handoff-create-linear-issues.md`
    - commands/handoff.md process block contains `@workflows/handoff-create-jira-tickets.md`
    - commands/handoff.md still contains `@workflows/handoff-create-prs.md` (existing GitHub PR dispatch preserved)
  </acceptance_criteria>
  <done>sync.md dispatches to sync-linear.md on --linear, sync-jira.md on --jira, and falls back to task_tracker config when no flag. handoff.md dispatches to Linear and Jira ticket creation workflows on respective flags.</done>
</task>

</tasks>

<verification>
1. `grep -c 'sync-linear.md' commands/sync.md` outputs >= 2
2. `grep -c 'task_tracker' commands/sync.md` outputs >= 2
3. `grep -c 'LINEAR_TEAM_ID' workflows/connect.md` outputs >= 3
4. `grep -c 'JIRA_PROJECT_KEY' workflows/connect.md` outputs >= 3
5. `grep -c 'handoff-create-linear-issues.md' commands/handoff.md` outputs >= 1
6. `grep -c 'handoff-create-jira-tickets.md' commands/handoff.md` outputs >= 1
</verification>

<success_criteria>
- connect.md captures teamId for Linear connections and projectKey/siteUrl for Atlassian connections
- sync.md dispatches --linear to sync-linear.md, --jira to sync-jira.md, no flags to task_tracker default
- handoff.md dispatches --create-linear-issues and --create-jira-tickets to respective workflows
- All existing GitHub functionality preserved unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/41-linear-jira-integration/41-02-SUMMARY.md`
</output>
