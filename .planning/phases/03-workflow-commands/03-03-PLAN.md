---
phase: 03-workflow-commands
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: []
autonomous: false
requirements: [CMD-01, CMD-02, CMD-03, CMD-04, CMD-05, CMD-06, CMD-07, CMD-08, CMD-09, CMD-10, CMD-11, CMD-12, CMD-13]

must_haves:
  truths:
    - "/pde:progress invokes successfully and calls pde-tools.cjs without path errors"
    - "/pde:help renders PDE command reference without errors"
    - "CLAUDE_PLUGIN_ROOT expands correctly in bash blocks inside workflow files"
    - "All 29+ /pde: commands appear in Claude Code's command palette"
  artifacts: []
  key_links:
    - from: "commands/progress.md"
      to: "workflows/progress.md"
      via: "@${CLAUDE_PLUGIN_ROOT}/workflows/progress.md"
      pattern: "CLAUDE_PLUGIN_ROOT.*workflows/progress"
    - from: "workflows/progress.md"
      to: "bin/pde-tools.cjs"
      via: "node ${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs"
      pattern: "CLAUDE_PLUGIN_ROOT.*bin/pde-tools"
---

<objective>
Smoke-test the PDE plugin commands to verify ${CLAUDE_PLUGIN_ROOT} expansion works in bash blocks and that the full command -> workflow -> pde-tools.cjs chain is operational.

Purpose: Empirically validates the critical open question from research -- whether CLAUDE_PLUGIN_ROOT expands in multi-line bash blocks inside workflow .md files. Also confirms the end-to-end command palette registration works.
Output: Confirmed working /pde: commands; if CLAUDE_PLUGIN_ROOT fails in bash, apply the documented fallback (PLUGIN_ROOT="${CLAUDE_PLUGIN_ROOT}" at top of each bash block) and re-verify.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workflow-commands/03-RESEARCH.md
@.planning/phases/03-workflow-commands/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke test /pde:progress and /pde:help (validates CLAUDE_PLUGIN_ROOT expansion)</name>
  <files>(no files modified unless fallback needed; if fallback needed: workflows/*.md)</files>
  <action>
Invoke /pde:progress in the current Claude Code session. This is the simplest workflow -- it calls pde-tools.cjs to read STATE.md and display progress.

What to observe:
1. Does the command appear in Claude Code's command palette?
2. Does the workflow load (i.e., does @${CLAUDE_PLUGIN_ROOT}/workflows/progress.md resolve)?
3. Does the bash block `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" ...` execute successfully?

If CLAUDE_PLUGIN_ROOT does NOT expand in bash blocks:
- Apply the documented fallback: add `PLUGIN_ROOT="${CLAUDE_PLUGIN_ROOT}"` as the first line of each bash block, then replace `${CLAUDE_PLUGIN_ROOT}` with `${PLUGIN_ROOT}` within that block.
- Apply this fix to ALL 34 workflow files using sed.
- Re-test /pde:progress after the fix.

If /pde:progress works, also test /pde:help to confirm another command works end-to-end.

Record the test results (pass/fail, any errors) for the checkpoint task.
  </action>
  <verify>
    <automated>echo "Manual invocation required: run /pde:progress in Claude Code session and confirm pde-tools.cjs call succeeds"</automated>
  </verify>
  <done>/pde:progress executes without path errors. CLAUDE_PLUGIN_ROOT expansion confirmed (or fallback applied and working). /pde:help also renders correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies /pde: commands appear in palette and are invokable</name>
  <files>(none)</files>
  <action>
Present the user with verification steps. The user opens Claude Code in this project directory, checks the command palette for /pde: commands, and confirms at least the 12 CMD-requirement commands are present and invokable.
  </action>
  <verify>
    <automated>echo "Human verification: user confirms commands appear in palette"</automated>
  </verify>
  <done>User confirms /pde: commands appear in Claude Code command palette and /pde:progress and /pde:help execute without errors.</done>
  <what-built>
Complete PDE command system: 34 command stubs in commands/ registering /pde: slash commands, 34 workflow files in workflows/ with portable CLAUDE_PLUGIN_ROOT paths, lib/ui/ rendering scripts, references/ and templates/ directories. The full two-tier delegation chain (command -> workflow -> pde-tools.cjs) should be operational.
  </what-built>
  <how-to-verify>
1. Open Claude Code in this project directory
2. Type "/" in the input and look for /pde: commands in the command palette
3. Verify at minimum these commands appear:
   - /pde:new-project
   - /pde:plan-phase
   - /pde:execute-phase
   - /pde:verify-work
   - /pde:progress
   - /pde:help
   - /pde:discuss-phase
   - /pde:quick
   - /pde:map-codebase
   - /pde:new-milestone
   - /pde:complete-milestone
   - /pde:audit-milestone
4. Try invoking /pde:progress -- should display project state without errors
5. Try invoking /pde:help -- should list all PDE commands
6. Confirm no error messages about missing files or unresolved paths
  </how-to-verify>
  <resume-signal>Type "approved" if commands appear and work, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
1. /pde:progress executes without path errors
2. /pde:help renders command list with /pde: prefix
3. /pde: commands visible in Claude Code command palette
4. No "cannot find module" or path resolution errors
</verification>

<success_criteria>
- CLAUDE_PLUGIN_ROOT confirmed working in bash blocks (or fallback applied)
- /pde:progress and /pde:help verified working end-to-end
- User confirms commands appear in palette and are invokable
- Phase 3 success criteria met: all /pde: commands are operational
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-commands/03-03-SUMMARY.md`
</output>
