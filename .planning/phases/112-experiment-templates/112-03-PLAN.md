---
phase: 112-experiment-templates
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - references/experiments/deploy.md
  - tests/phase-112/experiment-templates.test.mjs
autonomous: true
requirements: [EXP-12]
gap_closure: true

must_haves:
  truths:
    - "All 14 eligible design skills have at least one experiment template (EXP-12)"
    - "deploy.md template passes experiment-schema.cjs parseExperimentFile() validation"
    - "Nyquist test suite expects and validates 14 templates, not 13"
  artifacts:
    - path: "references/experiments/deploy.md"
      provides: "Deploy skill experiment template"
      contains: "nyquist-metric.cjs"
    - path: "tests/phase-112/experiment-templates.test.mjs"
      provides: "Updated Nyquist test expecting 14 templates"
      contains: "deploy.md"
  key_links:
    - from: "references/experiments/deploy.md"
      to: "bin/nyquist-metric.cjs"
      via: "verify field shell command"
      pattern: "verify: node bin/nyquist-metric.cjs"
    - from: "references/experiments/deploy.md"
      to: "bin/lib/experiment-schema.cjs"
      via: "YAML frontmatter parsed by parseExperimentFile()"
      pattern: "metric.*direction.*verify.*mutable_files"
    - from: "tests/phase-112/experiment-templates.test.mjs"
      to: "references/experiments/deploy.md"
      via: "EXPECTED_TEMPLATES array inclusion"
      pattern: "'deploy.md'"
---

<objective>
Close the single EXP-12 verification gap: create the missing `references/experiments/deploy.md` template (the 14th eligible skill) and update the Nyquist test to expect 14 templates instead of 13.

Purpose: experiment-boundaries.md explicitly lists 14 eligible skills including `workflows/deploy.md`. EXP-12 requires all 14 to have templates. Only 13 were created by Plans 01-02.

Output: `deploy.md` experiment template + updated test file. Full EXP-12 compliance.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@references/experiment-boundaries.md
@.planning/phases/112-experiment-templates/112-VERIFICATION.md

<interfaces>
<!-- experiment-schema.cjs REQUIRED_FIELDS -->
REQUIRED_FIELDS = ['metric', 'direction', 'verify', 'mutable_files']

<!-- Metric for deploy (non-visual-output skill, same as recommend/competitive/opportunity/ideate) -->
nyquist-metric.cjs -- Nyquist pass count (integer), direction: max, no argv[2] needed

<!-- Existing template pattern (from references/experiments/recommend.md) -->
```yaml
slug: recommend-quality
metric: nyquist_pass_count
direction: max
verify: node bin/nyquist-metric.cjs
mutable_files:
  - workflows/recommend.md
immutable_files: []
iteration_budget: 30
time_budget_minutes: 60
```

Prose sections after frontmatter: ## Search Space, ## Constraints, ## Stopping Rationale

<!-- Current EXPECTED_TEMPLATES in test (13 entries, needs deploy.md added) -->
```javascript
const EXPECTED_TEMPLATES = [
  'wireframe.md', 'mockup.md', 'system.md', 'flows.md',
  'critique.md', 'hig.md', 'iterate.md', 'handoff.md',
  'brief.md', 'recommend.md', 'competitive.md',
  'opportunity.md', 'ideate.md',
];
```

<!-- Current describe label (needs "13" -> "14") -->
describe('EXP-12: all 13 design skill experiment templates exist', ...)

<!-- Current nyquistTemplates array (needs deploy.md added) -->
```javascript
const nyquistTemplates = ['recommend.md', 'competitive.md', 'opportunity.md', 'ideate.md', 'critique.md'];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deploy.md experiment template and update Nyquist test to expect 14 templates</name>
  <files>
    references/experiments/deploy.md
    tests/phase-112/experiment-templates.test.mjs
  </files>
  <read_first>
    references/experiments/recommend.md
    tests/phase-112/experiment-templates.test.mjs
    references/experiment-boundaries.md
  </read_first>
  <action>
**1. Create `references/experiments/deploy.md`:**

Follow the exact pattern from `recommend.md` (non-browser skill using nyquist-metric.cjs). The file must contain this YAML frontmatter:

```yaml
slug: deploy-quality
metric: nyquist_pass_count
direction: max
verify: node bin/nyquist-metric.cjs
mutable_files:
  - workflows/deploy.md
immutable_files: []
iteration_budget: 30
time_budget_minutes: 60
```

Then three prose sections:

`## Search Space` -- Optimize prose in `workflows/deploy.md` within `<!-- OPTIMIZABLE -->` markers. Focus on deploy checklist framing, pre-deploy verification guidance, post-deploy smoke test instructions, environment configuration prose. Do NOT modify: Step 1 (init), artifact schema writes (deploy-manifest.json fields, designCoverage), error messages, MCP probe blocks, `<!-- LOCKED -->` sections.

`## Constraints` -- Only modify `<!-- OPTIMIZABLE -->` sections. Nyquist pass count measures structural quality of deploy workflow output. Mutations must remain within the annotated optimizable zones -- no changes to tool call blocks, schema writes, or error message format strings.

`## Stopping Rationale` -- Halt at consecutive_failure_limit (5), no_progress_limit (10), or iteration_budget (30).

**2. Update `tests/phase-112/experiment-templates.test.mjs`:**

Make these changes:

a) In `EXPECTED_TEMPLATES` array (line 31-36), add `'deploy.md'` as the last entry before the closing bracket. The array should have 14 entries total.

b) Replace the comment on line 37:
  FROM: `// Note: deploy.md is experiment-eligible but not a design skill — 13 design skill templates, not 14`
  TO: `// All 14 experiment-eligible design skill workflows from experiment-boundaries.md`

c) In the describe label on line 41, change `'all 13 design skill experiment templates exist'` to `'all 14 design skill experiment templates exist'`.

d) In the file header comment on line 4, change `all 13 experiment templates` to `all 14 experiment templates`.

e) Add `'deploy.md'` to the `nyquistTemplates` array on line 148. The array should become: `['recommend.md', 'competitive.md', 'opportunity.md', 'ideate.md', 'critique.md', 'deploy.md']` (6 entries).
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-112/experiment-templates.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - references/experiments/deploy.md exists
    - references/experiments/deploy.md contains "slug: deploy-quality"
    - references/experiments/deploy.md contains "metric: nyquist_pass_count"
    - references/experiments/deploy.md contains "direction: max"
    - references/experiments/deploy.md contains "verify: node bin/nyquist-metric.cjs"
    - references/experiments/deploy.md contains "workflows/deploy.md" in mutable_files
    - references/experiments/deploy.md contains "## Search Space" AND "## Constraints" AND "## Stopping Rationale"
    - tests/phase-112/experiment-templates.test.mjs EXPECTED_TEMPLATES contains 'deploy.md' (14 entries total)
    - tests/phase-112/experiment-templates.test.mjs describe label says "all 14" not "all 13"
    - tests/phase-112/experiment-templates.test.mjs nyquistTemplates array includes 'deploy.md'
    - `node --test tests/phase-112/experiment-templates.test.mjs` exits 0
  </acceptance_criteria>
  <done>deploy.md experiment template exists with valid frontmatter, test suite expects and validates 14 templates, all tests pass green</done>
</task>

</tasks>

<verification>
- `references/experiments/deploy.md` exists with all 4 REQUIRED_FIELDS in YAML frontmatter
- `deploy.md` passes `parseExperimentFile()` validation (tested by Nyquist suite)
- `EXPECTED_TEMPLATES` array has 14 entries including `'deploy.md'`
- `node --test tests/phase-112/experiment-templates.test.mjs` exits 0 with all assertions passing
- Total experiment template count: 14 (matching experiment-boundaries.md eligible list exactly)
</verification>

<success_criteria>
- references/experiments/deploy.md created following recommend.md pattern
- Test file updated: 14 entries in EXPECTED_TEMPLATES, describe label says "14", deploy.md in nyquistTemplates
- All Nyquist tests pass green
- EXP-12 gap closed: all 14 eligible skills have experiment templates
</success_criteria>

<output>
After completion, create `.planning/phases/112-experiment-templates/112-03-SUMMARY.md`
</output>
