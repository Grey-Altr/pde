---
phase: 23-fix-wireframe-filename-convention-final-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/wireframe.md
  - .planning/phases/14-design-system-pde-system/14-VALIDATION.md
  - .planning/phases/15.1-fix-integration-gaps-tech-debt/15.1-VALIDATION.md
  - .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md
autonomous: true
requirements:
  - ITR-01
  - ITR-02

must_haves:
  truths:
    - "wireframe.md writes files as WFR-{slug}.html, not {slug}.html"
    - "index.html links and nav links use the WFR- prefix"
    - "/pde:iterate can find wireframe files via its WFR-{slug}*.html Glob pattern"
    - "Phase 14 VALIDATION.md status is complete"
    - "Phase 15.1 VALIDATION.md status is complete"
    - "Phase 22 VALIDATION.md nyquist_compliant is true"
  artifacts:
    - path: "workflows/wireframe.md"
      provides: "WFR- prefix on all 6 filename references"
      contains: "WFR-{slug}.html"
    - path: ".planning/phases/14-design-system-pde-system/14-VALIDATION.md"
      provides: "Phase 14 validation complete"
      contains: "status: complete"
    - path: ".planning/phases/15.1-fix-integration-gaps-tech-debt/15.1-VALIDATION.md"
      provides: "Phase 15.1 validation complete"
      contains: "status: complete"
    - path: ".planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md"
      provides: "Phase 22 Nyquist sign-off"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "workflows/wireframe.md"
      to: "workflows/iterate.md"
      via: "WFR-{slug}.html filename convention"
      pattern: "WFR-\\{slug\\}"
---

<objective>
Fix the WFR- filename prefix mismatch between wireframe.md and iterate.md, and resolve the 3 remaining metadata tech debt items (Phase 14/15.1 VALIDATION status, Phase 22 VALIDATION Nyquist sign-off).

Purpose: Close WIRE-01 integration bug that breaks the wireframe-to-iterate handoff, and complete all v1.1 metadata compliance.
Output: Fixed wireframe.md (6 line edits), 3 updated VALIDATION.md files.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-fix-wireframe-filename-convention-final-tech-debt/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WFR- prefix in wireframe.md filename references</name>
  <files>workflows/wireframe.md</files>
  <read_first>
    - workflows/wireframe.md (full file — executor must see current state of all 6 target lines)
    - workflows/iterate.md (reference — confirms WFR- prefix is the authoritative convention at lines 111, 138, 143, 145, 293)
  </read_first>
  <action>
Edit workflows/wireframe.md to add the `WFR-` prefix to all 6 occurrences of `{slug}.html`. Each edit is a targeted string replacement on a specific line:

1. **Line 153** (dry-run planned output):
   - Find: `Files: .planning/design/ux/wireframes/{slug}.html (one per screen)`
   - Replace with: `Files: .planning/design/ux/wireframes/WFR-{slug}.html (one per screen)`

2. **Line 374** (in-screen nav links):
   - Find: `{  <a href="{slug}.html">{Screen Label}</a> for each existing screen}`
   - Replace with: `{  <a href="WFR-{slug}.html">{Screen Label}</a> for each existing screen}`

3. **Line 477** (write target path):
   - Find: "`.planning/design/ux/wireframes/{slug}.html`"
   - Replace with: "`.planning/design/ux/wireframes/WFR-{slug}.html`"

4. **Line 479** (display log):
   - Find: `Display per file: \`  -> Created: .planning/design/ux/wireframes/{slug}.html\``
   - Replace with: `Display per file: \`  -> Created: .planning/design/ux/wireframes/WFR-{slug}.html\``

5. **Line 513** (index.html list items):
   - Find: `{  <li><a href="{slug}.html">{Screen Label}</a>`
   - Replace with: `{  <li><a href="WFR-{slug}.html">{Screen Label}</a>`

6. **Line 699** (output section):
   - Find: "- `.planning/design/ux/wireframes/{slug}.html` — one self-contained HTML file per screen"
   - Replace with: "- `.planning/design/ux/wireframes/WFR-{slug}.html` — one self-contained HTML file per screen"

**Lines to NOT change:**
- Line 110: Glob pattern `*.html` (wildcard, already correct)
- Line 114: directory reference only
- Line 624: manifest-update directory path (not a filename)
- Lines 562, 614: DESIGN-STATE table entries (already use `WFR-wireframes/` directory code)

After all 6 edits, verify no remaining `{slug}.html` occurrences exist (only `WFR-{slug}.html` should remain).
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && echo "WFR-{slug} count:" && grep -c 'WFR-{slug}' workflows/wireframe.md && echo "Bare {slug}.html count (should be 0):" && grep -c '{slug}\.html' workflows/wireframe.md; test $? -ne 0 && echo "PASS: no bare {slug}.html found" || echo "FAIL: bare {slug}.html still present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'WFR-{slug}' workflows/wireframe.md` returns 6 or more
    - `grep '{slug}\.html' workflows/wireframe.md` returns NO matches (exit code 1)
    - Line 153 contains `WFR-{slug}.html`
    - Line 374 contains `href="WFR-{slug}.html"`
    - Line 477 contains `wireframes/WFR-{slug}.html`
    - Line 479 contains `WFR-{slug}.html`
    - Line 513 contains `href="WFR-{slug}.html"`
    - Line 699 contains `wireframes/WFR-{slug}.html`
  </acceptance_criteria>
  <done>All 6 filename references in wireframe.md use WFR-{slug}.html prefix. Zero bare {slug}.html references remain. The iterate.md WFR-{slug}*.html Glob will now match files produced by wireframe.md.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve 3 VALIDATION.md metadata tech debt items</name>
  <files>
    .planning/phases/14-design-system-pde-system/14-VALIDATION.md
    .planning/phases/15.1-fix-integration-gaps-tech-debt/15.1-VALIDATION.md
    .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/14-design-system-pde-system/14-VALIDATION.md (current state — status: draft at line 4)
    - .planning/phases/15.1-fix-integration-gaps-tech-debt/15.1-VALIDATION.md (current state — status: draft at line 4)
    - .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md (current state — status: draft, nyquist_compliant: false, wave_0_complete: false, checklist all unchecked, approval pending)
    - .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-01-SUMMARY.md (evidence that Phase 22 executed successfully — 13/13 compliance)
  </read_first>
  <action>
**Edit 1: Phase 14 VALIDATION.md**
- Line 4: change `status: draft` to `status: complete`
- No other changes needed (nyquist_compliant: true and wave_0_complete: true already set, approval line already reads "complete (Phase 15.1-02)")

**Edit 2: Phase 15.1 VALIDATION.md**
- Line 4: change `status: draft` to `status: complete`
- No other changes needed (nyquist_compliant: true and wave_0_complete: true already set, approval line already reads "complete (Phase 15.1-02)")

**Edit 3: Phase 22 VALIDATION.md — Full Nyquist sign-off**
Apply these exact changes:

Frontmatter (lines 3-6):
- `status: draft` -> `status: complete`
- `nyquist_compliant: false` -> `nyquist_compliant: true`
- `wave_0_complete: false` -> `wave_0_complete: true`

Body checklist (6 items near end of file) — change each `[ ]` to `[x]`:
- `- [x] All tasks have \`<automated>\` verify or Wave 0 dependencies`
- `- [x] Sampling continuity: no 3 consecutive tasks without automated verify`
- `- [x] Wave 0 covers all MISSING references`
- `- [x] No watch-mode flags`
- `- [x] Feedback latency < 5s`
- `- [x] \`nyquist_compliant: true\` set in frontmatter`

Approval line:
- `**Approval:** pending` -> `**Approval:** complete -- 2026-03-16`

Append after the approval line:

```
---

## Validation Audit 2026-03-16

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

All 5 tasks have automated grep verification. Phase executed successfully: 5 VALIDATION.md files updated to Nyquist compliant (phases 16, 17, 18, 20, 21), handoff.md Step 2b corrected to list all 7 designCoverage fields. No gaps to fill.
```

**Do NOT edit** `.planning/phases/13.2-manifest-top-level-nyquist-cleanup/13.2-01-SUMMARY.md` — already has `requirements_completed: [INFRA-04, BRF-02]` (verified).
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep 'status: complete' .planning/phases/14-design-system-pde-system/14-VALIDATION.md && grep 'status: complete' .planning/phases/15.1-fix-integration-gaps-tech-debt/15.1-VALIDATION.md && grep 'nyquist_compliant: true' .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md && grep 'status: complete' .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md && grep 'wave_0_complete: true' .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md && grep 'complete -- 2026-03-16' .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md && echo "ALL PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'status: complete' .planning/phases/14-design-system-pde-system/14-VALIDATION.md` returns a match
    - `grep 'status: complete' .planning/phases/15.1-fix-integration-gaps-tech-debt/15.1-VALIDATION.md` returns a match
    - `grep 'nyquist_compliant: true' .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md` returns a match
    - `grep 'status: complete' .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md` returns a match
    - `grep 'wave_0_complete: true' .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md` returns a match
    - `grep 'complete -- 2026-03-16' .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md` returns a match
    - `grep -c '\[x\]' .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md` returns 6
    - `grep 'Validation Audit' .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md` returns a match
    - `grep 'requirements_completed: \[INFRA-04, BRF-02\]' .planning/phases/13.2-manifest-top-level-nyquist-cleanup/13.2-01-SUMMARY.md` returns a match (verify-only, no edit)
  </acceptance_criteria>
  <done>Phase 14 and Phase 15.1 VALIDATION.md status is "complete". Phase 22 VALIDATION.md has full Nyquist sign-off (nyquist_compliant: true, wave_0_complete: true, all checklist items checked, approval dated, audit appendix present). Phase 13.2 SUMMARY requirements_completed confirmed as already correct.</done>
</task>

</tasks>

<verification>
Run all verification commands in sequence:

```bash
# Task 1: WFR- prefix fix
echo "=== Task 1: WFR prefix ===" && \
grep -c 'WFR-{slug}' workflows/wireframe.md && \
! grep '{slug}\.html' workflows/wireframe.md && \
echo "Task 1 PASS"

# Task 2: Metadata tech debt
echo "=== Task 2: Metadata ===" && \
grep 'status: complete' .planning/phases/14-design-system-pde-system/14-VALIDATION.md && \
grep 'status: complete' .planning/phases/15.1-fix-integration-gaps-tech-debt/15.1-VALIDATION.md && \
grep 'nyquist_compliant: true' .planning/phases/22-nyquist-compliance-tech-debt-cleanup/22-VALIDATION.md && \
grep 'requirements_completed: \[INFRA-04, BRF-02\]' .planning/phases/13.2-manifest-top-level-nyquist-cleanup/13.2-01-SUMMARY.md && \
echo "Task 2 PASS"

echo "=== ALL CHECKS PASSED ==="
```
</verification>

<success_criteria>
1. `workflows/wireframe.md` has 6+ occurrences of `WFR-{slug}` and 0 occurrences of bare `{slug}.html`
2. Phase 14 VALIDATION.md `status: complete`
3. Phase 15.1 VALIDATION.md `status: complete`
4. Phase 22 VALIDATION.md `nyquist_compliant: true`, `status: complete`, `wave_0_complete: true`, all checklist `[x]`, approval dated
5. Phase 13.2 SUMMARY `requirements_completed: [INFRA-04, BRF-02]` (verified, not edited)
</success_criteria>

<output>
After completion, create `.planning/phases/23-fix-wireframe-filename-convention-final-tech-debt/23-01-SUMMARY.md`
</output>
