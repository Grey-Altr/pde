---
phase: 19-design-to-code-handoff-pde-handoff
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/handoff.md
  - commands/handoff.md
autonomous: true
requirements: [HND-01, HND-02, HND-03]

must_haves:
  truths:
    - "Running /pde:handoff produces HND-handoff-spec-v{N}.md in .planning/design/handoff/"
    - "Running /pde:handoff produces HND-types-v{N}.ts with TypeScript interface declarations"
    - "Running /pde:handoff without STACK.md halts with a recovery message"
    - "Running /pde:handoff with sparse annotations emits a warning about TypeScript interface quality"
    - "design-manifest.json hasHandoff flag is set to true after handoff completes"
  artifacts:
    - path: "workflows/handoff.md"
      provides: "Full 7-step /pde:handoff skill workflow"
      min_lines: 400
    - path: "commands/handoff.md"
      provides: "Delegation stub wired to @workflows/handoff.md"
      contains: "Follow @workflows/handoff.md exactly"
  key_links:
    - from: "commands/handoff.md"
      to: "workflows/handoff.md"
      via: "delegation in process section"
      pattern: "@workflows/handoff.md"
    - from: "workflows/handoff.md"
      to: "pde-tools.cjs"
      via: "CLI calls for ensure-dirs, lock, manifest, coverage"
      pattern: "pde-tools.cjs design"
    - from: "workflows/handoff.md"
      to: "templates/handoff-spec.md"
      via: "output document structure scaffold"
      pattern: "templates/handoff-spec.md"
    - from: "workflows/handoff.md"
      to: ".planning/research/STACK.md"
      via: "hard dependency read for framework detection"
      pattern: "STACK.md"
---

<objective>
Create the /pde:handoff skill — a synthesis workflow that reads all upstream design pipeline artifacts (brief, flows, screen inventory, wireframes with annotations, design system tokens, critique reports, iteration changelogs) and produces two outputs: an implementation specification document (HND-handoff-spec-v{N}.md) and a TypeScript interface declarations file (HND-types-v{N}.ts).

Purpose: This is the terminal skill before the orchestrator — it bridges the design pipeline to implementation by producing stack-aligned component specs that engineers can implement without interpreting raw wireframes.

Output: `workflows/handoff.md` (new, ~500-600 lines), `commands/handoff.md` (rewritten from stub)
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-design-to-code-handoff-pde-handoff/19-RESEARCH.md
@.planning/phases/18-critique-driven-iteration-pde-iterate/18-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. -->

From commands/iterate.md (delegation stub pattern to replicate):
```markdown
---
name: pde:iterate
description: Revise design artifacts based on critique feedback with convergence tracking
argument-hint: '[--quick] [--force] [--dry-run]'
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Glob
  - Grep
  - Task
  - mcp__sequential-thinking__*
---
<objective>
Execute the /pde:iterate command.
</objective>

<process>
Follow @workflows/iterate.md exactly.

Pass all of $ARGUMENTS to the workflow.
</process>
```

From commands/handoff.md (CURRENT stub to be fully replaced):
```markdown
---
name: pde:handoff
description: Produce developer handoff package with component APIs
argument-hint: ""
allowed-tools: [Read, Write, Edit, Bash, Glob, Grep, Task]
---
<process>
**Status:** Planned -- available in PDE v2 (design pipeline).
...
</process>
```

From templates/handoff-spec.md (403 lines — output document scaffold):
- Sections: Design Coverage Summary, Route Structure, Global Token Mappings, Shared Component APIs, Accessibility Overview, Motion System, Gap Analysis, Test Specs Overview, Per-Screen Detail Specs (with React/Vue/Svelte stubs), Hardware Handoff (conditional)

From pde-tools.cjs design subcommands (CLI interface):
- ensure-dirs — creates .planning/design/ and all DOMAIN_DIRS including handoff/
- lock-acquire {skill} — acquires write lock (60s TTL)
- lock-release {skill} — releases write lock
- manifest-update {code} {field} {value} — sets artifact metadata
- manifest-set-top-level {field} {value} — sets root manifest field
- coverage-check — returns designCoverage JSON object
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workflows/handoff.md — full 7-step /pde:handoff skill workflow</name>
  <files>workflows/handoff.md</files>
  <read_first>
    - workflows/iterate.md (canonical 7-step pattern — replicate structure, especially Steps 1, 5, 6, 7)
    - workflows/critique.md (4-perspective synthesis approach — relevant to multi-artifact synthesis)
    - templates/handoff-spec.md (403-line output document scaffold — ALL sections must be populated by workflow instructions)
    - templates/handoff-manifest.json (separate handoff manifest template)
    - commands/handoff.md (current stub — understand what needs replacing)
    - references/skill-style-guide.md (output format, error messaging, universal flags)
    - references/mcp-integration.md (Sequential Thinking MCP probe pattern)
    - .planning/research/STACK.md (understand the file structure Claude must parse for framework detection)
    - workflows/wireframe.md (Step 4d — ANNOTATION comment format that handoff must parse)
    - bin/lib/design.cjs (lines 15-20 for DOMAIN_DIRS, cmdCoverageCheck implementation)
    - templates/design-manifest.json (6-field designCoverage schema — verify field names)
    - templates/design-state-domain.md (Artifact Index column names for Step 6)
  </read_first>
  <action>
Create `workflows/handoff.md` following the 7-step PDE skill workflow pattern. Target ~500-600 lines matching the complexity of iterate.md (558 lines) and critique.md (631 lines). The workflow must implement ALL of the following:

**Frontmatter:** Match iterate.md frontmatter pattern. Include title, description, and any metadata fields used by prior skills.

**Step 1/7: Initialize design directories**
- Run `pde-tools.cjs design ensure-dirs` using the exact Bash pattern from iterate.md Step 1.
- Parse `@file:` response pattern.

**Step 2/7: Check prerequisites and discover artifacts**
- Step 2a: Read `.planning/research/STACK.md` using Read tool. If absent, HALT with this exact error message:
  ```
  Error: No STACK.md found at .planning/research/STACK.md.
    /pde:handoff requires STACK.md to align component APIs to your project's technology.
    Create .planning/research/STACK.md documenting your tech stack:
      - Framework: React | Vue | Svelte | none
      - TypeScript: true | false
      - Component import path pattern (e.g., src/components/{ComponentName}.tsx)
    Then re-run /pde:handoff.
  ```
- Step 2a parsing: Extract framework by looking for table row patterns (`| React |`, `| Vue |`, `| Svelte |`) or explicit "Use X" directives. Extract TypeScript presence (scan for "TypeScript: true" or ".tsx" references). Extract import path conventions. Do NOT do simple string search — a sentence like "Unlike React, this project uses Vue 3" should detect Vue, not React.
- Step 2b: Read design-manifest.json coverage flags via `coverage-check` (soft — use Glob as fallback).
- Step 2c-2i: Discover all upstream artifacts using Glob:
  - Brief: `.planning/design/strategy/BRF-brief-v*.md` (soft — warn if absent)
  - Flows: `.planning/design/ux/FLW-flows-v*.md` (soft — warn if absent)
  - Screen inventory: `.planning/design/ux/FLW-screen-inventory.json` (soft)
  - Wireframes: `.planning/design/ux/wireframes/WFR-*.html` — exclude index.html; for each screen prefer highest versioned `WFR-{screen}-v{N}.html` over original `WFR-{screen}.html`
  - Design system tokens: `.planning/design/assets/tokens.css` (soft)
  - Critique: `.planning/design/review/CRT-critique-v*.md` (soft)
  - Changelogs: `.planning/design/review/ITR-changelog-v*.md` (soft)
- Step 2j: Annotation completeness check — for each wireframe, count `<!-- ANNOTATION:` occurrences vs `pde-state--` div occurrences. If less than 50% of state divs have annotations, emit warning: "Low annotation coverage — TypeScript interface quality may be degraded. Re-run /pde:wireframe to regenerate with full annotations."
- Step 2k: Version gate — Glob for `.planning/design/handoff/HND-handoff-spec-v*.md`. HND_VERSION = max(N) + 1 or 1 if none exist.
- After all checks: if NONE of brief, flows, wireframes, tokens are present (zero upstream artifacts), emit warning and halt: "No design artifacts found. Run /pde:brief, /pde:flows, /pde:wireframe before handoff."

**Step 3/7: Probe MCP availability**
- Sequential Thinking MCP only (no Axe, no Playwright).
- Respect `--no-mcp`, `--quick`, `--no-sequential-thinking` flags.
- Use exact probe pattern from references/mcp-integration.md.

**Step 4/7: Synthesize artifacts into spec content**
- Step 4a: Read all discovered artifacts into memory using Read tool.
- Step 4b: Extract `<!-- ANNOTATION: ... -->` content from each wireframe's `pde-state--*` blocks and interactive element annotations. Map annotations to TypeScript interface shapes using semantic reasoning (not mechanical regex).
- Step 4c: Parse screen inventory JSON for route mappings and component context.
- Step 4d: Parse tokens.css for Global Token Mappings section.
- Step 4e: Parse critique What Works + deferred findings for Completeness Warnings.
- Step 4f: If SEQUENTIAL_THINKING_AVAILABLE and annotation coverage is low, use `mcp__sequential-thinking__think` to reason about implied component interfaces from wireframe structure.
- Step 4g: For each screen, derive: component name list, TypeScript interface (Props with JSDoc), breakpoint spec, interaction spec, token mappings, accessibility requirements, test spec.
- Step 4h: Build global sections: Design Coverage Summary (with completeness warnings per Pattern 5 in research), Route Structure, Shared Component APIs, Accessibility Overview, Gap Analysis, Test Specs Overview.
- Step 4i: Check `productType` from brief or manifest. If "software", omit hardware sections from template. If "hardware", omit software component API sections. If "hybrid", include both. Default to "software" if productType unavailable.

**Step 5/7: Write output files with lock**
- Step 5a: Acquire lock via `pde-tools.cjs design lock-acquire pde-handoff` — retry 3 times, warn and continue if still locked.
- Step 5b: Write `.planning/design/handoff/HND-handoff-spec-v{HND_VERSION}.md` using Write tool. Follow `templates/handoff-spec.md` as structural scaffold. All sections populated with synthesized content.
- Step 5c: Write `.planning/design/handoff/HND-types-v{HND_VERSION}.ts` using Write tool. TypeScript interfaces ONLY — no `export default`, no `const`, no JSX, no imports. Only `interface {Name} { ... }` and `type {Name} = { ... }` declarations with JSDoc comments. Section headers as `// ─── Section ───` comments.
- Step 5d: Release lock via `pde-tools.cjs design lock-release pde-handoff` — ALWAYS release even on error.

**Step 6/7: Update handoff domain DESIGN-STATE**
- Glob for `.planning/design/handoff/DESIGN-STATE.md`. If absent, create using `templates/design-state-domain.md` scaffold.
- Add HND artifact row to Artifact Index table with columns: Code, Name, Skill, Status, Version, Enhanced By, Dependencies, Updated.

**Step 7/7: Update root DESIGN-STATE + manifest + coverage flag**
- Step 7a: Edit root `.planning/design/DESIGN-STATE.md` — mark Handoff complete in Pipeline Progress, append Decision Log entry, append Iteration History entry.
- Step 7b: Register HND artifact in manifest using the 7-call `manifest-update` pattern:
  ```
  manifest-update HND code HND
  manifest-update HND name "Design Handoff"
  manifest-update HND type handoff
  manifest-update HND domain handoff
  manifest-update HND path ".planning/design/handoff/"
  manifest-update HND status complete
  manifest-update HND version {HND_VERSION}
  ```
- Step 7c: Coverage flag read-before-set — run `coverage-check`, parse result, default any absent field (especially `hasIterate`) to `false`, then call `manifest-set-top-level designCoverage` with ALL 7 fields and `hasHandoff: true`. Exact fields: hasDesignSystem, hasFlows, hasWireframes, hasCritique, hasIterate, hasHandoff, hasHardwareSpec.
- Step 7d: Output skill summary table per skill-style-guide.md conventions.

**Flag handling:** Support `--quick` (skip MCP, minimal annotations), `--force` (skip prerequisite warnings), `--dry-run` (show what would be generated without writing), `--verbose` (detailed logging), `--no-mcp` (disable MCP), `--no-sequential-thinking` (disable Sequential Thinking specifically).
  </action>
  <verify>
    <automated>grep -c "Step 1/7\|Step 2/7\|Step 3/7\|Step 4/7\|Step 5/7\|Step 6/7\|Step 7/7" workflows/handoff.md | grep -q "^7$" && grep -q "STACK.md" workflows/handoff.md && grep -q "lock-acquire pde-handoff" workflows/handoff.md && grep -q "coverage-check" workflows/handoff.md && grep -q "hasIterate" workflows/handoff.md && grep -q "HND-handoff-spec" workflows/handoff.md && grep -q "HND-types" workflows/handoff.md && grep -q "ANNOTATION" workflows/handoff.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/handoff.md exists and has at least 400 lines (wc -l >= 400)
    - Contains all 7 step headers: "Step 1/7" through "Step 7/7"
    - Contains STACK.md hard dependency check with exact recovery message text including "Error: No STACK.md found"
    - Contains annotation completeness check with "Low annotation coverage" warning text
    - Contains lock-acquire/release pattern: "lock-acquire pde-handoff" and "lock-release pde-handoff"
    - Contains coverage-check read-before-set with all 7 fields: hasDesignSystem, hasFlows, hasWireframes, hasCritique, hasIterate, hasHandoff, hasHardwareSpec
    - Contains manifest-update HND 7-call pattern
    - Contains version gate logic referencing "HND-handoff-spec-v" and "HND-types-v"
    - Contains wireframe version selection logic (prefer highest versioned WFR-{screen}-v{N}.html)
    - Contains productType conditional for hardware sections
    - Contains Sequential Thinking MCP probe (mcp__sequential-thinking__think)
    - Contains "templates/handoff-spec.md" reference as output scaffold
    - Contains "--quick" and "--force" and "--dry-run" flag handling
    - Does NOT contain "PDE v2" or "mockup" references
    - node bin/lib/design.cjs --self-test exits 0 (infrastructure regression check)
  </acceptance_criteria>
  <done>workflows/handoff.md is a complete 7-step skill workflow that synthesizes all upstream design artifacts into HND-handoff-spec-v{N}.md and HND-types-v{N}.ts, with STACK.md hard dependency, annotation quality checks, and proper manifest/coverage integration</done>
</task>

<task type="auto">
  <name>Task 2: Update commands/handoff.md — replace stub with delegation to workflow</name>
  <files>commands/handoff.md</files>
  <read_first>
    - commands/handoff.md (current stub content to be fully replaced)
    - commands/iterate.md (delegation pattern to replicate exactly)
    - commands/critique.md (secondary reference for delegation pattern)
  </read_first>
  <action>
Fully replace the contents of `commands/handoff.md`. The current stub says "Planned -- available in PDE v2" and references "mockup" — both wrong. Replace with a delegation stub matching the exact pattern of `commands/iterate.md`.

Write this exact content:

```markdown
---
name: pde:handoff
description: Synthesize design pipeline artifacts into implementation specifications
argument-hint: "[--quick] [--dry-run] [--verbose] [--no-mcp] [--no-sequential-thinking] [--force]"
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Glob
  - Grep
  - Task
  - mcp__sequential-thinking__*
---
<objective>
Execute the /pde:handoff command.
</objective>

<process>
Follow @workflows/handoff.md exactly.

Pass all of $ARGUMENTS to the workflow.
</process>
```

Key differences from the current stub:
- `description` changed from "Produce developer handoff package with component APIs" to "Synthesize design pipeline artifacts into implementation specifications"
- `argument-hint` populated with all supported flags (was empty string)
- `mcp__sequential-thinking__*` ADDED to allowed-tools (was missing)
- Entire `<process>` block replaced — remove "Planned -- available in PDE v2" text
- Remove all references to "mockup" (not in v1.1 pipeline)
- Add `@workflows/handoff.md` delegation and `$ARGUMENTS` pass-through
  </action>
  <verify>
    <automated>grep -q "mcp__sequential-thinking__\*" commands/handoff.md && grep -q "@workflows/handoff.md" commands/handoff.md && grep -q "ARGUMENTS" commands/handoff.md && ! grep -q "PDE v2" commands/handoff.md && ! grep -q "mockup" commands/handoff.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - commands/handoff.md contains `mcp__sequential-thinking__*` in allowed-tools
    - commands/handoff.md contains `@workflows/handoff.md` in process section
    - commands/handoff.md contains `$ARGUMENTS` pass-through
    - commands/handoff.md contains argument-hint with "--quick" and "--dry-run" and "--force"
    - commands/handoff.md does NOT contain "PDE v2" anywhere
    - commands/handoff.md does NOT contain "mockup" anywhere
    - commands/handoff.md does NOT contain "Planned" anywhere
    - commands/handoff.md frontmatter name is "pde:handoff"
    - node bin/lib/design.cjs --self-test exits 0 (infrastructure regression check)
  </acceptance_criteria>
  <done>commands/handoff.md is a clean delegation stub matching the iterate.md pattern, with mcp__sequential-thinking__* in allowed-tools, @workflows/handoff.md delegation, and no stale stub content</done>
</task>

</tasks>

<verification>
1. `workflows/handoff.md` exists with 400+ lines and all 7 steps
2. `commands/handoff.md` delegates to `@workflows/handoff.md` with correct allowed-tools
3. No references to "PDE v2", "mockup", or "Planned" in either file
4. `node bin/lib/design.cjs --self-test` exits 0
5. Both files follow established PDE skill patterns from Phases 14-18
</verification>

<success_criteria>
- /pde:handoff command is fully wired: commands/handoff.md delegates to workflows/handoff.md
- Workflow reads STACK.md as hard dependency with recovery message on absence
- Workflow discovers and synthesizes all upstream design artifacts (brief, flows, wireframes, tokens, critique, changelogs)
- Workflow produces versioned HND-handoff-spec-v{N}.md using templates/handoff-spec.md scaffold
- Workflow produces HND-types-v{N}.ts with TypeScript interface declarations derived from wireframe annotations
- Workflow sets hasHandoff coverage flag via read-before-set with all 7 fields
- Workflow registers HND artifact in manifest via 7-call pattern
- Design pipeline is complete through handoff: brief -> system -> flows -> wireframe -> critique -> iterate -> handoff
</success_criteria>

<output>
After completion, create `.planning/phases/19-design-to-code-handoff-pde-handoff/19-01-SUMMARY.md`
</output>
