---
phase: 90-critique-hig-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/90-critique-hig-extensions/tests/test-critique-hig-business.cjs
  - workflows/critique.md
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-04]

must_haves:
  truths:
    - "critique.md detects businessMode and adds 4 business perspectives when true"
    - "critique.md performs pitch coherence cross-check comparing LCV UVP with DPD solution slide"
    - "Business critique findings use critical/major/minor/nit severity only — no 'info' severity"
    - "critique.md uses 20-field designCoverage write including hasBusinessThesis"
    - "Composite score formula uses denominator 9.5 in business mode (not 5.0)"
  artifacts:
    - path: ".planning/phases/90-critique-hig-extensions/tests/test-critique-hig-business.cjs"
      provides: "Nyquist structural tests for QUAL-01 through QUAL-04"
      contains: "manifest-get-top-level businessMode"
    - path: "workflows/critique.md"
      provides: "Business critique perspectives and coherence cross-check"
      contains: "Unit Economics Viability"
  key_links:
    - from: "workflows/critique.md"
      to: "references/business-track.md"
      via: "Load reference for track depth branching"
      pattern: "business-track.md"
    - from: "workflows/critique.md"
      to: ".planning/design/strategy/LCV-lean-canvas-v*.md"
      via: "Glob discovery for pitch coherence cross-check"
      pattern: "LCV-lean-canvas"
    - from: "workflows/critique.md"
      to: ".planning/design/launch/DPD-pitch-deck-outline-v*.md"
      via: "Glob discovery for pitch coherence cross-check"
      pattern: "DPD-pitch-deck-outline"
---

<objective>
Create the Nyquist test scaffold for Phase 90 (Wave 0), then extend critique.md with four business critique perspectives, a pitch coherence cross-check, and upgrade the designCoverage write from 16 to 20 fields.

Purpose: Business-mode users running /pde:critique get unit economics, GTM-ICP fit, pricing psychology, and investor readiness evaluations — plus a structural coherence check verifying lean canvas UVP matches pitch deck content.
Output: Green Nyquist tests for QUAL-01/QUAL-02/QUAL-04 assertions against critique.md; modified critique.md with business extensions.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-critique-hig-extensions/90-RESEARCH.md

<interfaces>
<!-- Business mode detection pattern (from wireframe.md lines 159-163): -->
BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
if [[ "$BM" == @file:* ]]; then BM=$(cat "${BM#@file:}"); fi
BT=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessTrack 2>/dev/null)
if [[ "$BT" == @file:* ]]; then BT=$(cat "${BT#@file:}"); fi

<!-- Severity levels (from critique.md Step 4): critical | major | minor | nit -->
<!-- Finding format: | {severity} | {effort} | {location} | {issue} | {suggestion} | {reference} | -->

<!-- DPD coherence anchors (from wireframe.md Step 4j): -->
<!-- Solution slide: LCV.box3.UVP = [YOUR_UVP] -->
<!-- Traction slide: LCV.box6.metrics = [YOUR_METRIC_1], [YOUR_METRIC_2] -->

<!-- Composite score: existing = (UX*1.5 + hierarchy*1.0 + a11y*1.5 + business*1.0) / 5.0 -->
<!-- Business mode = (UX*1.5 + hierarchy*1.0 + a11y*1.5 + business*1.0 + unitEcon*1.0 + gtmIcp*1.0 + pricingPsych*1.5 + investorReady*1.0) / 9.5 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Nyquist test scaffold for QUAL-01 through QUAL-04</name>
  <files>.planning/phases/90-critique-hig-extensions/tests/test-critique-hig-business.cjs</files>
  <read_first>
    - tests/phase-79/critique-hig-extensions.test.mjs (prior phase test pattern — note: Phase 79 used .mjs, Phase 89 used .cjs with require(); use .cjs pattern)
    - .planning/phases/89-wireframe-stage-launch-artifacts/tests/test-wireframe-launch.cjs (most recent .cjs test pattern)
    - .planning/phases/90-critique-hig-extensions/90-RESEARCH.md (test map in "Phase Requirements -> Test Map" section, lines 629-641)
    - workflows/critique.md (current state — verify tests fail against it)
    - workflows/hig.md (current state — verify tests fail against it)
  </read_first>
  <behavior>
    - QUAL-01a: critique.md contains `manifest-get-top-level businessMode` -> MUST FAIL (not yet added)
    - QUAL-01b: critique.md contains `Unit Economics Viability` -> MUST FAIL
    - QUAL-01c: critique.md contains `GTM-ICP Fit` -> MUST FAIL
    - QUAL-01d: critique.md contains `Pricing Psychology` -> MUST FAIL
    - QUAL-01e: critique.md contains `Investor Readiness` -> MUST FAIL
    - QUAL-02a: critique.md contains `LCV-lean-canvas` AND `strategy` -> MUST FAIL
    - QUAL-02b: critique.md contains `DPD-pitch-deck-outline` -> MUST FAIL
    - QUAL-02c: critique.md contains `LCV.box3.UVP` -> MUST FAIL
    - QUAL-04a: critique.md does NOT contain `| info |` as severity value -> SHOULD PASS (info not present in current file)
    - QUAL-04b: hig.md does NOT contain `| info |` -> SHOULD PASS
    - QUAL-03a: hig.md contains `manifest-get-top-level businessMode` -> MUST FAIL
    - QUAL-03b: hig.md contains `pitch deck readability` (case-insensitive check) -> MUST FAIL
    - QUAL-03c: hig.md contains `email cadence` (case-insensitive check) -> MUST FAIL
    - QUAL-03d: hig.md contains `content calendar` (case-insensitive check) -> MUST FAIL
    - INTG-02a: critique.md contains `hasBusinessThesis` in a designCoverage write -> MUST FAIL (currently 16-field)
    - INTG-02b: hig.md contains `hasBusinessThesis` in a designCoverage write -> MUST FAIL (currently 16-field)
    - QUAL-01f: critique.md contains `/ 9.5` for business mode composite denominator -> MUST FAIL
    - QUAL-01g: critique.md composite section contains all 8 perspective weights -> MUST FAIL
  </behavior>
  <action>
Create the test file at `.planning/phases/90-critique-hig-extensions/tests/test-critique-hig-business.cjs` using the `node:test` + `node:assert` pattern from Phase 89.

File structure:
```javascript
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert');
const fs = require('node:fs');
const path = require('node:path');

const ROOT = path.resolve(__dirname, '..', '..', '..', '..');
const critiqueContent = fs.readFileSync(path.join(ROOT, 'workflows', 'critique.md'), 'utf-8');
const higContent = fs.readFileSync(path.join(ROOT, 'workflows', 'hig.md'), 'utf-8');
```

Then write these test groups:

**describe('QUAL-01: 4 business critique perspectives in critique.md')**
- `critique.md contains "manifest-get-top-level businessMode"` — assert critiqueContent.includes('manifest-get-top-level businessMode')
- `critique.md contains "Unit Economics Viability"` — assert critiqueContent.includes('Unit Economics Viability')
- `critique.md contains "GTM-ICP Fit"` — assert critiqueContent.includes('GTM-ICP Fit')
- `critique.md contains "Pricing Psychology"` — assert critiqueContent.includes('Pricing Psychology')
- `critique.md contains "Investor Readiness"` — assert critiqueContent.includes('Investor Readiness')
- `critique.md business mode composite uses denominator 9.5` — assert critiqueContent.includes('/ 9.5')
- `critique.md contains "Groups Evaluated" with 8 perspectives` — assert critiqueContent.includes('Unit Economics Viability, GTM-ICP Fit, Pricing Psychology, Investor Readiness')

**describe('QUAL-02: pitch coherence cross-check in critique.md')**
- `critique.md contains "LCV-lean-canvas" glob in strategy/ path` — assert critiqueContent.includes('LCV-lean-canvas') AND critiqueContent.includes('strategy/')
- `critique.md contains "DPD-pitch-deck-outline" glob` — assert critiqueContent.includes('DPD-pitch-deck-outline')
- `critique.md contains coherence anchor "LCV.box3.UVP"` — assert critiqueContent.includes('LCV.box3.UVP')
- `critique.md contains coherence anchor "LCV.box6.metrics"` — assert critiqueContent.includes('LCV.box6.metrics')
- `critique.md contains "Pitch Coherence Cross-Check"` — assert critiqueContent.includes('Pitch Coherence Cross-Check')

**describe('QUAL-03: business communications HIG in hig.md')**
- `hig.md contains "manifest-get-top-level businessMode"` — assert higContent.includes('manifest-get-top-level businessMode')
- `hig.md contains pitch deck readability domain` — assert higContent.toLowerCase().includes('pitch deck readability')
- `hig.md contains email cadence domain` — assert higContent.toLowerCase().includes('email cadence')
- `hig.md contains content calendar domain` — assert higContent.toLowerCase().includes('content calendar')

**describe('QUAL-04: standard severity levels only')**
- `critique.md does NOT use "| info |" severity` — assert !critiqueContent.includes('| info |')
- `hig.md does NOT use "| info |" severity` — assert !higContent.includes('| info |')

**describe('INTG-02: 20-field designCoverage write')**
- `critique.md designCoverage contains "hasBusinessThesis"` — assert critiqueContent.includes('hasBusinessThesis')
- `hig.md designCoverage contains "hasBusinessThesis"` — assert higContent.includes('hasBusinessThesis')
- `critique.md designCoverage contains "hasMarketLandscape"` — assert critiqueContent.includes('hasMarketLandscape')
- `hig.md designCoverage contains "hasMarketLandscape"` — assert higContent.includes('hasMarketLandscape')
- `critique.md designCoverage contains "hasServiceBlueprint"` — assert critiqueContent.includes('hasServiceBlueprint')
- `critique.md designCoverage contains "hasLaunchKit"` — assert critiqueContent.includes('hasLaunchKit')

Create directory first: `mkdir -p .planning/phases/90-critique-hig-extensions/tests/`

Run tests after creation — expect most to FAIL (RED state). Only QUAL-04 negative assertions and possibly some INTG-02 checks should pass against current files. Confirm the test file runs without syntax errors.
  </action>
  <verify>
    <automated>node --test .planning/phases/90-critique-hig-extensions/tests/test-critique-hig-business.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/90-critique-hig-extensions/tests/test-critique-hig-business.cjs`
    - File contains `require('node:test')` and `require('node:assert')`
    - File contains `critiqueContent` and `higContent` variable reads from `workflows/critique.md` and `workflows/hig.md`
    - File contains at minimum 20 `it(` or `test(` calls
    - File contains string `Unit Economics Viability` in a test assertion
    - File contains string `LCV.box3.UVP` in a test assertion
    - File contains string `hasBusinessThesis` in a test assertion
    - File contains string `/ 9.5` in a test assertion
    - File contains string `| info |` in a negative assertion
    - `node --test` command runs without syntax errors (exit code 1 from failing tests is expected, not syntax crash)
  </acceptance_criteria>
  <done>Test scaffold exists with 20+ structural assertions covering QUAL-01/02/03/04 and INTG-02. Tests run (most fail RED as expected). No syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Business critique perspectives + pitch coherence + 20-field coverage in critique.md</name>
  <files>workflows/critique.md</files>
  <read_first>
    - workflows/critique.md (FULL file — understand Step 4 structure, Step 5b scorecard, Step 7c coverage write)
    - .planning/phases/90-critique-hig-extensions/90-RESEARCH.md (patterns 1-4, anti-patterns, perspective specifications BIZ-1 through BIZ-4, lines 89-310)
    - references/business-track.md (track vocabulary and depth thresholds)
    - references/business-financial-disclaimer.md (financial placeholder rules for unit economics findings)
    - references/launch-frameworks.md (lean canvas schema, pitch deck slide maps for coherence anchors)
    - workflows/wireframe.md (lines 155-165 for $BM/$BT detection pattern; lines 680-690 for DPD coherence anchor format)
  </read_first>
  <action>
Make THREE modifications to critique.md:

**Modification 1: Step 4-BUSINESS block (after Step 4g, before Step 5)**

Insert a new section `#### Step 4-BUSINESS: Business Mode Critique Extension` as an INDEPENDENT IF block (not ELSE IF) INSIDE the ELSE clause, after Step 4g (Visual observations from screenshot).

If `$BM` and `$BT` are not yet cached in the Step 4 preamble, add the detection lines at the start of the ELSE clause:
```
BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
if [[ "$BM" == @file:* ]]; then BM=$(cat "${BM#@file:}"); fi
BT=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessTrack 2>/dev/null)
if [[ "$BT" == @file:* ]]; then BT=$(cat "${BT#@file:}"); fi
```

The Step 4-BUSINESS block structure:

```
#### Step 4-BUSINESS: Business Mode Critique Extension

IF `$BM != "true"`: skip this entire block. Continue to Step 4-COHERENCE.

IF `$BM == "true"`:

Load @references/business-track.md for track vocabulary and depth thresholds.
Load @references/business-financial-disclaimer.md — ALL unit economics findings MUST use structural placeholders only.

**Track depth guide:**
- `solo_founder`: 2-3 findings per perspective (action-first, no jargon)
- `startup_team`: 4-5 findings per perspective (investor vocabulary, metrics framing)
- `product_leader`: 4-5 findings per perspective (P&L vocabulary, OKR framing, board-ready format)

##### Business Perspective BIZ-1: Unit Economics Viability (weight 1.0x)
[Full BIZ-1 spec from research — evaluation sources, checklist, financial disclaimer rule]

##### Business Perspective BIZ-2: GTM-ICP Fit (weight 1.0x)
[Full BIZ-2 spec from research — evaluation sources, checklist]

##### Business Perspective BIZ-3: Pricing Psychology (weight 1.5x)
[Full BIZ-3 spec from research — evaluation sources, checklist]

##### Business Perspective BIZ-4: Investor Readiness (weight 1.0x)
[Full BIZ-4 spec from research — evaluation sources, checklist]
```

Use the EXACT finding format from the existing critique.md Step 4: `| {severity} | {effort} | {location} | {issue} | {suggestion} | {reference} |`. Severity values: critical, major, minor, nit ONLY. Never use "info".

Render business perspective findings in a separate `## Business Mode Findings` section — NOT mixed into the existing Perspectives 1-4 findings table.

**Modification 2: Step 4-COHERENCE block (after Step 4-BUSINESS, before Step 5)**

Insert `#### Step 4-COHERENCE: Pitch Coherence Cross-Check` after Step 4-BUSINESS.

```
#### Step 4-COHERENCE: Pitch Coherence Cross-Check

IF `$BM != "true"`: skip entirely.

IF `$BM == "true"`:
  Use Glob to find: `.planning/design/strategy/LCV-lean-canvas-v*.md`
  Sort by version number, take highest -> SET LCV_FILE.

  Use Glob to find: `.planning/design/launch/DPD-pitch-deck-outline-v*.md`
  Sort by version number, take highest -> SET DPD_FILE.

  IF LCV_FILE is absent:
    SET COHERENCE_AVAILABLE = false
    Note in report: "Pitch coherence cross-check skipped: LCV lean canvas not found. Run /pde:brief in business mode first."

  IF DPD_FILE is absent:
    SET COHERENCE_AVAILABLE = false
    Note in report: "Pitch coherence cross-check skipped: DPD pitch deck outline not found. Run /pde:wireframe in business mode first."

  IF both present (COHERENCE_AVAILABLE = true):
    Read LCV_FILE. Extract Box 3 (UVP) content.
    Read DPD_FILE. Search for coherence anchors:

    Cross-check 1: LCV UVP <-> DPD Solution slide
      Look for anchor: "LCV.box3.UVP ="
      - If anchor contains "[YOUR_UVP]": advisory — "UVP not yet customized in pitch deck"
      - If anchor has real content: compare against LCV Box 3 UVP text
      - If anchor absent (older DPD): search Solution slide for LCV UVP language
      Pass: UVP language appears in solution slide
      Fail: no relationship — severity = major

    Cross-check 2: LCV Key Metrics <-> DPD Traction slide
      Look for anchor: "LCV.box6.metrics ="
      - Apply same pass/advisory/fail logic
      Pass: metrics appear in traction slide
      Fail: no relationship — severity = major

  Coherence findings appear in `## Pitch Coherence Cross-Check` section.
  Coherence findings are included in Action List for /pde:iterate.
  Coherence findings do NOT affect the composite score.
```

**Modification 3: Composite score and scorecard updates**

In Step 5 where the composite score is calculated:
- Add a conditional: IF `$BM == "true"`, use the 8-perspective formula with denominator 9.5:
  `(UX*1.5 + hierarchy*1.0 + a11y*1.5 + business*1.0 + unitEcon*1.0 + gtmIcp*1.0 + pricingPsych*1.5 + investorReady*1.0) / 9.5`
- Else keep existing 4-perspective formula with denominator 5.0.

In Step 5b Summary Scorecard:
- When business mode active, add 4 additional rows for the business perspectives.
- Update `Groups Evaluated` frontmatter field to include: `"UX/Usability, Visual Hierarchy, Accessibility, Business Alignment, Unit Economics Viability, GTM-ICP Fit, Pricing Psychology, Investor Readiness"`

In the `--focused business` documentation: note that in business mode, `--focused business` runs Perspective 4 (Business Alignment) AND all 4 business perspectives.

**Modification 4: Step 7c designCoverage write upgrade (16 -> 20 fields)**

Find the Step 7c designCoverage write section. Replace the 16-field pattern:
```
Extract ALL sixteen current flag values: hasDesignSystem, hasWireframes, ...hasProductionBible
```

With the 20-field read-before-set pattern:
```
Extract ALL TWENTY current coverage flag values (default absent fields to false):
hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit

Set `hasCritique: true`, pass all 19 other fields through with their current values.
```

Ensure the coverage write command string includes ALL 20 field names including `hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`.
  </action>
  <verify>
    <automated>node --test .planning/phases/90-critique-hig-extensions/tests/test-critique-hig-business.cjs 2>&1 | grep -E "(pass|fail)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - critique.md contains string `manifest-get-top-level businessMode`
    - critique.md contains string `Unit Economics Viability`
    - critique.md contains string `GTM-ICP Fit`
    - critique.md contains string `Pricing Psychology`
    - critique.md contains string `Investor Readiness`
    - critique.md contains string `LCV-lean-canvas` AND `strategy/`
    - critique.md contains string `DPD-pitch-deck-outline` AND `launch/`
    - critique.md contains string `LCV.box3.UVP`
    - critique.md contains string `LCV.box6.metrics`
    - critique.md contains string `Pitch Coherence Cross-Check`
    - critique.md contains string `/ 9.5`
    - critique.md contains string `Unit Economics Viability, GTM-ICP Fit, Pricing Psychology, Investor Readiness`
    - critique.md does NOT contain string `| info |`
    - critique.md contains string `hasBusinessThesis` in the designCoverage write section
    - critique.md contains string `hasMarketLandscape` in the designCoverage write section
    - critique.md contains string `hasServiceBlueprint`
    - critique.md contains string `hasLaunchKit`
    - critique.md contains string `business-financial-disclaimer.md` (reference load)
    - critique.md contains string `business-track.md` (reference load)
    - All QUAL-01, QUAL-02, QUAL-04, and critique-side INTG-02 tests pass GREEN
  </acceptance_criteria>
  <done>critique.md has 4 business perspectives in Step 4-BUSINESS, pitch coherence cross-check in Step 4-COHERENCE, updated composite score formula (denominator 9.5), expanded scorecard, and 20-field designCoverage write. All critique-related Nyquist tests pass.</done>
</task>

</tasks>

<verification>
Run full Nyquist suite:
```bash
node --test .planning/phases/90-critique-hig-extensions/tests/test-critique-hig-business.cjs
```

Confirm:
- All QUAL-01 tests (5+ assertions) pass
- All QUAL-02 tests (5 assertions) pass
- QUAL-04 negative assertions for critique.md pass
- INTG-02 assertions for critique.md pass (hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit present)
- QUAL-03 and hig-side INTG-02 tests still fail (expected — addressed in Plan 02)
</verification>

<success_criteria>
- critique.md contains business mode detection, 4 business perspectives, pitch coherence cross-check, updated composite formula, and 20-field coverage write
- All critique-related Nyquist tests pass GREEN
- hig.md-related tests still fail RED (expected, fixed in Plan 02)
- No "info" severity introduced anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/90-critique-hig-extensions/90-01-SUMMARY.md`
</output>
