---
phase: 83-cross-phase-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-83/wiring-fixes.test.mjs
  - workflows/system.md
  - workflows/critique.md
  - workflows/iterate.md
  - workflows/hig.md
  - workflows/ideate.md
  - workflows/competitive.md
  - workflows/opportunity.md
  - workflows/recommend.md
  - workflows/mockup.md
  - workflows/wireframe.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HDOF-06, DSYS-06, PRNT-04]
gap_closure: true

must_haves:
  truths:
    - "All 14 pipeline workflow files that write designCoverage use the 16-field schema (including hasPrintCollateral and hasProductionBible)"
    - "wireframe.md reads SYS-experience-tokens.json from .planning/design/visual/ (matching where system.md writes it)"
    - "REQUIREMENTS.md uses artifact code FLP (not FPL) for WIRE-03"
    - "Running the full experience pipeline preserves hasPrintCollateral: true through all stages"
  artifacts:
    - path: "tests/phase-83/wiring-fixes.test.mjs"
      provides: "Red-then-green test covering all 3 gaps"
      contains: "hasPrintCollateral"
    - path: "workflows/system.md"
      provides: "16-field designCoverage write"
      contains: "hasProductionBible"
    - path: "workflows/critique.md"
      provides: "16-field designCoverage write"
      contains: "hasProductionBible"
    - path: "workflows/iterate.md"
      provides: "16-field designCoverage write"
      contains: "hasProductionBible"
    - path: "workflows/hig.md"
      provides: "16-field designCoverage write"
      contains: "hasProductionBible"
    - path: "workflows/ideate.md"
      provides: "16-field designCoverage write"
      contains: "hasProductionBible"
    - path: "workflows/competitive.md"
      provides: "16-field designCoverage write"
      contains: "hasProductionBible"
    - path: "workflows/opportunity.md"
      provides: "16-field designCoverage write"
      contains: "hasProductionBible"
    - path: "workflows/recommend.md"
      provides: "16-field designCoverage write"
      contains: "hasProductionBible"
    - path: "workflows/mockup.md"
      provides: "16-field designCoverage write"
      contains: "hasProductionBible"
    - path: "workflows/wireframe.md"
      provides: "16-field designCoverage write + corrected token path"
      contains: "hasProductionBible"
  key_links:
    - from: "workflows/wireframe.md"
      to: ".planning/design/visual/SYS-experience-tokens.json"
      via: "Glob tool check path string"
      pattern: "design/visual/SYS-experience-tokens"
    - from: "all 10 workflows"
      to: "design-manifest.json designCoverage"
      via: "manifest-set-top-level command"
      pattern: "hasPrintCollateral.*hasProductionBible"
---

<objective>
Fix three integration gaps found by the v0.11 milestone audit: (1) 10 workflow files clobber hasPrintCollateral/hasProductionBible when writing designCoverage because they use the old 14/15-field schema instead of 16-field, (2) wireframe.md reads SYS-experience-tokens.json from the wrong path (assets/ instead of visual/), and (3) verify REQUIREMENTS.md FPL typo is already resolved.

Purpose: Without this fix, any workflow that runs after wireframe.md or handoff.md will silently erase the hasPrintCollateral and hasProductionBible flags from the manifest, breaking the print collateral pipeline detection.
Output: 10 corrected workflow files, 1 test file, all passing.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-cross-phase-wiring-fixes/83-RESEARCH.md

<interfaces>
<!-- The 16-field canonical designCoverage schema (from tests/phase-64/manifest-schema.test.mjs lines 27-36): -->

1.  hasDesignSystem
2.  hasWireframes
3.  hasFlows
4.  hasHardwareSpec
5.  hasCritique
6.  hasIterate
7.  hasHandoff
8.  hasIdeation
9.  hasCompetitive
10. hasOpportunity
11. hasMockup
12. hasHigAudit
13. hasRecommendations
14. hasStitchWireframes
15. hasPrintCollateral
16. hasProductionBible

<!-- Correct baseline files (already 16-field, DO NOT edit): -->
- workflows/flows.md — 16 fields at line 799/802
- workflows/handoff.md — 16 fields at lines 1201/1207/1210/1213

<!-- Files needing 14 -> 16 field fix (9 files): -->
- workflows/system.md (lines 1938, 1942, 1945)
- workflows/critique.md (lines 1020, 1023)
- workflows/iterate.md (lines 452, 455, 559)
- workflows/hig.md (lines 793, 795, 799, 802, 840)
- workflows/ideate.md (lines 688, 690, 694, 697, 730)
- workflows/competitive.md (lines 548, 550, 554, 557, 587)
- workflows/opportunity.md (lines 471, 473, 477, 480, 507)
- workflows/recommend.md (lines 565, 567-582, 584, 588, 593, 622)
- workflows/mockup.md (lines 1425, 1426, 1428, 1432, 1435, 1480)

<!-- File needing 15 -> 16 field fix (1 file): -->
- workflows/wireframe.md (lines 2015, 2019, 2022, 2025) — already has hasPrintCollateral, missing hasProductionBible

<!-- CRITICAL: recommend.md uses {current_hasFieldName} placeholder style, NOT {current} -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test for all 3 wiring gaps (RED)</name>
  <files>tests/phase-83/wiring-fixes.test.mjs</files>
  <read_first>
    tests/phase-82/regression-matrix.test.mjs
    tests/phase-64/manifest-schema.test.mjs
    .planning/phases/83-cross-phase-wiring-fixes/83-RESEARCH.md
  </read_first>
  <behavior>
    - Test "Gap 1: all 10 affected workflows contain hasPrintCollateral in designCoverage write section": reads each of the 10 workflow files listed below and asserts content.includes('hasPrintCollateral'). Currently PASSES for wireframe.md (already has it) but FAILS for the other 9.
    - Test "Gap 1: all 10 affected workflows contain hasProductionBible in designCoverage write section": reads each of the 10 workflow files and asserts content.includes('hasProductionBible'). Currently FAILS for all 10.
    - Test "Gap 2: wireframe.md reads experience tokens from visual/ path": reads wireframe.md and asserts content.includes('design/visual/SYS-experience-tokens.json'). Currently FAILS (reads from assets/).
    - Test "Gap 2: wireframe.md does NOT reference assets/ path for experience tokens": reads wireframe.md and asserts !content.includes('design/assets/SYS-experience-tokens.json'). Currently FAILS.
    - Test "Gap 3: REQUIREMENTS.md uses FLP not FPL for WIRE-03": reads .planning/REQUIREMENTS.md line containing WIRE-03 and asserts it contains 'FLP' and does not contain 'FPL'. Currently expected to PASS (already fixed).
  </behavior>
  <action>
Create `tests/phase-83/wiring-fixes.test.mjs` following the exact pattern from `tests/phase-82/regression-matrix.test.mjs`:

```javascript
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'fs';
import { resolve } from 'path';
import { fileURLToPath } from 'url';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');
```

Define the 10 affected workflow files array:
```javascript
const COVERAGE_WRITERS = [
  'workflows/system.md',
  'workflows/critique.md',
  'workflows/iterate.md',
  'workflows/hig.md',
  'workflows/ideate.md',
  'workflows/competitive.md',
  'workflows/opportunity.md',
  'workflows/recommend.md',
  'workflows/mockup.md',
  'workflows/wireframe.md',
];
```

Write 3 describe blocks:
1. `describe('Gap 1: designCoverage 16-field schema')` with a test per file checking `hasPrintCollateral` and `hasProductionBible` are both present in the file content.
2. `describe('Gap 2: wireframe.md token path')` with a test asserting `design/visual/SYS-experience-tokens.json` is present and `design/assets/SYS-experience-tokens.json` is absent.
3. `describe('Gap 3: REQUIREMENTS.md FLP artifact code')` with a test reading `.planning/REQUIREMENTS.md`, finding the line containing `WIRE-03`, and asserting it includes `FLP` and does not include `FPL`.

Run the test: `node --test tests/phase-83/wiring-fixes.test.mjs`. Expect Gap 1 and Gap 2 tests to FAIL (RED). Gap 3 test should PASS (already fixed per research).

Commit with message: `test(83-01): add failing tests for 3 wiring gaps`
  </action>
  <verify>
    <automated>node --test tests/phase-83/wiring-fixes.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-83/wiring-fixes.test.mjs exists and contains `import { test, describe } from 'node:test'`
    - tests/phase-83/wiring-fixes.test.mjs contains `hasPrintCollateral` string assertion
    - tests/phase-83/wiring-fixes.test.mjs contains `hasProductionBible` string assertion
    - tests/phase-83/wiring-fixes.test.mjs contains `design/visual/SYS-experience-tokens.json`
    - tests/phase-83/wiring-fixes.test.mjs contains `design/assets/SYS-experience-tokens.json` (negative assertion)
    - tests/phase-83/wiring-fixes.test.mjs contains `WIRE-03`
    - Running `node --test tests/phase-83/wiring-fixes.test.mjs` exits with non-zero (some tests fail RED)
    - Gap 3 test passes (grep for "FLP" in REQUIREMENTS.md succeeds)
  </acceptance_criteria>
  <done>Test file committed, Gap 1 and Gap 2 tests fail RED, Gap 3 test passes GREEN</done>
</task>

<task type="auto">
  <name>Task 2: Fix all 10 workflow files — designCoverage 16-field + token path (GREEN)</name>
  <files>
    workflows/system.md
    workflows/critique.md
    workflows/iterate.md
    workflows/hig.md
    workflows/ideate.md
    workflows/competitive.md
    workflows/opportunity.md
    workflows/recommend.md
    workflows/mockup.md
    workflows/wireframe.md
  </files>
  <read_first>
    .planning/phases/83-cross-phase-wiring-fixes/83-RESEARCH.md
    workflows/system.md
    workflows/critique.md
    workflows/iterate.md
    workflows/hig.md
    workflows/ideate.md
    workflows/competitive.md
    workflows/opportunity.md
    workflows/recommend.md
    workflows/mockup.md
    workflows/wireframe.md
  </read_first>
  <action>
Apply the exact edits documented in 83-RESEARCH.md for each file. The research file contains exact line numbers and before/after text for every change. Summary of the pattern for each file:

**For all 9 files with 14-field schema** (system.md, critique.md, iterate.md, hig.md, ideate.md, competitive.md, opportunity.md, recommend.md, mockup.md):

A. In the parse instruction text: change "fourteen" to "sixteen", "thirteen" to "fifteen", append `, \`hasPrintCollateral\`, \`hasProductionBible\`` to the field list.

B. In the JSON command string: append `,"hasPrintCollateral":{current},"hasProductionBible":{current}` before the closing `'`.

C. In IMPORTANT/anti-pattern notes: update "14 fields" to "16 fields", "13 fields" to "15 fields", "12 flags" to "15 flags" as applicable.

**CRITICAL exception for recommend.md:** This file uses `{current_hasFieldName}` placeholder style. Append `,"hasPrintCollateral":{current_hasPrintCollateral},"hasProductionBible":{current_hasProductionBible}` (NOT `{current}`). Also add two new rows to the field table at lines 567-582 for hasPrintCollateral and hasProductionBible.

**For wireframe.md (15-field, needs 16):**

A. Line 2015: change "fifteen" to "sixteen", append `, hasProductionBible` to field list.
B. Lines 2019, 2022, 2025: append `,"hasProductionBible":{current}` before closing `'` on each of the three JSON variant commands. Do NOT set hasProductionBible to `true` — wireframe does not produce a production bible.

**Gap 2 fix in wireframe.md:**
Line 161: change `.planning/design/assets/SYS-experience-tokens.json` to `.planning/design/visual/SYS-experience-tokens.json`.

**Exact edits per file (reference 83-RESEARCH.md for line numbers):**

1. **system.md** — Lines 1938, 1942, 1945: fourteen->sixteen, thirteen->fifteen, append two fields to JSON and field lists
2. **critique.md** — Lines 1020, 1023: fourteen->sixteen, thirteen->fifteen, append two fields to JSON
3. **iterate.md** — Lines 452, 455, 559: fourteen->sixteen, thirteen->fifteen, append two fields to JSON, "13 fields"->15
4. **hig.md** — Lines 793, 795, 799, 802, 840: append two fields to list, 14-field->16-field, append to JSON, update canonical order note, 13->15
5. **ideate.md** — Lines 688, 690, 694, 697, 730: 14-field->16-field, append two fields to JSON, update IMPORTANT, 13-field->15-field, 12->15
6. **competitive.md** — Lines 548, 550, 554, 557, 587: append two fields, 14-field->16-field, append to JSON, update IMPORTANT, 12->15
7. **opportunity.md** — Lines 471, 473, 477, 480, 507: 14-field->16-field, append two fields, append to JSON, update IMPORTANT, 12->15
8. **recommend.md** — Lines 565, 567-582, 584, 588, 593, 622: 14-field->16-field, add 2 field table rows, append `{current_hasPrintCollateral}` and `{current_hasProductionBible}` to JSON, 14-field->16-field in IMPORTANT, 13->15
9. **mockup.md** — Lines 1425, 1426, 1428, 1432, 1435, 1480: 14->16, append two fields to list, 14-field->16-field, append to JSON, update IMPORTANT, 13->15 and 12->15
10. **wireframe.md** — Line 161: assets->visual path. Lines 2015, 2019, 2022, 2025: fifteen->sixteen, append hasProductionBible:{current}

After all edits, run: `node --test tests/phase-83/wiring-fixes.test.mjs` — ALL tests must pass GREEN.

Also run full regression: `node --test tests/phase-82/regression-matrix.test.mjs tests/phase-82/milestone-completion.test.mjs tests/phase-83/wiring-fixes.test.mjs`

Commit with message: `fix(83-01): update 10 workflows to 16-field designCoverage + fix token path`
  </action>
  <verify>
    <automated>node --test tests/phase-83/wiring-fixes.test.mjs && node --test tests/phase-82/regression-matrix.test.mjs tests/phase-82/milestone-completion.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'hasPrintCollateral' workflows/system.md` returns at least 1
    - `grep -c 'hasProductionBible' workflows/system.md` returns at least 1
    - `grep -c 'hasPrintCollateral' workflows/critique.md` returns at least 1
    - `grep -c 'hasProductionBible' workflows/critique.md` returns at least 1
    - `grep -c 'hasPrintCollateral' workflows/iterate.md` returns at least 1
    - `grep -c 'hasProductionBible' workflows/iterate.md` returns at least 1
    - `grep -c 'hasPrintCollateral' workflows/hig.md` returns at least 1
    - `grep -c 'hasProductionBible' workflows/hig.md` returns at least 1
    - `grep -c 'hasPrintCollateral' workflows/ideate.md` returns at least 1
    - `grep -c 'hasProductionBible' workflows/ideate.md` returns at least 1
    - `grep -c 'hasPrintCollateral' workflows/competitive.md` returns at least 1
    - `grep -c 'hasProductionBible' workflows/competitive.md` returns at least 1
    - `grep -c 'hasPrintCollateral' workflows/opportunity.md` returns at least 1
    - `grep -c 'hasProductionBible' workflows/opportunity.md` returns at least 1
    - `grep -c 'hasPrintCollateral' workflows/recommend.md` returns at least 1
    - `grep -c 'hasProductionBible' workflows/recommend.md` returns at least 1
    - `grep -c 'current_hasPrintCollateral' workflows/recommend.md` returns at least 1 (correct placeholder style)
    - `grep -c 'current_hasProductionBible' workflows/recommend.md` returns at least 1 (correct placeholder style)
    - `grep -c 'hasPrintCollateral' workflows/mockup.md` returns at least 1
    - `grep -c 'hasProductionBible' workflows/mockup.md` returns at least 1
    - `grep -c 'hasProductionBible' workflows/wireframe.md` returns at least 1
    - `grep 'design/visual/SYS-experience-tokens.json' workflows/wireframe.md` matches (visual path present)
    - `grep 'design/assets/SYS-experience-tokens.json' workflows/wireframe.md` returns NO matches (old path removed)
    - `node --test tests/phase-83/wiring-fixes.test.mjs` exits 0 (all GREEN)
    - `node --test tests/phase-82/regression-matrix.test.mjs` exits 0 (no regressions)
  </acceptance_criteria>
  <done>All 10 workflow files write 16-field designCoverage. wireframe.md reads tokens from visual/ path. All tests pass GREEN. No regressions in phase-82 suite.</done>
</task>

</tasks>

<verification>
1. `node --test tests/phase-83/wiring-fixes.test.mjs` — all gap-specific assertions pass
2. `node --test tests/phase-82/regression-matrix.test.mjs tests/phase-82/milestone-completion.test.mjs` — no regressions from v0.11 baseline
3. `grep -rn 'hasPrintCollateral' workflows/ | wc -l` — should be 12 (was 3 before: flows + handoff + wireframe, now +9 more)
4. `grep -rn 'hasProductionBible' workflows/ | wc -l` — should be 12 (was 2 before: flows + handoff, now +10 more)
5. `grep 'design/assets/SYS-experience-tokens' workflows/wireframe.md` — no matches (old path removed)
</verification>

<success_criteria>
- All 12 workflow files that write designCoverage (10 fixed + 2 baselines) use the 16-field schema
- wireframe.md reads experience tokens from .planning/design/visual/ (matching system.md write path)
- REQUIREMENTS.md confirmed using FLP artifact code for WIRE-03
- Phase 83 test suite passes: `node --test tests/phase-83/wiring-fixes.test.mjs` exits 0
- Phase 82 regression suites still pass: no regressions introduced
</success_criteria>

<output>
After completion, create `.planning/phases/83-cross-phase-wiring-fixes/83-01-SUMMARY.md`
</output>
