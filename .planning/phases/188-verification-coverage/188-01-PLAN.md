---
phase: 188-verification-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v0.22-phases/176-data-extraction-ir-foundation/176-VALIDATION.md
  - .planning/milestones/v0.22-phases/177-command-interface-+-workflow-shell/177-VALIDATION.md
  - .planning/milestones/v0.22-phases/178-reference-personas-+-rendering-engine/178-VALIDATION.md
  - .planning/milestones/v0.22-phases/179-svg-charts/179-VALIDATION.md
  - .planning/milestones/v0.22-phases/180-claim-verification-+-pdf-export/180-VALIDATION.md
autonomous: true
requirements: [VER-01]

must_haves:
  truths:
    - "Phases 176-180 each have a VALIDATION.md with nyquist_compliant: true frontmatter"
    - "Each VALIDATION.md assertion runs a command that tests behavior, not just file existence"
    - "Assertions are derived from the phase's VERIFICATION.md observable truths table"
  artifacts:
    - path: ".planning/milestones/v0.22-phases/176-data-extraction-ir-foundation/176-VALIDATION.md"
      provides: "Nyquist-compliant validation for Phase 176"
      contains: "nyquist_compliant: true"
    - path: ".planning/milestones/v0.22-phases/177-command-interface-+-workflow-shell/177-VALIDATION.md"
      provides: "Nyquist-compliant validation for Phase 177"
      contains: "nyquist_compliant: true"
    - path: ".planning/milestones/v0.22-phases/178-reference-personas-+-rendering-engine/178-VALIDATION.md"
      provides: "Nyquist-compliant validation for Phase 178"
      contains: "nyquist_compliant: true"
    - path: ".planning/milestones/v0.22-phases/179-svg-charts/179-VALIDATION.md"
      provides: "Nyquist-compliant validation for Phase 179"
      contains: "nyquist_compliant: true"
    - path: ".planning/milestones/v0.22-phases/180-claim-verification-+-pdf-export/180-VALIDATION.md"
      provides: "Nyquist-compliant validation for Phase 180"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "VALIDATION.md assertions"
      to: "VERIFICATION.md observable truths"
      via: "Each assertion derived from a verified truth"
      pattern: "node -e|npx vitest|grep -c"
---

<objective>
Write Nyquist-compliant VALIDATION.md files for v0.22 phases 176 through 180.

Purpose: Phases 176-178 have draft stubs (nyquist_compliant: false) that need full rewrite. Phases 179-180 have no VALIDATION.md at all. Each file must contain runnable assertions derived from the phase's VERIFICATION.md observable truths that produce meaningful pass/fail results — not just key-existence checks.
Output: 5 VALIDATION.md files with nyquist_compliant: true frontmatter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/188-verification-coverage/188-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Nyquist VALIDATION.md for phases 176-178 (rewrite stubs)</name>
  <files>
    .planning/milestones/v0.22-phases/176-data-extraction-ir-foundation/176-VALIDATION.md
    .planning/milestones/v0.22-phases/177-command-interface-+-workflow-shell/177-VALIDATION.md
    .planning/milestones/v0.22-phases/178-reference-personas-+-rendering-engine/178-VALIDATION.md
  </files>
  <read_first>
    .planning/milestones/v0.22-phases/176-data-extraction-ir-foundation/176-VERIFICATION.md
    .planning/milestones/v0.22-phases/177-command-interface-+-workflow-shell/177-VERIFICATION.md
    .planning/milestones/v0.22-phases/178-reference-personas-+-rendering-engine/178-VERIFICATION.md
    .planning/milestones/v0.22-phases/176-data-extraction-ir-foundation/176-VALIDATION.md
    .planning/milestones/v0.22-phases/177-command-interface-+-workflow-shell/177-VALIDATION.md
    .planning/milestones/v0.22-phases/178-reference-personas-+-rendering-engine/178-VALIDATION.md
    .planning/phases/188-verification-coverage/188-RESEARCH.md
  </read_first>
  <action>
    Rewrite the 3 existing draft VALIDATION.md stubs (which have nyquist_compliant: false) into post-execution Nyquist-compliant VALIDATION.md files.

    For each file, use this frontmatter format:
    ```yaml
    ---
    phase: {N}
    slug: {slug from VERIFICATION.md}
    status: complete
    nyquist_compliant: true
    verified: {ISO date from VERIFICATION.md verified field}
    ---
    ```

    Then add a header: `# Phase {N} — Nyquist Validation`

    Then a note: `> Post-execution validation assertions. Each assertion below can be run against the codebase to confirm the phase goal is still met.`

    Then `## Assertions` section with one subsection per observable truth from VERIFICATION.md.

    Each assertion must follow this template:
    ```
    ### Truth {N}: {description from VERIFICATION.md observable truth}
    **Command:** `{runnable command}`
    **Expected:** {what a passing result looks like}
    **Meaningful because:** {why this checks behavior, not just existence}
    ```

    CRITICAL RULES:
    - Assertions MUST be runnable commands, not just `test -f` checks
    - Use the specific assertions from the RESEARCH.md "V0.22 Phase Observable Truths Inventory" section as the primary source for runnable commands
    - Phase 176 assertions: use `node bin/pde-tools.cjs presentation artifact-read`, `node -e` for IR field checks, `npx vitest run tests/phase-176/`, `grep -c` for sentinel count
    - Phase 177 assertions: use `test -f commands/present.md` with frontmatter grep, `grep -c 'pde:present' skill-registry.md`, `npx vitest run tests/phase-177/`, `grep -c 'artifact-read' workflows/present.md`
    - Phase 178 assertions: use `node bin/pde-tools.cjs presentation render executive-summary /tmp/test-178.html /tmp/test-178.md`, grep for no script tags, TOC presence, `npx vitest run tests/phase-178/`
  </action>
  <verify>
    <automated>for n in 176 177 178; do dir=$(ls -d .planning/milestones/v0.22-phases/${n}-*/); grep -q "nyquist_compliant: true" "${dir}${n}-VALIDATION.md" || (echo "FAIL: Phase $n missing nyquist_compliant: true" && exit 1); grep -q "node " "${dir}${n}-VALIDATION.md" || (echo "FAIL: Phase $n has no node assertions" && exit 1); done && echo "PASS: Phases 176-178 VALIDATION.md files are Nyquist-compliant"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nyquist_compliant: true" .planning/milestones/v0.22-phases/176-*/176-VALIDATION.md` returns 1
    - `grep -c "nyquist_compliant: true" .planning/milestones/v0.22-phases/177-*/177-VALIDATION.md` returns 1
    - `grep -c "nyquist_compliant: true" .planning/milestones/v0.22-phases/178-*/178-VALIDATION.md` returns 1
    - Each file contains at least 3 `### Truth` subsections
    - Each file contains `node ` or `npx vitest` commands (not just `test -f`)
    - No file contains `status: draft` or `nyquist_compliant: false`
  </acceptance_criteria>
  <done>Phases 176-178 have Nyquist-compliant VALIDATION.md files with runnable assertions derived from their VERIFICATION.md observable truths</done>
</task>

<task type="auto">
  <name>Task 2: Write Nyquist VALIDATION.md for phases 179-180 (create new)</name>
  <files>
    .planning/milestones/v0.22-phases/179-svg-charts/179-VALIDATION.md
    .planning/milestones/v0.22-phases/180-claim-verification-+-pdf-export/180-VALIDATION.md
  </files>
  <read_first>
    .planning/milestones/v0.22-phases/179-svg-charts/179-VERIFICATION.md
    .planning/milestones/v0.22-phases/180-claim-verification-+-pdf-export/180-VERIFICATION.md
    .planning/phases/188-verification-coverage/188-RESEARCH.md
  </read_first>
  <action>
    Create new Nyquist-compliant VALIDATION.md files for phases 179 and 180 (these have no existing stubs).

    Use the same format as Task 1: frontmatter with `nyquist_compliant: true`, status `complete`, verified date from VERIFICATION.md.

    Phase 179 (SVG Charts — 9 truths) assertions from RESEARCH.md:
    - `node -e "const c = require('./bin/lib/charts.cjs'); console.log(Object.keys(c))"` → returns all 4 chart function names
    - `node -e "const c = require('./bin/lib/charts.cjs'); const svg = c.burndownChart({requirements:{unavailable:true}}); console.log(svg.includes('unavailable'))"` → returns `true`
    - `node -e "const c = require('./bin/lib/charts.cjs'); const svg = c.burndownChart({}); console.log(svg.includes('<polyline'))"` → returns `true`
    - `npx vitest run tests/phase-179/ --reporter=verbose` → 48 tests pass
    - Plus additional assertions for each VERIFICATION.md truth

    Phase 180 (Claim Verification + PDF Export — 6 truths) assertions from RESEARCH.md:
    - `node -e "const m=require('./bin/lib/verify-presentation.cjs'); console.log(Object.keys(m))"` → returns `['buildClaimsMap','verifyPresentation','buildVerificationFooterHtml']`
    - `node -e "const m=require('./bin/lib/export-pdf.cjs'); console.log(Object.keys(m))"` → returns `['exportPdf','cmdPresentationPdf']`
    - `grep -c "subcommand === 'pdf'" bin/pde-tools.cjs` → returns 1
    - `npx vitest run tests/phase-180/ --reporter=verbose` → 43 tests pass
    - Plus additional assertions for each VERIFICATION.md truth

    CRITICAL: Read each VERIFICATION.md fully to get all observable truths. Each truth needs its own assertion subsection.
  </action>
  <verify>
    <automated>for n in 179 180; do dir=$(ls -d .planning/milestones/v0.22-phases/${n}-*/); grep -q "nyquist_compliant: true" "${dir}${n}-VALIDATION.md" || (echo "FAIL: Phase $n missing nyquist_compliant: true" && exit 1); done && echo "PASS: Phases 179-180 VALIDATION.md files created"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nyquist_compliant: true" .planning/milestones/v0.22-phases/179-*/179-VALIDATION.md` returns 1
    - `grep -c "nyquist_compliant: true" .planning/milestones/v0.22-phases/180-*/180-VALIDATION.md` returns 1
    - Each file contains `### Truth` subsections matching VERIFICATION.md truth count
    - Each file contains behavioral assertions (node -e, npx vitest), not just test -f
  </acceptance_criteria>
  <done>Phases 179-180 have new Nyquist-compliant VALIDATION.md files with runnable behavioral assertions</done>
</task>

</tasks>

<verification>
All 5 VALIDATION.md files (176-180) exist with `nyquist_compliant: true` in frontmatter and contain runnable behavioral assertions derived from their VERIFICATION.md observable truths.
</verification>

<success_criteria>
- 5 VALIDATION.md files written with nyquist_compliant: true
- Each file has assertions for every observable truth in its VERIFICATION.md
- Assertions use node -e, npx vitest, grep -c patterns — not just test -f
- No file has status: draft or nyquist_compliant: false
</success_criteria>

<output>
After completion, create `.planning/phases/188-verification-coverage/188-01-SUMMARY.md`
</output>
