---
phase: 93-designcoverage-clobber-audit-secondary-workflow-stubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs
  - workflows/recommend.md
  - workflows/iterate.md
autonomous: true
requirements:
  - INTG-01
  - INTG-08
user_setup: []

must_haves:
  truths:
    - "recommend.md writes all 20 designCoverage fields — no business flags are clobbered when /pde:recommend runs after /pde:handoff"
    - "iterate.md writes all 20 designCoverage fields — no business flags are clobbered when /pde:iterate runs after /pde:handoff"
    - "recommend.md contains a Business product type stub comment matching the experience stub pattern"
    - "iterate.md contains a Business product type stub comment matching the experience stub pattern"
    - "Nyquist tests exist and run RED before fixes, GREEN after fixes"
  artifacts:
    - path: ".planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs"
      provides: "Structural assertions for INTG-01 (4 workflows x 2 assertions) and INTG-08 (3 stubs)"
      min_lines: 60
    - path: "workflows/recommend.md"
      provides: "20-field designCoverage write + business stub"
      contains: "hasLaunchKit"
    - path: "workflows/iterate.md"
      provides: "20-field designCoverage write + business stub"
      contains: "hasLaunchKit"
  key_links:
    - from: "tests/test-clobber-audit.cjs"
      to: "workflows/recommend.md"
      via: "readWorkflow('recommend.md') then TWENTY_FIELDS.filter"
      pattern: "TWENTY_FIELDS.filter"
    - from: "tests/test-clobber-audit.cjs"
      to: "workflows/iterate.md"
      via: "readWorkflow('iterate.md') then TWENTY_FIELDS.filter"
      pattern: "TWENTY_FIELDS.filter"
---

<objective>
Create the Nyquist test scaffold for Phase 93, then fix the 16-field designCoverage clobber regression in recommend.md and iterate.md, and add business product type stubs to both files.

Purpose: Prevent silent clobber of hasBusinessThesis/hasMarketLandscape/hasServiceBlueprint/hasLaunchKit when /pde:recommend or /pde:iterate runs after business-mode workflows. Without this fix, deploy.md refuses to run ("Launch kit not yet assembled") after any recommend/iterate execution.

Output: Test file + 2 patched workflows (recommend.md, iterate.md)
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/93-RESEARCH.md

<interfaces>
<!-- The test file uses node:test, node:assert, node:fs, node:path — all built-in. -->
<!-- ROOT resolved as path.resolve(__dirname, '..', '..', '..', '..') to reach project root. -->
<!-- readWorkflow(name) reads workflows/{name} relative to ROOT. -->
<!-- TWENTY_FIELDS array: ['hasDesignSystem','hasWireframes','hasFlows','hasHardwareSpec','hasCritique','hasIterate','hasHandoff','hasIdeation','hasCompetitive','hasOpportunity','hasMockup','hasHigAudit','hasRecommendations','hasStitchWireframes','hasPrintCollateral','hasProductionBible','hasBusinessThesis','hasMarketLandscape','hasServiceBlueprint','hasLaunchKit'] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Nyquist test scaffold for INTG-01 and INTG-08</name>
  <files>.planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs</files>
  <read_first>
    - .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/93-RESEARCH.md (section "6. Nyquist Test Assertions for INTG-01" has the COMPLETE test file to create)
    - .planning/phases/87-flows-stage/tests/test-flows-sbp.cjs (established test pattern precedent)
  </read_first>
  <action>
Create the test file at `.planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs` with the EXACT content from the RESEARCH.md "Code Examples" section 6. The file contains:

1. Header comment explaining INTG-01 and INTG-08 scope, plus the NOTE explaining why INTG-08 uses per-file presence (not global count equality)
2. `TWENTY_FIELDS` array with all 20 canonical field names
3. `readWorkflow(name)` helper function reading `path.join(ROOT, 'workflows', name)`
4. **INTG-01 tests (8 assertions across 4 describe blocks):**
   - recommend.md: (a) all 20 fields present, (b) no `16-field JSON object` in IMPORTANT note
   - iterate.md: (a) all 20 fields present, (b) no `ALL sixteen` in prose
   - mockup.md: (a) all 20 fields present, (b) no `ALWAYS write all 16 fields` in IMPORTANT
   - ideate.md: (a) all 20 fields present, (b) no `ALWAYS write all 16 fields` in IMPORTANT
5. **INTG-08 tests (3 assertions across 3 describe blocks):**
   - recommend.md contains `<!-- Business product type`
   - iterate.md contains `<!-- Business product type`
   - mockup.md contains `<!-- Business product type`

Total: 11 test assertions.

Run command: `node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs`

After creating, run the tests. They MUST fail (RED) — all 11 tests should fail since no fixes have been applied yet. This confirms the test scaffold is correct.
  </action>
  <verify>
    <automated>node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs` exists
    - File contains `const TWENTY_FIELDS = [` with all 20 field names
    - File contains `readWorkflow('recommend.md')` and `readWorkflow('iterate.md')` and `readWorkflow('mockup.md')` and `readWorkflow('ideate.md')`
    - File contains `<!-- Business product type` assertion string
    - Running `node --test` exits non-zero (RED) — all 11 tests fail
    - File contains the header NOTE explaining INTG-08 per-file presence interpretation
  </acceptance_criteria>
  <done>Test scaffold exists with 11 assertions. All tests RED (expected — no fixes applied yet).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix recommend.md — 20-field write + business stub</name>
  <files>workflows/recommend.md</files>
  <read_first>
    - workflows/recommend.md (MUST read to see current 16-field state at lines 567-595)
    - .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/93-RESEARCH.md (section "1. Corrected 20-Field Write for recommend.md" and "5. Business Product Type Stub Format")
  </read_first>
  <behavior>
    - Test: recommend.md contains all 20 designCoverage field names (hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit now present)
    - Test: recommend.md IMPORTANT note does not contain "16-field JSON object"
    - Test: recommend.md contains `<!-- Business product type -- Phase 93 stub`
  </behavior>
  <action>
Three edits to workflows/recommend.md:

**Edit 1 — Parsing table (around lines 567-584):**
Add 4 new rows to the markdown table that lists fields to parse from coverage-check output. After the existing `| hasProductionBible | false |` row, append:
```
| hasBusinessThesis | false |
| hasMarketLandscape | false |
| hasServiceBlueprint | false |
| hasLaunchKit | false |
```

**Edit 2 — Write call (line ~590):**
Replace the 16-field JSON string with the 20-field version. The file uses per-field placeholder names (`{current_hasFieldName}`). The corrected write call ends with:
`...,"hasProductionBible":{current_hasProductionBible},"hasBusinessThesis":{current_hasBusinessThesis},"hasMarketLandscape":{current_hasMarketLandscape},"hasServiceBlueprint":{current_hasServiceBlueprint},"hasLaunchKit":{current_hasLaunchKit}}'`

Keep the existing `"hasRecommendations":true` (the owned flag). All other 19 fields use `{current_hasFieldName}` pass-through.

**Edit 3 — IMPORTANT note (line ~595):**
Change `"complete 16-field JSON object"` to `"complete 20-field JSON object"`. Also update the canonical field order list to include the 4 new fields after hasProductionBible.

**Edit 4 — Business stub (after line 199, immediately after the experience stub):**
Insert this exact HTML comment on the line after the experience stub:
```
<!-- Business product type — Phase 93 stub: business product recommendations include GTM tools, Stripe/Resend integrations, landing page builders, and investor deck tooling. Business-specific tool recommendations added in a future phase after launch artifact patterns are established. Current behavior: proceed with software tool recommendation path as temporary fallback for business product type. -->
```

After edits, run: `node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs 2>&1 | grep -E "recommend|pass|fail"`
  </action>
  <verify>
    <automated>node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs 2>&1 | grep -E "recommend"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/recommend.md contains string `hasBusinessThesis`
    - workflows/recommend.md contains string `hasMarketLandscape`
    - workflows/recommend.md contains string `hasServiceBlueprint`
    - workflows/recommend.md contains string `hasLaunchKit`
    - workflows/recommend.md does NOT contain string `16-field JSON object`
    - workflows/recommend.md contains string `20-field JSON object`
    - workflows/recommend.md contains string `<!-- Business product type`
    - All 3 recommend.md Nyquist tests pass (GREEN)
  </acceptance_criteria>
  <done>recommend.md writes all 20 designCoverage fields with per-field placeholder names. IMPORTANT note says 20-field. Business stub comment present after experience stub. All recommend.md tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix iterate.md — 20-field write + business stub</name>
  <files>workflows/iterate.md</files>
  <read_first>
    - workflows/iterate.md (MUST read to see current 16-field state at lines 452-455)
    - .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/93-RESEARCH.md (section "2. Corrected 20-Field Write for iterate.md" and "5. Business Product Type Stub Format")
  </read_first>
  <behavior>
    - Test: iterate.md contains all 20 designCoverage field names
    - Test: iterate.md does not contain "ALL sixteen"
    - Test: iterate.md contains `<!-- Business product type -- Phase 93 stub`
  </behavior>
  <action>
Three edits to workflows/iterate.md:

**Edit 1 — Prose (line ~452):**
Change `"Extract ALL sixteen current flag values: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible"` to `"Extract ALL twenty current flag values: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations, hasStitchWireframes, hasPrintCollateral, hasProductionBible, hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit"`.

**Edit 2 — Write call (line ~455):**
Replace the 16-field JSON string with the 20-field version. iterate.md uses generic `{current}` placeholders. The corrected write ends with:
`...,"hasProductionBible":{current},"hasBusinessThesis":{current},"hasMarketLandscape":{current},"hasServiceBlueprint":{current},"hasLaunchKit":{current}}'`

Keep `"hasIterate":true` as the owned flag. All other 19 fields use `{current}`.

**Edit 3 — Business stub (after line 10, immediately after the experience stub):**
Insert this exact HTML comment on the line after the experience stub:
```
<!-- Business product type — Phase 93 stub: business product iteration targets lean canvas hypothesis updates, pitch deck slide revisions, and service blueprint lane adjustments rather than wireframe HTML. Business-specific iteration path added in a future phase. Current behavior: proceed with wireframe iteration path as temporary fallback for business product type. -->
```

After edits, run: `node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs 2>&1 | grep -E "iterate|pass|fail"`
  </action>
  <verify>
    <automated>node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs 2>&1 | grep -E "iterate"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/iterate.md contains string `hasBusinessThesis`
    - workflows/iterate.md contains string `hasMarketLandscape`
    - workflows/iterate.md contains string `hasServiceBlueprint`
    - workflows/iterate.md contains string `hasLaunchKit`
    - workflows/iterate.md does NOT contain string `ALL sixteen`
    - workflows/iterate.md contains string `ALL twenty`
    - workflows/iterate.md contains string `<!-- Business product type`
    - All 3 iterate.md Nyquist tests pass (GREEN)
  </acceptance_criteria>
  <done>iterate.md writes all 20 designCoverage fields with generic {current} placeholders. Prose says "ALL twenty". Business stub comment present after experience stub. All iterate.md tests GREEN.</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm recommend.md and iterate.md assertions pass:
```bash
node --test .planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/tests/test-clobber-audit.cjs
```
Expected: 6 of 11 tests pass (recommend x3, iterate x3). Remaining 5 tests (mockup x3, ideate x2) still RED — those are fixed in Plan 02.
</verification>

<success_criteria>
- Test scaffold exists with 11 assertions covering INTG-01 and INTG-08
- recommend.md and iterate.md each write all 20 designCoverage fields
- recommend.md and iterate.md each contain a `<!-- Business product type` stub comment
- IMPORTANT notes and prose updated from "16" to "20" in both files
- 6 of 11 Nyquist tests GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/93-designcoverage-clobber-audit-secondary-workflow-stubs/93-01-SUMMARY.md`
</output>
