---
phase: 177-command-interface-+-workflow-shell
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/present.md
  - workflows/present.md
  - skill-registry.md
  - tests/phase-177/present-cmd.test.mjs
autonomous: true
requirements: [CMD-01, CMD-02]
must_haves:
  truths:
    - "Running /pde:present executive-summary triggers the generation pipeline and produces output files in .planning/presentations/"
    - "Running /pde:present with no argument displays a formatted list of all 15 personas with slug, name, audience, and description"
    - "Running /pde:present unknown-slug produces a clear error with the list of valid persona names"
    - "The workflow reads IR from pde-tools presentation artifact-read, never from raw .planning/ files"
  artifacts:
    - path: "commands/present.md"
      provides: "Thin command file delegating to workflows/present.md"
      contains: "pde:present"
    - path: "workflows/present.md"
      provides: "Full workflow with persona registry, argument dispatch, IR acquisition, generation stub"
      contains: "<skill_code>PRS</skill_code>"
    - path: "skill-registry.md"
      provides: "PRS skill code registration"
      contains: "| PRS |"
    - path: "tests/phase-177/present-cmd.test.mjs"
      provides: "Integration tests for command structure and workflow lint compliance"
  key_links:
    - from: "commands/present.md"
      to: "workflows/present.md"
      via: "@workflows/present.md delegation"
      pattern: "@workflows/present.md"
    - from: "workflows/present.md"
      to: "pde-tools presentation artifact-read"
      via: "bash shell call with @file: redirect handling"
      pattern: "pde-tools.cjs.*presentation.*artifact-read"
    - from: "skill-registry.md"
      to: "workflows/present.md"
      via: "PRS skill code row"
      pattern: "PRS.*workflows/present.md"
---

<objective>
Create the /pde:present command interface and workflow shell that routes persona generation requests through the Phase 176 IR pipeline.

Purpose: Wire the user-facing command that triggers presentation generation, lists all 15 personas, and handles invalid arguments — establishing the dispatch routing that Phase 178 will fill with actual rendering logic.
Output: commands/present.md, workflows/present.md, skill-registry.md update, integration tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-command-interface-+-workflow-shell/177-RESEARCH.md

@.planning/phases/176-data-extraction-ir-foundation/176-03-SUMMARY.md

<interfaces>
Phase 176 IR acquisition pattern (from 176-03-SUMMARY.md):

From bin/lib/presentation.cjs:
- buildPresentationIR(cwd) returns full IR JSON object with 17 top-level keys
- cmdPresentationArtifactRead(cwd, raw) outputs IR via pde-tools output()

CLI invocation (bash):
  node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" presentation artifact-read
  # Returns @file:/tmp/pde-XXXXXXXX.json (large payload redirect)
  # MUST handle: if [[ "$IR" == @file:* ]]; then IR=$(cat "${IR#@file:}"); fi

From skill-registry.md (existing codes, PRS is available):
BRF, FLW, SYS, WFR, MCK, CRT, HIG, ITR, HND, HDW, CMP, OPP, REC, IDT, AUD, IMP, PRT

From commands/audit.md (command file pattern):
  YAML frontmatter with: name, description, argument-hint, allowed-tools
  Then: objective section + process section delegating to @workflows/audit.md

From workflows/opportunity.md (workflow section pattern):
  purpose section, skill_code section, skill_domain section, context_routing section, process section
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create command file, workflow file with persona registry and dispatch, update skill-registry</name>
  <files>commands/present.md, workflows/present.md, skill-registry.md</files>
  <read_first>
    commands/audit.md
    workflows/opportunity.md
    workflows/audit.md
    skill-registry.md
    references/tooling-patterns.md
    references/skill-style-guide.md
    bin/pde-tools.cjs
    .planning/phases/177-command-interface-+-workflow-shell/177-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: commands/present.md exists and contains YAML frontmatter with name: pde:present
    - Test 2: workflows/present.md exists and contains all 5 required XML sections (purpose, skill_code, skill_domain, context_routing, process)
    - Test 3: workflows/present.md contains exactly 15 persona slugs in the registry
    - Test 4: skill-registry.md contains a PRS row linking to workflows/present.md with domain tooling
    - Test 5: workflows/present.md contains the @file: redirect handling pattern
    - Test 6: workflows/present.md contains three dispatch branches (list mode, generate mode, error mode)
  </behavior>
  <action>
    Per CMD-01 and CMD-02: Create the /pde:present command and workflow.

    STEP 1: Create tests/phase-177/present-cmd.test.mjs (RED)

    Create integration tests that verify:
    1. commands/present.md exists and contains "name: pde:present" in YAML frontmatter
    2. commands/present.md contains "@workflows/present.md" delegation
    3. workflows/present.md contains "<purpose>" section
    4. workflows/present.md contains "<skill_code>PRS</skill_code>"
    5. workflows/present.md contains "<skill_domain>tooling</skill_domain>"
    6. workflows/present.md contains "<context_routing>" section
    7. workflows/present.md contains "<process>" section
    8. workflows/present.md contains all 15 persona slugs: executive-summary, investor-update, sprint-review, client-deliverable, stakeholder-status, product-manager, project-manager, case-study, agile-report, design-persona, research-persona, post-mortem, adr-summary, launch-announcement, portfolio-overview
    9. workflows/present.md contains "@file:" redirect handling pattern (the string "@file:")
    10. workflows/present.md contains "pde-tools.cjs" or "pde-tools" reference for IR acquisition
    11. skill-registry.md contains "| PRS |" row with "workflows/present.md" and "tooling"
    12. workflows/present.md contains LIST MODE, GENERATE MODE, and ERROR MODE branches (or equivalent dispatch logic words)

    Use fs.readFileSync to read the files, describe/it blocks with vitest. Pattern: mirror tests/phase-176/presentation-cmd.test.mjs style for file reads.

    Run tests. They MUST all fail (RED).

    STEP 2: Create commands/present.md (GREEN - partial)

    Follow the exact command file pattern from commands/audit.md. The YAML frontmatter must contain:
    - name: pde:present
    - description: "Generate a stakeholder presentation for the specified persona, or list all available personas"
    - argument-hint: "[persona-slug] [--dry-run] [--verbose]"
    - allowed-tools: [Read, Write, Edit, Bash, Glob, Grep, Task]

    After frontmatter: objective section saying "Execute the /pde:present command."
    Then process section saying "Follow @workflows/present.md exactly. Pass all of $ARGUMENTS to the workflow."

    STEP 3: Create workflows/present.md (GREEN - main artifact)

    Create the workflow with all 5 required XML sections:

    PURPOSE SECTION: Generate audience-specific stakeholder presentations from the deterministic IR extraction pipeline. Reads structured IR from pde-tools (never raw .planning/ files), dispatches to persona-specific generation prompts, and writes dual HTML+Markdown output to .planning/presentations/.

    SKILL_CODE SECTION: PRS

    SKILL_DOMAIN SECTION: tooling

    CONTEXT_ROUTING SECTION: Load .planning/PROJECT.md (hard requirement, HALT if missing). Soft dependency on .planning/presentations/ directory (created by IR pipeline if missing).

    PROCESS SECTION: Numbered steps as follows:

    Step 0 Initialize: Display banner "PDE > PRESENT". Parse $ARGUMENTS for persona slug and flags (--dry-run, --verbose).

    Step 1 Detect invocation mode: Three branches:
    - If $ARGUMENTS is empty (no persona slug) then LIST MODE
    - If persona slug matches a known slug then GENERATE MODE
    - Otherwise then ERROR MODE

    Step 2 LIST MODE - Display persona registry: Print a formatted table of all 15 personas with columns: Slug, Display Name, Audience, Description. Use the exact data from the 15-persona registry (see RESEARCH.md for full table with all slugs, display names, audiences, and descriptions). Then HALT (do not proceed to generation).

    The 15 persona slugs that MUST appear in the registry table:
    executive-summary, investor-update, sprint-review, client-deliverable, stakeholder-status, product-manager, project-manager, case-study, agile-report, design-persona, research-persona, post-mortem, adr-summary, launch-announcement, portfolio-overview

    Step 3 ERROR MODE - Invalid persona: Display error using What/Why/What-to-do format from skill-style-guide.md. Include the unknown slug, explanation that a valid persona slug is required, the full list of valid slugs, and a usage example. Then HALT.

    Step 4 GENERATE MODE - Acquire IR: Run the pde-tools command to get the IR:
      IR=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" presentation artifact-read)
      if [[ "$IR" == @file:* ]]; then IR=$(cat "${IR#@file:}"); fi
    Parse the IR JSON. If the command fails or returns empty, HALT with error about IR extraction failure.

    Step 5 GENERATE MODE - Set output paths: Compute output filenames using DATE=$(date +%Y-%m-%d), HTML_PATH=.planning/presentations/[PERSONA_SLUG]-${DATE}.html, MD_PATH=.planning/presentations/[PERSONA_SLUG]-${DATE}.md. Ensure .planning/presentations/ directory exists (mkdir -p).

    Step 6 GENERATE MODE - Dispatch to persona generator: This is the generation stub that Phase 178 will replace with actual rendering logic. For now: Log the persona being generated and the IR metadata. Write a placeholder HTML file with a title matching the persona display name and a body noting Phase 178 will implement the rendering engine. Write a placeholder Markdown file with equivalent content. Log both output paths.

    Step 7 Complete: Display completion banner.

    STEP 4: Update skill-registry.md (GREEN - final)

    Add a new row to the skill-registry.md table:
    | PRS | /pde:present | workflows/present.md | tooling | active |

    Run tests. They MUST all pass (GREEN).
  </action>
  <verify>
    <automated>npx vitest run tests/phase-177/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "pde:present" commands/present.md returns at least 1
    - grep -c "@workflows/present.md" commands/present.md returns at least 1
    - grep -c "skill_code.PRS" workflows/present.md returns 1
    - grep -c "skill_domain.tooling" workflows/present.md returns 1
    - grep -c "executive-summary" workflows/present.md returns at least 1
    - grep -c "portfolio-overview" workflows/present.md returns at least 1
    - grep -c "@file:" workflows/present.md returns at least 1
    - grep -c "pde-tools" workflows/present.md returns at least 1
    - grep -c "| PRS |" skill-registry.md returns 1
    - All tests in tests/phase-177/ pass green
  </acceptance_criteria>
  <done>
    commands/present.md exists with correct YAML frontmatter and delegation to workflows/present.md.
    workflows/present.md exists with all 5 LINT-required XML sections, inline 15-persona registry, three-branch dispatch (list/generate/error), IR acquisition with @file: handling, and placeholder output for generation stub.
    skill-registry.md contains PRS row with tooling domain.
    All integration tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate lint compliance and end-to-end smoke test</name>
  <files>workflows/present.md, commands/present.md</files>
  <read_first>
    workflows/present.md
    commands/present.md
    skill-registry.md
    references/tooling-patterns.md
  </read_first>
  <action>
    Lint validation: Run the PDE lint validator to confirm no LINT errors on the new files:
      node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" test --lint 2>&1 | grep -i "present\|PRS\|LINT"
    If any LINT errors reference present.md or PRS, fix them in the workflow/command/registry.

    Smoke test the IR pipeline: Run the IR extraction to confirm it still works end-to-end:
      node bin/pde-tools.cjs presentation artifact-read
    Confirm it returns @file:/path/to/json and the JSON contains schema_version "1.0".

    Verify persona count: Count unique persona slugs in workflows/present.md. Use grep with each of the 15 slugs and confirm all 15 are present. Must find exactly 15 unique slugs.

    Fix any issues found during validation. If workflow needs edits, update and re-run tests to confirm they still pass.

    Final test run:
      npx vitest run tests/phase-177/ --reporter=verbose
  </action>
  <verify>
    <automated>npx vitest run tests/phase-177/ --reporter=verbose && node bin/pde-tools.cjs presentation artifact-read >/dev/null 2>&1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - node bin/pde-tools.cjs test --lint 2>&1 produces no LINT FAIL lines for present files
    - node bin/pde-tools.cjs presentation artifact-read exits 0
    - Persona slug count in workflows/present.md equals 15
    - All tests/phase-177/ tests pass
  </acceptance_criteria>
  <done>
    No LINT errors for present.md or PRS skill code. IR pipeline still works. All 15 persona slugs present in workflow. All tests green.
  </done>
</task>

</tasks>

<verification>
Phase 177 is verified when:
1. commands/present.md exists with correct YAML frontmatter delegating to @workflows/present.md
2. workflows/present.md exists with all 5 LINT-required sections, 15-persona registry, three-branch dispatch
3. skill-registry.md contains PRS row
4. npx vitest run tests/phase-177/ --reporter=verbose passes all tests
5. No LINT errors from pde-tools test --lint for present-related files
6. IR pipeline (pde-tools presentation artifact-read) still works end-to-end
</verification>

<success_criteria>
- CMD-01: /pde:present [persona] triggers the generation pipeline (stub output) and produces files in .planning/presentations/
- CMD-02: /pde:present (no argument) lists all 15 personas with slug, name, audience, description
- Invalid persona slug produces clear error with valid persona list
- Workflow reads IR from pde-tools, never from raw .planning/ files
- PRS skill code registered in skill-registry.md with tooling domain
- All integration tests pass, no LINT errors
</success_criteria>

<output>
After completion, create `.planning/phases/177-command-interface-+-workflow-shell/177-01-SUMMARY.md`
</output>
