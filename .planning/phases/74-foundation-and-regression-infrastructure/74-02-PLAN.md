---
phase: 74-foundation-and-regression-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["74-01"]
files_modified:
  - workflows/flows.md
  - workflows/system.md
  - workflows/wireframe.md
  - workflows/critique.md
  - workflows/iterate.md
  - workflows/handoff.md
  - workflows/ideate.md
  - workflows/competitive.md
  - workflows/opportunity.md
  - workflows/hig.md
  - workflows/recommend.md
  - workflows/mockup.md
  - workflows/build.md
  - references/experience-disclaimer.md
  - bin/lib/design.cjs
autonomous: true
requirements:
  - FNDX-02
  - FNDX-04

must_haves:
  truths:
    - "All 14 pipeline workflow files contain an experience branch site (brief.md from Plan 01 + 13 downstream from this plan)"
    - "Downstream experience branch stubs are empty placeholders with phase-forward comments, not content"
    - "No sub-type structural branching exists outside brief.md"
    - "Disclaimer block template exists in references/ and is loaded by critique.md and handoff.md"
    - "design.cjs DOMAIN_DIRS includes physical"
  artifacts:
    - path: "references/experience-disclaimer.md"
      provides: "Reusable regulatory disclaimer block"
      contains: "VERIFY WITH LOCAL AUTHORITY"
    - path: "workflows/critique.md"
      provides: "Experience branch stub + disclaimer reference"
      contains: "experience-disclaimer.md"
    - path: "workflows/handoff.md"
      provides: "Experience branch stub + disclaimer reference"
      contains: "experience-disclaimer.md"
    - path: "bin/lib/design.cjs"
      provides: "DOMAIN_DIRS with physical domain"
      contains: "physical"
  key_links:
    - from: "workflows/critique.md"
      to: "references/experience-disclaimer.md"
      via: "@references/ required_reading include"
      pattern: "@references/experience-disclaimer.md"
    - from: "workflows/handoff.md"
      to: "references/experience-disclaimer.md"
      via: "@references/ required_reading include"
      pattern: "@references/experience-disclaimer.md"
    - from: "tests/phase-74/experience-regression.test.mjs"
      to: "all 14 workflow files"
      via: "FNDX-02 describe block loop assertion"
      pattern: "content.includes.*experience"
---

<objective>
Add experience branch stubs to all 13 downstream pipeline workflow files, create the regulatory disclaimer block template, wire it into critique.md and handoff.md, and add the physical domain to design.cjs.

Purpose: Completes the branch site audit requirement (FNDX-02) ensuring every pipeline workflow has an experience conditional. Creates the disclaimer infrastructure that Phases 79 and 81 will consume. The physical domain directory prepares for Phase 80 print artifacts.

Output: 13 modified workflow files with experience stubs, new experience-disclaimer.md reference, updated design.cjs
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-foundation-and-regression-infrastructure/74-RESEARCH.md
@.planning/phases/74-foundation-and-regression-infrastructure/74-01-SUMMARY.md

<interfaces>
<!-- Key patterns the executor needs -->

From workflows/handoff.md (lines 510-516) — existing branch pattern to replicate:
```markdown
#### 4i. Apply productType conditional for hardware sections

Based on PRODUCT_TYPE:
- "software": [existing software behavior]
- "hardware": omit software component API sections, include Hardware Handoff sections
- "hybrid": include both software and hardware sections
- If PRODUCT_TYPE unavailable (default "software"): include software sections only
```

From workflows/critique.md (lines 5-12) — required_reading pattern:
```xml
<required_reading>
@references/skill-style-guide.md
@references/mcp-integration.md
@references/design-principles.md
@references/wcag-baseline.md
@references/quality-standards.md
@references/composition-typography.md
</required_reading>
```

From bin/lib/design.cjs (line 17) — DOMAIN_DIRS:
```javascript
const DOMAIN_DIRS = ['assets', 'strategy', 'ux', 'ux/mockups', 'visual', 'review', 'handoff', 'hardware'];
```

From tests/phase-74/experience-regression.test.mjs — FNDX-02 assertion:
```javascript
test('all 14 pipeline workflow files contain experience branch site', () => {
  for (const relPath of PIPELINE_WORKFLOW_FILES) {
    const content = readFileSync(join(ROOT, relPath), 'utf8');
    assert.ok(content.includes('experience'), `${relPath}: missing experience branch site`);
  }
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add experience branch stubs to all 13 downstream workflow files</name>
  <files>
    workflows/flows.md,
    workflows/system.md,
    workflows/wireframe.md,
    workflows/critique.md,
    workflows/iterate.md,
    workflows/handoff.md,
    workflows/ideate.md,
    workflows/competitive.md,
    workflows/opportunity.md,
    workflows/hig.md,
    workflows/recommend.md,
    workflows/mockup.md,
    workflows/build.md
  </files>
  <read_first>
    - workflows/handoff.md (lines 505-520 — the existing PRODUCT_TYPE conditional pattern at Step 4i is the reference template for all stubs)
    - workflows/critique.md (find any existing PRODUCT_TYPE or productType conditional)
    - workflows/system.md (find PRODUCT_TYPE reference — line 70)
    - workflows/flows.md (find any product type branching)
    - workflows/wireframe.md (find any product type branching)
    - workflows/hig.md (find any product type branching)
    - workflows/build.md (find stage name references)
    - .planning/phases/74-foundation-and-regression-infrastructure/74-RESEARCH.md (Pattern 2: exact stub template)
  </read_first>
  <action>
For each of the 13 downstream workflow files (all except brief.md which was handled in Plan 01), add an experience branch stub. The exact insertion point varies per file — find the existing PRODUCT_TYPE / productType conditional or the most relevant section where product type affects behavior.

**The stub template to insert at each branch site:**

```markdown
- "experience":  Phase 74 stub — experience-specific content added in subsequent phases.
                 Current behavior: proceed with software path as temporary fallback.
                 NEVER produce floor plans, production bibles, or experience token files from this stub.
```

**Per-file insertion instructions:**

1. **workflows/handoff.md** — At Step 4i (line ~510-516), the existing `Based on PRODUCT_TYPE:` block already has software/hardware/hybrid branches. Add `- "experience":` branch between `- "hybrid":` and `- If PRODUCT_TYPE unavailable`. Also add experience branch at line ~651 where hardware/hybrid output sections are listed. Also at line ~929 in the summary table and line ~554 in the Product Type output line.

2. **workflows/system.md** — Find the `PRODUCT_TYPE` reference (line ~70 where it reads the type from brief). Add experience branch stub near any conditional that references PRODUCT_TYPE. If no conditional block exists, add a comment block near the PRODUCT_TYPE variable extraction:
   ```
   <!-- Experience product type: experience-specific design system extensions (SYS-experience-tokens.json) added in Phase 76 -->
   ```

3. **workflows/critique.md** — Find where product type affects critique behavior. Add experience branch stub noting that experience critique perspectives (safety, accessibility, operations, sustainability, licensing, financial, community) are added in Phase 79.

4. **workflows/flows.md** — Add experience branch stub noting temporal, spatial, and social flow dimensions are added in Phase 77.

5. **workflows/wireframe.md** — Add experience branch stub noting floor plan (FLP) and timeline (TML) artifacts are added in Phase 78.

6. **workflows/hig.md** — Add experience branch stub noting physical interface guidelines replace WCAG checks in Phase 79.

7. **workflows/iterate.md** — Add experience branch stub noting experience iteration targets floor plan revisions in Phase 79.

8. **workflows/ideate.md** — Add experience branch stub as comment noting experience ideation follows same divergent pattern.

9. **workflows/competitive.md** — Add experience branch stub as comment noting competitive analysis applies to experience venues/events.

10. **workflows/opportunity.md** — Add experience branch stub as comment noting opportunity analysis applies to experience market.

11. **workflows/recommend.md** — Add experience branch stub as comment noting recommendations apply to experience tooling.

12. **workflows/mockup.md** — Add experience branch stub noting experience mockup extensions (if any) would be added in later phases.

13. **workflows/build.md** — Add experience to the valid product types list in the STAGES table or orchestrator context. Add a comment noting experience type follows the same stage progression. Do NOT add new stages.

**Critical rules:**
- Each stub MUST contain the string `experience` (for FNDX-02 test assertion)
- Stubs are EMPTY — no actual experience behavior, just placeholder comments with phase-forward references
- Do NOT add sub-type structural branches (`IF sub_type === "installation"`) in ANY of these files (FNDX-04)
- Preserve ALL existing content unchanged — only ADD the experience branch
- The existing software default/fallback MUST remain intact
  </action>
  <verify>
    <automated>for f in workflows/flows.md workflows/system.md workflows/wireframe.md workflows/critique.md workflows/iterate.md workflows/handoff.md workflows/ideate.md workflows/competitive.md workflows/opportunity.md workflows/hig.md workflows/recommend.md workflows/mockup.md workflows/build.md; do grep -l "experience" "$f" 2>/dev/null || echo "MISSING: $f"; done | sort</automated>
  </verify>
  <acceptance_criteria>
    - All 13 workflow files contain the string `experience` (grep returns all 13 filenames)
    - No workflow file other than brief.md contains `IF.*single-night` or `ELSE IF.*single-night` (FNDX-04 guard)
    - workflows/handoff.md contains `"experience":` in the Step 4i PRODUCT_TYPE conditional block
    - workflows/build.md contains the string `experience`
    - All 13 files remain valid markdown (no broken syntax)
    - No new workflow files were created (only existing files modified)
  </acceptance_criteria>
  <done>All 13 downstream workflow files contain experience branch stubs. Combined with brief.md from Plan 01, all 14 pipeline workflow files have experience branch sites. No sub-type structural branching outside brief.md.</done>
</task>

<task type="auto">
  <name>Task 2: Create disclaimer block template, wire into workflows, and update design.cjs</name>
  <files>
    references/experience-disclaimer.md,
    workflows/critique.md,
    workflows/handoff.md,
    bin/lib/design.cjs
  </files>
  <read_first>
    - references/wcag-baseline.md (first 10 lines — see existing reference file format/structure)
    - workflows/critique.md (lines 1-15 — see required_reading block for insertion point)
    - workflows/handoff.md (lines 1-15 — see required_reading block for insertion point)
    - bin/lib/design.cjs (lines 14-20 — DOMAIN_DIRS constant and self-test assertion at line 388)
  </read_first>
  <action>
**1. Create `references/experience-disclaimer.md`:**

Write a new file with the following exact content:

```markdown
# Experience Product Type — Regulatory Disclaimer

> [VERIFY WITH LOCAL AUTHORITY] — Regulatory requirements (occupancy limits, egress widths,
> noise ordinances, toilet ratios, staffing ratios) vary significantly by jurisdiction and are
> updated frequently. All numerical values in this section represent industry guidance ranges,
> not applicable regulations. Verify every compliance threshold with your local authority having
> jurisdiction (AHJ), fire marshal, and relevant licensing authority before finalizing.

## Usage

This disclaimer block MUST appear inline in every section of critique and handoff output that
contains regulatory values (egress times, ratios, distances, capacity limits, noise levels).

The `[VERIFY WITH LOCAL AUTHORITY]` tag must appear on the same line as or immediately adjacent
to every specific numerical regulatory value. Do not group disclaimers at the end of a section.

## Consumers

- `workflows/critique.md` — Phase 79: seven event-specific critique perspectives
- `workflows/handoff.md` — Phase 81: production bible safety and licensing sections

## Prohibited Patterns

- Stating a specific regulatory threshold as fact without the `[VERIFY WITH LOCAL AUTHORITY]` tag
- Using "print-ready" without an adjacent prepress disclaimer (see STATE.md Key Decision)
- Generating jurisdiction-specific permit requirements (out of scope per REQUIREMENTS.md)
```

**2. Wire disclaimer into critique.md `<required_reading>`:**

In `workflows/critique.md`, add `@references/experience-disclaimer.md` to the `<required_reading>` block (after the last existing `@references/` line, before `</required_reading>`).

**3. Wire disclaimer into handoff.md `<required_reading>`:**

In `workflows/handoff.md`, add `@references/experience-disclaimer.md` to the `<required_reading>` block (after the last existing `@references/` line, before `</required_reading>`).

**4. Add `physical` to design.cjs DOMAIN_DIRS:**

In `bin/lib/design.cjs`, line 17, change:
```javascript
const DOMAIN_DIRS = ['assets', 'strategy', 'ux', 'ux/mockups', 'visual', 'review', 'handoff', 'hardware'];
```
to:
```javascript
const DOMAIN_DIRS = ['assets', 'strategy', 'ux', 'ux/mockups', 'visual', 'review', 'handoff', 'hardware', 'physical'];
```

Also update the self-test assertion at line ~388 that checks `creates all 8 domain subdirectories` — change the test description string from `'creates all 8 domain subdirectories (including ux/mockups)'` to `'creates all 9 domain subdirectories (including ux/mockups, physical)'` and verify the loop correctly iterates DOMAIN_DIRS (it already does dynamically, so no assertion code change needed, only the description string).
  </action>
  <verify>
    <automated>test -f references/experience-disclaimer.md && grep "experience-disclaimer" workflows/critique.md && grep "experience-disclaimer" workflows/handoff.md && grep "physical" bin/lib/design.cjs | head -2 && node --test tests/phase-74/experience-regression.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - references/experience-disclaimer.md exists and contains `[VERIFY WITH LOCAL AUTHORITY]`
    - references/experience-disclaimer.md contains `Regulatory Disclaimer` in heading
    - workflows/critique.md contains `@references/experience-disclaimer.md` inside `<required_reading>` block
    - workflows/handoff.md contains `@references/experience-disclaimer.md` inside `<required_reading>` block
    - bin/lib/design.cjs line 17 contains `'physical'` in DOMAIN_DIRS array
    - bin/lib/design.cjs self-test description contains `9 domain subdirectories`
    - `node --test tests/phase-74/experience-regression.test.mjs` passes all tests (all FNDX-01 through FNDX-04 assertions green)
    - `node --test tests/phase-64/manifest-schema.test.mjs` still passes (no regression)
    - `grep -rn "experience-disclaimer" workflows/` returns exactly 2 matches (critique.md and handoff.md)
  </acceptance_criteria>
  <done>Disclaimer block template exists and is wired into critique.md and handoff.md required_reading. design.cjs DOMAIN_DIRS includes physical. Full regression smoke matrix passes.</done>
</task>

</tasks>

<verification>
After both tasks, run the full verification suite:

1. `node --test tests/phase-74/experience-regression.test.mjs` — ALL tests pass (all 14 workflow files have experience branch sites, brief.md has detection logic, no sub-type structural branching outside brief.md)
2. `node --test tests/phase-64/manifest-schema.test.mjs` — ALL tests pass (no manifest regression)
3. `grep -rn "experience-disclaimer" workflows/` — returns exactly 2 matches (critique.md and handoff.md)
4. `grep "physical" bin/lib/design.cjs` — returns DOMAIN_DIRS line with physical
5. `test -f references/experience-disclaimer.md` — file exists
6. Verify no new workflow files: `ls workflows/*.md | wc -l` matches pre-phase count (no new files added)
</verification>

<success_criteria>
- All 14 pipeline workflow files contain experience branch site (FNDX-02 complete)
- No sub-type structural branching outside brief.md (FNDX-04 enforcement)
- Disclaimer block template exists in references/ and is loaded by critique.md and handoff.md
- design.cjs creates physical domain directory
- Full regression smoke matrix passes
- No regressions in existing phase-64 tests
</success_criteria>

<output>
After completion, create `.planning/phases/74-foundation-and-regression-infrastructure/74-02-SUMMARY.md`
</output>
