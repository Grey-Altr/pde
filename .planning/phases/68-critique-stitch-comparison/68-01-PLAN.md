---
phase: 68-critique-stitch-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/critique.md
autonomous: true
requirements:
  - CRT-01
  - CRT-02
  - CRT-03
  - CRT-04

must_haves:
  truths:
    - "Running /pde:critique on a Stitch wireframe produces a report with a ## Stitch Comparison section"
    - "The 4-perspective critique and composite score are preserved unchanged for Stitch artifacts"
    - "DTCG token-format findings are suppressed for Stitch artifacts (no Token not applied findings)"
    - "Claude reads STH PNG screenshots using multimodal image analysis during critique"
    - "Stitch Comparison recommendations do NOT appear in the Action List or DESIGN-STATE Open Critique Items"
  artifacts:
    - path: "workflows/critique.md"
      provides: "Stitch-aware critique with three insertion points: Step 2g detection, Step 4 suppression+PNG, Step 5b-STITCH comparison section"
      contains: "## Stitch Comparison"
  key_links:
    - from: "workflows/critique.md Step 2g"
      to: "pde-tools.cjs design manifest-read"
      via: "MANIFEST variable shell command"
      pattern: "manifest-read"
    - from: "workflows/critique.md Step 2g"
      to: "STITCH_ARTIFACTS list"
      via: "source === stitch check from manifest"
      pattern: "STITCH_ARTIFACTS"
    - from: "workflows/critique.md Step 4"
      to: "SUPPRESS_TOKEN_FINDINGS gate"
      via: "STITCH_SOURCE boolean per artifact"
      pattern: "SUPPRESS_TOKEN_FINDINGS"
    - from: "workflows/critique.md Step 5b-STITCH"
      to: "## Stitch Comparison section"
      via: "conditional on STITCH_ARTIFACTS non-empty"
      pattern: "Stitch Comparison"
---

<objective>
Add Stitch-aware evaluation mode to critique.md at three insertion points: (1) Step 2g reads the design manifest to classify artifacts as Stitch-sourced or Claude-sourced, (2) Step 4 suppresses DTCG token-format findings for Stitch artifacts and reads paired PNG screenshots for multimodal analysis, (3) Step 5b-STITCH appends a conditional ## Stitch Comparison section with per-screen delta reports.

Purpose: Without this, running /pde:critique on Stitch-generated wireframes produces false-positive Major findings for every hardcoded hex value (Stitch does not use OKLCH custom properties). This phase makes critique Stitch-aware so feedback is meaningful.

Output: Modified workflows/critique.md with Stitch detection, token suppression, PNG reading, and Stitch Comparison section.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-critique-stitch-comparison/68-RESEARCH.md
</context>

<interfaces>
<!-- Key patterns the executor needs from the existing codebase -->

From workflows/critique.md Step 2f (line ~126-137) — insertion point for new Step 2g:
```markdown
#### 2f. Version gate

Use the Glob tool to check for existing `CRT-critique-v*.md` in `.planning/design/review/`.
...
Display: `Step 2/7: {N} wireframe(s) discovered at {fidelity} fidelity. Critique v{VERSION} will be generated.`

---

### Step 3/7: Probe MCP availability
```

From workflows/critique.md Step 4 (line ~235-238) — insertion point for Stitch source gate:
```markdown
For each wireframe file in WIREFRAME_FILES:

  Use the Read tool to read the wireframe HTML content.
  Determine fidelity from FIDELITY_MAP for severity calibration.

  Display progress: `Step 4/7: Evaluating {screen_slug} ({N} of {total})...`
```

From workflows/critique.md fidelity-severity calibration table (line ~347-357):
```markdown
| Finding Type | Lo-fi Severity | Mid-fi Severity | Hi-fi Severity |
|--------------|----------------|-----------------|----------------|
| Token not applied | Skip | Minor | Major |
```

From workflows/critique.md Step 5b sections (line ~529-605) — 8 sections ending with Footer:
```markdown
8. Footer:
   ```
   *Generated by PDE-OS /pde:critique | {date} | Mode: {mode} | Groups: {group_list}*
   ```

Use the Write tool to write to `.planning/design/review/CRT-critique-v{VERSION}.md`.
```

From workflows/critique.md Step 5b section 6 (line ~585-591) — Action List:
```markdown
6. `## Action List for /pde:iterate`

   Priority-ordered checkbox list for iterate consumption:
   ```
   - [ ] {finding_summary} — {severity}/{effort}
   ```
   Critical findings first, then major, then minor, then nits.
```

pde-tools.cjs manifest-read invocation pattern (same as all other pde-tools calls):
```bash
MANIFEST=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-read)
if [[ "$MANIFEST" == @file:* ]]; then MANIFEST=$(cat "${MANIFEST#@file:}"); fi
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 2g Stitch artifact classification and Step 2e fidelity fallback</name>
  <files>workflows/critique.md</files>
  <read_first>
    - workflows/critique.md (full file — executor must see current state of all steps)
    - .planning/phases/68-critique-stitch-comparison/68-RESEARCH.md (Architecture Patterns 1 and 6, Open Question 1)
  </read_first>
  <action>
Insert two blocks into workflows/critique.md:

**Block A — Step 2e Stitch fidelity fallback (modify existing Step 2e):**

After the existing line `- Extract fidelity from the 'pde-layout--{fidelity}' class on the '<body>' element (values: lofi, midfi, hifi)` in Step 2e, add:

```markdown
- **Stitch fidelity fallback:** If the current file starts with `STH-` (Stitch-generated) and no `pde-layout--{fidelity}` class is found on the body element: default fidelity to `hifi`. Stitch always generates hi-fi quality output and does not use PDE scaffold classes. Log: `  STH-{slug}: no pde-layout class — defaulting to hifi fidelity (Stitch-generated).`
```

**Block B — New Step 2g (insert AFTER Step 2f "Version gate" and its Display line, BEFORE the `---` separator that precedes Step 3/7):**

Add the following as a new sub-section of Step 2:

```markdown
#### 2g. Classify artifacts by source (Stitch vs Claude)

Read the design manifest:

```bash
MANIFEST=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-read)
if [[ "$MANIFEST" == @file:* ]]; then MANIFEST=$(cat "${MANIFEST#@file:}"); fi
```

Parse the JSON output. For each file path in WIREFRAME_FILES:
- Extract the artifact code from the filename stem (e.g., `STH-login.html` -> code `STH-login`)
- Look up `manifest.artifacts["STH-login"].source` in the parsed JSON
- If `source === "stitch"`: add code to STITCH_ARTIFACTS list; note paired PNG path as the same stem with `.png` extension in the same directory (e.g., `STH-login.png`)

If manifest-read returns null or fails: SET STITCH_ARTIFACTS = []. Log: `Warning: Could not read design manifest -- Stitch artifact detection skipped.`

If WIREFRAME_FILES contains `STH-*.html` files but the manifest entry has no `source` field: treat as non-Stitch (conservative -- avoids false suppression).

SET STITCH_ARTIFACTS = [list of artifact codes where source === "stitch"]

If STITCH_ARTIFACTS is non-empty:
  Display: `Step 2/7: {N} Stitch artifact(s) detected: {comma-separated slug list}. Stitch-aware evaluation mode active.`
Else:
  Display: `Step 2/7: No Stitch artifacts detected. Standard evaluation mode.`
```

Also update the Step 2 header description from "This step has six sub-sections" to "This step has seven sub-sections" (line 58 area of critique.md).

**Do NOT modify Steps 1, 3, 3.5, 6, or 7.** Only Step 2e and the new Step 2g are changed.
  </action>
  <verify>
    <automated>grep -c "manifest-read" workflows/critique.md | grep -q "[1-9]" && grep -c "STITCH_ARTIFACTS" workflows/critique.md | grep -q "[1-9]" && grep -c "2g\." workflows/critique.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/critique.md contains `#### 2g. Classify artifacts by source (Stitch vs Claude)`
    - workflows/critique.md contains `manifest-read` in the Step 2g bash block
    - workflows/critique.md contains `STITCH_ARTIFACTS` (at least 3 occurrences: set, non-empty check, display)
    - workflows/critique.md contains `source === "stitch"` or equivalent check
    - workflows/critique.md Step 2e contains `STH-` fidelity fallback with `hifi` default
    - workflows/critique.md Step 2 header says "seven sub-sections" not "six"
    - Step 2g appears AFTER Step 2f and BEFORE Step 3/7
  </acceptance_criteria>
  <done>Step 2g classifies STH artifacts by reading the manifest, Step 2e handles Stitch fidelity fallback, and STITCH_ARTIFACTS list is built for downstream use</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 4 Stitch source gate with token suppression and PNG multimodal reading</name>
  <files>workflows/critique.md</files>
  <read_first>
    - workflows/critique.md (re-read after Task 1 modifications)
    - .planning/phases/68-critique-stitch-comparison/68-RESEARCH.md (Architecture Patterns 2 and 3, Anti-Patterns)
  </read_first>
  <action>
Insert Stitch source gate into Step 4 of critique.md, inside the per-file evaluation loop. The insertion goes AFTER the existing lines:

```
  Use the Read tool to read the wireframe HTML content.
  Determine fidelity from FIDELITY_MAP for severity calibration.
```

And BEFORE `Display progress: Step 4/7: Evaluating {screen_slug}...`

Add this block:

```markdown
  #### Stitch source gate

  Extract artifact code from current file path (e.g., `STH-login.html` -> `STH-login`).
  IF artifact code is in STITCH_ARTIFACTS:
    SET STITCH_SOURCE = true
    SET SUPPRESS_TOKEN_FINDINGS = true

    Note in the evaluation context:
    "This artifact was generated by Google Stitch (source: stitch in manifest).
     Stitch produces hardcoded hex color values and numeric spacing values rather
     than OKLCH custom properties and CSS token references.
     DTCG token-format consistency findings (fidelity-severity row: 'Token not applied')
     are SUPPRESSED for this artifact. Token divergences will be reported in the
     ## Stitch Comparison section, not as findings."

    Construct paired PNG path: same directory, replace `.html` with `.png`
    (e.g., `.planning/design/ux/wireframes/STH-login.html` -> `.planning/design/ux/wireframes/STH-login.png`)

    Use the Read tool to read the PNG file.
    - If PNG read succeeds (image content present): SET HAS_PNG = true. Log: `  -> STH-{slug}.png loaded for visual evaluation.`
    - If PNG read fails (file not found): SET HAS_PNG = false. Log: `  -> Warning: STH-{slug}.png not found. Visual evaluation will use HTML source only.`

  ELSE:
    SET STITCH_SOURCE = false
    SET SUPPRESS_TOKEN_FINDINGS = false
    SET HAS_PNG = false
```

Then, AFTER the existing Perspective 2 section (Visual Hierarchy), add a token suppression application note. Find the line area around `#### Typography Pairing Assessment` and insert BEFORE it:

```markdown
  #### Token suppression gate (Stitch artifacts only)

  For design token application checks within Perspective 2 and the fidelity-severity calibration:
  IF SUPPRESS_TOKEN_FINDINGS is true:
    Skip any findings of type "Token not applied" (fidelity-severity table row: `| Token not applied | Skip | Minor | Major |`).
    Do NOT add "Token not applied" findings to the findings list. Do NOT deduct from the score.
    Add inline note to Perspective 2 findings:
    "[Token-format findings suppressed: Stitch artifact uses hardcoded hex values -- see ## Stitch Comparison for divergence report]"

  ELSE:
    Apply standard fidelity-severity calibration for "Token not applied" row (unchanged behavior).

  **Scope:** Only the "Token not applied" finding type is suppressed. ALL other finding types remain active for Stitch artifacts:
  - Color contrast failures (WCAG 1.4.3) -- still evaluated (hex values are checkable for 4.5:1 ratio)
  - ARIA label missing -- still evaluated
  - CTA prominence -- still evaluated
  - Missing state variants -- still evaluated
  - AI aesthetic flags (generic-gradient, etc.) -- still evaluated
  - All other calibration table rows -- still evaluated
```

Then, after Step 4e (AI Aesthetic Pattern Detection Pass) or Step 4f (Motion Choreography Assessment), add a visual observation requirement section:

```markdown
  #### Step 4g: Visual observations from screenshot (Stitch artifacts with HAS_PNG = true)

  IF HAS_PNG is true for the current artifact:
    After evaluating all four perspectives using HTML source, review the screenshot image loaded by the Read tool.

    For EACH perspective, include at least one observation that references what is VISIBLE IN THE IMAGE (not derivable from HTML source alone). These observations demonstrate multimodal analysis beyond HTML structure.

    Examples of valid image-derived observations:
    - Perspective 1 (UX): "The screenshot reveals that the form submit button appears visually buried -- the visual weight is similar to secondary actions despite the HTML marking it as primary"
    - Perspective 2 (Visual Hierarchy): "In the screenshot, the card grid lacks visual separation -- the box-shadow values in HTML produce imperceptible elevation at screen resolution"
    - Perspective 3 (Accessibility): "Screenshot shows the checkbox controls use color alone (no visible border on light background) to convey checked state"
    - Perspective 4 (Business Alignment): "The screenshot shows the pricing CTA competes visually with the support link -- both appear equal weight"

    Tag each image-derived observation with: `[visual: from screenshot]`

  IF HAS_PNG is false: Skip this section. All observations come from HTML source only (standard behavior).
```

**Critical anti-patterns to avoid:**
- Do NOT suppress color contrast findings -- only "Token not applied" is suppressed
- Do NOT make PNG reading blocking -- if Read fails, HAS_PNG = false and evaluation continues
- Do NOT modify the fidelity-severity calibration TABLE itself -- the table stays unchanged; suppression is applied in the evaluation logic ABOVE the table
  </action>
  <verify>
    <automated>grep -c "SUPPRESS_TOKEN_FINDINGS" workflows/critique.md | grep -q "[1-9]" && grep -c "HAS_PNG" workflows/critique.md | grep -q "[1-9]" && grep -c "STITCH_SOURCE" workflows/critique.md | grep -q "[1-9]" && grep -c "visual: from screenshot" workflows/critique.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/critique.md contains `SUPPRESS_TOKEN_FINDINGS` (at least 3 occurrences: set true, set false, conditional check)
    - workflows/critique.md contains `STITCH_SOURCE` (at least 2 occurrences: set true, set false)
    - workflows/critique.md contains `HAS_PNG` (at least 3 occurrences: set true, set false, conditional check)
    - workflows/critique.md contains `Token not applied` AND `SUPPRESS` on distinct lines showing the suppression gate
    - workflows/critique.md contains `.png` path construction from `.html` path for STH artifacts
    - workflows/critique.md contains `[visual: from screenshot]` tag for image-derived observations
    - workflows/critique.md contains `Step 4g` or equivalent visual observation section
    - The fidelity-severity calibration TABLE (the markdown table itself) is UNCHANGED
    - Color contrast row in calibration table is UNCHANGED (still Major/Critical at hifi)
  </acceptance_criteria>
  <done>Step 4 has Stitch source gate with SUPPRESS_TOKEN_FINDINGS for "Token not applied" only, PNG reading with HAS_PNG fallback, and visual observation requirements for multimodal analysis</done>
</task>

<task type="auto">
  <name>Task 3: Add Step 5b-STITCH Comparison section and Action List exclusion note</name>
  <files>workflows/critique.md</files>
  <read_first>
    - workflows/critique.md (re-read after Task 2 modifications)
    - .planning/phases/68-critique-stitch-comparison/68-RESEARCH.md (Architecture Patterns 4 and 5, Anti-Patterns section on Action List and DESIGN-STATE)
  </read_first>
  <action>
Insert two blocks into critique.md:

**Block A — Step 5b-STITCH section (insert AFTER section 8 Footer in Step 5b, BEFORE the `Use the Write tool to write to` line):**

```markdown
9. `## Stitch Comparison` (conditional — only when STITCH_ARTIFACTS is non-empty)

   IF STITCH_ARTIFACTS is empty: Skip this section entirely. The report ends after the Footer. No `## Stitch Comparison` heading appears.

   IF STITCH_ARTIFACTS is non-empty:

   Find design system tokens for comparison baseline:
   - Use Glob to find `.planning/design/visual/SYS-tokens-v*.css` -- sort descending by version number, read highest version using Read tool
   - If not found: use Glob to find `assets/tokens.json` -- read if present
   - If neither found: note "Design system tokens not available for comparison. Run /pde:system first." in section header

   Write the following section:

   ```markdown
   ## Stitch Comparison

   > This section compares Stitch-generated artifacts against the project design system.
   > Divergences are reported as recommendations, not scored findings.
   > The composite score above reflects the standard 4-perspective evaluation only.
   ```

   For each artifact code in STITCH_ARTIFACTS:

   Read the STH HTML source (already loaded in Step 4). Parse to extract:
   - All hardcoded color values (regex: `/#[0-9a-fA-F]{3,8}/g`)
   - All spacing values (regex: `/:\s*\d+px/g`)
   - Border-radius values
   - Font-family declarations

   Write per-artifact delta report:

   ```markdown
   ### STH-{slug} Delta Report

   **Source:** Google Stitch (hardcoded hex values, no OKLCH custom properties)
   **Design System Baseline:** {SYS-tokens-v{N}.css path or "not available"}

   #### Token Compliance

   | Category | Stitch Value Found | Design System Token | Compliance |
   |----------|--------------------|---------------------|-----------|
   | Primary color | `{hex from HTML}` | `--color-primary-500: oklch(...)` | {compliant or non-compliant} |
   | Background | `{hex from HTML}` | `--color-bg-default: oklch(...)` | {compliant or non-compliant} |
   | Border radius | `{px value}` | `--radius-md: {value}` | {compliant or non-compliant} |
   | Font family | `{font-family value}` | `--font-family-body: {value}` | {compliant or non-compliant} |
   | Shadow | `{shadow value}` | `--shadow-md: {value}` | {compliant or non-compliant} |

   **Token compliance:** {N}% ({compliant_count}/{total_checked} token categories matched)

   #### Properties Deviating from Design System

   List each property where Stitch value diverges from the design system token:
   - `{property}: {stitch_value}` -- use `var({token_name})` (`{token_value}`) instead
   (Focus on the 5 most impactful categories: primary color, background, border radius, font family, shadow. Additional properties noted if they stand out.)

   #### Novel Patterns in Stitch Output

   List patterns present in the Stitch output that are NOT in the design system:
   - {Pattern}: {description and whether it should be adopted}

   If no novel patterns found: "No novel patterns detected. Stitch output aligns with existing design system conventions."

   #### Patterns in Design System Absent from Stitch Output

   List design system tokens that appear UNUSED in the Stitch output:
   - {token category}: not referenced in Stitch output

   If design system tokens not available: Skip this sub-section. Note: "Run /pde:system first to generate design system tokens for comparison."

   #### Recommendations

   {1-3 actionable recommendations for reconciling Stitch output with the design system}
   ```

Also update the frontmatter Mode field when STITCH_ARTIFACTS is non-empty. In the frontmatter section of Step 5b, change Mode to:
```yaml
Mode: "{full|quick|focused} (Stitch-aware: {N} Stitch artifact(s))"
```
when STITCH_ARTIFACTS is non-empty. When empty, Mode stays unchanged.

**Block B — Action List exclusion note (modify existing section 6 in Step 5b):**

Find section `6. '## Action List for /pde:iterate'` in Step 5b. After the existing content (the checkbox list format), add:

```markdown
   Only findings from the 4-perspective evaluation appear here. Stitch Comparison recommendations are NOT included in the Action List. Stitch token divergences are reconciled by /pde:handoff (Phase 69), not /pde:iterate.
```

**Block C — DESIGN-STATE exclusion note (modify Step 6 description):**

In Step 6/7 where it says "For each Critical and Major finding from the critique report:", add a clarifying note:

```markdown
   Only Critical and Major findings from the 4-perspective evaluation are added to Open Critique Items. Stitch Comparison recommendations are NOT added to DESIGN-STATE Open Critique Items -- they are informational for /pde:handoff, not actionable by /pde:iterate.
```

**Anti-patterns to avoid:**
- Do NOT write `## Stitch Comparison` when STITCH_ARTIFACTS is empty
- Do NOT include Stitch Comparison recommendations in the Action List
- Do NOT include Stitch Comparison recommendations in DESIGN-STATE Open Critique Items
- Do NOT affect the composite score with token compliance percentage
- Do NOT add a "Stitch Comparison" entry to the "Groups Evaluated" frontmatter field
  </action>
  <verify>
    <automated>grep -c "## Stitch Comparison" workflows/critique.md | grep -q "[1-9]" && grep -c "recommendations" workflows/critique.md | grep -q "[1-9]" && grep "Stitch Comparison recommendations are NOT included" workflows/critique.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/critique.md contains `## Stitch Comparison` heading
    - workflows/critique.md contains `Token compliance` subsection within Stitch Comparison
    - workflows/critique.md contains `Properties Deviating from Design System` subsection
    - workflows/critique.md contains `Novel Patterns in Stitch Output` subsection
    - workflows/critique.md contains `Patterns in Design System Absent from Stitch Output` subsection
    - workflows/critique.md contains `Recommendations` subsection within Stitch Comparison
    - workflows/critique.md contains `Stitch Comparison recommendations are NOT included in the Action List`
    - workflows/critique.md contains `Stitch Comparison recommendations are NOT added to DESIGN-STATE Open Critique Items`
    - The `## Stitch Comparison` section appears AFTER the Footer (section 8) in Step 5b
    - The `## Stitch Comparison` section is gated: `IF STITCH_ARTIFACTS is empty: Skip this section entirely`
    - The Stitch Comparison section uses "recommendations" not "findings"
    - Mode frontmatter includes `Stitch-aware` when STITCH_ARTIFACTS non-empty
    - The composite score calculation is UNCHANGED (still based on 4-perspective evaluation only)
  </acceptance_criteria>
  <done>Step 5b-STITCH appends a conditional ## Stitch Comparison section with per-screen delta report, and both Action List and DESIGN-STATE explicitly exclude Stitch Comparison recommendations</done>
</task>

</tasks>

<verification>
After all three tasks complete, run these checks:

1. `grep -c "STITCH_ARTIFACTS" workflows/critique.md` -- should return 6+ occurrences
2. `grep -c "SUPPRESS_TOKEN_FINDINGS" workflows/critique.md` -- should return 3+ occurrences
3. `grep -c "HAS_PNG" workflows/critique.md` -- should return 3+ occurrences
4. `grep -c "## Stitch Comparison" workflows/critique.md` -- should return 1+ occurrence
5. `grep -c "manifest-read" workflows/critique.md` -- should return 1+ occurrence
6. `grep "Stitch Comparison recommendations are NOT included" workflows/critique.md` -- should match
7. Verify the fidelity-severity calibration table is unchanged: `grep "Token not applied | Skip | Minor | Major" workflows/critique.md` -- should still match
8. Verify Steps 1, 3, 3.5, 6 core logic, and 7 are not structurally broken by reading the full file
</verification>

<success_criteria>
- workflows/critique.md has three new insertion points (Step 2g, Step 4 Stitch gate, Step 5b-STITCH) that implement CRT-01 through CRT-04
- The existing 4-perspective critique, scoring, Action List, and DESIGN-STATE update are unchanged in structure
- Token suppression is scoped to "Token not applied" only -- all other finding types evaluated normally
- PNG reading is non-blocking with graceful HAS_PNG fallback
- Stitch Comparison section is conditional on STITCH_ARTIFACTS being non-empty
</success_criteria>

<output>
After completion, create `.planning/phases/68-critique-stitch-comparison/68-01-SUMMARY.md`
</output>
