---
phase: 04-workflow-engine
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - bin/lib/commands.cjs
autonomous: true
requirements:
  - WORK-01
  - WORK-06

must_haves:
  truths:
    - "Atomic git commits are created per completed task with correct format: {type}({phase}-{plan}): {description}"
    - "Commits include Co-Authored-By trailer for PDE attribution when --co-author flag is used"
    - "The full discuss -> plan -> execute -> verify cycle operates without state loss"
  artifacts:
    - path: "bin/lib/commands.cjs"
      provides: "cmdCommit with --co-author support"
      contains: "Co-Authored-By"
  key_links:
    - from: "execute-plan.md workflow"
      to: "cmdCommit in commands.cjs"
      via: "pde-tools commit CLI call"
      pattern: "pde-tools.*commit"
    - from: "cmdCommit"
      to: "git commit -m"
      via: "execGit with message including Co-Authored-By trailer"
      pattern: "Co-Authored-By"
---

<objective>
Fix the Co-Authored-By attribution gap in cmdCommit and verify atomic git commit creation per task.

Purpose: Close the WORK-06 gap (cmdCommit does not add Co-Authored-By trailers) identified in research, and verify the full atomic commit protocol. Also provide final WORK-01 confirmation by reviewing the end-to-end workflow cycle state.
Output: Updated commands.cjs with co-author support, verified git commit format, and WORK-01 end-to-end confirmation.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-workflow-engine/04-RESEARCH.md
@.planning/phases/04-workflow-engine/04-01-SUMMARY.md
@.planning/phases/04-workflow-engine/04-02-SUMMARY.md

<interfaces>
From bin/lib/commands.cjs (cmdCommit, lines 216-262):
```javascript
function cmdCommit(cwd, message, files, raw, amend) {
  // Stages files (defaults to .planning/ if none specified)
  // Calls git commit -m message
  // Returns { committed: true/false, hash, reason }
  // Does NOT add Co-Authored-By trailer — this is the WORK-06 gap
}
```

From references/git-integration.md (task-completion format):
```
{type}({phase}-{plan}): {task-name}

- [Key change 1]
- [Key change 2]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Co-Authored-By support to cmdCommit</name>
  <files>bin/lib/commands.cjs</files>
  <action>
Add a `--co-author` flag to `cmdCommit` that appends a `Co-Authored-By:` trailer to the commit message. This closes the WORK-06 gap identified in research.

Implementation:
1. Read `bin/lib/commands.cjs` fully
2. Modify the `cmdCommit` function signature to accept a `coAuthor` parameter (6th argument after `amend`)
3. Before the `git commit -m` call, if `coAuthor` is truthy, append `\n\nCo-Authored-By: ${coAuthor}` to the message string
4. Update the CLI argument parsing in `pde-tools.cjs` to pass `--co-author` value to cmdCommit. Find where `commit` command is routed and add the flag extraction.
5. Update `module.exports` if needed

The default behavior (no --co-author flag) remains unchanged — existing callers are unaffected. The executing Claude session adds the trailer via this flag when creating task commits per the execute-plan.md protocol.

Test the change:
- Create a temporary test file, stage it, and commit with: `node bin/pde-tools.cjs commit "test(04-03): verify co-author flag" --files /tmp/pde-test-file.txt --co-author "Claude <noreply@anthropic.com>"`
- Check `git log -1 --format="%B"` and verify the Co-Authored-By trailer appears
- Revert the test commit with `git reset --soft HEAD~1` and unstage the test file
- Clean up any test artifacts

NOTE: Keep the change minimal. Do NOT refactor cmdCommit or change any other behavior. Only add the co-author trailer injection.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep -q "Co-Authored-By\|coAuthor\|co.author" bin/lib/commands.cjs && echo "PASS: co-author support added to cmdCommit" || echo "FAIL: co-author support missing"</automated>
  </verify>
  <done>
- cmdCommit accepts --co-author flag
- When flag is provided, commit message includes Co-Authored-By trailer
- When flag is omitted, behavior is unchanged (backward compatible)
- Test commit verified trailer appears in git log output
- WORK-06 gap closed
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify atomic commit format and end-to-end workflow confirmation</name>
  <files>.planning/STATE.md</files>
  <action>
Step 1 — Verify atomic commit format (WORK-06):
- Run `git log --oneline -20` to see recent commit history
- Verify commits from Phases 1-3 follow the format: `{type}({phase}-{plan}): {description}`
- Check that task commits (feat, fix, chore) and metadata commits (docs) both appear
- Run `git log --format="%B" -5` to verify full commit message format with bullet points

Step 2 — Verify commit_docs gating:
- Run `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" state load` and check the commit_docs value is true
- This confirms commits will actually be created during Phase 4 execution (not silently skipped per Pitfall 5)

Step 3 — End-to-end workflow confirmation (WORK-01):
- Review STATE.md to confirm it reflects the discuss -> plan -> execute -> verify cycle:
  - Phase 4 has RESEARCH.md (research step completed)
  - Phase 4 has PLAN.md files (plan step in progress)
  - Plans 04-01 and 04-02 have SUMMARY.md (execute step completed for those plans)
  - STATE.md shows current progress accurately
- Run `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" roadmap analyze` and verify Phase 4 status matches reality
- Run `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" state json` and verify current_phase, status, and progress fields are accurate
- This provides the final WORK-01 confirmation: the phase-based workflow operates end-to-end without state loss

Step 4 — Mark remaining WORK requirements complete:
- Run `node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" requirements mark-complete WORK-02,WORK-03,WORK-04,WORK-05,WORK-06`
- Verify all WORK-01 through WORK-06 show as Complete in REQUIREMENTS.md
- NOTE: WORK-01 should be marked last since it represents the full cycle — but since this IS the final plan, mark it here: `requirements mark-complete WORK-01`
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep "WORK-06" .planning/REQUIREMENTS.md | grep -q "Complete\|x\]" && echo "PASS: WORK-06 marked complete" || echo "FAIL: WORK-06 still pending"</automated>
  </verify>
  <done>
- Git log shows atomic per-task commits in correct format from prior phases
- commit_docs is true — commits will not be silently skipped
- End-to-end workflow state confirmed: research -> plan -> execute -> verify cycle tracked in STATE.md
- All WORK-01 through WORK-06 marked Complete in REQUIREMENTS.md traceability table
- WORK-01 and WORK-06 satisfied
  </done>
</task>

</tasks>

<verification>
- WORK-01: Full discuss -> plan -> execute -> verify cycle confirmed operating without state loss across context resets
- WORK-06: Atomic git commits with Co-Authored-By attribution — cmdCommit updated with --co-author flag, commit format verified against git log
</verification>

<success_criteria>
- cmdCommit in commands.cjs accepts --co-author flag and appends trailer to commit message
- Git log shows correct commit format from prior phases
- STATE.md, ROADMAP.md, and REQUIREMENTS.md all reflect accurate Phase 4 state
- All 6 WORK requirements marked Complete in traceability table
</success_criteria>

<output>
After completion, create `.planning/phases/04-workflow-engine/04-03-SUMMARY.md`
</output>
