---
phase: 84-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/design-manifest.json
  - bin/lib/design.cjs
  - .planning/phases/84-foundation/tests/test-foundation.cjs
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "design-manifest.json template contains businessMode and businessTrack top-level fields"
    - "design-manifest.json template contains all 20 designCoverage fields (16 existing + 4 new)"
    - "ensureDesignDirs creates a launch/ subdirectory under .planning/design/"
    - "Existing 16 designCoverage fields are in their original order — new fields appended after hasProductionBible"
  artifacts:
    - path: "templates/design-manifest.json"
      provides: "Manifest template with businessMode, businessTrack, and 20-field designCoverage"
      contains: "businessMode"
    - path: "bin/lib/design.cjs"
      provides: "DOMAIN_DIRS with launch/ entry"
      contains: "'launch'"
    - path: ".planning/phases/84-foundation/tests/test-foundation.cjs"
      provides: "Structural validation tests for all 7 FOUND requirements"
      min_lines: 50
  key_links:
    - from: "templates/design-manifest.json"
      to: "bin/lib/design.cjs"
      via: "ensureDesignDirs reads template and creates directories from DOMAIN_DIRS"
      pattern: "DOMAIN_DIRS.*launch"
---

<objective>
Extend the PDE manifest schema with business mode fields and add launch/ directory support.

Purpose: Every downstream business-mode workflow depends on these fields existing in the manifest template and the launch/ directory being created. This must land before any workflow is authored.
Output: Updated design-manifest.json template, updated design.cjs with launch/ dir, and test scaffold for all Phase 84 requirements.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-foundation/84-RESEARCH.md
@.planning/phases/84-foundation/84-VALIDATION.md

<interfaces>
<!-- Current manifest template structure — executor must preserve all existing fields exactly -->

From templates/design-manifest.json (lines 7-9, insertion point):
```json
"experienceSubType": "single-night | multi-day | recurring-series | installation | hybrid-event | null",
"outputRoot": "string -- path to output directory (e.g., ./pde-output or .planning/design)",
```

From templates/design-manifest.json (lines 108-126, designCoverage block):
```json
"designCoverage": {
  "_comment": "Boolean flags indicating which design artifact types exist...",
  "hasDesignSystem": false,
  "hasWireframes": false,
  "hasFlows": false,
  "hasHardwareSpec": false,
  "hasCritique": false,
  "hasIterate": false,
  "hasHandoff": false,
  "hasIdeation": false,
  "hasCompetitive": false,
  "hasOpportunity": false,
  "hasMockup": false,
  "hasHigAudit": false,
  "hasRecommendations": false,
  "hasStitchWireframes": false,
  "hasPrintCollateral": false,
  "hasProductionBible": false
}
```

From bin/lib/design.cjs (line 17):
```javascript
const DOMAIN_DIRS = ['assets', 'strategy', 'ux', 'ux/mockups', 'visual', 'review', 'handoff', 'hardware', 'physical'];
```

From bin/lib/design.cjs (line 388, self-test comment):
```javascript
check('creates all 9 domain subdirectories (including ux/mockups, physical)', () => {
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffold for all Phase 84 requirements (Wave 0)</name>
  <files>.planning/phases/84-foundation/tests/test-foundation.cjs</files>
  <read_first>
    - .planning/phases/84-foundation/84-VALIDATION.md
    - templates/design-manifest.json
    - bin/lib/design.cjs
  </read_first>
  <action>
Create `.planning/phases/84-foundation/tests/test-foundation.cjs` using `node:test` (built-in, zero npm).

The test file must contain 7 structural assertions — one per FOUND requirement:

1. **FOUND-01 test:** Read `templates/design-manifest.json`, parse it (strip `_comment` keys if needed or just read as text), assert that both `"businessMode": false` and `"businessTrack"` strings appear in the file content. Also assert `businessMode` appears AFTER `experienceSubType` and BEFORE `outputRoot`.

2. **FOUND-02 test:** Read `templates/design-manifest.json`, assert that all 20 designCoverage field names appear: the 16 existing (`hasDesignSystem` through `hasProductionBible`) plus 4 new (`hasBusinessThesis`, `hasMarketLandscape`, `hasServiceBlueprint`, `hasLaunchKit`). Assert the 4 new fields appear AFTER `hasProductionBible` in the file.

3. **FOUND-03 test:** Read `bin/lib/design.cjs`, assert the string `'launch'` appears in the DOMAIN_DIRS array definition. Use a regex pattern like `/DOMAIN_DIRS\s*=\s*\[.*'launch'.*\]/s` to verify it is inside the array, not just anywhere in the file.

4. **FOUND-04 test:** Assert `references/business-track.md` exists via `fs.existsSync`. Assert the file contains the strings `solo_founder`, `startup_team`, and `product_leader`.

5. **FOUND-05 test:** Assert `references/launch-frameworks.md` exists. Assert it contains `Lean Canvas`, `Pitch Deck`, `Service Blueprint`, and `Pricing Config`.

6. **FOUND-06 test:** Assert `references/business-financial-disclaimer.md` exists. Assert it contains `[YOUR_` (placeholder pattern). Assert it does NOT contain `$` followed by a digit (no dollar amounts). Assert it contains `## Prohibited Patterns`.

7. **FOUND-07 test:** Assert `references/business-legal-disclaimer.md` exists. Assert it contains `checklist` (case-insensitive). Assert it contains `## Prohibited Patterns`. Assert it does NOT contain the phrase `Terms of Service` or `Privacy Policy` as generated content (only as prohibited patterns).

Use `node:test` `describe`/`it` blocks. Use `node:assert/strict`. Use `node:fs` for file reads. All paths should be resolved relative to the project root (use `path.resolve(__dirname, '..', '..', '..', '..')` to get project root from `.planning/phases/84-foundation/tests/`).

The test should exit with code 0 when all tests pass and non-zero on failure.
  </action>
  <verify>
    <automated>node .planning/phases/84-foundation/tests/test-foundation.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/84-foundation/tests/test-foundation.cjs exists and is >50 lines
    - File contains `require('node:test')` and `require('node:assert/strict')`
    - File contains 7 test blocks (one per FOUND-XX requirement)
    - Running the test currently FAILS (RED state — manifest and references not yet updated)
  </acceptance_criteria>
  <done>Test scaffold exists with 7 structural assertions covering all FOUND requirements. Tests fail because changes have not been made yet (RED state confirms tests are real, not vacuous).</done>
</task>

<task type="auto">
  <name>Task 2: Extend manifest schema with businessMode, businessTrack, and 4 new designCoverage fields</name>
  <files>templates/design-manifest.json</files>
  <read_first>
    - templates/design-manifest.json
    - .planning/phases/84-foundation/84-RESEARCH.md
  </read_first>
  <action>
Edit `templates/design-manifest.json` to make two changes:

**Change 1 — Add top-level fields (after line 8, between `experienceSubType` and `outputRoot`):**

Insert these two new lines:
```json
"businessMode": false,
"businessTrack": "solo_founder | startup_team | product_leader | null",
```

The resulting field order for lines 7-11 must be:
```json
"productType": "software | hardware | hybrid | experience",
"experienceSubType": "single-night | multi-day | recurring-series | installation | hybrid-event | null",
"businessMode": false,
"businessTrack": "solo_founder | startup_team | product_leader | null",
"outputRoot": "string -- path to output directory (e.g., ./pde-output or .planning/design)",
```

CRITICAL: `businessMode` is a boolean `false`, NOT a string. `businessTrack` is a string comment showing valid values (same pattern as `experienceSubType`). Do NOT add `"business"` to the `productType` enum — `businessMode` is an independent orthogonal axis.

**Change 2 — Append 4 new designCoverage fields (after `hasProductionBible`, before closing `}`):**

Add these 4 fields after `"hasProductionBible": false`:
```json
"hasBusinessThesis": false,
"hasMarketLandscape": false,
"hasServiceBlueprint": false,
"hasLaunchKit": false
```

The final designCoverage block must have exactly 20 boolean fields (16 existing + 4 new). The existing 16 fields MUST remain in their exact current order. The 4 new fields are appended at the bottom, after `hasProductionBible`.

Ensure valid JSON after editing — no trailing commas, proper comma after `"hasProductionBible": false,` (it needs a comma now since it is no longer the last field).
  </action>
  <verify>
    <automated>node -e "const m=require('./templates/design-manifest.json'); const keys=Object.keys(m.designCoverage).filter(k=>k!=='_comment'); console.log('Fields:', keys.length); console.log('businessMode:', m.businessMode); console.log('businessTrack type:', typeof m.businessTrack); console.log('Last 4:', keys.slice(-4).join(', '))"</automated>
  </verify>
  <acceptance_criteria>
    - templates/design-manifest.json is valid JSON (node -e "require('./templates/design-manifest.json')" exits 0)
    - File contains `"businessMode": false` (grep confirms exact string)
    - File contains `"businessTrack":` (grep confirms field exists)
    - designCoverage has exactly 20 non-comment keys
    - Last 4 designCoverage keys are hasBusinessThesis, hasMarketLandscape, hasServiceBlueprint, hasLaunchKit (in that order)
    - productType field does NOT contain the word "business"
    - businessMode appears after experienceSubType and before outputRoot in the file
  </acceptance_criteria>
  <done>Manifest template has businessMode boolean, businessTrack string, and 20 designCoverage fields with 4 new business flags appended after hasProductionBible.</done>
</task>

<task type="auto">
  <name>Task 3: Add launch/ to DOMAIN_DIRS in design.cjs and update self-test comment</name>
  <files>bin/lib/design.cjs</files>
  <read_first>
    - bin/lib/design.cjs
  </read_first>
  <action>
Make two edits to `bin/lib/design.cjs`:

**Edit 1 — Line 17: Add 'launch' to DOMAIN_DIRS array:**

Change:
```javascript
const DOMAIN_DIRS = ['assets', 'strategy', 'ux', 'ux/mockups', 'visual', 'review', 'handoff', 'hardware', 'physical'];
```
To:
```javascript
const DOMAIN_DIRS = ['assets', 'strategy', 'ux', 'ux/mockups', 'visual', 'review', 'handoff', 'hardware', 'physical', 'launch'];
```

`'launch'` is appended as the last element. Do not reorder existing elements.

**Edit 2 — Line 388: Update self-test comment:**

Change:
```javascript
check('creates all 9 domain subdirectories (including ux/mockups, physical)', () => {
```
To:
```javascript
check('creates all 10 domain subdirectories (including ux/mockups, physical, launch)', () => {
```

The test body (`for (const domain of DOMAIN_DIRS)`) is dynamic and does not need changes — it already iterates over the array.

After both edits, run the design.cjs self-test to confirm the launch/ directory is created:
```bash
node bin/lib/design.cjs self-test
```
  </action>
  <verify>
    <automated>node bin/lib/design.cjs self-test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/design.cjs line 17 DOMAIN_DIRS array contains 'launch' as the last element
    - bin/lib/design.cjs self-test comment on line 388 says "10 domain subdirectories" and includes "launch"
    - `node bin/lib/design.cjs self-test` exits with code 0
    - grep "'launch'" bin/lib/design.cjs returns a match on the DOMAIN_DIRS line
  </acceptance_criteria>
  <done>design.cjs creates launch/ subdirectory under .planning/design/ on project initialization. Self-test passes with 10 domain directories.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. `node -e "require('./templates/design-manifest.json')"` — exits 0 (valid JSON)
2. `node bin/lib/design.cjs self-test` — exits 0 (launch/ dir created)
3. `node .planning/phases/84-foundation/tests/test-foundation.cjs` — FOUND-01, FOUND-02, FOUND-03 tests pass (FOUND-04 through FOUND-07 will still fail until Plan 02 completes)
</verification>

<success_criteria>
- design-manifest.json template has businessMode (boolean false) and businessTrack (string) between experienceSubType and outputRoot
- designCoverage has exactly 20 fields with 4 new fields appended after hasProductionBible
- DOMAIN_DIRS includes 'launch' and self-test passes
- Test scaffold exists and validates all 7 FOUND requirements structurally
</success_criteria>

<output>
After completion, create `.planning/phases/84-foundation/84-01-SUMMARY.md`
</output>
