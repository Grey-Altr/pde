---
phase: 28-build-orchestrator-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/build.md
  - commands/build.md
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
must_haves:
  truths:
    - "/pde:build --dry-run displays exactly 13 stages in correct order: recommend, competitive, opportunity, ideate, brief, system, flows, wireframe, critique, iterate, mockup, hig, handoff"
    - "No hardcoded numeric literal 13 or 7 appears in any stage display message — all counts derived from the STAGES list length"
    - "User can pass --from wireframe and stages 1-7 are skipped without error"
    - "Invalid --from value halts with an error listing all valid stage names"
    - "After a full pipeline run all 13 coverage flags in design-manifest.json are true"
    - "HIG stage invokes pde:hig without --light flag"
    - "Ideate stage completion requires BOTH hasIdeation flag AND IDT-ideation-v*.md Glob match"
    - "Brief stage completion uses Glob only (no hasBrief coverage flag)"
  artifacts:
    - path: "workflows/build.md"
      provides: "13-stage pipeline orchestrator with data-driven stage list, --from flag, dynamic counts"
      contains: "recommend.*competitive.*opportunity.*ideate.*brief.*system.*flows.*wireframe.*critique.*iterate.*mockup.*hig.*handoff"
    - path: "commands/build.md"
      provides: "Command stub with updated description and --from flag"
      contains: "--from"
  key_links:
    - from: "workflows/build.md"
      to: "pde-tools.cjs design coverage-check"
      via: "Bash command in Step 2"
      pattern: "coverage-check"
    - from: "workflows/build.md"
      to: "All 13 skill workflows"
      via: "Skill() invocation in Step 3"
      pattern: "Skill.*pde:"
---

<objective>
Expand /pde:build from 7 to 13 stages, wiring all six new v1.2 skills into the pipeline orchestrator with a data-driven stage list, dynamic stage counting (no hardcoded numeric literals), and --from flag for mid-pipeline entry.

Purpose: Complete the v1.2 milestone by making all 13 design skills orchestrable through a single command.
Output: Rewritten workflows/build.md (13-stage orchestrator) and updated commands/build.md (command stub).
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-build-orchestrator-expansion/28-CONTEXT.md
@.planning/phases/28-build-orchestrator-expansion/28-RESEARCH.md

<interfaces>
<!-- Current build.md is the file being rewritten. Executor MUST read it first. -->
<!-- Key integration points from codebase: -->

From commands/build.md (current stub):
```markdown
---
name: pde:build
description: Run the full design pipeline (brief -> system -> flows -> wireframe -> critique -> iterate -> handoff). Resumes from last complete stage.
argument-hint: "[--dry-run] [--quick] [--verbose] [--force]"
allowed-tools:
  - Read
  - Bash
  - Glob
  - AskUserQuestion
  - Skill
---
```

Coverage-check JSON fields (from bin/lib/design.cjs):
```
hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate,
hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit,
hasRecommendations
```

Note: `hasBrief` does NOT exist in designCoverage. Brief uses Glob check only.
Note: `hasIterate` may be absent from JSON — default to false.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite workflows/build.md with 13-stage data-driven orchestrator</name>
  <files>workflows/build.md</files>
  <read_first>
    - workflows/build.md (current 7-stage orchestrator — the file being rewritten; preserve all structural patterns, anti-patterns section, crash recovery, usage examples)
    - .planning/phases/28-build-orchestrator-expansion/28-RESEARCH.md (contains exact stage list, coverage flag mapping, --from parsing pattern, all anti-patterns to avoid)
    - .planning/phases/28-build-orchestrator-expansion/28-CONTEXT.md (locked decisions: stage ordering, --from behavior, flat messaging, coverage mapping, PASSTHROUGH_ARGS scope)
    - references/skill-style-guide.md (lint rules referenced by build.md required_reading)
  </read_first>
  <action>
Rewrite workflows/build.md to expand from 7 to 13 stages. Preserve the overall document structure: purpose, required_reading, flags, process (4 steps), anti_patterns, crash_recovery, usage_examples.

**Stage list definition (at the top of the process block, before Step 1):**

Define a conceptual ordered STAGES table with these exact 13 entries:

| Index | Name         | Skill           | Check Method   | Check Field/Glob                                     |
|-------|-------------|-----------------|----------------|------------------------------------------------------|
| 1     | recommend   | pde:recommend   | coverage       | hasRecommendations                                   |
| 2     | competitive | pde:competitive | coverage       | hasCompetitive                                       |
| 3     | opportunity | pde:opportunity | coverage       | hasOpportunity                                       |
| 4     | ideate      | pde:ideate      | coverage+glob  | hasIdeation + .planning/design/strategy/IDT-ideation-v*.md |
| 5     | brief       | pde:brief       | glob           | .planning/design/strategy/BRF-brief-v*.md            |
| 6     | system      | pde:system      | coverage       | hasDesignSystem                                      |
| 7     | flows       | pde:flows       | coverage       | hasFlows                                             |
| 8     | wireframe   | pde:wireframe   | coverage       | hasWireframes                                        |
| 9     | critique    | pde:critique    | coverage       | hasCritique                                          |
| 10    | iterate     | pde:iterate     | coverage       | hasIterate (default false if absent)                 |
| 11    | mockup      | pde:mockup      | coverage       | hasMockup                                            |
| 12    | hig         | pde:hig         | coverage       | hasHigAudit                                          |
| 13    | handoff     | pde:handoff     | coverage       | hasHandoff                                           |

Define `TOTAL = count(STAGES)` — this is used everywhere instead of any numeric literal.

**purpose block changes:**
- Update first line: "Thin orchestrator that reads designCoverage from the design manifest, determines which of the TOTAL design stages are complete..."
- Update pipeline order to: recommend -> competitive -> opportunity -> ideate -> brief -> system -> flows -> wireframe -> critique -> iterate -> mockup -> hig -> handoff

**flags block changes:**
- Add `--from` flag: `| --from {stage} | Start pipeline at named stage, skipping all preceding stages. Valid names: recommend, competitive, opportunity, ideate, brief, system, flows, wireframe, critique, iterate, mockup, hig, handoff |`
- Keep --dry-run, --quick, --verbose, --force unchanged

**Step 1/4 changes:**
- Add --from argument parsing after existing flag parsing:
  ```
  Parse --from flag from $ARGUMENTS:
  FROM_STAGE=""
  if echo "$ARGUMENTS" | grep -qE '\-\-from\s+\S+'; then
    FROM_STAGE=$(echo "$ARGUMENTS" | grep -oE '\-\-from\s+\S+' | awk '{print $2}')
  fi
  ```
- Add FROM_STAGE validation immediately after parsing:
  ```
  If FROM_STAGE is set:
    Look up FROM_STAGE in the STAGES list name column (case-sensitive match)
    If not found:
      Display error: "Unknown stage '{FROM_STAGE}'. Valid stages: recommend, competitive, opportunity, ideate, brief, system, flows, wireframe, critique, iterate, mockup, hig, handoff"
      HALT (error)
    Set FROM_INDEX = matched stage index (1-based)
  ```
- Do NOT include --from in PASSTHROUGH_ARGS (it is orchestrator-only, like --dry-run)
- Display message: `Step 1/4: Initialized. Mode: {MODE}.` (unchanged)
- If FROM_STAGE is set, also display: `Starting from stage: {FROM_STAGE} (stage {FROM_INDEX}/{TOTAL})`

**Step 2/4 changes:**
- Coverage-check bash command: unchanged
- Parse the JSON result extracting ALL these fields (all boolean, default false if absent):
  - hasRecommendations — Stage 1
  - hasCompetitive — Stage 2
  - hasOpportunity — Stage 3
  - hasIdeation — Stage 4 (partial check, also needs Glob)
  - hasDesignSystem — Stage 6
  - hasFlows — Stage 7
  - hasWireframes — Stage 8
  - hasCritique — Stage 9
  - hasIterate — Stage 10 (default false if absent)
  - hasMockup — Stage 11
  - hasHigAudit — Stage 12
  - hasHandoff — Stage 13
- Brief (Stage 5): Glob on `.planning/design/strategy/BRF-brief-v*.md`
- Ideate (Stage 4): BOTH hasIdeation==true AND Glob on `.planning/design/strategy/IDT-ideation-v*.md` returns at least one file
- Build stage status table with TOTAL stages (use `{N}/{TOTAL}` format, NOT literal `1/13`):

  | Stage     | Skill            | Check                                          | Status                         |
  |-----------|------------------|-------------------------------------------------|-------------------------------|
  | 1/{TOTAL} | /pde:recommend   | hasRecommendations                              | complete / pending / skipped   |
  | 2/{TOTAL} | /pde:competitive | hasCompetitive                                  | complete / pending / skipped   |
  | ...       | ...              | ...                                             | ...                           |
  | {TOTAL}/{TOTAL} | /pde:handoff | hasHandoff                                    | complete / pending / skipped   |

  Where status is:
  - "skipped (--from {FROM_STAGE})" if FROM_INDEX is set and this stage's index < FROM_INDEX
  - "complete" if the stage's check passes
  - "pending" otherwise

- Count completed stages (N) and pending stages (TOTAL - N), excluding skipped-by-from stages from pending count
- If ALL stages are complete (or complete+skipped):
  - Display: `All {TOTAL} stages complete. Pipeline finished.`
  - Display: `Design artifacts are in .planning/design/. Run /pde:handoff --verbose to review the implementation spec.`
  - HALT (success)
- If --dry-run:
  - Display table with status
  - Display: `Dry-run mode -- no stages executed.`
  - HALT
- Display: `Step 2/4: Pipeline state loaded. {N}/{TOTAL} stages complete, {pending_count} pending.`

**Step 3/4 changes:**
- Process each stage from 1 to TOTAL in strict sequential order
- For each stage:
  - If FROM_INDEX is set AND stage index < FROM_INDEX:
    - Display: `Stage {N}/{TOTAL}: /pde:{skill} -- skipped (--from {FROM_STAGE})`
    - Continue to next stage
  - If stage is already complete:
    - Display: `Stage {N}/{TOTAL}: /pde:{skill} -- skipped (complete)`
    - Continue to next stage
  - If stage is pending:
    - Display: `Stage {N}/{TOTAL}: Running /pde:{skill}...`
    - Invoke: `Skill(skill="pde:{skill}", args="${PASSTHROUGH_ARGS}")`
    - NOTE: pde:hig receives NO --light flag. PASSTHROUGH_ARGS contains only --quick/--verbose/--force
    - Display: `Stage {N}/{TOTAL}: /pde:{skill} -- done.`
  - Verification gate (only between stages, not after last stage):
    - If MODE == "interactive":
      - AskUserQuestion: `Stage {N}/{TOTAL} (/pde:{skill}) complete. Continue to Stage {N+1}/{TOTAL} (/pde:{next_skill})?`
      - Options: ["Continue", "Stop here"]
      - If "Stop here": Display pause message and HALT
    - If MODE != "interactive":
      - Display: `[{MODE}] Advancing to Stage {N+1}/{TOTAL}...`

- Skill invocation list (all 13, in order):
  ```
  Stage 1/{TOTAL}: Skill(skill="pde:recommend", args="${PASSTHROUGH_ARGS}")
  Stage 2/{TOTAL}: Skill(skill="pde:competitive", args="${PASSTHROUGH_ARGS}")
  Stage 3/{TOTAL}: Skill(skill="pde:opportunity", args="${PASSTHROUGH_ARGS}")
  Stage 4/{TOTAL}: Skill(skill="pde:ideate", args="${PASSTHROUGH_ARGS}")
  Stage 5/{TOTAL}: Skill(skill="pde:brief", args="${PASSTHROUGH_ARGS}")
  Stage 6/{TOTAL}: Skill(skill="pde:system", args="${PASSTHROUGH_ARGS}")
  Stage 7/{TOTAL}: Skill(skill="pde:flows", args="${PASSTHROUGH_ARGS}")
  Stage 8/{TOTAL}: Skill(skill="pde:wireframe", args="${PASSTHROUGH_ARGS}")
  Stage 9/{TOTAL}: Skill(skill="pde:critique", args="${PASSTHROUGH_ARGS}")
  Stage 10/{TOTAL}: Skill(skill="pde:iterate", args="${PASSTHROUGH_ARGS}")
  Stage 11/{TOTAL}: Skill(skill="pde:mockup", args="${PASSTHROUGH_ARGS}")
  Stage 12/{TOTAL}: Skill(skill="pde:hig", args="${PASSTHROUGH_ARGS}")
  Stage 13/{TOTAL}: Skill(skill="pde:handoff", args="${PASSTHROUGH_ARGS}")
  ```

**Step 4/4 changes:**
- Display: `Step 4/4: Pipeline complete. All {TOTAL} stages finished.`
- Pipeline summary table uses `{N}/{TOTAL}` format for all 13 stages
- Closing message unchanged

**anti_patterns section updates:**
- Keep all 7 existing anti-patterns
- Add anti-pattern 8: `NEVER pass --light to /pde:hig when invoked from the pipeline. HIG runs full standalone mode (not --light). Light mode is only for /pde:critique delegation.`
- Add anti-pattern 9: `NEVER write a bare digit (like 7 or 13) in any stage progress message. Always reference TOTAL (the computed count of the STAGES list). This ensures future pipeline expansions require no message text changes.`
- Add anti-pattern 10: `NEVER forward --from or --dry-run to sub-skills via PASSTHROUGH_ARGS. Both are orchestrator-only flags.`
- Update anti-pattern 6 to say "1 to TOTAL" instead of "1 to 7"

**crash_recovery section:** Update to reference TOTAL instead of specific numbers. Logic is unchanged.

**usage_examples section updates:**
- Update all examples to reference TOTAL stages instead of 7
- Add new example for --from flag:
  ```
  **Start from a specific stage:**
  /pde:build --from wireframe
  Skips stages 1-7 (recommend through flows), begins execution at wireframe. Skipped stages are not checked for completion.
  ```
- Add new example for --from + --dry-run:
  ```
  **Preview pipeline from a specific stage:**
  /pde:build --from wireframe --dry-run
  Shows all stages with skipped/complete/pending status, starting execution point at wireframe. No skills invoked.
  ```

**CRITICAL CONSTRAINTS:**
- Use Skill() for all invocations, NEVER Task() (Issue #686)
- Orchestrator is strictly read-only (no coverage writes, no manifest mutations)
- Coverage read ONCE in Step 2, never re-read mid-pipeline
- PASSTHROUGH_ARGS = --quick, --verbose, --force ONLY (not --dry-run, not --from, not --light, not --yolo/--interactive)
- hasIterate defaults to false if absent from coverage-check JSON
- Brief completion is ALWAYS Glob-only (no hasBrief flag exists)
- Ideate completion requires BOTH flag AND Glob
- Every `{N}/{TOTAL}` in stage messages uses the computed TOTAL, never a literal number
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && grep -c "recommend\|competitive\|opportunity\|ideate\|brief\|system\|flows\|wireframe\|critique\|iterate\|mockup\|hig\|handoff" workflows/build.md && echo "---" && grep -c "\-\-from" workflows/build.md && echo "---" && grep -c "TOTAL" workflows/build.md && echo "---" && grep -cE "Stage [0-9]+/[0-9]+" workflows/build.md</automated>
  </verify>
  <acceptance_criteria>
    - grep for "recommend" in workflows/build.md returns matches (stage 1 present)
    - grep for "hasRecommendations" in workflows/build.md returns matches
    - grep for "hasCompetitive" in workflows/build.md returns matches
    - grep for "hasOpportunity" in workflows/build.md returns matches
    - grep for "hasIdeation" in workflows/build.md returns matches
    - grep for "hasMockup" in workflows/build.md returns matches
    - grep for "hasHigAudit" in workflows/build.md returns matches
    - grep for "--from" in workflows/build.md returns 5+ matches (flag def, parsing, validation, skip logic, usage example)
    - grep for "TOTAL" in workflows/build.md returns 10+ matches (dynamic count used throughout)
    - grep -cE "Stage [0-9]+/[0-9]+" workflows/build.md returns 0 (NO hardcoded stage count ratios like "1/13" or "1/7" in stage display messages)
    - grep for "IDT-ideation-v" in workflows/build.md returns matches (ideate dual check)
    - grep for "BRF-brief-v" in workflows/build.md returns matches (brief Glob check)
    - grep for "--light" does NOT appear in any Stage 12 skill invocation line
    - grep for "Skill.*pde:recommend" returns a match
    - grep for "Skill.*pde:hig" returns a match (without --light)
    - grep for "Skill.*pde:handoff" returns a match (stage 13)
    - The STAGES table lists exactly 13 entries (recommend through handoff)
    - anti_patterns section contains at least 10 anti-patterns
    - usage_examples section includes --from example
  </acceptance_criteria>
  <done>workflows/build.md contains a complete 13-stage orchestrator with data-driven stage list, --from flag parsing with validation, dynamic TOTAL used in all messages, correct coverage checks for all stages (including ideate dual check and brief Glob-only), and zero hardcoded stage count literals in display messages.</done>
</task>

<task type="auto">
  <name>Task 2: Update commands/build.md stub with expanded pipeline description</name>
  <files>commands/build.md</files>
  <read_first>
    - commands/build.md (current stub — must see current description and argument-hint to update correctly)
    - workflows/build.md (just-rewritten file — confirm the --from flag syntax and full stage list to match)
  </read_first>
  <action>
Update commands/build.md with these exact changes:

1. Change the `description` field from:
   `Run the full design pipeline (brief -> system -> flows -> wireframe -> critique -> iterate -> handoff). Resumes from last complete stage.`
   to:
   `Run the full design pipeline (recommend -> competitive -> opportunity -> ideate -> brief -> system -> flows -> wireframe -> critique -> iterate -> mockup -> hig -> handoff). Resumes from last complete stage.`

2. Change the `argument-hint` field from:
   `"[--dry-run] [--quick] [--verbose] [--force]"`
   to:
   `"[--from stage] [--dry-run] [--quick] [--verbose] [--force]"`

3. All other fields (name, allowed-tools, objective, process) remain UNCHANGED.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && grep "description:" commands/build.md && echo "---" && grep "argument-hint:" commands/build.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "description:" commands/build.md contains "recommend" and "handoff" and all 13 stage names
    - grep "argument-hint:" commands/build.md contains "--from stage"
    - grep "name: pde:build" commands/build.md still returns a match (name unchanged)
    - grep "Follow @workflows/build.md exactly" commands/build.md still returns a match (process unchanged)
    - The allowed-tools list still contains Read, Bash, Glob, AskUserQuestion, Skill
  </acceptance_criteria>
  <done>commands/build.md description lists all 13 pipeline stages and argument-hint includes the --from flag.</done>
</task>

</tasks>

<verification>
Structural verification (grep-based, no runtime needed):

1. **BUILD-01 (13 stages in order):** grep the STAGES table in workflows/build.md for all 13 stage names in order: recommend, competitive, opportunity, ideate, brief, system, flows, wireframe, critique, iterate, mockup, hig, handoff
2. **BUILD-02 (no hardcoded counts):** `grep -nE "Stage [0-9]+/[0-9]+" workflows/build.md` must return 0 matches (all stage display messages use {N}/{TOTAL} pattern, not literal digits)
3. **BUILD-03 (--from flag):** grep for "--from" in workflows/build.md returns multiple matches covering: flag definition, argument parsing, validation with error message listing valid stages, skip logic in Step 3
4. **Coverage completeness:** grep for each of the 13 coverage check fields/globs: hasRecommendations, hasCompetitive, hasOpportunity, hasIdeation+IDT-ideation-v*, BRF-brief-v*, hasDesignSystem, hasFlows, hasWireframes, hasCritique, hasIterate, hasMockup, hasHigAudit, hasHandoff
5. **Anti-pattern compliance:** grep confirms no --light in hig invocation, no Task() usage, no hasBrief reference
</verification>

<success_criteria>
- workflows/build.md contains exactly 13 stages in the locked order
- All stage count displays use dynamic TOTAL, zero hardcoded numeric literals
- --from flag parses stage name, validates against STAGES list, skips preceding stages
- Brief uses Glob-only check, ideate uses flag+Glob dual check
- HIG invoked without --light
- commands/build.md description and argument-hint updated
- All existing anti-patterns preserved, 3 new anti-patterns added
</success_criteria>

<output>
After completion, create `.planning/phases/28-build-orchestrator-expansion/28-01-SUMMARY.md`
</output>
