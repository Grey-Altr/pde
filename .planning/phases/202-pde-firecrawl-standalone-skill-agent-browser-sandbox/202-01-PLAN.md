---
phase: 202-pde-firecrawl-standalone-skill-agent-browser-sandbox
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/firecrawl.md
  - workflows/firecrawl.md
autonomous: true
requirements:
  - AGT-01
  - AGT-02

must_haves:
  truths:
    - "Running /pde:firecrawl agent 'query' displays a consent prompt showing estimated credit cost and does NOT proceed without explicit user confirmation"
    - "Every agent dispatch includes a --max-credits cap visible in the consent gate"
    - "Running /pde:firecrawl agent-status <job-id> returns current status and structured JSON results when complete"
  artifacts:
    - path: "workflows/firecrawl.md"
      provides: "agent and agent-status subcommand workflow prose"
      contains: "Subcommand: agent"
    - path: "commands/firecrawl.md"
      provides: "agent and agent-status tool routing"
      contains: "firecrawl_agent"
  key_links:
    - from: "commands/firecrawl.md"
      to: "workflows/firecrawl.md"
      via: "subcommand routing for agent and agent-status"
      pattern: "firecrawl_agent"
    - from: "workflows/firecrawl.md"
      to: "bin/lib/mcp-bridge.cjs"
      via: "probeFirecrawl + acquireFirecrawlSemaphore + incrementFirecrawlUsage"
      pattern: "probeFirecrawl|acquireFirecrawlSemaphore|incrementFirecrawlUsage"
    - from: "workflows/firecrawl.md"
      to: "bin/lib/firecrawl-cache.cjs"
      via: "writeSource for agent result caching"
      pattern: "writeSource"
---

<objective>
Add the `agent` and `agent-status` subcommands to the /pde:firecrawl command. The agent subcommand delegates natural language web research to Firecrawl's autonomous agent with a mandatory consent gate and credit cap. The agent-status subcommand polls job completion and retrieves structured results.

Purpose: Expose Firecrawl's highest-value research capability (autonomous multi-domain research) with appropriate cost guardrails (consent gate + maxCredits cap) that prevent accidental credit burn.

Output: Extended commands/firecrawl.md and workflows/firecrawl.md with agent and agent-status subcommands fully documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/202-pde-firecrawl-standalone-skill-agent-browser-sandbox/202-RESEARCH.md

@commands/firecrawl.md
@workflows/firecrawl.md

<interfaces>
<!-- Existing command file structure (commands/firecrawl.md) -->
<!-- Plan must ADD three MCP tools to allowed-tools and add agent/agent-status/interact to routing -->

From commands/firecrawl.md:
```yaml
allowed-tools:
  - mcp__firecrawl__firecrawl_scrape
  - mcp__firecrawl__firecrawl_search
  - mcp__firecrawl__firecrawl_map
  - mcp__firecrawl__firecrawl_extract
  - mcp__firecrawl__firecrawl_crawl
  - mcp__firecrawl__firecrawl_check_crawl_status
```

From workflows/firecrawl.md — subcommand routing block:
```
- `scrape` -> Subcommand: scrape
- `search` -> Subcommand: search
- `map` -> Subcommand: map
- `extract` -> Subcommand: extract
- `crawl` -> Subcommand: crawl
```

From bin/lib/mcp-bridge.cjs — existing functions used by all subcommands:
```javascript
probeFirecrawl()        // Returns { available, warning, credits: { remaining, total }, reason }
acquireFirecrawlSemaphore()  // Returns { lockPath }
incrementFirecrawlUsage(credits)  // Deducts credits from config.json
```

From bin/lib/firecrawl-cache.cjs:
```javascript
writeSource(slug, content, { type, added_by }, opts)  // Write content to scrapes/ dir
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add agent + agent-status subcommands to firecrawl workflow and command</name>
  <files>commands/firecrawl.md, workflows/firecrawl.md</files>
  <read_first>commands/firecrawl.md, workflows/firecrawl.md, .planning/phases/202-pde-firecrawl-standalone-skill-agent-browser-sandbox/202-RESEARCH.md</read_first>
  <action>
**Part A: Update commands/firecrawl.md**

1. Update the `description` field to include "agent" and "interact":
   `description: Direct Firecrawl MCP tool access — scrape, search, map, extract, crawl, agent, interact`

2. Update `argument-hint` to include agent and interact subcommands:
   `argument-hint: "scrape URL [--force] | search QUERY [--limit N] | map URL | extract URL --schema JSON | crawl URL [--limit N] [--max-depth N] | agent QUERY [--max-credits N] | agent-status JOB_ID | interact URL [--playwright CODE_FILE | --prompt TEXT]"`

3. Add three new MCP tools to `allowed-tools` list (after the existing firecrawl tools):
   - `mcp__firecrawl__firecrawl_agent`
   - `mcp__firecrawl__firecrawl_agent_status`
   - `mcp__firecrawl__firecrawl_interact`

**Part B: Update workflows/firecrawl.md**

1. Update the `<purpose>` block to mention agent and interact capabilities.

2. Add three new routing entries to the subcommand dispatcher at the top of `<process>`:
   - `agent` -> Subcommand: agent
   - `agent-status` -> Subcommand: agent-status
   - `interact` -> Subcommand: interact

3. Add `## Subcommand: agent QUERY [--max-credits N] [--model mini|pro] [--urls URL1,URL2]` section AFTER the crawl subcommand section and BEFORE the "Default: No subcommand or help" section. Follow the exact prose pattern from RESEARCH.md Code Examples section "Agent Subcommand — Consent Gate and Dispatch" (lines 227-285 of RESEARCH.md). This includes 9 steps:
   - Step 1: Parse arguments (QUERY, MAX_CREDITS default 500, MODEL default spark-1-mini, URLS optional)
   - Step 2: Credit guard check (same probeFirecrawl() pattern as other subcommands)
   - Step 3: Consent gate (REQUIRED — display credit cap, model, balance; require explicit "yes" to proceed; halt on anything else)
   - Step 4: Acquire semaphore
   - Step 5: Call mcp__firecrawl__firecrawl_agent with prompt, maxCredits, optional urls and schema
   - Step 6: Poll mcp__firecrawl__firecrawl_agent_status every 15 seconds, 5-minute timeout. Display elapsed time each iteration. On timeout: show job ID for manual re-check and halt. On failed: display error and halt.
   - Step 7: Track credits with incrementFirecrawlUsage(result.creditsUsed || MAX_CREDITS) + release semaphore
   - Step 8: Cache results via writeSource('firecrawl-agent-{JOB_ID}', ..., { type: 'agent', added_by: 'pde:firecrawl agent' })
   - Step 9: Display results (job ID, credits used, cache path, pretty-printed data)

4. Add `## Subcommand: agent-status JOB_ID` section AFTER the agent subcommand. Follow RESEARCH.md Code Examples "agent-status Subcommand" (lines 289-306). This includes 3 steps:
   - Step 1: Parse JOB_ID from arguments; if missing display usage error and halt
   - Step 2: Call mcp__firecrawl__firecrawl_agent_status({ id: JOB_ID })
   - Step 3: Display result based on status (processing/completed/failed/cancelled)

5. Update the "Default: No subcommand or help" section at the bottom to include agent, agent-status, and interact subcommands in the usage display with their costs and examples:
   - `agent QUERY [--max-credits N] [--model mini|pro] [--urls URL1,URL2]` — Cost: variable (up to max-credits cap, default 500)
   - `agent-status JOB_ID` — Cost: free (status check only)
   - `interact URL [--playwright CODE_FILE | --prompt TEXT]` — Cost: 2-7 credits/minute (documented here but implemented in Plan 02)

   Add examples:
   - `/pde:firecrawl agent "research React server components vs Next.js App Router" --max-credits 500`
   - `/pde:firecrawl agent-status agent-job-abc123`
   - `/pde:firecrawl interact https://example.com --prompt "extract the pricing table"`

IMPORTANT: The consent gate in Step 3 of the agent subcommand is the core safety mechanism for AGT-01. The workflow prose MUST explicitly state: "IF user does not respond 'yes': Display 'Agent dispatch cancelled.' and halt immediately. Do NOT proceed to Step 4."

IMPORTANT: maxCredits default is 500 (conservative), not 2500 (Firecrawl default) — per RESEARCH.md Pitfall 2 recommendation.

IMPORTANT: Include a note in the agent subcommand prose that maxCredits may be advisory-only if the MCP tool does not accept the parameter (the consent gate itself is the safety mechanism regardless).
  </action>
  <verify>
    <automated>grep -c "Subcommand: agent" workflows/firecrawl.md | grep -q "2" && grep -c "firecrawl_agent" commands/firecrawl.md | grep -q "2" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - commands/firecrawl.md includes mcp__firecrawl__firecrawl_agent and mcp__firecrawl__firecrawl_agent_status in allowed-tools
    - workflows/firecrawl.md has "Subcommand: agent" section with consent gate that explicitly halts on non-"yes" response
    - workflows/firecrawl.md has "Subcommand: agent-status" section with status display for all 4 states
    - Agent consent gate displays credit cap, model, and current balance
    - --max-credits flag defaults to 500 and is user-overridable
    - Agent polling loop runs every 15s with 5-minute timeout matching crawl pattern
    - Help section updated with agent, agent-status, and interact entries
    - Agent results cached via writeSource with type 'agent'
  </acceptance_criteria>
  <done>The agent and agent-status subcommands are fully documented in workflow prose with consent gate, polling loop, credit tracking, and cache integration. The commands file routes to both new MCP tools.</done>
</task>

</tasks>

<verification>
- `grep "firecrawl_agent" commands/firecrawl.md` shows both agent and agent_status tool entries
- `grep "Consent gate" workflows/firecrawl.md` shows the consent gate section exists
- `grep "Agent dispatch cancelled" workflows/firecrawl.md` confirms the halt-on-deny behavior
- `grep "agent-status" workflows/firecrawl.md` confirms the status subcommand exists
- `grep "writeSource" workflows/firecrawl.md` shows cache integration for agent results
</verification>

<success_criteria>
- The agent subcommand displays a consent prompt with credit cap before dispatching
- The agent subcommand does NOT proceed without explicit "yes" confirmation
- The agent-status subcommand returns job status and results for all 4 states
- Both subcommands follow the same credit-guard + semaphore pattern as existing subcommands
- Help text updated with all new subcommands
</success_criteria>

<output>
After completion, create `.planning/phases/202-pde-firecrawl-standalone-skill-agent-browser-sandbox/202-01-SUMMARY.md`
</output>
