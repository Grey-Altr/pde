---
phase: 202-pde-firecrawl-standalone-skill-agent-browser-sandbox
plan: 02
type: execute
wave: 2
depends_on:
  - 202-01
files_modified:
  - workflows/firecrawl.md
autonomous: true
requirements:
  - AGT-03
  - AGT-04

must_haves:
  truths:
    - "Running /pde:firecrawl interact <url> launches a cloud browser session with documented TTL and auto-terminates on expiry"
    - "User can execute Playwright code inside the browser sandbox via --playwright flag"
    - "User can extract content from auth-gated pages that WebFetch cannot access"
    - "Interact subcommand displays consent gate with session cost estimate before proceeding"
  artifacts:
    - path: "workflows/firecrawl.md"
      provides: "interact subcommand workflow prose"
      contains: "Subcommand: interact"
  key_links:
    - from: "workflows/firecrawl.md"
      to: "mcp__firecrawl__firecrawl_scrape"
      via: "scrape-first to obtain scrapeId before interact call"
      pattern: "firecrawl_scrape.*scrapeId"
    - from: "workflows/firecrawl.md"
      to: "mcp__firecrawl__firecrawl_interact"
      via: "interact call with scrapeId + code or prompt"
      pattern: "firecrawl_interact"
    - from: "workflows/firecrawl.md"
      to: "bin/lib/mcp-bridge.cjs"
      via: "probeFirecrawl + acquireFirecrawlSemaphore + incrementFirecrawlUsage"
      pattern: "probeFirecrawl|incrementFirecrawlUsage"
---

<objective>
Add the `interact` subcommand to the /pde:firecrawl command. This subcommand launches a cloud browser session via Firecrawl's interact API, enabling Playwright code execution and content extraction from authentication-gated pages. The session has a 10-minute TTL with auto-termination.

Purpose: Expose browser sandbox capability for extracting content from pages that require JavaScript rendering or authentication — pages that WebFetch and standard scrape cannot access.

Output: Extended workflows/firecrawl.md with the interact subcommand section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/202-pde-firecrawl-standalone-skill-agent-browser-sandbox/202-RESEARCH.md
@.planning/phases/202-pde-firecrawl-standalone-skill-agent-browser-sandbox/202-01-SUMMARY.md

@workflows/firecrawl.md

<interfaces>
<!-- From RESEARCH.md — interact subcommand pattern -->
<!-- interact requires scrapeId from a prior firecrawl_scrape call -->
<!-- firecrawl_interact parameters: scrapeId, code, prompt, language, timeout -->
<!-- Session TTL: 10 minutes total, 5 minutes idle — auto-terminated -->
<!-- Credit cost: 2 credits/min (code-only) or 7 credits/min (with prompt) -->

From workflows/firecrawl.md (after Plan 01):
```
Subcommand routing now includes:
- agent -> Subcommand: agent
- agent-status -> Subcommand: agent-status
- interact -> Subcommand: interact  (routing added by Plan 01, implementation added by this plan)
```

From bin/lib/mcp-bridge.cjs:
```javascript
probeFirecrawl()        // Returns { available, warning, credits: { remaining, total }, reason }
acquireFirecrawlSemaphore()  // Returns { lockPath }
incrementFirecrawlUsage(credits)  // Deducts credits from config.json
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interact subcommand to firecrawl workflow</name>
  <files>workflows/firecrawl.md</files>
  <read_first>workflows/firecrawl.md, .planning/phases/202-pde-firecrawl-standalone-skill-agent-browser-sandbox/202-RESEARCH.md</read_first>
  <action>
Add `## Subcommand: interact URL [--playwright CODE_FILE | --prompt "NATURAL_LANGUAGE"] [--language node|python|bash]` section to workflows/firecrawl.md. Place it AFTER the agent-status subcommand section and BEFORE the "Default: No subcommand or help" section. Follow the exact prose pattern from RESEARCH.md Code Examples "interact Subcommand" (lines 310-373 of RESEARCH.md). This includes 9 steps:

**Step 1: Parse arguments**
- SET URL = first argument after "interact"
- SET CODE_FILE = path after --playwright (optional)
- SET NATURAL_PROMPT = value after --prompt (optional)
- SET LANGUAGE = value after --language (default: "node")
- IF neither --playwright nor --prompt provided: Display error message explaining one is required, then halt
- Determine RATE: if NATURAL_PROMPT is set, rate = 7 credits/min; if CODE_FILE only, rate = 2 credits/min

**Step 2: Credit guard check**
Same probeFirecrawl() pattern as all other subcommands. If unavailable, display error and halt (no fallback for browser sandbox).

**Step 3: Consent gate (REQUIRED — browser session does NOT proceed without explicit user confirmation)**
Display:
```
Firecrawl Browser Sandbox
URL: {URL}
Session TTL: 10 minutes (auto-terminated)
Idle TTL: 5 minutes (auto-terminated if no activity)
Credit cost: {RATE} credits/minute ({code-only: 2/min; with AI prompt: 7/min})
Estimated max cost (full 10min session): {10 * RATE} credits
Current balance: {result.credits.remaining} credits

Note: This will first scrape the URL (1 credit) then open a browser session.

Proceed? Type "yes" to confirm, anything else to cancel.
```
IF user does not respond "yes": Display "Browser session cancelled." and halt immediately. Do NOT proceed to Step 4.

**Step 4: Acquire semaphore**
Same acquireFirecrawlSemaphore() pattern as other subcommands.

**Step 5: Scrape URL to obtain scrapeId**
Call mcp__firecrawl__firecrawl_scrape({ url: URL, onlyMainContent: false })
- Note: onlyMainContent is FALSE for interact (we need the full page for browser interaction)
- Extract scrapeId from response — check both `response.metadata.scrapeId` and `response.scrapeId` (exact path may vary per MCP response format)
- IF scrapeId not found in response: Display "Error: Could not obtain scrapeId from scrape response. The interact feature may not be available with your current Firecrawl plan." Release semaphore and halt.
- Track 1 credit for the scrape: incrementFirecrawlUsage(1)

**Step 6: Read code if --playwright flag used**
IF CODE_FILE is provided: Read the file content using the Read tool. SET CODE_STRING = file contents.
IF --prompt is provided: SET CODE_STRING = null (prompt will be passed directly)

**Step 7: Call mcp__firecrawl__firecrawl_interact**
```
mcp__firecrawl__firecrawl_interact({
  scrapeId: SCRAPE_ID,
  ...(CODE_STRING && { code: CODE_STRING, language: LANGUAGE }),
  ...(NATURAL_PROMPT && { prompt: NATURAL_PROMPT }),
  timeout: 30
})
```

**Step 8: Track credits + release semaphore**
- Track 2 credits as a floor estimate for the session (conservative; actual billing is by Firecrawl cloud based on session duration)
- incrementFirecrawlUsage(2)
- Release semaphore
- Note in prose: "Actual session cost may be higher depending on session duration. Check Firecrawl dashboard for accurate billing."

**Step 9: Display result**
```
Browser session completed
  URL: {URL}
  Session: {SCRAPE_ID}
  Credits tracked: 3 (1 scrape + 2 session floor)
  Note: Session TTL is 10 minutes. Re-run /pde:firecrawl interact {URL} for a new session.

{extracted content from interact response}
```

CRITICAL ANTI-PATTERNS TO AVOID:
- Do NOT call firecrawl_interact without a scrapeId from a prior scrape — the API requires it
- Do NOT use deprecated firecrawl_browser_create/execute/delete tools — use firecrawl_interact only
- Do NOT skip the consent gate — browser sessions bill at 2-7 credits/minute
- Do NOT set onlyMainContent: true for the scrape step — browser interaction needs the full page
  </action>
  <verify>
    <automated>grep -c "Subcommand: interact" workflows/firecrawl.md | grep -q "1" && grep "scrapeId" workflows/firecrawl.md | head -1 | grep -q "scrapeId" && grep "Browser session cancelled" workflows/firecrawl.md | grep -q "cancelled" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/firecrawl.md has "Subcommand: interact" section with all 9 steps
    - Consent gate displays session TTL, credit rate, estimated max cost, and current balance
    - Consent gate explicitly halts on non-"yes" response with "Browser session cancelled."
    - Step 5 scrapes the URL first to obtain scrapeId before calling interact
    - onlyMainContent is false for the scrape step (full page needed for interaction)
    - Error handling for missing scrapeId in response
    - --playwright flag reads a file and passes code to interact
    - --prompt flag passes natural language to interact
    - --language flag defaults to "node" with python and bash alternatives
    - Credit tracking: 1 for scrape + 2 floor for session
    - Interact call includes timeout: 30
  </acceptance_criteria>
  <done>The interact subcommand is fully documented in workflow prose with scrape-first pattern, consent gate, Playwright code execution via --playwright flag, natural language prompt via --prompt flag, TTL documentation, and credit tracking. Users can extract content from auth-gated pages.</done>
</task>

</tasks>

<verification>
- `grep "Subcommand: interact" workflows/firecrawl.md` shows the interact section exists
- `grep "scrapeId" workflows/firecrawl.md` confirms scrape-first pattern is documented
- `grep "Browser session cancelled" workflows/firecrawl.md` confirms consent gate halt behavior
- `grep "playwright" workflows/firecrawl.md` confirms --playwright flag handling
- `grep "onlyMainContent: false" workflows/firecrawl.md` confirms full-page scrape for interact
</verification>

<success_criteria>
- The interact subcommand scrapes the URL first, extracts scrapeId, then calls firecrawl_interact
- Consent gate prevents session launch without explicit "yes"
- Users can pass Playwright code via --playwright flag or natural language via --prompt
- Session TTL (10 min) and idle TTL (5 min) are documented in consent gate
- Credit tracking is conservative (floor estimate + scrape cost)
- Error handling covers missing scrapeId scenario
</success_criteria>

<output>
After completion, create `.planning/phases/202-pde-firecrawl-standalone-skill-agent-browser-sandbox/202-02-SUMMARY.md`
</output>
