---
phase: 82-integration-validation-and-regression-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-64/manifest-schema.test.mjs
  - tests/phase-64/workflow-pass-through.test.mjs
  - tests/phase-82/regression-matrix.test.mjs
autonomous: true
requirements:
  - SC-1
  - SC-3
  - SC-4
must_haves:
  truths:
    - "All 6 existing test suites pass with zero failures (134/134 green, up from 132/134)"
    - "No new workflow files were added during v0.11 milestone"
    - "All 13 pipeline skill workflow files exist and contain experience branch sites"
    - "Software/hardware/hybrid code paths in critique, hig, handoff, wireframe remain intact"
  artifacts:
    - path: "tests/phase-64/manifest-schema.test.mjs"
      provides: "Updated CANONICAL_FIELDS with 16 fields including hasPrintCollateral and hasProductionBible"
      contains: "hasPrintCollateral"
    - path: "tests/phase-64/workflow-pass-through.test.mjs"
      provides: "Relaxed hasStitchWireframes assertion acknowledging v0.9 activation"
      contains: "v0.9"
    - path: "tests/phase-82/regression-matrix.test.mjs"
      provides: "Cross-type regression matrix, skill registry check, no-new-workflows git assertion, software path preservation"
      contains: "THIRTEEN_PIPELINE_SKILLS"
  key_links:
    - from: "tests/phase-64/manifest-schema.test.mjs"
      to: "templates/design-manifest.json"
      via: "CANONICAL_FIELDS array matching designCoverage keys"
      pattern: "CANONICAL_FIELDS"
    - from: "tests/phase-82/regression-matrix.test.mjs"
      to: "workflows/*.md"
      via: "readFileSync checking experience keyword and software path markers"
      pattern: "THIRTEEN_PIPELINE_SKILLS"
---

<objective>
Fix 2 pre-existing Phase 64 test drift failures and write the cross-type regression matrix test suite confirming zero software/hardware/hybrid regressions from v0.11 experience additions.

Purpose: Restore the full test suite to green (134/134 -> all green) and establish structural assertions for SC-1 (no regressions), SC-3 (no new workflow files), and SC-4 (all 13 skills operational).
Output: 2 updated Phase 64 test files + 1 new regression-matrix.test.mjs
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-integration-validation-and-regression-audit/82-RESEARCH.md
@.planning/phases/82-integration-validation-and-regression-audit/82-VALIDATION.md

<interfaces>
<!-- Existing test patterns the executor must follow exactly -->

From tests/phase-64/manifest-schema.test.mjs:
```javascript
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'fs';
import { resolve, join } from 'path';
import { fileURLToPath } from 'url';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');

const CANONICAL_FIELDS = [
  'hasDesignSystem', 'hasWireframes', 'hasFlows', 'hasHardwareSpec',
  'hasCritique', 'hasIterate', 'hasHandoff', 'hasIdeation',
  'hasCompetitive', 'hasOpportunity', 'hasMockup', 'hasHigAudit',
  'hasRecommendations', 'hasStitchWireframes',
];
// Currently 14 fields — must become 16 with hasPrintCollateral, hasProductionBible
```

From tests/phase-64/workflow-pass-through.test.mjs:
```javascript
// Test at line 86: 'no workflow or JSON file sets hasStitchWireframes to true'
// This test is STALE — v0.9 Phase 66 legitimately activated the flag
// Must be updated to acknowledge v0.9 behavior
```

From tests/phase-74/experience-regression.test.mjs (ROOT/import pattern):
```javascript
const ROOT = resolve(__dirname, '..', '..');
// Same pattern — use this for new phase-82 test files
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Phase 64 manifest-schema.test.mjs — update CANONICAL_FIELDS from 14 to 16</name>
  <files>tests/phase-64/manifest-schema.test.mjs</files>
  <read_first>
    - tests/phase-64/manifest-schema.test.mjs
    - templates/design-manifest.json
  </read_first>
  <action>
Update `tests/phase-64/manifest-schema.test.mjs` with the following specific changes:

1. **Update file header comment** (line 5): Change `14-field canonical designCoverage schema` to `16-field canonical designCoverage schema`

2. **Add 2 fields to CANONICAL_FIELDS array** (after line 33 `'hasStitchWireframes',`):
   - Add `'hasPrintCollateral',      // Added Phase 80/81 — experience print artifacts`
   - Add `'hasProductionBible',      // Added Phase 81 — experience production bible`

3. **Update count assertion** (line 87): Change `14` to `16` in `assert.strictEqual(keys.length, 14, ...)` — update both the expected value and the error message string

4. **Update test name** (line 75): Change `'all 5 JSON manifest files have all 14 canonical designCoverage fields'` to `'all 5 JSON manifest files have all 16 canonical designCoverage fields'`

5. **Update field order test name** (line 93): Change `'canonical 14-field order is maintained'` to `'canonical 16-field order is maintained'`

6. **Do NOT change** the fixture-partial test (line 107) or fixture-rerun test (line 122) — their `CANONICAL_FIELDS.slice(0, 13)` usage remains safe because the 13 original fields occupy positions 0-12 in the updated array.

Run after editing: `node --test tests/phase-64/manifest-schema.test.mjs`
Expected: 6/6 pass (was 5/6 before edit)
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && node --test tests/phase-64/manifest-schema.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - manifest-schema.test.mjs contains `'hasPrintCollateral'` in CANONICAL_FIELDS array
    - manifest-schema.test.mjs contains `'hasProductionBible'` in CANONICAL_FIELDS array
    - manifest-schema.test.mjs contains `keys.length, 16` (not 14) in the count assertion
    - manifest-schema.test.mjs contains `16-field` in file header comment
    - `node --test tests/phase-64/manifest-schema.test.mjs` exits 0 with 6/6 pass
  </acceptance_criteria>
  <done>Phase 64 manifest schema test passes 6/6 with updated 16-field CANONICAL_FIELDS array</done>
</task>

<task type="auto">
  <name>Task 2: Fix Phase 64 workflow-pass-through.test.mjs — relax stale hasStitchWireframes:true assertion</name>
  <files>tests/phase-64/workflow-pass-through.test.mjs</files>
  <read_first>
    - tests/phase-64/workflow-pass-through.test.mjs
    - workflows/wireframe.md
  </read_first>
  <action>
Update `tests/phase-64/workflow-pass-through.test.mjs` with the following specific changes:

1. **Update file header comment** (line 7): Change `No hasStitchWireframes is set to true.` to `hasStitchWireframes activation state reflects v0.9 Phase 66 behavior.`

2. **Replace the failing test** at lines 86-105 (`'no workflow or JSON file sets hasStitchWireframes to true'`). Replace the entire test with:

```javascript
test('hasStitchWireframes activation reflects v0.9 behavior (wireframe.md may set true when Stitch active)', () => {
  // Phase 64 originally asserted hasStitchWireframes was never true.
  // v0.9 Phase 66 legitimately activated the flag in wireframe.md when --use-stitch is passed.
  // The Phase 64 invariant to preserve: all 12 workflow files INCLUDE the field.
  // The activation state (true/false) is controlled by v0.9 wireframe.md logic.
  for (const relPath of WORKFLOW_FILES) {
    const content = readWorkflow(relPath);
    assert.ok(
      content.includes('hasStitchWireframes'),
      `${relPath}: must still include hasStitchWireframes field`
    );
  }
  // JSON manifest files still have false as default — this is correct
  const jsonFiles = [
    'templates/design-manifest.json',
    '.planning/design/design-manifest.json',
    '.planning/pressure-test/fixture-greenfield/design/design-manifest.json',
    '.planning/pressure-test/fixture-partial/design/design-manifest.json',
    '.planning/pressure-test/fixture-rerun/design/design-manifest.json',
  ];
  for (const relPath of jsonFiles) {
    const content = readFileSync(join(ROOT, relPath), 'utf-8');
    const manifest = JSON.parse(content);
    assert.strictEqual(
      manifest.designCoverage.hasStitchWireframes,
      false,
      `${relPath}: JSON manifest default should remain false`
    );
  }
});
```

3. **Do NOT change** the other 5 tests in the file — they all pass and their assertions remain correct.

Run after editing: `node --test tests/phase-64/workflow-pass-through.test.mjs`
Expected: 6/6 pass (was 5/6 before edit)
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && node --test tests/phase-64/workflow-pass-through.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - workflow-pass-through.test.mjs contains `v0.9` in the updated test name or comment
    - workflow-pass-through.test.mjs does NOT contain `'no workflow or JSON file sets hasStitchWireframes to true'` as a test name
    - workflow-pass-through.test.mjs still contains all 12 WORKFLOW_FILES entries
    - workflow-pass-through.test.mjs still contains the `design.cjs self-test` test
    - `node --test tests/phase-64/workflow-pass-through.test.mjs` exits 0 with 6/6 pass
  </acceptance_criteria>
  <done>Phase 64 workflow pass-through test passes 6/6 with relaxed hasStitchWireframes assertion</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write regression-matrix.test.mjs — SC-1 cross-type regression, SC-3 no new workflows, SC-4 skill registry</name>
  <files>tests/phase-82/regression-matrix.test.mjs</files>
  <read_first>
    - tests/phase-74/experience-regression.test.mjs
    - workflows/critique.md
    - workflows/hig.md
    - workflows/handoff.md
    - workflows/wireframe.md
  </read_first>
  <behavior>
    - Test: All 13 pipeline skill workflow files exist at expected paths
    - Test: All 13 pipeline skill workflow files contain the word "experience" (branch site from FNDX-02)
    - Test: critique.md still contains "WCAG" and "POUR" (software critique path preserved)
    - Test: hig.md still contains "WCAG" (software HIG path preserved)
    - Test: handoff.md still contains "TypeScript" and "HND" (software handoff path preserved)
    - Test: wireframe.md still contains "WFR" (software wireframe path preserved)
    - Test: No new workflow files added during v0.11 (git diff --diff-filter=A v0.10..HEAD)
    - Test: Implemented workflows (critique, hig, handoff, wireframe) contain their experience implementation keywords, not just stubs
  </behavior>
  <action>
Create directory `tests/phase-82/` and file `tests/phase-82/regression-matrix.test.mjs`.

Use the standard test boilerplate:
```javascript
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync, existsSync } from 'fs';
import { resolve, join } from 'path';
import { fileURLToPath } from 'url';
import { spawnSync } from 'child_process';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const ROOT = resolve(__dirname, '..', '..');
```

Define constants:
```javascript
const THIRTEEN_PIPELINE_SKILLS = [
  'workflows/recommend.md',
  'workflows/competitive.md',
  'workflows/opportunity.md',
  'workflows/ideate.md',
  'workflows/brief.md',
  'workflows/system.md',
  'workflows/flows.md',
  'workflows/wireframe.md',
  'workflows/critique.md',
  'workflows/iterate.md',
  'workflows/mockup.md',
  'workflows/hig.md',
  'workflows/handoff.md',
];

const EXPERIENCE_IMPLEMENTED = {
  'workflows/critique.md': 'Experience Perspective 1: Safety',
  'workflows/hig.md': 'Physical HIG Domain 1: Wayfinding',
  'workflows/handoff.md': 'Production Bible',
  'workflows/wireframe.md': 'GENERATE_FLY',
};

const SOFTWARE_PATH_MARKERS = {
  'workflows/critique.md': ['WCAG', 'POUR'],
  'workflows/hig.md': ['WCAG'],
  'workflows/handoff.md': ['TypeScript', 'HND'],
  'workflows/wireframe.md': ['WFR'],
};
```

Write 4 describe blocks:

**describe('SC-4: skill registry — all 13 pipeline skills operational'):**
- `test('all 13 pipeline skill files exist')` — loop THIRTEEN_PIPELINE_SKILLS, assert existsSync
- `test('all 13 pipeline skills contain experience branch site')` — loop, readFileSync, assert includes 'experience'
- `test('implemented experience workflows contain their implementation keywords (not stubs)')` — loop EXPERIENCE_IMPLEMENTED, assert content includes the expected keyword

**describe('SC-1: software/hardware/hybrid code paths preserved'):**
- `test('critique.md preserves WCAG and POUR software critique paths')` — assert includes 'WCAG', assert includes 'POUR'
- `test('hig.md preserves WCAG software HIG path')` — assert includes 'WCAG'
- `test('handoff.md preserves TypeScript interface generation and HND artifact code')` — assert includes 'TypeScript', assert includes 'HND'
- `test('wireframe.md preserves WFR software wireframe artifact code')` — assert includes 'WFR'

**describe('SC-3: no new workflow files added during v0.11'):**
- `test('git diff --diff-filter=A v0.10..HEAD shows zero new workflow files')` — use spawnSync('git', ['diff', '--diff-filter=A', 'v0.10..HEAD', '--name-only'], { cwd: ROOT, encoding: 'utf8' }), filter stdout lines starting with 'workflows/', assert length === 0

**describe('SC-1: cross-type regression — existing test suites green'):**
- `test('Phase 64 manifest schema tests pass (updated to 16 fields)')` — spawnSync node --test on manifest-schema.test.mjs, assert status === 0
- `test('Phase 64 workflow pass-through tests pass (relaxed hasStitchWireframes)')` — spawnSync node --test on workflow-pass-through.test.mjs, assert status === 0
- `test('Phase 74 experience regression tests pass')` — spawnSync on experience-regression.test.mjs, assert status === 0

Total expected test count: ~12 tests.

Run: `node --test tests/phase-82/regression-matrix.test.mjs`
Expected: all pass
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && node --test tests/phase-82/regression-matrix.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-82/regression-matrix.test.mjs exists
    - File contains `THIRTEEN_PIPELINE_SKILLS` array with exactly 13 entries
    - File contains `spawnSync('git'` for the no-new-workflows assertion
    - File contains `WCAG` assertion for critique.md software path
    - File contains `TypeScript` assertion for handoff.md software path
    - File contains `WFR` assertion for wireframe.md software path
    - File contains `EXPERIENCE_IMPLEMENTED` object with 4 entries (critique, hig, handoff, wireframe)
    - `node --test tests/phase-82/regression-matrix.test.mjs` exits 0
  </acceptance_criteria>
  <done>Regression matrix test suite covers SC-1 (software path preservation + existing suite pass-through), SC-3 (no new workflows via git), SC-4 (all 13 skills operational with experience branches)</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run the combined suite:

```bash
node --test tests/phase-64/*.mjs tests/phase-74/*.mjs tests/phase-82/regression-matrix.test.mjs
```

Expected: 0 failures. Phase 64 tests restored to green (12/12), Phase 74 green (7/7), Phase 82 regression matrix green (~12 tests).
</verification>

<success_criteria>
- Phase 64 manifest-schema.test.mjs: 6/6 pass with 16-field CANONICAL_FIELDS
- Phase 64 workflow-pass-through.test.mjs: 6/6 pass with relaxed hasStitchWireframes assertion
- Phase 82 regression-matrix.test.mjs: all tests pass covering SC-1, SC-3, SC-4
- Combined suite `node --test tests/phase-64/*.mjs tests/phase-74/*.mjs tests/phase-82/*.mjs` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/82-integration-validation-and-regression-audit/82-01-SUMMARY.md`
</output>
