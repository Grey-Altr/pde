---
phase: 121-mcp-server
plan: 02
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - packages/pde-mcp-server/dist/index.js
  - packages/pde-mcp-server/.gitignore
autonomous: false
requirements:
  - MCP-03

must_haves:
  truths:
    - "TypeScript compiles to dist/ without errors"
    - "dist/index.js has shebang as first line"
    - "Running node packages/pde-mcp-server/dist/index.js from project root discovers .planning/ and starts server"
    - "Server responds to MCP protocol initialization"
  artifacts:
    - path: "packages/pde-mcp-server/dist/index.js"
      provides: "Compiled entry point with shebang"
      contains: "#!/usr/bin/env node"
    - path: "packages/pde-mcp-server/.gitignore"
      provides: "Ignores node_modules and dist"
      contains: "node_modules"
  key_links:
    - from: "packages/pde-mcp-server/package.json"
      to: "packages/pde-mcp-server/dist/index.js"
      via: "bin field points to dist/index.js"
      pattern: '"bin"'
---

<objective>
Build the MCP server TypeScript, verify npx-ready distribution, and confirm end-to-end server startup.

Purpose: Ensure the MCP server compiles, starts, and responds to protocol initialization -- validating the full distribution path (MCP-03).
Output: Compiled dist/, .gitignore, human-verified server startup.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-mcp-server/121-01-SUMMARY.md
@.planning/phases/121-mcp-server/121-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and verify distribution readiness</name>
  <files>
    packages/pde-mcp-server/dist/index.js,
    packages/pde-mcp-server/.gitignore
  </files>
  <action>
    1. Create `packages/pde-mcp-server/.gitignore` with entries: `node_modules/`, `dist/`.

    2. Run `cd packages/pde-mcp-server && npm run build` to compile TypeScript.

    3. Verify `dist/index.js` exists and has `#!/usr/bin/env node` as line 1. If tsc strips the shebang, add a postbuild script to package.json:
       `"postbuild": "echo '#!/usr/bin/env node' | cat - dist/index.js > tmp && mv tmp dist/index.js && chmod +x dist/index.js"`
       Then re-run build.

    4. Verify `chmod +x dist/index.js` so it is executable.

    5. Quick smoke test: run `echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"test","version":"0.1.0"}}}' | node packages/pde-mcp-server/dist/index.js --planning-dir .planning 2>/dev/null | head -1` and verify it outputs a JSON-RPC response (not an error).

    6. Run `node --test tests/phase-121/test-mcp-server.cjs` to ensure all existing tests still pass after build.
  </action>
  <verify>
    <automated>cd packages/pde-mcp-server && npm run build && test -f dist/index.js && head -1 dist/index.js | grep -q "#!/usr/bin/env node" && echo "BUILD OK"</automated>
  </verify>
  <done>
    - TypeScript compiles without errors
    - dist/index.js exists with shebang line 1
    - dist/index.js is executable
    - Server accepts JSON-RPC initialize message
    - All phase-121 tests still pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify MCP server end-to-end</name>
  <action>
    Human verifies the complete MCP server package:
    - packages/pde-mcp-server/ with isolated dependencies
    - 10 tools: get-project, get-design-state, get-manifest, get-tokens, get-handoff, get-artifact, get-roadmap, get-requirements, get-pipeline-status, list-artifacts
    - Pipeline status resource at pde://pipeline-status
    - Compiled TypeScript in dist/
    - Structural tests covering MCP-01 through MCP-05

    Steps to verify:
    1. Verify server starts: `echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"test","version":"0.1.0"}}}' | node packages/pde-mcp-server/dist/index.js --planning-dir .planning 2>/dev/null`
       Expected: JSON-RPC response with server capabilities

    2. Verify tests pass: `node --test tests/phase-121/test-mcp-server.cjs`
       Expected: All tests green

    3. Verify no root dependencies added: check that no package.json exists at project root (or if it does, no new dependencies were added)

    4. Verify tool count: `grep -c "registerTool" packages/pde-mcp-server/src/index.ts`
       Expected: 10

    5. Optionally test with Claude Code: `claude mcp add pde --command "node" --args "packages/pde-mcp-server/dist/index.js" "--planning-dir" ".planning"` then ask Claude to use the get-project tool
  </action>
  <verify>Human visual and functional verification</verify>
  <done>
    - Server starts and responds to MCP initialize
    - All tests green
    - Zero-dependency constraint preserved at project root
    - Human approves
  </done>
</task>

</tasks>

<verification>
- `cd packages/pde-mcp-server && npm run build` — compiles without errors
- `head -1 packages/pde-mcp-server/dist/index.js` — shows shebang
- `node --test tests/phase-121/test-mcp-server.cjs` — all tests green
- No npm dependencies added to project root
</verification>

<success_criteria>
- MCP server compiles from TypeScript to dist/
- dist/index.js is executable with shebang
- Server accepts MCP protocol initialization
- Human confirms server startup and tool availability
- Zero-dependency constraint preserved at project root
</success_criteria>

<output>
After completion, create `.planning/phases/121-mcp-server/121-02-SUMMARY.md`
</output>
