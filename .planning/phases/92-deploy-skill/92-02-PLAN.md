---
phase: 92-deploy-skill
plan: 02
type: execute
wave: 2
depends_on: ["92-01"]
files_modified:
  - commands/deploy.md
  - workflows/build.md
autonomous: true
requirements: [DEPLOY-08, DEPLOY-01]

must_haves:
  truths:
    - "/pde:deploy slash command exists and routes to workflows/deploy.md"
    - "build.md STAGES table includes Stage 14 (deploy) gated on businessMode"
    - "Non-business products see Stage 14 as skipped, not absent"
  artifacts:
    - path: "commands/deploy.md"
      provides: "/pde:deploy slash command entry point"
      contains: "pde:deploy"
    - path: "workflows/build.md"
      provides: "Stage 14 entry in STAGES table"
      contains: "deploy"
  key_links:
    - from: "commands/deploy.md"
      to: "workflows/deploy.md"
      via: "@workflows/deploy.md reference in process section"
      pattern: "workflows/deploy\\.md"
    - from: "workflows/build.md"
      to: "workflows/deploy.md"
      via: "Stage 14 STAGES entry with pde:deploy skill"
      pattern: "deploy.*pde:deploy"
---

<objective>
Create the /pde:deploy slash command entry point and wire Stage 14 into the build orchestrator's STAGES table with businessMode conditional gating.

Purpose: Without the slash command, users cannot invoke deploy directly. Without the build.md Stage 14 entry, /pde:build does not include deploy in the pipeline.
Output: commands/deploy.md (slash command), updated workflows/build.md (Stage 14 in STAGES table + businessMode gate logic)
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-deploy-skill/92-RESEARCH.md
@.planning/phases/92-deploy-skill/92-01-SUMMARY.md

<interfaces>
<!-- Executor needs the exact STAGES table format and command file format. -->

From workflows/build.md (current STAGES table, lines 29-43):

| Index | Name        | Skill           | Check Method  | Check Field/Glob                                              |
|-------|-------------|-----------------|---------------|---------------------------------------------------------------|
| 1     | recommend   | pde:recommend   | coverage      | hasRecommendations                                            |
| 2     | competitive | pde:competitive | coverage      | hasCompetitive                                                |
| 3     | opportunity | pde:opportunity | coverage      | hasOpportunity                                                |
| 4     | ideate      | pde:ideate      | coverage+glob | hasIdeation + .planning/design/strategy/IDT-ideation-v*.md   |
| 5     | brief       | pde:brief       | glob          | .planning/design/strategy/BRF-brief-v*.md                    |
| 6     | system      | pde:system      | coverage      | hasDesignSystem                                               |
| 7     | flows       | pde:flows       | coverage      | hasFlows                                                      |
| 8     | wireframe   | pde:wireframe   | coverage      | hasWireframes                                                 |
| 9     | critique    | pde:critique    | coverage      | hasCritique                                                   |
| 10    | iterate     | pde:iterate     | coverage      | hasIterate (default false if absent)                          |
| 11    | mockup      | pde:mockup      | coverage      | hasMockup                                                     |
| 12    | hig         | pde:hig         | coverage      | hasHigAudit                                                   |
| 13    | handoff     | pde:handoff     | coverage      | hasHandoff                                                    |

From commands/handoff.md (slash command file format) -- the file has YAML frontmatter with fields: name, description, argument-hint, allowed-tools list. Then XML sections: objective and process. The process section references the workflow via @workflows/handoff.md.

From workflows/build.md (Step 2/4 businessMode detection pattern, from 92-RESEARCH.md):
```bash
BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
if [[ "$BM" == @file:* ]]; then BM=$(cat "${BM#@file:}"); fi
```

From workflows/build.md (anti-pattern #9, line 254):
"NEVER write a bare digit (like 7 or 13) in any stage progress message. Always reference TOTAL."

From workflows/build.md (line 45):
"`TOTAL = count(STAGES)` -- used everywhere instead of any numeric literal."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/deploy.md slash command</name>
  <files>commands/deploy.md</files>
  <read_first>
    - commands/handoff.md (exact format reference for YAML frontmatter, objective, process sections)
    - commands/wireframe.md (secondary format reference for simpler command files)
    - workflows/deploy.md (verify it exists from Plan 01, confirm the exact workflow path to reference)
  </read_first>
  <action>
Create `commands/deploy.md` as the /pde:deploy slash command entry point. Follow the exact format from commands/handoff.md.

The file must have YAML frontmatter (between triple-dash delimiters) with these exact fields:
- `name: pde:deploy` (exact string -- DEPLOY-08 Nyquist test checks for this)
- `description: Generate deployable scaffolds (Next.js landing page, Stripe config, email templates) and deploy to Vercel with approval gates`
- `argument-hint: "[--force] [--verbose]"`
- `allowed-tools:` list containing: Read, Write, Edit, Bash, Glob, Grep, Task

After the frontmatter, include two XML sections:

1. `<objective>` section with text: "Execute the /pde:deploy command."

2. `<process>` section with text: "Follow @workflows/deploy.md exactly, passing all of $ARGUMENTS."

Key details:
- No MCP tools needed in allowed-tools (deploy workflow does not use Figma, GitHub, etc.)
- argument-hint includes --force and --verbose (consistent with other commands)
- Process section references @workflows/deploy.md (the file created in Plan 01)
  </action>
  <verify>
    <automated>grep "name: pde:deploy" commands/deploy.md && echo "DEPLOY-08 satisfied"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `commands/deploy.md`
    - File contains `name: pde:deploy` in YAML frontmatter
    - File contains `description:` field with non-empty value
    - File contains `allowed-tools:` section with at least Read, Write, Bash, Glob
    - File contains `@workflows/deploy.md` in the process section
    - File contains `<objective>` and `<process>` XML tags
    - DEPLOY-08 Nyquist tests (2 assertions) now pass GREEN
  </acceptance_criteria>
  <done>commands/deploy.md exists with pde:deploy slash command routing to workflows/deploy.md</done>
</task>

<task type="auto">
  <name>Task 2: Add Stage 14 to build.md STAGES table with businessMode gate</name>
  <files>workflows/build.md</files>
  <read_first>
    - workflows/build.md (full file -- need to understand STAGES table at lines 29-43, Step 2/4 coverage check logic, Step 3/4 stage execution loop, Step 4/4 completion check, anti-patterns section)
    - .planning/phases/92-deploy-skill/92-RESEARCH.md (Pattern 4: Build Orchestrator Stage 14 Conditional)
    - .planning/phases/92-deploy-skill/tests/test-deploy-skill.cjs (verify what DEPLOY-01 asserts about build.md)
  </read_first>
  <action>
Modify `workflows/build.md` to add Stage 14 (deploy) to the STAGES table and add businessMode-conditional execution logic.

**Change 1: Add row 14 to STAGES table (after row 13 handoff):**

Add this row to the STAGES table after the handoff row:
```
| 14    | deploy      | pde:deploy      | coverage      | hasDeployStaging (businessMode gate)                          |
```

This changes TOTAL from 13 to 14. Per anti-pattern #9, no bare digits exist in the file -- TOTAL is always derived from `count(STAGES)`. The existing text `TOTAL = count(STAGES)` on line 45 automatically reflects the new count.

**Change 2: Add businessMode gate in Step 2/4 (coverage check section):**

In Step 2/4, after reading all existing coverage flags, add businessMode detection:
```bash
BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
if [[ "$BM" == @file:* ]]; then BM=$(cat "${BM#@file:}"); fi
```

Then in the Stage 14 (deploy) status determination:
```
If $BM != "true":
  Stage 14 status = "skipped (non-business project)"
Else:
  Check hasDeployStaging coverage flag (same pattern as other coverage checks)
  If hasDeployStaging == true: Stage 14 status = "complete"
  Else: Stage 14 status = "pending"
```

**Change 3: Add Stage 14 skip logic in Step 3/4 (execution loop):**

In the stage execution loop, when reaching Stage 14:
- If `$BM != "true"`: Display "Stage 14/{TOTAL}: /pde:deploy -- skipped (non-business project)" and skip.
- If `$BM == "true"` and stage is pending: invoke Skill(pde:deploy) with PASSTHROUGH_ARGS.

This follows the existing `--from` skip display pattern already in build.md.

**Change 4: No changes to anti-patterns section.** The existing anti-pattern #9 already covers the "use TOTAL not bare digits" rule which handles the 13-to-14 transition automatically.

Do NOT modify any other existing stages or logic. The only additions are:
1. One row in the STAGES table
2. BM variable read in Step 2/4
3. Stage 14 conditional logic in Step 2/4 status and Step 3/4 execution
  </action>
  <verify>
    <automated>grep "deploy.*pde:deploy" workflows/build.md && grep "hasDeployStaging" workflows/build.md && node --test .planning/phases/92-deploy-skill/tests/test-deploy-skill.cjs 2>&1 | grep -E "(pass|fail)" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "| 14" workflows/build.md` returns a match containing "deploy" and "pde:deploy"
    - `grep "hasDeployStaging" workflows/build.md` returns at least one match
    - `grep "businessMode" workflows/build.md` returns at least one match (the BM gate)
    - `grep "non-business" workflows/build.md` returns at least one match (skip message)
    - The STAGES table now has 14 rows (count pipe-delimited rows between header and next section)
    - All 21/21 Nyquist tests GREEN (DEPLOY-01 build.md reference now satisfied, DEPLOY-08 commands/deploy.md now satisfied)
    - No existing stages (1-13) have been modified (diff shows additions only, no line changes to existing rows)
  </acceptance_criteria>
  <done>build.md STAGES table has 14 entries. Stage 14 (deploy) is gated on businessMode === true. All 21/21 Nyquist tests GREEN.</done>
</task>

</tasks>

<verification>
```bash
# All 21 Nyquist tests GREEN
node --test .planning/phases/92-deploy-skill/tests/test-deploy-skill.cjs

# Verify slash command exists
grep "name: pde:deploy" commands/deploy.md

# Verify build.md Stage 14
grep "| 14.*deploy.*pde:deploy" workflows/build.md
grep "hasDeployStaging" workflows/build.md
grep "non-business" workflows/build.md

# Verify no regression to existing stages
grep -c "^|" workflows/build.md  # Should show 15 (1 header + 14 data rows)
```
</verification>

<success_criteria>
- commands/deploy.md exists with `name: pde:deploy` and routes to workflows/deploy.md
- build.md STAGES table has 14 rows with deploy as Stage 14
- Stage 14 is gated on businessMode -- non-business projects see "skipped (non-business project)"
- All 21/21 DEPLOY-01 through DEPLOY-09 Nyquist tests GREEN
- No regressions to existing build.md stages 1-13
</success_criteria>

<output>
After completion, create `.planning/phases/92-deploy-skill/92-02-SUMMARY.md`
</output>
