---
phase: 92-deploy-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/92-deploy-skill/tests/test-deploy-skill.cjs
  - workflows/deploy.md
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03, DEPLOY-04, DEPLOY-05, DEPLOY-06, DEPLOY-07, DEPLOY-09]

must_haves:
  truths:
    - "deploy.md exists as a complete Stage 14 workflow with 4 approval gates"
    - "deploy.md reads upstream LDP/STR/OTR artifacts and generates scaffold specs"
    - "deploy.md invokes Vercel CLI with --prod --no-wait --yes for non-blocking deployment"
    - "deploy.md writes deploy-manifest.json with review_required: true per artifact"
    - "All deployment artifacts route to .planning/deploy-staging/ not .planning/design/"
  artifacts:
    - path: "workflows/deploy.md"
      provides: "Stage 14 deploy workflow"
      min_lines: 200
    - path: ".planning/phases/92-deploy-skill/tests/test-deploy-skill.cjs"
      provides: "Nyquist structural tests for DEPLOY-01 through DEPLOY-09"
      min_lines: 100
  key_links:
    - from: "workflows/deploy.md"
      to: ".planning/design/launch/LDP-landing-page-v*.md"
      via: "Glob read in Step 1"
      pattern: "LDP-landing-page"
    - from: "workflows/deploy.md"
      to: ".planning/deploy-staging/"
      via: "Write tool scaffold generation"
      pattern: "deploy-staging"
    - from: "workflows/deploy.md"
      to: "deploy-manifest.json"
      via: "Write tool at Step 5"
      pattern: "deploy-manifest\\.json"
---

<objective>
Create the deploy workflow (workflows/deploy.md) and its Nyquist test scaffold -- the complete Stage 14 pipeline step with four mandatory approval gates, Next.js/Stripe/Resend scaffold generation specs, Vercel CLI deployment, and deploy-manifest.json tracking.

Purpose: This is the final pipeline stage that transforms upstream design artifacts (LDP, STR, OTR) into deployable scaffold code with human approval at every external write boundary.
Output: workflows/deploy.md (Stage 14 workflow), test-deploy-skill.cjs (Nyquist structural assertions)
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-deploy-skill/92-RESEARCH.md

Relevant prior phase outputs:
@.planning/phases/91-handoff-launch-kit-assembly/91-02-SUMMARY.md
@.planning/phases/89-wireframe-stage-launch-artifacts/89-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From workflows/build.md STAGES table (line 29-43):

| Index | Name        | Skill           | Check Method  | Check Field/Glob                                              |
|-------|-------------|-----------------|---------------|---------------------------------------------------------------|
| 1     | recommend   | pde:recommend   | coverage      | hasRecommendations                                            |
...
| 13    | handoff     | pde:handoff     | coverage      | hasHandoff                                                    |

Stage 14 (deploy) will be added in Plan 02.

From commands/handoff.md (slash command format): The file has YAML frontmatter with fields: name, description, argument-hint, allowed-tools list (7 tools + MCP tools). Then XML sections: `<objective>` with "Execute the /pde:handoff command." and `<process>` which routes to @workflows/handoff.md. The deploy command file will follow this exact same pattern.

From workflows/handoff.md businessMode detection pattern:
```bash
BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
if [[ "$BM" == @file:* ]]; then BM=$(cat "${BM#@file:}"); fi
```

From .planning/phases/92-deploy-skill/92-RESEARCH.md:
- AskUserQuestion gate pattern: question + ["Proceed", "Halt -- stop deployment"]
- Vercel CLI: `npx vercel --prod --no-wait --yes --cwd [path]`
- Pre-auth check: `npx vercel whoami 2>/dev/null`
- deploy-manifest.json schema with 4 artifact entries, each with review_required: true
- Next.js 16.2.1 App Router scaffold structure at .planning/deploy-staging/landing-page/
- Stripe config at .planning/deploy-staging/stripe/stripe-config.json
- Email templates at .planning/deploy-staging/email/emails/
- .planning/deploy-staging/.gitignore with content `*`
- Pinned versions: Next.js 16.2.1, Stripe v20 (20.4.1), Resend 6.9.4, @react-email/components 1.0.10, Tailwind v4 (4.2.2)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Nyquist test scaffold for DEPLOY-01 through DEPLOY-09</name>
  <files>.planning/phases/92-deploy-skill/tests/test-deploy-skill.cjs</files>
  <read_first>
    - .planning/phases/91-handoff-launch-kit-assembly/tests/test-handoff-launch-kit.cjs (pattern reference for test structure)
    - .planning/phases/92-deploy-skill/92-RESEARCH.md (requirement-to-test mapping)
    - .planning/phases/92-deploy-skill/92-VALIDATION.md (validation strategy)
  </read_first>
  <action>
Create `.planning/phases/92-deploy-skill/tests/test-deploy-skill.cjs` using the Phase 91 test pattern (node:test, fs.readFileSync of target workflow, structural assertions).

The test file reads two files at module top:
```javascript
const deployContent = fs.readFileSync(path.join(ROOT, 'workflows', 'deploy.md'), 'utf-8');
const buildContent = fs.readFileSync(path.join(ROOT, 'workflows', 'build.md'), 'utf-8');
```
Also conditionally reads `commands/deploy.md` for DEPLOY-08 tests.

Create the following describe/it blocks:

**DEPLOY-01 (3 assertions):**
- `deploy.md` file exists at `workflows/deploy.md` (fs.existsSync check)
- `deploy.md` contains "Stage 14" (string match)
- `deploy.md` contains `manifest-get-top-level businessMode` (business gate)

**DEPLOY-02 (3 assertions):**
- `deploy.md` contains "16.2.1" (Next.js version pin)
- `deploy.md` contains "deploy-staging/landing-page" (scaffold output path)
- `deploy.md` contains "LDP-landing-page" (LDP artifact consumption)

**DEPLOY-03 (2 assertions):**
- `deploy.md` contains `pk_test_REPLACE_WITH_YOUR_KEY` (test-mode key placeholder)
- `deploy.md` contains "stripe-config.json" (Stripe config artifact)

**DEPLOY-04 (2 assertions):**
- `deploy.md` contains `@react-email/components` (React Email dependency)
- `deploy.md` contains "OTR" (OTR artifact consumption)

**DEPLOY-05 (2 assertions):**
- `deploy.md` contains `npx vercel --prod --no-wait` (non-blocking deploy)
- `deploy.md` contains `vercel whoami` (auth pre-check)

**DEPLOY-06 (3 assertions):**
- `deploy.md` contains "Gate 1/4" (first approval gate)
- `deploy.md` contains "Gate 4/4" (fourth approval gate)
- `deploy.md` contains "Halt" (halt option in gates)

**DEPLOY-07 (2 assertions):**
- `deploy.md` contains `.planning/deploy-staging/` (correct output directory)
- `deploy.md` does NOT contain `.planning/design/` as an output target for deploy artifacts (use regex to check it doesn't contain a Write to .planning/design/ -- assert that "deploy-staging" appears more than 5 times)

**DEPLOY-08 (2 assertions):**
- `commands/deploy.md` file exists at `commands/deploy.md` (fs.existsSync)
- `commands/deploy.md` contains `name: pde:deploy` (slash command name in YAML frontmatter)

**DEPLOY-09 (2 assertions):**
- `deploy.md` contains "deploy-manifest.json" (manifest file reference)
- `deploy.md` contains "review_required" (per-artifact review flag)

Total: 21 assertions. All should be RED initially (workflows/deploy.md and commands/deploy.md do not exist yet).

Use try/catch wrapping for file reads so tests that reference non-existent files produce clear RED failures (not uncaught exceptions). Pattern:
```javascript
let deployContent = '';
try { deployContent = fs.readFileSync(path.join(ROOT, 'workflows', 'deploy.md'), 'utf-8'); } catch { /* file not created yet */ }
```
  </action>
  <verify>
    <automated>node --test .planning/phases/92-deploy-skill/tests/test-deploy-skill.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/92-deploy-skill/tests/test-deploy-skill.cjs`
    - File contains `require('node:test')` and `require('node:assert')`
    - File contains all 9 describe blocks: `DEPLOY-01` through `DEPLOY-09`
    - File contains exactly 21 `it(` calls (one per assertion)
    - `node --test` runs without syntax errors (all 21 tests execute, all RED expected)
    - File contains `pk_test_REPLACE_WITH_YOUR_KEY` as a string literal in DEPLOY-03 assertion
    - File contains `npx vercel --prod --no-wait` as a string literal in DEPLOY-05 assertion
    - File contains `pde:deploy` as a string literal in DEPLOY-08 assertion
  </acceptance_criteria>
  <done>21 Nyquist tests exist and execute (all RED -- workflows/deploy.md not yet created)</done>
</task>

<task type="auto">
  <name>Task 2: Create workflows/deploy.md -- Stage 14 deploy workflow</name>
  <files>workflows/deploy.md</files>
  <read_first>
    - workflows/handoff.md (lines 1-50 for header pattern; search for "manifest-get-top-level businessMode" for detection pattern; search for "AskUserQuestion" for gate pattern; search for "Step 5e" for lock/write pattern)
    - workflows/build.md (lines 25-45 for STAGES table; lines 234-258 for anti-patterns section format)
    - .planning/phases/92-deploy-skill/92-RESEARCH.md (architecture patterns, code examples, deploy-manifest.json schema)
    - references/launch-frameworks.md (search for "Section Map" and "Pricing Config Schema" for LDP/STR structure)
    - .planning/phases/92-deploy-skill/tests/test-deploy-skill.cjs (the test file from Task 1 -- to ensure all 21 assertions will pass)
  </read_first>
  <action>
Create `workflows/deploy.md` as the Stage 14 deploy workflow. Structure it as a 6-step workflow following the established PDE workflow format (header comment, step structure, anti-patterns, output section).

**Header:**
```markdown
# Deploy Workflow -- Stage 14

> Generates deployable scaffolds from upstream launch artifacts (LDP, STR, OTR) with mandatory human approval gates before every external write. Conditionally executed only when `businessMode === true`.
```

**Step 1/6: Initialize and verify prerequisites**

Read businessMode from manifest:
```bash
BM=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessMode 2>/dev/null)
if [[ "$BM" == @file:* ]]; then BM=$(cat "${BM#@file:}"); fi
```
If `$BM != "true"`: Display "Deploy is only available for business-mode projects. Set businessMode via /pde:brief." and HALT.

Read businessTrack from manifest:
```bash
BT=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design manifest-get-top-level businessTrack 2>/dev/null)
if [[ "$BT" == @file:* ]]; then BT=$(cat "${BT#@file:}"); fi
```

Check hasLaunchKit coverage flag:
```bash
HLK=$(node "${CLAUDE_PLUGIN_ROOT}/bin/pde-tools.cjs" design coverage-check hasLaunchKit 2>/dev/null)
if [[ "$HLK" == @file:* ]]; then HLK=$(cat "${HLK#@file:}"); fi
```
If `$HLK != "true"`: Display "Launch kit not assembled. Run /pde:handoff first to generate LKT/CNT/OTR artifacts." and HALT.

Check for existing deploy-staging (collision detection):
```bash
ls .planning/deploy-staging/landing-page/package.json 2>/dev/null
```
If exists: Display warning "Deploy staging already exists at .planning/deploy-staging/. Continuing will overwrite existing files."

**Step 2/6: Read upstream artifacts**

Use Glob to find latest versions of upstream artifacts:
- `LDP_FILE` = Glob `.planning/design/launch/LDP-landing-page-v*.md` | tail -1
- `STR_FILE` = Glob `.planning/design/launch/STR-stripe-pricing-v*.md` | tail -1
- `OTR_FILE` = Glob `.planning/design/launch/OTR-outreach-v*.md` | tail -1

If any artifact is missing: Display "Required artifact not found: [artifact]. Run the upstream workflow first." and HALT.

Read each artifact content via Read tool. Extract:
- From LDP: Section Map (component names, Next.js paths, Server/Client designation)
- From STR: Pricing tiers, billing intervals, checkout mode
- From OTR: Email sequence names, triggers, delays

**Step 3/6: Approval-gated scaffold generation**

This step has 3 sequential approval gates (Gates 1-3). Each gate uses AskUserQuestion.

**Gate 1/4 -- Next.js Landing Page Scaffold:**

Use AskUserQuestion:
- question: "Approval Gate 1/4 -- Next.js Landing Page Scaffold\n\nAbout to generate Next.js 16.2.1 App Router scaffold at .planning/deploy-staging/landing-page/ with pinned versions:\n- Next.js 16.2.1\n- Tailwind v4 (4.2.2)\n- Stripe v20 (20.4.1)\n- Resend 6.9.4\n\nComponents from LDP spec: [list section components]\n\nProceed?"
- options: ["Proceed", "Halt -- stop deployment"]

If "Halt": Display "Deploy halted at Gate 1/4. No files written. Re-run /pde:deploy to restart." HALT.

If "Proceed": Generate Next.js scaffold at `.planning/deploy-staging/landing-page/` using Write tool:

Files to generate (from 92-RESEARCH.md architecture):
1. `package.json` -- with dependencies: next@16.2.1, react, react-dom, stripe@20.4.1, resend@6.9.4, tailwindcss@4.2.2, typescript@^5, @types/node@latest, eslint-config-next@16.2.1
2. `next.config.ts` -- minimal Next.js config (TypeScript format per Next.js 16+ standard)
3. `tsconfig.json` -- standard Next.js TypeScript config
4. `postcss.config.mjs` -- `export default { plugins: {} }` (Tailwind v4 uses CSS-first config, postcss config is minimal)
5. `app/globals.css` -- Tailwind v4 CSS-first config with `@import "tailwindcss";` and `@theme {}` block
6. `app/layout.tsx` -- root layout with `[YOUR_PRODUCT_NAME]` metadata placeholders
7. `app/(marketing)/layout.tsx` -- marketing route group layout with SiteNav + SiteFooter
8. `app/(marketing)/page.tsx` -- assembles all section components from LDP spec
9. `app/(marketing)/_components/hero-section.tsx` -- stub with `// Source: LDP artifact` comment, `[YOUR_HEADLINE]` placeholder
10. `app/(marketing)/_components/features-grid.tsx` -- stub
11. `app/(marketing)/_components/pricing-table.tsx` -- stub with `[YOUR_PRICE]` placeholder (never dollar amounts)
12. `app/(marketing)/_components/cta-banner.tsx` -- stub
13. `app/(marketing)/_components/faq-accordion.tsx` -- stub (if present in LDP)
14. Additional components from LDP Section Map (logo-bar, problem-statement, how-it-works, testimonials-block as applicable)
15. `README.md` -- deployment instructions for user

All components use `// Source: LDP artifact, Section: {section name}` header comment. Client Components get `'use client'` directive. All content uses `[YOUR_X]` structural placeholders.

Write `.planning/deploy-staging/.gitignore` with content: a single `*` wildcard (ignores all files in deploy-staging from git).

**Gate 2/4 -- Stripe Pricing Config:**

Use AskUserQuestion:
- question: "Approval Gate 2/4 -- Stripe Pricing Config\n\nAbout to generate Stripe pricing config at .planning/deploy-staging/stripe/stripe-config.json.\nKey: pk_test_REPLACE_WITH_YOUR_KEY (test mode only).\nPricing tiers from STR spec: [list tier names]\n\nProceed?"
- options: ["Proceed", "Halt -- stop deployment"]

If "Halt": Display "Deploy halted at Gate 2/4. Landing page scaffold written but Stripe config not generated. Re-run /pde:deploy to restart." HALT.

If "Proceed": Generate Stripe config scaffold:
1. `.planning/deploy-staging/stripe/stripe-config.json` -- from STR artifact with `pk_test_REPLACE_WITH_YOUR_KEY` and `sk_test_REPLACE_WITH_YOUR_KEY` placeholder keys. Pricing tiers from STR spec with `unit_amount: "[YOUR_PRICE_IN_CENTS]"`. checkout_mode from STR spec.
2. `.planning/deploy-staging/stripe/README.md` -- Stripe setup instructions

**Gate 3/4 -- Resend Email Templates:**

Use AskUserQuestion:
- question: "Approval Gate 3/4 -- Resend Email Templates\n\nAbout to generate React Email template stubs at .planning/deploy-staging/email/emails/ from OTR artifact.\nEmail count: [N] onboarding + [M] investor outreach.\nUses @react-email/components 1.0.10.\n\nProceed?"
- options: ["Proceed", "Halt -- stop deployment"]

If "Halt": Display "Deploy halted at Gate 3/4. Re-run /pde:deploy to restart." HALT.

If "Proceed": Generate email template stubs:
1. `.planning/deploy-staging/email/package.json` -- with dependencies: @react-email/components@1.0.10, react, react-dom, resend@6.9.4
2. `.planning/deploy-staging/email/emails/onboarding-01-welcome.tsx` through `onboarding-0N-{name}.tsx` -- one file per OTR onboarding email. Each file imports from `@react-email/components` (Body, Button, Container, Head, Heading, Html, Preview, Section, Text). Uses `[YOUR_X]` placeholders for all personalization fields.
3. `.planning/deploy-staging/email/emails/investor-01-intro.tsx` through `investor-03-{name}.tsx` -- investor outreach emails from OTR spec.
4. `.planning/deploy-staging/email/README.md` -- Resend setup instructions with send pattern code snippet.

**Step 4/6: Approval-gated Vercel deployment**

**Gate 4/4 -- Vercel Deploy:**

Pre-check authentication:
```bash
VERCEL_AUTH=$(npx vercel whoami 2>/dev/null)
VERCEL_EXIT=$?
```
If `$VERCEL_EXIT != 0`: Display "Not authenticated with Vercel CLI. Run `npx vercel login` first, then re-run /pde:deploy." HALT.

Use AskUserQuestion:
- question: "Approval Gate 4/4 -- Vercel Deployment\n\nAuthenticated as: $VERCEL_AUTH\nAbout to deploy .planning/deploy-staging/landing-page/ to Vercel production.\nCommand: npx vercel --prod --no-wait --yes\n\nThis will queue a deployment. The URL will be returned immediately.\n\nProceed?"
- options: ["Proceed", "Halt -- stop deployment"]

If "Halt": Display "Deploy halted at Gate 4/4. Scaffolds generated but not deployed. Re-run /pde:deploy to deploy." HALT.

If "Proceed":
```bash
DEPLOY_URL=$(npx vercel --prod --no-wait --yes --cwd .planning/deploy-staging/landing-page/ 2>deploy-error.txt)
DEPLOY_EXIT=$?
```
If `$DEPLOY_EXIT != 0`: Display error from deploy-error.txt and HALT.
If `$DEPLOY_EXIT == 0`: Store DEPLOY_URL for manifest.

**Step 5/6: Write deploy-manifest.json**

Write `.planning/deploy-staging/deploy-manifest.json` using Write tool. The JSON schema (from 92-RESEARCH.md) has these fields:
- `generated_at`: ISO8601 timestamp
- `business_track`: value of $BT variable
- `artifacts` object with 4 entries: `landing_page`, `stripe_config`, `email_templates`, `vercel_deployment`
- Each artifact entry has: `status` ("ready" or "queued"), `path`, `review_required: true`, `reviewed: false`, `notes`
- The `vercel_deployment` entry has `deployment_url` set to `$DEPLOY_URL`
- The `stripe_config` notes must include "pk_test_REPLACE_WITH_YOUR_KEY"

**Step 6/6: Output summary**

Display final summary listing all generated artifacts, their paths, the deployment URL, and next steps (replace placeholders, set API keys, check Vercel dashboard).

**Anti-patterns section** (placed after output, before closing) -- include 8 anti-patterns:
1. NEVER skip an approval gate
2. NEVER use live Stripe keys (only pk_test_REPLACE_WITH_YOUR_KEY)
3. NEVER write deploy artifacts to .planning/design/
4. NEVER use create-next-app
5. NEVER omit --yes from vercel command
6. NEVER omit --no-wait from vercel command
7. NEVER generate dollar amounts (use [YOUR_PRICE_IN_CENTS])
8. NEVER generate specific company names in emails (use [YOUR_COMPANY_NAME])

The workflow must also include a reference comment at the top noting this is "Stage 14" of the build pipeline and is conditionally gated on `businessMode === true`.
  </action>
  <verify>
    <automated>node --test .planning/phases/92-deploy-skill/tests/test-deploy-skill.cjs 2>&1 | grep -E "(pass|fail|# tests)" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `workflows/deploy.md`
    - File contains "Stage 14" (grep returns match)
    - File contains `manifest-get-top-level businessMode` (grep returns match)
    - File contains "16.2.1" (Next.js version pin)
    - File contains "deploy-staging/landing-page" (scaffold path)
    - File contains "LDP-landing-page" (LDP artifact consumption)
    - File contains `pk_test_REPLACE_WITH_YOUR_KEY` (Stripe test key placeholder)
    - File contains "stripe-config.json" (Stripe config reference)
    - File contains `@react-email/components` (React Email reference)
    - File contains "OTR" (OTR artifact reference)
    - File contains `npx vercel --prod --no-wait` (non-blocking deploy command)
    - File contains `vercel whoami` (auth pre-check)
    - File contains "Gate 1/4" and "Gate 4/4" (all 4 approval gates present)
    - File contains "Halt" (halt option in gates)
    - File contains "deploy-manifest.json" (manifest file reference)
    - File contains "review_required" (per-artifact review flag)
    - File contains `.planning/deploy-staging/` at least 5 times (primary output directory)
    - File does NOT contain a Write instruction targeting `.planning/design/` for deploy artifacts
    - At least 19 of 21 Nyquist tests pass (DEPLOY-01 through DEPLOY-07, DEPLOY-09 all GREEN; DEPLOY-08 remains RED until Plan 02 creates commands/deploy.md)
  </acceptance_criteria>
  <done>workflows/deploy.md exists with all 6 steps, 4 approval gates, pinned versions, structural placeholders, and Vercel CLI integration. 19/21 Nyquist tests GREEN (DEPLOY-08 x2 RED pending Plan 02).</done>
</task>

</tasks>

<verification>
```bash
# All DEPLOY-01 through DEPLOY-07 and DEPLOY-09 tests GREEN
node --test .planning/phases/92-deploy-skill/tests/test-deploy-skill.cjs

# Verify deploy.md structural completeness
grep -c "Gate" workflows/deploy.md  # Should be >= 4 (Gate 1/4 through 4/4)
grep "pk_test_REPLACE_WITH_YOUR_KEY" workflows/deploy.md  # Must match
grep "npx vercel --prod --no-wait" workflows/deploy.md  # Must match
grep "deploy-manifest.json" workflows/deploy.md  # Must match
grep "review_required" workflows/deploy.md  # Must match
```
</verification>

<success_criteria>
- workflows/deploy.md exists as a complete 6-step workflow with 4 approval gates
- Test scaffold has 21 assertions, at least 19 GREEN after Task 2
- Deploy workflow reads upstream LDP/STR/OTR artifacts
- Vercel CLI invoked with --prod --no-wait --yes flags
- deploy-manifest.json schema documented with review_required: true per artifact
- All deploy artifacts target .planning/deploy-staging/ (never .planning/design/)
- Stripe config uses pk_test_REPLACE_WITH_YOUR_KEY placeholder only
- Email templates use [YOUR_X] structural placeholders
</success_criteria>

<output>
After completion, create `.planning/phases/92-deploy-skill/92-01-SUMMARY.md`
</output>
