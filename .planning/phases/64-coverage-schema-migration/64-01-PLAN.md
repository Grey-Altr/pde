---
phase: 64-coverage-schema-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/design-manifest.json
  - .planning/design/design-manifest.json
  - .planning/pressure-test/fixture-greenfield/design/design-manifest.json
  - .planning/pressure-test/fixture-partial/design/design-manifest.json
  - .planning/pressure-test/fixture-rerun/design/design-manifest.json
  - workflows/wireframe.md
  - workflows/mockup.md
  - workflows/system.md
  - workflows/flows.md
  - workflows/critique.md
  - workflows/iterate.md
  - workflows/handoff.md
  - workflows/ideate.md
  - workflows/competitive.md
  - workflows/opportunity.md
  - workflows/hig.md
  - workflows/recommend.md
autonomous: true
requirements:
  - MCP-04

must_haves:
  truths:
    - "All 13 pipeline skills pass through hasStitchWireframes without clobbering any existing coverage flags"
    - "The design-manifest.json template includes hasStitchWireframes with default false"
    - "No existing skill coverage flags are reset to undefined or false by a schema-extended pipeline run"
    - "A grep of all 12 workflow files (excluding recommend.md manual check) confirms hasStitchWireframes in their designCoverage write block"
  artifacts:
    - path: "templates/design-manifest.json"
      provides: "14-field designCoverage schema template"
      contains: "hasStitchWireframes"
    - path: ".planning/design/design-manifest.json"
      provides: "Live manifest with 14-field designCoverage"
      contains: "hasStitchWireframes"
    - path: "workflows/wireframe.md"
      provides: "Wireframe skill with hasStitchWireframes pass-through"
      contains: "hasStitchWireframes"
    - path: "workflows/system.md"
      provides: "System skill with hasStitchWireframes pass-through"
      contains: "hasStitchWireframes"
  key_links:
    - from: "workflows/*.md (all 13)"
      to: "bin/lib/design.cjs manifest-set-top-level"
      via: "14-field JSON literal in manifest-set-top-level designCoverage call"
      pattern: "hasStitchWireframes.*current"
    - from: "templates/design-manifest.json"
      to: ".planning/design/design-manifest.json"
      via: "Template defines schema that new projects use"
      pattern: "hasStitchWireframes.*false"
---

<objective>
Extend the designCoverage schema from 13 fields to 14 fields by adding hasStitchWireframes across all pipeline skills, JSON templates, and fixture manifests.

Purpose: This is a hard prerequisite for all other v0.9 phases — without hasStitchWireframes in the pass-through-all pattern, any existing skill run after Stitch integration would clobber the Stitch coverage flag.
Output: All 13 workflow files, 1 template, 1 live manifest, and 3 fixtures updated with the 14-field designCoverage schema.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-coverage-schema-migration/64-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update JSON manifests and fixtures with 14-field designCoverage</name>
  <files>
    templates/design-manifest.json,
    .planning/design/design-manifest.json,
    .planning/pressure-test/fixture-greenfield/design/design-manifest.json,
    .planning/pressure-test/fixture-partial/design/design-manifest.json,
    .planning/pressure-test/fixture-rerun/design/design-manifest.json
  </files>
  <read_first>
    templates/design-manifest.json,
    .planning/design/design-manifest.json,
    .planning/pressure-test/fixture-greenfield/design/design-manifest.json,
    .planning/pressure-test/fixture-partial/design/design-manifest.json,
    .planning/pressure-test/fixture-rerun/design/design-manifest.json
  </read_first>
  <action>
    Update all 5 JSON files to include hasStitchWireframes in the designCoverage block.

    **templates/design-manifest.json** — Add `"hasStitchWireframes": false` after the `"hasRecommendations": false` line inside the designCoverage block (around lines 107-122). The result should be 14 boolean fields plus the _comment field.

    **Live manifest (.planning/design/design-manifest.json)** — This file currently has only 7 fields in designCoverage. Bring it to the full 14-field schema by adding these missing fields after the existing ones:
    - "hasIdeation": false
    - "hasCompetitive": false
    - "hasOpportunity": false
    - "hasMockup": false
    - "hasHigAudit": false
    - "hasRecommendations": false
    - "hasStitchWireframes": false

    IMPORTANT: Check what coverage values are currently true in the live manifest and preserve them. Only add missing fields as false; do not reset existing true values.

    **3 fixture manifests** — Each currently has 13-field designCoverage. Add `"hasStitchWireframes": false` after `"hasRecommendations": false` in each. Preserve all existing boolean values in each fixture (some may have flags set to true for testing).

    Canonical field order (must be maintained):
    1. hasDesignSystem, 2. hasWireframes, 3. hasFlows, 4. hasHardwareSpec,
    5. hasCritique, 6. hasIterate, 7. hasHandoff, 8. hasIdeation,
    9. hasCompetitive, 10. hasOpportunity, 11. hasMockup, 12. hasHigAudit,
    13. hasRecommendations, 14. hasStitchWireframes
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep -c "hasStitchWireframes" templates/design-manifest.json .planning/design/design-manifest.json .planning/pressure-test/fixture-greenfield/design/design-manifest.json .planning/pressure-test/fixture-partial/design/design-manifest.json .planning/pressure-test/fixture-rerun/design/design-manifest.json</automated>
  </verify>
  <acceptance_criteria>
    - templates/design-manifest.json contains `"hasStitchWireframes": false`
    - .planning/design/design-manifest.json contains `"hasStitchWireframes": false`
    - .planning/design/design-manifest.json contains all 14 designCoverage fields (hasDesignSystem through hasStitchWireframes)
    - All 3 fixture manifests contain `"hasStitchWireframes": false`
    - grep -c "hasStitchWireframes" on all 5 files returns 1 for each
    - No existing true values in any manifest are reset to false
  </acceptance_criteria>
  <done>All 5 JSON files have 14-field designCoverage with hasStitchWireframes: false and all prior values preserved</done>
</task>

<task type="auto">
  <name>Task 2: Update all 13 workflow files with hasStitchWireframes pass-through</name>
  <files>
    workflows/wireframe.md,
    workflows/mockup.md,
    workflows/system.md,
    workflows/flows.md,
    workflows/critique.md,
    workflows/iterate.md,
    workflows/handoff.md,
    workflows/ideate.md,
    workflows/competitive.md,
    workflows/opportunity.md,
    workflows/hig.md,
    workflows/recommend.md
  </files>
  <read_first>
    workflows/wireframe.md,
    workflows/mockup.md,
    workflows/system.md,
    workflows/flows.md,
    workflows/critique.md,
    workflows/iterate.md,
    workflows/handoff.md,
    workflows/ideate.md,
    workflows/competitive.md,
    workflows/opportunity.md,
    workflows/hig.md,
    workflows/recommend.md,
    .planning/phases/64-coverage-schema-migration/64-RESEARCH.md
  </read_first>
  <action>
    For each of the 12 workflow files (all except recommend.md), make TWO changes:

    **Change A — manifest-set-top-level JSON literal:**
    Append `,"hasStitchWireframes":{current}` immediately before the closing `}'` in the manifest-set-top-level designCoverage call. The exact insertion point is after `"hasRecommendations":{current}`.

    Before: `..."hasRecommendations":{current}}'`
    After:  `..."hasRecommendations":{current},"hasStitchWireframes":{current}}'`

    Specific line numbers for each file:
    - wireframe.md line 777 (sets hasWireframes:true, all others {current})
    - mockup.md lines 1222-1223 (multi-line with backslash)
    - system.md line 1846
    - flows.md line 536
    - critique.md line 701
    - iterate.md line 453
    - handoff.md line 720
    - ideate.md lines 484-485 (multi-line)
    - competitive.md lines 551-552 (multi-line)
    - opportunity.md lines 474-475 (multi-line)
    - hig.md lines 619-620 (multi-line)

    **Change B — Prose/field-list updates:**
    Update the prose where the skill enumerates designCoverage fields. Add `hasStitchWireframes` to the field list and change "thirteen" to "fourteen" (or "13" to "14") where applicable. Specific locations from research:
    - wireframe.md line 774: "thirteen" -> "fourteen", add hasStitchWireframes to field list
    - mockup.md line 1217: add hasStitchWireframes to field list
    - mockup.md line 1226: "13 fields" -> "14 fields", add to canonical field order list
    - competitive.md lines 545-546: add to field list
    - critique.md line 698: "thirteen" -> "fourteen", add to field list
    - flows.md line 518: "thirteen" -> "fourteen", add to field list
    - handoff.md line 717: "thirteen" -> "fourteen", add to field list
    - handoff.md line 2: "7" -> "14" (stale docstring)
    - hig.md line 614: add to field list
    - ideate.md line 479: add to field list
    - iterate.md line 450: "thirteen" -> "fourteen", add to field list
    - opportunity.md line 469: add to field list
    - system.md line 1849: "thirteen-field" -> "fourteen-field", add to field list

    **For recommend.md (special case — named placeholders):**
    - Change A: Append `,"hasStitchWireframes":{current_hasStitchWireframes}` before the closing `}'` in the manifest-set-top-level designCoverage call (lines 585-586)
    - Change B: Add a row for hasStitchWireframes to the field table, update "13-field" -> "14-field" (lines 564-580)

    DO NOT modify brief.md — it does not have a manifest-set-top-level designCoverage call and must not get one.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && echo "=== Workflow grep ===" && grep -l "hasStitchWireframes" workflows/wireframe.md workflows/mockup.md workflows/system.md workflows/flows.md workflows/critique.md workflows/iterate.md workflows/handoff.md workflows/ideate.md workflows/competitive.md workflows/opportunity.md workflows/hig.md workflows/recommend.md | wc -l && echo "=== Self-test ===" && node bin/lib/design.cjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "hasStitchWireframes" on all 12 workflow files returns 12 files
    - wireframe.md line 777 contains `,"hasStitchWireframes":{current}` in its manifest-set-top-level call
    - recommend.md contains `,"hasStitchWireframes":{current_hasStitchWireframes}` (named placeholder convention)
    - handoff.md line 2 contains "14" (not "7")
    - wireframe.md contains the word "fourteen" in its coverage prose section
    - No occurrence of "thirteen" remains in coverage field enumeration prose across any of the 12 files
    - brief.md does NOT contain "hasStitchWireframes"
    - node bin/lib/design.cjs --self-test passes (exit code 0)
  </acceptance_criteria>
  <done>All 12 workflow files have hasStitchWireframes in their manifest-set-top-level designCoverage JSON literal, prose field lists updated to 14-field, and design.cjs self-test passes</done>
</task>

</tasks>

<verification>
Run these checks after both tasks complete:

1. **Full grep audit** — All 17 files (12 workflows + 5 JSON) contain hasStitchWireframes:
   ```bash
   grep -l "hasStitchWireframes" workflows/wireframe.md workflows/mockup.md workflows/system.md workflows/flows.md workflows/critique.md workflows/iterate.md workflows/handoff.md workflows/ideate.md workflows/competitive.md workflows/opportunity.md workflows/hig.md workflows/recommend.md templates/design-manifest.json .planning/design/design-manifest.json .planning/pressure-test/fixture-*/design/design-manifest.json | wc -l
   ```
   Expected: 17

2. **No brief.md contamination:**
   ```bash
   grep -c "hasStitchWireframes" workflows/brief.md
   ```
   Expected: 0

3. **Self-test passes:**
   ```bash
   node bin/lib/design.cjs --self-test
   ```
   Expected: exit code 0

4. **No hasStitchWireframes set to true anywhere** (Phase 64 is schema extension only, not activation):
   ```bash
   grep "hasStitchWireframes.*true" templates/design-manifest.json .planning/design/design-manifest.json .planning/pressure-test/fixture-*/design/design-manifest.json workflows/*.md
   ```
   Expected: 0 matches
</verification>

<success_criteria>
- 12 workflow files contain hasStitchWireframes in their manifest-set-top-level designCoverage call
- 5 JSON files (1 template + 1 live + 3 fixtures) contain hasStitchWireframes: false
- brief.md is untouched
- design.cjs self-test passes
- hasStitchWireframes is never set to true (only false or {current}/{current_hasStitchWireframes})
- All existing coverage flags are preserved (no clobbering)
</success_criteria>

<output>
After completion, create `.planning/phases/64-coverage-schema-migration/64-01-SUMMARY.md`
</output>
