---
phase: 110-critique-a11y-deploy-smoke-test
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/deploy.md
  - tests/phase-110/deploy-smoke-test.test.mjs
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03, DEP-04, DEP-05]

must_haves:
  truths:
    - "deploy.md has a post-deploy smoke test step that runs after Gate 4/4 success"
    - "Smoke test navigates to DEPLOY_URL, captures screenshot and AOM snapshot"
    - "Smoke test verifies expected LDP sections (hero, pricing, CTA) are present in AOM tree"
    - "Smoke test retries with exponential backoff (10s/20s/40s) when deploy is not ready"
    - "Smoke test results are written to deploy-manifest.json with screenshot path"
  artifacts:
    - path: "workflows/deploy.md"
      provides: "Post-deploy smoke test step with Playwright navigation, backoff retry, section verification"
      contains: "smoke_test"
    - path: "tests/phase-110/deploy-smoke-test.test.mjs"
      provides: "Nyquist structural tests for DEP-01 through DEP-05"
      exports: ["describe", "test"]
  key_links:
    - from: "workflows/deploy.md Step 5"
      to: "bin/lib/mcp-bridge.cjs playwright:navigate"
      via: "bridge call resolution"
      pattern: "playwright:navigate"
    - from: "workflows/deploy.md Step 5"
      to: "workflows/deploy.md Step 6 manifest"
      via: "SMOKE_PASS and SMOKE_ATTEMPTS variables"
      pattern: "smoke_test"
---

<objective>
Add post-deploy smoke test to deploy.md that navigates to the deployed URL via Playwright MCP, captures screenshot + AOM snapshot, verifies expected LDP sections, retries with exponential backoff, and logs results to deploy-manifest.json.

Purpose: Automated verification that a deployment actually renders the expected landing page sections, catching build failures and missing content before the user manually checks.
Output: Updated workflows/deploy.md with smoke test step, tests/phase-110/deploy-smoke-test.test.mjs
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-critique-a11y-deploy-smoke-test/110-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs from existing codebase -->

From workflows/deploy.md current step structure:
  Step 1/6: Initialize and verify prerequisites
  Step 2/6: Read upstream artifacts (loads $LDP_SECTIONS from LDP Section Map table)
  Step 3/6: Generate scaffold artifacts (with 3 approval gates)
  Step 4/6: Approval-gated Vercel deployment (Gate 4/4 → stores $DEPLOY_URL, $DEPLOY_EXIT)
  Step 5/6: Write deploy-manifest.json
  Step 6/6: Output summary

After modification:
  Steps 1-4 unchanged
  NEW Step 5/7: Post-deploy smoke test (uses $DEPLOY_URL from Step 4)
  Step 6/7: Write deploy-manifest.json (now includes smoke_test key)
  Step 7/7: Output summary (now includes smoke test status)

From bin/lib/mcp-bridge.cjs TOOL_MAP:
  'playwright:navigate'   → 'mcp__playwright__browser_navigate'
  'playwright:screenshot'  → 'mcp__playwright__browser_take_screenshot'
  'playwright:snapshot'    → 'mcp__playwright__browser_snapshot'

From workflows/deploy.md Step 4 success path (line 826-828):
  `If $DEPLOY_EXIT == 0:` stores $DEPLOY_URL
  Coverage flag write follows

From workflows/deploy.md Step 5/6 manifest schema (line 857-895):
  "vercel_deployment" object gets new "smoke_test" key

Existing test pattern from tests/phase-109/wireframe-mockup-screenshots.test.mjs:
  Uses readFileSync to read workflow markdown, then assert.ok on string includes/regex
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Nyquist test scaffold for DEP-01 through DEP-05</name>
  <files>tests/phase-110/deploy-smoke-test.test.mjs</files>
  <read_first>
    - tests/phase-109/wireframe-mockup-screenshots.test.mjs (test pattern to follow)
    - workflows/deploy.md (current state — tests assert against this file's content)
    - .planning/phases/110-critique-a11y-deploy-smoke-test/110-RESEARCH.md (smoke test patterns)
  </read_first>
  <action>
Create `tests/phase-110/deploy-smoke-test.test.mjs` following the Phase 109 test pattern. File reads `workflows/deploy.md` content once at module level via `readFileSync`. Tests are structural:

**DEP-01 tests (deploy.md has smoke test step after Gate 4 success):**
- `deployContent.includes('smoke')` — smoke test concept present
- `/Step [567]\/7.*[Ss]moke/i.test(deployContent)` — smoke test is its own numbered step (Step 5/7 or similar)
- `deployContent.includes('DEPLOY_EXIT')` — smoke step references deploy success gate (already present, confirms ordering)

**DEP-02 tests (navigates, captures screenshot and AOM snapshot):**
- `deployContent.includes('playwright:navigate')` — bridge navigation call present
- `deployContent.includes('playwright:screenshot')` — bridge screenshot call present
- `deployContent.includes('playwright:snapshot')` — bridge AOM snapshot call present
- `deployContent.includes('DEPLOY_URL')` — navigates to deploy URL (already present, confirm smoke step uses it)

**DEP-03 tests (verifies expected sections from LDP spec):**
- `deployContent.includes('LDP_SECTIONS')` — references loaded LDP sections (already present in Step 2)
- `/hero|pricing|cta/i.test(deployContent)` — canonical section names mentioned in smoke verification
- `deployContent.includes('sections_found')` OR `deployContent.includes('SECTION_RESULTS')` — section verification result tracking

**DEP-04 tests (exponential backoff retry):**
- `/10.*20.*40|backoff/i.test(deployContent)` — backoff delays specified
- `/3 attempt|attempt.*3|BACKOFF_DELAYS/i.test(deployContent)` — 3-attempt limit
- `deployContent.includes('sleep')` OR `deployContent.includes('retry')` — retry mechanism present

**DEP-05 tests (results in deploy-manifest.json):**
- `deployContent.includes('smoke_test')` — smoke_test key in manifest schema
- `deployContent.includes('screenshot_path')` — screenshot path recorded
- `deployContent.includes('sections_found')` — found sections recorded
- `deployContent.includes('sections_missing')` — missing sections recorded

All tests should initially FAIL (RED) since deploy.md does not yet contain smoke test patterns. This is expected — Task 2 makes them GREEN.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-110/deploy-smoke-test.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-110/deploy-smoke-test.test.mjs exists and is valid ESM
    - File contains `import { test, describe } from 'node:test'`
    - File contains `describe("DEP-01` and `describe("DEP-02` and `describe("DEP-03` and `describe("DEP-04` and `describe("DEP-05`
    - File reads `workflows/deploy.md` via readFileSync
    - Running `node --test tests/phase-110/deploy-smoke-test.test.mjs` exits with failures (tests RED before implementation)
  </acceptance_criteria>
  <done>Test scaffold exists with structural assertions for all 5 DEP requirements. Tests are RED (expected — deploy.md not yet modified).</done>
</task>

<task type="auto">
  <name>Task 2: Add post-deploy smoke test step to deploy.md</name>
  <files>workflows/deploy.md</files>
  <read_first>
    - workflows/deploy.md (FULL file — understand current structure, LOCKED sections, step numbering)
    - .planning/phases/110-critique-a11y-deploy-smoke-test/110-RESEARCH.md (smoke test architecture, backoff pattern, manifest schema)
    - workflows/wireframe.md (existing Playwright bridge pattern from Step 5d — follow same bridge call style)
    - bin/lib/mcp-bridge.cjs (confirm playwright:navigate, playwright:screenshot, playwright:snapshot keys)
  </read_first>
  <action>
Modify `workflows/deploy.md` in three locations. Do NOT modify any LOCKED sections. The file uses LOCKED markers — respect them.

**Change 1: Renumber steps from 6-step to 7-step flow**

Update ALL step references throughout the file:
- `Step 1/6` → `Step 1/7`
- `Step 2/6` → `Step 2/7`
- `Step 3/6` → `Step 3/7`
- `Step 4/6` → `Step 4/7`
- Old `Step 5/6` (manifest write) → `Step 6/7`
- Old `Step 6/6` (output summary) → `Step 7/7`
- NEW `Step 5/7` inserted between old Steps 4 and 5

**Change 2: Insert new Step 5/7 after the coverage flag write block (after line ~848)**

Insert the following new step between the coverage flag write (end of Step 4 success path) and the manifest write step:

```markdown
---

### Step 5/7: Post-deploy smoke test

Verify the deployed site renders expected LDP sections. This step is informational — a smoke test failure does NOT halt the deploy workflow (the Vercel deployment already succeeded in Step 4).

#### 5a. Probe Playwright availability

```
node --input-type=module <<'EOF'
import { createRequire } from 'module';
const req = createRequire(import.meta.url);
const b = req(`${process.env.CLAUDE_PLUGIN_ROOT}/bin/lib/mcp-bridge.cjs`);
let navigateToolName = '', screenshotToolName = '', snapshotToolName = '';
try {
  navigateToolName   = b.call('playwright:navigate',   { url: 'about:blank' }).toolName;
  screenshotToolName = b.call('playwright:screenshot',  {}).toolName;
  snapshotToolName   = b.call('playwright:snapshot',    {}).toolName;
} catch (err) {
  navigateToolName = screenshotToolName = snapshotToolName = '';
}
process.stdout.write(JSON.stringify({ navigateToolName, screenshotToolName, snapshotToolName }) + '\n');
EOF
```

IF any tool name is empty string:
  SET SMOKE_PASS = "skipped"
  SET SMOKE_ATTEMPTS = 0
  Log: `Step 5/7: Smoke test skipped — Playwright MCP unavailable.`
  Skip to Step 6/7.

#### 5b. Exponential backoff retry loop

```
SMOKE_PASS = false
SMOKE_ATTEMPTS = 0
BACKOFF_DELAYS = [10, 20, 40]   # seconds between attempts
SMOKE_SCREENSHOT_PATH = ".planning/deploy-staging/smoke-screenshot.png"

FOR attempt = 1 to 3:
  SMOKE_ATTEMPTS = attempt
  Log: `  -> Smoke test attempt ${attempt}/3...`

  Navigate to $DEPLOY_URL using {navigateToolName}
  Call {snapshotToolName} to capture AOM tree → store as SMOKE_AOM

  IF SMOKE_AOM contains meaningful content (not Vercel "building" page, not empty):
    Call {screenshotToolName} → save to $SMOKE_SCREENSHOT_PATH

    Parse SMOKE_AOM for expected sections:
      Extract $LDP_SECTIONS (already loaded in Step 2 from LDP Section Map table)
      Key sections to always verify: hero, pricing, CTA
      For each section name in key sections:
        Check if SMOKE_AOM contains a landmark, heading, or region with matching text (case-insensitive)

    SET SECTION_RESULTS = [{section: name, found: true/false}, ...]
    SET SECTIONS_FOUND = [names where found=true]
    SET SECTIONS_MISSING = [names where found=false]

    IF all key sections found:
      SET SMOKE_PASS = true
      Log: `  -> Smoke test PASS — all sections verified: ${SECTIONS_FOUND.join(', ')}`
      BREAK
    ELSE:
      Log: `  -> Smoke test attempt ${attempt} — missing sections: ${SECTIONS_MISSING.join(', ')}`
      IF attempt < 3:
        Log: `     Retrying in ${BACKOFF_DELAYS[attempt-1]}s...`
        sleep ${BACKOFF_DELAYS[attempt-1]}

  ELSE (page not ready):
    Log: `  -> Deploy not ready (build in progress)`
    IF attempt < 3:
      Log: `     Retrying in ${BACKOFF_DELAYS[attempt-1]}s...`
      sleep ${BACKOFF_DELAYS[attempt-1]}

IF NOT SMOKE_PASS AND SMOKE_PASS != "skipped":
  Log: `  -> Smoke test FAIL after 3 attempts — deploy may still be building.`
  Log: `     Check Vercel dashboard manually: ${DEPLOY_URL}`
```

Store SMOKE_PASS, SMOKE_ATTEMPTS, SMOKE_SCREENSHOT_PATH, SECTIONS_FOUND, SECTIONS_MISSING for manifest in Step 6.

Display: `Step 5/7: Smoke test ${SMOKE_PASS ? 'passed' : 'failed'} (${SMOKE_ATTEMPTS} attempt(s)).`
```

**Change 3: Update manifest schema in Step 6/7 (formerly Step 5/6)**

Add `smoke_test` key to the `vercel_deployment` object in the manifest JSON schema:

```json
    "vercel_deployment": {
      "status": "queued",
      "deployment_url": "${DEPLOY_URL}",
      "review_required": true,
      "reviewed": false,
      "notes": "Deployment in progress — check Vercel dashboard for build status. URL is live once the build completes.",
      "smoke_test": {
        "status": "${SMOKE_PASS === true ? 'pass' : SMOKE_PASS === 'skipped' ? 'skipped' : 'fail'}",
        "attempts": ${SMOKE_ATTEMPTS},
        "screenshot_path": "${SMOKE_SCREENSHOT_PATH}",
        "sections_found": ${JSON.stringify(SECTIONS_FOUND || [])},
        "sections_missing": ${JSON.stringify(SECTIONS_MISSING || [])},
        "timestamp": "${ISO8601_TIMESTAMP}"
      }
    }
```

**Change 4: Update output summary in Step 7/7 (formerly Step 6/6)**

Add smoke test status line to the summary box after the Vercel deployment entry:

```
  ✓ Smoke test: ${SMOKE_PASS === true ? 'PASS' : SMOKE_PASS === 'skipped' ? 'SKIPPED (no Playwright)' : 'FAIL'}
      ${SMOKE_PASS === true ? 'Sections verified: ' + SECTIONS_FOUND.join(', ') : ''}
      ${SMOKE_PASS === 'skipped' ? '' : 'Screenshot: ' + SMOKE_SCREENSHOT_PATH}
```

CRITICAL constraints:
- Do NOT modify any content between `<!-- LOCKED:` and `<!-- /LOCKED -->` markers
- The smoke test is INFORMATIONAL — it must never HALT the workflow on failure
- All backoff sleeps use literal values: 10, 20, 40 seconds
- The smoke test only runs if $DEPLOY_EXIT == 0 (inside the success path)
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-110/deploy-smoke-test.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - workflows/deploy.md contains `smoke` (smoke test concept)
    - workflows/deploy.md contains `Step 5/7` with smoke test heading
    - workflows/deploy.md contains `playwright:navigate` (bridge navigation)
    - workflows/deploy.md contains `playwright:screenshot` (bridge screenshot)
    - workflows/deploy.md contains `playwright:snapshot` (bridge AOM snapshot)
    - workflows/deploy.md contains `10, 20, 40` or `10.*20.*40` (backoff delays)
    - workflows/deploy.md contains `3` in context of attempt limit
    - workflows/deploy.md contains `smoke_test` (manifest key)
    - workflows/deploy.md contains `screenshot_path` (manifest field)
    - workflows/deploy.md contains `sections_found` (manifest field)
    - workflows/deploy.md contains `sections_missing` (manifest field)
    - workflows/deploy.md contains `hero` and `pricing` and `CTA` or `cta` (canonical sections)
    - All tests in `node --test tests/phase-110/deploy-smoke-test.test.mjs` pass (exit 0)
    - LOCKED markers still present and content between them unmodified
    - Step count is 7 (was 6) — verify `Step 7/7` exists
  </acceptance_criteria>
  <done>deploy.md has a post-deploy smoke test step with Playwright navigation, exponential backoff retry (10s/20s/40s, 3 attempts), LDP section verification, and manifest result logging. All DEP-01 through DEP-05 Nyquist tests GREEN.</done>
</task>

</tasks>

<verification>
- `node --test tests/phase-110/deploy-smoke-test.test.mjs` — all pass
- `grep -c 'playwright:navigate' workflows/deploy.md` returns >= 1
- `grep -c 'smoke_test' workflows/deploy.md` returns >= 1
- `grep -c 'BACKOFF_DELAYS' workflows/deploy.md` returns >= 1
- `grep -c 'sections_found' workflows/deploy.md` returns >= 1
- `grep -c 'Step 7/7' workflows/deploy.md` returns >= 1
</verification>

<success_criteria>
deploy.md has a post-deploy smoke test (Step 5/7) that probes Playwright, retries with exponential backoff, verifies LDP sections via AOM, captures screenshot, and logs results to deploy-manifest.json. All 5 DEP structural tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/110-critique-a11y-deploy-smoke-test/110-02-SUMMARY.md`
</output>
