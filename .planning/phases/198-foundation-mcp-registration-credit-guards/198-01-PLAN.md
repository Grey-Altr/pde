---
phase: 198-foundation-mcp-registration-credit-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/mcp-bridge.cjs
  - references/mcp-integration.md
  - workflows/connect.md
  - tests/phase-198/mcp-bridge-firecrawl.test.mjs
autonomous: true
requirements: [FND-01, FND-02]
must_haves:
  truths:
    - "APPROVED_SERVERS contains a 'firecrawl' entry with correct transport, probeTool, and probeArgs"
    - "TOOL_MAP contains 12 firecrawl:* canonical entries mapping to mcp__firecrawl__* raw names"
    - "AUTH_INSTRUCTIONS.firecrawl provides 6-step setup instructions referencing env var, npx, and /pde:connect"
    - "connect.md lists firecrawl as an approved service"
    - "mcp-integration.md flag table includes --no-firecrawl"
    - "No Firecrawl API key or URL-with-key appears in any version-controlled file"
  artifacts:
    - path: "bin/lib/mcp-bridge.cjs"
      provides: "APPROVED_SERVERS.firecrawl, 12 TOOL_MAP entries, AUTH_INSTRUCTIONS.firecrawl"
      contains: "firecrawl:"
    - path: "references/mcp-integration.md"
      provides: "--no-firecrawl flag entry"
      contains: "--no-firecrawl"
    - path: "workflows/connect.md"
      provides: "firecrawl in approved services list"
      contains: "firecrawl"
    - path: "tests/phase-198/mcp-bridge-firecrawl.test.mjs"
      provides: "Unit tests for APPROVED_SERVERS.firecrawl, TOOL_MAP entries, AUTH_INSTRUCTIONS"
  key_links:
    - from: "bin/lib/mcp-bridge.cjs"
      to: "APPROVED_SERVERS object"
      via: "firecrawl key in object literal"
      pattern: "firecrawl:\\s*\\{"
    - from: "bin/lib/mcp-bridge.cjs"
      to: "TOOL_MAP object"
      via: "12 firecrawl:* entries"
      pattern: "'firecrawl:"
---

<objective>
Register Firecrawl as the ninth approved MCP server in mcp-bridge.cjs with full TOOL_MAP coverage, AUTH_INSTRUCTIONS, and documentation updates.

Purpose: Firecrawl must be a recognized, probe-ready MCP server before any credit guard or workflow integration can reference it. This plan establishes the registration foundation.
Output: APPROVED_SERVERS.firecrawl entry, 12 TOOL_MAP entries, AUTH_INSTRUCTIONS.firecrawl, updated connect.md and mcp-integration.md, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/198-foundation-mcp-registration-credit-guards/198-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From bin/lib/mcp-bridge.cjs (APPROVED_SERVERS pattern — use stitch as template):
```javascript
stitch: {
  displayName: 'Google Stitch',
  transport: 'stdio',
  url: null,
  installCmd: null, // Multi-step: env var + npx — see AUTH_INSTRUCTIONS
  probeTimeoutMs: 15000,
  probeTool: 'mcp__stitch__list_projects',
  probeArgs: {},
  container: { image: 'node:20-slim', startupMs: 3000, cmd: ['npx', '@_davideast/stitch-mcp', 'proxy'] },
},
```

From bin/lib/mcp-bridge.cjs (TOOL_MAP pattern):
```javascript
'stitch:probe': 'mcp__stitch__list_projects',  // TOOL_MAP_VERIFIED
'stitch:create-project': 'mcp__stitch__create_project',  // TOOL_MAP_VERIFIED
```

From bin/lib/mcp-bridge.cjs (AUTH_INSTRUCTIONS pattern — stitch has 7 steps):
```javascript
stitch: [
  '1. Go to stitch.withgoogle.com -> click your profile icon -> Settings -> API Key section',
  ...
  '7. Return here and run /pde:connect stitch --confirm',
],
```

From bin/lib/mcp-bridge.cjs (module.exports — line 749):
```javascript
module.exports = {
  APPROVED_SERVERS, TOOL_MAP, DYNAMIC_SERVERS, AUTH_INSTRUCTIONS,
  CONNECTIONS_PATH, assertApproved, loadDynamicServers, registerDynamicServer,
  loadConnections, saveConnections, getStatus, getAllStatuses, probe, call,
  updateConnectionStatus, readStitchQuota, incrementStitchQuota, checkStitchQuota,
  isDockerAvailable, getInstallCmd, getProbeTimeoutMs,
};
```

From tests/phase-65/stitch-bridge-registration.test.mjs (test pattern):
```javascript
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const bridge = require('../../bin/lib/mcp-bridge.cjs');
const { APPROVED_SERVERS, AUTH_INSTRUCTIONS, assertApproved, probe } = bridge;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test scaffold for Firecrawl MCP registration</name>
  <files>tests/phase-198/mcp-bridge-firecrawl.test.mjs</files>
  <read_first>
    - tests/phase-65/stitch-bridge-registration.test.mjs
    - tests/phase-65/tool-map-stitch.test.mjs
    - bin/lib/mcp-bridge.cjs
  </read_first>
  <behavior>
    - APPROVED_SERVERS.firecrawl exists with displayName "Firecrawl"
    - APPROVED_SERVERS.firecrawl.transport === "stdio"
    - APPROVED_SERVERS.firecrawl.url === null
    - APPROVED_SERVERS.firecrawl.installCmd === null
    - APPROVED_SERVERS.firecrawl.probeTimeoutMs === 15000
    - APPROVED_SERVERS.firecrawl.probeTool === "mcp__firecrawl__search" (TOOL_MAP_VERIFY_REQUIRED)
    - APPROVED_SERVERS.firecrawl.probeArgs deep equals { query: "test", limit: 1 }
    - TOOL_MAP contains exactly 12 firecrawl:* entries
    - TOOL_MAP['firecrawl:probe'] === 'mcp__firecrawl__search' (TOOL_MAP_VERIFY_REQUIRED)
    - TOOL_MAP['firecrawl:scrape'] === 'mcp__firecrawl__scrape' (TOOL_MAP_VERIFY_REQUIRED)
    - TOOL_MAP['firecrawl:search'] === 'mcp__firecrawl__search' (TOOL_MAP_VERIFY_REQUIRED)
    - TOOL_MAP['firecrawl:map'] === 'mcp__firecrawl__map' (TOOL_MAP_VERIFY_REQUIRED)
    - TOOL_MAP['firecrawl:crawl'] === 'mcp__firecrawl__crawl' (TOOL_MAP_VERIFY_REQUIRED)
    - TOOL_MAP['firecrawl:check-crawl-status'] === 'mcp__firecrawl__check_crawl_status' (TOOL_MAP_VERIFY_REQUIRED)
    - TOOL_MAP['firecrawl:extract'] === 'mcp__firecrawl__extract' (TOOL_MAP_VERIFY_REQUIRED)
    - TOOL_MAP['firecrawl:agent'] === 'mcp__firecrawl__agent' (TOOL_MAP_VERIFY_REQUIRED)
    - TOOL_MAP['firecrawl:agent-status'] === 'mcp__firecrawl__agent_status' (TOOL_MAP_VERIFY_REQUIRED)
    - TOOL_MAP['firecrawl:interact'] === 'mcp__firecrawl__interact' (TOOL_MAP_VERIFY_REQUIRED)
    - TOOL_MAP['firecrawl:browser-create'] === 'mcp__firecrawl__browser_create' (TOOL_MAP_VERIFY_REQUIRED, deprecated)
    - TOOL_MAP['firecrawl:browser-delete'] === 'mcp__firecrawl__browser_delete' (TOOL_MAP_VERIFY_REQUIRED, deprecated)
    - AUTH_INSTRUCTIONS.firecrawl is an array with 6 elements
    - AUTH_INSTRUCTIONS.firecrawl[0] contains "firecrawl.dev/app/api-keys"
    - AUTH_INSTRUCTIONS.firecrawl[3] contains "npx -y firecrawl-mcp"
    - AUTH_INSTRUCTIONS.firecrawl[5] contains "/pde:connect firecrawl --confirm"
    - assertApproved('firecrawl') does not throw
    - probe('firecrawl') returns object with probeTool field (non-live test — just validate it returns probe_deferred shape)
  </behavior>
  <action>
Create `tests/phase-198/mcp-bridge-firecrawl.test.mjs` following the exact pattern from `tests/phase-65/stitch-bridge-registration.test.mjs`.

File structure:
1. Imports: `node:test` (test, describe), `node:assert/strict`, `module` (createRequire)
2. Require `../../bin/lib/mcp-bridge.cjs`, destructure `APPROVED_SERVERS`, `AUTH_INSTRUCTIONS`, `TOOL_MAP`, `assertApproved`, `probe`
3. Describe block "FND-01: APPROVED_SERVERS firecrawl entry" with individual tests for each field
4. Describe block "FND-01: TOOL_MAP firecrawl entries" — test count (filter TOOL_MAP keys starting with 'firecrawl:' and assert length === 12) and each individual mapping
5. Describe block "FND-02: AUTH_INSTRUCTIONS firecrawl" — array length 6, key content checks
6. Test "assertApproved('firecrawl') does not throw" — wrap in assert.doesNotThrow
7. Test "probe('firecrawl') returns probe_deferred" — call probe('firecrawl'), assert result has probeTool field

Create `tests/phase-198/` directory first. All tests should RED (fail) at this point since firecrawl is not yet registered.
  </action>
  <verify>
    <automated>node --test tests/phase-198/mcp-bridge-firecrawl.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-198/mcp-bridge-firecrawl.test.mjs exists and contains `describe('FND-01: APPROVED_SERVERS firecrawl entry'`
    - tests/phase-198/mcp-bridge-firecrawl.test.mjs contains `describe('FND-01: TOOL_MAP firecrawl entries'`
    - tests/phase-198/mcp-bridge-firecrawl.test.mjs contains `describe('FND-02: AUTH_INSTRUCTIONS firecrawl'`
    - tests/phase-198/mcp-bridge-firecrawl.test.mjs contains `import { test, describe } from 'node:test'`
    - Running `node --test tests/phase-198/mcp-bridge-firecrawl.test.mjs` produces test output (failures expected — RED phase)
  </acceptance_criteria>
  <done>Test file exists with all behavior assertions. Tests fail because firecrawl is not yet registered (RED phase confirmed).</done>
</task>

<task type="auto">
  <name>Task 2: Register Firecrawl in mcp-bridge.cjs and update documentation</name>
  <files>bin/lib/mcp-bridge.cjs, references/mcp-integration.md, workflows/connect.md</files>
  <read_first>
    - bin/lib/mcp-bridge.cjs
    - references/mcp-integration.md
    - workflows/connect.md
    - .planning/phases/198-foundation-mcp-registration-credit-guards/198-RESEARCH.md
  </read_first>
  <action>
**In bin/lib/mcp-bridge.cjs:**

1. Add firecrawl entry to APPROVED_SERVERS (after playwright, before pde_remote — line ~110):
```javascript
firecrawl: {
  displayName: 'Firecrawl',
  transport: 'stdio',
  url: null,  // Dynamic — npx transport, no URL needed; HTTP alternative embeds API key in URL (never store)
  installCmd: null,  // Multi-step: env var + npx — see AUTH_INSTRUCTIONS
  probeTimeoutMs: 15000,
  probeTool: 'mcp__firecrawl__search',  // TOOL_MAP_VERIFY_REQUIRED — lightest read-only tool (0.2 credits)
  probeArgs: { query: 'test', limit: 1 },
},
```

2. Add 12 TOOL_MAP entries after the Playwright section (after line ~240, before the closing `};`):
```javascript
// Firecrawl — Phase 198 (tool names from docs.firecrawl.dev/mcp-server; VERIFY after first live probe)
'firecrawl:probe':              'mcp__firecrawl__search',               // TOOL_MAP_VERIFY_REQUIRED
'firecrawl:scrape':             'mcp__firecrawl__scrape',               // TOOL_MAP_VERIFY_REQUIRED
'firecrawl:search':             'mcp__firecrawl__search',               // TOOL_MAP_VERIFY_REQUIRED
'firecrawl:map':                'mcp__firecrawl__map',                  // TOOL_MAP_VERIFY_REQUIRED
'firecrawl:crawl':              'mcp__firecrawl__crawl',                // TOOL_MAP_VERIFY_REQUIRED
'firecrawl:check-crawl-status': 'mcp__firecrawl__check_crawl_status',   // TOOL_MAP_VERIFY_REQUIRED
'firecrawl:extract':            'mcp__firecrawl__extract',              // TOOL_MAP_VERIFY_REQUIRED
'firecrawl:agent':              'mcp__firecrawl__agent',                // TOOL_MAP_VERIFY_REQUIRED
'firecrawl:agent-status':       'mcp__firecrawl__agent_status',         // TOOL_MAP_VERIFY_REQUIRED
'firecrawl:interact':           'mcp__firecrawl__interact',             // TOOL_MAP_VERIFY_REQUIRED
'firecrawl:browser-create':     'mcp__firecrawl__browser_create',       // TOOL_MAP_VERIFY_REQUIRED — deprecated, use firecrawl:interact
'firecrawl:browser-delete':     'mcp__firecrawl__browser_delete',       // TOOL_MAP_VERIFY_REQUIRED — deprecated, use firecrawl:interact
```

3. Add AUTH_INSTRUCTIONS.firecrawl (after playwright entry, before closing `};` of AUTH_INSTRUCTIONS object):
```javascript
firecrawl: [
  '1. Get your API key from https://www.firecrawl.dev/app/api-keys',
  '2. Add export FIRECRAWL_API_KEY="fc-your-api-key" to your shell profile (~/.zshrc or ~/.bashrc)',
  '3. Restart your terminal or run: source ~/.zshrc',
  '4. Register Firecrawl MCP server: claude mcp add firecrawl -e FIRECRAWL_API_KEY=$FIRECRAWL_API_KEY -- npx -y firecrawl-mcp',
  '5. Verify Firecrawl appears in Claude Code MCP list: run /mcp in Claude Code',
  '6. Return here and run /pde:connect firecrawl --confirm',
],
```

**In references/mcp-integration.md:**

Add `--no-firecrawl` row to the flag table (currently at lines 35-43). Add after `--no-reference-mcp` row:
```
| `--no-firecrawl` | Firecrawl MCP | Skip Firecrawl web scraping when offline or credits exhausted |
```

**In workflows/connect.md:**

Update the approved services list on line 18 from:
```
Approved services: github, linear, figma, pencil, atlassian, stitch, greptile
```
to:
```
Approved services: github, linear, figma, pencil, atlassian, stitch, greptile, firecrawl
```

Also update the policy violation message list on line 52 if it lists specific servers — add firecrawl there too.
  </action>
  <verify>
    <automated>node --test tests/phase-198/mcp-bridge-firecrawl.test.mjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - bin/lib/mcp-bridge.cjs contains `firecrawl: {` inside APPROVED_SERVERS object
    - bin/lib/mcp-bridge.cjs contains `displayName: 'Firecrawl'`
    - bin/lib/mcp-bridge.cjs contains `probeTool: 'mcp__firecrawl__search'`
    - bin/lib/mcp-bridge.cjs contains `probeArgs: { query: 'test', limit: 1 }`
    - bin/lib/mcp-bridge.cjs contains `'firecrawl:scrape':` and 11 other firecrawl:* TOOL_MAP entries
    - bin/lib/mcp-bridge.cjs contains `AUTH_INSTRUCTIONS` block with `firecrawl:` key
    - references/mcp-integration.md contains `--no-firecrawl`
    - workflows/connect.md contains `firecrawl` in the approved services line
    - `node --test tests/phase-198/mcp-bridge-firecrawl.test.mjs` exits 0 (all tests pass — GREEN phase)
    - No occurrence of `fc-` (literal API key) in bin/lib/mcp-bridge.cjs
  </acceptance_criteria>
  <done>Firecrawl is registered in APPROVED_SERVERS with 12 TOOL_MAP entries and AUTH_INSTRUCTIONS. All registration tests pass. connect.md and mcp-integration.md updated. No API key stored in version-controlled files.</done>
</task>

</tasks>

<verification>
- `node --test tests/phase-198/mcp-bridge-firecrawl.test.mjs` — all tests pass
- `grep -c "firecrawl:" bin/lib/mcp-bridge.cjs` — returns 13+ (1 APPROVED_SERVERS + 12 TOOL_MAP)
- `grep "firecrawl" workflows/connect.md` — appears in approved services list
- `grep "no-firecrawl" references/mcp-integration.md` — flag table entry exists
- `grep -r "fc-" bin/ workflows/ references/` — no literal API keys in version-controlled files
</verification>

<success_criteria>
- APPROVED_SERVERS.firecrawl entry exists with all required fields
- 12 TOOL_MAP entries mapping firecrawl:* to mcp__firecrawl__* names
- AUTH_INSTRUCTIONS.firecrawl with 6-step setup guide
- --no-firecrawl in mcp-integration.md flag table
- firecrawl in connect.md approved services list
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/198-foundation-mcp-registration-credit-guards/198-01-SUMMARY.md`
</output>
