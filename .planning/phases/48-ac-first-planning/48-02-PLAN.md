---
phase: 48-ac-first-planning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/plan-phase.md
  - workflows/execute-plan.md
autonomous: true
requirements: [PLAN-03, PLAN-04, PLAN-05]

must_haves:
  truths:
    - "Planner prompt instructs generation of plan-level AC block with BDD Given/When/Then format and AC-N IDs"
    - "Planner prompt requires every task to have ac_refs referencing at least one AC-N"
    - "Planner prompt requires every AC-N to be referenced by at least one task"
    - "Planner prompt describes optional boundaries field for task-level path protection"
    - "Executor verifies AC-N behavioral outcomes before marking tasks done"
    - "Executor checks boundaries before executing a task and warns if overlap detected"
    - "Quality gate includes AC coverage checks"
    - "Downstream consumer note mentions AC section"
  artifacts:
    - path: "workflows/plan-phase.md"
      provides: "Planner instructions for AC block, ac_refs, boundaries"
      contains: "acceptance_criteria"
    - path: "workflows/execute-plan.md"
      provides: "Executor AC-N verification and boundaries enforcement"
      contains: "MANDATORY AC-N verification"
  key_links:
    - from: "workflows/plan-phase.md"
      to: "workflows/execute-plan.md"
      via: "AC-N schema: planner emits, executor verifies"
      pattern: "AC-N"
    - from: "workflows/plan-phase.md"
      to: "bin/lib/sharding.cjs"
      via: "ac_refs and boundaries fields extracted by sharding"
      pattern: "ac_refs"
---

<objective>
Update the planner and executor workflow prompts to support acceptance-criteria-first planning: the planner generates plan-level BDD acceptance criteria with AC-N identifiers and per-task ac_refs/boundaries fields; the executor enforces AC-N verification before marking tasks done and respects boundary constraints.

Purpose: Closes the instruction loop so that plans generated after Phase 48 include behavioral acceptance criteria that are traceable to tasks and verifiable by the executor.
Output: Updated plan-phase.md and execute-plan.md workflow files.
</objective>

<execution_context>
@workflows/execute-plan.md
@templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-ac-first-planning/48-RESEARCH.md
</context>

<acceptance_criteria>

**AC-1:** Given a planner agent reading plan-phase.md, when it processes the deep_work_rules section, then it finds items 4, 5, and 6 describing plan-level acceptance_criteria block format (BDD Given/When/Then with AC-N IDs), per-task ac_refs field (comma-separated AC-N references), and optional per-task boundaries field (protected paths)

**AC-2:** Given a planner agent reading plan-phase.md, when it checks the quality_gate section, then it finds three new checklist items requiring: plan-level AC block exists before tasks, every task has ac_refs, every AC-N is referenced by at least one task

**AC-3:** Given an executor agent reading execute-plan.md, when it processes the execute step, then it finds a MANDATORY AC-N verification instruction that requires verifying referenced AC-N "then" clauses before marking a task done

**AC-4:** Given an executor agent reading execute-plan.md, when it processes the execute step, then it finds a MANDATORY boundaries check instruction that warns before modifying paths listed in a task's boundaries field

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add AC-first rules to plan-phase.md planner prompt</name>
  <files>workflows/plan-phase.md</files>
  <ac_refs>AC-1, AC-2</ac_refs>
  <read_first>
    - workflows/plan-phase.md (full file — see current deep_work_rules at lines 390-417, quality_gate at lines 419-429, downstream_consumer at lines 382-388)
    - .planning/phases/48-ac-first-planning/48-RESEARCH.md (sections: "Planner prompt addition for deep_work_rules" and "New item in quality_gate")
  </read_first>
  <action>
Make three targeted edits to `workflows/plan-phase.md`:

**Edit 1 — deep_work_rules (after item 3, before the "Why this matters" paragraph, around line 415):**

Add these three new items:

```
4. **`<acceptance_criteria>` (plan-level, BEFORE `<tasks>`)** — BDD-format behavioral
   statements that define what success looks like for the plan as a whole. Rules:
   - Place the block AFTER `<context>` and BEFORE `<tasks>` — this is the definition,
     not a per-task check
   - Each entry: `**AC-N:** Given {condition}, when {action}, then {observable outcome}`
   - Sequential numbering starting at AC-1, unique within the plan
   - The "then" clause must be observable/verifiable (not subjective)
   - Typical count: 3-8 ACs per plan (one per significant capability delivered)
   - If adding new ACs during revision, append with higher numbers (do not renumber
     existing ACs that tasks already reference)

5. **`<ac_refs>` per task** — lists which plan-level AC-N identifiers this task satisfies.
   Rules:
   - Every task MUST reference at least one AC-N
   - Every AC-N in the plan's <acceptance_criteria> block MUST appear in some task's
     <ac_refs> (full coverage required — no orphaned ACs)
   - Format: comma-separated (e.g., `AC-1, AC-3`) — exact match, case-sensitive

6. **`<boundaries>` per task (optional)** — lists paths/sections this task must NOT modify.
   Rules:
   - Use when a task is adjacent to fragile/recently-changed code that must not regress
   - Each entry is a file path or a comment describing a section (e.g.,
     `# Phase 47 sharding logic in sharding.cjs`)
   - Executor will warn and require confirmation before modifying listed paths
```

**Edit 2 — quality_gate (add 3 items after the existing `must_haves` item, before the closing `</quality_gate>`):**

Add:
```
- [ ] Plan-level `<acceptance_criteria>` block exists BEFORE `<tasks>`, with each AC in **AC-N:** Given/When/Then format
- [ ] Every task has `<ac_refs>` referencing at least one AC-N
- [ ] Every AC-N in the plan-level block is referenced by at least one task
```

**Edit 3 — downstream_consumer (add one bullet to the existing list):**

Add after the `must_haves` bullet:
```
- Plan-level `<acceptance_criteria>` block with BDD AC-N entries; tasks have `<ac_refs>` and optional `<boundaries>`
```
  </action>
  <verify>
    <automated>grep -c "ac_refs" workflows/plan-phase.md | grep -q "[3-9]" && grep -c "boundaries" workflows/plan-phase.md | grep -q "[2-9]" && grep -c "AC-N" workflows/plan-phase.md | grep -q "[3-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/plan-phase.md deep_work_rules section contains item `4. **\`<acceptance_criteria>\` (plan-level`
    - workflows/plan-phase.md deep_work_rules section contains item `5. **\`<ac_refs>\` per task**`
    - workflows/plan-phase.md deep_work_rules section contains item `6. **\`<boundaries>\` per task (optional)**`
    - workflows/plan-phase.md quality_gate contains `Plan-level \`<acceptance_criteria>\` block exists BEFORE \`<tasks>\``
    - workflows/plan-phase.md quality_gate contains `Every task has \`<ac_refs>\` referencing at least one AC-N`
    - workflows/plan-phase.md quality_gate contains `Every AC-N in the plan-level block is referenced by at least one task`
    - workflows/plan-phase.md downstream_consumer contains `acceptance_criteria` and `ac_refs` and `boundaries`
  </acceptance_criteria>
  <done>plan-phase.md deep_work_rules has 6 items (was 3), quality_gate has 3 new AC checks, downstream_consumer mentions AC section</done>
</task>

<task type="auto">
  <name>Task 2: Add AC-N verification and boundaries enforcement to execute-plan.md</name>
  <files>workflows/execute-plan.md</files>
  <ac_refs>AC-3, AC-4</ac_refs>
  <read_first>
    - workflows/execute-plan.md (full file — see MANDATORY acceptance_criteria check at line 142, execute step starting at line 134)
    - .planning/phases/48-ac-first-planning/48-RESEARCH.md (sections: "execute-plan.md MANDATORY AC-N verification extension" and "execute-plan.md boundaries enforcement")
  </read_first>
  <action>
Make two targeted edits to `workflows/execute-plan.md`:

**Edit 1 — Extend the MANDATORY acceptance_criteria check (line 142):**

The current text at line 142 is:
```
   - **MANDATORY acceptance_criteria check:** After completing each task, if it has `<acceptance_criteria>`, verify EVERY criterion before moving to the next task. Use grep, file reads, or CLI commands to confirm each criterion. If any criterion fails, fix the implementation before proceeding. Do not skip criteria or mark them as "will verify later".
```

Replace it with:
```
   - **MANDATORY acceptance_criteria check:** After completing each task, if it has `<acceptance_criteria>`, verify EVERY criterion before moving to the next task. Use grep, file reads, or CLI commands to confirm each criterion. If any criterion fails, fix the implementation before proceeding. Do not skip criteria or mark them as "will verify later".
   - **MANDATORY AC-N verification:** After completing each task, if it has `<ac_refs>` (or an "**ACs this task satisfies:**" line in a task file), retrieve the corresponding AC-N entries from the plan's `<acceptance_criteria>` block (in the task file under "## Plan Acceptance Criteria (Reference)" or in PLAN.md directly if not sharded). For each referenced AC-N, verify that the "then" clause is true — the behavioral outcome must be observable. If any referenced AC is not satisfied, the task is NOT done — fix the implementation first. Do not move to the next task with an unsatisfied AC.
```

**Edit 2 — Add boundaries check after the read_first gate (after line 139):**

After the MANDATORY read_first gate bullet (line 139), add a new bullet:
```
   - **MANDATORY boundaries check:** Before executing a task, if it has a `<boundaries>` field (or "## Task Boundaries (DO NOT CHANGE)" section in a task file), check whether your planned changes would touch any listed path. If yes: STOP. Log: "BOUNDARY WARNING: Task {N} boundaries field lists {path} as DO NOT CHANGE. This task would modify that path. Confirm to proceed or adjust scope." Wait for user confirmation before modifying a listed path. If no conflict: proceed normally — boundaries are informational, not restrictive when no overlap exists.
```
  </action>
  <verify>
    <automated>grep -c "MANDATORY AC-N verification" workflows/execute-plan.md | grep -q "1" && grep -c "MANDATORY boundaries check" workflows/execute-plan.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/execute-plan.md contains `**MANDATORY AC-N verification:**`
    - workflows/execute-plan.md contains `retrieve the corresponding AC-N entries from the plan`
    - workflows/execute-plan.md contains `verify that the "then" clause is true`
    - workflows/execute-plan.md contains `**MANDATORY boundaries check:**`
    - workflows/execute-plan.md contains `BOUNDARY WARNING`
    - workflows/execute-plan.md contains `Wait for user confirmation before modifying a listed path`
  </acceptance_criteria>
  <done>execute-plan.md has AC-N verification instruction after the existing acceptance_criteria check, and boundaries enforcement instruction after the read_first gate</done>
</task>

</tasks>

<verification>
```bash
# Verify plan-phase.md changes
grep "ac_refs" workflows/plan-phase.md | wc -l  # Should be >= 3
grep "boundaries" workflows/plan-phase.md | wc -l  # Should be >= 2
grep "AC-N" workflows/plan-phase.md | wc -l  # Should be >= 3

# Verify execute-plan.md changes
grep "MANDATORY AC-N verification" workflows/execute-plan.md  # Should exist
grep "MANDATORY boundaries check" workflows/execute-plan.md  # Should exist
grep "BOUNDARY WARNING" workflows/execute-plan.md  # Should exist
```
</verification>

<success_criteria>
- plan-phase.md deep_work_rules has items 4, 5, 6 for AC block, ac_refs, boundaries
- plan-phase.md quality_gate has 3 new AC coverage checks
- plan-phase.md downstream_consumer mentions AC section
- execute-plan.md has MANDATORY AC-N verification instruction
- execute-plan.md has MANDATORY boundaries check instruction
- Both files remain syntactically valid markdown with proper structure
</success_criteria>

<output>
After completion, create `.planning/phases/48-ac-first-planning/48-02-SUMMARY.md`
</output>
