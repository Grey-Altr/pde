---
phase: 186-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - TST-01
  - TST-02

must_haves:
  truths:
    - "npx vitest run reports zero 'No test suite found' entries"
    - "npx vitest run --coverage produces a coverage report in ./coverage/ with per-module line/branch percentages"
    - "node:test files remain runnable via node --test (not deleted or disabled)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config with node:test exclusions and coverage block"
      contains: "phase-[4-9][0-9]"
    - path: "package.json"
      provides: "@vitest/coverage-v8 devDependency"
      contains: "@vitest/coverage-v8"
  key_links:
    - from: "vitest.config.ts"
      to: "tests/phase-*/**"
      via: "exclude glob patterns"
      pattern: "exclude.*phase-"
    - from: "vitest.config.ts"
      to: "bin/lib/**/*.cjs"
      via: "coverage.include"
      pattern: "coverage.*bin/lib"
---

<objective>
Configure vitest to exclude node:test files and produce a coverage baseline report.

Purpose: Eliminate 135 false "No test suite found" failures so vitest provides a reliable regression signal before any code changes in later phases.
Output: Updated vitest.config.ts with exclude globs and coverage config; @vitest/coverage-v8 installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/186-test-infrastructure/186-RESEARCH.md
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @vitest/coverage-v8 and add exclude globs to vitest config</name>
  <files>vitest.config.ts, package.json, package-lock.json</files>
  <read_first>vitest.config.ts, package.json, .planning/phases/186-test-infrastructure/186-RESEARCH.md</read_first>
  <action>
1. Install coverage provider (must match vitest version exactly):
   ```bash
   npm install --save-dev @vitest/coverage-v8@4.1.1
   ```
   Verify installation: `npm ls @vitest/coverage-v8` should show 4.1.1.

2. Update vitest.config.ts to the following complete config (replace entire file):
   ```typescript
   import { defineConfig } from 'vitest/config';
   export default defineConfig({
     test: {
       include: ['tests/**/*.{test,spec}.{cjs,mjs,js,ts}', 'tests/**/test-*.cjs'],
       exclude: [
         'tests/phase-[4-9][0-9]/**',
         'tests/phase-1[0-2][0-9]/**',
         'tests/phase-13[0-3]/**',
       ],
       globals: true,
       testTimeout: 15000,
       server: {
         deps: {
           inline: ['zod'],
         },
       },
       coverage: {
         provider: 'v8',
         include: ['bin/lib/**/*.cjs'],
       },
     },
   });
   ```

   The three exclude patterns cover all node:test phase directories (phase-40 through phase-133) without touching vitest phases (phase-134, phase-134.1, phase-136.1, phase-163+). Research confirmed this boundary via complete file audit.

   IMPORTANT: Do NOT add `coverage.enabled: true` -- coverage should only run when `--coverage` flag is passed. Do NOT delete or modify any node:test files. Do NOT change the existing `include` patterns or `server.deps.inline` config.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && npm ls @vitest/coverage-v8 && grep -c "phase-\[4-9\]" vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls @vitest/coverage-v8` shows `@vitest/coverage-v8@4.1.1`
    - vitest.config.ts contains the string `tests/phase-[4-9][0-9]/**`
    - vitest.config.ts contains the string `tests/phase-1[0-2][0-9]/**`
    - vitest.config.ts contains the string `tests/phase-13[0-3]/**`
    - vitest.config.ts contains `provider: 'v8'`
    - vitest.config.ts contains `include: ['bin/lib/**/*.cjs']` inside coverage block
    - vitest.config.ts still contains `inline: ['zod']`
    - vitest.config.ts does NOT contain `enabled: true` in coverage block
  </acceptance_criteria>
  <done>@vitest/coverage-v8@4.1.1 installed; vitest.config.ts has three exclude globs for node:test phases and a coverage block targeting bin/lib</done>
</task>

<task type="auto">
  <name>Task 2: Verify zero false failures and generate coverage baseline</name>
  <files>vitest.config.ts</files>
  <read_first>vitest.config.ts</read_first>
  <action>
1. Run vitest and confirm zero "No test suite found" entries:
   ```bash
   npx vitest run 2>&1 | grep -c "No test suite found"
   ```
   Expected output: `0`. If any remain, inspect which file was not excluded and add a targeted exclude pattern for it. The three globs should cover all 135 node:test files.

2. Verify node:test files still work independently (spot check):
   ```bash
   node --test tests/phase-44/concurrency-isolation.test.mjs 2>&1 | head -5
   ```
   Should show TAP output or test results, not an error about missing files.

3. Run vitest with coverage to generate baseline:
   ```bash
   npx vitest run --coverage
   ```
   This should complete without errors and produce a `./coverage/` directory.

4. Verify coverage output exists:
   ```bash
   ls coverage/index.html
   ```
   Should show the HTML coverage report file.

5. Document the results: Note the total vitest test count (pass/fail), and confirm coverage report was generated. Real test failures (e.g., git/env issues in worktree tests) are out of scope -- only "No test suite found" false failures matter for TST-01.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine && npx vitest run 2>&1 | grep -c "No test suite found" && ls coverage/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run 2>&1 | grep -c "No test suite found"` outputs `0`
    - `ls coverage/index.html` succeeds (file exists)
    - `npx vitest run --coverage` exits without "Failed to load custom reporter" errors
    - node:test files are NOT deleted from disk (e.g., `ls tests/phase-44/` still shows files)
  </acceptance_criteria>
  <done>Vitest reports zero false "No test suite found" failures (TST-01); coverage baseline report exists in ./coverage/ with per-module data (TST-02); node:test files remain on disk and runnable</done>
</task>

</tasks>

<verification>
Phase-level verification:
1. `npx vitest run 2>&1 | grep -c "No test suite found"` returns `0` (TST-01)
2. `npx vitest run --coverage` completes and `ls coverage/index.html` succeeds (TST-02)
3. `node --test tests/phase-44/concurrency-isolation.test.mjs` runs without error (node:test preserved)
4. `npm ls @vitest/coverage-v8` shows 4.1.1
</verification>

<success_criteria>
- Zero "No test suite found" entries in vitest output
- Coverage report generated in ./coverage/ with line/branch percentages for bin/lib modules
- No node:test files deleted or disabled
- @vitest/coverage-v8@4.1.1 installed as devDependency
</success_criteria>

<output>
After completion, create `.planning/phases/186-test-infrastructure/186-01-SUMMARY.md`
</output>
