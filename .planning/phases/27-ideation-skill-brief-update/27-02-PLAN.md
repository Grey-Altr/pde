---
phase: 27-ideation-skill-brief-update
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - workflows/brief.md
autonomous: true
requirements: [IDEAT-04]

must_haves:
  truths:
    - "Running /pde:brief after ideation/competitive/opportunity runs injects IDT/CMP/OPP context into the brief automatically"
    - "Running /pde:brief without any upstream artifacts produces the brief normally with a log note (not a failure)"
    - "Brief summary table shows upstream context row listing which artifacts were injected"
  artifacts:
    - path: "workflows/brief.md"
      provides: "Surgical update adding sub-step 2c for upstream context injection"
      contains: "Sub-step 2c"
  key_links:
    - from: "workflows/brief.md"
      to: ".planning/design/strategy/IDT-ideation-v*.md"
      via: "Glob detection in sub-step 2c"
      pattern: "IDT-ideation-v"
    - from: "workflows/brief.md"
      to: ".planning/design/strategy/CMP-competitive-v*.md"
      via: "Glob detection in sub-step 2c"
      pattern: "CMP-competitive-v"
    - from: "workflows/brief.md"
      to: ".planning/design/strategy/OPP-opportunity-v*.md"
      via: "Glob detection in sub-step 2c"
      pattern: "OPP-opportunity-v"
---

<objective>
Update /pde:brief to automatically inject upstream context from IDT, CMP, and OPP artifacts when present, while preserving the existing no-artifact code path.

Purpose: Completes the ideation-to-brief handoff contract (IDEAT-04). Users who run ideation, competitive, and opportunity analysis before brief get enriched output; users who skip those steps get identical behavior to today.
Output: Modified workflows/brief.md with sub-step 2c upstream context injection
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-ideation-skill-brief-update/27-RESEARCH.md
@.planning/phases/27-ideation-skill-brief-update/27-01-SUMMARY.md
</context>

<interfaces>
<!-- The IDT artifact structure created by Plan 01's workflow -->

IDT artifact sections (from workflows/ideate.md Step 6):
```markdown
## Recommended Direction
**{Direction Name}**
- **Rationale:** ...
- **Key Assumptions:** ...
- **Primary Risks:** ...

## Brief Seed
### Problem Statement
### Product Type
### Platform
### Target Users
### Scope Boundaries
### Constraints
### Key Decisions
### Risk Register
### Next Steps
```

CMP artifact sections (from workflows/competitive.md):
```markdown
## Gap Analysis
## Opportunity Highlights
### Top Opportunities
1. **[Gap Name]** -- description
   - Source: ...
   - Estimated reach: ...
   - Competitive advantage: ...
```

OPP artifact sections (from workflows/opportunity.md):
```markdown
## Now / Next / Later Buckets
### Now
### Next
### Later
```

Current brief.md structure to preserve:
- Step 1/7: Initialize design directories
- Step 2/7: Check prerequisites (PROJECT.md hard, REQUIREMENTS.md soft, version check)
- Step 3/7: Probe MCP
- Step 4/7: Detect product type
- Step 5/7: Synthesize brief content
- Step 6/7: Update strategy domain DESIGN-STATE
- Step 7/7: Update root DESIGN-STATE and manifest
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add upstream context injection to brief workflow</name>
  <files>workflows/brief.md</files>
  <read_first>
    - workflows/brief.md (FULL file — must understand current Step 2/7 structure, Step 5/7 section generation, and Summary table format before making any edits)
    - templates/brief-seed.md (exact field names that ## Brief Seed uses — must match parsing expectations)
    - workflows/competitive.md (confirm section heading "## Gap Analysis" and "## Opportunity Highlights" for CMP parsing)
    - workflows/opportunity.md (confirm section heading "## Now / Next / Later Buckets" for OPP parsing)
  </read_first>
  <action>
Make three surgical edits to `workflows/brief.md`. The existing Steps 1-7 pipeline, all section generation logic, and all anti-patterns must remain UNCHANGED. These edits are purely ADDITIVE.

**Edit 1: Add Sub-step 2c after the existing version determination block in Step 2/7**

Insert the following AFTER the line `Display: \`Step 2/7: Prerequisites satisfied. PROJECT.md loaded. Brief version: v{N}.\`` and BEFORE `### Step 3/7`:

```markdown

**Sub-step 2c: Upstream context injection (all soft — never halt)**

Probe for upstream strategy artifacts. All probes are optional — if an artifact is missing, log a note and continue. The brief MUST produce identical output for users who have not run ideation, competitive, or opportunity analysis.

**Ideation context (IDT):**
Use the Glob tool to find `.planning/design/strategy/IDT-ideation-v*.md`.
- If found: Sort by version descending, read the highest version. Parse the `## Recommended Direction` section (direction name, rationale, key assumptions) and the `## Brief Seed` section (using templates/brief-seed.md field schema: Problem Statement, Product Type, Platform, Target Users, Scope Boundaries, Constraints, Key Decisions, Risk Register, Next Steps). Store parsed content as IDT_CONTEXT for use in Step 5.
  Log: `"  -> Ideation artifact found: v{N} — enriching brief with recommended direction ({direction_name})"`
- If not found:
  Log: `"  -> No ideation artifact — generating brief from PROJECT.md alone"`
  SET IDT_CONTEXT = null. Continue normally.

**Competitive context (CMP):**
Use the Glob tool to find `.planning/design/strategy/CMP-competitive-v*.md`.
- If found: Sort by version descending, read the highest version. Parse the `## Gap Analysis` section (unmet needs and severity) and the `## Opportunity Highlights` section (top opportunities with source, reach, competitive advantage). Store parsed content as CMP_CONTEXT for use in Step 5.
  Log: `"  -> Competitive artifact found: v{N} — enriching brief competitive context"`
- If not found:
  Log: `"  -> No competitive artifact — competitive context from PROJECT.md"`
  SET CMP_CONTEXT = null. Continue normally.

**Opportunity context (OPP):**
Use the Glob tool to find `.planning/design/strategy/OPP-opportunity-v*.md`.
- If found: Sort by version descending, read the highest version. Parse the `## Now / Next / Later Buckets` section — extract the Now candidates (highest priority items). Store parsed content as OPP_CONTEXT for use in Step 5.
  Log: `"  -> Opportunity artifact found: v{N} — enriching brief scope with high-priority items"`
- If not found:
  Log: `"  -> No opportunity artifact — scope boundaries derived from PROJECT.md only"`
  SET OPP_CONTEXT = null. Continue normally.
```

**Edit 2: Add context injection guidance to Step 5/7 section generation**

In the Step 5/7 section, AFTER the line `**Fill all sections using \`templates/design-brief.md\` as the output structure:**` and BEFORE the section-by-section instructions, insert:

```markdown

**Upstream context enrichment (if available from Sub-step 2c):**

When generating brief sections, incorporate upstream context as follows. If a context variable is null, use the existing PROJECT.md-only generation logic unchanged.

- **Problem Statement:** If IDT_CONTEXT is available, use the Brief Seed's Problem Statement as the primary source, refined with PROJECT.md's core value. IDT Brief Seed represents the latest ideation thinking and supersedes raw PROJECT.md problem description when both exist.
- **Target Users:** If IDT_CONTEXT is available, merge Brief Seed's Target Users with PROJECT.md personas. IDT may add or refine user segments discovered during ideation.
- **Competitive Context:** If CMP_CONTEXT is available, use Gap Analysis findings and Opportunity Highlights to populate the competitor table with grounded research data instead of training-knowledge inference. Preserve confidence labels from the CMP artifact.
- **Key Assumptions:** If OPP_CONTEXT is available, add high-confidence Now-bucket items as validated assumptions (mark provenance as "opportunity-validated"). If IDT_CONTEXT is available, add Brief Seed's Key Decisions as assumption-adjacent inputs.
- **Scope Boundaries:** If OPP_CONTEXT is available, use Now-bucket items as in-scope features and Later-bucket items as out-of-scope rationale. If IDT_CONTEXT is available, use Brief Seed's Scope Boundaries as the primary scope source.
- **Constraints:** If IDT_CONTEXT is available, merge Brief Seed's Constraints table with PROJECT.md constraints.
```

**Edit 3: Add upstream context row to Summary table**

In the `## Summary` section at the bottom of the file, add a new row to the summary table AFTER the `| MCP enhancements |` row:

```markdown
| Upstream context | {If any of IDT_CONTEXT, CMP_CONTEXT, OPP_CONTEXT were found: list them as "IDT v{N} (ideation), CMP v{N} (competitive), OPP v{N} (opportunity)" for each found artifact, comma-separated. If none found: "none — using PROJECT.md only"} |
```
  </action>
  <verify>
    <automated>grep -c "Sub-step 2c\|IDT_CONTEXT\|CMP_CONTEXT\|OPP_CONTEXT\|Upstream context" workflows/brief.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/brief.md contains "Sub-step 2c: Upstream context injection"
    - workflows/brief.md contains "all soft — never halt" in the sub-step 2c heading
    - workflows/brief.md contains Glob pattern `IDT-ideation-v*.md` for IDT detection
    - workflows/brief.md contains Glob pattern `CMP-competitive-v*.md` for CMP detection
    - workflows/brief.md contains Glob pattern `OPP-opportunity-v*.md` for OPP detection
    - workflows/brief.md contains "IDT_CONTEXT = null" for the not-found branch
    - workflows/brief.md contains "CMP_CONTEXT = null" for the not-found branch
    - workflows/brief.md contains "OPP_CONTEXT = null" for the not-found branch
    - workflows/brief.md contains log message "No ideation artifact — generating brief from PROJECT.md alone"
    - workflows/brief.md contains log message "No competitive artifact — competitive context from PROJECT.md"
    - workflows/brief.md contains log message "No opportunity artifact — scope boundaries derived from PROJECT.md only"
    - workflows/brief.md Step 5 contains "Upstream context enrichment" guidance block
    - workflows/brief.md Step 5 contains "IDT Brief Seed represents the latest ideation thinking and supersedes raw PROJECT.md"
    - workflows/brief.md Step 5 contains CMP_CONTEXT injection into Competitive Context section
    - workflows/brief.md Step 5 contains OPP_CONTEXT injection into Scope Boundaries section
    - workflows/brief.md Summary table contains `| Upstream context |` row
    - workflows/brief.md existing Step 1/7 through Step 7/7 structure is preserved (all 7 step headings still present)
    - workflows/brief.md existing anti-patterns section is unchanged
    - workflows/brief.md existing `<purpose>` tag content is unchanged
  </acceptance_criteria>
  <done>Brief workflow enriched with soft upstream context injection from IDT, CMP, and OPP artifacts; no-artifact code path produces identical output with log notes; summary table shows upstream context row</done>
</task>

</tasks>

<verification>
- `grep "Sub-step 2c" workflows/brief.md` returns match
- `grep "IDT-ideation-v" workflows/brief.md` returns match
- `grep "CMP-competitive-v" workflows/brief.md` returns match
- `grep "OPP-opportunity-v" workflows/brief.md` returns match
- `grep "Upstream context" workflows/brief.md` returns match
- `grep -c "Step [1-7]/7" workflows/brief.md` returns 7 (all original steps preserved)
- `grep "NEVER continue if PROJECT.md is missing" workflows/brief.md` returns match (anti-patterns preserved)
</verification>

<success_criteria>
- Brief workflow has sub-step 2c with soft probes for IDT, CMP, OPP artifacts
- All three probes degrade gracefully (log + continue, never halt)
- Step 5 section generation uses upstream context when available
- Summary table includes upstream context row
- Existing pipeline (Steps 1-7) is structurally unchanged
- Anti-patterns section is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/27-ideation-skill-brief-update/27-02-SUMMARY.md`
</output>
