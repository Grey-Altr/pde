---
phase: 27-ideation-skill-brief-update
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - workflows/brief.md
autonomous: true
requirements: [IDEAT-04]

must_haves:
  truths:
    - "Running /pde:brief after competitive and opportunity runs automatically injects CMP/OPP context into the brief"
    - "Running /pde:brief after ideation automatically injects IDT recommended direction and Brief Seed into the brief"
    - "Running /pde:brief without upstream artifacts produces the brief normally with a log note (not a failure)"
    - "Brief summary table includes Upstream context row listing which artifacts were injected"
  artifacts:
    - path: "workflows/brief.md"
      provides: "Surgical update adding Sub-step 2c for upstream context injection"
      contains: "Sub-step 2c"
  key_links:
    - from: "workflows/brief.md"
      to: ".planning/design/strategy/IDT-ideation-v*.md"
      via: "Glob detection in Sub-step 2c"
      pattern: "IDT-ideation-v"
    - from: "workflows/brief.md"
      to: ".planning/design/strategy/CMP-competitive-v*.md"
      via: "Glob detection in Sub-step 2c"
      pattern: "CMP-competitive-v"
    - from: "workflows/brief.md"
      to: ".planning/design/strategy/OPP-opportunity-v*.md"
      via: "Glob detection in Sub-step 2c"
      pattern: "OPP-opportunity-v"
---

<objective>
Update /pde:brief with soft upstream context injection: when IDT, CMP, or OPP artifacts exist, automatically enrich the brief with ideation direction, competitive gaps, and opportunity priorities. When artifacts are absent, produce the brief normally with a log note.

Purpose: Complete the ideation-to-brief handoff contract so users who run /pde:ideate, /pde:competitive, and /pde:opportunity get enriched briefs without manual re-entry.
Output: Surgical 3-edit update to workflows/brief.md (Sub-step 2c, Step 5/7 enrichment, Summary table row)
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-ideation-skill-brief-update/27-RESEARCH.md
@.planning/phases/27-ideation-skill-brief-update/27-01-SUMMARY.md

@workflows/brief.md
@templates/brief-seed.md
@references/skill-style-guide.md
</context>

<interfaces>
<!-- Key structure from workflows/brief.md that must be preserved -->

From workflows/brief.md Step 2/7 (insertion point for Sub-step 2c):
```
Step 2/7: Check prerequisites
  ... existing PROJECT.md check, version determination ...
  Display: `Step 2/7: Prerequisites satisfied. PROJECT.md loaded. Brief version: v{N}.`
```
Sub-step 2c goes AFTER the version display line, BEFORE Step 3/7.

From workflows/ideate.md (Plan 01 output) -- IDT artifact sections to parse:
```
## Recommended Direction
  {direction name, rationale, key assumptions}
## Brief Seed
  {9 fields from templates/brief-seed.md}
```

From workflows/competitive.md -- CMP artifact sections to parse:
```
## Gap Analysis
  {unmet needs and severity}
## Opportunity Highlights
  {top opportunities with source, reach, competitive advantage}
```

From workflows/opportunity.md -- OPP artifact sections to parse:
```
## Now / Next / Later Buckets
  {Now candidates = highest priority items}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add upstream context injection to brief workflow</name>
  <files>workflows/brief.md</files>
  <read_first>
    - workflows/brief.md (FULL file — must understand complete step structure before making surgical edits; identify exact insertion points for Sub-step 2c after Step 2/7 version display, Step 5/7 enrichment block, and Summary table)
    - templates/brief-seed.md (field schema that IDT Brief Seed uses — needed to specify what Sub-step 2c parses)
    - references/skill-style-guide.md (prerequisite warning pattern for log messages)
    - workflows/ideate.md (IDT artifact structure — confirm ## Recommended Direction and ## Brief Seed section headings)
  </read_first>
  <action>
Make exactly 3 additive edits to workflows/brief.md. Do NOT restructure existing steps. Do NOT change any existing logic.

**Edit 1: Insert Sub-step 2c after Step 2/7 version display line**

After the line `Display: \`Step 2/7: Prerequisites satisfied. PROJECT.md loaded. Brief version: v{N}.\`` and before `### Step 3/7:`, insert:

```markdown
---

**Sub-step 2c: Upstream context injection (all soft -- never halt)**

Probe for upstream strategy artifacts. All probes are optional -- if an artifact is missing, log a note and continue. The brief MUST produce identical output for users who have not run ideation, competitive, or opportunity analysis.

**Ideation context (IDT):**
Use the Glob tool to find `.planning/design/strategy/IDT-ideation-v*.md`.
- If found: Sort by version descending, read the highest version. Parse the `## Recommended Direction` section (direction name, rationale, key assumptions) and the `## Brief Seed` section (using templates/brief-seed.md field schema: Problem Statement, Product Type, Platform, Target Users, Scope Boundaries, Constraints, Key Decisions, Risk Register, Next Steps). Store parsed content as IDT_CONTEXT for use in Step 5.
  Log: `"  -> Ideation artifact found: v{N} -- enriching brief with recommended direction ({direction_name})"`
- If not found:
  Log: `"  -> No ideation artifact -- generating brief from PROJECT.md alone"`
  SET IDT_CONTEXT = null. Continue normally.

**Competitive context (CMP):**
Use the Glob tool to find `.planning/design/strategy/CMP-competitive-v*.md`.
- If found: Sort by version descending, read the highest version. Parse the `## Gap Analysis` section (unmet needs and severity) and the `## Opportunity Highlights` section (top opportunities with source, reach, competitive advantage). Store parsed content as CMP_CONTEXT for use in Step 5.
  Log: `"  -> Competitive artifact found: v{N} -- enriching brief competitive context"`
- If not found:
  Log: `"  -> No competitive artifact -- competitive context from PROJECT.md"`
  SET CMP_CONTEXT = null. Continue normally.

**Opportunity context (OPP):**
Use the Glob tool to find `.planning/design/strategy/OPP-opportunity-v*.md`.
- If found: Sort by version descending, read the highest version. Parse the `## Now / Next / Later Buckets` section -- extract the Now candidates (highest priority items). Store parsed content as OPP_CONTEXT for use in Step 5.
  Log: `"  -> Opportunity artifact found: v{N} -- enriching brief scope with high-priority items"`
- If not found:
  Log: `"  -> No opportunity artifact -- scope boundaries derived from PROJECT.md only"`
  SET OPP_CONTEXT = null. Continue normally.
```

**Edit 2: Add upstream enrichment guidance to Step 5/7**

In Step 5/7 (the brief section generation step), add a guidance block that tells the executor how to use the context variables:

```markdown
**Upstream context enrichment (if context variables are non-null):**

When generating brief sections, incorporate upstream context as follows:
- **Problem Statement:** If IDT_CONTEXT is non-null, use the IDT Brief Seed Problem Statement as the primary problem framing (it represents the latest ideation thinking, superseding raw PROJECT.md problem description). If null, use PROJECT.md as before.
- **Target Users:** If IDT_CONTEXT is non-null, incorporate IDT Brief Seed Target Users to refine persona descriptions. If null, derive from PROJECT.md.
- **Competitive Context:** If CMP_CONTEXT is non-null, integrate gap analysis findings and opportunity highlights into the competitive landscape section. If null, generate from PROJECT.md and training knowledge.
- **Key Assumptions:** If OPP_CONTEXT is non-null, incorporate high-confidence Now items as validated assumptions. If null, derive from PROJECT.md.
- **Scope Boundaries:** If OPP_CONTEXT is non-null, use Now items for in-scope features and Later items for explicitly out-of-scope. If null, derive from PROJECT.md.
- **Constraints:** If IDT_CONTEXT is non-null, incorporate constraints from IDT Brief Seed. If null, derive from PROJECT.md.

All enrichment is additive. If a context variable is null, the corresponding section generation logic is UNCHANGED from the existing pipeline.
```

**Edit 3: Add Upstream context row to Summary table**

In the Summary table at the end of the process section, add a new row after the existing rows:

```markdown
| Upstream context | {list injected artifacts: "IDT v{N} (ideation), CMP v{N} (competitive), OPP v{N} (opportunity)" OR "none -- using PROJECT.md only"} |
```
  </action>
  <verify>
    <automated>grep -c "Sub-step 2c" workflows/brief.md && grep -c "IDT_CONTEXT" workflows/brief.md && grep -c "CMP_CONTEXT" workflows/brief.md && grep -c "OPP_CONTEXT" workflows/brief.md && grep -c "Upstream context" workflows/brief.md && grep "all soft" workflows/brief.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/brief.md contains "Sub-step 2c: Upstream context injection (all soft" between Step 2/7 and Step 3/7
    - workflows/brief.md contains Glob probe for `IDT-ideation-v*.md` with log messages for found and not-found cases
    - workflows/brief.md contains Glob probe for `CMP-competitive-v*.md` with log messages for found and not-found cases
    - workflows/brief.md contains Glob probe for `OPP-opportunity-v*.md` with log messages for found and not-found cases
    - workflows/brief.md contains `SET IDT_CONTEXT = null` for not-found case
    - workflows/brief.md contains `SET CMP_CONTEXT = null` for not-found case
    - workflows/brief.md contains `SET OPP_CONTEXT = null` for not-found case
    - workflows/brief.md Step 5/7 contains enrichment guidance block referencing IDT_CONTEXT, CMP_CONTEXT, OPP_CONTEXT
    - workflows/brief.md contains "IDT Brief Seed Problem Statement as the primary problem framing" in enrichment guidance
    - workflows/brief.md Summary table contains `| Upstream context |` row
    - workflows/brief.md existing Steps 1-7 structure is preserved (step numbering unchanged)
    - workflows/brief.md `<purpose>` tag content is unchanged
    - workflows/brief.md anti-patterns section is unchanged
  </acceptance_criteria>
  <done>workflows/brief.md has 3 additive edits: Sub-step 2c with soft Glob probes for IDT/CMP/OPP, Step 5/7 enrichment guidance, and Summary table Upstream context row; existing pipeline unchanged for users without upstream artifacts</done>
</task>

</tasks>

<verification>
1. `grep "Sub-step 2c" workflows/brief.md` confirms injection step exists
2. `grep "IDT-ideation-v" workflows/brief.md` confirms IDT Glob probe
3. `grep "CMP-competitive-v" workflows/brief.md` confirms CMP Glob probe
4. `grep "OPP-opportunity-v" workflows/brief.md` confirms OPP Glob probe
5. `grep "null.*Continue normally" workflows/brief.md` confirms graceful degradation
6. `grep "Upstream context" workflows/brief.md` confirms summary table row
7. `grep "all soft" workflows/brief.md` confirms non-blocking contract
</verification>

<success_criteria>
- /pde:brief detects and injects IDT/CMP/OPP artifacts when present
- /pde:brief produces identical output when no upstream artifacts exist (with log note)
- IDT Brief Seed supersedes PROJECT.md problem description when both exist
- Summary table shows which artifacts were injected per run
- Existing brief pipeline steps 1-7 structurally unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/27-ideation-skill-brief-update/27-02-SUMMARY.md`
</output>
