---
phase: 07-rebranding-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - PLUG-04

must_haves:
  truths:
    - "Zero occurrences of gsd, GSD, or get-shit-done in any plugin source file"
    - "Zero occurrences of /gsd: command prefix in any source file"
    - "Zero occurrences of .gsd or /.gsd config paths in any source file"
    - "Zero hardcoded absolute paths containing specific usernames (e.g., greyaltaer) in source"
    - "STATE.md frontmatter uses pde_state_version, not gsd_state_version"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Corrected frontmatter key (pde_state_version)"
      contains: "pde_state_version"
  key_links: []
---

<objective>
Fix the one remaining GSD artifact (STATE.md frontmatter key) and run a comprehensive grep audit across all plugin source directories to produce evidence that BRAND-01, BRAND-02, BRAND-03, and PLUG-04 are satisfied.

Purpose: Prior phases (1-6) eliminated all GSD references from plugin source code. This plan fixes the last known legacy artifact and produces grep-based proof that the source is clean.
Output: Corrected STATE.md frontmatter + grep audit evidence showing zero GSD occurrences across all source directories.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-rebranding-completeness/07-RESEARCH.md

IMPORTANT SCOPE RULES (from research):
- "Source files" means plugin source shipped to users: bin/, lib/, commands/, workflows/, templates/, references/, .claude-plugin/
- .planning/ is EXCLUDED from the audit — it contains historical development records documenting the GSD-to-PDE fork
- /Users/name/ in references/skill-style-guide.md is a generic placeholder, NOT a username violation
- /users/ in REST API examples (templates/phase-prompt.md) is NOT a username violation
- Do NOT modify .planning/ files other than STATE.md frontmatter — historical accuracy must be preserved
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix STATE.md frontmatter and run comprehensive GSD grep audit</name>
  <files>.planning/STATE.md</files>
  <action>
1. Read .planning/STATE.md and change line 2 from `gsd_state_version: 1.0` to `pde_state_version: 1.0`. Do NOT change any other content in STATE.md — only the frontmatter key name.

2. Run the following four grep audit commands from the project root. ALL must return 0 matches:

   a) BRAND-01 + BRAND-02 — GSD/get-shit-done in source:
      grep -rni "gsd\|get-shit-done" bin/ lib/ commands/ workflows/ templates/ references/ .claude-plugin/ 2>/dev/null | wc -l
      Expected: 0

   b) PLUG-04 — /gsd: command prefix in source:
      grep -rn "/gsd:" bin/ lib/ commands/ workflows/ templates/ references/ 2>/dev/null | wc -l
      Expected: 0

   c) BRAND-03 part 1 — .gsd config paths in source:
      grep -rn "\.gsd\|/\.gsd" bin/ lib/ commands/ workflows/ templates/ references/ 2>/dev/null | wc -l
      Expected: 0

   d) BRAND-03 part 2 — Username-specific absolute paths in source (broad pattern):
      grep -rni "greyaltaer\|/Users/[a-zA-Z0-9_-]*/" bin/ lib/ commands/ workflows/ templates/ references/ 2>/dev/null | grep -v "/Users/name/" | grep -v "/users/" | wc -l
      Expected: 0
      Note: /Users/name/ is a generic placeholder (not a violation). /users/ in REST API paths is not a violation. Any other /Users/username/ pattern IS a violation and must be fixed.

3. If ANY grep command returns non-zero: fix the offending files by replacing GSD references with PDE equivalents (or replacing hardcoded usernames with generic /Users/name/ placeholder), then re-run the audit.

4. Document audit scope explicitly: "Plugin source directories only (bin/, lib/, commands/, workflows/, templates/, references/, .claude-plugin/). .planning/ excluded as historical development records."
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep "pde_state_version" .planning/STATE.md && count=$(grep -rni "gsd\|get-shit-done" bin/ lib/ commands/ workflows/ templates/ references/ .claude-plugin/ 2>/dev/null | wc -l | tr -d ' ') && echo "GSD count: $count" && [ "$count" = "0" ] && count2=$(grep -rn "/gsd:" bin/ lib/ commands/ workflows/ 2>/dev/null | wc -l | tr -d ' ') && echo "/gsd: count: $count2" && [ "$count2" = "0" ] && count3=$(grep -rn "\.gsd\|/\.gsd" bin/ lib/ commands/ workflows/ templates/ references/ 2>/dev/null | wc -l | tr -d ' ') && echo ".gsd count: $count3" && [ "$count3" = "0" ] && count4=$(grep -rni "greyaltaer\|/Users/[a-zA-Z0-9_-]*/" bin/ lib/ commands/ workflows/ templates/ references/ 2>/dev/null | grep -v "/Users/name/" | grep -v "/users/" | wc -l | tr -d ' ') && echo "Username count: $count4" && [ "$count4" = "0" ] && echo "ALL AUDITS PASS"</automated>
  </verify>
  <done>STATE.md frontmatter corrected to pde_state_version. All four grep audits return zero matches. BRAND-01, BRAND-02, BRAND-03, and PLUG-04 verified clean.</done>
</task>

</tasks>

<verification>
Run all four grep audit commands independently. Each must return exactly 0 matches. STATE.md must contain pde_state_version in frontmatter.
</verification>

<success_criteria>
- STATE.md frontmatter key is pde_state_version (not gsd_state_version)
- grep -rni "gsd|get-shit-done" across source dirs returns 0
- grep -rn "/gsd:" across source dirs returns 0
- grep -rn ".gsd|/.gsd" across source dirs returns 0
- grep -rni "greyaltaer|/Users/username/" across source dirs returns 0 (excluding /Users/name/ placeholder and /users/ REST paths)
</success_criteria>

<output>
After completion, create `.planning/phases/07-rebranding-completeness/07-01-SUMMARY.md`
</output>
