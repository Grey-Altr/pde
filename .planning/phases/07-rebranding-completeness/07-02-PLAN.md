---
phase: 07-rebranding-completeness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/*.md
  - lib/ui/splash.cjs
  - lib/ui/render.cjs
autonomous: true
requirements:
  - BRAND-04
  - BRAND-05
  - BRAND-06

must_haves:
  truths:
    - "Zero GSD-branded banner calls exist in workflow files"
    - "splash.cjs displays Platform Development Engine branding"
    - "All workflow banner() calls pass PDE-branded stage names"
    - "No existing documentation files reference GSD (BRAND-06 partial — README creation is Phase 8)"
  artifacts: []
  key_links:
    - from: "workflows/*.md"
      to: "lib/ui/render.cjs banner()"
      via: "banner() function calls in workflow bash blocks"
      pattern: "banner.*RESEARCHING|banner.*PLANNING|banner.*EXECUTING"
    - from: "lib/ui/splash.cjs"
      to: "user terminal output"
      via: "splash screen render"
      pattern: "Platform Development Engine"
---

<objective>
Verify that all UI banners, stage names, and progress displays use PDE branding, and that no existing documentation references GSD.

Purpose: BRAND-04 and BRAND-05 require PDE branding in all user-visible UI elements. BRAND-06 requires documentation to reference PDE not GSD. Since no README exists yet (created in Phase 8), this plan verifies the banner/stage name layer and confirms no existing docs carry GSD references.
Output: Grep-based evidence that banner calls, stage names, and splash screen all use PDE branding. Confirmation that BRAND-06 is partially met (no GSD in existing docs) with README creation deferred to Phase 8.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-rebranding-completeness/07-RESEARCH.md

KEY CONTEXT FROM RESEARCH:
- banner() function in lib/ui/render.cjs renders stage names as "stage name"
- Stage names come from workflow callers, not hardcoded in the banner function
- All workflow callers already pass PDE-branded stage names (RESEARCHING, PLANNING PHASE X, etc.)
- splash.cjs line 89 contains "Platform Development Engine"
- The success criterion "PDE >" is a visual indicator, not literal required text — verify stage name content, not Unicode symbols
- BRAND-06 scope: ROADMAP Phase 7 header lists it but Phase 8 details assign README creation there. No README.md exists. Verify existing docs are GSD-free; README creation is Phase 8 scope.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify UI banners and stage names use PDE branding</name>
  <files>workflows/*.md, lib/ui/splash.cjs, lib/ui/render.cjs</files>
  <action>
Run the following verification commands from the project root. This is a READ-ONLY verification task — no files should be modified unless a violation is found. The files listed above are candidate modification targets IF violations are discovered.

1. BRAND-04 — Verify zero GSD-branded banner calls:
   grep -rn "banner.*GSD\|banner.*gsd\|\"GSD" workflows/ 2>/dev/null | wc -l
   Expected: 0

   Also check lib/ui/ for any GSD branding:
   grep -rn "GSD" lib/ui/ 2>/dev/null | wc -l
   Expected: 0

2. BRAND-05 — Verify splash.cjs shows PDE branding:
   grep "Platform Development Engine" lib/ui/splash.cjs
   Expected: At least one match (line 89)

3. BRAND-05 — Verify workflow stage names are PDE-branded (positive check):
   grep -rn "banner\|stage" workflows/ 2>/dev/null | head -20
   Verify all stage name strings are PDE-appropriate (RESEARCHING, PLANNING, EXECUTING, VERIFYING, etc.) with no GSD strings.

4. BRAND-06 — Verify no existing documentation references GSD:
   Check if README.md exists at project root. If it does, grep for GSD references. If it does not exist, note that README creation is Phase 8 scope.
   Also check any other .md files at project root (excluding .planning/):
   ls *.md 2>/dev/null
   For any found, grep for GSD references.

5. If ANY violation is found: fix the offending file (in workflows/*.md, lib/ui/splash.cjs, or lib/ui/render.cjs as appropriate), then re-run the audit.

6. Document findings for each requirement (BRAND-04, BRAND-05, BRAND-06).
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && count=$(grep -rn "banner.*GSD\|banner.*gsd\|\"GSD" workflows/ 2>/dev/null | wc -l | tr -d ' ') && echo "GSD banner count: $count" && [ "$count" = "0" ] && count2=$(grep -rn "GSD" lib/ui/ 2>/dev/null | wc -l | tr -d ' ') && echo "GSD in lib/ui: $count2" && [ "$count2" = "0" ] && grep -q "Platform Development Engine" lib/ui/splash.cjs && echo "splash.cjs PDE branding: FOUND" && echo "ALL BANNER/STAGE AUDITS PASS"</automated>
  </verify>
  <done>BRAND-04 verified: zero GSD-branded banner calls. BRAND-05 verified: splash.cjs shows "Platform Development Engine" and all stage names are PDE-branded. BRAND-06 partially verified: no existing documentation references GSD (README creation deferred to Phase 8).</done>
</task>

</tasks>

<verification>
All banner/stage name grep audits return zero GSD matches. splash.cjs contains "Platform Development Engine". No root-level documentation files contain GSD references.
</verification>

<success_criteria>
- grep for GSD in banner calls returns 0 matches
- grep for GSD in lib/ui/ returns 0 matches
- splash.cjs contains "Platform Development Engine"
- No existing documentation at project root references GSD
</success_criteria>

<output>
After completion, create `.planning/phases/07-rebranding-completeness/07-02-SUMMARY.md`
</output>
