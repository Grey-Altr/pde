---
phase: 24-schema-migration-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/system.md
  - workflows/flows.md
  - workflows/wireframe.md
  - workflows/critique.md
  - workflows/iterate.md
  - workflows/handoff.md
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "Running any v1.1 skill after a v1.2 skill preserves all v1.2 coverage flags"
    - "All 6 workflow files enumerate exactly 13 coverage flags in their Step 7 block"
    - "No workflow file references 'seven' or 'six' field counts -- all say 'thirteen'"
  artifacts:
    - path: "workflows/system.md"
      provides: "13-field coverage write for hasDesignSystem"
      contains: "hasRecommendations"
    - path: "workflows/flows.md"
      provides: "13-field coverage write for hasFlows"
      contains: "hasRecommendations"
    - path: "workflows/wireframe.md"
      provides: "13-field coverage write for hasWireframes"
      contains: "hasRecommendations"
    - path: "workflows/critique.md"
      provides: "13-field coverage write for hasCritique"
      contains: "hasRecommendations"
    - path: "workflows/iterate.md"
      provides: "13-field coverage write for hasIterate"
      contains: "hasRecommendations"
    - path: "workflows/handoff.md"
      provides: "13-field coverage write for hasHandoff"
      contains: "hasRecommendations"
  key_links:
    - from: "workflows/*.md"
      to: "bin/pde-tools.cjs design manifest-set-top-level"
      via: "Step 7 coverage-check then manifest-set-top-level with 13-field JSON"
      pattern: "manifest-set-top-level designCoverage.*hasRecommendations"
---

<objective>
Migrate all 6 existing skill workflows from 7-field to 13-field coverage flag write pattern (pass-through-all).

Purpose: After this migration, any v1.1 skill that runs will preserve coverage flags set by v1.2 skills (hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations) instead of silently deleting them via partial JSON replacement.
Output: 6 updated workflow files with 13-field coverage pattern in Step 7.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-schema-migration-infrastructure/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate system.md, flows.md, and wireframe.md to 13-field coverage pattern</name>
  <files>workflows/system.md, workflows/flows.md, workflows/wireframe.md</files>
  <read_first>
    - workflows/system.md (lines 1293-1310 for Step 7 coverage block)
    - workflows/flows.md (lines 476-495 for Step 7 coverage block)
    - workflows/wireframe.md (lines 630-645 for Step 7 coverage block)
  </read_first>
  <action>
For each of these 3 workflow files, apply the following changes. The canonical 13-field order is: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations.

**workflows/system.md:**

1. Line 1298 -- Replace the instruction text. Current:
   `Parse the JSON output from coverage-check. Extract ALL seven current flag values: `hasDesignSystem`, `hasFlows`, `hasWireframes`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasHardwareSpec`. Default any absent field to `false`. Merge `hasDesignSystem: true` while preserving all other values. Then write the full merged seven-field object:`
   New:
   `Parse the JSON output from coverage-check. Extract ALL thirteen current flag values: `hasDesignSystem`, `hasWireframes`, `hasFlows`, `hasHardwareSpec`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit`, `hasRecommendations`. Default any absent field to `false`. Merge `hasDesignSystem: true` while preserving all other twelve values. Then write the full merged thirteen-field object:`

2. Line 1302 -- Replace the JSON in the bash command. Current:
   `'{"hasDesignSystem":true,"hasFlows":{current},"hasWireframes":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasHardwareSpec":{current}}'`
   New:
   `'{"hasDesignSystem":true,"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current}}'`

3. Line 1305 -- Replace the anti-pattern reminder text. Current:
   `Use the actual values read from coverage-check — do not hardcode false for fields that may have been set by other skills. The key invariant: `manifest-set-top-level` performs FLAT key assignment, so `designCoverage` must be set as the FULL seven-field JSON object every time. All 7 fields: hasDesignSystem, hasFlows, hasWireframes, hasCritique, hasIterate, hasHandoff, hasHardwareSpec.`
   New:
   `Use the actual values read from coverage-check — do not hardcode false for fields that may have been set by other skills. The key invariant: `manifest-set-top-level` performs FLAT key assignment, so `designCoverage` must be set as the FULL thirteen-field JSON object every time. All 13 fields: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations.`

**workflows/flows.md:**

1. Lines 480-486 -- Replace the instruction text block. Current:
   ```
   Parse the JSON output to extract current flag values for ALL seven fields:
   - `hasDesignSystem` — current value from COV output
   - `hasWireframes` — current value from COV output
   - `hasCritique` — current value from COV output
   - `hasIterate` — current value from COV output
   - `hasHandoff` — current value from COV output
   - `hasHardwareSpec` — current value from COV output
   ```
   New:
   ```
   Parse the JSON output to extract current flag values for ALL thirteen fields:
   - `hasDesignSystem` — current value from COV output
   - `hasWireframes` — current value from COV output
   - `hasFlows` — (this skill sets to true)
   - `hasHardwareSpec` — current value from COV output
   - `hasCritique` — current value from COV output
   - `hasIterate` — current value from COV output
   - `hasHandoff` — current value from COV output
   - `hasIdeation` — current value from COV output (default false if absent)
   - `hasCompetitive` — current value from COV output (default false if absent)
   - `hasOpportunity` — current value from COV output (default false if absent)
   - `hasMockup` — current value from COV output (default false if absent)
   - `hasHigAudit` — current value from COV output (default false if absent)
   - `hasRecommendations` — current value from COV output (default false if absent)
   ```

2. Line 488 -- Replace "Merge `hasFlows: true` into the existing values, then write the full object (all flags must be present):" with:
   "Merge `hasFlows: true` into the existing values, then write the full thirteen-field object (all flags must be present — default any absent field to `false`):"

3. Line 491 -- Replace the JSON in the bash command. Current:
   `'{"hasDesignSystem":{current},"hasFlows":true,"hasWireframes":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasHardwareSpec":{current}}'`
   New:
   `'{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":true,"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current}}'`

**workflows/wireframe.md:**

1. Line 636 -- Replace instruction text. Current:
   `Parse the JSON output. Extract ALL seven current flag values: hasDesignSystem, hasFlows, hasWireframes, hasCritique, hasIterate, hasHandoff, hasHardwareSpec. Default any absent field to `false`. Merge `hasWireframes: true` while preserving all other values. Then write the full merged seven-field object:`
   New:
   `Parse the JSON output. Extract ALL thirteen current flag values: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations. Default any absent field to `false`. Merge `hasWireframes: true` while preserving all other twelve values. Then write the full merged thirteen-field object:`

2. Line 639 -- Replace the JSON in the bash command. Current:
   `'{"hasDesignSystem":{current},"hasFlows":{current},"hasWireframes":true,"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasHardwareSpec":{current}}'`
   New:
   `'{"hasDesignSystem":{current},"hasWireframes":true,"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current}}'`
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep -c "hasRecommendations" workflows/system.md workflows/flows.md workflows/wireframe.md && grep -c "seven" workflows/system.md workflows/flows.md workflows/wireframe.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/system.md contains `hasRecommendations` (at least 2 occurrences: JSON line + anti-pattern reminder)
    - workflows/system.md does NOT contain the string "seven-field" or "seven current flag" or "All 7 fields"
    - workflows/system.md contains `thirteen-field` and `All 13 fields`
    - workflows/flows.md contains `hasRecommendations` (at least 2 occurrences: flag list + JSON line)
    - workflows/flows.md contains `ALL thirteen fields` and does NOT contain `ALL seven fields`
    - workflows/wireframe.md contains `hasRecommendations` (at least 1 occurrence in JSON line)
    - workflows/wireframe.md contains `thirteen` and does NOT contain `seven current flag`
    - All 3 files contain `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit` in their JSON command lines
    - All 3 JSON command lines use canonical field order: hasDesignSystem first, hasRecommendations last
    - grep "seven" returns 0 matches across all 3 files (in the coverage-related sections)
  </acceptance_criteria>
  <done>system.md, flows.md, and wireframe.md all write 13-field designCoverage objects in canonical order with no residual "seven" or "7 fields" references in coverage-related sections.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate critique.md, iterate.md, and handoff.md to 13-field coverage pattern</name>
  <files>workflows/critique.md, workflows/iterate.md, workflows/handoff.md</files>
  <read_first>
    - workflows/critique.md (lines 570-582 for Step 7 coverage block)
    - workflows/iterate.md (lines 445-458 for Step 7 coverage block)
    - workflows/handoff.md (lines 82-87 for early coverage-check parse, lines 618-630 for Step 7 coverage block, lines 660-668 for anti-pattern section, line 690 for output section)
  </read_first>
  <action>
For each of these 3 workflow files, apply the following changes. The canonical 13-field order is: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations.

**workflows/critique.md:**

1. Line 575 -- Replace instruction text. Current:
   `Parse the JSON output from coverage-check. Extract ALL seven current flag values: hasDesignSystem, hasFlows, hasWireframes, hasCritique, hasIterate, hasHandoff, hasHardwareSpec. Default any absent field to `false`. Merge `hasCritique: true` while preserving all other values. Then write the full merged seven-field object:`
   New:
   `Parse the JSON output from coverage-check. Extract ALL thirteen current flag values: hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations. Default any absent field to `false`. Merge `hasCritique: true` while preserving all other twelve values. Then write the full merged thirteen-field object:`

2. Line 578 -- Replace the JSON in the bash command. Current:
   `'{"hasDesignSystem":{current},"hasFlows":{current},"hasWireframes":{current},"hasCritique":true,"hasIterate":{current},"hasHandoff":{current},"hasHardwareSpec":{current}}'`
   New:
   `'{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":true,"hasIterate":{current},"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current}}'`

**workflows/iterate.md:**

1. Line 450 -- Replace instruction text. Current:
   `Parse the JSON output from coverage-check. Extract ALL six current flag values: `hasDesignSystem`, `hasFlows`, `hasWireframes`, `hasCritique`, `hasHandoff`, `hasHardwareSpec`. Merge `hasIterate: true` as the seventh field while preserving all other values. Then write the full merged seven-field object:`
   New:
   `Parse the JSON output from coverage-check. Extract ALL thirteen current flag values: `hasDesignSystem`, `hasWireframes`, `hasFlows`, `hasHardwareSpec`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit`, `hasRecommendations`. Default any absent field to `false`. Merge `hasIterate: true` while preserving all other twelve values. Then write the full merged thirteen-field object:`

2. Line 453 -- Replace the JSON in the bash command. Current:
   `'{"hasDesignSystem":{current},"hasFlows":{current},"hasWireframes":{current},"hasCritique":{current},"hasIterate":true,"hasHandoff":{current},"hasHardwareSpec":{current}}'`
   New:
   `'{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":true,"hasHandoff":{current},"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current}}'`

**workflows/handoff.md:**

There are 4 locations to update in handoff.md:

1. Line 87 -- Replace the early coverage-check parse instruction. Current:
   `Parse JSON from COV_RAW. Store all current flag values: `hasDesignSystem`, `hasFlows`, `hasWireframes`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasHardwareSpec`. If coverage-check fails or returns invalid JSON, default all to `false` and continue.`
   New:
   `Parse JSON from COV_RAW. Store all current flag values: `hasDesignSystem`, `hasWireframes`, `hasFlows`, `hasHardwareSpec`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit`, `hasRecommendations`. If coverage-check fails or returns invalid JSON, default all to `false` and continue.`

2. Line 622 -- Replace instruction text. Current:
   `Parse the JSON output from coverage-check. Extract ALL seven current flag values: `hasDesignSystem`, `hasFlows`, `hasWireframes`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasHardwareSpec`. Default any absent field to `false`. Merge `hasHandoff: true` while preserving all other values. Then write the full merged seven-field object:`
   New:
   `Parse the JSON output from coverage-check. Extract ALL thirteen current flag values: `hasDesignSystem`, `hasWireframes`, `hasFlows`, `hasHardwareSpec`, `hasCritique`, `hasIterate`, `hasHandoff`, `hasIdeation`, `hasCompetitive`, `hasOpportunity`, `hasMockup`, `hasHigAudit`, `hasRecommendations`. Default any absent field to `false`. Merge `hasHandoff: true` while preserving all other twelve values. Then write the full merged thirteen-field object:`

3. Line 625 -- Replace the JSON in the bash command. Current:
   `'{"hasDesignSystem":{current},"hasFlows":{current},"hasWireframes":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":true,"hasHardwareSpec":{current}}'`
   New:
   `'{"hasDesignSystem":{current},"hasWireframes":{current},"hasFlows":{current},"hasHardwareSpec":{current},"hasCritique":{current},"hasIterate":{current},"hasHandoff":true,"hasIdeation":{current},"hasCompetitive":{current},"hasOpportunity":{current},"hasMockup":{current},"hasHigAudit":{current},"hasRecommendations":{current}}'`

4. Line 665 -- Replace anti-pattern text. Current:
   `- **Skip coverage-check before setting designCoverage:** `manifest-set-top-level` replaces the ENTIRE designCoverage object. Reading coverage-check first and preserving all 7 fields (hasDesignSystem, hasFlows, hasWireframes, hasCritique, hasIterate, hasHandoff, hasHardwareSpec) is mandatory to avoid clobbering flags set by other skills.`
   New:
   `- **Skip coverage-check before setting designCoverage:** `manifest-set-top-level` replaces the ENTIRE designCoverage object. Reading coverage-check first and preserving all 13 fields (hasDesignSystem, hasWireframes, hasFlows, hasHardwareSpec, hasCritique, hasIterate, hasHandoff, hasIdeation, hasCompetitive, hasOpportunity, hasMockup, hasHigAudit, hasRecommendations) is mandatory to avoid clobbering flags set by other skills.`

5. Line 690 -- Replace output reference. Current:
   `- `.planning/design/design-manifest.json` — manifest updated with HND artifact entry and hasHandoff: true in designCoverage (all 7 fields preserved via read-before-set)`
   New:
   `- `.planning/design/design-manifest.json` — manifest updated with HND artifact entry and hasHandoff: true in designCoverage (all 13 fields preserved via read-before-set)`
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && grep -c "hasRecommendations" workflows/critique.md workflows/iterate.md workflows/handoff.md && echo "---seven check---" && grep -n "seven\|six current\|all 7 fields\|all 7 field" workflows/critique.md workflows/iterate.md workflows/handoff.md || echo "no seven/six references found (good)"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/critique.md contains `hasRecommendations` (at least 1 occurrence in JSON line)
    - workflows/critique.md contains `thirteen` and does NOT contain `seven current flag`
    - workflows/iterate.md contains `hasRecommendations` (at least 1 occurrence in JSON line)
    - workflows/iterate.md does NOT contain `six current flag` or `seventh field` -- says `thirteen` instead
    - workflows/handoff.md contains `hasRecommendations` (at least 3 occurrences: line 87 parse, line 622 instruction, line 625 JSON, line 665 anti-pattern)
    - workflows/handoff.md does NOT contain `seven current flag` or `all 7 fields` -- says `thirteen`/`13` instead
    - workflows/handoff.md line 690 says `all 13 fields` not `all 7 fields`
    - All 3 JSON command lines use canonical field order: hasDesignSystem first, hasRecommendations last
    - Each skill's own flag is set to `true` while all others are `{current}`
    - grep for "six current\|seven current\|all 7 fields" across all 3 files returns 0 matches
  </acceptance_criteria>
  <done>critique.md, iterate.md, and handoff.md all write 13-field designCoverage objects in canonical order. iterate.md's "six/seventh" inconsistency is resolved. handoff.md's anti-pattern section and output section reference 13 fields.</done>
</task>

</tasks>

<verification>
- `grep -c "hasRecommendations" workflows/system.md workflows/flows.md workflows/wireframe.md workflows/critique.md workflows/iterate.md workflows/handoff.md` -- all 6 files return >= 1
- `grep -rn "seven\|six current" workflows/{system,flows,wireframe,critique,iterate,handoff}.md` -- zero matches in coverage-related sections
- `grep "manifest-set-top-level designCoverage" workflows/{system,flows,wireframe,critique,iterate,handoff}.md | grep -c "hasRecommendations"` -- returns 6 (one per file)
- `node bin/lib/design.cjs --self-test` -- still passes 20/20 (workflow text edits don't affect self-test)
</verification>

<success_criteria>
- All 6 workflow files migrated from 7-field to 13-field coverage write pattern
- Canonical field order used in all 6 JSON command lines
- No residual "seven"/"six" field count references in coverage sections
- Each skill preserves all 12 other flags and sets only its own to true
- iterate.md's legacy "six existing + one new" narrative replaced with consistent "thirteen" language
</success_criteria>

<output>
After completion, create `.planning/phases/24-schema-migration-infrastructure/24-02-SUMMARY.md`
</output>
