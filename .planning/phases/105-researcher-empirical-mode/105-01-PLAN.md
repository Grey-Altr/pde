---
phase: 105-researcher-empirical-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/agents/pde-phase-researcher.md
  - workflows/research-phase.md
  - tests/phase-105/researcher-empirical-flag.test.mjs
  - tests/phase-105/empirical-routing.test.mjs
  - tests/phase-105/experiments-attempted-section.test.mjs
autonomous: true
requirements:
  - RSRCH-01
  - RSRCH-02
  - RSRCH-03

must_haves:
  truths:
    - "pde-phase-researcher agent accepts --empirical flag and enters empirical mode steps instead of desk research"
    - "research-phase.md auto-routes to empirical mode when phase goal/CONTEXT.md contains optimization keywords"
    - "RESEARCH.md produced in empirical mode includes an Experiments Attempted section with iteration table"
  artifacts:
    - path: "~/.claude/agents/pde-phase-researcher.md"
      provides: "Empirical mode flag parsing and steps E1-E8"
      contains: "--empirical"
    - path: "workflows/research-phase.md"
      provides: "Keyword detection and --empirical routing in Step 3.5"
      contains: "Detect Empirical Mode"
    - path: "tests/phase-105/researcher-empirical-flag.test.mjs"
      provides: "Structural tests for RSRCH-01"
    - path: "tests/phase-105/empirical-routing.test.mjs"
      provides: "Structural tests for RSRCH-02"
    - path: "tests/phase-105/experiments-attempted-section.test.mjs"
      provides: "Structural tests for RSRCH-03"
  key_links:
    - from: "workflows/research-phase.md"
      to: "~/.claude/agents/pde-phase-researcher.md"
      via: "Task() spawn with --empirical appended to prompt arguments"
      pattern: "--empirical"
    - from: "~/.claude/agents/pde-phase-researcher.md"
      to: "agents/pde-experiment-runner.md"
      via: "Task(subagent_type=pde-experiment-runner) dispatch in empirical mode steps"
      pattern: "pde-experiment-runner"
    - from: "~/.claude/agents/pde-phase-researcher.md"
      to: "RESEARCH.md output"
      via: "Experiments Attempted section in output_format"
      pattern: "Experiments Attempted"
---

<objective>
Add empirical hypothesis-testing mode to the research agent and auto-routing in the research workflow, so optimization-focused phases get research grounded in measured outcomes rather than desk research alone.

Purpose: Research findings for optimization phases should be validated against real metrics, not just literature. The researcher becomes a lightweight orchestrator around the existing experiment runner infrastructure (Phases 100-104).
Output: Modified pde-phase-researcher.md with --empirical flag, modified research-phase.md with keyword auto-routing, 3 structural test files.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-researcher-empirical-mode/105-RESEARCH.md

<interfaces>
<!-- Key files the executor needs. Extracted from codebase. -->

From ~/.claude/agents/pde-phase-researcher.md (current structure):
- YAML frontmatter with name, description, tools, color, skills
- role section defining responsibilities
- execution_flow with Steps 1-8 (scope, domains, protocol, validation, quality, write, commit, return)
- output_format defining RESEARCH.md structure (sections: Summary, Standard Stack, Architecture Patterns, Don't Hand-Roll, Common Pitfalls, Code Examples, State of the Art, Open Questions, Validation Architecture, Sources, Metadata)
- structured_returns with return format

From workflows/research-phase.md (current structure):
- Step 0: Resolve Model Profile
- Step 1: Normalize and Validate Phase
- Step 2: Check Existing Research
- Step 3: Gather Phase Context (extracts phase_dir, padded_phase, phase_number, context_path)
- Step 4: Spawn Researcher (Task() call with prompt, subagent_type, model)
- Step 5: Handle Return

From agents/pde-experiment-runner.md:
- subagent_type: "pde-experiment-runner"
- argument-hint: "[experiment-md-path] [iteration] [baseline-sha] [context-mode full|diff]"
- model: haiku

From tests/phase-104/nyquist-metric.test.mjs (test pattern reference):
- Uses node:test, node:assert/strict, node:fs, node:path
- Structural tests: file existence, content pattern matching via fs.readFileSync + includes/match
- ROOT = path.resolve(__dirname, '../..')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 structural tests for RSRCH-01, RSRCH-02, RSRCH-03</name>
  <files>tests/phase-105/researcher-empirical-flag.test.mjs, tests/phase-105/empirical-routing.test.mjs, tests/phase-105/experiments-attempted-section.test.mjs</files>
  <read_first>
    - tests/phase-104/nyquist-metric.test.mjs (test pattern reference -- use identical structure)
    - ~/.claude/agents/pde-phase-researcher.md (current content to understand what will be added)
    - workflows/research-phase.md (current content to understand what will be added)
  </read_first>
  <action>
Create three structural test files following the exact pattern from tests/phase-104/nyquist-metric.test.mjs (node:test, node:assert/strict, fs.readFileSync content checks).

**tests/phase-105/researcher-empirical-flag.test.mjs** (RSRCH-01):
- Read pde-phase-researcher.md via path.join(os.homedir(), '.claude/agents/pde-phase-researcher.md')
- Assert file contains "--empirical" flag reference
- Assert file contains "Empirical Mode" section heading (the mode steps block)
- Assert file contains steps E1 through E8 (at minimum E1, E3, E5, E8 step references)
- Assert file contains "pde-experiment-runner" subagent dispatch reference
- Assert file contains "experiment cleanup" cleanup reference
- Assert file contains fallback to desk research when no verify command found

**tests/phase-105/empirical-routing.test.mjs** (RSRCH-02):
- Read workflows/research-phase.md via path.join(ROOT, 'workflows/research-phase.md')
- Assert file contains "Detect Empirical Mode" or "Empirical Mode" step heading
- Assert file contains at least 3 of the strong optimization keywords: optimize, optimization, experiment, empirical, benchmark, ablation
- Assert file contains "--empirical" string (appended to researcher args)
- Assert file contains "Auto-routing to empirical mode" display message

**tests/phase-105/experiments-attempted-section.test.mjs** (RSRCH-03):
- Read pde-phase-researcher.md
- Assert file contains "## Experiments Attempted" section definition in the output format
- Assert section template contains "Metric" field reference
- Assert section template contains "Iterations" or "iterations" field reference
- Assert section template contains table header with "Status" column (for KEEP/DISCARD/CRASH)
- Assert section template contains "Synthesis" field reference

All tests MUST fail initially (RED) because the agent and workflow have not been modified yet.
  </action>
  <verify>
    <automated>node --test tests/phase-105/*.test.mjs 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "empirical" tests/phase-105/researcher-empirical-flag.test.mjs returns >= 3
    - grep -c "optimize\|empirical\|benchmark" tests/phase-105/empirical-routing.test.mjs returns >= 3
    - grep -c "Experiments Attempted" tests/phase-105/experiments-attempted-section.test.mjs returns >= 1
    - Three test files exist in tests/phase-105/
  </acceptance_criteria>
  <done>Three structural test files exist covering all RSRCH-01/02/03 behaviors. Tests run (expected to fail RED since implementation not yet done).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add empirical mode to pde-phase-researcher.md and auto-routing to research-phase.md</name>
  <files>~/.claude/agents/pde-phase-researcher.md, workflows/research-phase.md</files>
  <read_first>
    - ~/.claude/agents/pde-phase-researcher.md (full current content -- will be modified)
    - workflows/research-phase.md (full current content -- will be modified)
    - workflows/optimize.md (reference for Task() dispatch pattern with pde-experiment-runner)
    - agents/pde-experiment-runner.md (subagent interface -- argument-hint format)
    - templates/experiment.md (experiment.md format for transient construction)
    - .planning/phases/105-researcher-empirical-mode/105-RESEARCH.md (architecture patterns and code examples)
  </read_first>
  <behavior>
    - Test: researcher-empirical-flag.test.mjs passes (--empirical flag, E1-E8 steps, runner dispatch, cleanup, fallback)
    - Test: empirical-routing.test.mjs passes (Step 3.5, keyword list, --empirical append, auto-routing message)
    - Test: experiments-attempted-section.test.mjs passes (output format section, metric/iterations/status/synthesis fields)
  </behavior>
  <action>
**Modify pde-phase-researcher.md (at ~/.claude/agents/pde-phase-researcher.md):**

1. In the execution_flow section, add a new Step 0 BEFORE existing Step 1:

   "## Step 0: Mode Detection"
   Parse $ARGUMENTS for --empirical flag. If present, skip Steps 1-8 and jump to "Empirical Mode Steps" below.

2. After the existing Step 8 (Return Structured Result), add a new section titled "## Empirical Mode Steps (--empirical flag detected)". This section contains steps E1-E8:

   **E1: Determine Research Metric** - From phase context (CONTEXT.md, ROADMAP goal, requirements), identify what is being optimized, what verify command produces a float metric, what files are eligible for mutation (must have OPTIMIZABLE markers), and direction (max/min). If no verify command can be determined: fall back to desk research (Steps 1-8). Log: "Empirical mode requires a measurable metric. No verify command found -- falling back to desk research."

   **E2: Construct Transient experiment.md** - Write to /tmp/empirical-{phase-number}-experiment.md with YAML frontmatter containing: slug (research-{phase-number}), metric (from context or default nyquist_pass_count), direction (max), verify (the verify command), mutable_files (relevant files), iteration_budget (10), time_budget_minutes (20). Include Search Space, Constraints, and Stopping Rationale prose sections after the frontmatter.

   **E3: Init Experiment State** - Run: node bin/pde-tools.cjs experiment init --slug research-{phase-number}

   **E4: Capture Baseline** - Run: node bin/pde-tools.cjs experiment eval-metric --slug research-{phase-number}

   **E5: Run Iterations** - Loop up to 10 iterations. Set context_mode to "full" for iteration 1, "diff" for subsequent. Dispatch Task(subagent_type="pde-experiment-runner") with the experiment.md content, iteration number, baseline SHA, and context mode. Parse JSON result. Track consecutiveFailures counter: increment on DISCARD/CRASH, reset on KEEP. If consecutiveFailures >= 3, break early. Display "Empirical iteration {N}: {status} | metric={value} | delta={delta}" after each iteration.

   **E6: Collect Results** - Read results.jsonl and EXPERIMENT-BEST.json from .planning/experiments/research-{phase-number}/ immediately before writing RESEARCH.md (keep read-and-write close to avoid context loss per Pitfall 5 from RESEARCH.md).

   **E7: Cleanup Branch** - Run: node bin/pde-tools.cjs experiment cleanup --slug research-{phase-number}. Always run regardless of outcome. Verify no experiment branches remain.

   **E8: Write RESEARCH.md** - Write standard RESEARCH.md sections PLUS the "Experiments Attempted" section populated from results.jsonl. Then return structured result.

3. In the output_format section, add the "Experiments Attempted" section as a conditional block AFTER "## Common Pitfalls" and BEFORE "## Sources". The section format:

   "## Experiments Attempted"
   Include this section ONLY when empirical mode ran.
   Required fields: Mode (Empirical), Metric ({name} ({direction})), Verify command, Iterations run ({completed} / {budget}), Best result ({value} iteration {N} commit {sha}).
   Required table with columns: #, ID, Change Description, Metric, Delta, Status (KEEP/DISCARD/CRASH).
   Required Synthesis field: 1-2 sentences summarizing what experiments revealed.

**Modify workflows/research-phase.md:**

4. Between existing Step 3 (Gather Phase Context) and Step 4 (Spawn Researcher), add Step 3.5:

   "## Step 3.5: Detect Empirical Mode"
   Read the ROADMAP goal for this phase and the phase CONTEXT.md (if exists). Check for optimization keywords (case-insensitive): optimize, optimization, experiment, empirical, benchmark, tune, metric, measure, performance, scoring, ablation. Require at least one "strong" keyword: optimize, optimization, experiment, empirical, benchmark, ablation. If strong keyword matches OR if CONTEXT.md explicitly contains "empirical" or "experiment": set RESEARCHER_ARGS="--empirical" and display "Auto-routing to empirical mode (keyword: {matched_keyword})". Else: set RESEARCHER_ARGS="" for standard desk research.

5. In Step 4 (Spawn Researcher), modify the Task() prompt to include {RESEARCHER_ARGS} so --empirical is passed through when auto-detected. Add "Arguments: {RESEARCHER_ARGS}" to the prompt template.

**Anti-patterns to avoid (from 105-RESEARCH.md):**
- Do NOT duplicate the full optimize.md circuit-breaker machinery. Only use consecutive-failure counter (3 = halt).
- Do NOT write transient experiment.md to .planning/experiments/. Use /tmp/ to avoid polluting project experiment history.
- Do NOT skip the cleanup step (E7). Experiment branches must always be cleaned up.
- Do NOT block on BREAK-05 cost confirmation. Empirical mode has reduced budget (10 iterations) making auto-proceed safe.
  </action>
  <verify>
    <automated>node --test tests/phase-105/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "empirical" ~/.claude/agents/pde-phase-researcher.md returns >= 10
    - grep -c "Experiments Attempted" ~/.claude/agents/pde-phase-researcher.md returns >= 2
    - grep -c "Detect Empirical Mode" workflows/research-phase.md returns >= 1
    - grep -c "empirical" workflows/research-phase.md returns >= 3
    - node --test tests/phase-105/*.test.mjs exits 0
  </acceptance_criteria>
  <done>All three structural tests pass GREEN. pde-phase-researcher.md has --empirical flag, empirical mode steps E1-E8, experiment runner dispatch, cleanup, fallback to desk research, and "Experiments Attempted" output format section. research-phase.md has Step 3.5 keyword detection with strong keyword gate and --empirical arg passthrough to Task() spawn.</done>
</task>

</tasks>

<verification>
1. All 3 structural tests pass: node --test tests/phase-105/*.test.mjs
2. pde-phase-researcher.md contains --empirical flag handling and empirical mode steps
3. research-phase.md contains keyword detection and auto-routing logic
4. RESEARCH.md output format includes "Experiments Attempted" section definition
5. Full Nyquist suite unaffected: node --test tests/**/*.test.mjs (no regressions)
</verification>

<success_criteria>
- RSRCH-01: pde-phase-researcher.md has --empirical flag parsing in Step 0, empirical mode steps E1-E8, and delegates to pde-experiment-runner via Task()
- RSRCH-02: research-phase.md Step 3.5 detects optimization keywords and appends --empirical to researcher spawn arguments
- RSRCH-03: pde-phase-researcher.md output_format includes "Experiments Attempted" section with metric, iterations, result table, and synthesis
- All structural tests in tests/phase-105/ pass
- No regressions in existing test suites
</success_criteria>

<output>
After completion, create .planning/phases/105-researcher-empirical-mode/105-01-SUMMARY.md
</output>
