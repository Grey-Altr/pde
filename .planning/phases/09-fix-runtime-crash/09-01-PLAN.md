---
phase: 09-fix-runtime-crash
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/telemetry.cjs
autonomous: true
requirements:
  - BRAND-04
  - BRAND-05

must_haves:
  truths:
    - "require('../telemetry.cjs') in render.cjs resolves without MODULE_NOT_FOUND"
    - "banner(), panel(), progress(), checkpoint(), divider(), splash() all execute without crash"
    - "UI banners render PDE-branded stage names (not GSD)"
  artifacts:
    - path: "lib/telemetry.cjs"
      provides: "Telemetry module with full API surface consumed by render.cjs"
      exports: ["getTrackingPlanVersion", "checkConsent", "saveConsent", "getSessionId", "appendEvent", "readEvents", "getStatus"]
  key_links:
    - from: "lib/ui/render.cjs"
      to: "lib/telemetry.cjs"
      via: "require('../telemetry.cjs') at line 10"
      pattern: "require\\('\\.\\./telemetry\\.cjs'\\)"
---

<objective>
Create the missing lib/telemetry.cjs module to fix the critical MODULE_NOT_FOUND crash that prevents all UI rendering in PDE.

Purpose: render.cjs requires telemetry.cjs at load time (line 10). Without it, every banner(), panel(), progress(), checkpoint(), divider(), and splash() call across 14 workflow files crashes immediately. BRAND-04 and BRAND-05 are already correct at the source level (PDE-branded stage names, zero GSD strings) but cannot render because the module is missing.

Output: A working lib/telemetry.cjs that exports the 7-function API surface render.cjs depends on.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fix-runtime-crash/09-RESEARCH.md

<interfaces>
<!-- render.cjs line 10 — the require that crashes -->
const telemetry = require('../telemetry.cjs');

<!-- Functions called on the telemetry object by render.cjs: -->
telemetry.getTrackingPlanVersion()     — returns string version (e.g., "v1")
telemetry.checkConsent(version)        — returns boolean
telemetry.saveConsent(version)         — persists consent to disk
telemetry.getSessionId()              — returns stable hex string per process
telemetry.appendEvent(event)           — appends JSON event to JSONL log
telemetry.readEvents(filterType)       — returns array of event objects
telemetry.getStatus()                  — returns { consented, version, eventCount, fileSize, lastEvent }

<!-- Path resolution: -->
<!-- lib/ui/render.cjs requires ../telemetry.cjs → resolves to lib/telemetry.cjs -->

<!-- Existing project patterns: -->
<!-- All lib/ files use 'use strict'; CommonJS with .cjs extension -->
<!-- Home dir override: process.env.PDE_TEST_DIR || os.homedir() -->
<!-- Telemetry storage path: ~/.pde/telemetry/ -->
<!-- Zero npm dependencies — Node.js built-ins only -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/telemetry.cjs with full API surface</name>
  <files>lib/telemetry.cjs</files>
  <action>
Create lib/telemetry.cjs implementing the complete 7-function API that render.cjs depends on. Use the authoritative skeleton from 09-RESEARCH.md (section "Complete telemetry.cjs skeleton") as the implementation — it was derived directly from render.cjs call sites.

Key implementation rules:
- CommonJS format: 'use strict'; module.exports = { ... }
- Zero npm dependencies — use only node:fs, node:os, node:path, node:crypto (lazy-loaded)
- Zero side effects at require-time — no fs calls outside function bodies
- All file operations wrapped in try/catch returning safe defaults ([] for readEvents, false for checkConsent, 0 for fileSize)
- Use process.env.PDE_TEST_DIR || os.homedir() as base dir (matches render.cjs pattern)
- Telemetry data stored in ~/.pde/telemetry/ (consent.json + events.jsonl)
- Session ID: crypto.randomBytes(8).toString('hex'), cached per-process in module-level variable
- getStatus() must return numeric fileSize (default 0), not undefined — render.cjs does (status.fileSize / 1024).toFixed(1)
- Lazy directory creation via fs.mkdirSync(dir, { recursive: true }) on first write, not at require-time

The 7 exported functions:
1. getTrackingPlanVersion() — returns 'v1' constant
2. checkConsent(version) — reads consent.json, returns boolean
3. saveConsent(version) — writes consent.json with { version, accepted: true, timestamp }
4. getSessionId() — returns cached random hex string
5. appendEvent(event) — appends JSON line to events.jsonl
6. readEvents(filterType) — reads events.jsonl, filters by type if provided
7. getStatus() — returns { consented, version, eventCount, fileSize, lastEvent }
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node -e "const t = require('./lib/telemetry.cjs'); console.log(Object.keys(t).sort().join(',')); console.log('version:', t.getTrackingPlanVersion()); console.log('session:', typeof t.getSessionId()); console.log('consent:', t.checkConsent('v1')); console.log('events:', JSON.stringify(t.readEvents(null))); console.log('status:', JSON.stringify(t.getStatus()))"</automated>
  </verify>
  <done>lib/telemetry.cjs exists, exports all 7 functions, require() succeeds without error, all functions return correct types</done>
</task>

<task type="auto">
  <name>Task 2: Verify render.cjs and all UI commands execute without crash</name>
  <files>lib/telemetry.cjs</files>
  <action>
Run the full render.cjs command surface to confirm the MODULE_NOT_FOUND crash is resolved. Execute each render.cjs command and verify exit code 0:

1. node lib/ui/render.cjs banner "PDE TEST" — must exit 0 with block-char output (BRAND-04)
2. node lib/ui/render.cjs splash — must exit 0 with PDE logo (BRAND-05)
3. node lib/ui/render.cjs panel "TITLE" — must exit 0
4. node lib/ui/render.cjs progress "Loading" — must exit 0
5. node lib/ui/render.cjs checkpoint "TEST" — must exit 0
6. node lib/ui/render.cjs divider — must exit 0

If any command fails, diagnose and fix lib/telemetry.cjs. The fix is in this file only — do NOT modify render.cjs or any other file.

After all commands pass, confirm zero GSD strings in lib/telemetry.cjs:
  grep -ni "gsd\|get-shit-done" lib/telemetry.cjs  — must return nothing
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && failed=0; for cmd in banner splash panel progress checkpoint divider; do node lib/ui/render.cjs $cmd "TEST" >/dev/null 2>&1 || { echo "FAIL: $cmd"; failed=1; }; done; test $failed -eq 0 && echo "ALL PASS" || echo "SOME FAILED"; grep -cni "gsd\|get-shit-done" lib/telemetry.cjs 2>/dev/null && echo "GSD FOUND" || echo "BRAND CLEAN"</automated>
  </verify>
  <done>All 6 render.cjs commands exit 0 without MODULE_NOT_FOUND. Zero GSD strings in telemetry.cjs. BRAND-04 and BRAND-05 are functionally satisfied — UI banners and stage displays render correctly.</done>
</task>

</tasks>

<verification>
1. lib/telemetry.cjs exists at the correct path (lib/telemetry.cjs, NOT lib/ui/telemetry.cjs)
2. require('../telemetry.cjs') from lib/ui/render.cjs resolves without error
3. All 6 render.cjs commands (banner, splash, panel, progress, checkpoint, divider) exit 0
4. Zero GSD references in lib/telemetry.cjs
5. No side effects at require-time (no directory creation on require)
</verification>

<success_criteria>
- BRAND-04: banner() calls across 14 workflow files can execute — "PDE ►" stage names render
- BRAND-05: splash, progress, checkpoint, panel all render with PDE branding
- Zero MODULE_NOT_FOUND errors from render.cjs
- lib/telemetry.cjs has zero npm dependencies and zero GSD strings
</success_criteria>

<output>
After completion, create `.planning/phases/09-fix-runtime-crash/09-01-SUMMARY.md`
</output>
