---
phase: 160-declarative-approval-gates-workflow-flags
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/wireframe.md
  - workflows/mockup.md
  - workflows/critique.md
  - workflows/competitive.md
  - dashboard/lib/__tests__/workflow-flags.test.ts
autonomous: true
requirements:
  - WFL-02
  - WFL-03
  - WFL-04
  - WFL-05

must_haves:
  truths:
    - "Running /pde:wireframe --webmcp produces output with a WebMCP Context section"
    - "Running /pde:mockup --webmcp produces output with a WebMCP Context section"
    - "Running /pde:critique --webmcp produces output with a WebMCP Context section"
    - "Running /pde:competitive --webmcp produces output with a WebMCP Context section"
    - "Running any workflow without --webmcp produces unchanged output (no WebMCP section)"
  artifacts:
    - path: "workflows/wireframe.md"
      provides: "--webmcp flag support"
      contains: "--webmcp"
    - path: "workflows/mockup.md"
      provides: "--webmcp flag support"
      contains: "--webmcp"
    - path: "workflows/critique.md"
      provides: "--webmcp flag support"
      contains: "--webmcp"
    - path: "workflows/competitive.md"
      provides: "--webmcp flag support"
      contains: "--webmcp"
    - path: "dashboard/lib/__tests__/workflow-flags.test.ts"
      provides: "Source-inspection tests for --webmcp flag in all four workflows"
  key_links:
    - from: "workflows/wireframe.md"
      to: "pde_approval_gate tool"
      via: "WebMCP Context section references tool name and gate_id"
      pattern: "pde_approval_gate"
    - from: "workflows/mockup.md"
      to: "pde_approval_gate tool"
      via: "WebMCP Context section references tool name and gate_id"
      pattern: "pde_approval_gate"
---

<objective>
Add --webmcp flag to all four design workflow commands (wireframe, mockup, critique, competitive) that appends a WebMCP tool context section to workflow output for browser AI agent consumption.

Purpose: WFL-02 through WFL-05 require each workflow to produce WebMCP-enhanced output when the flag is passed, while preserving unchanged behavior without it.
Output: Four modified workflow markdown files with --webmcp flag table entry, parse step, and conditional output section. Tests verifying flag presence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-declarative-approval-gates-workflow-flags/160-CONTEXT.md
@.planning/phases/160-declarative-approval-gates-workflow-flags/160-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create workflow flags test scaffold + add --webmcp to wireframe.md and mockup.md</name>
  <files>
    dashboard/lib/__tests__/workflow-flags.test.ts,
    workflows/wireframe.md,
    workflows/mockup.md
  </files>
  <read_first>
    workflows/wireframe.md,
    workflows/mockup.md,
    dashboard/lib/__tests__/manifest.test.ts
  </read_first>
  <behavior>
    - Test: wireframe.md contains `--webmcp` in the flags table
    - Test: wireframe.md contains `USE_WEBMCP` variable set
    - Test: wireframe.md contains `## WebMCP Context` conditional section
    - Test: wireframe.md contains `pde_approval_gate` in the WebMCP Context section
    - Test: mockup.md contains `--webmcp` in the flags table
    - Test: mockup.md contains `USE_WEBMCP` variable set
    - Test: mockup.md contains `## WebMCP Context` conditional section
    - Test: mockup.md contains `pde_approval_gate` in the WebMCP Context section
    - Test: critique.md contains `--webmcp` (will fail initially — RED)
    - Test: competitive.md contains `--webmcp` (will fail initially — RED)
  </behavior>
  <action>
    Per D-06, D-07, D-08, D-09: Add --webmcp flag following the exact --use-stitch pattern.

    **1. Create test file first (RED phase):**

    `dashboard/lib/__tests__/workflow-flags.test.ts`:
    - Source-inspection tests using `fs.readFileSync` to read each workflow file
    - For each of the four workflows (wireframe.md, mockup.md, critique.md, competitive.md):
      - Assert file contains `| \`--webmcp\`` in flags table
      - Assert file contains `USE_WEBMCP`
      - Assert file contains `## WebMCP Context`
      - Assert file contains `pde_approval_gate`
    - Use `path.resolve(__dirname, '../../../workflows/{name}.md')` to locate files (relative from dashboard/lib/__tests__/)

    **2. Modify wireframe.md (GREEN for WFL-02):**

    Three insertions following the --use-stitch pattern exactly:

    **(a) Flags table** — Add row after `--use-stitch` row (line 32):
    ```
    | `--webmcp` | Boolean | Append WebMCP tool context section to output for browser AI agent consumption. Does not affect artifact generation. |
    ```

    **(b) Parse step** — Add new step `2h. Parse --webmcp flag` after the existing `2g. Parse --use-stitch flag` section (after line 211):
    ```markdown
    #### 2h. Parse --webmcp flag

    Check $ARGUMENTS for `--webmcp`:
    - If present: SET USE_WEBMCP = true. Log: `  -> --webmcp detected: WebMCP context section will be appended to output.`
    - If absent: SET USE_WEBMCP = false.
    ```

    Also add `--webmcp` to the early flags check list on line 39 (the line that says "Check for flags in $ARGUMENTS before beginning").

    **(c) Output section** — Find the final output summary step (Step 7f) and append AFTER its closing content:
    ```markdown
    IF USE_WEBMCP is true, append this section after the standard output summary:

    ---

    ## WebMCP Context

    This output includes context for browser AI agents accessing PDE via WebMCP.

    **Available tools registered in this browser session:**

    | Tool | Call When | Required Input |
    |------|-----------|----------------|
    | `pde_approval_gate` | To approve or reject this wireframe output | `gate_id` (shown below), `action` (`approve` or `reject`) |
    | `get_design_state` | To check current PDE design phase | (none) |
    | `list_artifacts` | To enumerate all design artifacts | `filter` (optional) |
    | `get_project_info` | To get project name and milestone | (none) |

    **Pending gate for this run:**

    Gate ID: `wireframe-{PHASE_NUMBER}-{YYYYMMDD}-{4_HEX}`

    To approve via WebMCP tool call:
    ```json
    {
      "name": "pde_approval_gate",
      "arguments": { "gate_id": "wireframe-{PHASE_NUMBER}-{YYYYMMDD}-{4_HEX}", "action": "approve" }
    }
    ```

    To reject via WebMCP tool call:
    ```json
    {
      "name": "pde_approval_gate",
      "arguments": { "gate_id": "wireframe-{PHASE_NUMBER}-{YYYYMMDD}-{4_HEX}", "action": "reject", "reason": "..." }
    }
    ```

    > Gate state file: `.planning/gates/{GATE_ID}.json`
    > Fallback: Users not using WebMCP can approve via the dashboard approval UI.
    ```

    **3. Modify mockup.md (GREEN for WFL-03):**

    Same three insertions as wireframe.md, adapted for mockup:

    **(a) Flags table** — Add `--webmcp` row after `--use-stitch` row (line 69)
    **(b) Parse step** — Add `2f. Parse --webmcp flag` after existing `2e. Parse --use-stitch flag` (after line 170). Also add `--webmcp` to the early flags check list (line 77).
    **(c) Output section** — Append WebMCP Context section at the final output step. Replace "wireframe" with "mockup" in tool description and gate ID prefix: `mockup-{PHASE_NUMBER}-{YYYYMMDD}-{4_HEX}`.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine/dashboard && npx vitest run --reporter=verbose lib/__tests__/workflow-flags.test.ts 2>&1 | grep -E "PASS|FAIL|wireframe|mockup|critique|competitive"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/wireframe.md contains `| \`--webmcp\`` in flags table
    - workflows/wireframe.md contains `USE_WEBMCP`
    - workflows/wireframe.md contains `## WebMCP Context`
    - workflows/wireframe.md contains `pde_approval_gate`
    - workflows/mockup.md contains `| \`--webmcp\`` in flags table
    - workflows/mockup.md contains `USE_WEBMCP`
    - workflows/mockup.md contains `## WebMCP Context`
    - workflows/mockup.md contains `pde_approval_gate`
    - dashboard/lib/__tests__/workflow-flags.test.ts exists and wireframe+mockup tests pass
  </acceptance_criteria>
  <done>wireframe.md and mockup.md have --webmcp flag in flags table, parse step, and conditional WebMCP Context output section. Test file created covering all four workflows (critique and competitive tests expected to fail until Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Add --webmcp flag to critique.md and competitive.md</name>
  <files>
    workflows/critique.md,
    workflows/competitive.md
  </files>
  <read_first>
    workflows/critique.md,
    workflows/competitive.md,
    workflows/wireframe.md
  </read_first>
  <action>
    Per D-08, D-09: Apply the identical --webmcp pattern to critique.md and competitive.md. Read wireframe.md first to see the completed pattern, then replicate exactly.

    **1. Modify critique.md (GREEN for WFL-04):**

    **(a) Flags table** — The critique flags table starts at line 16. Add `--webmcp` row after the last existing flag row (before `</flags>`):
    ```
    | `--webmcp` | Boolean | Append WebMCP tool context section to output for browser AI agent consumption. Does not affect critique analysis. |
    ```

    **(b) Parse step** — Find the argument parsing section (around line 35 where it says "Check for flags in $ARGUMENTS before beginning"). Add `--webmcp` to that list. Then add a new parse step after the last flag parse step in the init section:
    ```markdown
    #### {N}. Parse --webmcp flag

    Check $ARGUMENTS for `--webmcp`:
    - If present: SET USE_WEBMCP = true. Log: `  -> --webmcp detected: WebMCP context section will be appended to output.`
    - If absent: SET USE_WEBMCP = false.
    ```

    **(c) Output section** — Find the final output/summary step and append the WebMCP Context section. Replace workflow-specific text: tool description says "critique output", gate ID prefix is `critique-{PHASE_NUMBER}-{YYYYMMDD}-{4_HEX}`.

    **2. Modify competitive.md (GREEN for WFL-05):**

    **(a) Flags table** — The competitive flags table starts at line 64. Add `--webmcp` row after the last existing flag row:
    ```
    | `--webmcp` | Boolean | Append WebMCP tool context section to output for browser AI agent consumption. Does not affect competitive analysis. |
    ```

    **(b) Parse step** — Find argument parsing (around line 86). Add `--webmcp` to the early check list. Add parse step after the last flag parse:
    ```markdown
    #### {N}. Parse --webmcp flag

    Check $ARGUMENTS for `--webmcp`:
    - If present: SET USE_WEBMCP = true. Log: `  -> --webmcp detected: WebMCP context section will be appended to output.`
    - If absent: SET USE_WEBMCP = false.
    ```

    **(c) Output section** — Append WebMCP Context section at the final output step. Gate ID prefix is `competitive-{PHASE_NUMBER}-{YYYYMMDD}-{4_HEX}`.

    After both files are modified, run the full workflow-flags test suite to confirm all four workflows pass.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/Platform\ Development\ Engine/dashboard && npx vitest run --reporter=verbose lib/__tests__/workflow-flags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - workflows/critique.md contains `| \`--webmcp\`` in flags table
    - workflows/critique.md contains `USE_WEBMCP`
    - workflows/critique.md contains `## WebMCP Context`
    - workflows/critique.md contains `pde_approval_gate`
    - workflows/competitive.md contains `| \`--webmcp\`` in flags table
    - workflows/competitive.md contains `USE_WEBMCP`
    - workflows/competitive.md contains `## WebMCP Context`
    - workflows/competitive.md contains `pde_approval_gate`
    - All tests in workflow-flags.test.ts pass (exit 0)
  </acceptance_criteria>
  <done>All four workflow files (wireframe, mockup, critique, competitive) have --webmcp flag in flags table, parse step setting USE_WEBMCP, and conditional WebMCP Context output section. All workflow flag tests green.</done>
</task>

</tasks>

<verification>
- `cd dashboard && npx vitest run --reporter=verbose lib/__tests__/workflow-flags.test.ts` — all tests pass
- `grep -c "\-\-webmcp" workflows/wireframe.md` — returns 3+ (flags table, parse step, early check list)
- `grep -c "\-\-webmcp" workflows/mockup.md` — returns 3+
- `grep -c "\-\-webmcp" workflows/critique.md` — returns 3+
- `grep -c "\-\-webmcp" workflows/competitive.md` — returns 3+
- `grep "## WebMCP Context" workflows/wireframe.md workflows/mockup.md workflows/critique.md workflows/competitive.md` — all four contain the section
</verification>

<success_criteria>
- All four workflows have --webmcp flag in their flags table
- All four workflows parse the flag and set USE_WEBMCP variable
- All four workflows conditionally append a WebMCP Context section with tool table, gate ID, and usage examples
- Existing output is unchanged when --webmcp is not passed (additive only, per D-06)
- All workflow flag source-inspection tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/160-declarative-approval-gates-workflow-flags/160-02-SUMMARY.md`
</output>
