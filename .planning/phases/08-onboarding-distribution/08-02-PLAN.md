---
phase: 08-onboarding-distribution
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - GETTING-STARTED.md
autonomous: true
requirements: [BRAND-06]

must_haves:
  truths:
    - "A naive user can follow GETTING-STARTED.md from install to first project completion without external help"
    - "Every workflow stage (discuss, plan, execute, verify) has numbered steps with terminal output examples"
    - "Key .planning/ file snippets shown at milestones so users can verify they are on track"
    - "Command cheat sheet groups all /pde: commands by workflow stage"
    - "Zero GSD references anywhere in the document"
  artifacts:
    - path: "GETTING-STARTED.md"
      provides: "Walk-through tutorial for new PDE users"
      min_lines: 200
      contains: "/pde:new-project"
  key_links:
    - from: "GETTING-STARTED.md"
      to: "commands/"
      via: "References all /pde: commands in tutorial and cheat sheet"
      pattern: "/pde:"
---

<objective>
Write the GETTING-STARTED.md walk-through tutorial that takes a naive user from install to first project completion.

Purpose: This is the primary onboarding document. A user with no prior knowledge of PDE (or GSD) should be able to follow this guide end-to-end and ship their first project. Per user decision, this follows Stripe/Vercel documentation tone.

Output: `GETTING-STARTED.md` at repo root
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-onboarding-distribution/08-CONTEXT.md
@.planning/phases/08-onboarding-distribution/08-RESEARCH.md

<interfaces>
<!-- Command inventory for cheat sheet accuracy -->
34 /pde: commands exist in commands/ directory. Key commands by workflow stage:

Setup: /pde:new-project, /pde:settings, /pde:set-profile
Planning: /pde:discuss-phase, /pde:plan-phase, /pde:research-phase
Execution: /pde:execute-phase, /pde:quick
Verification: /pde:verify-work, /pde:diagnose-issues
Maintenance: /pde:progress, /pde:help, /pde:map-codebase, /pde:add-phase, /pde:add-todo, /pde:check-todos
Milestones: /pde:new-milestone, /pde:complete-milestone, /pde:audit-milestone

Install commands (from RESEARCH.md):
Step 1: /plugin marketplace add Grey-Altr/pde
Step 2: /plugin install platform-development-engine@pde
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write GETTING-STARTED.md walk-through tutorial</name>
  <files>GETTING-STARTED.md</files>
  <action>
Create `GETTING-STARTED.md` at repo root. Follow ALL locked decisions from CONTEXT.md exactly:

**Document structure (in order):**

1. **Title and intro** — "Getting Started with PDE" with a 1-2 sentence hook explaining what the user will accomplish

2. **Prerequisites** — Short section listing:
   - Claude Code installed (link to claude.ai/code)
   - Active Anthropic subscription (Claude Code requires it)
   - Git installed and configured
   - A project idea (or use one of the suggested examples)

3. **Philosophy** — 2-3 paragraphs explaining the workflow loop:
   - PDE structures development into phases, each flowing through discuss -> plan -> execute -> verify
   - Claude handles the building; you handle the vision and verification
   - .planning/ directory is the project's memory — persists across context resets
   - Mention `/clear` concept here: fresh context between stages prevents quality degradation

4. **Section 1: Install PDE** — Two-step install process:
   ```
   /plugin marketplace add Grey-Altr/pde
   /plugin install platform-development-engine@pde
   ```
   Verify: type `/pde:` and see commands in palette

5. **Section 2: Create Your Project** — Run `/pde:new-project`
   - Offer 2-3 sample project ideas:
     a) "Personal task tracker" — a CLI todo app with priorities and due dates
     b) "Weather dashboard" — a web app showing forecasts from a public API
     c) "Markdown blog engine" — a static site generator from markdown files
   - Or bring your own idea
   - PDE will ask questions to understand scope, then create PROJECT.md, REQUIREMENTS.md, ROADMAP.md
   - Show a snippet of what PROJECT.md looks like after creation (abbreviated example)

6. **Section 3: Discuss Your First Phase** — Run `/pde:discuss-phase`
   - PDE asks targeted questions about implementation preferences
   - Your answers become locked decisions that guide the build
   - Show example of what a CONTEXT.md decision block looks like
   - Mention: `/clear` after discuss to free context for planning

7. **Section 4: Plan the Phase** — Run `/pde:plan-phase`
   - PDE creates PLAN.md files with specific tasks, verification criteria, and wave structure
   - Show a brief snippet of a PLAN.md (abbreviated — just the task structure)
   - Explain waves: independent tasks run in parallel, dependent tasks run sequentially
   - Mention: `/clear` after planning

8. **Section 5: Execute** — Run `/pde:execute-phase`
   - PDE builds each task autonomously, committing after each
   - Checkpoints pause for your review when human judgment needed
   - Each completed task produces a SUMMARY.md
   - Mention: context resets happen automatically between plans

9. **Section 6: Verify** — Run `/pde:verify-work`
   - PDE checks built artifacts against requirements
   - Produces VERIFICATION.md showing pass/fail for each requirement
   - If gaps found: `/pde:plan-phase --gaps` creates fix plans

10. **What's Next** — Brief section covering:
    - Continue the loop: discuss -> plan -> execute -> verify for remaining phases
    - `/pde:progress` to see current state at any time
    - `/pde:new-milestone` when starting a new major version
    - `--auto` flag for autonomous advancement between phases
    - `/pde:settings` and `/pde:set-profile` for configuration
    - Point to GitHub issues for questions (no troubleshooting section)

11. **Command Cheat Sheet** — Table or grouped list of ALL /pde: commands, organized by workflow stage:
    - **Project Setup:** new-project, settings, set-profile
    - **Phase Planning:** discuss-phase, research-phase, plan-phase, discovery-phase
    - **Execution:** execute-phase, quick
    - **Verification:** verify-work, diagnose-issues
    - **Navigation:** progress, help, update
    - **Codebase:** map-codebase, cleanup
    - **Phase Management:** add-phase
    - **Task Management:** add-todo, check-todos
    - **Milestones:** new-milestone, complete-milestone, audit-milestone
    - Include /pde:quick here (per user decision: cheat sheet only, not in tutorial body)

**Tone and style rules (per user decisions):**
- Professional but approachable (Stripe/Vercel docs style)
- Short sentences, active voice
- Terminal output shown as code blocks — NO screenshots
- No marketing superlatives ("amazing", "powerful", "blazing fast")
- No time estimates
- No mention of GSD anywhere — PDE stands on its own
- No troubleshooting section — point to GitHub issues
- Concrete examples over abstract descriptions

**Anti-patterns to avoid:**
- Do NOT mention /pde:quick in the tutorial body (cheat sheet only)
- Do NOT add a troubleshooting section
- Do NOT include time estimates
- Do NOT use screenshots
- Do NOT reference GSD in any way
  </action>
  <verify>
    <automated>test -f GETTING-STARTED.md && wc -l GETTING-STARTED.md | awk '{if ($1 >= 200) print "OK: " $1 " lines"; else print "FAIL: only " $1 " lines"}' && grep -ci "gsd\|get-shit-done" GETTING-STARTED.md || echo "OK: Zero GSD references"</automated>
  </verify>
  <done>GETTING-STARTED.md exists at repo root with 200+ lines, contains all 6 numbered tutorial sections plus prerequisites, philosophy, What's Next, and command cheat sheet, zero GSD references, professional tone throughout</done>
</task>

</tasks>

<verification>
- `test -f GETTING-STARTED.md` succeeds (file exists at repo root)
- `wc -l GETTING-STARTED.md` shows 200+ lines
- `grep -c "/pde:" GETTING-STARTED.md` shows multiple command references
- `grep -c "/pde:new-project" GETTING-STARTED.md` shows at least 1 match
- `grep -ci "gsd\|get-shit-done" GETTING-STARTED.md` returns 0 (BRAND-06)
- Document has sections: Prerequisites, Philosophy, Install, Create Project, Discuss, Plan, Execute, Verify, What's Next, Cheat Sheet
</verification>

<success_criteria>
- A naive user reading GETTING-STARTED.md can install PDE, create a project, and complete their first discuss-plan-execute-verify cycle
- All locked decisions from CONTEXT.md are reflected in the document structure
- Zero GSD references (BRAND-06 compliance)
- Cheat sheet covers all /pde: commands grouped by workflow stage
</success_criteria>

<output>
After completion, create `.planning/phases/08-onboarding-distribution/08-02-SUMMARY.md`
</output>
