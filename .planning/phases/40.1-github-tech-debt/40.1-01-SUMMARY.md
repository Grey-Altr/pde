---
phase: 40.1-github-tech-debt
plan: 01
subsystem: github
tags: [mcp, github, allowed-tools, error-handling, try-catch]

# Dependency graph
requires:
  - phase: 40-github-integration
    provides: sync-github.md, brief-from-github.md, handoff-create-prs.md, sync.md, pipeline-status.md with initial implementations
provides:
  - commands/sync.md with mcp__github__* in allowed-tools (enables runtime MCP calls)
  - commands/pipeline-status.md with mcp__github__* in allowed-tools
  - workflows/brief-from-github.md Step 1 b.call wrapped in try/catch (graceful degradation)
  - workflows/handoff-create-prs.md Step 0 b.call wrapped in try/catch (graceful degradation)
affects: [sync-github, pipeline-status, brief-from-github, handoff-create-prs]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "allowed-tools mcp__github__* wildcard grants runtime MCP tool access in command files"
    - "b.call() wrapped in try/catch with let toolName = '' before the block — process.stdout.write after"

key-files:
  created: []
  modified:
    - commands/sync.md
    - commands/pipeline-status.md
    - workflows/brief-from-github.md
    - workflows/handoff-create-prs.md

key-decisions:
  - "mcp__github__* wildcard added to sync.md and pipeline-status.md only — no other tools added (Edit, Task, etc. not needed by these commands)"
  - "try/catch wraps only the b.call() line; let toolName declaration is before the block and process.stdout.write is after — matching sync-github.md reference pattern exactly"

patterns-established:
  - "Command files must include mcp__github__* in allowed-tools whenever the downstream workflow calls MCP tools via mcp-bridge"
  - "b.call() in workflow bash blocks must be wrapped in try/catch with toolName='' fallback for graceful MCP-unavailable degradation"

requirements-completed: [GH-01, GH-04]

# Metrics
duration: 5min
completed: 2026-03-19
---

# Phase 40.1 Plan 01: GitHub Tech Debt Gap Closure Summary

**mcp__github__* added to two command allowed-tools lists and b.call() wrapped in try/catch in two workflow bash blocks — four surgical edits closing all Phase 40 consistency gaps**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-19T03:27:00Z
- **Completed:** 2026-03-19T03:32:47Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- `commands/sync.md` and `commands/pipeline-status.md` now include `mcp__github__*` in their `allowed-tools` frontmatter — Claude Code will no longer silently block MCP calls initiated by downstream workflows
- `workflows/brief-from-github.md` Step 1 and `workflows/handoff-create-prs.md` Step 0 now wrap `b.call()` in try/catch, matching the defensive pattern already established in `workflows/sync-github.md`
- All canonical keys (`github:get-issue`, `github:create-pr`) preserved unchanged; `process.stdout.write` remains outside the try block in both files

## Task Commits

Each task was committed atomically:

1. **Task 1: Add mcp__github__* to sync.md and pipeline-status.md** - `dc47fd4` (feat)
2. **Task 2: Wrap b.call() in try/catch in brief-from-github.md and handoff-create-prs.md** - `609d172` (fix)

**Plan metadata:** (docs commit — see below)

## Files Created/Modified
- `commands/sync.md` - Added `  - mcp__github__*` after `  - Grep` in allowed-tools
- `commands/pipeline-status.md` - Added `  - mcp__github__*` after `  - Bash` in allowed-tools
- `workflows/brief-from-github.md` - Step 1: b.call('github:get-issue') now in try/catch with let toolName fallback
- `workflows/handoff-create-prs.md` - Step 0: b.call('github:create-pr') now in try/catch with let toolName fallback

## Decisions Made
- Only `mcp__github__*` was added to allowed-tools in each command file — no other tools (Edit, Task, etc.) were added to avoid expanding tool surface unnecessarily
- The try/catch pattern follows `sync-github.md` exactly: `let toolName = ''` before the try, assignment inside, catch resets to `''`, `process.stdout.write` after the block

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All four Phase 40 gap-closure edits complete; GH-01 and GH-04 requirements satisfied
- GitHub integration is now consistent: sync, pipeline-status, brief-from-github, and handoff-create-prs all correctly gate on MCP availability and won't crash on TOOL_MAP lookup failures

---
*Phase: 40.1-github-tech-debt*
*Completed: 2026-03-19*
