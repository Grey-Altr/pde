---
phase: 100-git-state-machine
plan: "02"
type: execute
wave: 2
depends_on: ["100-01"]
files_modified:
  - bin/pde-tools.cjs
  - tests/phase-100/experiment-dispatch.test.mjs
autonomous: true
requirements: [GIT-05]

must_haves:
  truths:
    - "All 6 experiment subcommands (init, commit, reset, promote, status, cleanup) dispatch correctly from pde-tools.cjs"
    - "Each subcommand produces structured JSON output"
    - "Unknown experiment subcommand produces a helpful error message listing available subcommands"
  artifacts:
    - path: "bin/pde-tools.cjs"
      provides: "Experiment subcommand dispatch via case experiment block"
      contains: "case 'experiment'"
    - path: "tests/phase-100/experiment-dispatch.test.mjs"
      provides: "Dispatch integration tests spawning pde-tools.cjs"
      contains: "experiment"
  key_links:
    - from: "bin/pde-tools.cjs"
      to: "bin/lib/experiment.cjs"
      via: "lazy require inside case experiment block"
      pattern: "require.*experiment\\.cjs"
---

<objective>
Wire the 6 experiment subcommands into pde-tools.cjs dispatch using the established lazy-require pattern, and add integration tests that spawn the CLI process to verify end-to-end dispatch.

Purpose: Makes the experiment state machine accessible via the standard pde-tools CLI interface, enabling all downstream phases (101-107) to call experiment operations through the established command pattern.
Output: Updated pde-tools.cjs with experiment case block, integration test file.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-git-state-machine/100-01-SUMMARY.md

<interfaces>
<!-- Dispatch pattern from pde-tools.cjs design case block (lines 519-546) -->

The design case uses lazy require and if/else subcommand routing:
  case 'design': {
    const subcommand = args[1];
    const design = require('./lib/design.cjs');
    if (subcommand === 'ensure-dirs') {
      design.cmdEnsureDirs(cwd, raw);
    } else if (...) {
      ...
    } else {
      error('Unknown design subcommand. Available: ...');
    }
    break;
  }

<!-- experiment.cjs exports from Plan 01 -->

  cmdExperimentInit(cwd, slug, raw)
  cmdExperimentCommit(cwd, slug, metric, description, raw)
  cmdExperimentReset(cwd, slug, raw)
  cmdExperimentPromote(cwd, slug, raw)
  cmdExperimentStatus(cwd, slug, raw)
  cmdExperimentCleanup(cwd, slug, raw)
  checkBoundaries(cwd, mutableFiles)

<!-- pde-tools.cjs arg parsing -->

  const args = process.argv.slice(2);
  const raw = args.includes('--raw');
  const command = args[0];
  // Subcommand: args[1]
  // Named flags: args.indexOf('--slug'), etc.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire experiment subcommands into pde-tools.cjs dispatch</name>
  <files>bin/pde-tools.cjs, tests/phase-100/experiment-dispatch.test.mjs</files>
  <read_first>
    - bin/pde-tools.cjs (full file to find insertion point before default case)
    - bin/lib/experiment.cjs (verify exported function signatures from Plan 01)
  </read_first>
  <behavior>
    - Test: pde-tools.cjs experiment init --slug test-slug produces JSON with initialized field (in temp git repo)
    - Test: pde-tools.cjs experiment status --slug test-slug produces JSON with found field
    - Test: pde-tools.cjs experiment unknown-sub produces error listing available subcommands
    - Test: pde-tools.cjs experiment with no subcommand produces error listing available subcommands
  </behavior>
  <action>
    Write tests FIRST in tests/phase-100/experiment-dispatch.test.mjs. Tests use execFileSync to spawn node bin/pde-tools.cjs with experiment subcommands. Tests create temp git repos and set cwd. Parse stdout as JSON to verify structured output.

    Then add a case 'experiment' block to the switch statement in bin/pde-tools.cjs. Insert BEFORE the default case (after the suggestions case block around line 826). Follow the exact design case pattern with lazy require:

    The case block must:
    1. Read subcommand from args[1]
    2. Lazy require('./lib/experiment.cjs')
    3. Parse --slug flag via args.indexOf('--slug') and read args[slugIdx + 1]
    4. Route to the correct cmd function based on subcommand string
    5. For 'commit' subcommand, additionally parse --metric (parseFloat) and --description flags
    6. Require --slug for all subcommands (error if missing)
    7. Require --metric for commit subcommand (error if missing or NaN)
    8. Output helpful error for unknown subcommands listing: init, commit, reset, promote, status, cleanup

    Also update the usage comment block at the top of pde-tools.cjs to document all 6 experiment subcommands with their flags:
    - experiment init --slug SLUG
    - experiment commit --slug SLUG --metric NUMBER --description TEXT
    - experiment reset --slug SLUG
    - experiment promote --slug SLUG
    - experiment status --slug SLUG
    - experiment cleanup --slug SLUG
  </action>
  <verify>
    <automated>node --test tests/phase-100/experiment-dispatch.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - grep "case 'experiment'" bin/pde-tools.cjs returns match
    - grep "require.*experiment.cjs" bin/pde-tools.cjs returns match
    - grep "experiment init" bin/pde-tools.cjs returns match in usage comment
    - grep "experiment commit" bin/pde-tools.cjs returns match in usage comment
    - grep "experiment reset" bin/pde-tools.cjs returns match in usage comment
    - grep "experiment promote" bin/pde-tools.cjs returns match in usage comment
    - grep "experiment status" bin/pde-tools.cjs returns match in usage comment
    - grep "experiment cleanup" bin/pde-tools.cjs returns match in usage comment
    - grep "slug" bin/pde-tools.cjs returns match for --slug flag parsing
    - node --test tests/phase-100/experiment-dispatch.test.mjs exits 0
  </acceptance_criteria>
  <done>
    All 6 experiment subcommands dispatch correctly from pde-tools.cjs. The --slug flag is parsed for all subcommands, --metric and --description flags parsed for commit. Unknown subcommands produce helpful error. Usage comment updated. Integration tests pass.
  </done>
</task>

</tasks>

<verification>
- node --test tests/phase-100/experiment-dispatch.test.mjs passes
- node --test tests/phase-100/experiment-state-machine.test.mjs still passes (regression)
- grep "case 'experiment'" bin/pde-tools.cjs returns exactly 1 match
</verification>

<success_criteria>
- All 6 experiment subcommands accessible via pde-tools.cjs CLI
- Each subcommand requires --slug flag and produces structured JSON
- commit subcommand additionally requires --metric flag
- Unknown subcommands produce helpful error listing available options
- Integration tests verify end-to-end dispatch through process spawning
</success_criteria>

<output>
After completion, create `.planning/phases/100-git-state-machine/100-02-SUMMARY.md`
</output>
