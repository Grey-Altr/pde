---
phase: 200-core-scraping-tools-competitive-recommend-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/competitive.md
  - workflows/competitive.md
  - commands/recommend.md
  - workflows/recommend.md
autonomous: true
requirements:
  - PIP-01

must_haves:
  truths:
    - "Running /pde:competitive uses firecrawl_search and firecrawl_extract when FIRECRAWL_AVAILABLE is true"
    - "Running /pde:competitive falls back to WebSearch when FIRECRAWL_AVAILABLE is false"
    - "Running /pde:recommend probes both WebSearch and Firecrawl independently"
    - "Firecrawl tools are listed in allowed-tools of both command files"
  artifacts:
    - path: "commands/competitive.md"
      provides: "Updated allowed-tools with Firecrawl MCP tools"
      contains: "mcp__firecrawl__firecrawl_search"
    - path: "workflows/competitive.md"
      provides: "FIRECRAWL_AVAILABLE probe block and Firecrawl-enhanced competitor analysis"
      contains: "FIRECRAWL_AVAILABLE"
    - path: "commands/recommend.md"
      provides: "Updated allowed-tools with Firecrawl search tool"
      contains: "mcp__firecrawl__firecrawl_search"
    - path: "workflows/recommend.md"
      provides: "Firecrawl dual-probe and enhanced catalog discovery"
      contains: "FIRECRAWL_AVAILABLE"
  key_links:
    - from: "workflows/competitive.md"
      to: "bin/lib/mcp-bridge.cjs"
      via: "probeFirecrawl() call in Step 3"
      pattern: "probeFirecrawl"
    - from: "workflows/recommend.md"
      to: "bin/lib/mcp-bridge.cjs"
      via: "probeFirecrawl() call in Step 3"
      pattern: "probeFirecrawl"
---

<objective>
Integrate Firecrawl into the competitive analysis and recommend workflows. Add FIRECRAWL_AVAILABLE probe blocks to both workflows, enhance competitive.md Step 4 with firecrawl_search + firecrawl_extract for live competitor data, and enhance recommend.md Step 4 with Firecrawl-powered tool discovery.

Purpose: Highest-value existing workflows gain live web intelligence via Firecrawl, with graceful WebSearch fallback.
Output: Modified commands/competitive.md, workflows/competitive.md, commands/recommend.md, workflows/recommend.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/200-core-scraping-tools-competitive-recommend-integration/200-RESEARCH.md

<interfaces>
<!-- Key patterns and section boundaries the executor needs -->

From workflows/competitive.md (section structure):
- Line 1: `<!-- LOCKED: purpose, required_reading, flags, process header, init steps, prerequisites, MCP probe -->`
- Lines 187-233: Step 3/8 MCP probe section (WebSearch probe at line 212, Sequential Thinking probe at line 220)
- Line 233: `<!-- /LOCKED -->`
- Line 235: `<!-- OPTIMIZABLE: competitive analysis guidance, comparison framing, analysis structure, output prose -->`
- Line 473: `<!-- /OPTIMIZABLE -->`
- Line 475: `<!-- LOCKED: artifact writes... -->`

Firecrawl probe insertion point: INSIDE Step 3, between the Sequential Thinking probe block and the `<!-- /LOCKED -->` comment at line 233. The probe block must go BEFORE line 233.

Step 4 enhancement: Between lines 235-473 (OPTIMIZABLE section). Add IF FIRECRAWL_AVAILABLE block in the competitor profile section.

From workflows/recommend.md (section structure):
- Line 1: `<!-- LOCKED: purpose, skill_code, required_reading, flags, process header, init steps, MCP probe -->`
- Lines 110-186: Step 3/7 MCP probes (mcp-compass line 132, WebSearch line 150, Sequential Thinking line 168)
- Line 186: `<!-- /LOCKED -->`
- Line 188: `<!-- OPTIMIZABLE: recommendation framing, context analysis, catalog matching, output structure -->`
- Line 332: `<!-- /OPTIMIZABLE -->`

Firecrawl probe insertion point: INSIDE Step 3, between Sequential Thinking probe and `<!-- /LOCKED -->` at line 186.

Step 4 enhancement: Between lines 188-332 (OPTIMIZABLE section). Add Firecrawl search after existing WebSearch results in catalog enrichment.

From commands/competitive.md (current allowed-tools):
```yaml
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Glob
  - Grep
  - Task
```
MISSING: All Firecrawl tools. Must add mcp__firecrawl__firecrawl_scrape, mcp__firecrawl__firecrawl_search, mcp__firecrawl__firecrawl_extract.

From commands/recommend.md (current allowed-tools):
```yaml
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Glob
  - Grep
  - Task
```
MISSING: Firecrawl search tool. Must add mcp__firecrawl__firecrawl_search.

From bin/lib/mcp-bridge.cjs (probe API):
- probeFirecrawl() -> { available: boolean, reason?: string, credits?: { remaining, total }, warning?: boolean }
- Call pattern: node --input-type=module with createRequire pattern (see 200-RESEARCH.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Firecrawl tools to command file allowed-tools</name>
  <files>commands/competitive.md, commands/recommend.md</files>
  <read_first>
    - commands/competitive.md (full file — see current allowed-tools list)
    - commands/recommend.md (full file — see current allowed-tools list)
    - commands/source.md (reference for Firecrawl tool name format — uses mcp__firecrawl__firecrawl_* double-prefix)
  </read_first>
  <action>
**Edit commands/competitive.md** — add these entries to the allowed-tools list in frontmatter (after existing Task entry):
```yaml
  - mcp__firecrawl__firecrawl_scrape
  - mcp__firecrawl__firecrawl_search
  - mcp__firecrawl__firecrawl_extract
```

**Edit commands/recommend.md** — add this entry to the allowed-tools list in frontmatter (after existing Task entry):
```yaml
  - mcp__firecrawl__firecrawl_search
```

Use the double-firecrawl prefix pattern (`mcp__firecrawl__firecrawl_*`) matching commands/source.md.
  </action>
  <verify>
    <automated>grep "mcp__firecrawl__firecrawl_scrape" commands/competitive.md && grep "mcp__firecrawl__firecrawl_search" commands/competitive.md && grep "mcp__firecrawl__firecrawl_extract" commands/competitive.md && grep "mcp__firecrawl__firecrawl_search" commands/recommend.md</automated>
  </verify>
  <acceptance_criteria>
    - commands/competitive.md allowed-tools contains `mcp__firecrawl__firecrawl_scrape`
    - commands/competitive.md allowed-tools contains `mcp__firecrawl__firecrawl_search`
    - commands/competitive.md allowed-tools contains `mcp__firecrawl__firecrawl_extract`
    - commands/recommend.md allowed-tools contains `mcp__firecrawl__firecrawl_search`
    - Existing allowed-tools entries (Read, Write, Edit, Bash, Glob, Grep, Task) are preserved in both files
    - No other content in either file is modified
  </acceptance_criteria>
  <done>Both command files have Firecrawl MCP tools in their allowed-tools lists, enabling Claude to call these tools during workflow execution.</done>
</task>

<task type="auto">
  <name>Task 2: Add Firecrawl probe and enhancement to competitive.md and recommend.md workflows</name>
  <files>workflows/competitive.md, workflows/recommend.md</files>
  <read_first>
    - workflows/competitive.md (full file — identify LOCKED/OPTIMIZABLE boundaries, Step 3 probe section, Step 4 competitor profiles)
    - workflows/recommend.md (full file — identify LOCKED/OPTIMIZABLE boundaries, Step 3 probe section, Step 4 catalog)
    - references/mcp-integration.md (lines 473-565 — Firecrawl probe pattern to replicate)
    - .planning/phases/200-core-scraping-tools-competitive-recommend-integration/200-RESEARCH.md (Patterns 5 and 6 — exact probe blocks and enhancement paths)
  </read_first>
  <action>
**Edit workflows/competitive.md Step 3** — insert Firecrawl probe block BEFORE the `<!-- /LOCKED -->` comment (after Sequential Thinking probe, before the closing LOCKED tag):

```markdown
**Probe Firecrawl MCP:**

IF --no-firecrawl NOT in $ARGUMENTS AND ALL_MCP_DISABLED = false:
  Run probeFirecrawl() via node --input-type=module pattern:
  ```bash
  node --input-type=module <<'PROBE_EOF'
  import { createRequire } from 'module';
  const req = createRequire(import.meta.url);
  const { probeFirecrawl } = req(`${process.env.CLAUDE_PLUGIN_ROOT}/bin/lib/mcp-bridge.cjs`);
  const result = probeFirecrawl();
  process.stdout.write(JSON.stringify(result));
  PROBE_EOF
  ```
  If result.available === true: SET FIRECRAWL_AVAILABLE = true
    Log: {timestamp} | CMP | firecrawl | probe | success | {duration_ms}
    IF result.warning: emit credit warning to user
  If result.available === false: SET FIRECRAWL_AVAILABLE = false
    Log: {timestamp} | CMP | firecrawl | probe | failure | reason={result.reason} | 0
    Tag: [Firecrawl unavailable ({reason}) -- using WebSearch for competitor intelligence]
ELSE:
  SET FIRECRAWL_AVAILABLE = false
  Log: {timestamp} | CMP | firecrawl | probe | skipped | 0
```

Update the Step 3 display line to include Firecrawl status:
`Display: Step 3/8: MCP probes complete. WebSearch: {available|unavailable}. Sequential Thinking: {available|unavailable}. Firecrawl: {available|unavailable}.`

Also add `--no-firecrawl` to the flags table in the LOCKED header section:
`| --no-firecrawl | Boolean | Skip Firecrawl MCP specifically while allowing other MCPs. |`

**Edit workflows/competitive.md Step 4** — add Firecrawl enhancement in the OPTIMIZABLE section, in the competitor profiles area (Step 4a-4f). Insert this block at the start of the competitive intelligence gathering:

```markdown
IF FIRECRAWL_AVAILABLE:
  For each competitor URL identified in Step 4a:
    1. Call mcp__firecrawl__firecrawl_search with query: "{competitor_name} pricing features"
       -> Use top result URLs to identify current pricing page
    2. Call mcp__firecrawl__firecrawl_scrape on competitor main URL with { onlyMainContent: true }
       -> Cache result via writeSource for future reference
    3. If pricing page found, call mcp__firecrawl__firecrawl_extract on pricing URL with schema:
       { "type": "object", "properties": {
           "pricing_tiers": { "type": "array", "items": { "type": "object", "properties": { "name": { "type": "string" }, "price": { "type": "string" }, "features": { "type": "array" } } } },
           "positioning": { "type": "string" }
       }}
       Note: This schema is a SUGGESTED starting point. The extract call costs 5 credits.
    4. Cache extracted data via writeSource() if content is substantial
    Tag: [Enhanced by Firecrawl MCP -- competitor data extracted from live site]
ELSE (WebSearch fallback):
  Use existing WebSearch path (already present in the workflow)
  Tag: [Baseline mode -- Firecrawl unavailable, using WebSearch]
```

Update the artifact footer tags section to add Firecrawl attribution:
`{If Firecrawl MCP was used: "[Enhanced by Firecrawl MCP -- live competitor data as of {date}]"}`

**Edit workflows/recommend.md Step 3** — insert Firecrawl probe block BEFORE `<!-- /LOCKED -->` (after Sequential Thinking probe):

```markdown
**Probe Firecrawl MCP:**

IF --no-firecrawl NOT in $ARGUMENTS AND ALL_MCP_DISABLED = false:
  Run probeFirecrawl() via node --input-type=module pattern (same as competitive.md)
  If result.available === true: SET FIRECRAWL_AVAILABLE = true
    Log: -> Firecrawl MCP: available (live tool discovery via structured search enabled)
  If result.available === false: SET FIRECRAWL_AVAILABLE = false
    Log: -> Firecrawl MCP: unavailable (Firecrawl tool discovery disabled)
    Tag: [Firecrawl unavailable -- Firecrawl tool discovery disabled]
ELSE:
  SET FIRECRAWL_AVAILABLE = false
```

Update the Step 3 display to include Firecrawl:
`Display: Step 3/7: MCP probes complete. mcp-compass: {available|unavailable}. WebSearch: {available|unavailable}. Sequential Thinking: {available|unavailable}. Firecrawl: {available|unavailable}.`

Also add `--no-firecrawl` to the flags table.

**Edit workflows/recommend.md Step 4** — add Firecrawl enhancement in the OPTIMIZABLE catalog enrichment section:

```markdown
IF FIRECRAWL_AVAILABLE:
  After mcp-compass results and WebSearch results, use Firecrawl for deeper tool discovery:
  3. **Firecrawl results** (if FIRECRAWL_AVAILABLE = true): Call mcp__firecrawl__firecrawl_search with query: "MCP servers for {primary_language} {framework} 2026" with category: "technology", limit: 10
     Extract tool names, install commands, and descriptions from results.
     Deduplicate against catalog results already found from mcp-compass and WebSearch.
     Tag: [Enhanced by Firecrawl MCP -- live tool discovery via structured search]
```

Update the recommend.md artifact Enhanced By field to include Firecrawl:
In the metadata section, add Firecrawl to the comma-separated MCPs list.

Update the footer tag:
`If FIRECRAWL_AVAILABLE = true: [Enhanced by Firecrawl MCP -- live tool discovery]`
  </action>
  <verify>
    <automated>grep "FIRECRAWL_AVAILABLE" workflows/competitive.md && grep "probeFirecrawl" workflows/competitive.md && grep "firecrawl_search" workflows/competitive.md && grep "firecrawl_extract" workflows/competitive.md && grep "FIRECRAWL_AVAILABLE" workflows/recommend.md && grep "probeFirecrawl" workflows/recommend.md && grep "firecrawl_search" workflows/recommend.md && grep "no-firecrawl" workflows/competitive.md && grep "no-firecrawl" workflows/recommend.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/competitive.md contains "FIRECRAWL_AVAILABLE" variable set from probeFirecrawl() result
    - workflows/competitive.md contains "probeFirecrawl" call in Step 3 section
    - workflows/competitive.md contains "mcp__firecrawl__firecrawl_search" in Step 4 enhancement
    - workflows/competitive.md contains "mcp__firecrawl__firecrawl_extract" in Step 4 enhancement
    - workflows/competitive.md contains "--no-firecrawl" flag in flags table
    - workflows/competitive.md Step 3 display line includes "Firecrawl:"
    - workflows/competitive.md LOCKED comment boundaries are NOT moved or deleted
    - workflows/recommend.md contains "FIRECRAWL_AVAILABLE" variable set from probeFirecrawl() result
    - workflows/recommend.md contains "probeFirecrawl" call in Step 3 section
    - workflows/recommend.md contains "firecrawl_search" in Step 4 catalog enrichment
    - workflows/recommend.md contains "--no-firecrawl" flag in flags table
    - workflows/recommend.md Step 3 display line includes "Firecrawl:"
    - workflows/recommend.md LOCKED comment boundaries are NOT moved or deleted
    - Both workflows preserve ALL existing content (WebSearch paths, Sequential Thinking paths, artifact write sections)
  </acceptance_criteria>
  <done>Competitive analysis uses firecrawl_search + firecrawl_extract for live competitor pricing/features/positioning with WebSearch fallback. Recommend uses firecrawl_search for deeper MCP tool discovery with WebSearch fallback. Both workflows probe Firecrawl independently in Step 3 and degrade gracefully.</done>
</task>

</tasks>

<verification>
- `grep -c "FIRECRAWL_AVAILABLE" workflows/competitive.md` returns >= 3 (probe set, IF check, ELSE)
- `grep -c "FIRECRAWL_AVAILABLE" workflows/recommend.md` returns >= 3
- `grep "mcp__firecrawl__firecrawl_" commands/competitive.md` returns 3 tool entries
- `grep "mcp__firecrawl__firecrawl_" commands/recommend.md` returns 1 tool entry
- `grep "probeFirecrawl" workflows/competitive.md` confirms credit guard in Step 3
- `grep "probeFirecrawl" workflows/recommend.md` confirms credit guard in Step 3
- `grep "LOCKED" workflows/competitive.md` shows LOCKED boundaries are intact
- `grep "LOCKED" workflows/recommend.md` shows LOCKED boundaries are intact
</verification>

<success_criteria>
- competitive.md probes Firecrawl in Step 3, uses firecrawl_search + firecrawl_extract in Step 4 when available
- competitive.md falls back to existing WebSearch path when Firecrawl unavailable
- recommend.md probes Firecrawl in Step 3, uses firecrawl_search in Step 4 for deeper tool discovery when available
- recommend.md dual-probe: both WebSearch and Firecrawl probed independently (not one-or-the-other)
- Both command files have required Firecrawl MCP tools in allowed-tools
- Both workflow files preserve LOCKED section boundaries
- Both workflows add --no-firecrawl flag for user opt-out
</success_criteria>

<output>
After completion, create `.planning/phases/200-core-scraping-tools-competitive-recommend-integration/200-02-SUMMARY.md`
</output>
