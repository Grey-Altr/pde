---
phase: 44-end-to-end-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-44/concurrency-isolation.test.mjs
  - tests/phase-44/auth-recovery-structure.test.mjs
  - tests/phase-44/writeback-confirmation.test.mjs
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03]

must_haves:
  truths:
    - "All sync workflows write to distinct non-overlapping sections in .planning/ files"
    - "sync.md routes each flag to exactly one sub-workflow with no combined multi-flag execution"
    - "All 10 MCP-dependent workflows call loadConnections() at startup for auth recovery"
    - "mcp-bridge.cjs updateConnectionStatus writes required base fields plus extraFields"
    - "All 4 write-back workflows have confirmation gate BEFORE write step"
    - "All 4 write-back workflows document case-insensitive y/yes acceptance"
    - "All 4 write-back workflows have cancel path for non-yes response"
  artifacts:
    - path: "tests/phase-44/concurrency-isolation.test.mjs"
      provides: "VAL-01 structural audit — routing isolation, section isolation, ROADMAP.md write pattern, Pencil dispatch location"
      min_lines: 80
    - path: "tests/phase-44/auth-recovery-structure.test.mjs"
      provides: "VAL-02 structural audit — loadConnections() in all 10 workflows, mcp-bridge.cjs schema fields"
      min_lines: 60
    - path: "tests/phase-44/writeback-confirmation.test.mjs"
      provides: "VAL-03 structural audit — gate ordering for 4 write-back workflows with correct step numbers"
      min_lines: 100
  key_links:
    - from: "tests/phase-44/concurrency-isolation.test.mjs"
      to: "commands/sync.md"
      via: "fs.readFileSync + regex assertions on routing syntax"
      pattern: "follow.*@workflows/sync-"
    - from: "tests/phase-44/concurrency-isolation.test.mjs"
      to: "workflows/sync-github.md, sync-linear.md, sync-jira.md, sync-figma.md, sync-pencil.md"
      via: "fs.readFileSync + string includes on section headers"
      pattern: "### GitHub Issues|### Linear Issues|### Jira Issues"
    - from: "tests/phase-44/auth-recovery-structure.test.mjs"
      to: "all 10 MCP workflow files"
      via: "fs.readFileSync + string includes"
      pattern: "loadConnections()"
    - from: "tests/phase-44/writeback-confirmation.test.mjs"
      to: "workflows/handoff-create-prs.md, handoff-create-linear-issues.md, handoff-create-jira-tickets.md, mockup-export-figma.md"
      via: "fs.readFileSync + regex assertions on gate/write section ordering"
      pattern: "Step 3.*confirmation gate|Step 4.*confirmation gate"
---

<objective>
Create three structural test files that validate all MCP integrations (phases 39-43) satisfy VAL-01 (concurrency isolation), VAL-02 (auth recovery from disk), and VAL-03 (write-back confirmation gates).

Purpose: This is the capstone validation for v0.5 MCP Integrations. These tests prove the structural properties that guarantee safe concurrent operation, post-compaction recovery, and write-back safety — without requiring live MCP server connections.

Output: Three test files in `tests/phase-44/` that pass GREEN immediately (validating existing code, not building new features).
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-end-to-end-validation/44-RESEARCH.md
@.planning/phases/44-end-to-end-validation/44-VALIDATION.md

<interfaces>
<!-- Established test pattern from phase 43 — all Phase 44 tests follow this exact structure -->
From tests/phase-43/sync-pencil-workflow.test.mjs:
```javascript
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import fs from 'fs';
import path from 'path';
import { fileURLToPath } from 'url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const WORKFLOW_PATH = path.resolve(__dirname, '../../workflows/sync-pencil.md');
```

<!-- Key file paths (relative from project root) -->
Commands: commands/sync.md, commands/system.md
Workflows (sync): workflows/sync-github.md, workflows/sync-linear.md, workflows/sync-jira.md, workflows/sync-figma.md, workflows/sync-pencil.md
Workflows (write-back): workflows/handoff-create-prs.md, workflows/handoff-create-linear-issues.md, workflows/handoff-create-jira-tickets.md, workflows/mockup-export-figma.md
Workflows (other MCP): workflows/critique-pencil-screenshot.md
Bridge: bin/lib/mcp-bridge.cjs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create concurrency-isolation.test.mjs (VAL-01)</name>
  <files>tests/phase-44/concurrency-isolation.test.mjs</files>
  <read_first>
    commands/sync.md
    commands/system.md
    workflows/sync-github.md
    workflows/sync-linear.md
    workflows/sync-jira.md
    workflows/sync-figma.md
    workflows/sync-pencil.md
    tests/phase-43/sync-pencil-workflow.test.mjs
    .planning/phases/44-end-to-end-validation/44-RESEARCH.md
  </read_first>
  <action>
Create `tests/phase-44/concurrency-isolation.test.mjs` using the established `node:test` + `node:assert/strict` + `fs` pattern from phase 43.

The file must contain these test groups (all structural — read file content, assert with string/regex):

**Group 1: Command routing isolation (sync.md)**
- Assert `commands/sync.md` contains `--github` routing to `sync-github.md` (pattern: `follow.*@workflows/sync-github.md`)
- Assert `commands/sync.md` contains `--linear` routing to `sync-linear.md`
- Assert `commands/sync.md` contains `--jira` routing to `sync-jira.md`
- Assert `commands/sync.md` contains `--figma` routing to `sync-figma.md`
- Assert `commands/sync.md` does NOT contain `--pencil` flag (Pencil dispatches from system.md)
- Assert `commands/sync.md` does NOT combine multiple sync workflows (no `sync-github.*AND.*sync-linear` pattern)

**Group 2: Pencil dispatch from system.md (not sync.md)**
- Assert `commands/system.md` contains `mcp__pencil__*` in content (allowed-tools for Pencil)

**Group 3: REQUIREMENTS.md section isolation**
- Assert `workflows/sync-github.md` contains literal string `### GitHub Issues`
- Assert `workflows/sync-linear.md` contains literal string `### Linear Issues`
- Assert `workflows/sync-jira.md` contains literal string `### Jira Issues`
- These are the distinct named sections that prevent cross-service write conflicts

**Group 4: Figma writes to tokens.json, not REQUIREMENTS.md**
- Assert `workflows/sync-figma.md` contains `assets/tokens.json`
- Assert `workflows/sync-figma.md` does NOT contain `writeFileSync.*REQUIREMENTS` pattern

**Group 5: Pencil writes to external canvas, not .planning/**
- Assert `workflows/sync-pencil.md` contains `pencil:set-variables`
- Assert `workflows/sync-pencil.md` does NOT contain `writeFileSync.*.planning` pattern

**Group 6: ROADMAP.md write isolation for Linear LIN-02**
- Assert `workflows/sync-linear.md` contains `<!-- Linear` (HTML comment pattern for cycle annotation)
- Assert `workflows/sync-linear.md` contains `ROADMAP.md` reference

Use `path.resolve(__dirname, '../../', relativeFilePath)` for all file reads. Use `describe()` for grouping, `it()` for individual assertions. File header comment must state "VAL-01 — Concurrency isolation structural audit".

Run `node --test tests/phase-44/concurrency-isolation.test.mjs` after creation — all tests must pass GREEN.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-44/concurrency-isolation.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-44/concurrency-isolation.test.mjs exists and has >= 80 lines
    - File contains `import { describe, it } from 'node:test'`
    - File contains `import assert from 'node:assert/strict'`
    - File contains string `### GitHub Issues`
    - File contains string `### Linear Issues`
    - File contains string `### Jira Issues`
    - File contains string `assets/tokens.json`
    - File contains string `pencil:set-variables`
    - File contains string `mcp__pencil__*`
    - File contains `<!-- Linear` pattern assertion
    - File contains `doesNotMatch` for --pencil in sync.md
    - `node --test tests/phase-44/concurrency-isolation.test.mjs` exits 0
  </acceptance_criteria>
  <done>VAL-01 structural audit passes — sync routing isolation, section isolation, ROADMAP.md write pattern, and Pencil dispatch location all verified</done>
</task>

<task type="auto">
  <name>Task 2: Create auth-recovery-structure.test.mjs (VAL-02)</name>
  <files>tests/phase-44/auth-recovery-structure.test.mjs</files>
  <read_first>
    bin/lib/mcp-bridge.cjs
    workflows/sync-github.md
    workflows/sync-linear.md
    workflows/sync-jira.md
    workflows/sync-figma.md
    workflows/sync-pencil.md
    workflows/handoff-create-prs.md
    workflows/handoff-create-linear-issues.md
    workflows/handoff-create-jira-tickets.md
    workflows/mockup-export-figma.md
    workflows/critique-pencil-screenshot.md
    tests/phase-43/sync-pencil-workflow.test.mjs
    .planning/phases/44-end-to-end-validation/44-RESEARCH.md
  </read_first>
  <action>
Create `tests/phase-44/auth-recovery-structure.test.mjs` using the established `node:test` + `node:assert/strict` + `fs` pattern.

The file must contain these test groups:

**Group 1: All 10 MCP workflows call loadConnections() at startup**
Define array of all 10 MCP-dependent workflow paths:
```
workflows/sync-github.md
workflows/sync-linear.md
workflows/sync-jira.md
workflows/sync-figma.md
workflows/sync-pencil.md
workflows/handoff-create-prs.md
workflows/handoff-create-linear-issues.md
workflows/handoff-create-jira-tickets.md
workflows/mockup-export-figma.md
workflows/critique-pencil-screenshot.md
```
For each: read file, assert it contains `loadConnections()` or `b.loadConnections()`. Use parameterized loop pattern (for...of with it() inside).

**Group 2: mcp-bridge.cjs updateConnectionStatus writes required base fields**
Read `bin/lib/mcp-bridge.cjs`, assert it contains each of these strings:
- `server_key`
- `display_name`
- `transport`
- `status`
- `last_updated`
- `extraFields`

**Group 3: mcp-connections.json is gitignored**
Read `.gitignore`, assert it contains `mcp-connections.json`.

File header comment: "VAL-02 — Auth recovery structural audit: loadConnections() presence in all MCP workflows, mcp-bridge.cjs schema completeness".

Run `node --test tests/phase-44/auth-recovery-structure.test.mjs` after creation — all tests must pass GREEN.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-44/auth-recovery-structure.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-44/auth-recovery-structure.test.mjs exists and has >= 60 lines
    - File contains `import { describe, it } from 'node:test'`
    - File contains `import assert from 'node:assert/strict'`
    - File contains all 10 workflow paths as strings
    - File contains `loadConnections()` assertion pattern
    - File contains `server_key` assertion
    - File contains `display_name` assertion
    - File contains `extraFields` assertion
    - File contains `.gitignore` read for mcp-connections.json check
    - `node --test tests/phase-44/auth-recovery-structure.test.mjs` exits 0
  </acceptance_criteria>
  <done>VAL-02 structural audit passes — all 10 MCP workflows confirmed to call loadConnections() at startup, mcp-bridge.cjs schema has all required fields, mcp-connections.json is gitignored</done>
</task>

<task type="auto">
  <name>Task 3: Create writeback-confirmation.test.mjs (VAL-03)</name>
  <files>tests/phase-44/writeback-confirmation.test.mjs</files>
  <read_first>
    workflows/handoff-create-prs.md
    workflows/handoff-create-linear-issues.md
    workflows/handoff-create-jira-tickets.md
    workflows/mockup-export-figma.md
    tests/phase-43/sync-pencil-workflow.test.mjs
    .planning/phases/44-end-to-end-validation/44-RESEARCH.md
  </read_first>
  <action>
Create `tests/phase-44/writeback-confirmation.test.mjs` using the established `node:test` + `node:assert/strict` + `fs` pattern.

Define parameterized workflow audit array with these EXACT entries (from research — verified by codebase inspection):

```javascript
const WRITEBACK_WORKFLOWS = [
  {
    file: 'workflows/handoff-create-prs.md',
    requirement: 'GH-02',
    gateText: 'Create this PR?',
    cancelText: 'No PRs created',
    gateSection: /Step 3.*confirmation gate/is,
    writeSection: /Step 4.*only after user confirm/is,
  },
  {
    file: 'workflows/handoff-create-linear-issues.md',
    requirement: 'LIN-03',
    gateText: 'Create this issue in Linear?',
    cancelText: 'No issues created',
    gateSection: /Step 3.*confirmation gate/is,
    writeSection: /Step 4.*only after user confirm/is,
  },
  {
    file: 'workflows/handoff-create-jira-tickets.md',
    requirement: 'JIRA-03',
    gateText: 'Create this ticket in Jira?',
    cancelText: 'No tickets created',
    // JIRA HAS 5 STEPS: gate at Step 4, write at Step 5
    gateSection: /Step 4.*confirmation gate/is,
    writeSection: /Step 5.*only after user confirm/is,
  },
  {
    file: 'workflows/mockup-export-figma.md',
    requirement: 'FIG-04',
    gateText: 'This will write to your Figma file. Proceed?',
    cancelText: 'Export cancelled',
    gateSection: /Step 3|confirmation gate/is,
    writeSection: /Step 4|only after user confirm/is,
  },
];
```

For each workflow in the array, create a `describe()` block named `VAL-03 -- {requirement}: {basename}` containing these `it()` tests:
1. **Confirmation gate text present** — `content.includes(wf.gateText)`
2. **(y/n) prompt present** — `content` matches `/\(y\/n\)/`
3. **Cancel path present** — `content.includes(wf.cancelText)`
4. **Case-insensitive acceptance documented** — `content` matches `/case-insensitive/i`
5. **Gate section precedes write section** — `content.search(wf.gateSection)` < `content.search(wf.writeSection)`, both must be found (not -1)

**Additional standalone test:**
- **Exactly 4 write-back workflows** — assert WRITEBACK_WORKFLOWS.length === 4 with comment: "sync-pencil.md excluded by design — non-interactive token push, no confirmation gate"

File header comment: "VAL-03 — Write-back confirmation gate structural audit: gate ordering, cancel paths, case-insensitive acceptance for all 4 write-back workflows. Note: sync-pencil.md is intentionally excluded (non-interactive by design)."

CRITICAL: The Jira workflow uses Step 4 for gate and Step 5 for write (5-step workflow). GitHub/Linear use Step 3/Step 4 (4-step workflow). Figma uses a slightly different pattern. The regex patterns above are calibrated for each. Do NOT normalize them.

Run `node --test tests/phase-44/writeback-confirmation.test.mjs` after creation — all tests must pass GREEN.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-44/writeback-confirmation.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-44/writeback-confirmation.test.mjs exists and has >= 100 lines
    - File contains `import { describe, it } from 'node:test'`
    - File contains `import assert from 'node:assert/strict'`
    - File contains exactly 4 entries in WRITEBACK_WORKFLOWS array
    - File contains `Create this PR?` string
    - File contains `Create this issue in Linear?` string
    - File contains `Create this ticket in Jira?` string
    - File contains `This will write to your Figma file. Proceed?` string
    - File contains `No PRs created` cancel text
    - File contains `No issues created` cancel text
    - File contains `No tickets created` cancel text
    - File contains `Export cancelled` cancel text
    - File contains `Step 4.*confirmation gate` regex for Jira (not Step 3)
    - File contains `Step 5.*only after user confirm` regex for Jira (not Step 4)
    - File contains `case-insensitive` assertion
    - File contains `WRITEBACK_WORKFLOWS.length` assertion for count = 4
    - `node --test tests/phase-44/writeback-confirmation.test.mjs` exits 0
  </acceptance_criteria>
  <done>VAL-03 structural audit passes — all 4 write-back workflows confirmed to have confirmation gate before write step, cancel paths present, case-insensitive y/yes documented, Jira step numbers correct (4/5 not 3/4)</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. Run phase-44 tests: `node --test tests/phase-44/*.test.mjs` — all must pass
2. Run full v0.5 test suite: `cd "/Users/greyaltaer/code/projects/Platform Development Engine" && node --test tests/phase-40/*.test.mjs tests/phase-41/*.test.mjs tests/phase-42/*.test.mjs tests/phase-43/*.test.mjs tests/phase-44/*.test.mjs` — no regressions
3. Verify 3 test files exist in tests/phase-44/ with correct names
4. Verify total test count across all three files covers: VAL-01 (routing + section isolation + ROADMAP write + Pencil dispatch), VAL-02 (10 workflow loadConnections + bridge schema + gitignore), VAL-03 (4 workflow gates with correct step ordering)
</verification>

<success_criteria>
- Three test files exist: concurrency-isolation.test.mjs, auth-recovery-structure.test.mjs, writeback-confirmation.test.mjs
- `node --test tests/phase-44/*.test.mjs` exits 0 (all GREEN)
- Full suite (phases 40-44) exits 0 (no regressions)
- VAL-01 validated: sync routing isolation, section isolation, ROADMAP.md write pattern, Pencil dispatch
- VAL-02 validated: all 10 MCP workflows call loadConnections(), mcp-bridge.cjs schema complete, mcp-connections.json gitignored
- VAL-03 validated: all 4 write-back workflows have gate before write, cancel paths, case-insensitive y/yes, correct Jira step numbers
</success_criteria>

<output>
After completion, create `.planning/phases/44-end-to-end-validation/44-01-SUMMARY.md`
</output>
