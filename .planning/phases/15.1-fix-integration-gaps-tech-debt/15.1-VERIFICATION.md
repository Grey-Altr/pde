---
phase: 15.1-fix-integration-gaps-tech-debt
verified: 2026-03-15T00:00:00Z
status: passed
score: 7/7 must-haves verified
re_verification: false
---

# Phase 15.1: Fix Integration Gaps & Tech Debt — Verification Report

**Phase Goal:** All integration contradictions, stale template data, and orphaned CLI paths from Phases 12-15 are resolved before wireframe work begins
**Verified:** 2026-03-15
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `workflows/system.md` and `workflows/flows.md` Step 7 no longer reference `hasBrief` in coverage-merge instructions (INT-01 + FLW-BREAK-01 resolved) | VERIFIED | `grep "hasBrief" workflows/system.md` returns 1 hit — the anti-pattern line at L1340 (`NEVER set hasBrief`). Zero hits in Step 7. `grep "hasBrief" workflows/flows.md` returns zero hits. |
| 2 | `templates/design-state-root.md` Domain Files table has no `{count}` placeholders (INT-02 + FLW-BREAK-02 resolved) | VERIFIED | `grep "{count}" templates/design-state-root.md .planning/design/DESIGN-STATE.md` returns zero matches. Table header + on-demand comment lines preserved. Both files confirmed at lines 43-55. |
| 3 | `workflows/system.md` Step 5 calls `pde-tools.cjs design tokens-to-css` for CSS generation instead of inline Write (INT-03 resolved) | VERIFIED | `grep "tokens-to-css" workflows/system.md` returns 2 matches at L1049 and L1054. Full CLI invocation with `--raw` flag and `@file:` redirect check at L1055. Old `FULL INLINE COPY` instruction absent. |
| 4 | Phase 14 VALIDATION.md has `nyquist_compliant: true` after audit | VERIFIED | `.planning/phases/14-design-system-pde-system/14-VALIDATION.md` frontmatter has `nyquist_compliant: true` (L5) and `wave_0_complete: true` (L6). Approval: complete (Phase 15.1-02). |
| 5 | `design.cjs` self-test includes dark mode invariant test verifying `generateCssVars` does NOT produce `@media` or `[data-theme]` blocks | VERIFIED | `grep "generateCssVars does not produce" bin/lib/design.cjs` returns 1 match at L516. Both `@media` and `[data-theme]` negative assertions present at L518-519. |
| 6 | `design.cjs --self-test` passes with the new dark mode test included | VERIFIED | `node bin/lib/design.cjs --self-test` output: `20 tests — 20 passed, 0 failed`. |
| 7 | Phase 15.1 VALIDATION.md has `nyquist_compliant: true` and `wave_0_complete: true` | VERIFIED | `.planning/phases/15.1-fix-integration-gaps-tech-debt/15.1-VALIDATION.md` frontmatter: `nyquist_compliant: true` (L5), `wave_0_complete: true` (L6). Approval: complete (Phase 15.1-02). |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `workflows/system.md` | Corrected coverage-merge (no hasBrief) + tokens-to-css CLI wiring | VERIFIED | L1340 contains only the anti-pattern `NEVER` line for hasBrief. L1049-1067 contains the full tokens-to-css CLI invocation block replacing the old inline write. |
| `workflows/flows.md` | Corrected coverage-merge (no hasBrief), hasHardwareSpec preserved | VERIFIED | Zero hasBrief references. `hasHardwareSpec` at L485 and L490 intact. `hasDesignSystem`, `hasFlows` both present in manifest-set-top-level command. |
| `templates/design-state-root.md` | Clean Domain Files template without placeholder rows | VERIFIED | Lines 43-55: header row + two comment lines only. No data rows. `{count}` absent. On-demand comment preserved. |
| `.planning/design/DESIGN-STATE.md` | Clean live Domain Files table without placeholder rows | VERIFIED | Lines 43-55: identical structure — header row + two comment lines. No data rows. `{count}` absent. On-demand comment preserved. |
| `bin/lib/design.cjs` | Dark mode invariant test in self-test group 3 | VERIFIED | L516-520: `check('generateCssVars does not produce @media or [data-theme] dark mode blocks', ...)` with two negative assertions. 20 tests pass. |
| `.planning/phases/14-design-system-pde-system/14-VALIDATION.md` | Nyquist compliance sign-off for Phase 14 | VERIFIED | `nyquist_compliant: true`, `wave_0_complete: true`, dark mode test item checked, integration smoke test marked `[~]` (deferred-manual), Approval complete. |
| `.planning/phases/15.1-fix-integration-gaps-tech-debt/15.1-VALIDATION.md` | Nyquist compliance sign-off for Phase 15.1 | VERIFIED | `nyquist_compliant: true`, `wave_0_complete: true`, Wave 0 item resolved, Approval complete. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `workflows/system.md` Step 5 File 10 | `bin/pde-tools.cjs design tokens-to-css` | Bash CLI invocation | WIRED | Pattern `pde-tools.cjs.*design.*tokens-to-css` found at L1054. `@file:` redirect check at L1055. Dark mode manual append caveat present. |
| `workflows/system.md` Step 7 | `design-manifest.json designCoverage` | `manifest-set-top-level` without hasBrief | WIRED | Pattern at L1302: `manifest-set-top-level designCoverage '{"hasDesignSystem":true,"hasWireframes":{current_hasWireframes},...}'` — no `hasBrief` key. |
| `bin/lib/design.cjs` self-test group 3 | `generateCssVars` function | Assert output excludes `@media` and `[data-theme]` | WIRED | L518: `assert.ok(!css.includes('@media'), ...)` and L519: `assert.ok(!css.includes('[data-theme]'), ...)`. Both assertions run against live `generateCssVars` output. |

---

### Requirements Coverage

Phase 15.1 is explicitly scoped as an integration fix phase for already-satisfied requirements (Phases 12-15). The ROADMAP.md and both PLAN files declare the same IDs: `INFRA-01, INFRA-02, INFRA-03, INFRA-04, BRF-01, BRF-02, SYS-01, SYS-02, SYS-03`.

**Note on traceability:** REQUIREMENTS.md maps these IDs to Phases 12-14 (not Phase 15.1). This is by design — Phase 15.1 is a gap-closure phase that strengthens integration coverage for previously satisfied requirements; it does not re-claim ownership of the IDs. The ROADMAP.md entry for Phase 15.1 explicitly calls this out: "(integration fixes for already-satisfied requirements)". No REQUIREMENTS.md update was required or expected.

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| INFRA-01 | 15.1-01 | .planning/design/ directory on first invocation | SATISFIED | Domain Files table clean — no misleading placeholder rows at dir-creation time |
| INFRA-02 | 15.1-01 | DESIGN-STATE.md tracks pipeline stage completion | SATISFIED | Live DESIGN-STATE.md Domain Files table corrected; reads correct (no stale rows) |
| INFRA-03 | 15.1-01, 15.1-02 | bin/lib/design.cjs DTCG-to-CSS conversion | SATISFIED | tokens-to-css CLI wired into system.md Step 5; dark mode invariant test added and passing |
| INFRA-04 | 15.1-01 | Design manifest tracks all generated artifacts | SATISFIED | tokens-to-css invocation exercises manifest path; hasBrief removed from merge prevents corrupt manifest state |
| BRF-01 | 15.1-01 | /pde:brief produces structured problem framing document | SATISFIED | Contradictory hasBrief instruction removed from system.md; brief↔manifest contract no longer ambiguous |
| BRF-02 | 15.1-01 | Brief detects product type and sets design constraints | SATISFIED | brief.md anti-pattern ("NEVER add hasBrief") now consistent across all workflow files |
| SYS-01 | 15.1-01 | /pde:system generates DTCG JSON tokens | SATISFIED | system.md Step 7 corrected; Step 5 wired to tokens-to-css CLI |
| SYS-02 | 15.1-01, 15.1-02 | CSS custom properties derived from DTCG tokens | SATISFIED | tokens-to-css CLI wired into Step 5; dark mode invariant test encodes correct derivation contract |
| SYS-03 | 15.1-02 | Typography, color, and spacing tokens generated | SATISFIED | No blocking integration issue; mentioned for completeness of INT-01 affected requirements scope |

**Orphaned requirements (mapped to Phase 15.1 in REQUIREMENTS.md but not in any plan):** None. All 9 declared IDs appear in at least one plan.

**Requirements not in Phase 15.1 plans:** `FLW-01`, `FLW-02` are not claimed by Phase 15.1 plans — correct, those belong to Phase 15.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `workflows/system.md` | 794 | "Do not leave placeholder values in the final JSON" | Info | Instructional prose about downstream artifact quality, not a code stub — correct and intentional |
| `workflows/system.md` | 1160 | `placeholder="you@example.com"` | Info | HTML input placeholder attribute inside an inline code example — not a template placeholder or stub |
| `workflows/system.md` | 1185 | "Replace all `{placeholder}` values..." | Info | Instructional text telling Claude to replace placeholders — intentional workflow instruction |
| `bin/lib/design.cjs` | 402-403 | `{date} placeholder should be replaced` — as test description | Info | This is a test assertion verifying {date} placeholders ARE removed — correct and active test behavior |

No blockers or warnings. All flagged patterns are intentional workflow instructions or in-test assertions, not implementation stubs.

---

### Human Verification Required

The following items are manual-only and cannot be verified programmatically:

**1. /pde:system integration smoke test (deferred-manual)**

- **Test:** Run `/pde:system` against a real project brief and verify `assets/tokens.css` and `visual/SYS-tokens.json` exist with expected DTCG shape.
- **Expected:** tokens.css includes a `:root {}` block generated by the tokens-to-css CLI plus manually-composed dark mode `@media` and `[data-theme]` blocks.
- **Why human:** Requires Claude to generate real DTCG tokens from brief input; not automatable as a unit test. Classified as deferred-manual in Phase 14 VALIDATION.md and verified manually during Phase 14 execution.

**2. Brief product type detection (BRF-02)**

- **Test:** Run `/pde:brief` against a hardware project brief and inspect the output frontmatter.
- **Expected:** `productType: hardware` present in brief frontmatter.
- **Why human:** Product type detection requires Claude inference over brief content.

These items were identified as manual-only in the VALIDATION.md and are not blocking — they were classified and accepted during Phase 14 and 15.1 planning.

---

### Commit Verification

All commits documented in SUMMARY files confirmed present in git history:

| Commit | Description | Verified |
|--------|-------------|---------|
| `0a959e0` | fix: remove hasBrief from Step 7 coverage-merge | Present |
| `65232e4` | feat: wire tokens-to-css CLI into system.md Step 5 | Present |
| `03e4672` | fix: remove {count} placeholder rows | Present |
| `fa2ad3e` | docs: Plan 01 SUMMARY | Present |
| `bf0183c` | feat: add dark mode invariant test to design.cjs | Present |
| `b323695` | docs: flip nyquist compliance flags | Present |
| `319247c` | docs: Plan 02 SUMMARY | Present |

---

### Gaps Summary

None. All 7 truths verified, all 7 required artifacts substantive and wired, all 3 key links confirmed. Phase goal achieved.

The two manual-only items (integration smoke test, BRF-02 product type inference) were scoped and accepted as manual-only during phase planning and are not blockers for the stated goal.

---

_Verified: 2026-03-15_
_Verifier: Claude (gsd-verifier)_
