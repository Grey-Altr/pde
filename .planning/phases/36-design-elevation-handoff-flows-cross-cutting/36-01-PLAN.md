---
phase: 36-design-elevation-handoff-flows-cross-cutting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand01_motion_spec.sh
  - .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand02_impl_notes.sh
  - .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_flow01_transitions.sh
  - .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_cross01_includes.sh
  - workflows/handoff.md
  - workflows/flows.md
autonomous: true
requirements: [HAND-01, HAND-02, FLOW-01, CROSS-01, CROSS-02, ORDER-01]

must_haves:
  truths:
    - "handoff.md TypeScript interface instructions include motionTrigger, motionDuration, motionEasing, respectReducedMotion fields (conditional on upstream motion annotations)"
    - "handoff.md includes an Implementation Notes subsection pattern for concept-specific interactions, referencing VISUAL-HOOK and using Recommended Approach table column"
    - "handoff.md has @references/motion-design.md in its required_reading block"
    - "flows.md instructs transition annotation on screen-to-screen Mermaid edges using slide/fade/morph/shared-element vocabulary"
    - "All 7 skill files (system, wireframe, critique, hig, mockup, handoff, flows) have at least one @references/ include in required_reading"
    - "ORDER-01 dependency rationale documented in VERIFICATION.md"
    - "CROSS-02 audit delta procedure documented in VERIFICATION.md with pre/post audit instructions"
  artifacts:
    - path: "workflows/handoff.md"
      provides: "Elevated handoff skill with motion spec interface fields and implementation notes"
      contains: "motionTrigger"
    - path: "workflows/flows.md"
      provides: "Elevated flows skill with transition annotations on screen-to-screen edges"
      contains: "slide-up\\|slide-right\\|fade\\|morph\\|shared-element"
    - path: ".planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand01_motion_spec.sh"
      provides: "Nyquist tests for HAND-01 (5 checks)"
    - path: ".planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand02_impl_notes.sh"
      provides: "Nyquist tests for HAND-02 (3 checks)"
    - path: ".planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_flow01_transitions.sh"
      provides: "Nyquist tests for FLOW-01 (2 checks)"
    - path: ".planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_cross01_includes.sh"
      provides: "Nyquist tests for CROSS-01 (7 checks — one per skill file)"
  key_links:
    - from: "workflows/handoff.md"
      to: "references/motion-design.md"
      via: "@references/motion-design.md in required_reading block"
      pattern: "@references/motion-design.md"
    - from: "workflows/handoff.md"
      to: "motionTrigger interface field"
      via: "Step 4g motion spec subsection (conditional on upstream motion annotation)"
      pattern: "motionTrigger"
    - from: "workflows/flows.md"
      to: "transition vocabulary in Mermaid edge labels"
      via: "parenthetical annotation format: -->|\"CTA click (slide-up)\"|"
      pattern: "slide-up\\|slide-right\\|fade\\|morph\\|shared-element"
    - from: "test_cross01_includes.sh"
      to: "all 7 skill files"
      via: "grep @references/ in each skill's required_reading"
      pattern: "@references/"
---

<objective>
Create 4 Nyquist test scripts (Wave 0), elevate workflows/handoff.md with motion spec TypeScript interface fields and implementation notes, elevate workflows/flows.md with screen transition annotations, and document ORDER-01 / CROSS-02 verification evidence.

Purpose: This is the final design skill elevation phase. handoff.md and flows.md are the only two skills without motion/transition awareness. After this plan, all 7 skills form a coherent motion-aware pipeline: system defines tokens → wireframe/critique use composition references → mockup generates spring physics + scroll-driven → handoff exports motion as TypeScript props → flows annotates transitions between screens. The cross-cutting verification (CROSS-01, CROSS-02) confirms elevation uniformity across all 7 skills.

Output: 4 Nyquist test scripts + elevated workflows/handoff.md + elevated workflows/flows.md + VERIFICATION.md with ORDER-01 and CROSS-02 evidence.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greyaltaer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-design-elevation-handoff-flows-cross-cutting/36-RESEARCH.md
@.planning/phases/36-design-elevation-handoff-flows-cross-cutting/36-VALIDATION.md
</context>

<interfaces>
<!-- Key patterns established in phases 32-35 — executor should use these directly -->

Nyquist test pattern (from test_mock01_spring_physics.sh):
```bash
#!/usr/bin/env bash
set -euo pipefail
PASS=0; FAIL=0
SKILL="workflows/handoff.md"   # or flows.md

check() {
  if grep -qE "$1" "$SKILL"; then
    PASS=$((PASS+1))
  else
    echo "FAIL: $2"
    FAIL=$((FAIL+1))
  fi
}

# ... checks ...

echo "HAND-01 motion_spec: $PASS passed, $FAIL failed"
[ "$FAIL" -eq 0 ]
```
CRITICAL: Use `PASS=$((PASS+1))` NOT `((PASS++))` — bash set -e kills the process when arithmetic
expression returns 0 (which happens when PASS=0 and you use ((PASS++))).

Current workflows/handoff.md required_reading:
```
<required_reading>
@references/skill-style-guide.md
@references/mcp-integration.md
</required_reading>
```

Current workflows/flows.md required_reading:
```
<required_reading>
@references/skill-style-guide.md
@references/mcp-integration.md
</required_reading>
```

Phase 32-35 @include pattern — append AFTER existing includes, never reorder:
```
<required_reading>
@references/skill-style-guide.md
@references/mcp-integration.md
@references/motion-design.md          ← NEW (append only)
</required_reading>
```

CROSS-01 audit table (from 36-RESEARCH.md):
| system.md | skill-style-guide, mcp-integration, motion-design, composition-typography | COMPLIANT |
| wireframe.md | skill-style-guide, mcp-integration, composition-typography | COMPLIANT |
| critique.md | skill-style-guide, mcp-integration, quality-standards, composition-typography | COMPLIANT |
| hig.md | skill-style-guide, mcp-integration, motion-design | COMPLIANT |
| mockup.md | skill-style-guide, mcp-integration, motion-design | COMPLIANT |
| handoff.md | skill-style-guide, mcp-integration | NEEDS: @references/motion-design.md |
| flows.md | skill-style-guide, mcp-integration | No gap — transition vocabulary embedded in prose |

Motion spec field format (from 36-RESEARCH.md Pattern 1):
```typescript
// --- Motion specification (from mockup motion annotations) ---
/**
 * Entrance animation trigger.
 * 'on-load' = plays on DOMContentLoaded (above-fold hero)
 * 'on-scroll' = plays when component enters viewport
 * @default 'on-load'
 */
motionTrigger?: 'on-load' | 'on-scroll' | 'on-interaction';

/**
 * Entrance animation duration in milliseconds.
 * Corresponds to design token: --duration-slow (500ms) or --duration-dramatic (800ms)
 * @default 700
 */
motionDuration?: number;

/**
 * CSS easing function name (maps to design token).
 * 'ease-enter' = cubic-bezier(0, 0, 0.2, 1)
 * 'spring' = cubic-bezier(0.34, 1.56, 0.64, 1)
 * @default 'ease-enter'
 */
motionEasing?: 'ease-standard' | 'ease-enter' | 'ease-exit' | 'spring';

/**
 * When true, skips all motion for prefers-reduced-motion users.
 * WCAG 2.2.2 compliance (Level A).
 * @default true
 */
respectReducedMotion?: boolean;
```
Placement: CONDITIONAL — add motion fields ONLY when component has upstream motion annotation.
Always place in a `// --- Motion specification ---` comment block within the interface.
These are always optional props (?). Include `// No motion annotations found in upstream mockup`
comment and omit the block for static/data-display components with no motion.

Implementation Notes format (from 36-RESEARCH.md Pattern 2):
```markdown
### Implementation Notes

**Concept-specific interactions detected in upstream mockup:**

| Interaction | VISUAL-HOOK ID | Description | Recommended Approach | Library |
|-------------|----------------|-------------|---------------------|---------|
| hero-parallax-depth | depth-scroll | Hero background at 0.3x scroll speed | CSS `animation-timeline: view()` inside `@supports` guard. Fallback: static for Firefox. | CSS native or GSAP ScrollTrigger 3.14 |
```
Omit this subsection entirely if the component/screen has no VISUAL-HOOK or concept-specific interaction.
Use `document.startViewTransition()` recommendations with fallback (Chrome 111+, Firefox 126+, Safari 18+ — ~75-80% coverage).

Transition annotation format for flows.md (from 36-RESEARCH.md Pattern 3):
```mermaid
J1_1 -->|"CTA click (slide-up)"| J1_2
J1_3 -->|"No (fade-push-right)"| J1_4
J1_4 -->|"Verified (morph-expand)"| J1_DONE
```
Transition vocabulary: slide-right, slide-left, slide-up, slide-down, fade, morph, morph-expand, morph-collapse, shared-element.
RULE: Annotate screen-to-screen edges only. Decision node branches ({}) get semantic label only (Yes/No) — NOT a transition annotation.
Step Descriptions extension:
```
4. **J1_4 - Email Verification Screen:** Confirmation with "Check your email" message.
   → Transition to J1_5: `morph-expand` — rationale here.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 Nyquist test scripts (Wave 0 — all RED initially)</name>
  <files>
    .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand01_motion_spec.sh
    .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand02_impl_notes.sh
    .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_flow01_transitions.sh
    .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_cross01_includes.sh
  </files>
  <read_first>
    workflows/handoff.md
    workflows/flows.md
    .planning/phases/36-design-elevation-handoff-flows-cross-cutting/36-VALIDATION.md
    .planning/phases/35-design-elevation-mockup-skill/test_mock01_spring_physics.sh
  </read_first>
  <action>
Create 4 bash Nyquist test scripts following the Phase 32-35 established pattern exactly.

Each script:
- Starts with `#!/usr/bin/env bash` and `set -euo pipefail`
- Has a `check()` function using `grep -qE`
- Uses `PASS=0; FAIL=0` counters
- Uses `PASS=$((PASS+1))` and `FAIL=$((FAIL+1))` — NEVER `((PASS++))` or `((FAIL++))` (bash set -e pitfall when value is 0)
- Ends with `echo "LABEL: $PASS passed, $FAIL failed"` and `[ "$FAIL" -eq 0 ]`

---

**test_hand01_motion_spec.sh** (HAND-01 — 5 checks, SKILL="workflows/handoff.md"):

Check 1: `motionTrigger` prop pattern
  Pattern: `motionTrigger`
  Fail msg: "motionTrigger prop not found in handoff.md"

Check 2: `motionDuration` prop pattern
  Pattern: `motionDuration`
  Fail msg: "motionDuration prop not found in handoff.md"

Check 3: `motionEasing` prop pattern
  Pattern: `motionEasing`
  Fail msg: "motionEasing prop not found in handoff.md"

Check 4: `respectReducedMotion` prop pattern
  Pattern: `respectReducedMotion`
  Fail msg: "respectReducedMotion prop not found in handoff.md"

Check 5: `@references/motion-design.md` in required_reading
  Pattern: `@references/motion-design`
  Fail msg: "@references/motion-design.md not found in handoff.md required_reading"

Summary line: `echo "HAND-01 motion_spec: $PASS passed, $FAIL failed"`

---

**test_hand02_impl_notes.sh** (HAND-02 — 3 checks, SKILL="workflows/handoff.md"):

Check 1: `Implementation Notes` section header pattern
  Pattern: `Implementation Notes`
  Fail msg: "Implementation Notes section not found in handoff.md"

Check 2: `VISUAL-HOOK` reference (connecting to upstream mockup convention)
  Pattern: `VISUAL-HOOK`
  Fail msg: "VISUAL-HOOK reference not found in handoff.md"

Check 3: `Recommended Approach` table column header
  Pattern: `Recommended Approach`
  Fail msg: "Recommended Approach column header not found in handoff.md"

Summary line: `echo "HAND-02 impl_notes: $PASS passed, $FAIL failed"`

---

**test_flow01_transitions.sh** (FLOW-01 — 2 checks, SKILL="workflows/flows.md"):

Check 1: Transition annotation vocabulary present (case-insensitive, OR)
  Pattern (extended regex): `slide-right|slide-left|slide-up|slide-down|morph|shared-element`
  Use `-qi` flag: `grep -qiE "$pattern" "$SKILL"`
  Fail msg: "Transition annotation vocabulary (slide/fade/morph/shared-element) not found in flows.md"

Check 2: Transition annotation instruction present (case-insensitive)
  Pattern: `transition annotation|visual mechanism|visual transition`
  Use `-qi` flag
  Fail msg: "Transition annotation instruction not found in flows.md"

Summary line: `echo "FLOW-01 transitions: $PASS passed, $FAIL failed"`

---

**test_cross01_includes.sh** (CROSS-01 — 7 checks, one per skill file):

This script checks a different SKILL file per check — do NOT set a single SKILL variable at the top.
Instead, inline the file path in each check call.

Use this modified check function that accepts the file as a second argument:
```bash
check_file() {
  local pattern="$1"
  local file="$2"
  local msg="$3"
  if grep -qE "$pattern" "$file"; then
    PASS=$((PASS+1))
  else
    echo "FAIL: $msg"
    FAIL=$((FAIL+1))
  fi
}
```

Check 1: workflows/system.md has @references/ in required_reading
  check_file "@references/" "workflows/system.md" "system.md missing @references/ include in required_reading"

Check 2: workflows/wireframe.md has @references/ in required_reading
  check_file "@references/" "workflows/wireframe.md" "wireframe.md missing @references/ include in required_reading"

Check 3: workflows/critique.md has @references/ in required_reading
  check_file "@references/" "workflows/critique.md" "critique.md missing @references/ include in required_reading"

Check 4: workflows/hig.md has @references/ in required_reading
  check_file "@references/" "workflows/hig.md" "hig.md missing @references/ include in required_reading"

Check 5: workflows/mockup.md has @references/ in required_reading
  check_file "@references/" "workflows/mockup.md" "mockup.md missing @references/ include in required_reading"

Check 6: workflows/handoff.md has @references/ in required_reading
  check_file "@references/" "workflows/handoff.md" "handoff.md missing @references/ include in required_reading"

Check 7: workflows/flows.md has @references/ in required_reading
  check_file "@references/" "workflows/flows.md" "flows.md missing @references/ include in required_reading"

Summary line: `echo "CROSS-01 includes: $PASS passed, $FAIL failed"`

---

After creating all 4 scripts, run them against the current (un-elevated) skill files and confirm they exit non-zero (RED). This validates the tests are meaningful — they detect the absence of elevation content.

```bash
cd "/Users/greyaltaer/code/projects/Platform Development Engine" && for f in .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand01_motion_spec.sh .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand02_impl_notes.sh .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_flow01_transitions.sh .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_cross01_includes.sh; do echo "--- $f ---"; bash "$f" 2>&1 || true; done
```

test_cross01_includes.sh will be partially GREEN (checks 1-5 pass because phases 32-35 already added @references/ to those files). Checks 6 and 7 (handoff.md, flows.md) will be RED. That is CORRECT and expected.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && for f in .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand01_motion_spec.sh .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand02_impl_notes.sh .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_flow01_transitions.sh .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_cross01_includes.sh; do echo "--- $f ---"; bash "$f" 2>&1 || true; done</automated>
  </verify>
  <acceptance_criteria>
    - test_hand01_motion_spec.sh exists and contains `SKILL="workflows/handoff.md"`
    - test_hand01_motion_spec.sh contains `motionTrigger` in a grep pattern
    - test_hand02_impl_notes.sh exists and contains `VISUAL-HOOK` in a grep pattern
    - test_flow01_transitions.sh exists and contains `slide-right|slide-left|slide-up` in a grep pattern
    - test_cross01_includes.sh exists and greps all 7 skill files for `@references/`
    - No script uses `((PASS++))` or `((FAIL++))` — confirm with: `grep -l "((PASS++))\|((FAIL++))" .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_*.sh` returns empty
    - All 4 scripts use `set -euo pipefail`
    - test_hand01_motion_spec.sh exits non-zero (RED) against current un-elevated handoff.md
    - test_hand02_impl_notes.sh exits non-zero (RED) against current un-elevated handoff.md
    - test_flow01_transitions.sh exits non-zero (RED) against current un-elevated flows.md
    - test_cross01_includes.sh exits non-zero (PARTIAL RED — checks 6+7 fail for handoff.md and flows.md)
  </acceptance_criteria>
  <done>4 Nyquist test scripts exist in phase directory, all appropriately RED against un-elevated skills, following Phase 32-35 bash grep pattern exactly</done>
</task>

<task type="auto">
  <name>Task 2: Elevate handoff.md — motion spec fields, implementation notes, @motion-design.md include (HAND-01, HAND-02, CROSS-01)</name>
  <files>workflows/handoff.md</files>
  <read_first>
    workflows/handoff.md
    references/motion-design.md
    .planning/phases/36-design-elevation-handoff-flows-cross-cutting/36-RESEARCH.md
    .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand01_motion_spec.sh
    .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand02_impl_notes.sh
  </read_first>
  <action>
Elevate workflows/handoff.md with 3 additive changes. Follow the established elevation pattern from phases 32-35: additive inserts only, no structural modification to the 7-step pipeline, no steps added or removed, no steps renumbered.

**Change 1: Add @references/motion-design.md to required_reading block (CROSS-01)**

Find the `<required_reading>` block:
```
<required_reading>
@references/skill-style-guide.md
@references/mcp-integration.md
</required_reading>
```

Replace it with (append AFTER existing includes — never reorder):
```
<required_reading>
@references/skill-style-guide.md
@references/mcp-integration.md
@references/motion-design.md
</required_reading>
```

**Change 2: Add motion spec fields to TypeScript interface generation instructions (HAND-01)**

Find the section in Step 4g or Step 5c where TypeScript interface generation is described (the step that instructs writing `export interface {Component}Props`).

After the existing instruction for component props, add the following subsection. If there is a heading like `#### 4g.7` or similar, add it as `#### 4g.8 Motion specification fields` (or the next available subsection number). If the step uses prose rather than numbered subsections, add it as a clearly separated paragraph block under Step 4g.

The exact text to insert:

```
#### Motion specification fields in TypeScript interfaces (HAND-01)

For each component in the per-screen spec: check whether the upstream mockup defined motion behavior for this component. A component has upstream motion if either:
- The `### Motion Specs` table for its screen has an entry for this component, OR
- A `<!-- VISUAL-HOOK: -->` comment in the upstream mockup references this component.

**If upstream motion exists:** Add the following motion spec block to the `export interface {Component}Props` declaration, separated by a `// --- Motion specification (from mockup motion annotations) ---` comment:

```typescript
// --- Motion specification (from mockup motion annotations) ---
/**
 * Entrance animation trigger for this component.
 * 'on-load' = plays on DOMContentLoaded (above-fold components)
 * 'on-scroll' = plays when component enters viewport
 * 'on-interaction' = plays on user action (hover, click)
 * @default 'on-load'
 */
motionTrigger?: 'on-load' | 'on-scroll' | 'on-interaction';

/**
 * Entrance animation duration in milliseconds.
 * Corresponds to design token: --duration-slow (500ms) or --duration-dramatic (800ms)
 * Reference: @references/motion-design.md — Duration Scale section
 * @default 700
 */
motionDuration?: number;

/**
 * CSS easing function name (maps to design token).
 * 'ease-enter' = cubic-bezier(0, 0, 0.2, 1) — recommended for entrances
 * 'spring' = cubic-bezier(0.34, 1.56, 0.64, 1) — for spring overshoot
 * @default 'ease-enter'
 */
motionEasing?: 'ease-standard' | 'ease-enter' | 'ease-exit' | 'spring';

/**
 * When true, skips all motion for users with prefers-reduced-motion.
 * Required for WCAG 2.2.2 compliance (Level A).
 * @default true
 */
respectReducedMotion?: boolean;
```

**If no upstream motion exists (static/data-display component):** Do NOT add motion fields. Instead, add a single comment at the end of the interface:
```typescript
// No motion annotations found in upstream mockup for this component.
```

**Critical rule:** Never invent motion props for components that have no upstream motion definition (e.g., a DataTable, NavigationLink, or TextBlock). Motion fields are only generated where mockup motion was explicitly defined.
```

**Change 3: Add Implementation Notes subsection instruction (HAND-02)**

Find the section in Step 4g or Step 5b where "Per-Screen Detail Spec" output is described (the section that generates the `### Interaction Specs` subsection).

After the existing `### Interaction Specs` instruction, add the following new subsection instruction:

```
#### Implementation Notes subsection (HAND-02)

After `### Interaction Specs`, check whether this screen has concept-specific interactions identified in the upstream mockup. A screen has concept-specific interactions if:
- Any `<!-- VISUAL-HOOK: {id} -->` comment was present in the upstream mockup for this screen, OR
- The critique or iterate phase identified a distinctive, named interaction pattern for this screen.

**If concept-specific interactions exist:** Add a `### Implementation Notes` subsection immediately after `### Interaction Specs`:

```markdown
### Implementation Notes

**Concept-specific interactions detected in upstream mockup:**

| Interaction | VISUAL-HOOK ID | Description | Recommended Approach | Library |
|-------------|----------------|-------------|---------------------|---------|
| {interaction-name} | {visual-hook-id} | {brief description from mockup annotation} | {recommended CSS/JS approach with @supports guard if required} | {CSS only / GSAP ScrollTrigger 3.14 / View Transitions API} |

**Implementation guidance:** {Prose note for any interaction requiring a fallback, browser support caveat, or non-obvious implementation detail. Always include browser support for View Transitions API: Chrome 111+, Firefox 126+, Safari 18+ — ~75-80% coverage. Wrap in document.startViewTransition() feature detect with CSS fallback.}
```

**Vocabulary for Recommended Approach column** (draw from @references/motion-design.md):
- Scroll-driven parallax → "CSS `animation-timeline: view()` inside `@supports (animation-timeline: scroll())` guard. Fallback: static layout for Firefox. Or GSAP ScrollTrigger 3.14 (100% coverage)."
- Spring button feedback → "CSS `transition: transform 150ms cubic-bezier(0.34, 1.56, 0.64, 1)` — no library needed."
- Variable font animation → "CSS `font-weight` transition (variable font required — confirm `wght` axis available for the loaded font)."
- Shared element transition → "`document.startViewTransition()` — Chrome 111+, Firefox 126+, Safari 18+. Feature detect required."
- Page/route transition → "GSAP timeline with `autoAlpha` for FOUC prevention. Or View Transitions API with fallback."

**If no concept-specific interactions exist:** Omit the `### Implementation Notes` subsection entirely. Do not emit an empty subsection.

**Important:** Do NOT duplicate content from `### Interaction Specs`. Interaction Specs documents the 7 states (hover, focus, active, etc.). Implementation Notes is exclusively for concept-specific visual hooks requiring named libraries or non-trivial CSS techniques.
```

**CRITICAL: Preserve ALL existing content.** Do NOT remove or reorder any existing steps. Do NOT change step numbering. The 7-step pipeline structure (Steps 1-7), all prerequisite handling, all flag behavior, all artifact discovery, all TypeScript interface rules for non-motion props, and all design-manifest update logic must remain exactly as they are.
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && bash .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand01_motion_spec.sh && bash .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand02_impl_notes.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@references/motion-design' workflows/handoff.md` returns at least 1
    - `grep -c 'motionTrigger' workflows/handoff.md` returns at least 1
    - `grep -c 'motionDuration' workflows/handoff.md` returns at least 1
    - `grep -c 'motionEasing' workflows/handoff.md` returns at least 1
    - `grep -c 'respectReducedMotion' workflows/handoff.md` returns at least 1
    - `grep -c 'Implementation Notes' workflows/handoff.md` returns at least 1
    - `grep -c 'VISUAL-HOOK' workflows/handoff.md` returns at least 1
    - `grep -c 'Recommended Approach' workflows/handoff.md` returns at least 1
    - test_hand01_motion_spec.sh exits 0 (GREEN — all 5 checks pass)
    - test_hand02_impl_notes.sh exits 0 (GREEN — all 3 checks pass)
    - `grep -c 'Step [1-7]' workflows/handoff.md` returns the same count as before elevation (7-step structure preserved)
  </acceptance_criteria>
  <done>HAND-01 and HAND-02 Nyquist tests GREEN; handoff.md has motion spec TypeScript fields, implementation notes pattern, and @references/motion-design.md in required_reading; 7-step structure unchanged</done>
</task>

<task type="auto">
  <name>Task 3: Elevate flows.md — screen transition annotations + document ORDER-01 and CROSS-02 (FLOW-01, ORDER-01, CROSS-02)</name>
  <files>
    workflows/flows.md
    .planning/phases/36-design-elevation-handoff-flows-cross-cutting/36-VERIFICATION.md
  </files>
  <read_first>
    workflows/flows.md
    .planning/phases/36-design-elevation-handoff-flows-cross-cutting/36-RESEARCH.md
    .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_flow01_transitions.sh
    .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_cross01_includes.sh
  </read_first>
  <action>
Two sub-tasks in this task: (A) elevate workflows/flows.md, and (B) create the VERIFICATION.md.

---

**Sub-task A: Elevate workflows/flows.md with screen transition annotations (FLOW-01)**

Find the section in flows.md that describes the Mermaid flowchart generation for user journey diagrams (the step that produces `flowchart TD` diagrams). Add the following transition annotation instructions as an additive subsection within that step. Do NOT restructure steps or renumber them.

The exact text to insert (place it after the existing Mermaid node/edge generation instructions, before or after "Step Descriptions" generation instructions — whichever is the natural location):

```
#### Transition annotations on screen-to-screen edges (FLOW-01)

For every edge between two screen nodes, annotate the visual transition mechanism using parenthetical notation appended to the edge label:

**Format:**
```mermaid
J1_1 -->|"CTA click (slide-up)"| J1_2
J1_3 -->|"No (fade)"| J1_ERR1
J1_4 -->|"Verified (morph-expand)"| J1_DONE
```

**Transition vocabulary — use exactly these terms:**

| Category | Variants | When to Use |
|----------|----------|-------------|
| `slide-right` / `slide-left` / `slide-up` / `slide-down` | Directional slide | Navigation with a clear hierarchy (forward/back, deeper/up) |
| `fade` | Cross-dissolve | Parallel screens with no directional hierarchy; modal/overlay contexts; error states |
| `morph` / `morph-expand` / `morph-collapse` | Element expands to become next screen | Card-to-detail, button-to-form-reveal, thumbnail-to-full |
| `shared-element` | Named element visually persists between screens | Product image in cart, avatar in profile, hero expanding to detail |

**RULE — Screen-to-screen edges only:** Annotate ONLY edges between rectangular screen nodes (`["Screen Name"]`). Decision node branches (`{Decision text}` curly brace shape) get their existing semantic label (Yes/No/Success/Error) but do NOT get a transition annotation — logical branches are not visual transitions.

**Annotation placement:**
- Inline: Append `(transition-type)` to the existing edge label — `-->|"Yes (slide-right)"|`
- If the edge has no existing label: `-->|"(fade)"|`
- Decision node edge with semantic label stays semantic only: `-->|"No"|` (no transition)

**Step Descriptions extension:** After the narrative description for each screen node in the `### Step Descriptions` subsection, add a transition rationale line for each outgoing screen-to-screen edge:

```
4. **J1_4 - Email Verification Screen:** Confirmation with "Check your email" message.
   → Transition to J1_5: `morph-expand` — verification confirmation expands to fill dashboard layout, communicating "you've arrived."
```

**Include error state transitions:** Annotate ALL screen-to-screen edges including error state paths. Error transitions are typically `fade` — fast to document and gives engineers the same information. Decision branches that lead directly to error screens should annotate the edge from the error screen outward (if one exists), not the decision branch itself.

**Deprecated vocabulary:** Do NOT use "push" or "pop" (older iOS UIKit convention). Do NOT use `@scroll-timeline` at-rule in any transition recommendation — use `animation-timeline` CSS property. Transition annotations are visual mechanism descriptions, not implementation recommendations — save implementation details for handoff.md `### Implementation Notes`.
```

**CRITICAL: Preserve ALL existing content.** Do NOT remove or modify any step logic in flows.md. The Mermaid generation, screen inventory JSON, brief reading, coverage flag updates, all flag handling, and all step 5/6/7 logic must remain unchanged.

---

**Sub-task B: Create 36-VERIFICATION.md documenting ORDER-01 and CROSS-02 evidence**

Create `.planning/phases/36-design-elevation-handoff-flows-cross-cutting/36-VERIFICATION.md` with the following content:

```markdown
---
phase: 36-design-elevation-handoff-flows-cross-cutting
status: complete
nyquist_tests: GREEN
created: {today's date}
---

# Phase 36 — Verification

## Automated Tests

All 4 Nyquist test scripts GREEN:

```bash
# Full suite run:
for f in .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand*.sh .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_flow*.sh .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_cross*.sh; do bash "$f"; done
```

| Script | Requirement | Checks | Status |
|--------|-------------|--------|--------|
| test_hand01_motion_spec.sh | HAND-01 | 5 | GREEN |
| test_hand02_impl_notes.sh | HAND-02 | 3 | GREEN |
| test_flow01_transitions.sh | FLOW-01 | 2 | GREEN |
| test_cross01_includes.sh | CROSS-01 | 7 | GREEN |

## ORDER-01: Design Elevation Dependency Order (Manual Verification)

**Requirement:** Design elevation follows strict dependency order — system → wireframe → critique/iterate → mockup (upstream quality sets downstream ceiling).

**Evidence:** All upstream elevation phases completed before Phase 36:

| Phase | Skill | Completed | Dependency Rationale |
|-------|-------|-----------|---------------------|
| Phase 32 | system.md | 2026-03-18 | Foundation — motion tokens, OKLCH palettes, APCA contrast, type pairings. Sets the vocabulary all downstream skills reference. |
| Phase 33 | wireframe.md | 2026-03-18 | Requires system tokens (density spacing, grid rationale). Wireframe quality ceiling set by upstream system. |
| Phase 34 | critique.md, hig.md | 2026-03-18 | Requires wireframe (evaluates wireframe composition quality). Requires system (references quality-standards.md). |
| Phase 35 | mockup.md | 2026-03-18 | Requires system (spring tokens, DTCG format), wireframe (layout quality), critique/iterate framework (runs before mockup in pipeline). |
| Phase 36 | handoff.md, flows.md | 2026-03-18 | Requires mockup (VISUAL-HOOK convention — handoff implementation notes reference mockup annotations). Requires system (motion-design.md — handoff motion spec vocabulary drawn from motion tokens). |

**Dependency chain (canonical order):**
```
system → wireframe → critique/iterate → mockup → handoff + flows
```

All phases executed in this order. ORDER-01 satisfied.

## CROSS-01: Reference Injection Uniformity (Automated + Manual Confirmation)

**Requirement:** All 7 elevated skills load quality references via `@` includes in `required_reading`. No structural changes to the 7-step skill anatomy.

**Automated evidence:** `test_cross01_includes.sh` passes 7/7 checks.

| Skill | @references/ includes | CROSS-01 |
|-------|-----------------------|----------|
| system.md | skill-style-guide, mcp-integration, motion-design, composition-typography | COMPLIANT |
| wireframe.md | skill-style-guide, mcp-integration, composition-typography | COMPLIANT |
| critique.md | skill-style-guide, mcp-integration, quality-standards, composition-typography | COMPLIANT |
| hig.md | skill-style-guide, mcp-integration, motion-design | COMPLIANT |
| mockup.md | skill-style-guide, mcp-integration, motion-design | COMPLIANT |
| handoff.md | skill-style-guide, mcp-integration, motion-design | COMPLIANT (added Phase 36) |
| flows.md | skill-style-guide, mcp-integration | COMPLIANT (transition vocabulary embedded in prose — no additional reference file needed) |

7-step pipeline structure preserved in all 7 skills. No steps added or renumbered.

## CROSS-02: Audit Delta Measurement (Manual Verification)

**Requirement:** Running /pde:audit after all elevation phases produces measurable quality delta against Phase 30 baseline.

**Procedure:**

1. **Pre-elevation baseline** (run before Phase 36 skill edits):
   ```
   /pde:audit --save-baseline
   ```
   Records to `.planning/audit-baseline.json`. Note the `finding_count` and `skill_quality_pct` for handoff.md and flows.md specifically.

2. **Apply Phase 36 elevations** (Tasks 2 and 3 of this plan).

3. **Post-elevation audit**:
   ```
   /pde:audit
   ```
   Compare: `finding_count_delta` (negative = improvement) and `skill_quality_pct` delta for handoff/flows domain.

**Acceptance criteria for CROSS-02:**
One of the following must be true:
- (a) `finding_count_delta < 0` (fewer total findings), OR
- (b) handoff.md or flows.md specific findings are fewer in post-elevation audit, OR
- (c) If audit tool does not capture the specific HAND/FLOW additions (motion spec fields are generated output, not evaluated by auditor against rubric), document this explicitly: "Audit tool evaluates skill generation quality via rubric — HAND-01/02 and FLOW-01 additions are structural (instruction quality), not scored as generated artifacts. Delta is qualitative: handoff.md gained 3 new generation directives; flows.md gained 1. Quality delta documented via Nyquist test GREEN state."

**Status:** Pending — run /pde:audit --save-baseline before elevation, then /pde:audit after, and record delta here.
```

After creating the VERIFICATION.md, run the full test suite to confirm test_cross01_includes.sh is GREEN (all 7 checks pass after handoff.md was elevated in Task 2):

```bash
cd "/Users/greyaltaer/code/projects/Platform Development Engine" && bash .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_flow01_transitions.sh && bash .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_cross01_includes.sh
```
  </action>
  <verify>
    <automated>cd "/Users/greyaltaer/code/projects/Platform Development Engine" && bash .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_flow01_transitions.sh && bash .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_cross01_includes.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ciE 'slide-right|slide-left|slide-up|slide-down|morph|shared-element' workflows/flows.md` returns at least 1
    - `grep -ci 'transition annotation\|visual mechanism\|visual transition' workflows/flows.md` returns at least 1
    - test_flow01_transitions.sh exits 0 (GREEN — 2/2 checks pass)
    - test_cross01_includes.sh exits 0 (GREEN — 7/7 checks pass, including handoff.md and flows.md)
    - `.planning/phases/36-design-elevation-handoff-flows-cross-cutting/36-VERIFICATION.md` exists
    - `grep -c 'ORDER-01' .planning/phases/36-design-elevation-handoff-flows-cross-cutting/36-VERIFICATION.md` returns at least 1
    - `grep -c 'CROSS-02' .planning/phases/36-design-elevation-handoff-flows-cross-cutting/36-VERIFICATION.md` returns at least 1
    - `grep -c 'system.*wireframe.*critique\|system.*→.*wireframe\|dependency order' .planning/phases/36-design-elevation-handoff-flows-cross-cutting/36-VERIFICATION.md` returns at least 1
    - `grep -c 'Step [1-7]' workflows/flows.md` returns the same count as before elevation (7-step structure preserved)
  </acceptance_criteria>
  <done>FLOW-01 and CROSS-01 Nyquist tests GREEN; flows.md has transition annotation vocabulary and instructions; VERIFICATION.md documents ORDER-01 dependency rationale and CROSS-02 audit delta procedure</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm all 4 Nyquist tests GREEN:

```bash
cd "/Users/greyaltaer/code/projects/Platform Development Engine" && for f in .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand01_motion_spec.sh .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_hand02_impl_notes.sh .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_flow01_transitions.sh .planning/phases/36-design-elevation-handoff-flows-cross-cutting/test_cross01_includes.sh; do echo "--- $f ---"; bash "$f"; done
```

Expected output: All 4 scripts exit 0. Total: 17 checks (5 + 3 + 2 + 7).

Manual confirmation:
- ORDER-01: VERIFICATION.md section documents system→wireframe→critique→mockup→handoff+flows dependency rationale
- CROSS-02: VERIFICATION.md section documents audit delta procedure with pre/post instructions
</verification>

<success_criteria>
- 4 Nyquist test scripts exist in .planning/phases/36-design-elevation-handoff-flows-cross-cutting/
- test_hand01_motion_spec.sh GREEN (5/5): motionTrigger, motionDuration, motionEasing, respectReducedMotion, @references/motion-design in handoff.md
- test_hand02_impl_notes.sh GREEN (3/3): Implementation Notes section, VISUAL-HOOK reference, Recommended Approach column in handoff.md
- test_flow01_transitions.sh GREEN (2/2): transition annotation vocabulary and instruction in flows.md
- test_cross01_includes.sh GREEN (7/7): all 7 skill files have @references/ in required_reading
- workflows/handoff.md: @references/motion-design.md in required_reading, motionTrigger/motionDuration/motionEasing/respectReducedMotion conditional fields documented, Implementation Notes pattern with VISUAL-HOOK linkage documented
- workflows/flows.md: slide/fade/morph/shared-element vocabulary, screen-to-screen annotation rule (decision nodes excluded), Step Descriptions transition rationale extension
- 36-VERIFICATION.md: ORDER-01 dependency chain documented, CROSS-02 audit delta procedure documented with pre/post audit instructions
- All 7-step pipeline structures preserved in handoff.md and flows.md (no steps added, removed, or renumbered)
</success_criteria>

<output>
After completion, create `.planning/phases/36-design-elevation-handoff-flows-cross-cutting/36-01-SUMMARY.md`
</output>
