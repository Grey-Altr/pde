---
phase: 170-pde-utilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/utils/mermaid-renderer.cjs
  - bin/lib/utils/token-validator.cjs
  - tests/phase-170/mermaid-renderer.test.mjs
  - tests/phase-170/token-validator.test.mjs
  - package.json
autonomous: true
requirements: [UTL-01, UTL-02, UTL-03]

must_haves:
  truths:
    - "detectRenderer() returns 'mmdr' when mmdr binary is found, 'mmdc' as fallback, null when neither exists"
    - "renderMermaid() calls the detected binary with correct -i/-o/-e flags and produces output"
    - "validateTokens() recursively walks a DTCG token tree and flags tokens missing $type or violating {group}.{token} naming"
    - "checkOklchGamut() flags OKLCH color values outside P3 gamut using colorjs.io"
    - "checkApcaContrast() flags text/background pairs below 60 Lc for body text and 45 Lc for large text"
  artifacts:
    - path: "bin/lib/utils/mermaid-renderer.cjs"
      provides: "mmdr/mmdc binary detection and Mermaid diagram rendering"
      exports: ["detectRenderer", "renderMermaid"]
    - path: "bin/lib/utils/token-validator.cjs"
      provides: "DTCG token validation with OKLCH gamut and APCA contrast checks"
      exports: ["validateTokens", "checkOklchGamut", "checkApcaContrast", "runTokenValidation"]
    - path: "tests/phase-170/mermaid-renderer.test.mjs"
      provides: "Unit tests for mermaid renderer"
    - path: "tests/phase-170/token-validator.test.mjs"
      provides: "Unit tests for token validator"
  key_links:
    - from: "bin/lib/utils/mermaid-renderer.cjs"
      to: "mmdr/mmdc binary"
      via: "execFileSync with dependency injection"
      pattern: "_execFn.*mmdr"
    - from: "bin/lib/utils/token-validator.cjs"
      to: "apca-w3"
      via: "require('apca-w3')"
      pattern: "calcAPCA"
    - from: "bin/lib/utils/token-validator.cjs"
      to: "colorjs.io"
      via: "require('colorjs.io')"
      pattern: "inGamut.*p3"
---

<objective>
Build the mermaid renderer and DTCG token validator utility modules with full test coverage.

Purpose: Provide the core logic modules that the /pde:render-mermaid and /pde:validate-tokens commands will call. These are the two most complex modules in Phase 170 — the renderer involves binary auto-detection with fallback, and the token validator combines DTCG spec validation with OKLCH gamut and APCA contrast checking.

Output: Two CJS modules in bin/lib/utils/ with dependency injection, plus vitest test suites. npm dependencies apca-w3 and colorjs.io installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/170-pde-utilities/170-CONTEXT.md
@.planning/phases/170-pde-utilities/170-RESEARCH.md

@bin/lib/3d-pipeline/cad.cjs (binary detection pattern — checkCadQuery)
@bin/lib/design.cjs (dtcgToCssLines token traversal pattern)
@bin/lib/image-pipeline/visual-diff.cjs (CJS module structure pattern)

<interfaces>
<!-- Established patterns executor must follow -->

From bin/lib/3d-pipeline/cad.cjs (binary detection pattern):
```javascript
function checkCadQuery(_execFn) {
  const execFn = _execFn || execFileSync;
  try {
    execFn('cadquery', ['--version'], { encoding: 'utf8', timeout: 5000 });
    return true;
  } catch (_) { return false; }
}
```

From bin/lib/design.cjs (DTCG traversal pattern):
```javascript
function dtcgToCssLines(tokens, prefix) {
  prefix = prefix || '';
  const lines = [];
  for (const key of Object.keys(tokens)) {
    if (key.startsWith('$')) continue;
    const node = tokens[key];
    if (node && typeof node === 'object' && '$value' in node) {
      // Leaf token
    } else if (node && typeof node === 'object') {
      lines.push(...dtcgToCssLines(node, prefix + key + '-'));
    }
  }
  return lines;
}
```

From apca-w3 (npm):
```javascript
const { calcAPCA } = require('apca-w3');
// First arg = text foreground, second = background
const Lc = calcAPCA('#333333', '#ffffff'); // returns e.g. 88.0
```

From colorjs.io:
```javascript
const Color = require('colorjs.io').default;
const c = new Color('oklch(0.7 0.2 120)');
c.inGamut('p3');   // true/false
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install deps + create mermaid-renderer.cjs with tests</name>
  <files>package.json, bin/lib/utils/mermaid-renderer.cjs, tests/phase-170/mermaid-renderer.test.mjs</files>
  <read_first>
    - bin/lib/3d-pipeline/cad.cjs (binary detection pattern with _execFn injection)
    - bin/lib/image-pipeline/visual-diff.cjs (CJS module structure, exports pattern)
    - tests/phase-169/cad.test.mjs (test file structure for CJS modules)
    - package.json (current deps)
  </read_first>
  <behavior>
    - detectRenderer(_execFn) returns 'mmdr' when mmdr --version succeeds
    - detectRenderer(_execFn) returns 'mmdc' when mmdr fails but mmdc --version succeeds
    - detectRenderer(_execFn) returns null when both fail
    - renderMermaid({ input, outputPath, format, _execFn }) calls mmdr with ['-i', input, '-o', outputPath, '-e', format]
    - renderMermaid falls back to mmdc with ['-i', input, '-o', outputPath] when mmdr not found
    - renderMermaid throws with install instructions when no renderer found
    - renderMermaid defaults format to 'svg' when not specified
  </behavior>
  <action>
    1. Run `npm install apca-w3 colorjs.io` to add both dependencies (needed for Task 2).

    2. Create `bin/lib/utils/` directory.

    3. Create `tests/phase-170/mermaid-renderer.test.mjs` FIRST with failing tests for all behaviors above. Use `createRequire(import.meta.url)` to import the CJS module. Mock _execFn to simulate binary detection.

    4. Create `bin/lib/utils/mermaid-renderer.cjs`:
       - 'use strict' header with JSDoc module description
       - `const { execFileSync } = require('child_process');`
       - `const MMDR_INSTALL_MSG` with brew tap + npm install instructions
       - `function detectRenderer(_execFn)` — try mmdr --version first, then mmdc --version, return 'mmdr'/'mmdc'/null. Use `process.env.MMDR_PATH || 'mmdr'` for the binary path.
       - `function renderMermaid({ input, outputPath, format, _execFn })` — detect renderer, call with appropriate args per renderer type:
         - mmdr: `['-i', input, '-o', outputPath, '-e', format || 'svg']`
         - mmdc: `['-i', input, '-o', outputPath]` (mmdc infers format from extension)
         - null: throw Error with MMDR_INSTALL_MSG
       - `module.exports = { detectRenderer, renderMermaid }`

    5. Run tests, confirm GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/phase-170/mermaid-renderer.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "detectRenderer" bin/lib/utils/mermaid-renderer.cjs
    - grep -q "renderMermaid" bin/lib/utils/mermaid-renderer.cjs
    - grep -q "module.exports" bin/lib/utils/mermaid-renderer.cjs
    - grep -q "MMDR_INSTALL_MSG" bin/lib/utils/mermaid-renderer.cjs
    - grep -q "process.env.MMDR_PATH" bin/lib/utils/mermaid-renderer.cjs
    - grep -q "_execFn" bin/lib/utils/mermaid-renderer.cjs
    - grep -q "detectRenderer" tests/phase-170/mermaid-renderer.test.mjs
    - grep -q "renderMermaid" tests/phase-170/mermaid-renderer.test.mjs
    - npx vitest run tests/phase-170/mermaid-renderer.test.mjs exits 0
  </acceptance_criteria>
  <done>detectRenderer returns 'mmdr'/'mmdc'/null based on binary availability. renderMermaid calls the correct binary with correct args or throws with install instructions. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create token-validator.cjs with OKLCH/APCA checks and tests</name>
  <files>bin/lib/utils/token-validator.cjs, tests/phase-170/token-validator.test.mjs</files>
  <read_first>
    - bin/lib/design.cjs (dtcgToCssLines traversal pattern — lines 164-190)
    - package.json (confirm apca-w3 and colorjs.io installed from Task 1)
    - .planning/phases/170-pde-utilities/170-RESEARCH.md (APCA/colorjs.io API patterns, pitfalls 2 and 3)
  </read_first>
  <behavior>
    - validateTokens(tokens) flags missing $type on leaf tokens (have $value but no $type)
    - validateTokens(tokens) flags naming violations — tokens at root level without a group prefix (no dot in path)
    - validateTokens(tokens) recursively walks nested groups, skipping $-prefixed keys
    - checkOklchGamut(colorValue) returns { inP3: true/false, inSrgb: true/false } for valid OKLCH strings
    - checkOklchGamut(badValue) returns { error: 'parse-error' } for unparseable color values
    - checkApcaContrast(textColor, bgColor) returns { Lc: number, bodyPass: boolean, largePass: boolean }
    - checkApcaContrast uses thresholds: |Lc| >= 60 for body text, |Lc| >= 45 for large text
    - runTokenValidation(tokens) combines schema + gamut + contrast checks into a structured report with violations array and summary
    - runTokenValidation outputs both JSON report object and markdown summary string
  </behavior>
  <action>
    1. Create `tests/phase-170/token-validator.test.mjs` FIRST with failing tests:
       - Test validateTokens with valid token tree (no violations)
       - Test validateTokens with missing $type (should flag)
       - Test validateTokens with root-level token without group (should flag naming)
       - Test validateTokens with nested groups (should recurse correctly)
       - Test checkOklchGamut with in-P3 color 'oklch(0.7 0.1 120)' — expect inP3: true
       - Test checkOklchGamut with out-of-P3 color 'oklch(0.9 0.4 150)' — expect inP3: false
       - Test checkOklchGamut with invalid string — expect error: 'parse-error'
       - Test checkApcaContrast('#333333', '#ffffff') — expect Lc > 60, bodyPass: true, largePass: true
       - Test checkApcaContrast('#999999', '#aaaaaa') — expect bodyPass: false (low contrast)
       - Test runTokenValidation with mixed tokens — expect violations array with specific issues, markdown string with table

    2. Create `bin/lib/utils/token-validator.cjs`:
       - 'use strict' header with JSDoc
       - Require apca-w3: `const { calcAPCA } = require('apca-w3');`
       - Require colorjs.io: `const Color = require('colorjs.io').default;`
       - `function validateTokens(tokens, prefix, violations)` — recursive walk following dtcgToCssLines pattern. For each leaf token ($value present):
         - If no $type, push { token: path, issue: 'missing $type' }
         - If path has no '.' (root level), push { token: path, issue: 'naming: must be {group}.{token}' }
       - `function checkOklchGamut(colorValue)` — try `new Color(colorValue)`, call `.inGamut('p3')` and `.inGamut('srgb')`. Catch parse errors, return `{ error: 'parse-error' }`.
       - `function checkApcaContrast(textColor, bgColor)` — call `calcAPCA(textColor, bgColor)`, compute `absLc = Math.abs(Lc)`, return `{ Lc, bodyPass: absLc >= 60, largePass: absLc >= 45 }`.
       - `function runTokenValidation(tokens)` — orchestrator:
         1. Call validateTokens for schema violations
         2. Walk color tokens ($type === 'color') and call checkOklchGamut
         3. Find text/bg pairs by group name containing 'text'/'fg' vs 'bg'/'surface' and check APCA contrast
         4. Build violations array with { token, issue, details }
         5. Build markdown summary as table: `| Token | Issue | Details |`
         6. Return `{ violations, summary, stats: { total, passed, failed } }`
       - `module.exports = { validateTokens, checkOklchGamut, checkApcaContrast, runTokenValidation }`

    3. Run tests, confirm GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/phase-170/token-validator.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "validateTokens" bin/lib/utils/token-validator.cjs
    - grep -q "checkOklchGamut" bin/lib/utils/token-validator.cjs
    - grep -q "checkApcaContrast" bin/lib/utils/token-validator.cjs
    - grep -q "runTokenValidation" bin/lib/utils/token-validator.cjs
    - grep -q "calcAPCA" bin/lib/utils/token-validator.cjs
    - grep -q "inGamut" bin/lib/utils/token-validator.cjs
    - grep -q "module.exports" bin/lib/utils/token-validator.cjs
    - grep -q "bodyPass" bin/lib/utils/token-validator.cjs
    - grep -q "60" bin/lib/utils/token-validator.cjs (body text threshold)
    - grep -q "45" bin/lib/utils/token-validator.cjs (large text threshold)
    - npx vitest run tests/phase-170/token-validator.test.mjs exits 0
  </acceptance_criteria>
  <done>Token validator recursively validates DTCG tokens for schema completeness and naming conventions. OKLCH gamut checks flag out-of-P3 colors. APCA contrast checks flag pairs below 60 Lc (body) and 45 Lc (large). runTokenValidation produces structured JSON report + markdown summary. All tests pass.</done>
</task>

</tasks>

<verification>
npx vitest run tests/phase-170/mermaid-renderer.test.mjs
npx vitest run tests/phase-170/token-validator.test.mjs
grep -q "apca-w3" package.json
grep -q "colorjs.io" package.json
</verification>

<success_criteria>
- mermaid-renderer.cjs exports detectRenderer and renderMermaid with _execFn dependency injection
- token-validator.cjs exports validateTokens, checkOklchGamut, checkApcaContrast, runTokenValidation
- apca-w3 and colorjs.io are in package.json dependencies
- All unit tests pass via vitest
</success_criteria>

<output>
After completion, create `.planning/phases/170-pde-utilities/170-01-SUMMARY.md`
</output>
