---
phase: 170-pde-utilities
plan: 03
type: execute
wave: 2
depends_on: [170-01, 170-02]
files_modified:
  - bin/pde-tools.cjs
  - commands/validate-tokens.md
  - commands/gen-tests.md
  - commands/verify-handoff.md
  - commands/render-mermaid.md
  - commands/visual-diff.md
autonomous: true
requirements: [UTL-01, UTL-02, UTL-03, UTL-04, UTL-05, UTL-06, UTL-07, UTL-08]

must_haves:
  truths:
    - "node bin/pde-tools.cjs utils render-mermaid --input file.mmd --output file.svg calls renderMermaid"
    - "node bin/pde-tools.cjs utils validate-tokens --tokens-file tokens.json calls runTokenValidation"
    - "node bin/pde-tools.cjs utils gen-tests calls generateTestScaffold and writes output file"
    - "node bin/pde-tools.cjs utils verify-handoff calls verifyHandoff and prints gap report"
    - "/pde:visual-diff delegates to existing image diff command"
    - "Each new /pde: command has a skill file in commands/ with usage, parameters, and output documentation"
  artifacts:
    - path: "bin/pde-tools.cjs"
      provides: "case 'utils' block routing to all four utility modules"
      contains: "case 'utils'"
    - path: "commands/validate-tokens.md"
      provides: "/pde:validate-tokens skill file"
    - path: "commands/gen-tests.md"
      provides: "/pde:gen-tests skill file"
    - path: "commands/verify-handoff.md"
      provides: "/pde:verify-handoff skill file"
    - path: "commands/render-mermaid.md"
      provides: "/pde:render-mermaid skill file"
    - path: "commands/visual-diff.md"
      provides: "/pde:visual-diff skill file confirming delegation to image diff"
  key_links:
    - from: "bin/pde-tools.cjs"
      to: "bin/lib/utils/mermaid-renderer.cjs"
      via: "require in case 'utils' render-mermaid subcommand"
      pattern: "require.*mermaid-renderer"
    - from: "bin/pde-tools.cjs"
      to: "bin/lib/utils/token-validator.cjs"
      via: "require in case 'utils' validate-tokens subcommand"
      pattern: "require.*token-validator"
    - from: "bin/pde-tools.cjs"
      to: "bin/lib/utils/flow-test-gen.cjs"
      via: "require in case 'utils' gen-tests subcommand"
      pattern: "require.*flow-test-gen"
    - from: "bin/pde-tools.cjs"
      to: "bin/lib/utils/handoff-verifier.cjs"
      via: "require in case 'utils' verify-handoff subcommand"
      pattern: "require.*handoff-verifier"
    - from: "commands/visual-diff.md"
      to: "bin/lib/image-pipeline/visual-diff.cjs"
      via: "pde-tools.cjs image diff delegation"
      pattern: "pde-tools.cjs image diff"
---

<objective>
Wire all Phase 170 utility modules into pde-tools.cjs and create skill files for each /pde: command.

Purpose: Connect the four utility modules (mermaid-renderer, token-validator, flow-test-gen, handoff-verifier) to the central CLI router and create command documentation so agents and users can discover and invoke them. Also confirms UTL-04 (visual diff) is already accessible via `image diff`.

Output: A new `case 'utils'` block in pde-tools.cjs with four subcommands, plus four skill files in commands/. This is the final plan of Phase 170 — the milestone capstone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/170-pde-utilities/170-CONTEXT.md
@.planning/phases/170-pde-utilities/170-01-SUMMARY.md
@.planning/phases/170-pde-utilities/170-02-SUMMARY.md

@bin/pde-tools.cjs (main CLI router — find insertion point after '3d' case block, before 'phase-plan-index')
@commands/visual-diff.md (existing — confirms UTL-04 already documented)
@commands/3d.md (skill file format reference)

<interfaces>
<!-- Module APIs from Plans 01 and 02 -->

From bin/lib/utils/mermaid-renderer.cjs:
```javascript
module.exports = { detectRenderer, renderMermaid };
// renderMermaid({ input, outputPath, format, _execFn }) → void (writes file)
// format defaults to 'svg'
```

From bin/lib/utils/token-validator.cjs:
```javascript
module.exports = { validateTokens, checkOklchGamut, checkApcaContrast, runTokenValidation };
// runTokenValidation(tokens) → { violations, summary, stats: { total, passed, failed } }
```

From bin/lib/utils/flow-test-gen.cjs:
```javascript
module.exports = { parseFlowchart, generateTestScaffold, findLatestFlowsFile };
// findLatestFlowsFile(designDir) → string|null
// generateTestScaffold({ nodes, edges, baseUrl }) → string (test file content)
```

From bin/lib/utils/handoff-verifier.cjs:
```javascript
module.exports = { parseHandoffSpec, searchForExport, verifyHandoff, findLatestHandoffSpec };
// verifyHandoff({ specPath, srcDir }) → { status, gaps, markdown, stats } or { status: 'no-spec', message }
// findLatestHandoffSpec(designDir) → string|null
```

<!-- pde-tools.cjs insertion point -->
The `case 'utils'` block goes AFTER the `case '3d'` block (ends ~line 984) and BEFORE `case 'phase-plan-index'` (starts ~line 986). This follows the established pattern of media commands (image, video, 3d) then utility commands.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add case 'utils' block to pde-tools.cjs with four subcommands</name>
  <files>bin/pde-tools.cjs</files>
  <read_first>
    - bin/pde-tools.cjs (lines 910-990 — the 3d case block through phase-plan-index, to find exact insertion point)
    - bin/pde-tools.cjs (lines 750-835 — the image case block, as pattern reference for subcommand routing with args.indexOf)
    - bin/lib/utils/mermaid-renderer.cjs (confirm exports)
    - bin/lib/utils/token-validator.cjs (confirm exports)
    - bin/lib/utils/flow-test-gen.cjs (confirm exports)
    - bin/lib/utils/handoff-verifier.cjs (confirm exports)
  </read_first>
  <action>
    Insert a `case 'utils':` block in the main switch statement of pde-tools.cjs, AFTER `case '3d'` (ends with `break; }` around line 984) and BEFORE `case 'phase-plan-index'` (around line 986).

    The block should follow the exact same pattern as `case 'image'` and `case '3d'`:

    ```javascript
    case 'utils': {
      const subcommand = args[1];
      if (subcommand === 'render-mermaid') {
        const { renderMermaid } = require('./lib/utils/mermaid-renderer.cjs');
        const inputIdx = args.indexOf('--input');
        const outputIdx = args.indexOf('--output');
        const formatIdx = args.indexOf('--format');
        const input = inputIdx !== -1 ? args[inputIdx + 1] : undefined;
        const outputPath = outputIdx !== -1 ? args[outputIdx + 1] : undefined;
        if (!input || !outputPath) {
          console.error('Usage: utils render-mermaid --input <file.mmd> --output <file.svg> [--format svg|png]');
          process.exit(1);
        }
        const format = formatIdx !== -1 ? args[formatIdx + 1] : 'svg';
        renderMermaid({ input, outputPath, format });
        console.log(JSON.stringify({ input, output: outputPath, format }, null, 2));
      } else if (subcommand === 'validate-tokens') {
        const { runTokenValidation } = require('./lib/utils/token-validator.cjs');
        const fs = require('fs');
        const tokensIdx = args.indexOf('--tokens-file');
        const tokensFile = tokensIdx !== -1 ? args[tokensIdx + 1] : undefined;
        if (!tokensFile) {
          console.error('Usage: utils validate-tokens --tokens-file <tokens.json>');
          process.exit(1);
        }
        const tokens = JSON.parse(fs.readFileSync(tokensFile, 'utf8'));
        const result = runTokenValidation(tokens);
        console.log(result.summary);
        console.log(JSON.stringify(result.stats, null, 2));
      } else if (subcommand === 'gen-tests') {
        const { parseFlowchart, generateTestScaffold, findLatestFlowsFile } = require('./lib/utils/flow-test-gen.cjs');
        const fs = require('fs');
        const path = require('path');
        const flowsIdx = args.indexOf('--flows-file');
        const outputIdx = args.indexOf('--output');
        const baseUrlIdx = args.indexOf('--base-url');
        let flowsFile = flowsIdx !== -1 ? args[flowsIdx + 1] : null;
        if (!flowsFile) {
          flowsFile = findLatestFlowsFile(path.join(cwd, '.planning/design/ux'));
        }
        if (!flowsFile) {
          console.error('No flows file found. Run /pde:flows first or pass --flows-file <path>');
          process.exit(1);
        }
        const content = fs.readFileSync(flowsFile, 'utf8');
        // Extract mermaid blocks from markdown
        const mermaidMatch = content.match(/```mermaid\n([\s\S]*?)```/);
        const mermaidText = mermaidMatch ? mermaidMatch[1] : content;
        const { nodes, edges } = parseFlowchart(mermaidText);
        const baseUrl = baseUrlIdx !== -1 ? args[baseUrlIdx + 1] : 'http://localhost:3000';
        const scaffold = generateTestScaffold({ nodes, edges, baseUrl });
        const outputPath = outputIdx !== -1 ? args[outputIdx + 1] : path.join(cwd, 'tests/e2e/flow-navigation.spec.ts');
        fs.mkdirSync(path.dirname(outputPath), { recursive: true });
        fs.writeFileSync(outputPath, scaffold, 'utf8');
        console.log(JSON.stringify({ output: outputPath, tests: edges.length, nodes: nodes.size }, null, 2));
      } else if (subcommand === 'verify-handoff') {
        const { verifyHandoff, findLatestHandoffSpec } = require('./lib/utils/handoff-verifier.cjs');
        const path = require('path');
        const specIdx = args.indexOf('--spec');
        const srcIdx = args.indexOf('--src-dir');
        let specPath = specIdx !== -1 ? args[specIdx + 1] : null;
        if (!specPath) {
          specPath = findLatestHandoffSpec(path.join(cwd, '.planning/design/handoff'));
        }
        if (!specPath) {
          console.error('No handoff spec found. Run /pde:handoff first or pass --spec <path>');
          process.exit(1);
        }
        const srcDir = srcIdx !== -1 ? args[srcIdx + 1] : path.join(cwd, 'src');
        const result = verifyHandoff({ specPath, srcDir });
        if (result.status === 'no-spec') {
          console.error(result.message);
          process.exit(1);
        }
        console.log(result.markdown);
        console.log(JSON.stringify(result.stats, null, 2));
      } else {
        console.error('Usage: utils <render-mermaid|validate-tokens|gen-tests|verify-handoff> [options]');
        process.exit(1);
      }
      break;
    }
    ```

    Ensure the block is properly indented to match surrounding case blocks. Do NOT modify any existing case blocks.
  </action>
  <verify>
    <automated>grep -q "case 'utils'" bin/pde-tools.cjs && node bin/pde-tools.cjs utils 2>&1 | grep -q "render-mermaid"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "case 'utils'" bin/pde-tools.cjs
    - grep -q "render-mermaid" bin/pde-tools.cjs
    - grep -q "validate-tokens" bin/pde-tools.cjs
    - grep -q "gen-tests" bin/pde-tools.cjs
    - grep -q "verify-handoff" bin/pde-tools.cjs
    - grep -q "require.*mermaid-renderer" bin/pde-tools.cjs
    - grep -q "require.*token-validator" bin/pde-tools.cjs
    - grep -q "require.*flow-test-gen" bin/pde-tools.cjs
    - grep -q "require.*handoff-verifier" bin/pde-tools.cjs
    - node bin/pde-tools.cjs utils 2>&1 | grep -q "render-mermaid|validate-tokens|gen-tests|verify-handoff" (usage output)
  </acceptance_criteria>
  <done>pde-tools.cjs has a case 'utils' block with four subcommands routing to the four utility modules. Running `node bin/pde-tools.cjs utils` shows usage with all four subcommand names.</done>
</task>

<task type="auto">
  <name>Task 2: Create skill files for all /pde: utility commands and confirm UTL-04 coverage</name>
  <files>commands/validate-tokens.md, commands/gen-tests.md, commands/verify-handoff.md, commands/render-mermaid.md, commands/visual-diff.md</files>
  <read_first>
    - commands/3d.md (skill file format reference — frontmatter, sections, CLI examples)
    - commands/visual-diff.md (existing skill file — confirm it references pde-tools.cjs image diff)
  </read_first>
  <action>
    Create four skill files following the exact format of commands/3d.md and commands/visual-diff.md:

    **commands/render-mermaid.md:**
    - Title: `/pde:render-mermaid`
    - Description: Render Mermaid diagrams via the mmdr Rust renderer (500-1000x faster than mermaid-cli). Falls back to mmdc if mmdr is not installed.
    - Usage: `/pde:render-mermaid --input <file.mmd> --output <file.svg>`
    - CLI: `node bin/pde-tools.cjs utils render-mermaid --input <file.mmd> --output <file.svg> [--format svg|png]`
    - Parameters: --input (Mermaid .mmd file path), --output (output file path), --format (svg or png, default svg)
    - Output: Rendered SVG/PNG file at the specified output path
    - Prerequisites: `brew tap 1jehuang/mmdr && brew install mmdr` OR `npm install -g @mermaid-js/mermaid-cli` as fallback. Set `MMDR_PATH` env var if mmdr binary is not in PATH.

    **commands/validate-tokens.md:**
    - Title: `/pde:validate-tokens`
    - Description: Validate DTCG design tokens against schema completeness, naming conventions, OKLCH gamut ranges, and APCA contrast ratios.
    - Usage: `/pde:validate-tokens --tokens-file <tokens.json>`
    - CLI: `node bin/pde-tools.cjs utils validate-tokens --tokens-file <tokens.json>`
    - Parameters: --tokens-file (path to DTCG token JSON file)
    - Output: Markdown summary table of violations + JSON stats. Checks: missing $type, naming convention {group}.{token}, OKLCH outside P3 gamut, APCA contrast below 60 Lc (body text) or 45 Lc (large text).

    **commands/gen-tests.md:**
    - Title: `/pde:gen-tests`
    - Description: Generate Playwright E2E test skeletons from /pde:flows flow diagram output. Parses Mermaid flowcharts and creates one test per navigation edge.
    - Usage: `/pde:gen-tests [--flows-file <path>] [--output <path>] [--base-url <url>]`
    - CLI: `node bin/pde-tools.cjs utils gen-tests [--flows-file <path>] [--output <path>] [--base-url <url>]`
    - Parameters: --flows-file (optional, auto-detects latest FLW-flows-v*.md), --output (default tests/e2e/flow-navigation.spec.ts), --base-url (default http://localhost:3000)
    - Output: Playwright test file with `test('navigates X -> Y', ...)` blocks

    **commands/verify-handoff.md:**
    - Title: `/pde:verify-handoff`
    - Description: Verify implementation matches the handoff spec by comparing component APIs and TypeScript interfaces in source files against HANDOFF-SPEC.md. Produces a gap report.
    - Usage: `/pde:verify-handoff [--spec <path>] [--src-dir <path>]`
    - CLI: `node bin/pde-tools.cjs utils verify-handoff [--spec <path>] [--src-dir <path>]`
    - Parameters: --spec (optional, auto-detects latest HND-handoff-spec-v*.md), --src-dir (default src/)
    - Output: Markdown gap report table: Component | Spec Status | Code Status | Gap Type (missing/diverged/matched). JSON stats with total/matched/missing/diverged counts.

    **UTL-04 verification (per D-04):** Confirm that `commands/visual-diff.md` already documents the `pde-tools.cjs image diff` delegation path. If the file does not already contain a reference to `pde-tools.cjs image diff`, add a CLI section: `node bin/pde-tools.cjs image diff --base <branch-a> --compare <branch-b>`. This satisfies UTL-04 without any new module.
  </action>
  <verify>
    <automated>ls commands/render-mermaid.md commands/validate-tokens.md commands/gen-tests.md commands/verify-handoff.md && grep -q "pde-tools.cjs image diff" commands/visual-diff.md && echo "PASS: all skill files exist and UTL-04 confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - test -f commands/render-mermaid.md
    - test -f commands/validate-tokens.md
    - test -f commands/gen-tests.md
    - test -f commands/verify-handoff.md
    - grep -q "pde-tools.cjs utils render-mermaid" commands/render-mermaid.md
    - grep -q "pde-tools.cjs utils validate-tokens" commands/validate-tokens.md
    - grep -q "pde-tools.cjs utils gen-tests" commands/gen-tests.md
    - grep -q "pde-tools.cjs utils verify-handoff" commands/verify-handoff.md
    - grep -q "pde-tools.cjs image diff" commands/visual-diff.md
    - grep -q "OKLCH" commands/validate-tokens.md
    - grep -q "APCA" commands/validate-tokens.md
    - grep -q "mmdr" commands/render-mermaid.md
    - grep -q "Playwright" commands/gen-tests.md
    - grep -q "gap" commands/verify-handoff.md
  </acceptance_criteria>
  <done>Four skill files created in commands/ directory. Each documents usage, CLI command, parameters, and output. commands/visual-diff.md confirmed to reference `pde-tools.cjs image diff` delegation (UTL-04). All eight UTL requirements have corresponding command surface.</done>
</task>

</tasks>

<verification>
grep -q "case 'utils'" bin/pde-tools.cjs
node bin/pde-tools.cjs utils 2>&1 | grep -q "render-mermaid"
ls commands/render-mermaid.md commands/validate-tokens.md commands/gen-tests.md commands/verify-handoff.md
grep -q "pde-tools.cjs image diff" commands/visual-diff.md
npx vitest run tests/phase-170/
</verification>

<success_criteria>
- pde-tools.cjs routes `utils render-mermaid`, `utils validate-tokens`, `utils gen-tests`, `utils verify-handoff` to correct modules
- Four skill files exist in commands/ with complete documentation
- commands/visual-diff.md confirms UTL-04 delegation to `pde-tools.cjs image diff`
- All Phase 170 tests pass: npx vitest run tests/phase-170/
- All eight UTL requirements (UTL-01 through UTL-08) have corresponding /pde: commands
</success_criteria>

<output>
After completion, create `.planning/phases/170-pde-utilities/170-03-SUMMARY.md`
</output>
