import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

/**
 * Registers pipeline-related MCP tools on the given server.
 *
 * This is a stub — Plan 03 (RMT-06) fills in start_pipeline_run,
 * check_pipeline_run, and related tools.
 */
export function registerPipelineTools(_server: McpServer): void {
  // Plan 03 will register tools here
}
